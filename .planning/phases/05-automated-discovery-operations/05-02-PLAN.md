---
phase: 05-automated-discovery-operations
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/github_search/page_one.json
  - tests/fixtures/github_search/page_duplicates.json
  - tests/fixtures/github_search/page_incomplete.json
  - tests/fixtures/github_search/error_matrix.json
  - tests/recorded_transport.py
  - tests/test_github_search.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-03]
must_haves:
  truths:
    - "Recorded fixtures define every Search boundary before production code."
    - "One serial request has exact method/path/query/headers and yields only strict projected facts."
    - "Hostile prose, Link, oversized/malformed bodies and rate errors fail or project without leakage."
  artifacts:
    - {path: tests/fixtures/github_search, provides: "Bounded deterministic Search provider corpus"}
    - {path: tests/test_github_search.py, provides: "Failing Search adapter specification"}
  key_links:
    - {from: tests/recorded_transport.py, to: tests/fixtures/github_search, via: "MockTransport route table with unrecorded-request failure"}
  prohibitions:
    - "Fixtures contain no live credential and no copied complete repository body."
    - "Tests never follow an arbitrary Link URL or trust Search qualifiers as admission."
---

<objective>
Create the Wave 0 recorded Search corpus and failing boundary tests.

Purpose: Lock external API behavior, hostile inputs and DISC-03 telemetry before extending the existing GitHub client.
Output: Deterministic fixtures and RED tests consumed by Plan 05-04.

Locked decisions: D-02 numeric-ID dedup; D-08 allowlisted telemetry only.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/github.py
@tests/recorded_transport.py
@tests/test_github_adapter.py
@.planning/phases/05-automated-discovery-operations/05-PATTERNS.md
</context>

<feature>
  <name>Recorded GitHub repository Search boundary</name>
  <files>tests/fixtures/github_search/*, tests/recorded_transport.py, tests/test_github_search.py</files>
  <behavior>
    Cases: one page and multiple pages; within/across-query duplicate; rename with same numeric ID; incomplete results; invalid public/fork/archive facts; hostile discarded prose and Link; malformed/oversized payload; 403/429/5xx; missing/malformed rate headers.
  </behavior>
  <implementation>Write RED tests and fixtures only. Extend the recorder with a dedicated Search fixture loader while preserving exact unrecorded-request failure. Assert fixed API host/version, query-owned parameters, per_page/page bounds, serial call count, Authorization-header-only token, integer next cursor, response cap, rate telemetry and sanitized errors. Plan 05-04 turns these tests GREEN.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-02-01: Record bounded Search happy and dedup observations</name>
  <files>tests/fixtures/github_search/page_one.json, tests/fixtures/github_search/page_duplicates.json, tests/fixtures/github_search/page_incomplete.json, tests/recorded_transport.py, tests/test_github_search.py</files>
  <read_first>tests/recorded_transport.py, tests/test_github_adapter.py, src/skillscout/adapters/github.py, .planning/phases/05-automated-discovery-operations/05-RESEARCH.md</read_first>
  <action>RED: add fixtures and tests for exact first/next pages, cross-query duplicates, rename with stable numeric ID, partial results, strict item projection and exact query/page/rate provenance. Include hostile description/topic/text-match canaries and assert they never appear in returned objects, recorder summaries or durable candidate projections. Extend only the test recorder loader; every unrecorded route remains an assertion failure. Until Plan 05-04 implements the adapter, mark only the named adapter-behavior nodes `xfail(strict=True, reason="phase5-wave0-search-adapter-missing")`; fixture/parser/recorder tests remain ordinary passing tests. A syntax, import, collection, dependency, fixture or unrelated assertion failure must fail this task, and an unexpected adapter pass must XPASS-fail.</action>
  <acceptance_criteria>
    - Fixtures are bounded, deterministic and contain metadata only.
    - D-02 tests prove duplicate/rename decisions are keyed solely by numeric repository ID.
    - The suite fails only because the production Search method/contracts do not yet exist.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_github_search.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q -rxX tests/test_github_search.py -k "page or duplicate or incomplete" -x</automated></verify>
  <done>Happy, pagination and dedup Search behavior is frozen as RED tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-02-02: Record hostile pagination, caps and rate failures</name>
  <files>tests/fixtures/github_search/error_matrix.json, tests/test_github_search.py</files>
  <read_first>src/skillscout/adapters/github.py, src/skillscout/adapters/github_publish.py, tests/test_github_adapter.py, tests/test_publication_security.py</read_first>
  <action>RED: add matrix tests for wrong host/path/query in Link, fragments/userinfo, noninteger or out-of-policy page, redirect, malformed JSON, oversized body, 403 rate exhaustion, 429, 5xx, transport failure and missing/malformed mandatory rate facts. Require bounded sleep/defer evidence, one serial request at a time, fixed closed failure codes, and token confinement to Authorization. Do not encode raw provider error bodies into expected diagnostics per D-08. Mark only the named missing-adapter behavior nodes with the same strict Wave 0 reason; keep fixture integrity, static imports and recorder routing unmarked so any unrelated defect fails immediately.</action>
  <acceptance_criteria>
    - Every hostile route has a deterministic expected closed result.
    - No test authorizes arbitrary URL following, parallel Search or provider-body persistence.
    - The RED suite names each missing production behavior unambiguously.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_github_search.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q -rxX tests/test_github_search.py -k "hostile or oversized or rate or error" -x</automated></verify>
  <done>Search abuse, rate and resource boundaries are frozen before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Search HTTP → strict observation | Entire provider envelope is untrusted. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04 | Spoofing | pagination Link | high | mitigate | Fixed host/path/query and integer-cursor tests. |
| T-05-05 | Information Disclosure | Search prose/errors | high | mitigate | Canary discard and sanitized diagnostic tests. |
| T-05-06 | Denial of Service | body/rate behavior | high | mitigate | Caps, serial requests and bounded defer tests. |
| T-05-SC | Tampering | dependencies | high | mitigate | Fixtures and tests only; no install. |
</threat_model>

## Artifacts and symbols produced

Search fixture loader, recorded page/error corpus, and the complete failing `GitHubReadClient.search_repositories` specification.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Safe bounded Search | 05-02 | COVERED |
| REQ | DISC-01 | Exact query execution | 05-02 | COVERED |
| REQ | DISC-02 | Numeric-ID dedup evidence | 05-02 | COVERED |
| REQ | DISC-03 | Page/rate/source facts | 05-02 | COVERED |
| REQ | OPS-03 | Prose/error/secret exclusion | 05-02 | COVERED |
| RESEARCH | — | Complete Search scenario matrix | 05-02 | COVERED |
| CONTEXT | D-02,D-08 | Approved defaults | 05-02 | COVERED |

<verification>
The focused test command must fail for missing production behavior, not syntax, fixture or collection errors.
</verification>

<success_criteria>
Plan 05-04 can implement Search against a complete offline provider contract without discovering new requirements.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-02-SUMMARY.md`.</output>
