---
phase: 05-automated-discovery-operations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/discovery-queries-v1.json
  - src/skillscout/domain/discovery.py
  - tests/test_discovery_domain.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "The reviewed query set and every durable discovery fact have strict, versioned, content-addressed contracts with exact three-database path ownership."
    - "Discovery and semantic limits are literal hard ceilings of 100 and 20 with no caller override."
    - "Durable contracts cannot represent raw repository prose, arbitrary headers, secrets, raw errors, or unvalidated paths."
  artifacts:
    - {path: config/discovery-queries-v1.json, provides: "Exact ordered four-query policy"}
    - {path: src/skillscout/domain/discovery.py, provides: "Discovery authorities, observations, reservations, terminals, state objects and roots", exports: [DiscoveryQuerySetV1, DiscoveryBudgetPolicyV1, DiscoveryRunAuthorityV1, SearchPageObservationV1, DiscoveredCandidateV1, DiscoveryReservationV1, DiscoveryStateRootV1]}
    - {path: tests/test_discovery_domain.py, provides: "Mutation and boundary proof for all Phase 5 contracts"}
  key_links:
    - {from: config/discovery-queries-v1.json, to: DiscoveryQuerySetV1, via: "strict parse and canonical digest"}
    - {from: DiscoveryRunAuthorityV1, to: DiscoveryReservationV1, via: "query/budget/provider/profile authority binding"}
  prohibitions:
    - "No runtime query text, budget, ordering, cursor-host, or state-branch override is representable."
    - "No mutable repository name is deduplication authority; numeric repository ID is the key."
    - "No state-object pruning policy is introduced in Phase 5."
---

<objective>
Freeze the complete Phase 5 contract and policy surface before adapters or orchestration.

Purpose: Make discovery, cost, audit, rebuild and sanitization invariants executable and impossible to widen through runtime input.
Output: Reviewed query policy, strict domain contracts, and mutation-tested contract suite.

Locked decisions: D-02 numeric repository-ID dedup; D-03 durable 100/20 reservations; D-05 complete trimmed JSON plus SQLite authority; D-08 allowlisted persistence only; D-09 no pruning.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automated-discovery-operations/05-RESEARCH.md
@.planning/phases/05-automated-discovery-operations/05-PATTERNS.md
@src/skillscout/domain/models.py
@src/skillscout/domain/canonical.py

<interfaces>
Use `StrictFrozenModel` from `domain.models`, and `canonical_json_bytes` / `sha256_digest` from `domain.canonical`. Every self-hashed contract excludes only its own digest field and rejects extras.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-01-01: Freeze exact query and budget authority</name>
  <files>config/discovery-queries-v1.json, src/skillscout/domain/discovery.py, tests/test_discovery_domain.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/application/phase3.py, .planning/phases/05-automated-discovery-operations/05-RESEARCH.md</read_first>
  <behavior>
    - The query set is `github-repository-search-v1`, four ordered approved queries, per_page 25, four pages/query, round-robin, sort updated, order desc.
    - The budget policy admits at most 100 first-seen numeric repository IDs and 20 semantic repository reservations.
    - Any field, order, query text, policy value, digest, or ceiling mutation fails strict validation or changes authority.
  </behavior>
  <action>RED: write exact-policy, extra-field, order, digest and 99/100/101 plus 19/20/21 boundary tests. GREEN: create the reviewed JSON and `DiscoveryQueryV1`, `DiscoveryQuerySetV1`, `DiscoveryBudgetPolicyV1`, and `DiscoveryRunAuthorityV1`. Bind generated run ID, query-set digest, budget digest, Phase 2/3 profiles, semantic provider/model identities, and initial state-root digest; keep changing checkpoint heads outside stable run identity. Enforce D-02 and D-03 as code-owned constants with no constructor or CLI widening seam.</action>
  <acceptance_criteria>
    - Exact approved bytes parse and produce stable canonical digests.
    - Query-set or authority mutation is a clean mismatch; malformed authority fails closed.
    - The 101st discovery selection and 21st semantic reservation are structurally unrepresentable as admitted ordinals.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_domain.py -k "query or budget or authority" -x</automated></verify>
  <done>DISC-01 and DISC-02 have immutable, versioned authority contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-01-02: Freeze observations, terminals and rebuild facts</name>
  <files>src/skillscout/domain/discovery.py, tests/test_discovery_domain.py</files>
  <read_first>src/skillscout/domain/candidate_authority.py, src/skillscout/domain/publication.py, .planning/phases/05-automated-discovery-operations/05-RESEARCH.md</read_first>
  <behavior>
    - Page facts bind exact query text/version/digest, cursor, counts, incomplete flag, request ID and numeric rate facts.
    - Candidate facts retain numeric ID, validated owner/name and first-seen/duplicate provenance without Search prose.
    - Terminal and state-root vocabularies separate business outcomes, confirmed transient states, unknown semantic outcomes, integrity conflicts and permanent failures.
  </behavior>
  <action>RED: mutation-test every closed field, self-hash, sequence, exact path owner and forbidden-field attempt. GREEN: add strict `SearchRateLimitFactsV1`, `SearchPageObservationV1`, `SearchRepositoryObservationV1`, `DiscoveredCandidateV1`, discovery/semantic reservation, candidate terminal, run summary, immutable state object locator and root contracts. Per D-04 encode outcome-unknown semantic attempts as quarantined/manual and distinct from confirmed-retryable failures. Per D-05/D-08, root contracts allow exactly `state/root.json`, content objects below `state/objects/sha256/{first-two-hex}/{64-hex}.json`, and owned DBs `state/databases/pipeline.sqlite3`, `state/databases/operations.sqlite3`, `state/databases/publication.sqlite3`; bind complete rebuild projections/digests using only allowlisted IDs, hashes, bounded telemetry and validated locators. The root has prior-root reachability but no pruning operation.</action>
  <acceptance_criteria>
    - DISC-03 facts are complete and canonically ordered.
    - Raw Search descriptions, topics, text matches, Link values, provider bodies, arbitrary headers/errors, source bodies and secret-shaped fields cannot enter a durable model.
    - Root/object hash, locator, ordinal or projection mutation fails validation.
    - Swapped database path/owner or any fourth database path fails validation.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_domain.py -x</automated></verify>
  <done>The full trimmed audit/rebuild vocabulary is strict, closed and content addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reviewed policy → runtime authority | Runtime must not widen query or cost policy. |
| GitHub/LLM facts → durable contracts | Provider-controlled fields are untrusted and aggressively projected. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | query/budget authority | high | mitigate | Exact JSON, strict models, self-digests and mutation tests. |
| T-05-02 | Repudiation | reservations/outcomes | high | mitigate | Closed ordinals, non-refundable reservation contracts and distinct outcome taxonomy. |
| T-05-03 | Information Disclosure | durable models | high | mitigate | Field allowlists and negative construction tests per D-08. |
| T-05-SC | Tampering | dependencies | high | mitigate | No package changes; locked local uv only. |
</threat_model>

## Artifacts and symbols produced

`DiscoveryQuerySetV1`, `DiscoveryBudgetPolicyV1`, `DiscoveryRunAuthorityV1`, page/rate/candidate/reservation/terminal/root contracts and exact query JSON.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Bounded auditable discovery authority | 05-01 | COVERED |
| REQ | DISC-01 | Versioned query set | 05-01 | COVERED |
| REQ | DISC-02 | 100/20 hard ceilings | 05-01 | COVERED |
| REQ | DISC-03 | Complete discovery facts | 05-01 | COVERED |
| REQ | OPS-02 | JSON/SQLite rebuild contracts | 05-01 | COVERED |
| REQ | OPS-03 | Closed durable fields | 05-01 | COVERED |
| RESEARCH | — | Four-query defaults, outcome taxonomy, no pruning | 05-01 | COVERED |
| CONTEXT | D-02,D-03,D-04,D-05,D-08,D-09 | Approved defaults supplied in planning request | 05-01 | COVERED |

<verification>
Run the complete domain suite and Ruff on the new files; no network, credentials, dependency mutation or implementation outside the listed files.
</verification>

<success_criteria>
Every later Phase 5 plan can import exact contracts without inventing policy or persistence fields.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-01-SUMMARY.md`.</output>
