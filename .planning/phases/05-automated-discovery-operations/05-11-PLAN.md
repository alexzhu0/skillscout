---
phase: 05-automated-discovery-operations
plan: "11"
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - src/skillscout/adapters/semantic_provider.py
  - src/skillscout/adapters/openai_extract.py
  - src/skillscout/adapters/openai_generate.py
  - src/skillscout/adapters/openai_review.py
  - tests/test_semantic_provider.py
  - tests/test_openai_extract.py
  - tests/test_openai_generate.py
  - tests/test_openai_review.py
autonomous: true
requirements: [DISC-02, OPS-03]
must_haves:
  truths:
    - "Every OpenAI and DeepSeek semantic adapter returns or raises one closed transport disposition: confirmed_retryable, semantic_outcome_unknown, permanent, or decided semantic result."
    - "Timeout, connection loss, ambiguous 5xx and any post-send indeterminate failure are always outcome-unknown, never retryable."
    - "Only deterministic provider evidence that semantic processing did not produce a result can be confirmed-retryable."
    - "Every adapter invocation still performs exactly one SDK/HTTP request with SDK retries disabled."
  artifacts:
    - {path: src/skillscout/adapters/semantic_provider.py, provides: "Provider-neutral semantic transport disposition", exports: [SemanticTransportDisposition, SemanticProviderFailure]}
    - {path: src/skillscout/adapters/openai_extract.py, provides: "Classified Extractor transport boundary"}
    - {path: src/skillscout/adapters/openai_generate.py, provides: "Classified Generator transport boundary"}
    - {path: src/skillscout/adapters/openai_review.py, provides: "Classified Reviewer transport boundary"}
  key_links:
    - {from: OpenAI and DeepSeek SDK exceptions/responses, to: SemanticProviderFailure, via: "one shared closed classifier with request-sent/response evidence"}
  prohibitions:
    - "No timeout, connection loss, ambiguous 5xx or post-send failure maps to the retryable class."
    - "No provider-specific free-form error, response body, header or exception text crosses the adapter boundary."
---

<objective>
Split semantic transport failures into provably retryable versus outcome-unknown across every provider and semantic stage.

Purpose: Prevent an ambiguous OpenAI or DeepSeek request from being automatically replayed while preserving bounded retry only when no semantic result could have been produced.
Output: Shared closed classification, updated Extractor/Generator/Reviewer adapters and full provider mutation tests.

Locked decision: D-04 outcome-unknown requests consume their attempt and require quarantine/manual retry.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/skillscout/adapters/semantic_provider.py
@src/skillscout/adapters/openai_extract.py
@src/skillscout/adapters/openai_generate.py
@src/skillscout/adapters/openai_review.py
@tests/test_semantic_provider.py
@tests/test_openai_extract.py
@tests/test_openai_generate.py
@tests/test_openai_review.py
@.planning/phases/05-automated-discovery-operations/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-11-01: Define provider-neutral semantic transport disposition</name>
  <files>src/skillscout/adapters/semantic_provider.py, tests/test_semantic_provider.py</files>
  <read_first>src/skillscout/adapters/semantic_provider.py, tests/test_semantic_provider.py, AGENTS.md, .planning/phases/05-automated-discovery-operations/05-RESEARCH.md</read_first>
  <behavior>
    - OpenAI Responses and DeepSeek Chat Completions share the same closed four-way transport/result classification.
    - Request rejection proven before semantic processing may be confirmed-retryable; ambiguous delivery/processing is unknown.
    - Unknown and permanent failures expose closed codes plus bounded request ID/telemetry only.
  </behavior>
  <action>RED: mutation-test rate-limit/rejection evidence, pre-send validation/auth, timeout, connection loss, ambiguous 5xx, malformed response and post-send parsing/durability boundaries for both configured providers. GREEN: add strict `SemanticTransportDisposition` and `SemanticProviderFailure` in the existing provider boundary. Define an exhaustive classifier based only on deterministic typed provider facts; default ambiguity to `semantic_outcome_unknown`. Keep both SDK clients at `max_retries=0`, no tools and existing `store=false` behavior. Never use raw message text to authorize retry.</action>
  <acceptance_criteria>
    - Every typed OpenAI/DeepSeek failure has exactly one disposition.
    - Timeout/connection/ambiguous 5xx mutations always remain outcome-unknown.
    - No raw error/body/header/credential appears in the classified object or diagnostics.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_provider.py -x</automated></verify>
  <done>All semantic transports share one fail-closed retry/unknown vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-11-02: Apply classification to Extractor, Generator and Reviewer</name>
  <files>src/skillscout/adapters/openai_extract.py, src/skillscout/adapters/openai_generate.py, src/skillscout/adapters/openai_review.py, tests/test_openai_extract.py, tests/test_openai_generate.py, tests/test_openai_review.py</files>
  <read_first>src/skillscout/adapters/semantic_provider.py, src/skillscout/adapters/openai_extract.py, src/skillscout/adapters/openai_generate.py, src/skillscout/adapters/openai_review.py, tests/test_openai_extract.py, tests/test_openai_generate.py, tests/test_openai_review.py</read_first>
  <behavior>
    - Each semantic adapter makes exactly one provider request and projects its transport disposition without collapsing unknown into transient.
    - Decided refusal/incomplete/schema/business outcomes remain successful closed semantic results and are never retried.
    - DeepSeek compatibility execution through each semantic stage uses identical local classification and strict schema validation.
  </behavior>
  <action>RED: extend all three adapter suites across OpenAI and DeepSeek paths with exact one-request counts for confirmed retryable, unknown, permanent and decided outcomes. GREEN: route typed SDK/compatibility failures through Plan 05-11-01 and return/raise the closed failure carrier without converting unknown to the legacy transient code. Preserve strict response models, no tools, `store=false` where supported, local DeepSeek JSON validation and bounded telemetry. Refusal/incomplete/schema failures stay decided results; malformed/extra DeepSeek JSON fails closed and is not provider-validated structured output.</action>
  <acceptance_criteria>
    - Extractor, Generator and Reviewer each prove the complete classification matrix for OpenAI and DeepSeek.
    - Unknown cases have exactly one recorded request and no adapter retry.
    - Existing happy/refusal/schema/request-shape security tests remain green.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_provider.py tests/test_openai_extract.py tests/test_openai_generate.py tests/test_openai_review.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/semantic_provider.py src/skillscout/adapters/openai_extract.py src/skillscout/adapters/openai_generate.py src/skillscout/adapters/openai_review.py</automated></verify>
  <done>All semantic stage adapters preserve the closed retry/unknown distinction across both providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider transport → retry authority | Ambiguous delivery must not authorize a second paid semantic request. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-37 | Repudiation | ambiguous semantic request | high | mitigate | Closed unknown class, exactly one request, no adapter retry. |
| T-05-38 | Denial of Service | retry amplification | high | mitigate | Confirmed-only retry evidence and SDK retries zero. |
| T-05-39 | Information Disclosure | provider errors | high | mitigate | Closed codes and bounded telemetry only. |
| T-05-SC | Tampering | SDK behavior | high | mitigate | Locked dependencies and request-count tests. |
</threat_model>

## Artifacts and symbols produced

`SemanticTransportDisposition`, `SemanticProviderFailure`, shared classifier and classified OpenAI/DeepSeek Extractor/Generator/Reviewer adapters.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Duplicate-safe resumable semantic operation | 05-11 | COVERED |
| REQ | DISC-02 | LLM budget cannot expand through ambiguous replay | 05-11 | COVERED |
| REQ | OPS-03 | Sanitized provider failure facts | 05-11 | COVERED |
| RESEARCH | — | Resolved retryable vs unknown classification | 05-11 | COVERED |
| CONTEXT | D-04 | Unknown outcome quarantines/manual | 05-11 | COVERED |

<verification>
Run all provider and semantic adapter suites plus Ruff; no live provider call.
</verification>

<success_criteria>
No semantic adapter can turn an ambiguous OpenAI or DeepSeek effect into automatic retry authority.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-11-SUMMARY.md`.</output>
