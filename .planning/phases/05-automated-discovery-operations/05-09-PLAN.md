---
phase: 05-automated-discovery-operations
plan: "09"
type: execute
wave: 8
depends_on: [05-08]
files_modified:
  - .github/workflows/discover.yml
  - tests/test_discovery_workflow.py
  - tests/test_discovery_security.py
autonomous: false
requirements: [DISC-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "One workflow supports daily schedule and workflow_dispatch through the same fixed discovery path."
    - "All production runs share `skillscout-production`, never cancel an active run, and use hosted-validated queue behavior only."
    - "Discovery/state job invokes only the unprotected discovery entry point and has no Phase 4 factory or catalog credentials."
    - "Protected job invokes only the protected publication entry point, re-reads the exact state commit and re-admits locally before late catalog token minting and PublicationApplication construction."
    - "Immutable action pins, minimum permissions and fixed shell prevent candidate injection and secret/state leakage."
    - "Phase 5 hosted concurrency evidence is separate from Gate B4; the new discover workflow receives publication credit only after a Gate B4 run bound to its exact protected workflow bytes and identities."
  artifacts:
    - {path: .github/workflows/discover.yml, provides: "Scheduled/manual two-zone production workflow"}
    - {path: tests/test_discovery_workflow.py, provides: "Static/mutation workflow contract"}
  key_links:
    - {from: discovery entry point/job, to: protected publication entry point/job, via: "bounded eligible locators/authorities, root digest and exact state-head SHA only"}
    - {from: protected exact-commit reread and local re-admission, to: catalog token action and PublicationApplication construction, via: "strict ordering before credential release or publisher construction"}
    - {from: Gate B4 exact workflow/identity evidence, to: protected publication credit, via: "existing publish workflow baseline plus fresh exact discover-workflow Gate B4 before new production credit"}
  prohibitions:
    - "No catalog secret/variable in discovery/state job, cache/artifact authority, mutable action, candidate shell expression, automatic merge/approve/ready or broad GITHUB_TOKEN write."
    - "No Phase 5 concurrency canary substitutes for or refreshes Gate B4."
---

<objective>
Deliver one serialized scheduled/manual workflow with two strictly separated credential zones, then obtain hosted-platform evidence.

Purpose: Satisfy DISC-01/OPS-02 production operation and OPS-03 credential/data boundaries on ephemeral runners.
Output: Pinned workflow, exhaustive static tests and one blocking hosted verification record.

Locked decisions: D-06 CAS conflict stop; D-07 fixed shared non-cancelling workflow and catalog credential isolation; D-08 allowlisted handoff.

Publication authority: Gate B4 passed only for existing protected publication workflow SHA-256 `224c843ad1211bd3fa250e055e4040417d58bb5ecd837ed0fd8f148af6c0ca8c` and the reviewed App/catalog/ruleset/environment/reviewer/installation identities. The new `discover.yml` is a distinct protected workflow surface, so it requires its own fresh Gate B4 bound to exact bytes and identities before its publication job receives production credit. The existing Gate B4 remains baseline evidence for the unchanged Phase 4 application/workflow only; Phase 5 concurrency evidence cannot extend it.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.github/workflows/publish-candidate.yml
@src/skillscout/cli.py
@tests/test_publication_security.py
@tests/test_discovery_workflow.py
@tests/test_discovery_security.py
@AGENTS.md
@RELEASE.md
@.planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-09-01: Implement pinned daily/manual two-zone workflow</name>
  <files>.github/workflows/discover.yml, tests/test_discovery_workflow.py, tests/test_discovery_security.py</files>
  <read_first>.github/workflows/publish-candidate.yml, src/skillscout/cli.py, tests/test_publication_security.py, tests/test_discovery_workflow.py, tests/test_discovery_security.py</read_first>
  <behavior>
    - Triggers are cron `17 3 * * *` and `workflow_dispatch`; concurrency group is exactly `skillscout-production`; cancel-in-progress is false.
    - Discovery job restores/verifies state, runs through Review, fast-forwards state and emits only bounded run/root/head values.
    - Protected publication job checks out same code, invokes the separate protected entry point with the exact state commit and bounded locators, re-derives admissions, then mints token, constructs PublicationApplication and invokes it sequentially.
  </behavior>
  <action>Turn Wave 0 workflow/security tests GREEN. Treat the existing Gate-B4-bound publication workflow SHA-256 `224c843ad1211bd3fa250e055e4040417d58bb5ecd837ed0fd8f148af6c0ca8c` as a baseline analog only; do not claim it authorizes the new workflow. Copy the exact immutable Action identities, `persist-credentials:false`, locked local uv, explicit safe shell and minimum permissions. Implement one workflow whose discovery/state job invokes only the unprotected discovery command from Plan 05-08, receives source/state authority only, and emits only bounded eligible locators/authorities plus root digest and exact state commit. Its protected job invokes only the distinct protected publication command, checks out and re-reads that exact commit, verifies/rebuilds all stores and re-derives canonical admissions before its catalog environment can mint a token or construct `PublicationApplication`. Add static/mutation assertions for distinct command names, no discovery import/reference to publication construction, exact handoff allowlist, and admission→token→construction→invocation ordering. Use fixed shell with validated environment bridges—never candidate interpolation. Use `queue: max` only if repository parser/audit and hosted control plane accept it; otherwise omit it while retaining the fixed group/non-cancel behavior and explicitly record pending-run replacement semantics per D-07. Before production publication credit, require a fresh Gate B4 bound to the exact `discover.yml` digest and reviewed App scope, catalog, ruleset, environment, reviewers and installation identity. Any later byte or identity change invalidates that new evidence. Phase 5 concurrency evidence proves only scheduling/zone behavior and never substitutes for either Gate B4 record.</action>
  <acceptance_criteria>
    - Exact trigger/group/non-cancel/action/permissions/job-order tests pass.
    - Discovery job contains no catalog App secret/token or protected catalog/reviewer variables.
    - Discovery invokes no protected publication entry point and cannot construct a Phase 4 application or remote publisher.
    - Protected job rejects any exact-state or admission mismatch before token minting and publisher construction.
    - Cache/artifact is not canonical state; all state persistence uses Plan 05-06 CAS.
    - No candidate value appears in shell expression or unbounded job output.
    - Existing Phase 4 Gate B4 baseline remains exact and unchanged; the new discover workflow is blocked from publication credit until its own fresh exact Gate B4 passes.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_workflow.py tests/test_discovery_security.py -x</automated></verify>
  <done>The production workflow is statically proven serialized, pinned, least-privilege and zone-separated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-09-02: Verify hosted concurrency and protected token ordering</name>
  <files>.github/workflows/discover.yml, tests/test_discovery_workflow.py</files>
  <read_first>.github/workflows/discover.yml, .planning/phases/05-automated-discovery-operations/05-VALIDATION.md, .planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md</read_first>
  <action>After the automated workflow suite passes, run two separately authorized, separately recorded hosted checks. First, run the bounded concurrency canary: trigger overlapping manual/scheduled-equivalent runs without candidate-controlled input and confirm non-cancel/queue/state-conflict/credential-zone behavior. Second, run Gate B4 for the exact new `discover.yml` protected publication surface and reviewed App/catalog/ruleset/environment/reviewer/installation identities; prove positive Draft/reviewer behavior plus causal default/merge/admin/unauthorized denials and separate cleanup exactly as Phase 4 required. The concurrency canary cannot satisfy the Gate B4 check. Also verify the old `publish-candidate.yml` Gate B4 hash remains unchanged. Record only stable identities/digests, closed outcomes and human approvals; never secrets, headers or environment dumps.</action>
  <acceptance_criteria>
    - Hosted run proves the exact checked-in concurrency behavior; unsupported queue syntax is removed and fallback semantics are documented before approval.
    - Protected token is absent from discovery/state and minted only after exact re-admission.
    - No raw source or secret canary appears in hosted logs/outputs/state tree.
    - Existing Phase 4 Gate B4 remains valid only for its exact old workflow; a separate fresh Gate B4 passes for exact `discover.yml` before new publication credit.
    - Concurrency evidence claims no publication-denial authority and cannot satisfy the fresh Gate B4.
  </acceptance_criteria>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_workflow.py tests/test_discovery_security.py -x</automated>
    <human-check>Review the bounded hosted run evidence for concurrency, authority-zone separation, token ordering and sanitized outputs.</human-check>
  </verify>
  <done>Hosted concurrency and fresh Gate B4 facts are separately human-reviewed and bound to their exact workflow bytes/identities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Trigger/concurrency control plane → running job | Hosted behavior cannot be established by local parser alone. |
| Discovery job → protected publication job | Credential and candidate authority must not flow backward. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-29 | Denial of Service | concurrent runs | high | mitigate | Fixed group, non-cancel and hosted evidence. |
| T-05-30 | Elevation | catalog token timing | high | mitigate | Job split, protected environment, pre-token re-admission and exact Gate B4 equality. |
| T-05-31 | Injection | workflow shell | high | mitigate | Fixed run blocks and static expression tests. |
| T-05-32 | Information Disclosure | logs/outputs | high | mitigate | Bounded outputs and hosted canary scan. |
| T-05-47 | Spoofing | Gate B4 substitution | high | mitigate | Fresh Gate B4 for exact discover workflow/identities; concurrency canary explicitly non-substituting. |
| T-05-SC | Tampering | Actions | high | mitigate | Existing human-approved full commit SHAs. |
</threat_model>

## Artifacts and symbols produced

`.github/workflows/discover.yml`, workflow audit tests and exact hosted verification record referenced by the summary.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Daily/manual ephemeral-runner operations | 05-09 | COVERED |
| REQ | DISC-01 | Schedule + manual trigger | 05-09 | COVERED |
| REQ | OPS-02 | Shared concurrency and state branch | 05-09 | COVERED |
| REQ | OPS-03 | Credential/data separation | 05-09 | COVERED |
| RESEARCH | — | Queue grammar uncertainty and two authority zones | 05-09 | COVERED |
| CONTEXT | D-06,D-07,D-08 | Approved defaults | 05-09 | COVERED |

<verification>
Static/mutation suites must pass before the single hosted human gate. Hosted evidence is required only for control-plane facts that local tests cannot prove.
</verification>

<success_criteria>
The exact workflow bytes are locally audited and hosted-confirmed without exposing catalog authority to discovery.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-09-SUMMARY.md` with hosted evidence locator/digest and approval signal.</output>
