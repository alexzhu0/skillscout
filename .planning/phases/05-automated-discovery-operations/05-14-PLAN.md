---
phase: 05-automated-discovery-operations
plan: "14"
type: execute
wave: 4
depends_on: [05-06, 05-11, 05-12]
files_modified:
  - src/skillscout/application/ports.py
  - src/skillscout/adapters/state_branch.py
  - tests/test_semantic_durability.py
autonomous: true
requirements: [DISC-02, OPS-02, OPS-03]
must_haves:
  truths:
    - "One explicit ThreeStoreDurabilityBarrier exports pipeline, operations and publication stores through their owning APIs and advances the fixed state branch."
    - "Barrier success returns only after the exact remote commit is reread and verified to bind all three database digests and the expected transition authority."
    - "Attempt-start must be remotely confirmed before a provider request; every decided, confirmed-retryable or outcome-unknown result must be remotely confirmed before retry or terminal."
    - "Barrier failure is a closed operational stop and cannot authorize a provider request, retry or terminal transition."
    - "Crash/restart immediately before or after either barrier resumes the same pending state without ambiguous semantic replay."
  artifacts:
    - {path: src/skillscout/application/ports.py, provides: "Narrow three-store remote durability port", exports: [ThreeStoreDurabilityBarrier, DurabilityReceipt]}
    - {path: src/skillscout/adapters/state_branch.py, provides: "State-branch implementation over store-owned canonical exports"}
    - {path: tests/test_semantic_durability.py, provides: "Three-stage/two-provider barrier ordering and crash matrix"}
  key_links:
    - {from: pipeline/operations/publication store exports, to: fixed state branch, via: "one canonical root and parent-bound CAS"}
    - {from: DurabilityReceipt, to: semantic request/retry authority, via: "exact remote head/root/transition acknowledgement"}
  prohibitions:
    - "No store schema duplication, local-only success receipt, best-effort sync, swallowed conflict, provider call before pre-request receipt, or retry before post-result receipt."
---

<objective>
Define and implement the explicit three-store remote durability barrier required by every semantic runner.

Purpose: Eliminate crash windows in which a provider request or retry could occur without the corresponding attempt/result being durably recoverable from the canonical state branch.
Output: Narrow barrier port, state-branch adapter and exhaustive ordering/crash/sync-failure tests.

Locked decisions: D-03 non-refundable semantic accounting, D-04 no ambiguous replay, D-06 parent-bound non-force state synchronization, and D-08 bounded diagnostics.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/application/ports.py
@src/skillscout/adapters/state.py
@src/skillscout/adapters/operations_state.py
@src/skillscout/adapters/publication_state.py
@src/skillscout/adapters/state_branch.py
@.planning/phases/05-automated-discovery-operations/05-06-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-11-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-14-01: Define remote-confirmed three-store barrier contract</name>
  <files>src/skillscout/application/ports.py, tests/test_semantic_durability.py</files>
  <read_first>src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/adapters/operations_state.py, src/skillscout/adapters/publication_state.py, tests/test_state_integrity.py, tests/test_state_branch.py</read_first>
  <behavior>
    - Barrier input identifies the exact run/candidate/workflow/stage/attempt, transition kind, expected prior state head and all three store-owned projections.
    - Barrier output is a closed receipt containing exact verified remote head, root digest and transition authority; absence/mismatch is failure.
    - Transition kinds distinguish attempt-start from decided, confirmed-retryable and outcome-unknown results without carrying raw provider text.
  </behavior>
  <action>RED: create strict contract tests for `ThreeStoreDurabilityBarrier` and `DurabilityReceipt`, including invalid stage/attempt/transition combinations, wrong parent/root/store digests, and sanitized failures. GREEN: define a narrow application port whose input references the three store-owned canonical export seams from Plan 05-12 and whose receipt proves the exact state-branch commit was advanced and reread. The contract must encode whether the caller is synchronizing `attempt_started`, `result_decided`, `result_confirmed_retryable`, or `result_outcome_unknown`; it must not expose provider payloads or implement any store schema. Model sync conflict, integrity mismatch, permission/configuration failure and missing receipt as closed operational failures that grant no request/retry/terminal authority.</action>
  <acceptance_criteria>
    - Invalid or incomplete receipts cannot satisfy request/retry authority.
    - Contract covers Extractor, Generator and Reviewer with provider-neutral transition identities.
    - No secret, raw repository text, exception string or provider response enters barrier input/output.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_durability.py -k "contract or receipt or transition or sanitize" -x</automated></verify>
  <done>The application has one explicit, closed port for proving all semantic attempt state remotely durable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-14-02: Implement and crash-test the state-branch barrier</name>
  <files>src/skillscout/adapters/state_branch.py, tests/test_semantic_durability.py</files>
  <read_first>src/skillscout/adapters/state_branch.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/adapters/operations_state.py, src/skillscout/adapters/publication_state.py, tests/test_state_branch.py, tests/test_state_integrity.py</read_first>
  <behavior>
    - One barrier call obtains canonical exports from all three owning stores, assembles the exact root, performs parent-bound non-force CAS, rereads remote commit/tree/root/objects, and only then returns a receipt.
    - Repeating the same already-confirmed transition is idempotent; divergent local/remote state, a head race, partial export, push/reread failure or integrity mismatch fails closed.
    - Test matrices cover both providers and Extractor/Generator/Reviewer crashes before and after pre-request/post-result barriers with exact zero-ambiguous-replay expectations.
  </behavior>
  <action>RED: add a table-driven barrier matrix for OpenAI and DeepSeek across Extractor, Generator and Reviewer. For each stage/provider, interrupt immediately before and after the attempt-start barrier and immediately before and after the decided/confirmed-retryable/outcome-unknown result barrier; also inject each export, CAS, reread and verification failure. Assert request/retry/terminal spies remain zero whenever their guarding receipt is absent and restart recognizes the same confirmed transition without creating a new attempt. GREEN: implement the port in `state_branch.py` by calling each store owner's Plan 05-12 export, assembling the exact three-database root/object tree, advancing with Plan 05-06 parent-bound CAS, then rereading and verifying the remote commit and projections before returning `DurabilityReceipt`. Treat exact already-present state as idempotent success only after full reread equality. Never force, merge, accept local-only durability or catch-and-continue.</action>
  <acceptance_criteria>
    - Every successful receipt proves exact remote equality for pipeline, operations and publication store projections.
    - Every injected sync failure blocks the guarded request/retry/terminal.
    - Across restart, all two-provider/three-stage crash cases have exact attempt/request counts and zero ambiguous replay.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_durability.py tests/test_state_branch.py tests/test_state_integrity.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/application/ports.py src/skillscout/adapters/state_branch.py tests/test_semantic_durability.py</automated></verify>
  <done>The fixed state branch is the acknowledged durability boundary for all semantic request and retry authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Three local stores → canonical state branch | Partial or stale snapshots must not grant semantic authority. |
| Remote durability receipt → provider request/retry | A forged or unverified receipt can cause duplicate ambiguous effects. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-50 | Repudiation | semantic attempt durability | high | mitigate | Exact transition identity and verified remote receipt. |
| T-05-51 | Tampering | three-store snapshot | high | mitigate | Store-owned exports, root binding and cross-store projection equality. |
| T-05-52 | Denial of Service | failed state sync | medium | accept | Fail closed and resume the same pending barrier; do not issue external semantic effects. |
| T-05-53 | Spoofing | durability receipt | high | mitigate | Reread exact remote commit/tree/root/objects before receipt. |
| T-05-SC | Tampering | dependencies | high | mitigate | No new package installation. |
</threat_model>

## Artifacts and symbols produced

`ThreeStoreDurabilityBarrier`, `DurabilityReceipt`, state-branch implementation and two-provider/three-stage interruption matrix.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Resume semantic work without ambiguous duplicate effects | 05-14 | COVERED |
| REQ | DISC-02 | Non-refundable semantic attempt accounting | 05-14 | COVERED |
| REQ | OPS-02 | Exact remote recovery across ephemeral runners | 05-14 | COVERED |
| REQ | OPS-03 | Bounded safe state and diagnostics | 05-14 | COVERED |
| RESEARCH | — | Pre-request and post-result remote durability barriers | 05-14 | COVERED |
| CONTEXT | D-03,D-04,D-06,D-08 | Budget, unknown outcome, CAS and sanitization | 05-14 | COVERED |

<verification>
Barrier contract, state-branch integrity and exhaustive crash/restart matrices pass offline under the locked toolchain.
</verification>

<success_criteria>
No Extractor, Generator or Reviewer request/retry/terminal can cross an unconfirmed three-store remote durability boundary.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-14-SUMMARY.md`.</output>
