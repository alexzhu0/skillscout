---
phase: 05-automated-discovery-operations
plan: "05"
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - src/skillscout/adapters/operations_state.py
  - tests/test_operations_state.py
autonomous: true
requirements: [DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "Discovery selection and semantic reservation are atomic, unique, durable, contiguous and never refunded."
    - "The discovery-only operations database is exact-schema, private, snapshot-serialized and fully verifiable against its canonical JSON facts."
    - "Valid operations JSON can rebuild missing/corrupt operations SQLite; disagreement or ambiguity fails closed."
  artifacts:
    - {path: src/skillscout/adapters/operations_state.py, provides: "Dedicated discovery-only ledger, canonical export/import and rebuild", exports: [OperationsStateStore]}
    - {path: tests/test_operations_state.py, provides: "Budget, crash, integrity and rebuild evidence"}
  key_links:
    - {from: OperationsStateStore.reserve_semantic_candidate, to: durable snapshot, via: "single BEGIN IMMEDIATE candidate transaction"}
    - {from: canonical JSON objects, to: OperationsStateStore.rebuild, via: "same validated insertion paths and exact projection equality"}
  prohibitions:
    - "No COUNT-then-INSERT race, refund/delete path, active SQLite copy, WAL/journal, physical-integrity-only authorization, or copied Phase 1/3/4 schema."
    - "No raw source, provider body, environment, header or arbitrary error/path field enters SQLite or JSON."
---

<objective>
Implement the durable multi-candidate operations ledger and its owned JSON rebuild authority.

Purpose: Make DISC-02 limits and OPS-02 recovery survive every retry, crash and business outcome.
Output: Exact discovery-only operations store with canonical export/import/rebuild and GREEN adversarial tests; Plan 05-12 integrates existing store-owned rebuild seams.

Locked decisions: D-02, D-03, D-04, D-05, D-08, D-09.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/localfs.py
@src/skillscout/adapters/state.py
@src/skillscout/adapters/publication_state.py
@src/skillscout/domain/discovery.py
@tests/test_operations_state.py
@.planning/phases/05-automated-discovery-operations/05-01-SUMMARY.md

<interfaces>
Copy `SQLiteStateStore._snapshot_transaction`: private `:memory:` candidate, `BEGIN IMMEDIATE`, serialize, anchored atomic write, promote only after durability. Reuse `AnchoredDirectory`; do not give SQLite an operator pathname.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-05-01: Implement exact schema and non-refundable reservations</name>
  <files>src/skillscout/adapters/operations_state.py, tests/test_operations_state.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/adapters/localfs.py, src/skillscout/domain/discovery.py, tests/test_operations_state.py, tests/test_state_integrity.py</read_first>
  <behavior>
    - One transaction inserts first-seen repository reservation and its ordinal or returns its exact existing record.
    - One transaction inserts semantic reservation immediately before first Extractor request or denies the 21st.
    - Rejection, retry, resume, interruption, quarantine and completion never delete/refund either reservation.
  </behavior>
  <action>Turn reservation RED tests GREEN. Build a dedicated descriptor-anchored `OperationsStateStore`, retained flock, exact schema fingerprint, foreign keys and serialized candidate transactions. Tables bind run authority, ordered page/candidate observations, discovery reservations, semantic reservations/attempts, candidate terminals, run health and root checkpoints. Use unique `(run_id, repository_id)` keys, contiguous ordinals and literal 100/20 checks in the same `BEGIN IMMEDIATE`. Per D-04 persist outcome-unknown attempt as consumed plus quarantined/manual; it cannot re-enter an automatic request path.</action>
  <acceptance_criteria>
    - Concurrent/repeated reservation of one numeric ID returns one immutable record.
    - Exact 100/20 boundaries and every non-refund class pass.
    - Tampered counters, ordinals, status or authority fail before reuse.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_operations_state.py -k "reservation or budget or ordinal or refund" -x</automated></verify>
  <done>Durable slots, not loop counters, own all discovery and semantic cost authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-05-02: Implement canonical export, integrity and JSON rebuild</name>
  <files>src/skillscout/adapters/operations_state.py, tests/test_operations_state.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/adapters/publication_state.py, src/skillscout/adapters/phase2_state.py, src/skillscout/domain/discovery.py, tests/test_operations_state.py</read_first>
  <behavior>
    - Export under the retained lock yields complete canonical JSON facts and a consistent bounded SQLite snapshot.
    - Restore requires canonical operations-object digests, exact operations schema, integrity/foreign keys, discovery-chain checks and projection equality.
    - Valid operations JSON rebuilds a fresh private operations database through its validated insertion paths and exact re-export equality.
  </behavior>
  <action>Implement the discovery-owned portion of D-05 export/import/rebuild. Serialize or use SQLite backup while holding operations-store authority; never copy a live file. Verify `PRAGMA integrity_check` exactly `ok`, foreign keys, the operations schema fingerprint, discovery run/page/reservation/attempt/terminal chains and exact operations projection equality. If operations DB bytes are absent/corrupt but its complete JSON facts are valid, replay into a fresh private operations database through the same reservation/attempt/terminal insertion validators, fully reverify, serialize and atomically replace. Do not inspect, reproduce or replay the private schemas of `SQLiteStateStore` or `PublicationStateStore`; Plan 05-12 adds their owner-provided seams and cross-store root equality. Retain all reachable objects per D-09.</action>
  <acceptance_criteria>
    - Missing/corrupt operations DB rebuilds byte-valid discovery query state from owned JSON.
    - Valid operations DB with wrong digest/projection fails closed.
    - Static ownership test proves no Phase 1/3 or publication private table/schema statement appears in `operations_state.py`.
    - Killed writer exposes only previous or complete next snapshot.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_operations_state.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/operations_state.py tests/test_operations_state.py</automated></verify>
  <done>The discovery operations SQLite is a consistent query index over its complete owned rebuild facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application events → transactional ledger | Crashes/concurrency must not reset budgets. |
| Operations JSON/SQLite bytes → discovery reuse authority | Physically valid bytes can still be semantically tampered. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14 | Tampering | 100/20 reservations | high | mitigate | Unique keys and count+insert in one immediate transaction. |
| T-05-15 | Repudiation | unknown semantic attempt | high | mitigate | Durable consumed attempt and quarantine terminal. |
| T-05-16 | Tampering | operations DB/JSON authority | high | mitigate | Digest graph, owned schema/integrity/chains and equality. |
| T-05-17 | Information Disclosure | export | high | mitigate | Closed object builders and prospective payload canaries. |
| T-05-SC | Tampering | dependencies | high | mitigate | Stdlib SQLite and existing localfs only. |
</threat_model>

## Artifacts and symbols produced

`OperationsStateStore` with create/resume/page/candidate/reservation/attempt/terminal/checkpoint/export/restore/rebuild operations, owning only Phase 5 discovery tables.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Durable resumable multi-candidate state | 05-05 | COVERED |
| REQ | DISC-02 | 100/20 durable caps | 05-05 | COVERED |
| REQ | DISC-03 | Stored page/dedup facts | 05-05 | COVERED |
| REQ | OPS-02 | SQLite + JSON rebuild | 05-05 | COVERED |
| REQ | OPS-03 | Trimmed state | 05-05 | COVERED |
| RESEARCH | — | Durable reservation and rebuild patterns | 05-05 | COVERED |
| CONTEXT | D-02,D-03,D-04,D-05,D-08,D-09 | Approved defaults | 05-05 | COVERED |

<verification>
Operations suite, killed-writer/integrity mutations and Ruff; no live network.
</verification>

<success_criteria>
Reservations and every rebuild fact survive crash/resume without cost reset, ambiguity or leakage.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-05-SUMMARY.md`.</output>
