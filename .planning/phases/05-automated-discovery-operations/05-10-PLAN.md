---
phase: 05-automated-discovery-operations
plan: "10"
type: execute
wave: 9
depends_on: [05-09]
files_modified:
  - tools/verify_phase5_acceptance.py
  - tests/test_phase5_acceptance.py
  - tools/verify_phase5_validation_map.py
  - tests/test_phase5_validation_map.py
  - .planning/phases/05-automated-discovery-operations/05-VALIDATION.md
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "An independent standard-library-only inspector proves all five requirements and every prohibition without importing project code."
    - "The validation map is a bijection over all 14 exact Phase 5 plans/tasks, waves, dependencies, commands, artifacts and requirement inverse map."
    - "The final locked release chain includes all Phase 5 suites, prior-phase regression suites, Ruff, full pytest, acceptance and hosted evidence identity."
  artifacts:
    - {path: tools/verify_phase5_acceptance.py, provides: "Independent read-only Phase 5 acceptance inspector"}
    - {path: tools/verify_phase5_validation_map.py, provides: "Exact plan/task/requirement/release-map checker"}
    - {path: .planning/phases/05-automated-discovery-operations/05-VALIDATION.md, provides: "Nyquist-complete task verification map"}
  key_links:
    - {from: every 05-XX-PLAN task, to: 05-VALIDATION.md, via: "exact task ID and focused locked command"}
    - {from: five requirements, to: acceptance claims, via: "one independently mutation-tested inverse map"}
  prohibitions:
    - "No project import, network, write, mutable Action reference, nonlocal tool, self-success claim, plan drift, secret/raw-source acceptance or Phase 6 real-repository campaign."
---

<objective>
Independently verify the entire Phase 5 plan/task map, security boundary and release chain.

Purpose: Prevent implementation/tests from self-certifying and close Nyquist, security and release acceptance before Phase 6.
Output: Two independent verifiers, mutation suites and finalized validation map.

Locked decisions: D-01 through D-09; this plan proves each without changing scope.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tools/verify_phase4_acceptance.py
@tools/verify_phase4_validation_map.py
@tests/test_phase4_acceptance_tool.py
@tests/test_phase4_validation_map.py
@.planning/phases/05-automated-discovery-operations/05-VALIDATION.md
@.planning/phases/05-automated-discovery-operations/05-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-10-01: Build independent Phase 5 acceptance inspector</name>
  <files>tools/verify_phase5_acceptance.py, tests/test_phase5_acceptance.py</files>
  <read_first>tools/verify_phase4_acceptance.py, tests/test_phase4_acceptance_tool.py, .planning/REQUIREMENTS.md, .planning/phases/05-automated-discovery-operations/05-RESEARCH.md</read_first>
  <behavior>
    - Inspector reads bounded UTF-8 source/config/plan bytes, uses AST/structured inspection, imports only stdlib, and performs no write/network.
    - It independently proves exact query/triggers, durable 100/20 caps, complete discovery facts, provider-wide unknown quarantine, three-store pre-request/post-result barriers, per-workflow fan-out, closed discovery/publication handoff, three-store JSON rebuild/CAS/concurrency and cross-surface non-disclosure.
    - Mutation of each claim, ordering, prohibition, action pin, credential zone or hosted evidence identity causes failure.
  </behavior>
  <action>RED: mutation-test every DISC-01..03 and OPS-02..03 claim plus D-01..D-09 prohibition. GREEN: adapt the Phase 4 inspector into a size-bounded, read-only, stdlib-only Phase 5 tool. Inspect AST/constants/routes/methods, strict schemas, workflow structure, exact plan artifacts and hosted evidence binding; do not import SkillScout modules or trust test success strings. Require no new package/agent framework, numeric-ID authority, literal 100/20 transactions, one reservation for 0–3 independent workflows, explicit three-store attempt-start→remote receipt→request and result→remote receipt→retry/terminal ordering across both providers and all three stages, closed OpenAI/DeepSeek disposition handling, exact three-store path/ownership plus store-owned rebuild seams and cross-store equality, parent-bound non-force reread, discovery with no Phase 4/publisher construction route, protected exact-state reread/admission→token→PublicationApplication construction ordering, credential split, allowlisted state/tree/output, no pruning, and Gate B4 equality that cannot be replaced by the Phase 5 concurrency canary.</action>
  <acceptance_criteria>
    - Success case passes only against the complete implemented tree.
    - Each requirement and prohibition has at least one mutation that fails before command credit.
    - Tool leaves repository bytes/metadata unchanged and works from an external cwd.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase5_acceptance.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase5_acceptance.py</automated></verify>
  <done>All five requirements and locked boundaries have independent, mutation-tested acceptance evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-10-02: Finalize exact validation map and release chain</name>
  <files>tools/verify_phase5_validation_map.py, tests/test_phase5_validation_map.py, .planning/phases/05-automated-discovery-operations/05-VALIDATION.md</files>
  <read_first>tools/verify_phase4_validation_map.py, tests/test_phase4_validation_map.py, .planning/phases/05-automated-discovery-operations/05-01-PLAN.md, .planning/phases/05-automated-discovery-operations/05-09-PLAN.md, .planning/phases/05-automated-discovery-operations/05-VALIDATION.md</read_first>
  <behavior>
    - Map pins every exact Phase 5 task ID, wave/dependency, focused command, artifact, requirement and prohibition evidence.
    - Each of five requirements has complete inverse coverage and no absent or incorrectly mapped task.
    - Full release chain begins with independent map/acceptance preflights, runs Ruff/full pytest and ends with fresh independent acceptance plus hosted evidence identity check.
  </behavior>
  <action>RED: add drift/missing/duplicate/mutable-action/nonlocal-command/prohibition/requirement mutations. GREEN: implement the stdlib-only validation-map checker and keep `05-VALIDATION.md` synchronized with exact task rows, commands and artifacts for all 14 plans. Pin the repository-local `.tools/uv-0.11.29/bin/uv run --locked` command, every Phase 5 suite including `test_semantic_durability.py` and `test_discovery_publication_handoff.py`, semantic provider plus OpenAI Extractor/Generator/Reviewer matrices, Phase 1/3 state integrity, Phase 2 resume/extraction, Phase 3 pipeline, Phase 4 publication recovery/security, Ruff, full pytest, acceptance, exact Gate B4 workflow/identity evidence and separate hosted concurrency evidence digest. Preserve `nyquist_compliant: true` for complete planning coverage; execution and hosted evidence remain separately pending and cannot be inferred from the planning flag. Do not claim Phase 6 real-repository acceptance.</action>
  <acceptance_criteria>
    - Exact plan/task bijection and five-requirement inverse map pass.
    - Every mutation fails; no checker self-certifies from mutable plan prose alone.
    - One fresh complete release chain passes with no skipped Phase 5 local behavior and exact hosted evidence binding.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase5_validation_map.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase5_validation_map.py tests/test_phase5_acceptance.py tests/test_discovery_domain.py tests/test_github_search.py tests/test_operations_state.py tests/test_state_branch.py tests/test_discovery_application.py tests/test_discovery_publication_handoff.py tests/test_semantic_durability.py tests/test_discovery_workflow.py tests/test_discovery_security.py tests/test_semantic_provider.py tests/test_openai_extract.py tests/test_openai_generate.py tests/test_openai_review.py tests/test_state_integrity.py tests/test_pipeline_resume.py tests/test_phase3_pipeline.py tests/test_publication_recovery.py tests/test_publication_security.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check . &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase5_acceptance.py</automated></verify>
  <done>The exact task map, security claims and full release chain independently pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Implementation/tests/plans → release claim | Mutable project artifacts must not self-certify. |
| Hosted evidence → local acceptance | External facts must bind exact workflow/code/state identities. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-33 | Repudiation | acceptance claim | high | mitigate | Independent stdlib inspector and mutation suite. |
| T-05-34 | Tampering | validation map | high | mitigate | Exact task/requirement bijection and drift rejection. |
| T-05-35 | Spoofing | hosted/Gate B4 evidence | high | mitigate | Separate exact Gate B4 workflow/identity and Phase 5 concurrency evidence bindings. |
| T-05-36 | Information Disclosure | verifier inputs/output | medium | mitigate | Bounded reads and digest/allowlist output only. |
| T-05-SC | Tampering | release graph | high | mitigate | Locked uv, immutable Action identities and no dependency changes. |
</threat_model>

## Artifacts and symbols produced

`verify_phase5_acceptance.py`, `verify_phase5_validation_map.py`, their mutation suites, and finalized Nyquist-complete `05-VALIDATION.md`.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Independently accepted automated operations | 05-10 | COVERED |
| REQ | DISC-01 | Query/triggers | 05-10 | COVERED |
| REQ | DISC-02 | Durable 100/20 | 05-10 | COVERED |
| REQ | DISC-03 | Complete facts | 05-10 | COVERED |
| REQ | OPS-02 | Rebuild/CAS/concurrency | 05-10 | COVERED |
| REQ | OPS-03 | No raw source/secrets | 05-10 | COVERED |
| RESEARCH | — | Acceptance inspector, validation map, release chain | 05-10 | COVERED |
| CONTEXT | D-01..D-09 | All approved defaults independently checked | 05-10 | COVERED |

<verification>
Run the exact complete command in Task 05-10-02 without partial credit; the hosted evidence referenced by Plan 05-09 must match current workflow bytes.
</verification>

<success_criteria>
All five requirements, all 14 plans and every locked prohibition are independently verified with no missing source-audit item.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-10-SUMMARY.md` with exact command outcomes and final requirement evidence map.</output>
