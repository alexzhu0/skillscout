---
phase: 02-safe-single-repository-extraction
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/skillscout/adapters/openai_extract.py
  - src/skillscout/application/processors.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/cli.py
  - tests/recorded_transport.py
  - tests/fixtures/github/blob_readme.json
  - tests/fixtures/openai/parsed_2_workflows.json
  - tests/fixtures/openai/parsed_zero_workflows.json
  - tests/fixtures/openai/refusal.json
  - tests/fixtures/openai/incomplete_max_tokens.json
  - tests/fixtures/openai/schema_invalid.json
  - tests/fixtures/openai/compromised_url_in_steps.json
  - tests/fixtures/openai/compromised_fake_evidence.json
  - tests/fixtures/openai/openai_429.json
  - tests/fixtures/openai/openai_500.json
  - tests/fixtures/injection/direct_override.md
  - tests/fixtures/injection/privilege_masquerade.md
  - tests/fixtures/injection/secret_solicitation.md
  - tests/fixtures/injection/action_solicitation.md
  - tests/fixtures/injection/exfiltration_markup.md
  - tests/fixtures/injection/encoded_payload.md
  - tests/fixtures/injection/cross_stage_amplification.md
  - tests/test_openai_extract.py
  - tests/test_extractor_boundary.py
  - tests/test_cli_extract_repo.py
  - tests/test_cli_security.py
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-04, SEC-01]
must_haves:
  truths:
    - "Every extraction request is tool-less with store=false, a strict text.format JSON schema generated from the ExtractorResponse Pydantic model, versioned developer instructions (extract-prompt-v1) containing zero repository bytes, and repository text only inside the user role wrapped in explicit untrusted delimiters; the recorded request JSON proves each property."
    - "All four extraction outcomes are diagnosable succeeded attempts that never consume retry budget: extracted (1-3 workflows), no_workflow (0 workflows with rejection_reason), refused, incomplete (with incomplete_details reason) and schema_failure (parse, contract or all-dropped boundary failures with sanitized diagnostics); only API 429/5xx/timeout map to stage_transient_failure under the existing three-attempt RetryPolicy."
    - "A run performs at most one extraction call per attempt with a bounded max_output_tokens and at most three workflows per repository as structural limits; a model response with more than three workflows is a schema_failure, and 0-, 1-, 2- and 3-workflow fixtures all produce contract-valid outcomes."
    - "Every surviving WorkflowSpec carries the complete EXTR-03 field list plus schema_version workflow-spec-v1, deterministic workflow_id, skillscout-computed wf-fingerprint-v1 fingerprint and per-evidence content_hash; each workflow has at least one verified evidence and every step has at least one verified evidence whose path exists in the read record, whose blob_sha matches, and whose excerpt is a verbatim substring of at most 280 characters of the recorded blob, or the workflow is dropped with recorded reasons."
    - "WorkflowSpec is the only semantic artifact crossing downstream: the full-text canary appears in no manifest, SQLite row, stdout byte or extraction-summary.json, extractor payloads carry only bounded excerpts, and a filter-rejected run completes with zero recorded OpenAI requests."
    - "The SEC-01 boundary holds end to end (one entry covering both cross-cutting probe rows): fake SKILLSCOUT_GITHUB_TOKEN and OPENAI_API_KEY canaries appear only in Authorization headers and in no recorded request body, fixture-free durable surface, log or stdout; all seven markdown injection classes (direct override, privilege masquerade, secret solicitation, action solicitation, exfiltration markup, encoded payloads, cross-stage amplification) yield contract-valid outcomes with no tool, network or secret effect; and compromised-model responses carrying URLs, shell-shaped commands, secret shapes or fabricated evidence are dropped by deterministic boundary validation."
    - "The extract-repo CLI completes the happy path over recorded fixtures with status completed and a durable extraction-summary.json, resumes an interrupted run without re-calling any succeeded Scout/Filter endpoint (recorded call counts) and with exactly one total LLM call, and rejects a malformed subject with the closed INVALID_SUBJECT error, no state file and no echoed input bytes; the only Phase 1 test edit is the additive extract-repo member in the subparser-set assertion."
  artifacts:
    - { path: src/skillscout/adapters/openai_extract.py, provides: "closed no-tools store=false Responses extraction adapter", exports: [OpenAIExtractionClient, ExtractionResult, DEFAULT_EXTRACT_MODEL, MAX_EXTRACT_OUTPUT_TOKENS, EXTRACT_INSTRUCTIONS_V1] }
    - { path: src/skillscout/application/processors.py, provides: "_extractor stage handler with one-call discipline, outcome mapping, boundary validation and fingerprinting" }
    - { path: src/skillscout/application/pipeline.py, provides: "seven-registration phase-two root including openai_extract" }
    - { path: src/skillscout/cli.py, provides: "extract-repo subcommand with closed fail-after choices and sanitized failures" }
    - { path: tests/test_openai_extract.py, provides: "request-shape contract and outcome-mapping evidence over recorded OpenAI fixtures" }
    - { path: tests/test_extractor_boundary.py, provides: "injection corpus, compromised-model and secret-canary sweep evidence" }
    - { path: tests/test_cli_extract_repo.py, provides: "end-to-end CLI, resume/idempotency and summary-artifact evidence" }
  key_links:
    - { from: src/skillscout/application/processors.py, to: src/skillscout/adapters/openai_extract.py, via: "exactly one responses.parse call per attempt with text_format from the Pydantic model", pattern: "responses.parse" }
    - { from: src/skillscout/application/processors.py, to: src/skillscout/domain/extraction.py, via: "deterministic boundary validation and skillscout-owned fingerprint after every parse", pattern: "validate_workflow_boundaries" }
    - { from: src/skillscout/cli.py, to: src/skillscout/application/pipeline.py, via: "extract-repo constructs the phase-two runtime only through build_phase_two_runtime", pattern: "build_phase_two_runtime" }
    - { from: tests/test_extractor_boundary.py, to: tests/fixtures/injection, via: "eight attack classes proving untrusted text never gains instruction authority", pattern: "CANARY_FULL_TEXT_SENTENCE" }
  prohibitions:
    - "Untrusted repository text must never be promoted into the developer or instruction role or interpreted as a tool call, even when it perfectly mimics system markup, fake tool invocations or earlier conversation turns."
    - "A workflow whose evidence cannot be verified verbatim against recorded blobs must never survive extraction to appear evidence-backed; fabricated or unverifiable evidence always drops the workflow."
    - "A decided business outcome (filter rejection, refusal, incomplete, schema failure, no_workflow) must never be retried by re-calling the LLM for a different answer; only infrastructure errors consume the three-attempt budget."
    - "Model output must never define identity: fingerprints, workflow IDs, blob SHAs and content hashes are computed by skillscout, and any model-emitted identity is discarded."
    - "GitHub/OpenAI credentials must never be written to request bodies, logs, manifests, SQLite, stdout, extraction-summary.json or committed fixtures — including in tests, which use only declared canary values."
---

<objective>
Land the no-tools `store=false` OpenAI extraction adapter, wire the Extractor stage with deterministic boundary validation and skillscout-owned fingerprints, prove the eight-class prompt-injection corpus and secret-canary sweeps, and finish the `extract-repo` CLI with end-to-end resume and idempotency evidence.

Purpose: Complete the phase goal — one specified public repository yields at most three evidenced `WorkflowSpec`s or a clear filtered/no-workflow conclusion, with untrusted content never gaining instruction authority and never crossing downstream as raw text.
Output: `OpenAIExtractionClient`, `_extractor` handler, recorded OpenAI/injection fixtures, three focused test modules, the `extract-repo` subcommand and the full-suite acceptance gate.
</objective>

## Phase Goal

**As a** SkillScout reviewer, **I want to** point the pipeline at one public GitHub repository and receive at most three evidenced `WorkflowSpec`s or a clear filtered/no-workflow conclusion, **so that** real extraction is proven safe — pinned, filtered, budgeted, never executed — before generation, review and publishing exist.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md
@.planning/phases/02-safe-single-repository-extraction/02-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md
@.planning/phases/02-safe-single-repository-extraction/02-03-SUMMARY.md
@src/skillscout/application/processors.py
@src/skillscout/domain/extraction.py
@src/skillscout/adapters/github.py
@src/skillscout/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-04-01: No-tools store=false OpenAI extraction adapter over recorded fixtures</name>
  <files>src/skillscout/adapters/openai_extract.py, tests/recorded_transport.py, tests/fixtures/openai/parsed_2_workflows.json, tests/fixtures/openai/parsed_zero_workflows.json, tests/fixtures/openai/refusal.json, tests/fixtures/openai/incomplete_max_tokens.json, tests/fixtures/openai/schema_invalid.json, tests/fixtures/openai/openai_429.json, tests/fixtures/openai/openai_500.json, tests/test_openai_extract.py</files>
  <read_first>src/skillscout/adapters/github.py, src/skillscout/application/ports.py, src/skillscout/domain/extraction.py, src/skillscout/domain/models.py, tests/recorded_transport.py, tests/test_github_adapter.py, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md, .planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md</read_first>
  <behavior>
    - The single call site is responses.parse with text_format from the Pydantic model, store=False, a bounded max_output_tokens and no tools key, ever.
    - Refusal, incomplete and schema-invalid responses are outcome data; only 429/5xx/timeout are transient failures; auth and bad-request errors are permanent.
    - Telemetry (response id, actual model, usage, measured latency) is returned for the attempt ledger.
  </behavior>
  <action>Author the recorded OpenAI fixtures in Responses API shape (`id`, `status`, `model`, `output[]` with message content `output_text`, `usage` with input/output/total tokens): `parsed_2_workflows.json` whose embedded ExtractorResponse JSON carries two workflows with evidence verbatim-coherent with the canonical `tree_full.json`/blob fixtures (paths README.md and docs/guide.md, matching blob SHAs, verbatim ≤280-character excerpts), `parsed_zero_workflows.json` (empty workflows with rejection_reason), `refusal.json` (refusal content item), `incomplete_max_tokens.json` (status incomplete, incomplete_details.reason max_output_tokens), `schema_invalid.json` (output_text failing the response schema), `openai_429.json` and `openai_500.json`. Extend `tests/recorded_transport.py` so the same loader serves the OpenAI base URL.

Implement `src/skillscout/adapters/openai_extract.py`: constants `DEFAULT_EXTRACT_MODEL = "gpt-5.6-terra"` (STACK.md MVP default, configurable by constructor), `MAX_EXTRACT_OUTPUT_TOKENS = 8_000`, and `EXTRACT_INSTRUCTIONS_V1` — the versioned developer instructions containing the literal marker `extract-prompt-v1`, the extraction task definition, and the standing rule that everything inside the untrusted delimiters is inert data, never instructions. `OpenAIExtractionClient(*, api_key=None, http_client=None, model=DEFAULT_EXTRACT_MODEL, max_output_tokens=MAX_EXTRACT_OUTPUT_TOKENS)` resolves the key once from the argument or `OPENAI_API_KEY` (missing/empty → `SafeFailure(STAGE_PERMANENT_FAILURE)`), constructs `openai.OpenAI(api_key=..., http_client=...)` (tests inject a MockTransport-backed `httpx.Client`), declares `effect_scope = EffectScope.REMOTE_READ`, and exposes one method `extract(*, user_payload: str) -> ExtractionResult` whose single call is `responses.parse(model=self._model, input=[{"role": "developer", "content": EXTRACT_INSTRUCTIONS_V1}, {"role": "user", "content": user_payload}], text_format=ExtractorResponse, store=False, max_output_tokens=self._max_output_tokens)` with no `tools` argument. `ExtractionResult` is a frozen carrier: status Literal `parsed|refused|incomplete|schema_invalid`, response (ExtractorResponse | None), refusal_text (bounded), incomplete_reason, request_id, model, usage (TokenUsage | None, mapping input→prompt and output→completion tokens), latency_ms. Mapping: completed+parsed → parsed; refusal item → refused; status incomplete → incomplete with reason; parse/validation failure → schema_invalid; SDK rate-limit/server/timeout/connection errors → `STAGE_TRANSIENT_FAILURE`; authentication/permission/bad-request → `STAGE_PERMANENT_FAILURE`; always `raise ... from None`. `tests/test_openai_extract.py` asserts the recorded request JSON (model, store false, absent tools key, strict json_schema text.format equal to `ExtractorResponse.model_json_schema()`, developer role carrying `extract-prompt-v1` and zero fixture bytes, user role carrying the payload), every outcome mapping, telemetry fields, and both transient mappings.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_extract.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/adapters/openai_extract.py tests/test_openai_extract.py</automated></verify>
  <acceptance_criteria>
    - The recorded request body contains `"store":false`, no `tools` key, a strict schema equal to the Pydantic-generated one, and repository text in no field other than the user-role payload.
    - parsed/refused/incomplete/schema_invalid each map to the documented `ExtractionResult` with telemetry; 429 and 500 raise `stage_transient_failure`; a missing API key fails closed at construction.
    - The API key canary appears only in the Authorization header of recorded OpenAI requests, never in URLs or bodies.
    - No VCR, tenacity or tiktoken import exists; the only new import is `openai` confined to this module per the capability sweep.
  </acceptance_criteria>
  <done>The LLM capability exists only as one closed, recorded-fixture-proven extraction call with total outcome and error mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-04-02: Extractor handler, eight-class injection corpus and compromised-model boundary proof</name>
  <files>src/skillscout/application/processors.py, tests/fixtures/github/blob_readme.json, tests/fixtures/injection/direct_override.md, tests/fixtures/injection/privilege_masquerade.md, tests/fixtures/injection/secret_solicitation.md, tests/fixtures/injection/action_solicitation.md, tests/fixtures/injection/exfiltration_markup.md, tests/fixtures/injection/encoded_payload.md, tests/fixtures/injection/cross_stage_amplification.md, tests/fixtures/openai/compromised_url_in_steps.json, tests/fixtures/openai/compromised_fake_evidence.json, tests/test_extractor_boundary.py</files>
  <read_first>src/skillscout/application/processors.py, src/skillscout/adapters/openai_extract.py, src/skillscout/domain/extraction.py, src/skillscout/domain/canonical.py, tests/test_reader.py, tests/test_openai_extract.py, tests/recorded_transport.py, tests/fixtures/github/blob_readme.json, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md</read_first>
  <behavior>
    - Exactly one extraction call per attempt; the bundle reaches the model only inside untrusted delimiters in the user role.
    - Boundary validation and fingerprinting are deterministic and skillscout-owned; decided outcomes are never retried.
    - The injection corpus and compromised-model fixtures cannot produce tools, network actions, secret disclosure or unverifiable evidence.
  </behavior>
  <action>Extend `blob_readme.json` with two sentinel sentences: `CANARY_FULL_TEXT_SENTENCE_DO_NOT_PERSIST_9f3b` (must never appear in any durable or output surface) and `CANARY_EVIDENCE_SENTENCE_VERBATIM_7a21` (appears as one bounded verbatim excerpt in the happy-path parsed fixture). Author the seven markdown injection fixtures per 02-RESEARCH.md §Security Threat Model Inputs classes 1–7, each embedding the full-text canary.

Implement `_extractor` in `src/skillscout/application/processors.py`: beyond the Plan 02-02 skip guards, skip with `"reader_empty"` when the accepted reader payload has zero files (no LLM call); obtain the bundle from `context.scratch["read_bundle"]` or rebuild it through `hydrate_read_bundle` (byte-verified against the recorded content hashes); serialize the user payload as one preamble line plus one block per file in read_order (`<<<UNTRUSTED REPOSITORY FILE path="..." blob_sha="...">>>`, text, `<<<END UNTRUSTED FILE>>>`); issue exactly one `openai.extract` call; then map outcomes — parsed: run `validate_workflow_boundaries` per candidate against the bundle texts and recorded file map, require at least one verified workflow-level evidence and at least one verified evidence per step, drop violators with recorded reasons, build `WorkflowSpec` survivors (`workflow_id = "wf-" + fingerprint[7:23]`, `workflow_fingerprint(repo_id=..., goal=..., steps=...)` with the Scout repo id, per-evidence content_hash from the read record), outcome `extracted` for 1–3 survivors, `no_workflow` when the model returned zero, `schema_failure` when every returned workflow was dropped or the count exceeds three; refused → `refused` with bounded text; incomplete → `incomplete` with reason; schema_invalid → `schema_failure` with sanitized diagnostics. Payload: schema facts, outcome, bounded repository_summary, rejection_reason, workflows, dropped `[{title, reasons}]`, prompt_version and configured-vs-actual model; telemetry carries prompt_version `extract-prompt-v1`, actual `response.model` as model_id, `response.id` as request_id, measured latency and token usage. All outcomes are succeeded attempts — nothing here raises except infrastructure mapping inside the adapter.

`tests/test_extractor_boundary.py`: the 0/1/2/3-workflow fixtures produce contract-valid payloads (a synthetic 4-workflow response is `schema_failure`); fingerprint properties hold on live survivors (stable under rewording-only normalization variants, sensitive to semantic and step-order change); every injection markdown runs the full Scout→Filter→Reader→Extractor path over MockTransport with outcomes contract-valid, the recorded OpenAI request showing the injected text only inside user-role delimiters, zero effect beyond the one recorded call, and the full-text canary absent from every manifest, SQLite row and stdout byte while the evidence canary appears only as a bounded excerpt; both compromised-model fixtures lose their tainted workflows (URL/shell-shaped steps, fabricated path, mismatched blob_sha, non-verbatim excerpt) with recorded drop reasons, and all-dropped runs end `schema_failure` with attempt count 1 and zero retries; fake `SKILLSCOUT_GITHUB_TOKEN`/`OPENAI_API_KEY` canaries set through the environment appear only in Authorization headers across every recorded request and in no request body or durable surface.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_extractor_boundary.py tests/test_openai_extract.py tests/test_reader.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_extractor_boundary.py</automated></verify>
  <acceptance_criteria>
    - Recorded OpenAI request counts are exactly 1 per extraction attempt; filter-rejected and reader-empty paths record 0 OpenAI requests.
    - All seven injection classes and both compromised-model fixtures pass with contract-valid outcomes, recorded drop reasons and zero retries on decided outcomes.
    - Both canary disciplines hold: full-text canary absent from all durable/output surfaces, evidence canary present only as a ≤280-character verbatim excerpt; secret canaries confined to Authorization headers.
    - Surviving WorkflowSpecs carry the full EXTR-03 field list, deterministic workflow_id, wf-fingerprint-v1 fingerprint and per-evidence content_hash.
  </acceptance_criteria>
  <done>Extraction is one bounded, tool-less call whose output survives only through deterministic evidence verification, with the injection corpus and secret canaries proven.</done>
</task>

<task type="auto">
  <name>Task 02-04-03: extract-repo CLI, end-to-end resume/idempotency evidence and the full acceptance gate</name>
  <files>src/skillscout/cli.py, src/skillscout/application/pipeline.py, tests/test_cli_extract_repo.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/cli.py, src/skillscout/application/pipeline.py, src/skillscout/application/processors.py, src/skillscout/adapters/subjects.py, src/skillscout/adapters/github.py, src/skillscout/adapters/openai_extract.py, tests/conftest.py, tests/test_cli_dry_run.py, tests/test_cli_security.py, tests/test_phase2_pipeline.py, tests/recorded_transport.py</read_first>
  <action>Add the `extract-repo` subcommand in `src/skillscout/cli.py` as a sibling branch mirroring `dry-run`: `--subject <path>`, `--state <db>`, `--output <dir>` (all required `type=Path`) and `--fail-after` with `choices=PHASE_TWO_STAGE_SEQUENCE`; the branch runs `load_subject` → `SQLiteStateStore` → `GitHubReadClient()` → `OpenAIExtractionClient()` → `PhaseTwoProcessor(github, openai)` → `build_phase_two_runtime(state, processor)` → `runner.run(...).as_dict()` printed as compact sorted-key JSON, with credentials only from the environment, errors only through the existing SafeFailure/exception sanitizers. Extend `build_phase_two_runtime` additively: the processor now holds both clients, the closed registry gains `openai_extract` (the exact instance held by the processor) and the expected-type tuple gains `OpenAIExtractionClient`; update the root-policy tests for the seven-registration set. Amend exactly one Phase 1 assertion: `tests/test_cli_security.py:132` subparser set gains `"extract-repo"`.

Write `tests/test_cli_extract_repo.py` with the transport seam named up front: every case that exercises Scout/Filter/Reader/Extractor runs in-process via `cli.main([...])` — the Phase 1-sanctioned style of tests/test_cli_dry_run.py:359-370 (monkeypatch plus capsys) — with the client constructors monkeypatched at their `skillscout.cli` module lookup names so the branch builds `GitHubReadClient(transport=...)` and `OpenAIExtractionClient(http_client=httpx.Client(transport=...))` over the recorded MockTransports, and with the conftest `outbound_socket_sentinel` fixture requested so any real connection attempt fails the test; the test retains the transport recorder objects across invocations, which is how recorded call counts stay observable for the resume and idempotency demonstrations. The subprocess `run_cli` seam (tests/conftest.py:41-51) is kept only for cases that fail before any client is constructed. Cases: happy path over the canonical recorded fixtures with fake env credentials — `cli.main` returns 0, stdout summary reports status `completed`, last_stage `extractor`, `remote_writes_attempted` 0, `extraction-summary.json` parses as `ExtractionSummary` with four stage outcomes and the surviving fingerprints, four succeeded attempts with extractor telemetry (model_id, request_id, token counts) and `verify_run_chain`-consistent state; filter-rejection path (`repo_archived.json`) — COMPLETED with filter rejected, reader/extractor skipped and zero recorded OpenAI requests; resume demonstration — `--fail-after reader` returns 1 INTERRUPTED, plain rerun returns 0 COMPLETED, the retained recorders prove metadata/commits/tree/license call counts unchanged at 1 each, blob GETs re-issued only for hash-verified hydration, and exactly one OpenAI request across both invocations; idempotency — a third in-process invocation on the same state reuses all four verified stages and records zero GitHub and zero OpenAI calls; hostile subject — malformed/oversized/symlinked subject file via subprocess exits 1 with `invalid_subject`, no state file created and no input bytes echoed; bad `--fail-after` choice via subprocess exits 2 with the fixed argparse diagnostic. Finish with the full gate under the canonical prefix: `uv lock --check`, `uv build --no-sources`, `ruff check .`, and the complete `pytest -q`.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_extract_repo.py tests/test_cli_security.py tests/test_extractor_boundary.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - The in-process happy path (`cli.main` with constructor-monkeypatched recorded transports and the socket sentinel active) returns 0 with a COMPLETED run, four succeeded attempts, extractor telemetry populated and a parseable bounded `extraction-summary.json`.
    - Resume after `--fail-after reader` completes across two in-process `cli.main` invocations with Scout/Filter recorded call counts at 1 each (asserted on the test-retained transport recorders), hash-verified blob hydration only, and exactly one total OpenAI request; a third invocation makes zero remote calls (full reuse).
    - Filter-rejection E2E records zero OpenAI requests; the subprocess cases never reach client construction — hostile subjects fail closed as `invalid_subject` without state creation or input echo and a bad `--fail-after` choice exits 2; the amended subparser assertion passes with three commands.
    - The complete gate (`lock --check`, `build --no-sources`, `ruff check .`, full `pytest -q`) exits 0 with zero credentials and only the single sanctioned Phase 1 test amendment in this plan; zero live network is proven by the active socket sentinel in every in-process network-touching test and by the subprocess cases failing before any client is constructed.
  </acceptance_criteria>
  <done>The extract-repo path is end-to-end green over recorded fixtures with resume, idempotency, injection and disclosure evidence, and the phase is ready for verification.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted repository text → LLM request | Adversary-controlled prose enters exactly one request region and must never gain instruction authority. |
| Model output → WorkflowSpec | A compromised or manipulated model must not fabricate evidence, identity or executable intent. |
| Credentials → request/durable surfaces | API keys exist only as headers; every emitted byte is swept. |
| CLI → operator and state | New subcommand must keep the closed, non-echoing failure discipline. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04-PI | Tampering / Elevation of Privilege | prompt injection via repository content | critical | mitigate | Tool-less request, store=false, versioned developer-only instructions, untrusted delimiters in the user role only, seven-class corpus proof, deterministic post-validation (Tasks 02-04-01/02; canon: injection is also owned by `/gsd:secure-phase` — breadcrumb, concrete checks here). |
| T-02-04-SX | Information Disclosure | secret exfiltration into requests/logs/state | high | mitigate | Header-only credentials, minimized request content, store=false, canary sweeps over request bodies, manifests, SQLite, stdout and summary (Tasks 02-04-01/02/03). |
| T-02-04-CM | Tampering / Spoofing | compromised or manipulated model output | high | mitigate | Deterministic boundary validation (verbatim evidence, recorded blob SHAs, forbidden URL/shell/secret patterns), skillscout-computed fingerprints and workflow IDs, all-dropped → schema_failure (Task 02-04-02). |
| T-02-04-COST | Denial of Service | runaway LLM/API cost | medium | mitigate | Structural one-call-per-attempt discipline, bounded max_output_tokens, ≤3 workflows in schema, skip cascades with zero-call proof, existing three-attempt transient-only retry budget (Tasks 02-04-01/02/03). |
| T-02-04-LK | Information Disclosure | raw repository text crossing downstream | high | mitigate | Bounded excerpts only in payloads, full-text canary sweep across every durable surface, WorkflowSpec as sole semantic carrier (Tasks 02-04-02/03). |

</threat_model>

## Artifacts this phase produces

- Source files: `src/skillscout/adapters/openai_extract.py` (`OpenAIExtractionClient`, `ExtractionResult`, `DEFAULT_EXTRACT_MODEL`, `MAX_EXTRACT_OUTPUT_TOKENS`, `EXTRACT_INSTRUCTIONS_V1`); `_extractor` handler in `src/skillscout/application/processors.py`; `extract-repo` branch and `PHASE_TWO_STAGE_SEQUENCE` choices in `src/skillscout/cli.py`; seven-registration `build_phase_two_runtime` in `src/skillscout/application/pipeline.py`.
- Test assets: `tests/test_openai_extract.py`, `tests/test_extractor_boundary.py`, `tests/test_cli_extract_repo.py`; recorded OpenAI fixtures (`parsed_2_workflows`, `parsed_zero_workflows`, `refusal`, `incomplete_max_tokens`, `schema_invalid`, `compromised_url_in_steps`, `compromised_fake_evidence`, `openai_429`, `openai_500`); seven injection markdown files; canary sentences `CANARY_FULL_TEXT_SENTENCE_DO_NOT_PERSIST_9f3b` and `CANARY_EVIDENCE_SENTENCE_VERBATIM_7a21` in `blob_readme.json`.
- CLI surface: `skillscout extract-repo --subject <path> --state <db> --output <dir> [--fail-after {scout,filter,reader,extractor}]`.
- Behaviors: one-call extraction with strict structured outputs, four diagnosable business outcomes, deterministic boundary validation and fingerprinting, skip cascades with zero-LLM proof, resume with hash-verified hydration, full reuse idempotency.
- Phase 1 test amendment: the additive `"extract-repo"` member in the subparser-set assertion (the only Phase 1 test edit in this plan).

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Up to three evidenced WorkflowSpecs or a clear conclusion, nothing executed or passed raw downstream | 02-04 | COVERED | Extraction + boundary + canary sweeps + CLI E2E. |
| REQ | EXTR-01 | Tool-less strict structured request; diagnosable refusal/schema failure | 02-04 | COVERED | Request-shape contract + four outcome classes. |
| REQ | EXTR-02 | ≤3 independent workflows with stable fingerprints | 02-04 | COVERED | 0–3 fixtures, >3 schema_failure, fingerprint properties. |
| REQ | EXTR-03 | Full WorkflowSpec field list with evidence | 02-04 | COVERED | Contract + verified-evidence drop rules. |
| REQ | EXTR-04 | WorkflowSpec as sole semantic boundary | 02-04 | COVERED | Bounded excerpts; full-text canary sweep. |
| REQ | SEC-01 (both probe rows) | store=false, no tools, untrusted-input handling, no secrets | 02-04 | COVERED | One truth entry + request-shape, canary and injection evidence cover both cross-cutting rows. |
| RESEARCH | — | One extraction call per run; ≤3 specs; injection corpus; CLI + summary + resume demos | 02-04 | COVERED | Structural one-call discipline and the two CLI demonstrations. |
| PROBE | rows | EXTR-01..04 and SEC-01×2 edge predicates authored into must_haves truths | 02-04 | COVERED | All 15 probe rows now authored across the four plans. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No CONTEXT.md decisions exist. |

<verification>
- `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_extract.py tests/test_extractor_boundary.py tests/test_cli_extract_repo.py` passes, then the full gate: `... lock --check && ... build --no-sources && ... run --locked ruff check . && ... run --locked pytest -q`.
- Recorded request JSON proves store=false, no tools, strict Pydantic-generated schema, developer-only instructions and user-role-only repository text.
- The seven-class injection corpus and both compromised-model fixtures produce only contract-valid, retry-free outcomes; both canary disciplines hold across every surface.
- Resume and idempotency demonstrations show unchanged Scout/Filter call counts, hash-verified hydration only, exactly one total LLM call, and a zero-call third run; the full-text canary is absent from every durable surface.
- The suite passes with zero credentials and zero live network; the only Phase 1 test edit is the additive subparser member.
</verification>

<success_criteria>
- One specified repository yields at most three fully evidenced WorkflowSpecs or a clear filtered/no-workflow conclusion through `extract-repo`, with complete stage ledgers and a durable extraction summary.
- Prompt injection cannot reach tools, network, secrets or downstream surfaces; compromised-model output is dropped by deterministic validation; fingerprints are skillscout-owned.
- Cost is structurally capped at one extraction call and three workflows; business outcomes are never retried; the entire suite is green under the approved lock.
</success_criteria>

<output>After completion, create `.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md` with the request-shape evidence, injection/boundary outcomes, CLI demonstration transcripts (happy path, resume, idempotency) and the final gate results.</output>
