---
phase: 01-auditable-dry-run-spine
plan: "18"
type: execute
wave: 17
depends_on: ["01-17"]
files_modified:
  - tools/verify_phase1_gap_evidence.py
  - tests/test_phase1_evidence_verifier.py
  - tests/test_phase1_gap_closure.py
  - .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "The evidence source authority explicitly binds tests/fixtures/pipeline/approved.json and tests/fixtures/state/v1-cli-provenance.json alongside the existing exact source set."
    - "A whitespace-only or key-order-only byte change to either bound JSON fixture invalidates recorded evidence at the source-digest check before any command result is credited."
    - "The finding map credits exactly the current 2026-07-19 review findings CR-01 and WR-01 through digest-bound top-level test nodes; the superseded seven-finding constants are replaced."
    - "Fresh evidence is recorded only after all bound code, review, and verification bytes are stable, and read-only verify --rerun passes from an external working directory."
    - "uv.lock and the frozen schema-version-1 database hashes remain unchanged before record, after record, and after the independent rerun."
  artifacts:
    - { path: tools/verify_phase1_gap_evidence.py, provides: "fixture-complete closed source set and current two-finding authority map" }
    - { path: tests/test_phase1_evidence_verifier.py, provides: "stale JSON fixture rejection regressions proven before command credit" }
    - { path: tests/test_phase1_gap_closure.py, provides: "current two-finding AST mapping and composed smoke" }
    - { path: .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, provides: "fresh schema-version-2 evidence bound to the stabilized tree" }
  key_links:
    - { from: evidence.source_digests, to: both reviewed JSON fixtures, via: "explicit closed paths in _source_paths recomputed on every record and verification", pattern: "approved.json" }
    - { from: evidence.current_findings, to: current review CR-01/WR-01, via: "AST-resolved crash-recovery and fixture-authority nodes executed by the closed registry", pattern: "current_findings" }
    - { from: verify --rerun, to: external working directory, via: "read-only independent rerun of the full registry with unchanged bound bytes", pattern: "--rerun" }
    - { from: recorded evidence, to: Plan 01-17 crash-recovery bytes, via: "source digests captured after the crash-recovery plan lands", pattern: "source_digests" }
  prohibitions:
    - "The evidence document and the verifier outcome must never certify themselves; no self-hash, self-success, record, or verify claim enters the authority they report."
    - "The closed authority set must never widen implicitly; every bound source, fixture, and registry command is explicit and reviewed."
    - "Recorded evidence must never contain secrets, credential-shaped values, raw exception text, or unreviewed absolute paths; only bounded normalized digests and allowlisted structured facts."
---

<objective>
Close the 2026-07-19 WARNING: the Phase-1 acceptance evidence authority is stale, excludes both reviewed JSON fixtures, and maps superseded finding semantics.

Purpose: `tools/verify_phase1_gap_evidence.py` `_source_paths` binds only `.py` under tests, so semantically neutral byte changes to `tests/fixtures/pipeline/approved.json` or `tests/fixtures/state/v1-cli-provenance.json` leave every recorded claim accepted; `CURRENT_FINDING_NODES` still labels the prior review's findings; and `01-GAP-VALIDATION.md` digests are stale so `verify --rerun` exits 1. Binding the two fixtures, replacing the finding map with the current CR-01/WR-01 semantics, and recording fresh evidence only after the tree stabilizes restores current, independently rerunnable authority (OPS-01, OPS-04).
Output: Fixture-complete source authority, stale-fixture rejection regressions, the current two-finding map, and a freshly recorded evidence document proven by an external-cwd rerun.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-16-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-17-PLAN.md
@tools/verify_phase1_gap_evidence.py
@tests/test_phase1_evidence_verifier.py
@tests/test_phase1_gap_closure.py
@tests/fixtures/pipeline/approved.json
@tests/fixtures/state/v1-cli-provenance.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-18-01: Fixture-complete source set and current two-finding authority map</name>
  <files>tools/verify_phase1_gap_evidence.py, tests/test_phase1_evidence_verifier.py, tests/test_phase1_gap_closure.py</files>
  <read_first>tools/verify_phase1_gap_evidence.py, tests/test_phase1_evidence_verifier.py, tests/test_phase1_gap_closure.py, tests/fixtures/pipeline/approved.json, tests/fixtures/state/v1-cli-provenance.json, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md, .planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md, .planning/phases/01-auditable-dry-run-spine/01-17-PLAN.md</read_first>
  <behavior>
    - After recording, a whitespace-only byte change to approved.json makes verify_evidence raise before the injected runner is consulted (runner call count stays zero); the same holds for a key-order-only byte change to v1-cli-provenance.json.
    - The recorded source_digests claim contains exactly both JSON fixture paths; removing either from the claim, or substituting a non-bound fixture path, fails closed.
    - The verifier finding map contains exactly CR-01 and WR-01, each resolving to a top-level test function in the digest-bound tree.
    - The composed smoke and node-definition tests in tests/test_phase1_gap_closure.py follow the two-finding current map while the superseded seven-finding map remains preserved under a past-tense label.
  </behavior>
  <action>Setup first in `tests/test_phase1_evidence_verifier.py`: extend the existing `evidence_repository` fixture so the harness repository contains bounded, deterministic copies of both reviewed JSON fixtures at their exact relative paths before any `record_evidence` call — `shutil.copyfile` from `PROJECT_ROOT / "tests/fixtures/pipeline/approved.json"` to `repository / "tests/fixtures/pipeline/approved.json"` and from `PROJECT_ROOT / "tests/fixtures/state/v1-cli-provenance.json"` to `repository / "tests/fixtures/state/v1-cli-provenance.json"`, creating parent directories exactly like the existing `v1-cli.db` copy and keeping the dynamic stub-module generation loop derived from `CURRENT_FINDING_NODES` unchanged; without this step the new mandatory `_source_paths` literals make every harness record call fail with a missing-source error instead of exercising the digest check. RED next in the same file using the extended harness and injected `CommandRunner`. Add exactly `test_stale_json_fixture_bytes_are_rejected_before_command_credit` (this name is a bound external contract; the verifier's WR-01 node references it): record evidence in the harness repository with the successful runner, then (a) flip only whitespace inside the harness copy of `tests/fixtures/pipeline/approved.json` and assert `verify_evidence(..., rerun=True, runner=spy)` raises while the spy runner's call count is zero, proving rejection at the source-digest check before command credit; (b) repeat with a key-order-only byte change to `tests/fixtures/state/v1-cli-provenance.json`; (c) assert the recorded `source_digests` contains exactly `tests/fixtures/pipeline/approved.json` and `tests/fixtures/state/v1-cli-provenance.json`, and that dropping either from the claim or substituting a non-bound fixture path fails closed. Then GREEN in `tools/verify_phase1_gap_evidence.py`: add `Path("tests/fixtures/pipeline/approved.json")` and `Path("tests/fixtures/state/v1-cli-provenance.json")` to the fixed `relative_paths` literal in `_source_paths`, keeping the `.py`-only walk, the sorted/deterministic ordering, and the `len(ordered) == len(relative_paths)` duplicate guard; the frozen database remains a separately pinned immutable input, never a source claim. Replace `CURRENT_FINDING_NODES` with exactly two entries: `CR-01` mapping to `tests/test_pipeline_resume.py::test_killed_writer_stale_state_temp_recovers_and_resumes_without_prefix_replay` (owned by Plan 01-17; confirm it resolves through `_resolve_nodes` against the bound tree) and `WR-01` mapping to `tests/test_phase1_evidence_verifier.py::test_stale_json_fixture_bytes_are_rejected_before_command_credit`. Update `render_document`: rename the `## Current Seven-Finding Matrix` heading to `## Current Two-Finding Matrix` and rewrite the deferred-item prose so the OS/syscall network-denial item remains assigned only to Phase 6 without referencing the retired seven-finding WR-04 entry. Keep `SCHEMA_VERSION = "2"`, `EXPECTED_TOP_LEVEL`, `CLI_FACTS`, `COMMAND_REGISTRY`, and normalization semantics unchanged. In `tests/test_phase1_gap_closure.py`, preserve the superseded seven-finding map under the past-tense label `PRIOR_REVIEW_FINDING_NODES` with its own definitions-existence assertion (matching the `LEGACY_GAP_FINDING_NODES` precedent), redefine `CURRENT_REVIEW_FINDING_NODES` as exactly `("CR-01", "WR-01")` pointing to the same two nodes, and update `test_current_review_finding_node_definitions_exist` to assert the two-key tuple.</action>
  <acceptance_criteria>
    - `grep -c "approved.json" tools/verify_phase1_gap_evidence.py` and `grep -c "v1-cli-provenance.json" tools/verify_phase1_gap_evidence.py` each return at least 1; binding inside the `_source_paths` fixed literal is proven behaviorally by `test_stale_json_fixture_bytes_are_rejected_before_command_credit` rejecting stale fixture bytes before command credit.
    - `CURRENT_FINDING_NODES` contains exactly the keys CR-01 and WR-01; `_resolve_nodes` passes against the current tree.
    - `test_stale_json_fixture_bytes_are_rejected_before_command_credit` proves zero runner calls on stale fixture bytes for both fixtures, plus exact claim-set membership.
    - `tests/test_phase1_gap_closure.py` asserts `("CR-01", "WR-01")` for the current map and keeps the prior seven-finding map resolvable under its new label.
    - The full verifier adversarial file, the gap-closure file, and Ruff pass under the locked offline prefix.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase1_evidence_verifier.py tests/test_phase1_gap_closure.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check tools/verify_phase1_gap_evidence.py tests/test_phase1_evidence_verifier.py tests/test_phase1_gap_closure.py</automated></verify>
  <done>Both reviewed JSON fixtures are digest-bound, stale fixture bytes fail before command credit, and the authority map credits exactly the current CR-01/WR-01 findings.</done>
</task>

<task type="auto">
  <name>Task 01-18-02: Record fresh evidence after byte stabilization and rerun independently</name>
  <files>.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md</files>
  <read_first>tools/verify_phase1_gap_evidence.py, .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-16-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md</read_first>
  <action>Precondition gate before any write: Plans 01-17 and 01-18-01 are complete, the full locked suite and Ruff pass, and no bound file (any `src/skillscout/**/*.py`, `tests/**/*.py`, both JSON fixtures, `pyproject.toml`, `tools/verify_phase1_gap_evidence.py`, `01-REVIEW.md`, `01-VERIFICATION.md`) changes after this point — this task itself must not edit `01-REVIEW.md` or `01-VERIFICATION.md`. First check the immutable inputs: `uv.lock` SHA-256 `caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32` and `tests/fixtures/state/v1-cli.db` SHA-256 `49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251`. From a newly created temporary working directory outside the repository, invoke the absolute repository-local uv with the full managed-Python/no-download/offline prefix to run `tools/verify_phase1_gap_evidence.py record` against `.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md`; the closed six-command registry (packaged smoke, current_findings — now the two current nodes, full pytest, Ruff, lock check, build) executes in per-command temporary roots with no network, credential, remote-write, or candidate-code action. The record rewrites the document with actual source digests (now including both JSON fixtures), node resolutions, normalized output digests, exits/counts, CLI facts, the two-finding matrix, and immutable pre/post hashes. Then, from a different new temporary working directory, run `verify --rerun` once; it must independently repeat and compare every command and exit 0 with the fixed success line. Recheck both authoritative hashes after record and after rerun. Do not modify the document or any bound file afterward; if any bound byte must change later, the evidence is stale by design and this task restarts. Commit the verifier/test changes (Task 01-18-01) separately from the generated evidence document so behavior changes and evidence recording remain atomic.</action>
  <acceptance_criteria>
    - The record command exits 0 and the regenerated document's canonical payload has `current_findings` keys exactly CR-01 and WR-01, both status pass, with the two bound node strings.
    - The regenerated `source_digests` includes `tests/fixtures/pipeline/approved.json` and `tests/fixtures/state/v1-cli-provenance.json` with their current SHA-256 values, and the total set is sorted and duplicate-free.
    - `verify --rerun` from an external working directory exits 0 and prints `phase1 gap evidence valid`; the document bytes are byte-identical before and after the rerun.
    - Both immutable SHA-256 values match at all three checkpoints (before record, after record, after rerun).
    - `.planning/config.json` remains user-owned, unmodified, unstaged, and absent from the evidence authority set.
  </acceptance_criteria>
  <verify><automated>repo_root="$(pwd -P)" &amp;&amp; test "$(shasum -a 256 "$repo_root/uv.lock" | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 "$repo_root/tests/fixtures/state/v1-cli.db" | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; acceptance_root="$(mktemp -d)" &amp;&amp; cd "$acceptance_root" &amp;&amp; UV_PYTHON_INSTALL_DIR="$repo_root/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$repo_root/.tools/uv-0.11.29/bin/uv" run --project "$repo_root" --locked python "$repo_root/tools/verify_phase1_gap_evidence.py" verify --rerun "$repo_root/.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md" &amp;&amp; test "$(shasum -a 256 "$repo_root/uv.lock" | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 "$repo_root/tests/fixtures/state/v1-cli.db" | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251"</automated></verify>
  <done>Current, fixture-complete, independently rerun evidence exists for the stabilized tree and no bound byte changes afterward.</done>
</task>

</tasks>

<prohibitions>
- Do not accept a document's claimed exit, count, node, digest, or source path without recomputation or an independent rerun.
- Do not include the document self-hash, record command, verifier command, or verifier outcome inside the evidence it validates.
- Do not record evidence before Plan 01-17 and Task 01-18-01 bytes are stable, and do not change any bound file (including 01-REVIEW.md and 01-VERIFICATION.md) after recording.
- Do not widen the source authority implicitly (no new globbed extensions or directories); the two JSON fixtures enter only as explicit literal paths.
- Do not normalize away arbitrary output, failure text, node names, exit status, or counts.
- Do not access the network, install or relock packages, call external services, write remotely, or execute candidate repository content.
- Do not implement or mark complete the separately deferred Phase-6 OS/syscall network-denial item.
- Do not read, stage, commit, hash into evidence, or modify `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reviewed fixture bytes → evidence authority | A stale fixture must invalidate evidence at the digest check before any command is credited. |
| Finding label → regression evidence | A current CR/WR identifier is authority only after AST resolution in digest-bound source and execution by the closed registry. |
| Bound documents → recorded evidence | Any post-record byte change to a bound file stales the evidence by design. |
| Temporary acceptance cwd → repository-local runtime | Caller location must not grant ambient tool, Python, network, or path authority. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-18-01 | Tampering | fixture byte drift | high | mitigate | Explicit closed source set binds both JSON fixtures; stale bytes fail the digest check before command credit, proven by zero-runner-call regressions. |
| T-01-18-02 | Repudiation | finding-map semantics | high | mitigate | Superseded seven-finding constants replaced; current CR-01/WR-01 nodes AST-resolved from bound bytes and executed by the registry. |
| T-01-18-03 | Tampering | post-record bound-byte change | high | mitigate | Record only after code/review/verification bytes stabilize; rerun is read-only; any later bound change stales evidence and forces re-record. |
| T-01-18-04 | Information Disclosure | captured output/evidence | medium | mitigate | Store only bounded normalized digests and allowlisted structured facts; existing canary tests cover streams and durable bytes. |
| T-01-18-SC | Tampering | dependency graph | high | mitigate | Exact pre/post lock and frozen-database hashes, `--locked`, offline mode, no install/relock operation. |
</threat_model>

## Artifacts this phase produces

- Updated `_source_paths` fixed literal in `tools/verify_phase1_gap_evidence.py`: two new explicit `Path` entries for the reviewed JSON fixtures.
- Replaced constant: `CURRENT_FINDING_NODES` with exactly `CR-01` and `WR-01` entries; renamed document section `## Current Two-Finding Matrix`; revised deferred-item prose.
- New test: `tests/test_phase1_evidence_verifier.py::test_stale_json_fixture_bytes_are_rejected_before_command_credit` (exact name, externally bound as the WR-01 node).
- New label: `PRIOR_REVIEW_FINDING_NODES` in `tests/test_phase1_gap_closure.py`; `CURRENT_REVIEW_FINDING_NODES` redefined as `("CR-01", "WR-01")`.
- Regenerated `.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md` (schema version 2, fixture-complete source digests, two-finding matrix).
- No new files, CLI flags, schema versions, or dependencies.

## Flagged Assumptions (specless edge probe — unresolved, review manually)

- OPS-01 edge (probe: unclassified): this plan assumes exact source/fixture byte binding plus independent rerun equivalence is sufficient audit evidence for OPS-01 this round; no new telemetry fields or audit surfaces are added.
- Chicken-and-egg sequencing: the current `01-REVIEW.md` (2026-07-19T11:44:44Z) and `01-VERIFICATION.md` (2026-07-19T11:55:07Z) bytes are the stable bound inputs at record time, per the verification's own instruction ("After code, review, and this verification report are stable, record fresh evidence…"). A later review or verification replacement deliberately stales this evidence and requires a fresh record — a standing property of the authority design, not a new defect.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Current, independently rerunnable acceptance evidence | 01-18 | COVERED | Verification gap 2 truth restored with fixture-complete authority. |
| REQ | OPS-01 | Versioned trustworthy audit evidence | 01-18 | COVERED | Bound bytes, nodes, outputs, and immutable inputs are recomputed and rerun. |
| REQ | OPS-04 | Recovery and dry-run claims under current evidence | 01-17, 01-18 | COVERED | CR-01 crash regression is credited by the new map and rerun. |
| RESEARCH | — | Deterministic-first, fail-closed, offline locked tooling | 01-18 | COVERED | No new dependency; registry stays closed and offline. |
| CONTEXT | — | No CONTEXT.md; verification missing items are the driver | 01-18 | COVERED | All three gap-2 missing items map to tasks 01-18-01/01-18-02. |

<verification>
- Authority gates: stale-fixture rejections (both JSON fixtures, zero runner calls), exact claim-set membership, and two-key finding map pass under the locked offline pytest prefix.
- Node gates: `_resolve_nodes` and the gap-closure definition tests resolve the crash-regression and fixture-authority nodes from digest-bound bytes.
- Freshness gate: record runs only after byte stabilization; `verify --rerun` exits 0 from an external cwd; the document is byte-identical across the rerun.
- Immutable gates: `uv.lock` and the frozen database hashes match before record, after record, and after rerun; `.planning/config.json` stays outside all operations.
</verification>

<success_criteria>
- Semantically neutral byte changes to either reviewed JSON fixture can no longer leave stale evidence accepted.
- The evidence credits exactly the current review's CR-01 and WR-01 findings through named, bound, freshly passing nodes.
- `verify --rerun` exits 0 from outside the repository against the stabilized tree.
- Verification gap 2 (WARNING) is ready for independent re-review; OPS-01 and OPS-04 evidence authority is current.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-18-SUMMARY.md` with the new source authority set (including both JSON fixtures), the two-finding matrix, record/rerun command lines with working directories, immutable pre/post hashes, and the byte-stability declaration.</output>
