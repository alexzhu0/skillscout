---
phase: 01-auditable-dry-run-spine
plan: "06"
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/state.py
  - tests/test_stage_contracts.py
  - tests/test_pipeline_resume.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "The same immutable producer/schema registry governs runtime acceptance, manifest writes, migration and manifest reads; unsupported producers fail before run creation."
    - "Every processor output is strict JSON, structurally bounded and below the canonical manifest byte cap before any temporary manifest file opens."
    - "Every failure after start_attempt closes or deterministically reconciles the durable attempt/run lifecycle without committing a result or checkpoint."
    - "Migration applies the same output and manifest bounds before creating migrated evidence."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "bounded JSON-only stage payload and supported producer/schema contract", exports: [StagePayload, SUPPORTED_PRODUCER_SCHEMAS] }
    - { path: src/skillscout/application/pipeline.py, provides: "whole-stage post-start lifecycle boundary" }
    - { path: src/skillscout/adapters/state.py, provides: "writer/reader-symmetric preflight and orphan reconciliation" }
    - { path: tests/test_stage_contracts.py, provides: "cap, depth, non-JSON and non-finite output regressions" }
    - { path: tests/test_pipeline_resume.py, provides: "CR-03, CR-04 and WR-01 lifecycle regressions" }
  key_links:
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/domain/models.py, via: "processor output validates as StagePayload before hashing", pattern: "StagePayload" }
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/domain/models.py, via: "write, migration and read use one producer/schema and manifest-byte contract", pattern: "SUPPORTED_PRODUCER_SCHEMAS" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "post-start failures close or reconcile attempt/run state", pattern: "fail_attempt" }
---

<objective>
Close the semantic and lifecycle half of verification root gap 2: CR-03, CR-04 and WR-01.

Purpose: Ensure SkillScout never reports success for evidence its own reader rejects, never writes unbounded processor output, and never leaves an inaccurate running lifecycle after a known stage failure.
Output: Shared producer support, bounded JSON payloads, pre-write manifest limits, and a complete post-start failure boundary.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-05-SUMMARY.md
@src/skillscout/application/pipeline.py
@src/skillscout/domain/models.py
@src/skillscout/adapters/state.py
@tests/test_stage_contracts.py
@tests/test_pipeline_resume.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-06-01: Define one bounded JSON and producer support contract</name>
  <files>src/skillscout/domain/models.py, src/skillscout/application/ports.py, tests/test_stage_contracts.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/application/ports.py, tests/test_stage_contracts.py, .planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md</read_first>
  <behavior>
    - StagePayload accepts only JSON primitives, bounded strings/keys/collections and finite numbers, with deterministic maximum nesting and node counts.
    - Bytes at the manifest cap are accepted and cap-plus-one is rejected before filesystem activity.
    - Sets, bytes, objects, non-string mapping keys, NaN and infinities are rejected through one closed stage-output error.
    - The supported producer/schema registry contains the Phase-1 fixture writer identities and is shared rather than duplicated.
  </behavior>
  <action>Write RED contract cases for cap, cap-plus-one, excessive nesting/node count, non-JSON objects, non-string keys and non-finite numbers. Add a strict frozen `StagePayload` contract that recursively validates JSON-only values and deterministic structural bounds before `StageEnvelope` construction; keep explicit nulls and stable canonical JSON. Define one immutable `SUPPORTED_PRODUCER_SCHEMAS` registry covering the accepted schema/producer pairs used by the frozen schema-1 migration and schema-2 writer. Add a closed `STAGE_OUTPUT_INVALID` failure code and fixed bounded ASCII summary for processor contract violations. Keep the manifest byte limit in one shared domain constant so writer and reader cannot drift. Do not weaken existing `StageEnvelope` strictness or introduce arbitrary serialization hooks.</action>
  <acceptance_criteria>
    - All JSON primitive cases have deterministic pass/fail results, and non-finite/non-JSON values never reach hashing or persistence.
    - Canonical manifest bytes exactly at the configured cap pass; one byte above fails with the closed output error.
    - Excessive depth, collection size, key length, string length and total nodes are bounded before recursion or storage can exhaust resources.
    - Producer/schema support exists in one importable registry used by later writer and reader code.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_stage_contracts.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain src/skillscout/application/ports.py tests/test_stage_contracts.py</automated></verify>
  <done>One strict contract bounds every Phase-1 output and one registry defines every producer/schema identity the system may both write and read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-06-02: Enforce writer-reader symmetry and close the complete stage lifecycle</name>
  <files>src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, tests/test_pipeline_resume.py, tests/test_state_integrity.py</read_first>
  <behavior>
    - Unsupported producer/schema identity is rejected before create_run and no run row is created.
    - Manifest canonical bytes are computed once, bounded and validated before any temporary file opens; read and migration use the same support registry and bound.
    - Output conversion, validation, hashing, envelope construction, manifest persistence and DB completion all sit inside the post-start failure boundary.
    - Known contract or persistence failures leave the attempt and run in a closed sanitized state with no result/checkpoint; an indeterminate DB failure is reconciled explicitly on next open.
  </behavior>
  <action>First reproduce the fixture-v2 success-then-inspect failure, oversized success-then-inspect failure, and running-attempt leak. At the beginning of `PipelineRunner.run`, validate the processor/schema pair against the shared registry before resumable lookup or run creation. Replace `dict(output)` with strict `StagePayload` validation. In `_write_manifest` and v1 migration, compute canonical envelope bytes once, enforce the shared byte cap before path creation/open, and pass those exact bytes through the atomic writer. Wrap the entire lifecycle after `start_attempt`—processor call, output validation, hashes, models, manifest and DB completion—in one classifier. Contract failures become `STAGE_OUTPUT_INVALID`; persistence failures retain the closed state code. When the DB remains writable, atomically close the running attempt/run without a result/checkpoint; when it cannot be updated, reconcile orphan running attempts deterministically on the next successful store open before any resume decision. Never overwrite the original failure with raw exception content.</action>
  <acceptance_criteria>
    - A processor with unsupported producer identity cannot create a run, result, manifest or publication plan.
    - Cap and invalid-output failures write no manifest/result/checkpoint and persist only the closed code/fixed summary when state remains writable.
    - Processor, conversion, validation, canonicalization, manifest and DB-completion failures no longer leave a known running attempt/run after the call returns.
    - Opening after an injected indeterminate DB failure reconciles exactly the orphan attempt without replaying or deleting prior evidence.
    - A writer-accepted completed run is immediately resumable and inspectable by the same support registry.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_stage_contracts.py tests/test_pipeline_resume.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_pipeline_resume.py</automated></verify>
  <done>Every accepted writer state is readable, every manifest is bounded before I/O, and every post-start failure has accurate durable lifecycle evidence or explicit next-open reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stage processor → StagePayload | Processor output is untrusted even for the deterministic fixture adapter. |
| Validated envelope → manifest filesystem | Bounded canonical bytes must be proven before local storage is allocated. |
| Running attempt → terminal stage outcome | Any failure after durable start must close or reconcile lifecycle state. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G2-01 | Tampering | producer/schema writer-reader contract | high | mitigate | One immutable registry gates runtime, migration, writes and reads before run creation. |
| T-01-G2-02 | Denial of Service | StagePayload/manifest writer | high | mitigate | JSON type, depth, node, collection and byte caps execute before opening output files. |
| T-01-G2-03 | Repudiation | attempt/run lifecycle | high | mitigate | Whole-stage exception boundary, atomic closed failures and next-open orphan reconciliation. |
| T-01-SC | Tampering | locked dependencies | high | mitigate | No dependency changes; exact lock and frozen-v1 hashes are rechecked with repository-local locked commands. |
</threat_model>

## Artifacts this phase produces

- Symbols/fields: `StagePayload`, shared manifest-byte/structure limits, `SUPPORTED_PRODUCER_SCHEMAS`, `STAGE_OUTPUT_INVALID`, next-open reconciliation path.
- Tests: producer rejection before run creation; cap/cap-plus-one; deep/non-JSON/non-finite payloads; migration bounds; post-processor and state failure lifecycle closure.
- Preserved files: `uv.lock` and `tests/fixtures/state/v1-cli.db` remain byte-identical to their authoritative hashes.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every stage yields durable inspectable structured evidence | 01-06 | COVERED | Writer/read symmetry and lifecycle accuracy close the semantic half of root gap 2. |
| REQ | OPS-01 | Versioned structured data and attempt errors | 01-06 | COVERED | Producer registry, payload contract and closed lifecycle fields. |
| REQ | OPS-04 | Finite safe recovery | 01-06 | COVERED | Failed attempts cannot remain ambiguously running. |
| RESEARCH | — | Strict Pydantic contracts and bounded local evidence | 01-06 | COVERED | Output becomes strict before canonicalization or I/O. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No deferred item is planned. |

<verification>
- Reproductions for CR-03, CR-04 and WR-01 fail before implementation and pass after it.
- Focused contract/recovery suites and Ruff pass with the exact uv prefix.
- Authoritative lock and frozen database hashes remain unchanged.
</verification>

<success_criteria>
- The system cannot create successful evidence that its reader immediately rejects.
- Untrusted output cannot exceed deterministic structural or byte budgets.
- Every known post-start failure is durably closed with sanitized evidence and no checkpoint advance.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-06-SUMMARY.md` with boundary tests, lifecycle rows and preserved hashes.</output>
