---
phase: 01-auditable-dry-run-spine
plan: "11"
type: execute
wave: 11
depends_on: ["01-10"]
files_modified:
  - tests/test_phase1_gap_closure.py
  - tools/verify_phase1_gap_evidence.py
  - .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "All CR-01 through CR-08 and WR-01 through WR-03 reproductions pass as deterministic local regressions; WR-04 remains explicitly deferred to Phase 6."
    - "The full locked test, Ruff, lock-check and build gates pass without changing uv.lock or the frozen schema-v1 fixture."
    - "Fresh happy, interruption/resume/inspect, changed A-prime and A/B/A acceptance flows produce structured evidence with zero remote writes and no disclosure canary."
    - "A re-verifier can map every original root gap and review finding to named tests and exact command/hash evidence."
    - "After all product/quality commands finish, a standalone stdlib verifier validates the machine-readable evidence block; standard pytest never reads the generated document."
  artifacts:
    - { path: tests/test_phase1_gap_closure.py, provides: "cross-root adversarial Phase-1 acceptance and static capability invariants" }
    - { path: tools/verify_phase1_gap_evidence.py, provides: "acyclic standalone stdlib evidence validator" }
    - { path: .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, provides: "re-verification evidence index for all gaps/findings" }
  key_links:
    - { from: 01-VERIFICATION.md, to: tests/test_phase1_gap_closure.py, via: "four root gaps have named acceptance tests", pattern: "root_gap" }
    - { from: 01-REVIEW.md, to: 01-GAP-VALIDATION.md, via: "CR/WR finding-to-test mapping", pattern: "CR-01" }
    - { from: pyproject.toml, to: uv.lock, via: "lock --check and exact Gate-B hash", pattern: "caeeddcf" }
    - { from: tools/verify_phase1_gap_evidence.py, to: 01-GAP-VALIDATION.md, via: "post-generation canonical JSON validation that excludes its own result", pattern: "evidence-json" }
---

<objective>
Prove the complete Phase-1 gap closure under the authoritative lock and frozen migration fixture, and leave a deterministic evidence index for independent re-verification.

Purpose: Ensure focused fixes compose correctly across the real CLI/SQLite/manifest path and that no blocker is hidden by isolated unit tests.
Output: Cross-root adversarial acceptance tests, full locked quality gates and `01-GAP-VALIDATION.md` mapping every finding to evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-10-SUMMARY.md
@tests/test_side_effect_policy.py
@tests/test_stage_contracts.py
@tests/test_pipeline_resume.py
@tests/test_state_integrity.py
@tests/test_cli_security.py
@tests/test_cli_dry_run.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-11-01: Add cross-root adversarial acceptance and capability invariants</name>
  <files>tests/test_phase1_gap_closure.py</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md, tests/test_side_effect_policy.py, tests/test_stage_contracts.py, tests/test_pipeline_resume.py, tests/test_state_integrity.py, tests/test_cli_security.py, tests/test_cli_dry_run.py, pyproject.toml</read_first>
  <behavior>
    - One cross-root flow proves immutable authority, bounded/durable evidence, strict inspect verification and exact identity resume compose in a single process.
    - Production source imports and package metadata contain no GitHub/OpenAI/HTTP client, shell/candidate execution or remote publication capability.
    - Fresh packaged-CLI tests cover happy/interruption/resume/inspect, changed A-prime dual inspect and A/B/A exact resume.
    - Finding-to-node references are checked by parsing test-module AST definitions; this test never launches or depends on nested pytest collection.
    - No test depends on network, credentials, external services, wall-clock ordering or mutable repository state.
  </behavior>
  <action>Create `tests/test_phase1_gap_closure.py` as a small integration/coverage layer, not a copy of every focused test. Add three explicitly named fresh packaged-CLI tests using the real console entry path: `test_packaged_cli_happy_interrupt_resume_inspect_gap_acceptance`, `test_packaged_cli_changed_a_prime_dual_inspect_gap_acceptance`, and `test_packaged_cli_a_b_a_exact_resume_gap_acceptance`. They must compose the sealed production runtime, bounded/durable writer, strict inspect and exact identity selection; assert structured outputs, all nine stages, verified reusable prefix, zero remote writes, no disclosure canary, distinct/dual-inspect A-prime runs and original-A reuse with B byte/row facts unchanged. Add deterministic AST/metadata assertions over `src/skillscout` and `pyproject.toml` that forbid production imports/declarations for network clients, socket/subprocess/shell, GitHub/OpenAI, candidate execution and remote publication.

Maintain an explicit CR-01..CR-08 and WR-01..WR-03 mapping to focused test module/function names and verify their definitions by parsing those test modules with `ast`; do not invoke pytest, subprocess pytest collection or pytest internals from a pytest test. No standard pytest test may open, parse, hash or require `01-GAP-VALIDATION.md`; document validation belongs exclusively to the standalone Task-2 verifier. Record WR-04 only as the Phase-6 OS/syscall denial item and do not implement or falsely claim it here.</action>
  <acceptance_criteria>
    - Cross-root acceptance passes entirely under temporary local files and the supported concrete production composition.
    - Capability scan checks parsed imports/project declarations rather than comments or broad textual false positives.
    - Finding mapping contains CR-01 through CR-08 and WR-01 through WR-03 exactly, and every referenced test function exists according to deterministic AST parsing.
    - The three packaged-CLI acceptance node IDs are independently runnable and do not depend on `01-GAP-VALIDATION.md`.
    - Standard pytest collection and execution are independent of the not-yet-generated planning evidence document.
    - WR-04 is not marked fixed and no OS/network sandbox dependency is added to Phase 1.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase1_gap_closure.py::test_packaged_cli_happy_interrupt_resume_inspect_gap_acceptance tests/test_phase1_gap_closure.py::test_packaged_cli_changed_a_prime_dual_inspect_gap_acceptance tests/test_phase1_gap_closure.py::test_packaged_cli_a_b_a_exact_resume_gap_acceptance tests/test_phase1_gap_closure.py::test_gap_finding_node_definitions_exist tests/test_phase1_gap_closure.py::test_production_capability_surface_remains_local_only</automated></verify>
  <done>All in-scope review findings are collected and exercised together without adding the explicitly deferred OS-level network proof.</done>
</task>

<task type="auto">
  <name>Task 01-11-02: Run the authoritative locked gates and publish the gap evidence index</name>
  <files>tools/verify_phase1_gap_evidence.py, .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-05-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-06-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-07-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-08-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-09-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-10-SUMMARY.md, pyproject.toml, uv.lock, tests/fixtures/state/v1-cli-provenance.json</read_first>
  <action>Create `tools/verify_phase1_gap_evidence.py` using only the Python standard library. It accepts exactly one document path, extracts one sentinel-delimited canonical JSON object, rejects duplicate/missing keys and non-canonical re-encoding, and validates schema version; exact pre/post lock and frozen-DB hashes; command IDs/argv summaries/zero exits/actual integer passed-or-collected counts; roots `1`..`4`; findings CR-01..CR-08 and WR-01..WR-03 with non-empty node IDs; sanitized CLI facts; and WR-04 exactly `{status: "deferred", addressed_in: "Phase 6"}`. It independently recomputes the two current repository hashes. It must reject any evidence command/result for itself and must not validate or require a hash of `01-GAP-VALIDATION.md`, so its own outcome and document bytes are outside the claims it validates.

Then follow one acyclic execution order. Before project code, require `uv.lock` SHA-256 exactly `caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32` and `tests/fixtures/state/v1-cli.db` SHA-256 exactly `49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251`; any mismatch stops and reopens the relevant human gate. Next run the three packaged-CLI nodes, AST capability/mapping nodes, focused CR/WR files, one explicit non-recursive `pytest --collect-only -q` over the seven Phase-1 test modules, full pytest, Ruff, lock check and build. Repeat both authoritative hash checks. Only after those completed facts exist, generate `01-GAP-VALIDATION.md`. Finally run the standalone verifier exactly once; do not rerun product/full tests to add the verifier result, and do not rewrite the document after verification.

Create the document from only the already completed pre-document facts: human-readable tables plus the sentinel JSON containing pre/post hashes, product/collection/quality command exits and counts, roots/findings mappings, packaged-CLI facts and WR-04 deferral. Do not include the standalone verifier invocation/outcome, a document self-hash, or any later command. Record actual results only; do not claim success for a command that did not run, omit a count/exit, or suppress a failing exit.</action>
  <acceptance_criteria>
    - Both authoritative hashes match before and after all commands; no lock or frozen fixture byte changes occur.
    - Focused adversarial suite, full pytest, Ruff, lock --check and build --no-sources all exit zero with the exact prefix.
    - Exact named packaged-CLI happy/interruption/resume/inspect, changed-A-prime and A/B/A nodes all execute and pass during the automated verify, not only in prose.
    - The standalone stdlib verifier checks actual pre-document command exits/counts, both pre/post hashes, roots 1..4, CR-01..08, WR-01..03 and exact WR-04 Phase-6 deferral.
    - No standard pytest test reads the evidence document, and the evidence omits verifier outcome and document self-hash.
    - Fresh happy/resume/inspect output is structured, verified, disclosure-free and reports zero remote writes.
    - `01-GAP-VALIDATION.md` maps all four root gaps and CR-01..08/WR-01..03 to named passing evidence and marks WR-04 deferred to Phase 6 without ambiguity.
    - No external service, credential, remote write or candidate repository code is accessed or executed.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase1_gap_closure.py::test_packaged_cli_happy_interrupt_resume_inspect_gap_acceptance tests/test_phase1_gap_closure.py::test_packaged_cli_changed_a_prime_dual_inspect_gap_acceptance tests/test_phase1_gap_closure.py::test_packaged_cli_a_b_a_exact_resume_gap_acceptance &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked python tools/verify_phase1_gap_evidence.py .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md</automated></verify>
  <done>The gap closure has full locked regression evidence and an independent verifier can trace each original blocker to an exact passing command/test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Focused fixes → composed CLI runtime | Individually green controls may conflict when exercised end to end. |
| Test output → re-verification claim | Evidence must reflect actual commands and exact immutable inputs. |
| Deferred WR-04 → Phase-1 status | Deferral must not be misrepresented as a Phase-1 fix or substitute for authority sealing. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-FINAL-01 | Repudiation | gap evidence index | high | mitigate | Record exact hashes, actual commands/exits and named test nodes; never suppress failures. |
| T-01-FINAL-02 | Elevation of Privilege | composed runtime | critical | mitigate | Cross-root acceptance plus parsed capability/dependency checks and zero-remote-write CLI evidence. |
| T-01-FINAL-03 | Information Disclosure | CLI/inspect/evidence docs | high | mitigate | Secret/path canaries and strict projection tests cover emitted and durable outputs. |
| T-01-FINAL-04 | Tampering | Gate-B/frozen inputs | high | mitigate | Exact pre/post SHA-256 checks; any byte change stops execution and invalidates approval. |
</threat_model>

## Artifacts this phase produces

- File: `tests/test_phase1_gap_closure.py` with three named packaged-CLI flows, parsed capability invariants and AST node mapping; it has no evidence-document dependency.
- File: `tools/verify_phase1_gap_evidence.py`, a standalone stdlib validator run only after evidence generation and excluded from its claims.
- File: `.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md` with actual pre/post hashes, command exits/counts, machine-readable root/findings matrix and WR-04 deferral.
- Evidence: full pytest/Ruff/lock/build gates; fresh happy, interrupt/resume/inspect; changed A-prime and A/B/A results; zero remote writes and no disclosure canary.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete auditable/resumable local pipeline with architectural no-remote boundary | 01-11 | COVERED | Cross-root and full locked acceptance prove composition. |
| REQ | OPS-01 | Structured, versioned, inspectable stage/attempt evidence | 01-11 | COVERED | Full suite and evidence index map every root gap. |
| REQ | OPS-04 | Finite exact resume and local-only publication plan | 01-11 | COVERED | Happy, interruption/resume, A-prime and A/B/A all run. |
| RESEARCH | — | Locked commands, deterministic fixtures, no remote adapters/dependencies | 01-11 | COVERED | Exact prefixes/hashes and parsed capability checks are mandatory. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | WR-04 remains in its approved Phase-6 scope. |

<verification>
- All in-scope CR/WR findings map to named passing tests.
- Full locked quality/build gates pass with authoritative hashes unchanged.
- Re-verification evidence is self-contained and records actual results.
- WR-04 is explicitly deferred to Phase 6 and is not counted as a Phase-1 blocker or fix.
</verification>

<success_criteria>
- All four structured root gaps are closed with deterministic evidence.
- CR-01..CR-08 and WR-01..WR-03 are regression protected.
- OPS-01 and OPS-04 are ready for independent phase re-verification.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-11-SUMMARY.md` with actual full-gate outputs, root-gap matrix and authoritative pre/post hashes.</output>
