---
phase: 01-auditable-dry-run-spine
plan: "08"
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/domain/canonical.py
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/adapters/state.py
  - tests/test_stage_contracts.py
  - tests/test_pipeline_resume.py
  - tests/test_state_integrity.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Each persisted stage result has a run-scoped row identity distinct from its non-unique semantic result digest, so convergent outputs across runs cannot collide."
    - "Fresh runs persist their complete reusable run identity before the first attempt; resumable lookup queries exact identity rather than only subject and recency."
    - "Migrated v1 runs are explicitly legacy_unbound: they cannot authorize resume or full inspect until the current fixture supplies an expected RunIdentity that validates and binds transactionally."
    - "A-interrupt, B-interrupt, A-rerun resumes the original A run and leaves B untouched."
    - "A changed fixture with the same subject completes in a new run and both old/new runs remain independently inspectable."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "strict complete run identity", exports: [RunIdentity] }
    - { path: src/skillscout/application/ports.py, provides: "provider-independent typed state lookup, binding and checkpoint contracts" }
    - { path: src/skillscout/domain/canonical.py, provides: "run-scoped result-row identity", exports: [make_result_row_id] }
    - { path: src/skillscout/adapters/state.py, provides: "collision-free result associations and exact-identity resume query" }
    - { path: tests/test_pipeline_resume.py, provides: "CR-02 and WR-03 A-prime/A-B-A regression evidence" }
  key_links:
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/domain/models.py, via: "RunIdentity is computed before create/find", pattern: "RunIdentity" }
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/domain/canonical.py, via: "result_row_id keys run-stage association while result_id remains semantic", pattern: "result_row_id" }
    - { from: find_resumable_run, to: runs, via: "exact schema/subject/fixture/producer/retry identity predicate", pattern: "fixture_hash" }
    - { from: bind_legacy_run, to: RunIdentity, via: "expected current identity proves and atomically binds legacy_unbound before reuse/inspect", pattern: "legacy_unbound" }
---

<objective>
Close verification root gap 4 and its CR-02/WR-03 causes by separating semantic result identity from row ownership and selecting resumable runs by exact identity.

Purpose: Make legitimate input/producer/policy evolution collision-free while preserving deterministic semantic hashes and exact checkpoint recovery.
Output: Complete run identity, run-scoped result rows, migrated associations, and changed-input plus A/B/A regressions.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-07-SUMMARY.md
@src/skillscout/domain/models.py
@src/skillscout/domain/canonical.py
@src/skillscout/application/ports.py
@src/skillscout/application/pipeline.py
@src/skillscout/adapters/state.py
@tests/test_pipeline_resume.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-08-01: Separate semantic result digest from run-scoped row identity</name>
  <files>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/state.py, tests/test_stage_contracts.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/state.py, tests/test_stage_contracts.py, tests/test_state_integrity.py, tests/fixtures/state/v1-cli-provenance.json</read_first>
  <behavior>
    - result_id remains the existing semantic digest and may repeat across runs.
    - result_row_id is deterministic for one run/stage association, differs across runs and is the primary/foreign-key target used by checkpoints.
    - One run still has at most one successful result per closed stage and each result still belongs to exactly one succeeded attempt.
    - Frozen schema-1 semantic result IDs migrate byte-for-byte while receiving new run-scoped row IDs.
  </behavior>
  <action>Write identity and migration RED cases first. Add `make_result_row_id(run_id, stage)` with a documented canonical preimage that is distinct from `make_result_id`. Change schema creation and v1 migration so `stage_results.result_row_id` is the primary key, semantic `result_id` is indexed but not unique, `(run_id, stage)` and `attempt_id` remain unique, and checkpoints reference `result_row_id` while retaining semantic `result_id` for audit projection. Update insert, join, migration, locator and foreign-key logic consistently; do not include run_id in the semantic result digest or mutate frozen schema-1 identities. Extend strict models/projections with `result_row_id` where persisted association identity is required.</action>
  <acceptance_criteria>
    - Two runs with identical stage semantics have equal result_id and distinct result_row_id without an integrity error.
    - Checkpoint foreign keys target the correct run-scoped row and cannot reference another run's semantic twin.
    - Duplicate stage within one run and duplicate attempt association are still rejected.
    - Frozen schema-1 database hash remains unchanged; a migrated copy preserves all six legacy result_id values and passes foreign-key checks.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_stage_contracts.py tests/test_state_integrity.py tests/test_pipeline_resume.py -k "identity or result or migration"</automated></verify>
  <done>Semantic content identity is reusable audit data, while every persisted run/stage association has a collision-free row identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-08-02: Persist complete run identity and resume the exact A/B/A candidate</name>
  <files>src/skillscout/domain/models.py, src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py, .planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md</read_first>
  <behavior>
    - RunIdentity contains schema_version, subject_id, fixture_hash, producer_version and retry_policy_version and is persisted before the first attempt for every fresh run.
    - find_resumable_run selects the newest unfinished exact identity, not merely the newest row for a subject.
    - A migrated legacy_unbound run fails full inspect and cannot grant checkpoint reuse; exact current identity binds it transactionally, while a wrong identity leaves it unbound and unchanged.
    - A-interrupt/B-interrupt/A-rerun returns A's run_id, reuses A's verified stages and does not change B.
    - A changed A-prime run completes with zero reuse, and both the old interrupted A and completed A-prime inspect successfully.
  </behavior>
  <action>Add changed-A-prime, A/B/A and legacy binding tests before implementation, using the same subject ID and deliberate downstream semantic convergence. Introduce strict `RunIdentity`; compute it before any store mutation and pass it to typed state operations. Extend the runs schema with complete identity columns, `identity_state` constrained to `bound|legacy_unbound`, and an exact lookup index. Fresh rows require `bound` plus every non-null identity field.

Frozen v1 has no fixture_hash, so migration must set `identity_state='legacy_unbound'`, a null fixture hash and only the producer/retry facts provable from its attempt rows. This state may expose only a fixed `STATE_IDENTITY_UNBOUND` failure for full inspect and may not authorize resume/checkpoint reuse. Add `bind_legacy_run(expected: RunIdentity)`: search a bounded ordered set of unfinished legacy candidates matching the available subject/producer/retry/schema facts; reconstruct every StageInput using the supplied fixture hash, verify input/reusable/output/schema-specific semantic result/manifest/order/cardinality facts available at this wave, and update fixture hash plus identity_state only inside the same serialized snapshot transaction. A wrong expected identity returns no match and leaves bytes/rows unchanged. After exact binding, rerun verification and return a typed run/checkpoint for Validators-first recovery. Plan 10 will consolidate this exact binding proof into `verify_run_chain`; do not claim a complete `VerifiedRunChain` before binding.

Update `StateStore` in `application/ports.py` so `create_run` and `find_resumable_run` accept/return strict `RunIdentity`/`RunRecord`, `bind_legacy_run` is explicit, `latest_checkpoint` returns `Checkpoint | None`, and lifecycle reconciliation is a provider-independent operation. No protocol method may expose `sqlite3.Row`, raw mappings or SQL-specific suffixes. Never treat a mismatch as corruption or mutate the mismatching run; continue searching exact eligible candidates or create a new run. Preserve retry budgets keyed by reusable digest.</action>
  <acceptance_criteria>
    - Exact-identity lookup uses the declared index and does not stop after a newer mismatching subject row.
    - A/B/A resumes A with the expected reused count and byte-identical reused hashes; B status/checkpoint/updated_at remain unchanged.
    - Changed A-prime completes with a distinct run ID and no row collision; inspect verifies both runs independently.
    - Producer and retry-policy identity changes create/select their own runs and budgets without replaying another identity.
    - Legacy migrated v1 binding occurs only after canonical proof and is transactional.
    - Inspect-before-binding returns only STATE_IDENTITY_UNBOUND; wrong binding preserves exact pre-bind bytes; exact binding enables post-binding inspect and Validators-first resume.
    - StateStore protocol signatures match the concrete identity lookup/binding/checkpoint/reconciliation APIs and expose only domain models.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py -k "changed or identity or resume or migration" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_pipeline_resume.py</automated></verify>
  <done>Recovery selects exact identity across A/B/A history and valid evolution cannot collide with or corrupt earlier evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Semantic digest → SQLite row ownership | Equal semantic content across runs must not imply one row belongs to multiple runs. |
| Current fixture identity → resumable history | Subject ID alone is insufficient authority to reuse checkpoints. |
| Legacy v1 run → exact v2 identity | Missing legacy fixture identity may be bound only after full canonical verification. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G4-01 | Tampering / Repudiation | stage_results/checkpoints | high | mitigate | Run-scoped result_row_id owns associations; semantic result_id remains non-unique verified content identity. |
| T-01-G4-02 | Spoofing | resumable-run selection | high | mitigate | Persist and query complete RunIdentity with exact index; verify before legacy binding. |
| T-01-G4-03 | Denial of Service | valid identity evolution | high | mitigate | Changed inputs and convergent output digests cannot trigger global PK collisions. |
| T-01-G4-04 | Spoofing / Tampering | legacy_unbound binding | high | mitigate | No resume/full inspect before binding; wrong identity is non-mutating; supplied current identity must verify every reconstructible chain fact in one serialized transaction. |
| T-01-SC | Tampering | frozen migration authority | high | mitigate | Frozen v1 and lock hashes are checked; fixture bytes are never modified. |
</threat_model>

## Artifacts this phase produces

- Symbols/fields: `RunIdentity`, `make_result_row_id`, `runs.fixture_hash`, `runs.producer_version`, `runs.retry_policy_version`, `runs.identity_state`, `stage_results.result_row_id`, `checkpoints.result_row_id`, `STATE_IDENTITY_UNBOUND`.
- Port APIs: typed `create_run`, exact `find_resumable_run`, `bind_legacy_run`, typed `latest_checkpoint` and lifecycle reconciliation; no SQLite row leakage.
- Indexes/constraints: exact resumable-run identity index; non-unique semantic result index; unique run-stage and attempt associations.
- Tests: semantic-twin rows across runs, changed A-prime dual inspect, A-interrupt/B-interrupt/A-resume, producer/policy identity isolation, inspect-before-binding, wrong binding, exact binding, post-binding inspect and Validators-first legacy resume.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Resume from the latest matching successful checkpoint | 01-08 | COVERED | Exact identity replaces subject-only newest-row selection. |
| REQ | OPS-01 | Stable IDs and hashes remain auditable | 01-08 | COVERED | Semantic and association identities are explicit and independently verified. |
| REQ | OPS-04 | Recovery without replay or collision | 01-08 | COVERED | Changed A-prime and A/B/A are direct regressions. |
| RESEARCH | — | Canonical five-field reuse identity and preserved migration facts | 01-08 | COVERED | Run lookup aligns with the existing reusable-key contract without mutating v1 facts. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No remote provider scope is introduced. |

<verification>
- CR-02 and WR-03 reproductions pass as explicit changed-A-prime and A/B/A tests.
- Legacy migration is explicitly unbound until current fixture identity is supplied; pre-bind inspect/resume, wrong bind, exact bind and post-bind flows are tested.
- Frozen v1 semantic IDs and database hash remain unchanged; migrated associations pass FK checks.
- Focused identity/migration/recovery suites and Ruff pass using the exact uv prefix.
</verification>

<success_criteria>
- Verification root gap 4 is closed for collisions and exact resume selection.
- Every run can own semantically identical results without ambiguity.
- Exact A identity is recovered even when a newer B run exists for the same subject.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-08-SUMMARY.md` with schema association facts, A-prime/A-B-A results and preserved hashes.</output>
