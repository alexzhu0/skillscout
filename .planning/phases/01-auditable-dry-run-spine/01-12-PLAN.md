---
phase: 01-auditable-dry-run-spine
plan: "12"
type: execute
wave: 12
depends_on: ["01-11"]
files_modified:
  - src/skillscout/adapters/localfs.py
  - src/skillscout/adapters/state.py
  - tests/test_state_integrity.py
  - tests/test_cli_security.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "A state mutation never returns failure after its replacement snapshot is the durable authority; reopen observes the same outcome the caller received."
    - "A state filename that aliases its derived manifest directory is rejected before any state, lock, or manifest entry is created."
    - "Existing state and manifest files are trusted only when they are private, single-link regular files owned by the current runtime identity."
    - "Filesystem integrity failures expose only the closed state diagnostic and never an operator-selected path or rejected value."
  artifacts:
    - { path: src/skillscout/adapters/localfs.py, provides: "truthful post-commit backup retirement and private-file validation" }
    - { path: src/skillscout/adapters/state.py, provides: "collision-free state/manifest namespace admission" }
    - { path: tests/test_state_integrity.py, provides: "post-commit fault injection plus mode/link/owner regressions" }
    - { path: tests/test_cli_security.py, provides: "sanitized namespace-collision CLI regression" }
  key_links:
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/adapters/localfs.py, via: "snapshot promotion occurs only after atomic_write reaches its durable replacement commit point", pattern: "atomic_write" }
    - { from: src/skillscout/adapters/localfs.py, to: existing state and manifest bytes, via: "pre-open and descriptor metadata must both satisfy the private regular-file policy", pattern: "st_nlink" }
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/cli.py, via: "early collision and permission failures map to fixed SafeFailure diagnostics", pattern: "STATE_INTEGRITY_ERROR" }
---

<objective>
Close CR-01, WR-03, and WR-04 at the local persistence boundary.

Purpose: Make durable mutation outcomes truthful, keep the state and manifest namespaces disjoint, and prevent writable or foreign-owned existing evidence from becoming audit authority.
Output: Commit-point-aware atomic replacement, early namespace rejection, private-file admission, and deterministic fault/permission regressions.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-07-SUMMARY.md
@src/skillscout/adapters/localfs.py
@src/skillscout/adapters/state.py
@tests/test_state_integrity.py
@tests/test_cli_security.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-12-01: Make snapshot replacement outcomes agree with reopened authority</name>
  <files>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py, tests/test_pipeline_resume.py, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md</read_first>
  <behavior>
    - Failure before replacement-directory durability returns the closed state failure and reopen observes the prior snapshot.
    - Once replacement bytes and the containing directory are synced, the mutation returns success and the in-memory candidate is promoted even if backup retirement fails.
    - Failure injected immediately after backup unlink cannot make the API report rollback; reopen contains exactly the committed mutation.
    - A retained or crash-resurrected backup is housekeeping only and can never supersede a valid authoritative target.
  </behavior>
  <action>Write RED fault-injection tests around the exact backup-unlink and backup-directory-sync seams. Refactor `AnchoredDirectory.atomic_write` so the authoritative commit point is the successful target file fsync, rename, and containing-directory fsync performed by `_atomic_write_once`. Add an `after_backup_unlink` seam and a non-throwing `_retire_backup_after_commit` path: after the commit point, backup unlink or cleanup sync failure is recoverable housekeeping and must not raise an outcome that `_snapshot_transaction` interprets as rollback. Preserve fatal behavior and restoration for every failure before the target directory sync. On startup and the next write, ignore or safely retire a stale backup only after the target passes the normal anchored validation; never restore a backup over a valid target merely because cleanup was interrupted. Keep `_snapshot_transaction` promotion ordered after the durable commit and prove the promoted connection and reopened bytes contain the same row set.</action>
  <acceptance_criteria>
    - The focused reproduction that previously raised after backup unlink now returns success and the requested row is present after reopen exactly once.
    - Every pre-commit file/rename/directory-sync injection still returns `state_operation_failed`, restores the prior authoritative bytes, and requires reopen where already established.
    - No post-commit cleanup exception poisons the live store or invites a duplicate retry.
    - Backup artifacts are never selected as authority while a valid target exists.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py -k 'snapshot or backup_cleanup or failed_snapshot' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/adapters/localfs.py src/skillscout/adapters/state.py tests/test_state_integrity.py</automated></verify>
  <done>Returned mutation outcomes and reopened snapshot authority agree at every injected durability seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-12-02: Reject namespace collisions and non-private existing evidence</name>
  <files>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, src/skillscout/cli.py, tests/test_state_integrity.py, tests/test_cli_security.py</read_first>
  <behavior>
    - A state path whose derived manifest root is the same path fails before parent creation, lock acquisition, or snapshot creation.
    - Existing state and manifest files must be regular, non-symlink, single-link, current-owner files with no group/other permission bits at both stat and fstat.
    - Unsafe existing state maps to the fixed incompatible/integrity boundary without reading SQLite bytes; unsafe manifests fail before JSON decoding.
    - Platforms unable to establish owner identity fail closed; mode and link-count tests remain deterministic on every supported runtime.
  </behavior>
  <action>Add a single private-file predicate in `adapters/localfs.py` and apply it to both metadata snapshots in `AnchoredDirectory.read_bytes`; require regular type, no symlink, one link, the current effective user identity, and mode `0600` or stricter with no group/other bits. Reuse the same predicate for the retained lock check instead of maintaining a divergent state-only helper. Treat a missing portable owner primitive as a closed secure-owner-check failure. In `SQLiteStateStore.__init__`, compare the absolute state path with its derived manifest root before opening or creating the parent and reject equality through the fixed integrity diagnostic. Preserve the established manifest naming for non-colliding paths. Write real chmod and hard-link tests for state and manifest files, a deterministic simulated foreign-owner test at the predicate boundary, and a packaged CLI case using a canary-bearing `.manifests` state filename; assert exit failure, no state/lock/directory creation, and no canary in stdout or stderr.</action>
  <acceptance_criteria>
    - The `.manifests` collision case leaves the temporary parent byte-for-byte free of state, lock, and manifest artifacts.
    - Group-readable, group/other-writable, multiply linked, foreign-owned, symlink, and non-regular existing snapshots are rejected before deserialization.
    - Existing manifest permission/link/owner violations are rejected before their content is parsed or projected.
    - Newly written state, lock, backup, temporary, and manifest files continue to use private modes and pass reopen.
    - CLI diagnostics contain only an allowlisted code and fixed bounded summary.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_cli_security.py -k 'permission or private or owner or hardlink or namespace or manifests' &amp;&amp; test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251"</automated></verify>
  <done>Only disjoint, private, single-owner local files can become state or manifest authority, with sanitized early rejection.</done>
</task>

</tasks>

<prohibitions>
- Do not change `uv.lock` or the frozen schema-version-1 database.
- Do not install dependencies, access the network, clone repositories, or execute candidate content.
- Do not add a remote adapter, branch/PR operation, merge path, or default-branch write capability.
- Do not implement the Phase-6 OS/syscall network-denial item; the current WR-04 is strictly the ownership/mode finding.
- Do not stage or commit `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Candidate in-memory SQLite snapshot → durable state child | The return value must reflect the generation that reopen will select. |
| Existing local file → trusted state/manifest bytes | Local path selection does not grant ownership or integrity authority. |
| CLI state path → state/manifest namespace | An operator-controlled leaf can collide with an internally derived directory. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12-01 | Repudiation | `atomic_write` / `_snapshot_transaction` | high | mitigate | Define one durable replacement commit point; post-commit backup retirement cannot report rollback. |
| T-01-12-02 | Tampering | existing state and manifest files | high | mitigate | Require matching private regular single-link stat/fstat metadata before any read or decode. |
| T-01-12-03 | Information Disclosure | collision/permission diagnostics | high | mitigate | Map every rejection to the closed fixed diagnostic without selected path/value interpolation. |
| T-01-12-04 | Elevation of Privilege | local-only runtime boundary | critical | mitigate | Preserve the sealed none/local-state composition and introduce no network or remote write path. |
| T-01-12-SC | Tampering | approved supply-chain inputs | high | mitigate | Recheck exact lock and frozen-database SHA-256 values; make no dependency change. |
</threat_model>

## Artifacts this phase produces

- Symbol: `AnchoredDirectory._require_private_regular` as the single state/manifest/lock file-admission policy.
- Symbol: `AnchoredDirectory._retire_backup_after_commit` and fault seam `after_backup_unlink` for post-commit housekeeping.
- Behavior: early `SQLiteStateStore` namespace-equality rejection before filesystem creation.
- Tests: `test_post_commit_backup_cleanup_failure_returns_success_and_reopen_observes_mutation`, `test_state_manifest_namespace_collision_is_rejected_before_creation`, `test_existing_state_and_manifest_require_private_single_owner_files`, and `test_cli_rejects_colliding_state_name_without_disclosure`.
- Preserved artifacts: `uv.lock` and `tests/fixtures/state/v1-cli.db` retain their locked bytes.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Auditable, recoverable local state | 01-12 | COVERED | Mutation outcomes, namespaces, and existing evidence authority become truthful. |
| REQ | OPS-01 | Trusted structured audit state | 01-12 | COVERED | Unsafe or ambiguous local evidence cannot enter inspectable state. |
| REQ | OPS-04 | Retry/reopen agrees with durable outcome | 01-12 | COVERED | Callers never retry a mutation falsely reported as rolled back. |
| RESEARCH | — | Descriptor-anchored private local persistence | 01-12 | COVERED | Existing-file admission now matches the retained-lock policy. |
| CONTEXT | — | No CONTEXT.md; defaults approved | — | N/A | The Phase-6 network-denial deferral is excluded. |

<verification>
- RED reproductions fail on the current post-unlink exception, collision, and unsafe-mode paths, then pass after implementation.
- Focused state/CLI suites and Ruff pass through the exact repository-local locked runtime.
- Lock and frozen database hashes remain authoritative and unchanged.
</verification>

<success_criteria>
- No supported fault can make a returned mutation outcome disagree with reopened state.
- Colliding state/manifest namespaces are rejected before mutation.
- Existing state and manifests cannot be group/other accessible, multiply linked, or foreign-owned.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-12-SUMMARY.md` with fault matrix, permission matrix, collision evidence, and preserved hashes.</output>
