---
phase: 01-auditable-dry-run-spine
plan: "09"
type: execute
wave: 9
depends_on: ["01-08"]
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/state.py
  - tests/test_state_integrity.py
  - tests/test_cli_security.py
  - tests/test_pipeline_resume.py
autonomous: true
gap_closure: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Opening a user_version=2 database validates the exact tables, columns, types, nullability, keys, constraints, foreign keys and indexes plus SQLite integrity before any read/write."
    - "Run, attempt and checkpoint rows pass strict status/error/telemetry models before public projection."
    - "Persisted error_code must be a closed ErrorCode with its exact fixed summary; any mismatch fails with STATE_INTEGRITY_ERROR without echoing stored content."
    - "v1 migration validates diagnostics before copying and cannot create new durable or CLI output containing a credential/path canary."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "strict persisted run/attempt/checkpoint projection contracts", exports: [PersistedRunRecord, PersistedAttemptRecord, PersistedCheckpointRecord] }
    - { path: src/skillscout/adapters/state.py, provides: "exact schema fingerprint and sanitized persisted projection" }
    - { path: tests/test_state_integrity.py, provides: "WR-02 exact-schema malformed-v2 matrix" }
    - { path: tests/test_cli_security.py, provides: "CR-06 persisted diagnostic canary matrix" }
  key_links:
    - { from: SQLiteStateStore.__init__, to: _validate_current_schema, via: "exact fingerprint, quick_check and foreign_key_check before acceptance", pattern: "quick_check" }
    - { from: SQLiteStateStore.inspect_run, to: src/skillscout/domain/models.py, via: "strict row models precede JSON projection", pattern: "PersistedRunRecord" }
    - { from: persisted error_code, to: ERROR_SUMMARIES, via: "exact allowlisted code-summary equality", pattern: "STATE_INTEGRITY_ERROR" }
---

<objective>
Close the schema and diagnostic-projection half of verification root gap 3: WR-02 and CR-06.

Purpose: Reject subtly incompatible ledgers at open and ensure corrupted or legacy diagnostics never flow verbatim into `inspect-run`.
Output: Exact schema fingerprint validation, strict persisted row models, fixed diagnostic projection and tampering/migration canary tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-08-SUMMARY.md
@src/skillscout/domain/models.py
@src/skillscout/adapters/state.py
@tests/test_state_integrity.py
@tests/test_cli_security.py
@tests/test_pipeline_resume.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-09-01: Validate the exact schema-v2 fingerprint and database integrity</name>
  <files>src/skillscout/adapters/state.py, tests/test_state_integrity.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/adapters/state.py, tests/test_state_integrity.py, tests/test_pipeline_resume.py, tests/fixtures/state/v1-cli-provenance.json, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md</read_first>
  <behavior>
    - Exact expected table_info includes every column name/order/type/notnull/default/pk position.
    - Exact foreign keys and indexes include columns/order/uniqueness/origin/partial flags; required CHECK and UNIQUE constraints match normalized CREATE SQL.
    - quick_check returns only ok and foreign_key_check is empty before the database is accepted.
    - Missing extra-subset columns, changed nullability/type/default, absent unique/check/FK/index, extra shadow definitions and corrupted pages fail at open as STATE_SCHEMA_INCOMPATIBLE without mutation.
  </behavior>
  <action>Write malformed schema-2 fixtures for every WR-02 omission before implementation. Replace subset column checks with a versioned expected fingerprint assembled from explicit immutable descriptors for the post-01-08 schema. Compare ordered `PRAGMA table_info`, `foreign_key_list`, `index_list` and `index_info` results; compare normalized trusted `sqlite_master.sql` for constraints that PRAGMA does not fully expose. Require `PRAGMA quick_check` to return exactly one `ok` row and `foreign_key_check` to return none. Reject unexpected/missing tables, indexes or constraints that affect ledger semantics. Run the same exact validation after v1 migration before committing user_version=2. Never repair or recreate an incompatible existing database.</action>
  <acceptance_criteria>
    - Every malformed-v2 variant is rejected during store construction before inspect, migration fallback or state mutation.
    - A newly created database and a migrated frozen-v1 copy match the identical expected schema fingerprint.
    - Required run identity/result-row/retry indexes and all FK targets from Plan 08 are covered explicitly.
    - quick_check and foreign_key_check failures map only to STATE_SCHEMA_INCOMPATIBLE without raw SQLite text.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_pipeline_resume.py -k "schema or malformed or migration or index or constraint or integrity"</automated></verify>
  <done>user_version alone is no longer trusted; only the exact versioned schema and an internally consistent database are accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-09-02: Parse persisted rows through strict sanitized projections</name>
  <files>src/skillscout/domain/models.py, src/skillscout/adapters/state.py, tests/test_cli_security.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/adapters/state.py, src/skillscout/application/ports.py, src/skillscout/domain/enums.py, tests/test_cli_security.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py</read_first>
  <behavior>
    - Run/attempt/checkpoint row types are strict, extra-free and validate closed status, index, identifiers, booleans, timestamps and nullable telemetry coherence.
    - Error fields are both null or a valid closed code plus its exact ERROR_SUMMARIES value; status/error/retryability combinations are legal.
    - Fixture-v1 telemetry fields that have no provider meaning remain null; attacker-controlled request/model fields cannot be projected.
    - Any row mismatch returns only state_integrity_error and its fixed summary; stored canary bytes never appear in stdout/stderr or newly produced artifacts.
  </behavior>
  <action>Add direct DB-tampering cases for run and attempt code, summary, status/error coherence, retryability, telemetry and identifier bounds, plus a copied legacy-v1 credential/path canary. Add strict `PersistedRunRecord`, flattened `PersistedAttemptRecord` and `PersistedCheckpointRecord` models with cross-field validators; keep output fields required and explicit even when null. In `state.py`, validate the code string against `ErrorCode` and require exact `ERROR_SUMMARIES` equality before building any public projection; perform producer-specific telemetry validation so fixture-v1 accepts only its declared null telemetry. Replace every `dict(row)` public path with validated model dumps. Apply the same diagnostic checks to v1 source rows before copying; on failure rollback without new manifests/schema and never echo the offending value. Keep original tampered source bytes untouched as evidence rather than trying to sanitize in place.</action>
  <acceptance_criteria>
    - Tampering any persisted diagnostic/status/telemetry field makes inspect fail with the fixed state_integrity_error only.
    - Credential, raw exception and attacker-path canaries are absent from stdout, stderr, manifests, publication plans and any migrated copy output; the source fixture itself is excluded from the leak scan and remains byte-identical.
    - Valid happy, interrupted, failed, abandoned and planned-not-published records project with the complete OPS-01 field set and explicit nulls.
    - Migration rejects invalid legacy diagnostics transactionally before user_version changes or new durable evidence remains.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_security.py tests/test_pipeline_resume.py tests/test_state_integrity.py -k "diagnostic or canary or inspect or migration or error or telemetry" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_cli_security.py</automated></verify>
  <done>Persisted diagnostics become trusted only through strict closed validation and cannot disclose corrupted or legacy content through inspect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing SQLite bytes → accepted schema | user_version and a few columns cannot establish structural compatibility. |
| Persisted rows → inspect JSON | Database values may be corrupt, legacy, or accidentally secret-bearing. |
| v1 source rows → migrated evidence | Migration must not bless unsafe diagnostics into new durable surfaces. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G3-01 | Tampering | schema-v2 open | high | mitigate | Exact table/FK/index/constraint fingerprint plus quick_check and foreign_key_check. |
| T-01-G3-02 | Information Disclosure | inspect-run diagnostics | high | mitigate | Strict persisted models and exact ErrorCode/summary/telemetry projection before JSON. |
| T-01-G3-03 | Tampering / Disclosure | v1 migration | high | mitigate | Validate source diagnostics before copy and rollback all new evidence on mismatch. |
| T-01-SC | Tampering | frozen source and lock | high | mitigate | Frozen-v1 and lock hashes remain unchanged and are checked in focused commands. |
</threat_model>

## Artifacts this phase produces

- Symbols: exact schema-v2 fingerprint descriptors; `PersistedRunRecord`, `PersistedAttemptRecord`, `PersistedCheckpointRecord`; validated diagnostic projection.
- Tests: missing/changed columns, constraints, FKs and indexes; quick-check/FK failures; run/attempt status/error/telemetry tampering; migrated credential/path canary.
- Public behavior: inspect emits the full valid structured field set or one fixed state_integrity_error, never raw persisted content.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Inspectable versioned evidence and incompatible-schema rejection | 01-09 | COVERED | Exact schema and safe projection close root gap 3 support paths. |
| REQ | OPS-01 | Structured attempts/errors/telemetry | 01-09 | COVERED | Persisted rows are strict and coherent before output. |
| RESEARCH | — | Fail-closed schema migration and fixed diagnostics | 01-09 | COVERED | Both new and legacy state use the same validation boundary. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No external provider metadata is introduced. |

<verification>
- WR-02 malformed schemas and CR-06 canary reproduction fail before fixes and pass afterward.
- Fresh and migrated schema fingerprints are identical.
- Focused integrity/security/migration suites and Ruff pass with frozen/lock hashes unchanged.
</verification>

<success_criteria>
- No structurally incompatible schema-2 database is accepted.
- No persisted diagnostic reaches inspect without closed semantic validation.
- Legacy migration cannot turn unsafe source rows into trusted evidence.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-09-SUMMARY.md` with schema fingerprint checks, tamper matrix and canary leak-scan evidence.</output>
