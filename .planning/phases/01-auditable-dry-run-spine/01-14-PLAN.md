---
phase: 01-auditable-dry-run-spine
plan: "14"
type: execute
wave: 14
depends_on: ["01-13"]
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/state.py
  - tests/test_state_integrity.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Full-chain verification recomputes the complete resume-event hash chain and derives the public reuse count from its latest checkpoint-bound event."
    - "The denormalized run count and latest event head are accepted only when they exactly match verified event authority."
    - "Every public run projection, latest-checkpoint lookup, resume decision, and completed-chain check rejects the same resume-event or count tamper."
    - "Tampering any event field, order, association, head, or projected count produces only the fixed state-integrity failure."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "VerifiedRunChain resume-event projection and derived reuse authority", exports: [VerifiedRunChain] }
    - { path: src/skillscout/adapters/state.py, provides: "single canonical resume-event verifier across all bound trust paths" }
    - { path: tests/test_state_integrity.py, provides: "event/count/head/order tamper matrix and public projection proof" }
  key_links:
    - { from: src/skillscout/adapters/state.py, to: resume_events, via: "_verify_run_chain recomputes each canonical event and prior head", pattern: "resume_event_hash" }
    - { from: VerifiedRunChain.reused_stage_count, to: SQLiteStateStore.inspect_run, via: "inspect projects only the verified latest event count", pattern: "reused_stage_count" }
    - { from: runs.latest_resume_event_hash, to: VerifiedRunChain.resume_events, via: "the stored head must equal the final recomputed event hash", pattern: "latest_resume_event_hash" }
---

<objective>
Finish CR-02 by extending the canonical run-chain proof over resume-event provenance and every public reuse projection.

Purpose: Ensure a mutable or coherently edited run summary cannot be mistaken for verified audit data.
Output: Verified event-chain derivation, converged trust-path enforcement, and exhaustive tamper regression coverage.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-10-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md
@src/skillscout/domain/models.py
@src/skillscout/adapters/state.py
@tests/test_state_integrity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-14-01: Extend the canonical run-chain proof through every resume event</name>
  <files>src/skillscout/domain/models.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py, .planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md</read_first>
  <behavior>
    - Event indices are exactly zero through N, prior hashes form one chain, and the run head equals event N.
    - Genesis is bound to an empty prefix; every later event count equals its referenced checkpoint stage index plus one.
    - Referenced result-row and manifest identities equal the already verified checkpoint at that prefix position.
    - Event timestamps are monotonic, no earlier than the reused prefix completion, and no later than attempts first started after that decision.
    - VerifiedRunChain exposes the validated events and derives reuse count from the final event rather than the runs row.
  </behavior>
  <action>Add RED direct-store cases for missing/extra/duplicate/reordered events, broken prior hashes, wrong ordinals, partial checkpoint references, wrong result-row/manifest associations, non-monotonic timing, incorrect run head, and incorrect denormalized count. Extend `VerifiedRunChain` with an immutable `resume_events` tuple and a `reused_stage_count` property derived from its final event. In `_verify_run_chain`, load events in ordinal order; strictly model-validate each row; recompute each event hash; verify contiguous ordinals and prior heads; require the genesis empty-prefix contract; compare every positive event with the already canonical checkpoint/result at `count - 1`; enforce timing against verified attempt completion/start facts; and require `runs.latest_resume_event_hash` plus `runs.reused_stage_count` to equal the recomputed final event. Return a run model whose projected count is the verified value only after all comparisons pass.</action>
  <acceptance_criteria>
    - Every event-chain corruption fails `verify_run_chain` with `state_integrity_error` and no state mutation.
    - A direct update of only `runs.reused_stage_count` is rejected.
    - A coherent edit of count, event payload, event hash, and run head is still rejected when its checkpoint association or ordinal/timing facts disagree.
    - Valid genesis, one-resume, repeated-resume, and migrated chains return the expected derived count.
    - No raw SQLite event row or unattested run count crosses the typed VerifiedRunChain boundary.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py -k 'resume_event or reused_stage_count or event_head or full_chain' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain/models.py src/skillscout/adapters/state.py tests/test_state_integrity.py</automated></verify>
  <done>The full-chain proof independently derives reuse from a verified event chain and rejects every mismatched mutable duplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-14-02: Converge all public reuse projections on the verified event count</name>
  <files>src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/application/pipeline.py, src/skillscout/domain/models.py, tests/test_state_integrity.py</read_first>
  <behavior>
    - inspect_run returns the same verified reuse count at top level and inside its nested run object.
    - find_resumable_run, latest_checkpoint, verify_completed_results, read_run where authoritative, and inspect_run all reject the event tamper matrix consistently.
    - Rejection emits no partial run/attempt/result/checkpoint projection.
    - A valid completed resumed run remains inspectable after close and reopen.
  </behavior>
  <action>Route every authority-bearing run projection through the extended `verify_run_chain` result. In `inspect_run`, populate both the nested run count and top-level count from `chain.reused_stage_count`; do not project the database column before verification. Make any authoritative `read_run` consumer either return the chain-validated run or remain restricted to internal non-projecting lifecycle use with an explicit name that cannot be mistaken for audit verification. Parameterize the existing trust-entry consistency test with raw count, event count, head, event hash, checkpoint reference, order, missing, and duplicate damage. Capture inspect output and assert failure occurs before any JSON fragment or unverified run fact is returned.</action>
  <acceptance_criteria>
    - Public inspect of a valid resumed run reports the event-derived count consistently in every location.
    - Each listed bound trust path rejects every resume-event/count tamper with the same closed error.
    - Failed inspect returns no run, attempt, result, checkpoint, event, or count payload.
    - Valid fresh and resumed chains remain accepted across reopen and exact-identity lookup.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py &amp;&amp; test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32"</automated></verify>
  <done>No public audit path can project `reused_stage_count` without proving its complete event and checkpoint provenance.</done>
</task>

</tasks>

<prohibitions>
- Do not treat content addressing alone as sufficient; verify the event's checkpoint, order, prior head, and run duplicates.
- Do not retain a weaker inspect/resume/count path beside `verify_run_chain`.
- Do not serialize raw SQLite rows or partial projections on failure.
- Do not add external services, network access, remote writes, or candidate execution.
- Do not stage or commit `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted resume-event rows → VerifiedRunChain | Every duplicate and association is untrusted until recomputed. |
| VerifiedRunChain → inspect/resume/checkpoint consumers | Only typed verified facts may grant authority or reach output. |
| Mutable run summary → public reuse count | The summary must equal, never define, the event-derived value. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Tampering | resume event/count/head | high | mitigate | Recompute hashes, order, prior heads, checkpoint identities, timing, and mutable duplicates in one verifier. |
| T-01-14-02 | Repudiation | inspect audit projection | high | mitigate | Derive every public count from the final verified event and return no partial payload on failure. |
| T-01-14-03 | Elevation of Privilege | resume authority | critical | mitigate | All resume/checkpoint decisions consume the same exact-identity full-chain proof. |
| T-01-14-04 | Information Disclosure | corrupt event diagnostics | high | mitigate | Use only the fixed state-integrity diagnostic; never echo persisted values. |
| T-01-14-SC | Tampering | approved artifacts | high | mitigate | Preserve exact lock/frozen database hashes and run only the offline locked test toolchain. |
</threat_model>

## Artifacts this phase produces

- Field: `VerifiedRunChain.resume_events`.
- Property: `VerifiedRunChain.reused_stage_count`, derived from the final verified event.
- Verification behavior: `_verify_run_chain` validates event hashes, ordinals, prior heads, checkpoint associations, timing, run head, and denormalized count.
- Projection behavior: `inspect_run` and every bound trust path consume the same event-derived count.
- Tests: exhaustive event/count/head/order/association tamper matrix and cross-entry-point consistency.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Fully auditable stage ledger and resume | 01-14 | COVERED | Run-level reuse facts join the canonical proof. |
| REQ | OPS-01 | Trusted structured audit fields | 01-14 | COVERED | Public count is derived and every duplicate is checked. |
| REQ | OPS-04 | Exact checkpoint recovery without replay | 01-14 | COVERED | Resume authority requires the verified event/checkpoint chain. |
| RESEARCH | — | One canonical full-chain verifier | 01-14 | COVERED | No weaker alternate projection remains. |
| CONTEXT | — | No CONTEXT.md; defaults approved | — | N/A | No deferred functionality appears. |

<verification>
- The current direct count-tamper reproduction fails RED and is rejected after implementation.
- Coherent event edits, order/cardinality damage, and every public entry point are covered.
- Valid fresh/migrated/resumed chains still pass focused integrity tests.
</verification>

<success_criteria>
- `reused_stage_count` cannot be changed, reheaded, or coherently relocated without full-chain rejection.
- Inspect and recovery expose identical verified reuse authority.
- CR-02 has direct and public-projection tamper evidence.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-14-SUMMARY.md` with event tamper matrix, derived-count proof, and trust-entry consistency results.</output>
