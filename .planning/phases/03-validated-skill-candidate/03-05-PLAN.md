---
phase: 03-validated-skill-candidate
plan: "05"
type: execute
wave: 4
depends_on: [03-02, 03-04]
files_modified:
  - src/skillscout/domain/review.py
  - src/skillscout/adapters/openai_review.py
  - tests/test_openai_review.py
  - tests/fixtures/openai/reviewer/cases.json
autonomous: true
requirements: [REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "Reviewer is a separate fresh one-call tool-less store-false request and receives exactly WorkflowSpec, rendered artifact, provenance, and ValidationReport as inert user data."
    - "Reviewer output is a strict bounded YES/NO judgment with confidence, reasons, missing assumptions, and minimal modifications; it cannot represent files, patches, replacements, or edits."
    - "Only qualification pass + zero validation errors + reviewed YES + confidence at least 0.80 yields an eligible local candidate."
    - "NO, confidence 0.799, refusal, incomplete, and schema-invalid are auditable non-approval business outcomes and are never re-asked for a different judgment."
  artifacts:
    - { path: src/skillscout/domain/review.py, provides: "judge-only review schema and pure final eligibility predicate", exports: [REVIEW_PROMPT_VERSION, REVIEW_SCHEMA_VERSION, REVIEW_CONFIDENCE_THRESHOLD, ReviewerJudgment, ReviewResult, CandidateDecision, evaluate_candidate_eligibility] }
    - { path: src/skillscout/adapters/openai_review.py, provides: "fresh isolated Reviewer Responses adapter", exports: [OpenAIReviewClient] }
    - { path: tests/test_openai_review.py, provides: "four-input isolation, judge-only schema, outcomes, errors, confidence and final-gate proof" }
    - { path: tests/fixtures/openai/reviewer/cases.json, provides: "named recorded Reviewer judgment/boundary/error/delimiter cases" }
  key_links:
    - { from: src/skillscout/adapters/openai_review.py, to: src/skillscout/domain/review.py, via: "strict ReviewerJudgment Structured Output", pattern: "ReviewerJudgment" }
    - { from: src/skillscout/domain/review.py, to: src/skillscout/domain/validation.py, via: "zero-error deterministic eligibility gate", pattern: "ValidationReport" }
    - { from: src/skillscout/domain/review.py, to: src/skillscout/domain/qualification.py, via: "qualification passed predicate", pattern: "QualificationReport" }
  prohibitions:
    - "Reviewer receives no raw Reader bundle, complete README/docs/source, Generator reasoning, credentials, tools, prior conversation, or publication capability."
    - "Reviewer text is never passed to the renderer or used to write/replace artifact files."
    - "Reviewer cannot override qualification failure or validation errors regardless of verdict/confidence."
    - "Business rejection cannot consume retry budget or trigger regeneration/re-review seeking a favorable answer."
---

<objective>
Add an independent judge-only Reviewer and the deterministic final eligibility gate for a local validated candidate.

Purpose: Separate generation from judgment while ensuring deterministic qualification and validation always outrank model opinion.
Output: Closed review contracts, isolated Reviewer adapter, exact 0.80 gate, recorded outcomes, and injection/isolation evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-04-SUMMARY.md
@src/skillscout/domain/qualification.py
@src/skillscout/domain/skill_artifacts.py
@src/skillscout/domain/validation.py
@src/skillscout/adapters/openai_extract.py
</context>

<interfaces>
The pure gate consumes `QualificationReport`, `ValidationReport`, and a returned `ReviewResult`. The adapter payload contains exactly the validated `WorkflowSpec`, rendered `SkillPackage` file projection, `SkillProvenance`, and `ValidationReport`. Mirror the verified OpenAI adapter error/telemetry discipline but use a distinct client class, prompt, schema and fresh request context.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-05-01: Freeze the judge-only schema and exact deterministic eligibility matrix</name>
  <files>src/skillscout/domain/review.py, tests/test_openai_review.py</files>
  <read_first>src/skillscout/domain/review.py (new; confirm absent), tests/test_openai_review.py (new; confirm absent), src/skillscout/domain/models.py, src/skillscout/domain/qualification.py, src/skillscout/domain/validation.py, src/skillscout/domain/skill_artifacts.py, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - ReviewerJudgment has exactly verdict YES/NO, confidence 0..1, bounded reasons, missing assumptions, and minimal modifications with extra fields forbidden.
    - The schema contains no file/body/replacement/patch/content-map authority and returned strings can never modify a SkillPackage.
    - Eligibility passes only at clean qualification + zero errors + reviewed YES + 0.800; 0.799, NO, non-reviewed status, qualification fail, or any validation error rejects with an ordered code.
  </behavior>
  <action>Create `REVIEW_PROMPT_VERSION = "review-prompt-v1"`, `REVIEW_SCHEMA_VERSION = "review-result-v1"`, and `REVIEW_CONFIDENCE_THRESHOLD = 0.80`. Define strict bounded `ReviewerJudgment` with only `verdict: Literal["YES", "NO"]`, confidence in [0,1], reasons, missing_assumptions, and minimal_modifications. Define `ReviewResult` closed statuses `reviewed|refused|incomplete|schema_invalid` with telemetry/reference facts but no artifact bodies or edit fields. Define `CandidateDecision` with exact package/artifact/lineage identities, `eligible_for_publication`, ordered decision codes and all qualification/validation/review version facts; it is a local typed decision, not a `PublicationPlan`. Implement pure `evaluate_candidate_eligibility` with the exact conjunction and fixed reason precedence: qualification failure, validation errors, non-reviewed outcome, Reviewer NO, confidence below 0.80. Tests must inspect generated JSON Schema recursively for prohibited authority-shaped keys and prove the complete truth table including 0.799/0.800.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_review.py -k 'schema or gate or confidence or decision'</automated>
  </verify>
  <acceptance_criteria>
    - Strict schema contains exactly the allowed judgment fields and rejects extra/replacement-shaped fields.
    - Pure-gate tests cover every predicate independently and the exact 0.799 reject/0.800 pass boundary.
    - `CandidateDecision` binds exact identities/versions but contains no publication action, remote target, branch, PR, merge, or approval authority.
    - Reviewer suggestions have no code path to `render_skill_package` or `SkillPackageWriter`.
  </acceptance_criteria>
  <done>The judge-only contract and deterministic gate make model judgment subordinate to qualification and validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-02: Implement the fresh four-input Reviewer request and closed outcomes</name>
  <files>src/skillscout/adapters/openai_review.py, tests/test_openai_review.py, tests/fixtures/openai/reviewer/cases.json</files>
  <read_first>src/skillscout/adapters/openai_review.py (new; confirm absent), tests/test_openai_review.py, tests/fixtures/openai/reviewer/cases.json (new; confirm absent), src/skillscout/adapters/openai_extract.py, src/skillscout/adapters/openai_generate.py, src/skillscout/domain/review.py, src/skillscout/domain/extraction.py, src/skillscout/domain/skill_artifacts.py, src/skillscout/domain/validation.py, tests/test_openai_extract.py, tests/recorded_transport.py</read_first>
  <behavior>
    - Each `review()` uses a new request context with one POST, `store=false`, no tools, bounded tokens, separate versioned developer instructions, and exactly four user-data sections.
    - Section delimiters are fresh and absent from all four payloads; injection-like section markers remain inert data and cannot create extra roles/tools/sections.
    - YES, NO, 0.799, 0.800, refusal, incomplete and schema-invalid outcomes are returned once; only 429/5xx/timeout/connection map to runner-owned transient failure.
  </behavior>
  <action>Implement `OpenAIReviewClient` as a concrete client independent from Extractor and Generator. Configure environment/API key once, configurable model default `gpt-5.6-terra`, bounded output tokens, SDK `max_retries=0`, and exactly one `responses.parse` with `text_format=ReviewerJudgment`, `store=False`, no tools. Developer content is the versioned judge-only policy and contains zero payload bytes. Serialize the user content as exactly four labeled canonical inert-data sections — `WorkflowSpec`, rendered artifact files, provenance, `ValidationReport` — using a fresh injected/testable high-entropy delimiter factory; regenerate/reject delimiters that occur in any section. Do not include Generator reasoning, qualification internals beyond the eligibility fact needed by caller, raw repository content, credentials, or prior messages. Map all decided outcomes to `ReviewResult` and never ask again; map infrastructure errors exactly like the verified adapters. Add one bounded repository-owned `cases.json` map with named YES, NO, 0.799, 0.800, refusal, incomplete, schema-invalid, 429, 500, and injection/delimiter recordings; load named cases locally and retain API-key/header canaries.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_review.py &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain/review.py src/skillscout/adapters/openai_review.py tests/test_openai_review.py</automated>
  </verify>
  <acceptance_criteria>
    - Request tests assert a fresh independent client/request, exactly four data sections, one POST, `store=false`, no tools, strict schema and zero payload bytes in developer content.
    - Injection/delimiter fixtures cannot add a role, tool, fifth section, instruction authority, or edit field.
    - Every decided response uses one request and returns a bounded business result; transient/permanent matrices preserve runner-only retry authority.
    - Credentials remain header-only and raw repository/source bodies remain absent from requests, fixtures, outcomes and diagnostics.
  </acceptance_criteria>
  <done>The Reviewer independently judges only the four allowed artifacts and can return no file or authority-bearing output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Four approved artifacts → Reviewer request | Model-influenced content is untrusted user data in a fresh external request. |
| Reviewer output → eligibility gate | Model judgment is untrusted until strict schema validation and deterministic conjunction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-PI | Spoofing / Tampering | Reviewer prompt boundary | high | mitigate | Separate developer policy, exactly four canonical data sections, fresh collision-free delimiters, no tools, strict schema and adversarial delimiter/injection fixtures. |
| T-03-REVIEW | Elevation of Privilege | final eligibility | high | mitigate | Pure conjunction requires qualification pass, zero validation errors, reviewed YES and confidence ≥0.80; Reviewer cannot override deterministic failures. |
| T-03-05-R | Repudiation | repeated favorable-answer seeking | medium | mitigate | Closed business outcomes are persisted once; SDK retries disabled; only runner-owned transient failures may retry. |
| T-03-05-ID | Information Disclosure | Reviewer request/telemetry | high | mitigate | Four-input allowlist, header-only credentials, bounded telemetry and no raw repository/Generator reasoning. |
</threat_model>

## Artifacts this phase produces

- New source files: `src/skillscout/domain/review.py`, `src/skillscout/adapters/openai_review.py`.
- New symbols: `REVIEW_PROMPT_VERSION`, `REVIEW_SCHEMA_VERSION`, `REVIEW_CONFIDENCE_THRESHOLD`, `ReviewerJudgment`, `ReviewResult`, `CandidateDecision`, `evaluate_candidate_eligibility`, `OpenAIReviewClient` and Reviewer model/token/instruction constants.
- New test/fixture paths: `tests/test_openai_review.py` and `tests/fixtures/openai/reviewer/cases.json` containing all named judgment, boundary, refusal, invalid, infrastructure, and delimiter recordings.
- Runtime artifact: judge-only `ReviewResult` and local `CandidateDecision`; no modified Skill file and no publication record.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain independent Reviewer conclusions | 03-05 | COVERED | Closed judgment/outcome and decision codes. |
| REQ | REV-01 | Fresh separate context with only four allowed inputs | 03-05 | COVERED | Dedicated client and request-shape proof. |
| REQ | REV-02 | Strict YES/NO/confidence/reasons/assumptions/minimal modifications; judge only | 03-05 | COVERED | Closed schema with edit authority absent. |
| REQ | REV-03 | Clean validation + YES + confidence ≥0.80 | 03-05 | COVERED | Pure exact truth table and boundary tests. |
| RESEARCH | Business rejection does not retry | One-call outcomes | 03-05 | COVERED | NO/low/refusal/incomplete/invalid are returned decisions. |
| RESEARCH | Prompt-injection resistance | Four-section inert envelope | 03-05 | COVERED | Fresh collision-free delimiters and adversarial fixtures. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | No locked/deferred decisions to map. |

<verification>
- Review schema/gate/adapter/outcome/injection suites and Ruff pass under the exact approved lock.
- Recursive schema inspection proves no output field can carry a file, patch, replacement, or body.
- Recorded call counts prove one request per attempt and no favorable-answer retry.
</verification>

<success_criteria>
- Reviewer is independent, sees exactly the allowed four inputs, and emits only a bounded judgment.
- Eligibility is deterministically false for any qualification failure, validation error, NO, non-reviewed status, or confidence below 0.80.
- No Reviewer result can edit a package or create publication authority.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-05-SUMMARY.md` with the exact request envelope, schema, outcome matrix, and eligibility truth table.</output>
