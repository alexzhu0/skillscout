---
phase: 03-validated-skill-candidate
plan: 11
type: execute
wave: 10
depends_on:
  - 03-05
  - 03-10
  - 03-04
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/state.py
  - tests/test_phase3_pipeline.py
autonomous: true
requirements:
  - GEN-05
  - VAL-03
  - REV-03
must_haves:
  truths:
    - "Phase 3 has a dedicated ledger rooted in complete CandidateExecutionAuthorityV1 and ordered exactly QUALIFIER → GENERATOR → VALIDATOR → REVIEWER."
    - "Phase 1/2 global PIPELINE_PROFILES and existing verify_run_chain behavior remain unchanged."
    - "Terminal, semantic generated-artifact identity, rendered package identity/bytes, validation report, and review attestation canonical bytes are stored as distinct external records and projected byte-for-byte."
    - "A completed exact-authority reuse returns identical bytes with zero model/validator/output calls, zero new rows/events, and unchanged counts in every relevant Phase 3 table."
  artifacts:
    - path: "src/skillscout/domain/models.py"
      provides: "Dedicated Phase 3 stage/run/attempt/result/resume/verified-chain models."
    - path: "src/skillscout/adapters/state.py"
      provides: "Additive Phase 3 tables, chain verifier, persistence, resume, completed lookup, and exact projection."
    - path: "tests/test_phase3_pipeline.py"
      provides: "Ledger order, tamper, exact-byte, resume, and zero-side-effect reuse tests."
  key_links:
    - from: "CandidateExecutionAuthorityV1"
      to: "phase3_runs"
      via: "Complete canonical authority is the run root and exact-reuse key."
    - from: "PhaseThreeStageV1"
      to: "phase3_attempts/results/resume_events"
      via: "Closed monotonic stage order and hash chaining."
    - from: "phase3_artifacts and phase3_terminals"
      to: "project_candidate_terminal"
      via: "Exact canonical bytes are returned without reconstruction drift."
  prohibitions:
    - "Do not append Phase 3 to application/pipeline.py PIPELINE_PROFILES."
    - "Do not modify or relax the existing Phase 1/2 verify_run_chain implementation."
    - "Do not synthesize fake Phase 2 rows or import Phase 2 stages into the Phase 3 ledger."
---

<objective>
Persist Phase 3 in a separate canonical ledger with exact reuse and byte-stable projections.

Purpose: Qualification, generation, validation, and review need resumable auditable state without weakening the already-shipped Phase 1/2 chain.
Output: Dedicated Phase 3 domain events/tables/verifier, exact external artifact persistence, and mutation/reuse tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/02-workflow-extraction/02-04-SUMMARY.md
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/review.py
@src/skillscout/domain/models.py
@src/skillscout/adapters/state.py
@src/skillscout/application/pipeline.py
@tests/test_phase2_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the isolated Phase 3 canonical event chain</name>
  <files>src/skillscout/domain/models.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/domain/models.py` — extend with new names while preserving all Phase 1/2 contracts.
    - `src/skillscout/application/pipeline.py` — observe protected global profiles; do not modify it.
    - `tests/test_phase2_pipeline.py` — reuse canonical hash-chain invariants without combining ledgers.
    - `src/skillscout/domain/candidate_authority.py` — root the new run in complete execution authority.
  </read_first>
  <behavior>
    - PhaseThreeStageV1 has exactly QUALIFIER, GENERATOR, VALIDATOR, REVIEWER in that order.
    - CandidateRunIdentityV1 is rooted in complete canonical execution authority.
    - Attempts/results/resume events bind previous hashes, stage, attempt number, versions, sanitized outcome, and canonical payload digest.
    - Missing, reordered, duplicated, cross-run, or tampered events fail strict chain verification.
  </behavior>
  <action>
Add dedicated strict frozen `PhaseThreeStageV1`, `CandidateRunIdentityV1`, `CandidateStageAttemptV1`, `CandidateStageResultV1`, `CandidateResumeEventV1`, and `VerifiedCandidateRunChain` models to `domain/models.py` with separate Phase 3 schema/profile constants. The closed stage sequence is exactly QUALIFIER, GENERATOR, VALIDATOR, REVIEWER. Root every run/event hash in the complete canonical `CandidateExecutionAuthorityV1`; bind stage order, monotonically increasing attempt number, previous event hash, version authorities, sanitized typed outcome, and canonical payload digest. Do not reuse or extend the global Phase 1/2 pipeline profile registry, do not import fake Phase 2 rows, and do not change existing models' validation behavior. Add construction and mutation tests for every bound field and invalid transition.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k domain_chain</automated>
  </verify>
  <acceptance_criteria>
    - Closed stage enum/order is exactly the four Phase 3 stages.
    - Complete execution authority roots every event.
    - All event-chain mutations and invalid transitions fail deterministically.
    - Existing Phase 1/2 model and profile tests remain byte-for-byte behavior compatible.
  </acceptance_criteria>
  <done>Phase 3 has a standalone canonical event vocabulary and chain that cannot be confused with Phase 1/2 state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and verify the dedicated Phase 3 ledger</name>
  <files>src/skillscout/adapters/state.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` — preserve existing schema/methods/verifier and add isolated tables/functions.
    - `src/skillscout/domain/models.py` — persist Task 1 exact canonical models.
    - `src/skillscout/domain/review.py` — store external package/report/attestation/terminal canonical bytes.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — follow the Phase 3-relative ledger and manifest-first persistence pattern.
  </read_first>
  <behavior>
    - Additive tables are exactly phase3_runs, phase3_attempts, phase3_results, phase3_resume_events, phase3_terminals, and phase3_artifacts.
    - New verify_candidate_run_chain independently checks authority root, stage order, attempts, hashes, and canonical payload bytes.
    - Existing verify_run_chain source and behavior are unchanged.
    - Transactions never expose a terminal pointing to missing/mismatched external artifact/report/attestation bytes.
  </behavior>
  <action>
Add isolated additive SQLite tables `phase3_runs`, `phase3_attempts`, `phase3_results`, `phase3_resume_events`, `phase3_terminals`, and `phase3_artifacts` plus dedicated APIs `verify_candidate_run_chain(...)`, `find_resumable_candidate(...)`, `find_completed_candidate(...)`, `persist_candidate_terminal(...)`, `project_candidate_terminal(...)`, and `project_verified_prior_lineage_evidence(...)`. Use exact canonical bytes/digests and foreign/uniqueness constraints rooted in execution authority. Store qualification, generated-artifact identity, frozen package manifest/content, ValidationReport, ReviewAttestation, CandidateTerminalSummary, canonical prior-lineage binding, and its durable approval record as separately typed immutable `phase3_artifacts` records; validate digest equality before committing a terminal transaction. The prior-lineage projection API looks up only the exact digest supplied by the descriptor/execution authority and must first verify the binding/approval-record bytes, prior Phase 3 chain, exact terminal-summary bytes/digest, generated-artifact identity, package manifest/digest, and full lineage/slug/repository authority before constructing Plan 03-05's narrow `VerifiedPriorLineageEvidenceV1`; no caller can mark an unverified projection as valid, and absence never falls back to a search. Implement resume only at the next legal Phase 3 stage with exact prior results. Preserve existing Phase 1/2 DDL, queries, verifier body, and public behavior; do not append to global profiles.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k state_ledger</automated>
  </verify>
  <acceptance_criteria>
    - Only isolated `phase3_*` tables are added and each has authority/digest constraints.
    - New verifier catches row deletion, insertion, reorder, payload mutation, authority swap, and artifact mismatch.
    - Prior-lineage projection requires verified prior chain, terminal, generated-artifact identity, package digest, lineage/slug/repository authority, and approval-record binding; every mutation fails closed.
    - Terminal commit is atomic with all outcome-required external records.
    - Existing Phase 1/2 state tests still pass without expectation changes.
  </acceptance_criteria>
  <done>Phase 3 stages and external evidence persist atomically in an isolated verifiable ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reproject completed candidates byte-for-byte with zero side effects</name>
  <files>src/skillscout/adapters/state.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` — build exact reuse on Task 2 completed lookup/projection.
    - `src/skillscout/domain/review.py` — validate branch-specific optional evidence.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — implement exact-reuse byte and count invariants.
  </read_first>
  <behavior>
    - Exact complete execution authority returns the same terminal summary and every applicable generated-artifact identity/package identity/package/report/attestation byte.
    - Reprojection does not insert attempts, results, resume events, terminals, artifacts, or any other Phase 3 row.
    - Reprojection emits no model call, validator call, output writer call, or reuse event.
    - Tampered/incomplete completed state is never returned as reuse.
  </behavior>
  <action>
Implement completed projection as a read-only operation keyed by the full execution authority digest and guarded by the dedicated chain plus external-artifact verifier. Return stored canonical bytes exactly; do not parse and reserialize for the public projection, and do not create a convenience reuse event. Before and after projection, tests snapshot row counts for every Phase 3 table and exact database-visible terminal/generated-artifact-identity/rendered-package-identity/package/report/attestation bytes, then assert equality. Use spies to prove generator, official validator, Reviewer, materializer/output writer, and resume/event append paths are untouched. Corrupt any terminal/generated-artifact identity/package identity/package/report/attestation byte or digest and prove reuse fails closed rather than silently recomputing.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k exact_reuse</automated>
  </verify>
  <acceptance_criteria>
    - Every relevant table count and stored canonical byte string is unchanged across exact reuse.
    - All applicable semantic identity, rendered identity/package, report, attestation, and terminal artifacts are returned byte-for-byte.
    - Zero downstream calls/events/writes occur.
    - Any corrupt or incomplete completed state is rejected.
  </acceptance_criteria>
  <done>Completed exact-authority candidates are replayed as immutable projections with zero state or external side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain stage outcomes → SQLite | Canonical events and external artifacts must remain ordered, authority-bound, and atomic. |
| SQLite completed state → reuse projection | Stored state may be tampered or incomplete and cannot be trusted without verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-31 | Tampering | Phase 3 ledger chain | high | mitigate | Dedicated authority-rooted verifier covers order, hashes, payloads, and external artifacts. |
| T-03-32 | Spoofing | Cross-run artifact reuse | high | mitigate | Foreign/unique constraints and complete execution-authority digest bind every record. |
| T-03-33 | Repudiation | Resume/reuse history | medium | mitigate | Resume is explicit canonical event; exact completed reuse is intentionally zero-write and proven by snapshots. |
| T-03-34 | Tampering | Existing Phase 1/2 verifier | high | mitigate | Protected behavior/source regression tests and separate Phase 3 APIs/tables. |
</threat_model>

## Artifacts this phase produces

- Dedicated Phase 3 canonical event models and isolated SQLite ledger.
- Separate chain/external-evidence verifier.
- Exact byte-stable, zero-side-effect reuse projection.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Auditable, repeatable qualification/generation/validation/review | COVERED by isolated canonical state and exact reuse. |
| REQ | GEN-05, VAL-03, REV-03 | COVERED — stable identity, report persistence, exact eligibility outcome. |
| RESEARCH | Phase 3-relative ledger; protected Phase 1/2 seams; exact-byte reuse | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Then run `sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_pipeline.py tests/test_phase3_pipeline.py`.
</verification>

<success_criteria>
- Phase 3 chain is isolated, ordered, authority-rooted, and tamper-evident.
- Existing Phase 1/2 state verification remains unchanged.
- Exact reuse returns identical bytes and changes no row/count/call/output.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-11-SUMMARY.md` when done.
</output>
