---
phase: 03-validated-skill-candidate
plan: 09
type: execute
wave: 8
depends_on:
  - 03-08
  - 03-04
files_modified:
  - src/skillscout/domain/validation.py
  - src/skillscout/adapters/skills_ref.py
  - tests/test_skill_validation.py
  - tests/fixtures/skills/valid-skill/SKILL.md
  - tests/fixtures/skills/valid-skill/references/provenance.json
autonomous: true
requirements:
  - GEN-02
  - GEN-03
  - GEN-04
  - VAL-01
  - VAL-02
  - VAL-03
must_haves:
  truths:
    - "An exact workspace-admission gate proves regular non-linked 0644 UTF-8 non-binary files, exact manifest/path/hash/size/mode equality, and TOCTOU-resistant reads before official validation."
    - "Validation runs the Gate-B3-approved official skills-ref validator and a truthful separate SkillScout structural validator against the same admitted frozen package."
    - "Local structural checks cover frontmatter/name where needed, broken/orphan/deep references/assets, and progressive disclosure; policy checks separately cover secrets, dangerous commands, unauthorized tools, download-execute patterns, injection residue, URLs, missing provenance, scripts/binaries/executable modes, over-copying, and attribution."
    - "Every finding has error/warning/info severity, stable code, location, bounded message, and policy/validator version."
    - "Any error makes validation fail; warnings and info remain visible without silently escalating or disappearing."
    - "ValidationReportV1 directly binds selected full fingerprint, WorkflowSpecAuthorityV1, CandidateExecutionAuthorityV1, RENDERER_VERSION, and its own report-schema authority in addition to both artifact/package identities."
  artifacts:
    - path: "src/skillscout/adapters/skills_ref.py"
      provides: "The only official-validator import/invocation boundary."
    - path: "src/skillscout/domain/validation.py"
      provides: "Workspace admission, truthful local structural/safety/provenance policies, and strict ValidationReportV1."
    - path: "tests/test_skill_validation.py"
      provides: "Official-adapter, security, attribution, over-copying, severity, and determinism tests."
  key_links:
    - from: "FrozenSkillPackageV1"
      to: "workspace admission → skills_ref adapter"
      via: "Exact manifest is materialized and reverified with descriptor-based regular-file checks before official invocation."
    - from: "official and local findings"
      to: "ValidationReportV1"
      via: "Stable normalized finding codes/severities and version authorities."
    - from: "ValidationReportV1"
      to: "review eligibility"
      via: "Zero errors is mandatory."
  prohibitions:
    - "Do not import skills_ref outside src/skillscout/adapters/skills_ref.py."
    - "Do not let the official validator replace local safety/provenance/over-copying checks."
    - "Do not attribute broken-reference, orphan/depth, or progressive-disclosure coverage to skills-ref; SkillScout owns those local structural checks."
    - "Do not execute any candidate file or repository code during validation."
---

<objective>
Validate the exact frozen package with both the approved official standard validator and deterministic safety/provenance policy.

Purpose: A standard-conforming package may still be unsafe, over-copied, unattributed, or provenance-incomplete; both layers must report through one auditable contract.
Output: Isolated official-validator adapter, versioned local checks, strict validation report, and malicious/valid package fixtures.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/domain/skill_artifacts.py
@config/supply-chain/phase3-gate-b3.lock.sha256
@tools/verify_phase3_gate_b3.sh
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit an exact workspace and wrap the approved official validator</name>
  <files>src/skillscout/domain/validation.py, src/skillscout/adapters/skills_ref.py, tests/test_skill_validation.py, tests/fixtures/skills/valid-skill/SKILL.md, tests/fixtures/skills/valid-skill/references/provenance.json</files>
  <read_first>
    - `.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md` — require exact Gate B3 approval.
    - `config/supply-chain/phase3-gate-b3.lock.sha256` — bind adapter evidence to the approved lock.
    - `src/skillscout/domain/skill_artifacts.py` — consume exact frozen package manifests without executing content.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — follow the verified installed skills-ref interface and its limitations.
  </read_first>
  <behavior>
    - Admission rejects missing/extra/changed paths, symlinks, hard-link surprises, non-regular files, traversal, size excess, non-UTF-8/binary content, non-0644 mode, and descriptor/path/hash/size/mode mismatch before official invocation.
    - Adapter materializes exact files/modes into a private temporary directory, opens each file without following links, compares lstat/fstat identity and link count, invokes only the approved skills-ref interface, and always cleans up.
    - Output is normalized into bounded stable findings without leaking temp paths, environment, or package internals.
    - A missing, wrong-version, malformed, or crashing official validator fails closed as an infrastructure validation error.
    - Candidate files are read as data and never executed.
  </behavior>
  <action>
Create a deterministic workspace-admission contract in `src/skillscout/domain/validation.py` and the sole `skills_ref` import/invocation boundary in `src/skillscout/adapters/skills_ref.py`. Verify the installed distribution identity/version and approved lock authority before invocation. Materialize the exact in-memory frozen manifest to a newly created private temporary directory using create-new files and fixed 0644 modes. Before calling skills-ref, enumerate the directory and require exact path-set equality; for every entry reject traversal, symlink, hard-link count other than one, non-regular type, wrong size/mode, non-UTF-8 or binary bytes, and path/content-hash/size/mode mismatch. Use no-follow descriptor opens plus lstat/fstat identity comparisons and recheck the exact manifest immediately before invocation so replacement/race attempts close; never validate a caller-supplied workspace path. Invoke only the public skills-ref interface confirmed during implementation, and normalize only findings the official validator actually emits, such as its supported standard/frontmatter/name checks. Do not claim official coverage for broken/orphan/deep references or progressive disclosure. Redact nondeterministic temp paths, capture official distribution/version authority, and clean the directory on every outcome. Treat admission/import/version/interface/exception failures as fail-closed errors. Add valid, missing/extra/changed manifest, symlink, hard-link, FIFO/non-regular, mode, size, UTF-8/binary, lstat/fstat replacement, immediately-before-call mutation, validator crash, and cleanup cases.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_validation.py -k "official or admission"</automated>
  </verify>
  <acceptance_criteria>
    - `skills_ref` imports occur only in the adapter.
    - Exact workspace admission rejects every file-type/link/manifest/size/UTF-8/mode/hash and TOCTOU mutation case before official invocation.
    - Exact approved distribution/version and lock authority are recorded.
    - Official findings are limited to capabilities the installed validator actually provides and are stable after temp-path normalization.
    - Candidate content is never imported, sourced, spawned, or executed.
  </acceptance_criteria>
  <done>The frozen package can be checked by the approved official standard validator through one isolated fail-closed boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce local structure, safety, provenance, and over-copying policy</name>
  <files>src/skillscout/domain/validation.py, tests/test_skill_validation.py</files>
  <read_first>
    - `src/skillscout/domain/skill_artifacts.py` — inspect exact manifest, provenance, and attribution contracts.
    - `.planning/REQUIREMENTS.md` — implement every VAL-02 category.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — adopt bounded quote and unregistered-match limits as versioned project policy.
    - `src/skillscout/domain/canonical.py` — use deterministic finding ordering/digests.
  </read_first>
  <behavior>
    - SkillScout structural findings cover invalid/missing frontmatter/name where official output is insufficient, broken references, orphan references/assets, nested/deep resources, and progressive-disclosure violations.
    - Policy findings cover secret shapes, dangerous commands, unauthorized tools, download-and-execute, injection residue, unapproved URLs, missing/inconsistent provenance, scripts/binaries/executable modes, and over-copying.
    - Registered quote caps are 120 characters each and 240 total; unregistered source matches of 80 or more normalized characters are errors.
    - Provenance path/hash/commit/license/authority mismatches are errors.
    - The same package and source authority always produce identical ordered findings.
  </behavior>
  <action>
Create pure deterministic validators over the admitted frozen package bytes plus the bounded source-evidence hashes/text already authorized for comparison. Export separate local structural, progressive-disclosure, safety, provenance, URL, and over-copying policy versions. Truthfully cover what skills-ref does not: validate the expected simple frontmatter/name constraints where needed, every internal reference target, orphaned `references/` and `assets/` entries, the single-layer resource-depth rule, and bounded progressive-disclosure structure/size. Detect common credential/token/private-key shapes without logging matches; dangerous shell or installation instructions; unauthorized tool grants; fetch-pipe-execute and download-then-run sequences; prompt-injection residue; non-allowlisted URLs; absent/inconsistent provenance; forbidden scripts, binaries, executable modes, or copied executable code. Enforce research's conservative agent-discretion limits as explicit versioned policy: registered quotes at most 120 characters each and 240 total, with source path plus exact commit; normalized unregistered source match length 80 or greater is an error. These are project review thresholds, not legal safe harbors. Use stable finding codes, normalized relative locations, bounded messages, deterministic sort order, and no raw secret/source excerpts. Add positive/negative cases for broken targets, orphan files, nested/deep resources, cycles if the reference grammar permits them, and progressive-disclosure boundaries.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_validation.py -k "local_structure or local_policy"</automated>
  </verify>
  <acceptance_criteria>
    - Broken/orphan/deep resource and progressive-disclosure cases are covered by explicit local structural tests rather than attributed to skills-ref.
    - Every VAL-02 category has positive and negative tests.
    - Exact quote and unregistered-match boundary tests cover 119/120/121, 239/240/241, and 79/80 characters.
    - Secret/source matches never appear in findings or logs.
    - Finding order and canonical bytes are deterministic.
  </acceptance_criteria>
  <done>The package is checked for deterministic safety, provenance, attribution, and over-copying violations without execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose immutable structured validation reports</name>
  <files>src/skillscout/domain/validation.py, tests/test_skill_validation.py</files>
  <read_first>
    - `src/skillscout/domain/validation.py` — compose Task 1/2 normalized results.
    - `src/skillscout/domain/skill_artifacts.py` — bind the report to exact external package identity.
    - `.planning/REQUIREMENTS.md` — implement VAL-03 severities and blocking rule exactly.
  </read_first>
  <behavior>
    - ValidationReportV1 directly binds selected full fingerprint, WorkflowSpecAuthorityV1, CandidateExecutionAuthorityV1, RENDERER_VERSION, VALIDATION_REPORT_SCHEMA_VERSION, both GeneratedArtifactIdentityV1 and the distinct package digest, workspace-admission result, official validator authority, all local structural/policy versions, and ordered findings.
    - Finding severities are exactly error, warning, or info.
    - `passed` is true exactly when there are zero errors and infrastructure succeeded.
    - Any package/policy/validator/finding mutation changes the report digest.
  </behavior>
  <action>
Define exported `VALIDATION_REPORT_SCHEMA_VERSION`, strict frozen `ValidationFindingV1` and `ValidationReportV1`, one constructor, and canonical digest. Normalize workspace-admission, official, local-structural, and local-policy results into exact `error|warning|info` severity, stable code, normalized location, bounded safe message, and responsible validator/policy version. The report header must directly bind the selected full fingerprint, complete `WorkflowSpecAuthorityV1`, complete prelookup `CandidateExecutionAuthorityV1`, `RENDERER_VERSION`, `VALIDATION_REPORT_SCHEMA_VERSION`, semantic `GeneratedArtifactIdentityV1`, rendered `PackageIdentityV1`/package digest, exact admission manifest/result, official distribution/lock authority, and every local structural/policy version. Validate that fingerprint/WorkflowSpec authority align with execution authority, the semantic/package identities align with the renderer/package evidence, and no transitive digest substitutes for these direct fields. Recompute counts and `passed`; any admission/structure/policy/official error or official-validator infrastructure failure blocks, while warnings/info remain visible. Reject duplicate identities, unknown severities, inconsistent counts/pass, missing authorities, and hand-crafted report digests. Add deterministic merge/digest, severity, empty findings, warning-only, one-error, infrastructure failure, full direct-header sensitivity, fingerprint/WorkflowSpec/execution-authority swap, renderer/report-schema mutation, identity-layer swap, and tamper tests.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Report directly contains selected fingerprint, WorkflowSpec authority, execution authority, RENDERER_VERSION, report-schema authority, separate generated-artifact/rendered-package identities, admission, validator, lock, structural, and policy authorities.
    - Every direct header mutation changes the digest; cross-candidate and identity-layer swaps fail validation.
    - Any error or validator infrastructure failure makes `passed=false`.
    - Warnings/info are retained with stable ordering.
    - Canonical digest is deterministic and changes on every relevant mutation.
  </acceptance_criteria>
  <done>Official and local findings produce one immutable structured report whose error count deterministically gates review eligibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen package → official validator | Untrusted files are materialized for static validation only. |
| Package/source evidence → local scanners | Potential secrets, commands, and copied content must be classified without leaking it. |
| Findings → eligibility | A forged severity/count/pass could bypass safety policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Elevation of Privilege | Candidate validation | high | mitigate | Never execute candidate files; controlled temp materialization and static checks only. |
| T-03-24 | Information Disclosure | Secret/source matches | high | mitigate | Stable codes and bounded messages omit matched bytes. |
| T-03-25 | Tampering | Validation pass/counts | high | mitigate | Strict constructor recomputes counts/pass and binds exact package/validator/policy authority. |
| T-03-26 | Tampering | Official validator substitution | high | mitigate | Verify approved distribution/version/lock and isolate the sole import boundary. |
</threat_model>

## Artifacts this phase produces

- Official skills-ref adapter.
- Layered deterministic safety/provenance/over-copying validators.
- Immutable structured ValidationReportV1 and fixtures.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Explain format and safety validation | COVERED — layered report exposes official/local findings. |
| REQ | GEN-02, GEN-03, GEN-04, VAL-01, VAL-02, VAL-03 | COVERED exactly. |
| RESEARCH | Official adapter isolation; validation layering; quote/match limits; structured severity | COVERED as versioned conservative policy. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Use an AST/import scan to confirm `skills_ref` is imported only by `src/skillscout/adapters/skills_ref.py`.
</verification>

<success_criteria>
- The approved official validator and all deterministic local checks run over the same frozen package.
- Any error blocks and all findings remain structured and auditable.
- Candidate content is never executed or leaked.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-09-SUMMARY.md` when done.
</output>
