---
phase: 03-validated-skill-candidate
plan: "06"
type: execute
wave: 5
depends_on: [03-02, 03-03, 03-04, 03-05]
files_modified:
  - src/skillscout/domain/subjects.py
  - src/skillscout/domain/models.py
  - src/skillscout/application/phase3.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/adapters/state.py
  - tests/test_phase3_pipeline.py
autonomous: true
requirements: [QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "An additive phase3-v1 prefix runs Scout through Reviewer without changing phase2-v1 behavior or exceeding NONE/LOCAL_STATE/REMOTE_READ authority."
    - "Selected WorkflowSpecs are sorted by full fingerprint and capped at three; each becomes one isolated CandidateSubjectV1/run with no cross-workflow request, approval, retry, artifact, or terminal authority."
    - "Before any resume/completed lookup, one versioned configured execution-authority digest binds selected workflow, resolved lineage, every prompt/policy/schema/model/renderer/validator/profile/retry version, and Gate-B3 lock authority; finalized identity also binds actual returned model IDs."
    - "Qualification rejection makes zero Generator/Reviewer calls; generation rejection makes one Generator/zero Reviewer calls; validation error makes one Generator/zero Reviewer calls; review outcomes make one Generator/one Reviewer call."
    - "Business rejections complete as auditable local terminal decisions without consuming retry budget or producing PublicationPlan; only transient infrastructure failures retry."
    - "Every verified completed terminal branch reprojects its exact summary with zero GitHub/OpenAI/official-validator calls and zero new ledger rows/events; package bytes are materialized only for branches that possess a verified package."
  artifacts:
    - { path: src/skillscout/domain/subjects.py, provides: "isolated workflow subject plus configured/finalized execution-authority contracts", exports: [CandidateSubjectV1, PhaseThreeExecutionAuthorityV1, FinalizedPhaseThreeExecutionIdentityV1, derive_candidate_subjects, phase_three_execution_authority] }
    - { path: src/skillscout/domain/models.py, provides: "phase3-v1 producer support and bounded local terminal summary", exports: [ValidatedCandidateSummary] }
    - { path: src/skillscout/application/phase3.py, provides: "composition-based PhaseThreeProcessor and closed stage cascade", exports: [PhaseThreeProcessor] }
    - { path: src/skillscout/application/pipeline.py, provides: "phase3-v1 profile/runtime/root/reuse writer", exports: [PHASE_THREE_STAGE_SEQUENCE, PhaseThreeRuntime, build_phase_three_runtime] }
    - { path: src/skillscout/adapters/state.py, provides: "exact prior-lineage and configured/finalized completed-candidate lookup seams" }
    - { path: tests/test_phase3_pipeline.py, provides: "selection, stage, call-count, retry, resume, reuse, authority and regression proof" }
  key_links:
    - { from: src/skillscout/application/phase3.py, to: src/skillscout/application/processors.py, via: "exact PhaseTwoProcessor composition for Scout through Extractor", pattern: "PhaseTwoProcessor" }
    - { from: src/skillscout/application/phase3.py, to: src/skillscout/domain/review.py, via: "deterministic final CandidateDecision", pattern: "evaluate_candidate_eligibility" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "verified full-chain resume/completed-run reuse", pattern: "find_completed_run" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/skill_packages.py, via: "exact package materialization/reprojection", pattern: "SkillPackageWriter" }
  prohibitions:
    - "Do not mutate phase2-v1 profile, terminal, composition root, processor behavior, or verified regression expectations."
    - "No REMOTE_WRITE adapter, Publisher, PublicationPlan, branch, PR, merge, approval, release, runtime install, subprocess, candidate execution, or caller-supplied scope widening may enter the root."
    - "No raw Reader bundle or complete source text may cross into qualifier/generator/validator/reviewer or durable stage payloads."
    - "No lookup keyed only by candidate subject, producer, retry version, short workflow ID, or package identity may authorize resume/completed reuse."
    - "No validation error, Reviewer NO, confidence below 0.80, refusal, incomplete, or schema invalidity may reach publication eligibility."
---

<objective>
Wire qualification, generation, validation, and independent review into an auditable `phase3-v1` pipeline with per-workflow identity, exact reuse, and no publication authority.

Purpose: Turn the isolated Phase 3 capabilities into one resumable local candidate flow while preserving every Phase 1/2 boundary and invalidating stale authority before lookup.
Output: Isolated subject derivation, complete execution identity, lineage authority, PhaseThreeProcessor, additive profile/runtime/root, branch-aware terminal reprojection, and integration evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-VERIFICATION.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-04-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-05-SUMMARY.md
@tools/verify_phase3_gate_b3.sh
@src/skillscout/application/processors.py
@src/skillscout/application/pipeline.py
@src/skillscout/domain/models.py
@src/skillscout/domain/subjects.py
</context>

<interfaces>
Preserve `PipelineStage` global prefix indices and the existing runner's `StageContext.prior_payloads`, `StageOutcome`, `StageTelemetry`, retry, `verify_run_chain`, and durable writer semantics. Extend state with exact Phase 3 configured-authority and finalized-identity lookups rather than weakening the Phase 1/2 seams. Compose an exact `PhaseTwoProcessor`; do not subclass it. New stage handlers consume only validated prior payloads and one isolated full workflow fingerprint selector.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-06-01: Add isolated subjects and the complete pre-lookup execution identity</name>
  <files>src/skillscout/domain/subjects.py, src/skillscout/domain/models.py, src/skillscout/application/pipeline.py, tests/test_phase3_pipeline.py</files>
  <read_first>src/skillscout/domain/subjects.py, src/skillscout/domain/models.py, src/skillscout/application/pipeline.py, tests/test_phase3_pipeline.py (new; confirm absent), src/skillscout/domain/enums.py, src/skillscout/domain/extraction.py, src/skillscout/domain/review.py, tests/test_phase2_pipeline.py, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - Candidate derivation sorts all validated WorkflowSpecs by full fingerprint, takes at most three, and emits one strict CandidateSubjectV1 per workflow with an optional explicit PriorLineageBindingV1; siblings never share a run or call.
    - PhaseThreeExecutionAuthorityV1 canonical bytes include every configured reuse-sensitive authority dimension before state lookup; changing each dimension independently changes its full digest.
    - FinalizedPhaseThreeExecutionIdentityV1 adds closed actual generator/Reviewer model IDs or explicit not-invoked sentinels and the terminal branch; inconsistent or multiple finalized identities fail closed.
    - `phase3-v1` is an additive spine prefix Scout..Reviewer with context enabled and COMPLETED terminal; phase2-v1 remains byte-behavior compatible.
    - ValidatedCandidateSummary contains only bounded identities, stage outcomes, qualification/validation/review decisions, relative artifact locator, package digest, and remote_writes_attempted=0.
  </behavior>
  <action>Add strict `CandidateSubjectV1(schema_version, repository, workflow_fingerprint, prior_lineage_binding)` and `derive_candidate_subjects(repository, workflows, bindings)` that reparses/binds workflows, sorts by the complete lowercase fingerprint, caps at three before any Phase 3 LLM call, rejects duplicate fingerprints/bindings, and returns one immutable subject per workflow; there is no batch subject or shared downstream authority. Resolve each optional `PriorLineageBindingV1` through Plan 03-03's exact durable lookup before run lookup. Define `PhaseThreeExecutionAuthorityV1` and canonical digest with: schema version; complete CandidateSubjectV1 and selected WorkflowSpec schema/fingerprint/version; exact `LineageResolutionV1` plus binding/authority digest; RunIdentity/retry/profile/producer/terminal-summary schema versions; qualification policy/report schema; generator prompt/output schema and configured generator model ID; renderer, artifact, provenance and quote-policy versions; validation policy/report schema; official validator distribution name/version, approved wheel identity and Gate-B3 uv.lock digest; Reviewer prompt/output/eligibility-policy versions and configured Reviewer model ID. Define `FinalizedPhaseThreeExecutionIdentityV1` by adding actual generator and Reviewer model IDs when invoked, explicit `not_invoked` for skipped calls, terminal branch code, and the configured-authority digest. Every field is closed/bounded and participates in canonical full SHA-256. Add independent table-driven sensitivity tests for every field, including each configured and actual model; wrong/missing/duplicate actual authority fails closed. Bind configured authority bytes into `RunIdentity` before any resumable/completed lookup. Add `("2", "phase3-v1")` to `SUPPORTED_PRODUCER_SCHEMAS`. Define bounded `ValidatedCandidateSummary` with configured/finalized identity digests, lineage resolution, optional artifact/package locator/digest only when present, stage outcomes and decisions. Add `PHASE_THREE_STAGE_SEQUENCE`, profile/runtime contracts, and unchanged Phase 2 regressions.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k 'subject or profile or summary or producer or execution_authority' tests/test_phase2_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - CandidateSubjectV1 rejects short IDs, malformed digests, duplicate/cross-workflow bindings, extra fields, and mismatched nested repository contracts.
    - Profile is exactly the global first eight stages and existing phase2 profile/root/terminal tests remain green.
    - Summary strictness/size tests prove only bounded decisions/identities and relative locator persist.
    - Sorted derivation is deterministic, capped at three, and proves sibling call/retry/ledger isolation with no batch authority.
    - Every execution-authority dimension has its own mutation test proving pre-lookup invalidation; finalized actual-model/branch mismatches cannot reuse.
  </acceptance_criteria>
  <done>The ledger can identify one exact workflow and complete execution authority before lookup, without batching, ambiguity, stale reuse, or Phase 2 mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-02: Compose PhaseTwoProcessor with the four closed Phase 3 stage handlers</name>
  <files>src/skillscout/application/phase3.py, tests/test_phase3_pipeline.py</files>
  <read_first>src/skillscout/application/phase3.py (new; confirm absent), tests/test_phase3_pipeline.py, src/skillscout/application/processors.py, src/skillscout/application/ports.py, src/skillscout/domain/qualification.py, src/skillscout/domain/skill_artifacts.py, src/skillscout/domain/validation.py, src/skillscout/domain/review.py, src/skillscout/adapters/openai_generate.py, src/skillscout/adapters/skills_ref.py, src/skillscout/adapters/openai_review.py, tests/test_phase2_pipeline.py</read_first>
  <behavior>
    - Scout..Extractor dispatch to the exact composed PhaseTwoProcessor with the nested RepositorySubject and preserve all verified Phase 2 payload/telemetry behavior.
    - Qualifier selects exactly the full fingerprint and emits the deterministic report; later handlers consume only prior validated payloads and never hydrate/read raw repository bundles.
    - The exact rejection call-count cascade is qualification 0/0, generation 1/0, validation 1/0, review 1/1 for Generator/Reviewer; official validator is called only after successful generation and preflight.
  </behavior>
  <action>Create composition-based `PhaseThreeProcessor` with exact concrete collaborators. For SCOUT..EXTRACTOR delegate after projecting the nested repository subject; never access its scratch read bundle downstream. QUALIFIER requires one exact selected full fingerprint and an authorized `new|retained` lineage resolution; any human-review-required lineage result becomes its own terminal business outcome with zero Generator/Reviewer calls and no lineage/package fallback. GENERATOR requires passed qualification, makes exactly one semantic call, renders/writes trusted package bytes, and persists bounded package/provenance/identity payload; refusal, incomplete, and schema invalid are distinct succeeded terminal branches. VALIDATORS requires a generated package, retains writer ownership, calls official then custom validation, and returns a succeeded validation-rejected branch on any error. REVIEWER is skipped on validation errors; otherwise it makes exactly one fresh call and records distinct refusal, incomplete, schema-invalid, NO, low-confidence, or eligible branches. Finalize `FinalizedPhaseThreeExecutionIdentityV1` from the configured authority plus the actual model returned at each invoked model stage or explicit not-invoked sentinel. Every business outcome is succeeded and immutable; only sanitized transient infrastructure failures retry. Populate telemetry with all authority versions and configured/actual model facts.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k 'processor or selector or cascade or call_count or telemetry'</automated>
  </verify>
  <acceptance_criteria>
    - Direct-dispatch tests cover happy path and every skip/rejection outcome with exact Generator/official-validator/Reviewer call counts.
    - Downstream handler inputs contain WorkflowSpec/provenance/package/report only; full Reader text and scratch hydration calls are absent.
    - Reviewer is structurally unreachable after any validation error, and its result never returns to renderer/writer.
    - All business rejections return StageOutcome payloads/telemetry rather than SafeFailure and therefore do not consume retry budget.
  </acceptance_criteria>
  <done>The four new stages form a closed, explainable cascade in which deterministic gates control every model call and terminal decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-03: Build the closed runtime, verified reuse path, and full integration proof</name>
  <files>src/skillscout/application/pipeline.py, src/skillscout/domain/models.py, src/skillscout/adapters/state.py, tests/test_phase3_pipeline.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/domain/models.py, tests/test_phase3_pipeline.py, src/skillscout/application/phase3.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/adapters/skill_packages.py, tests/test_phase2_pipeline.py, tests/test_pipeline_resume.py, .planning/phases/02-safe-single-repository-extraction/02-VERIFICATION.md</read_first>
  <behavior>
    - build_phase_three_runtime admits only exact supported concrete collaborators under the existing NONE/LOCAL_STATE/REMOTE_READ ceiling and rejects REMOTE_WRITE, missing, wrong, or subclassed registrations before invocation.
    - Interrupted runs resume only from verified prior payloads without replaying the successful prefix; only transient failures use the existing finite retry budget.
    - Resume/completed lookup is first keyed by exact configured execution-authority digest; completed reuse additionally verifies one intact full chain and exact finalized identity including actual model IDs and terminal branch.
    - Every completed rejection reprojects its exact summary with zero processor/GitHub/OpenAI/official-validator calls and zero new run/event/attempt rows; a package is rewritten only when the verified branch contains one.
  </behavior>
  <action>Add `build_phase_three_runtime()` and branch-aware terminal reprojection without changing `build_phase_two_runtime()`. Use exactly `PHASE_TWO_MAX_SCOPES` and exact concrete collaborators with no policy/registration widening. Extend `state.py` with exact lookup seams: verify a supplied prior-lineage binding against exactly one intact terminal record; find resumable/completed Phase 3 records only by the canonical configured execution-authority digest; then full-chain verify and require one exact finalized identity, including actual-model/not-invoked fields and terminal branch, before reuse. Zero or multiple finalized matches, any payload/digest/branch inconsistency, or lineage ambiguity fails closed before calls/writes. Reproject every verified terminal branch from its authoritative payload: qualification hard fail, qualification low score, lineage human review, generator refusal, generator incomplete, generator schema invalid, validation failure, Reviewer refusal, Reviewer incomplete, Reviewer schema invalid, Reviewer NO, Reviewer low confidence, and success. Always rewrite canonical `validated-candidate-summary.json`; materialize through `SkillPackageWriter` only for branches whose verified generator payload contains an exact package, and never require/reconstruct a package for pre-generation terminals. Reuse performs zero stage/GitHub/OpenAI/official-validator calls and appends zero run/event/attempt/result rows. Add same-identity rerun tests for every branch, byte-identical summary/package where applicable, absence of package otherwise, and independent fresh-work tests for every configured authority dimension; inject conflicting finalized actual-model records to prove fail-closed behavior. Preserve retry/resume, corruption, root/capability, canary and full Phase 1/2 regressions.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py tests/test_phase2_pipeline.py tests/test_pipeline_resume.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain/subjects.py src/skillscout/domain/models.py src/skillscout/application/phase3.py src/skillscout/application/pipeline.py src/skillscout/adapters/state.py tests/test_phase3_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Full integration produces eight verified succeeded attempts, exact local package/summary identities and `remote_writes_attempted=0`.
    - Every rejection terminates with auditable decision codes and exact no-call/retry counts; no rejection returns PublicationPlan.
    - Same-identity rerun coverage enumerates every terminal branch, creates no new ledger evidence, and materializes a package if and only if the verified branch has one.
    - Resume/reuse tests assert verified prefix ownership, complete configured/finalized identities, exact package digest when present, zero calls, and fresh work on every individual authority-version change.
    - Closed-root and source-capability tests prove no remote-write/publisher/subprocess/runtime-install/candidate-execution authority and all Phase 1/2 suites remain green.
  </acceptance_criteria>
  <done>The complete Phase 3 pipeline is resumable, idempotent, capability-closed, and outputs only an exact local validated-candidate decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CandidateSubjectV1 + lineage resolution → configured execution authority | One selected full fingerprint and exact lineage decision determine one isolated run before any reuse lookup. |
| Verified stage ledger → resumed/reused execution | Persisted payloads authorize skipping work only after full-chain verification. |
| Component adapters → production composition root | Adapter scope/type registration determines reachable filesystem/network authority. |
| CandidateDecision → Phase 4 | Only the exact eligible local decision/package digest may be a future publication input; Phase 3 has no writer to remote systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06-SEL | Spoofing | workflow selector/lineage binding | high | mitigate | Sorted full fingerprints, strict CandidateSubjectV1, exact durable binding lookup, isolated runs, and missing/duplicate/ambiguous failure. |
| T-03-06-AUTH | Elevation of Privilege | composition root | critical | mitigate | Exact concrete registration, immutable REMOTE_READ ceiling, no caller widening, no Publisher/REMOTE_WRITE, rejection before invocation. |
| T-03-06-LEDGER | Tampering / Repudiation | resume/completed reuse | high | mitigate | Existing full-chain verifier authorizes every prefix/reuse; recompute identities/digests and reject corruption before calls/writes. |
| T-03-06-PII | Information Disclosure | stage/durable payloads | high | mitigate | WorkflowSpec-only semantic boundary, bounded contracts, relative locators, secret/raw-text canary sweeps. |
| T-03-06-RETRY | Repudiation / Denial of Service | business outcomes and retry | medium | mitigate | Decided outcomes succeed once; SDK retries zero; existing finite runner policy alone retries transient infrastructure failures. |
</threat_model>

## Artifacts this phase produces

- New source file: `src/skillscout/application/phase3.py`.
- Modified source files: `src/skillscout/domain/subjects.py`, `src/skillscout/domain/models.py`, `src/skillscout/application/pipeline.py`, `src/skillscout/adapters/state.py`.
- New symbols: `CandidateSubjectV1`, `derive_candidate_subjects`, `PhaseThreeExecutionAuthorityV1`, `FinalizedPhaseThreeExecutionIdentityV1`, `phase_three_execution_authority`, `ValidatedCandidateSummary`, `PhaseThreeProcessor`, `PHASE_THREE_STAGE_SEQUENCE`, `PhaseThreeRuntime`, `build_phase_three_runtime`, exact prior-lineage/configured-authority/finalized-identity state lookups, additive `("2", "phase3-v1")` producer/profile entries and branch-aware terminal reprojection helpers.
- New test file: `tests/test_phase3_pipeline.py` with selector, profile, processor, call-count, telemetry, retry, resume, reuse, corruption, authority, canary and regression groups.
- Runtime artifacts: exact generated Skill package, external package manifest/digest, `validated-candidate-summary.json`, eight-stage ledger/checkpoint manifests; no PublicationPlan or remote artifact.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Convert extracted workflow to explainable validated local Skill | 03-06 | COVERED | End-to-end local pipeline and terminal summary. |
| REQ | QUAL-01, QUAL-02 | Qualification executes before generation | 03-06 | COVERED | Processor ordering, skip/call-count proof. |
| REQ | GEN-01, GEN-02, GEN-03, GEN-04, GEN-05 | Generate/materialize identity-bound package and reuse/update | 03-06 | COVERED | Generator stage, selector/lineage, exact reuse. |
| REQ | VAL-01, VAL-02, VAL-03 | Official/custom structured validation blocks Reviewer | 03-06 | COVERED | Validator stage and zero-Reviewer error cascade. |
| REQ | REV-01, REV-02, REV-03 | Independent review and exact eligibility predicate | 03-06 | COVERED | Reviewer stage and terminal CandidateDecision. |
| RESEARCH | Additive phase3-v1 full prefix | Preserve ledger invariants | 03-06 | COVERED | No cross-profile checkpoint import. |
| RESEARCH | One workflow per request | Selection resolution | 03-06 | COVERED | Sorted full-fingerprint CandidateSubjectV1 derivation creates up to three independent runs with no batch authority. |
| RESEARCH | Same identity zero-call reuse | Idempotency | 03-06 | COVERED | Full-chain completed reuse including validator zero-call. |
| PHASE2 | EXTR-04 handoff | WorkflowSpec sole semantic boundary | 03-06 | COVERED | No Reader bundle/hydration downstream of Extractor. |
| PHASE2 | Authority/retry | REMOTE_READ ceiling and runner-owned retry | 03-06 | COVERED | Root and call-count tests preserve both. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | No locked/deferred decisions to map. |

<verification>
- Phase 3 integration, Phase 2 profile, and shared resume suites pass under the exact approved lock; Ruff passes owned paths.
- Exact business-outcome call-count matrix, all-stage resume, complete reuse, corruption rejection, selector identity, and remote-write canaries are green.
- Durable-surface scans contain no raw repository text, credentials, absolute paths, Reviewer replacement bytes, or publication authority.
</verification>

<success_criteria>
- One exact extracted workflow becomes one auditable local candidate through the complete eight-stage prefix.
- Same identity reuses the exact verified package with zero provider/validator calls; changed identity/version re-evaluates.
- No deterministic failure or unfavorable Reviewer result can become eligible or reach publication planning.
- Phase 1 and Phase 2 verified behavior remains green and unchanged.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-06-SUMMARY.md` with the selector contract, profile/root registry, call-count table, terminal schema, and resume/reuse evidence.</output>
