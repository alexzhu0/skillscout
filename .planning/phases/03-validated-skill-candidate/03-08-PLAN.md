---
phase: 03-validated-skill-candidate
plan: 08
type: execute
wave: 7
depends_on:
  - 03-05
  - 03-07
  - 03-04
files_modified:
  - src/skillscout/domain/skill_artifacts.py
  - src/skillscout/adapters/openai_generate.py
  - tests/recorded_transport.py
  - tests/test_openai_generate.py
  - tests/test_skill_generation.py
  - tests/fixtures/openai/generator/cases.json
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
must_haves:
  truths:
    - "A qualified WorkflowSpec is transformed by one tool-free, store=false, strict Structured Outputs generation call into a docs-only semantic draft."
    - "A deterministic renderer produces a standard Agent Skills directory with SKILL.md, necessary references/assets only, no scripts, binaries, executable modes, copied executable code, or allowed-tools grant."
    - "Generated instructions generalize the workflow and any bounded quote is attributed to source path and exact commit."
    - "GeneratedArtifactIdentityV1 binds the canonical semantic draft to the complete generation-time authority projection before rendering; it is distinct from the rendered package digest."
    - "Provenance binds repository URL/ID, exact SHA, SPDX, path/blob/content hashes, Phase 2 anchor, complete WorkflowSpec authority, lineage/slug, qualification report authority, producer/profile/retry/request/usage evidence, required versions, and configured plus actual Generator model identity."
    - "After provenance is final, package_digest independently binds the rendered path/hash/mode/size manifest; neither identity includes Reviewer or future facts or causes package bytes to be rewritten."
  artifacts:
    - path: "src/skillscout/domain/skill_artifacts.py"
      provides: "Strict semantic-draft identity, provenance, rendered manifest/package digest, frozen package, and deterministic renderer contracts."
    - path: "src/skillscout/adapters/openai_generate.py"
      provides: "Isolated OpenAI Generator adapter with bounded semantic outcomes."
    - path: "tests/test_openai_generate.py"
      provides: "Transport contract, refusal/incomplete/schema failure, telemetry, and injection-resistance tests."
    - path: "tests/test_skill_generation.py"
      provides: "Renderer, package tree/mode, quote, provenance, digest, and stability tests."
  key_links:
    - from: "QualificationReportV1 pass"
      to: "OpenAIGenerationClient"
      via: "Only an accepted report authorizes one bounded Generator request."
    - from: "GeneratedSkillDraft + generation-time authority"
      to: "GeneratedArtifactIdentityV1"
      via: "Canonical semantic identity is computed before rendering."
    - from: "GeneratedArtifactIdentityV1 + complete provenance"
      to: "FrozenSkillPackageV1"
      via: "Versioned deterministic renderer."
    - from: "FrozenSkillPackageV1"
      to: "package_digest"
      via: "A distinct external path/hash/mode/size manifest digest is computed only after provenance bytes are final."
  prohibitions:
    - "Do not send raw repository contents, secrets, credentials, tools, or code-execution permissions to the Generator."
    - "Do not generate scripts/, binaries, executable files, or copied executable code."
    - "Do not place Reviewer identity, validation results, eligibility, terminal outcome, or package_digest inside package provenance."
---

<objective>
Generate and freeze a standard, source-clear, docs-only Agent Skill package.

Purpose: The semantic Generator should generalize a verified workflow, while deterministic code owns layout, provenance, quote budgets, modes, and immutable package identity.
Output: Strict generation contract and adapter, deterministic package renderer, complete provenance, external package identity, and adversarial tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/qualification.py
@src/skillscout/domain/extraction.py
@src/skillscout/adapters/openai_extract.py
@tests/recorded_transport.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the semantic draft and frozen package contracts</name>
  <files>src/skillscout/domain/skill_artifacts.py, tests/test_skill_generation.py</files>
  <read_first>
    - `src/skillscout/domain/extraction.py` — follow strict frozen structured-contract conventions.
    - `src/skillscout/domain/candidate_authority.py` — bind every artifact to complete source/execution authority and lineage.
    - `.planning/REQUIREMENTS.md` — implement GEN-01 through GEN-05 artifact and provenance fields.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — implement the frozen package/external identity boundary.
  </read_first>
  <behavior>
    - Unknown/missing generation or provenance fields fail strict validation.
    - Artifact paths are relative, normalized, collision-free, non-link, and restricted to SKILL.md plus required references/assets.
    - GeneratedArtifactIdentityV1 changes when the canonical semantic draft or any generation-time authority field changes, but not because of a renderer-only path/mode choice.
    - Package content and modes are immutable after construction; package_digest changes when any rendered path/hash/mode/size changes.
    - The semantic artifact identity and rendered package digest are separate canonical layers with separate schema/version fields.
  </behavior>
  <action>
Create strict frozen versioned models for `GeneratedSkillDraft`, attributed quote/source references, `GenerationAuthorityProjectionV1`, external semantic `GeneratedArtifactIdentityV1`, provenance records, rendered files, `RenderedPackageManifestV1`, `FrozenSkillPackageV1`, and external `PackageIdentityV1`. Export separate generation-draft, generated-artifact-identity, provenance, quote, rendered-manifest, and package-identity schema versions plus `RENDERER_VERSION = "skill-renderer-v1"`. `GeneratedArtifactIdentityV1` must be computed before rendering from the canonical `GeneratedSkillDraft` bytes and an explicit generation-only `GenerationAuthorityProjectionV1`: Phase 2 run/terminal/chain anchor, complete WorkflowSpec authority and selected full fingerprint, repository/lineage/stable slug, qualification report digest plus qualification schema/policy/threshold versions, configured and actual Generator model, generation prompt/output-schema/policy versions, renderer/artifact/provenance schema versions, and Generator producer/profile/retry versions. The projection must not embed `CandidateExecutionAuthorityV1` or its digest because that authority also contains downstream validator, Reviewer, and eligibility versions. It must also exclude request ID, token usage/latency, rendered paths/modes/manifest, package provenance bytes, validation, Reviewer, eligibility, and terminal facts. Model package paths with a closed grammar: root `SKILL.md`, `references/`, and `assets/`; reject absolute/traversal/duplicate/case-colliding/link paths, `scripts/`, binary content, and executable modes. Provenance must carry the semantic generated-artifact identity and explicitly repeat the source repository URL/ID/exact commit/SPDX/path/blob/content hashes, Phase 2 anchor, complete WorkflowSpec authority/fingerprint, lineage/slug, qualification digest/schema/policy/threshold, Generator producer/profile/retry, request ID/usage/latency evidence, renderer/artifact/provenance schemas, and configured plus actual Generator identity. Exclude validator, Reviewer, eligibility, terminal, and all other future facts. After provenance bytes are final, build `RenderedPackageManifestV1` from every relative path, content hash, mode, and size and derive the distinct external `package_digest(...)`/`PackageIdentityV1`. Neither identity may self-embed its own digest.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_generation.py -k contracts</automated>
  </verify>
  <acceptance_criteria>
    - Contracts are strict/frozen/versioned and reject path, mode, binary, and future-fact violations.
    - `RENDERER_VERSION` is exported from the owning module.
    - GeneratedArtifactIdentityV1 is a canonical draft + explicit generation-only authority identity and has full-field sensitivity tests; downstream validator/Reviewer/eligibility versions and request/usage evidence are proven absent from its preimage.
    - Complete Phase 2, source, lineage/slug, qualification, producer/profile/retry/request/usage, and configured/actual Generator authority are present in provenance.
    - PackageIdentityV1 is a separate rendered path/hash/mode/size identity computed after provenance; neither layer is self-embedded.
  </acceptance_criteria>
  <done>Semantic output, package provenance, immutable package bytes, and external package identity have non-circular strict contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate the tool-free OpenAI Generator request</name>
  <files>src/skillscout/adapters/openai_generate.py, tests/recorded_transport.py, tests/test_openai_generate.py, tests/fixtures/openai/generator/cases.json</files>
  <read_first>
    - `src/skillscout/adapters/openai_extract.py` — mirror Responses API transport, strict parsing, `store=false`, request-ID, usage, and sanitized-failure patterns without changing extraction.
    - `src/skillscout/domain/skill_artifacts.py` — target Task 1's strict `GeneratedSkillDraft`.
    - `tests/recorded_transport.py` — extend the shared fake transport without weakening existing fixtures.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — enforce Generator context and refusal/incomplete/schema-failure boundaries.
  </read_first>
  <behavior>
    - The request uses the configured model, strict Structured Outputs, `store=false`, no tools, no code execution, and only verified WorkflowSpec plus bounded trusted policy context.
    - Untrusted WorkflowSpec text is clearly delimited as data and cannot change developer policy.
    - Success captures actual response model, request ID, latency, and token usage separately from package semantics.
    - Refusal, incomplete response, schema failure, transport failure, and retry exhaustion are distinct bounded results with no raw response leakage.
  </behavior>
  <action>
Implement `OpenAIGenerationClient` and strict `GenerationResult` in `adapters/openai_generate.py`, using the same injected transport style as extraction. Send only the verified structured WorkflowSpec, complete source authority references, lineage/slug, qualification report, and trusted generation policy; never send raw repository files or previous model transcripts. Set `store=false`, declare no tools, request the strict `GeneratedSkillDraft` schema, and cap input/output/retries using the versioned runtime profile. Keep configured model ID in execution authority and capture actual response model ID only in outcome telemetry/provenance. Map refusal, incomplete, schema validation failure, retryable infrastructure failure, and terminal infrastructure failure to bounded typed outcomes. Sanitize logs/exceptions and record request ID/token/latency without content or credentials. Add recorded success, refusal, incomplete, malformed/extra-field output, prompt-injection, retry, and no-tools/store=false cases.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_generate.py</automated>
  </verify>
  <acceptance_criteria>
    - One isolated adapter owns all Generator OpenAI imports.
    - Tests inspect the exact request and prove no tools, `store=false`, strict schema, bounded context, and configured model.
    - Actual model/request/usage telemetry is external evidence and cannot mutate prelookup authority.
    - All semantic failure modes are distinguishable and sanitized.
  </acceptance_criteria>
  <done>The Generator is a bounded semantic transform with no tool or raw-source execution channel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render generalized instructions, attribution, and immutable provenance</name>
  <files>src/skillscout/domain/skill_artifacts.py, tests/test_skill_generation.py</files>
  <read_first>
    - `src/skillscout/domain/skill_artifacts.py` — render only validated Task 1/2 contracts.
    - `.planning/REQUIREMENTS.md` — implement GEN-01, GEN-02, GEN-03, and GEN-04 output guarantees.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — adopt quote caps and frontmatter choice as versioned project policy.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — preserve manifest-first, provenance-before-digest ordering.
  </read_first>
  <behavior>
    - Rendering is deterministic for identical draft, authority, lineage, and version inputs.
    - SKILL.md frontmatter has a valid stable name/description and omits `allowed-tools`.
    - Every quote is at most 120 characters, all quotes total at most 240 characters, and each quote records source path plus exact commit SHA.
    - Semantic artifact identity is fixed before rendering; package contains no scripts/binaries/executable modes/copied executable code and provenance is complete before the separate package digest.
  </behavior>
  <action>
Implement a deterministic `generated_artifact_identity(...)` followed by `render_skill_package(...)`, both owned by `skill_artifacts.py`. First bind the exact canonical semantic draft to only the explicit generation-only authority projection; never pass the full CandidateExecutionAuthority into this constructor. Then produce the stable lineage slug directory, `SKILL.md`, `references/provenance.json`, and only genuinely necessary generated references/assets. Adopt the research recommendations under agent discretion as explicit versioned conservative policy: omit `allowed-tools`; cap each attributed quote at 120 characters and all attributed quotes at 240 characters. Require every quote to cite source path and exact commit SHA, and paraphrase/generalize all remaining instructions so they describe a reusable workflow rather than the source repository. Refuse any draft that requests scripts, binaries, executable modes, copied executable blocks, network download-and-execute, or missing attribution. Serialize the complete non-future provenance preimage, including request/usage evidence that is deliberately outside semantic identity, safely and deterministically; freeze all file bytes/modes, then derive the distinct rendered path/hash/mode/size manifest and external package digest. Add golden/determinism tests, path/mode/binary adversarial cases, quote boundaries, every provenance/generation-authority-field sensitivity case, and semantic-identity exclusion tests proving validator/Reviewer/eligibility version or request/usage mutations do not change GeneratedArtifactIdentityV1. Separately prove request/usage/provenance and renderer-only path/hash/mode/size mutations change PackageIdentityV1, preserve stable slug/update identity, exclude self-digests, and never allow later validation/review/terminal facts to rewrite package bytes.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Standard package has `SKILL.md` and `references/provenance.json`; optional references/assets are justified and deterministic.
    - Package tree has no `scripts/`, binary bytes, executable mode, or `allowed-tools`.
    - Quote budgets and path+commit attribution are enforced at both boundaries.
    - Identical inputs produce exact identical semantic identity, package, manifest, and package digest.
    - A semantic-draft or generation-only-authority mutation changes GeneratedArtifactIdentityV1; downstream validator/Reviewer/eligibility or request/usage mutations do not.
    - Request/usage/provenance or renderer-only path/hash/mode/size mutations change PackageIdentityV1 without conflating the two.
  </acceptance_criteria>
  <done>A qualified workflow renders into a frozen, generalized, attributed, docs-only Skill package with complete non-circular provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkflowSpec → OpenAI Generator | Untrusted extracted text enters a tool-free semantic model request as data only. |
| Generated draft → local Skill package | Model output cannot choose paths, modes, provenance omissions, or executable content unchecked. |
| Frozen package → external identity | Digest must bind exact bytes without circular self-reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Elevation of Privilege | Generator prompt injection | high | mitigate | No tools/code execution; untrusted structured data is delimited; strict output schema and deterministic renderer. |
| T-03-20 | Tampering | Generated package paths/modes | high | mitigate | Closed path grammar, binary/script/executable rejection, manifest-first digest tests. |
| T-03-21 | Repudiation | Source attribution | high | mitigate | Complete provenance plus bounded path+exact-SHA quote records. |
| T-03-22 | Information Disclosure | Model/log payloads | high | mitigate | No raw repository/secrets; `store=false`; sanitized logs and bounded telemetry. |
</threat_model>

## Artifacts this phase produces

- Strict Generator and frozen package contracts.
- Tool-free OpenAI Generator adapter.
- Deterministic docs-only Skill renderer and provenance.
- External package identity and adversarial fixture suites.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Standard, docs-only, source-clear local Skill | COVERED by frozen renderer/provenance. |
| REQ | GEN-01, GEN-02, GEN-03, GEN-04, GEN-05 | COVERED exactly. |
| RESEARCH | Structured Generator; quote caps; provenance path; omitted allowed-tools; frozen package/external digest | COVERED as versioned conservative policy. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Confirm the renderer does not import OpenAI and the adapter does not write files.
</verification>

<success_criteria>
- Generator has no execution/tool/raw-source channel.
- Renderer deterministically owns every package byte/path/mode.
- Provenance is complete, package identity is external, and review cannot rewrite the package.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-08-SUMMARY.md` when done.
</output>
