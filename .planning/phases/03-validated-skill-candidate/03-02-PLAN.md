---
phase: 03-validated-skill-candidate
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "Only the exact skills-ref 0.1.1 candidate approved at Gate A3 is added to the locked project graph."
    - "Resolution is registry-only and does not install, import, test, or execute skills-ref or any transitive artifact."
    - "The resulting uv.lock contains the exact wheel hash audited in research and an inspectable complete transitive graph."
  artifacts:
    - path: "pyproject.toml"
      provides: "Exact direct dependency declaration for skills-ref 0.1.1."
    - path: "uv.lock"
      provides: "Exact registry-resolved graph and artifact hashes for Gate B3 review."
  key_links:
    - from: "03-01-SUMMARY.md"
      to: "pyproject.toml"
      via: "The Gate A3 approval authorizes only the exact declaration."
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv resolves the complete graph without executing package code."
  prohibitions:
    - "Do not run uv sync, uv run, Python imports, pytest, Ruff, mypy, skills-ref, or any installed console script."
    - "Do not accept a different skills-ref version or an artifact whose hash differs from the audited wheel."
---

<objective>
Resolve the Gate-A3-approved official validator candidate into an exact lock graph without executing it.

Purpose: Gate B3 needs exact transitive package names, versions, sources, hashes, and immutable `uv.lock` bytes to review.
Output: A narrowly changed `pyproject.toml` and complete `uv.lock`, ready for blocking human inspection.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the exact official-validator graph without execution</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - `.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md` — require the literal `approved A3` decision.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — use the audited version, wheel hash, and Gate B3 evidence requirements.
    - `pyproject.toml` — preserve existing dependency grouping and project conventions.
    - `uv.lock` — preserve lock format and unrelated exact versions.
  </read_first>
  <action>
After confirming Gate A3 approval, add exactly `skills-ref==0.1.1` in the project dependency location consistent with the existing `pyproject.toml`. Use uv's registry resolver only to update `uv.lock`; do not sync the environment and do not invoke any package code. Preserve unrelated locked versions unless resolution requires a documented transitive change. Confirm the lock selects the audited PyPI wheel SHA-256 `d35db5bb8de71ae301daf5ca9cb71f8a555e8c6f83a6d40e46a5bc09f8f461b5`, records `click` and `strictyaml` plus their complete transitive artifacts, and contains hashes/sources sufficient for Gate B3 inspection. If the exact artifact is unavailable, the hash differs, or the graph introduces an unreviewed source type, stop and report the mismatch rather than resolving a substitute.
  </action>
  <verify>
    <automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" tree --locked --package skills-ref</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` declares exactly `skills-ref==0.1.1`.
    - `uv.lock` is internally consistent and contains the audited wheel hash.
    - Every transitive package has an exact version, source, and artifact hash available for Gate B3 review.
    - No command installed, imported, tested, or executed package code.
  </acceptance_criteria>
  <done>The exact candidate graph is locked and inspectable, with no package code executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry metadata → lock graph | External package metadata and artifacts become candidate executable dependencies. |
| Gate A3 summary → dependency declaration | Only the approved exact identity may cross into project metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | uv dependency resolution | high | mitigate | Pin exact version, confirm audited wheel hash, preserve full artifact hashes, and prohibit execution before Gate B3. |
| T-03-03 | Elevation of Privilege | Build/install hooks | high | mitigate | Use lock-only registry resolution; prohibit sync, imports, tests, and console scripts. |
| T-03-04 | Tampering | Transitive sources | high | mitigate | Fail if the graph includes unexpected source types or lacks exact artifact hashes. |
</threat_model>

## Artifacts this phase produces

- Exact `skills-ref==0.1.1` dependency declaration.
- Exact `uv.lock` graph for Gate B3 inspection.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Safe standards validation | COVERED — resolves the official validator without crossing the execution boundary. |
| REQ | VAL-01 | COVERED — prepares the exact official validator dependency. |
| RESEARCH | Exact version/hash; registry-only resolution; Gate B3 prerequisite | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run only lock metadata checks shown in the task. Save the complete graph and `git diff -- pyproject.toml uv.lock` in the plan summary for Gate B3.
</verification>

<success_criteria>
- Lock metadata exactly matches the approved validator identity and audited wheel.
- No candidate distribution code has executed.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md` when done.
</output>
