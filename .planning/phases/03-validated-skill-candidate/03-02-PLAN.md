---
phase: 03-validated-skill-candidate
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/skillscout/domain/qualification.py
  - tests/test_qualification.py
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "Every WorkflowSpec is revalidated and scored by a versioned deterministic 100-point rubric before generation."
    - "The default pass threshold is exactly 75, and any ordered hard failure rejects regardless of score."
    - "Qualification reports expose every check, points, observed code, policy/schema version, pass state, and ordered rejection reasons without raw source text."
  artifacts:
    - { path: src/skillscout/domain/qualification.py, provides: "closed qualification contracts and pure evaluator", exports: [QUALIFICATION_POLICY_VERSION, QUALIFICATION_REPORT_SCHEMA_VERSION, QualificationDimension, QualificationCheck, QualificationReport, qualify_workflow] }
    - { path: tests/test_qualification.py, provides: "score, binding, ordering, threshold, and hard-failure matrix" }
  key_links:
    - { from: src/skillscout/domain/qualification.py, to: src/skillscout/domain/extraction.py, via: "strict WorkflowSpec revalidation and evidence binding", pattern: "WorkflowSpec" }
    - { from: src/skillscout/domain/qualification.py, to: src/skillscout/domain/canonical.py, via: "canonical hashes for binding comparisons", pattern: "sha256_digest" }
  prohibitions:
    - "No LLM, filesystem, network, candidate-code execution, or model-provided score participates in qualification."
    - "A high numeric score cannot override invalid contracts, low confidence, missing evidence, unauthorized execution, credential access, injection residue, or unapproved side effects."
    - "Qualification output must not persist complete repository text, raw secret-like matches, or provider exceptions."
---

<objective>
Implement the deterministic qualification gate that decides whether an extracted workflow may reach generation.

Purpose: Make eligibility explainable, stable, and fail-closed before any generation cost or model authority exists.
Output: Versioned qualification contracts, a pure bound evaluator, and boundary-complete unit evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md
@src/skillscout/domain/extraction.py
@src/skillscout/domain/canonical.py
@src/skillscout/domain/models.py
</context>

<interfaces>
Use `WorkflowSpec` and its evidence/step contracts from `domain/extraction.py`; all persisted models inherit `StrictFrozenModel(extra="forbid", frozen=True, strict=True)`. Use `sha256_digest` only for canonical comparisons; do not invent a second digest format.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Freeze the 100-point policy and itemized report contract</name>
  <files>src/skillscout/domain/qualification.py, tests/test_qualification.py</files>
  <read_first>src/skillscout/domain/qualification.py (new; confirm absent), tests/test_qualification.py (new; confirm absent), src/skillscout/domain/extraction.py, src/skillscout/domain/models.py, tests/test_phase2_contracts.py, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - The declared rubric totals exactly 100 points in fixed order: specificity 25, reusability 20, verifiability 20, evidence sufficiency 25, safety readiness 10.
    - Scores 74/75/76 without hard failures yield fail/pass/pass at the exact default threshold 75.
    - Reports are frozen, strict, bounded, versioned, contain every itemized check, and derive score/counts/pass state rather than accepting contradictory caller values.
  </behavior>
  <action>Create `src/skillscout/domain/qualification.py` with `QUALIFICATION_POLICY_VERSION = "qualification-policy-v1"`, `QUALIFICATION_REPORT_SCHEMA_VERSION = "qualification-report-v1"`, `QUALIFICATION_THRESHOLD = 75`, `MIN_EXTRACTION_CONFIDENCE = 0.70`, a closed `QualificationDimension`, fixed ordered check definitions, `QualificationCheck`, and `QualificationReport`. Encode the exact rubric from research: specificity (goal 5, at least three ordered steps 10, inputs 5, outputs 5); reusability (applicability 5, preconditions 5, non-goals 5, absence of exact owner/repo/evidence-path dependence 5); verifiability (failure modes 5, every step evidence-bound 5, workflow evidence 5, every evidence hash bound 5); evidence sufficiency (distinct path 5, 100% step coverage 10, bounded support statements 5, complete repo/commit/license facts 5); safety readiness (prohibited actions 5, side effects explicitly safe or approval-named 5). Keep check IDs, rationale codes, observed codes, and rejection codes closed and ordered; cap all strings/collections. Derive `score`, `hard_failures`, `passed`, and `rejection_reasons` inside the trusted constructor/evaluator so a caller cannot create an inconsistent report. Write the exact score/threshold/order/schema tests first, then implement until they pass.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_qualification.py -k 'rubric or threshold or order or contract'</automated>
  </verify>
  <acceptance_criteria>
    - Source exports all named constants/models and the points-possible sum is asserted equal to 100.
    - The check sequence and rejection sequence are stable across repeated evaluation and input mapping order.
    - Exact 74/75/76 tests pass; threshold is serialized as 75 with both schema and policy versions.
    - Contradictory report totals/pass state and extra fields are rejected by the strict contract.
  </acceptance_criteria>
  <done>The qualification policy is a closed 100-point contract whose threshold and explanation ordering are mechanically proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: Bind WorkflowSpec evidence and enforce every hard failure</name>
  <files>src/skillscout/domain/qualification.py, tests/test_qualification.py</files>
  <read_first>src/skillscout/domain/qualification.py, tests/test_qualification.py, src/skillscout/domain/extraction.py, src/skillscout/domain/filtering.py, tests/test_extractor_boundary.py, .planning/phases/02-safe-single-repository-extraction/02-VERIFICATION.md</read_first>
  <behavior>
    - Confidence 0.699 hard-fails and 0.700 remains eligible for scoring.
    - Fewer than three steps, empty inputs/outputs, invalid WorkflowSpec, repo/fingerprint/evidence binding mismatch, unknown or hash-mismatched evidence, candidate install/build/import/execute dependence, credential access, destructive/download-execute/permission bypass, prompt-injection residue, and approval-requiring side effects without a named approval always fail.
    - Every failure is returned as an ordered succeeded business decision and never invokes external code or consumes retry authority.
  </behavior>
  <action>Implement keyword-only `qualify_workflow(workflow, *, repo_id, repository_url, commit_sha, license_spdx, recorded_evidence) -> QualificationReport`. Reparse `workflow` through `WorkflowSpec.model_validate`, recompute its fingerprint with the established `workflow_fingerprint` function, and compare every evidence path/blob/content hash to the verified upstream record before scoring. Add closed normalized pattern checks for candidate-repository execution/install/build/import dependence, credential access, destructive or download-and-execute instructions, permission bypass, prompt-injection residue, and unnamed approval-requiring side effects; return pattern/rationale codes only, never matched text. Apply hard failures before deriving pass state but still emit every rubric check in fixed order. Adopt the research-recommended 0.70 confidence floor as a versioned policy decision and pin it in tests. Table-drive each hard failure, binding mismatch, 0.699/0.700 boundary, and a fully valid 75+ report; assert the input object is not mutated and no filesystem/network/LLM seam exists.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_qualification.py &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain/qualification.py tests/test_qualification.py</automated>
  </verify>
  <acceptance_criteria>
    - Every named hard failure has a focused test asserting `passed is False` regardless of score.
    - The evidence-binding matrix covers unknown path, blob mismatch, content-hash mismatch, missing repo/commit/license facts, and fingerprint mismatch.
    - The report contains no complete source text or matched secret/injection text; only bounded closed observed/rationale codes leave the evaluator.
    - The focused suite and Ruff exit 0 under the Gate-B3-approved exact lock.
  </acceptance_criteria>
  <done>Only a strictly bound WorkflowSpec with score at least 75 and zero hard failures can proceed to Generator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted Extractor payload → qualification | Model-originated structured data remains untrusted and must be rebound to verified upstream facts. |
| Qualification report → Generator authority | `passed` is the sole deterministic authorization to spend a generation call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-QUAL | Tampering | `qualify_workflow` inputs/report | high | mitigate | Strict reparse, fingerprint/repo/evidence rebind, derived totals/pass state, immutable contracts, and full boundary matrix. |
| T-03-02-SPOOF | Spoofing | evidence identity | high | mitigate | Require exact path/blob/content hash matches to the verified Phase 2 record before any points or authorization. |
| T-03-02-ID | Information Disclosure | qualification diagnostics | medium | mitigate | Emit closed pattern/rationale/observed codes only; never include matched source or secret-like bytes. |
</threat_model>

## Artifacts this phase produces

- New file `src/skillscout/domain/qualification.py`.
- New symbols: `QUALIFICATION_POLICY_VERSION`, `QUALIFICATION_REPORT_SCHEMA_VERSION`, `QUALIFICATION_THRESHOLD`, `MIN_EXTRACTION_CONFIDENCE`, `QualificationDimension`, `QualificationCheck`, `QualificationReport`, `qualify_workflow` and the closed ordered rubric/hard-failure registries.
- New test file `tests/test_qualification.py` with score, confidence, binding, hard-failure, ordering, and strict-contract matrices.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain qualification conclusions | 03-02 | COVERED | Itemized ordered report with versions and reasons. |
| REQ | QUAL-01 | Deterministic versioned qualification dimensions and unauthorized-execution checks | 03-02 | COVERED | Pure evaluator and hard-failure matrix. |
| REQ | QUAL-02 | 75/100 threshold, no hard failures, itemized output | 03-02 | COVERED | Exact 100-point rubric and boundary tests. |
| RESEARCH | Closed qualification policy | 0.70 confidence floor and recommended weights | 03-02 | COVERED | Adopted as explicit versioned policy with tests. |
| RESEARCH | WorkflowSpec sole boundary | No raw Reader bundle | 03-02 | COVERED | Evaluator accepts only WorkflowSpec plus verified fact map. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | No locked/deferred decisions to map. |

<verification>
- The repository-local locked command passes all qualification tests and Ruff.
- Rubric sum, threshold boundaries, hard failures, deterministic order, evidence bindings, and sanitized diagnostics are all directly asserted.
- No new dependency, filesystem, network, LLM, or candidate execution seam exists.
</verification>

<success_criteria>
- Qualification is deterministic, versioned, totals exactly 100 points, and passes only at score ≥75 with no hard failure.
- Every conclusion is explainable through ordered bounded checks and reasons.
- Generator authorization cannot be forged by model output or mismatched upstream facts.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md` with the frozen policy, all boundary evidence, and exported interfaces for downstream plans.</output>
