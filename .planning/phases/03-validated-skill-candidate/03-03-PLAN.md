---
phase: 03-validated-skill-candidate
plan: "03"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/skillscout/domain/skill_artifacts.py
  - src/skillscout/adapters/skill_packages.py
  - src/skillscout/adapters/openai_generate.py
  - tests/test_skill_generation.py
  - tests/test_openai_generate.py
  - tests/fixtures/openai/generator/cases.json
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05]
must_haves:
  truths:
    - "A model returns bounded semantic fields only; trusted code owns slug, paths, frontmatter, links, modes, provenance, identities, and exact package bytes."
    - "A qualified WorkflowSpec can render a stable Agent Skill tree containing SKILL.md, provenance.json, and only necessary one-level text resources."
    - "Generated packages contain no scripts directory, binary, executable bit, allowed-tools authority, candidate executable code, or unregistered long copy."
    - "Lineage identity, semantic artifact identity, and exact package digest are distinct, full SHA-256-bound concepts with no self-hash cycle."
    - "The Generator is one fresh tool-less store-false Structured Outputs call per attempt and model rejection is a closed business outcome."
  artifacts:
    - { path: src/skillscout/domain/skill_artifacts.py, provides: "draft/provenance/manifest contracts, identities, and deterministic renderer", exports: [GENERATION_PROMPT_VERSION, ARTIFACT_SCHEMA_VERSION, PROVENANCE_SCHEMA_VERSION, LINEAGE_VERSION, GeneratedSkillDraft, SkillProvenance, SkillFile, SkillPackage, lineage_id, artifact_id, package_digest, render_skill_package] }
    - { path: src/skillscout/adapters/skill_packages.py, provides: "descriptor-anchored documentation-only package writer", exports: [SkillPackageWriter] }
    - { path: src/skillscout/adapters/openai_generate.py, provides: "isolated Generator Responses adapter", exports: [OpenAIGenerationClient, GenerationResult] }
    - { path: tests/test_skill_generation.py, provides: "identity, bytes, tree, mode, attribution, and durability matrix" }
    - { path: tests/test_openai_generate.py, provides: "Generator request/outcome/error/credential contract proof" }
    - { path: tests/fixtures/openai/generator/cases.json, provides: "named recorded Generator response cases" }
  key_links:
    - { from: src/skillscout/adapters/openai_generate.py, to: src/skillscout/domain/skill_artifacts.py, via: "strict GeneratedSkillDraft Structured Output", pattern: "GeneratedSkillDraft" }
    - { from: src/skillscout/domain/skill_artifacts.py, to: src/skillscout/domain/canonical.py, via: "canonical full SHA-256 preimages", pattern: "sha256_digest" }
    - { from: src/skillscout/adapters/skill_packages.py, to: src/skillscout/adapters/localfs.py, via: "no-follow anchored atomic writes under retained ownership", pattern: "AnchoredDirectory" }
  prohibitions:
    - "The model cannot select filenames, directories, frontmatter, file modes, IDs, digests, or pre-approved tools."
    - "No scripts, binaries, symlinks, hard-link aliases, executable bits, nested resource directories, remote writes, subprocesses, or candidate-code execution may be created."
    - "The package digest must not appear inside provenance.json or any file included in its own preimage."
    - "Generator refusal, incomplete output, or schema invalidity must not trigger a second request for a more favorable result."
---

<objective>
Generate a stable, documentation-only, source-traceable Agent Skill package from one qualified WorkflowSpec.

Purpose: Separate model-owned semantics from trusted byte/path/identity authority while satisfying every generation and provenance requirement.
Output: Strict Generator adapter, deterministic package renderer and writer, complete provenance, distinct identities, and adversarial tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md
@src/skillscout/domain/extraction.py
@src/skillscout/domain/canonical.py
@src/skillscout/domain/models.py
@src/skillscout/adapters/localfs.py
@src/skillscout/adapters/openai_extract.py
</context>

<interfaces>
Generator input is one validated `WorkflowSpec`. Mirror `OpenAIExtractionClient`: environment/API-key construction, `max_retries=0`, `responses.parse`, developer/user role separation, `store=False`, no `tools`, bounded tokens, closed transient/permanent error mapping, and returned business outcomes for refusal/incomplete/schema invalidity. Use `AnchoredDirectory` for all filesystem access and `canonical_json_bytes`/`sha256_digest` for identities.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-03-01: Define semantic draft, provenance, lineage, artifact, and package identities</name>
  <files>src/skillscout/domain/skill_artifacts.py, tests/test_skill_generation.py</files>
  <read_first>src/skillscout/domain/skill_artifacts.py (new; confirm absent), tests/test_skill_generation.py (new; confirm absent), src/skillscout/domain/extraction.py, src/skillscout/domain/canonical.py, src/skillscout/domain/models.py, tests/test_phase2_contracts.py, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - Generator schema accepts semantic content and up to four named reference topics but contains no arbitrary path, mode, frontmatter, tool-authority, script, binary, or digest authority field.
    - Lineage stays stable for the same repo ID + normalized title + sorted evidence paths across commit/content changes; content artifact identity changes with fingerprint/draft/prompt/policy/model changes.
    - Package digest changes with any path/byte/mode/size change and is absent from provenance bytes.
  </behavior>
  <action>Create `skill_artifacts.py` with version constants `GENERATION_PROMPT_VERSION = "generate-prompt-v1"`, `ARTIFACT_SCHEMA_VERSION = "skill-artifact-v1"`, `PROVENANCE_SCHEMA_VERSION = "skill-provenance-v1"`, `LINEAGE_VERSION = "skill-lineage-v1"`, and `QUOTE_POLICY_VERSION = "quote-policy-v1"`. Define bounded strict `GeneratedSkillDraft` semantic fields: description, overview, when_to_use, inputs, ordered steps, outputs, failure_handling, approvals, limitations, registered short quotes, and at most four reference topics whose names match a trusted identifier pattern rather than paths. Define `SkillProvenance` with repository URL, numeric repo ID, exact commit SHA, SPDX license, workflow ID/fingerprint/version, every evidence path/blob/content hash, registered quote attribution, WorkflowSpec/extraction/qualification/generation/artifact/validation version facts, configured and actual generation model, and bounded request/usage facts. Define `SkillFile` and `SkillPackage` with canonical relative path, exact bytes/hash/size and fixed mode metadata. Implement `lineage_id` from version + repo ID + normalized title + sorted evidence paths, `artifact_id` from lineage + workflow fingerprint + generation versions/model + canonical draft hash, and `package_digest` from the ordered path→hash/mode/size manifest. Use full digests for authority; slug suffixes are presentation only. Fail closed on lineage collision/ambiguous mapping. Tests must prove stable/sensitive preimages and that provenance has no package-digest field.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_generation.py -k 'contract or lineage or artifact_id or package_digest or provenance'</automated>
  </verify>
  <acceptance_criteria>
    - Strict schemas reject extra fields and cannot represent model-selected filesystem/tool/executable authority.
    - Full lineage/artifact/package identities have distinct versioned preimages and sensitivity tests.
    - Provenance contains every GEN-04 field and exact evidence binding while avoiding a self-hash cycle.
    - Ambiguous lineage/collision cases fail closed rather than selecting an existing candidate silently.
  </acceptance_criteria>
  <done>The semantic and machine-readable contracts separate update identity, generated-content identity, and reviewed-byte identity without circular hashing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Render and durably materialize the closed documentation-only package tree</name>
  <files>src/skillscout/domain/skill_artifacts.py, src/skillscout/adapters/skill_packages.py, tests/test_skill_generation.py</files>
  <read_first>src/skillscout/domain/skill_artifacts.py, src/skillscout/adapters/skill_packages.py (new; confirm absent), tests/test_skill_generation.py, src/skillscout/adapters/localfs.py, src/skillscout/domain/canonical.py, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - Rendering the same admitted inputs twice yields byte-identical SKILL.md/provenance/resources, slug, modes, manifest, and package digest.
    - The tree is exactly `<slug>/SKILL.md`, `<slug>/references/provenance.json`, optional one-level `references/*.md`, and only justified one-level text `assets/*.md|txt|json`, all mode 0644.
    - Scripts, binaries, executable modes, hidden/absolute/traversal/nested/model-selected paths, orphan resources, arbitrary URLs/HTML/images, and unregistered long copying are rejected before durable write.
  </behavior>
  <action>Implement trusted `render_skill_package(workflow, draft, provenance_facts) -> SkillPackage`. Derive a stable slug from normalized workflow title plus a short repo-ID-derived suffix while retaining full `lineage_id` as collision authority. Trusted code emits fixed simple frontmatter containing only specification-supported keys, exact name-directory equality, fixed section order, deterministic relative links, and concise progressive disclosure (`SKILL.md` capped at 500 lines and 5000 estimated tokens). Do not emit `allowed-tools`. Default to paraphrase; permit registered quotes of at most 120 characters each and 240 total with source path + commit SHA attribution, and reject any unregistered normalized source match of at least 80 characters as the versioned conservative project policy. Render canonical `references/provenance.json`; optional references are one level, UTF-8 Markdown, and referenced exactly once. Text assets are admitted only when the semantic draft justifies a template and are `.md`, `.txt`, or `.json`. Implement `SkillPackageWriter` over `AnchoredDirectory` with closed child names, no-follow regular-file/identity checks, retained nonblocking flock, exact `0644`, atomic replace, fatal fsync before success, and deterministic stale-temp recovery. Preflight all bytes before creating directories; never execute or import package content. Add repeatability, hostile path/type/mode/link, quote-boundary, tree-shape, and failure-atomicity tests.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_skill_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Two renders/writes of identical inputs yield exact same bytes, manifest, modes, IDs, and digest.
    - Tests cover quote lengths 120/121, total 240/241, unregistered normalized matches 79/80, stable slug/update, and collision failure.
    - Every admitted file is UTF-8 regular mode 0644 under the exact closed tree; link, binary, executable, nested, hidden, traversal, scripts, and extra-file cases fail before success.
    - Package digest is computed after final provenance bytes and stored only outside the hashed package files.
  </acceptance_criteria>
  <done>A qualified semantic draft becomes a byte-stable, safely materialized, documentation-only Agent Skill package with complete attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-03: Add the isolated one-call Structured Outputs Generator</name>
  <files>src/skillscout/adapters/openai_generate.py, tests/test_openai_generate.py, tests/fixtures/openai/generator/cases.json</files>
  <read_first>src/skillscout/adapters/openai_generate.py (new; confirm absent), tests/test_openai_generate.py (new; confirm absent), tests/fixtures/openai/generator/cases.json (new; confirm absent), src/skillscout/adapters/openai_extract.py, src/skillscout/domain/skill_artifacts.py, tests/test_openai_extract.py, tests/recorded_transport.py, .planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md</read_first>
  <behavior>
    - One `generate()` invocation performs exactly one Responses request with `store=false`, no tools, bounded tokens, strict GeneratedSkillDraft schema, versioned developer instructions, and WorkflowSpec only in the user role.
    - Parsed, refused, incomplete, and schema-invalid responses return closed `GenerationResult` business outcomes; 429/5xx/timeout/connection are transient failures and other SDK errors permanent, with no SDK retry.
    - API credentials occur only in the Authorization header and never in payloads, results, fixtures, exceptions, or telemetry.
  </behavior>
  <action>Mirror the verified `OpenAIExtractionClient` structure in a separate `OpenAIGenerationClient`. Use configurable model default `gpt-5.6-terra`, bounded output tokens, `max_retries=0`, a single `responses.parse` call, `text_format=GeneratedSkillDraft`, `store=False`, and omit tools. Developer content is exactly the versioned generation policy and contains zero WorkflowSpec/source bytes; user content is one canonical inert-data envelope for the single qualified WorkflowSpec and trusted version facts. Return strict `GenerationResult` statuses `generated|refused|incomplete|schema_invalid` with bounded telemetry and no filesystem fields. Map refusal/incomplete/missing parsed/schema-invalid responses as decided business outcomes; only runner-owned transient infrastructure failures may retry. Add one bounded repository-owned `cases.json` map containing named parsed, refusal, incomplete, schema-invalid, 429, and 500 HTTP recordings; load a named case through a local test helper and reject every unrecorded request.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_generate.py &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain/skill_artifacts.py src/skillscout/adapters/skill_packages.py src/skillscout/adapters/openai_generate.py tests/test_skill_generation.py tests/test_openai_generate.py</automated>
  </verify>
  <acceptance_criteria>
    - Request-shape tests assert exactly one POST, developer/user isolation, strict schema, `store=false`, absent tools, and bounded output tokens.
    - Outcome fixtures and error matrices prove no decided business result is retried and every infrastructure attempt is one request.
    - API key canaries appear only in authorization headers; full upstream repository bytes are absent from the request and every durable/test surface.
    - Generator output cannot authorize paths, modes, frontmatter, tools, scripts, binaries, IDs, or digests.
  </acceptance_criteria>
  <done>The Generator performs bounded semantic rewriting only, with trusted rendering retaining all artifact and authority decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| WorkflowSpec → Generator request | Structured but model-derived evidence crosses into a fresh external LLM request as inert user data. |
| Generator output → trusted renderer | Model semantic fields remain untrusted and must not gain path, mode, identity, or tool authority. |
| Rendered package → local filesystem | Exact approved bytes are materialized under link/ownership/mode/durability controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-GEN | Tampering / Elevation of Privilege | model draft and renderer | high | mitigate | Closed semantic schema, trusted path/frontmatter/mode/identity renderer, fixed tree, no tools/scripts/binaries, and adversarial tests. |
| T-03-LINEAGE | Spoofing | slug/lineage mapping | high | mitigate | Full versioned lineage identity authorizes updates; short suffix is presentation only; collisions and ambiguity fail closed. |
| T-03-03-FS | Tampering | package materialization | high | mitigate | Anchored no-follow descriptor writes, identity checks, retained lock, exact modes, atomic replace and fatal fsync. |
| T-03-03-ID | Information Disclosure | credentials/source/model output | high | mitigate | Header-only key, WorkflowSpec-only payload, bounded provenance, registered excerpts, sanitized outcomes and canary sweeps. |
| T-03-03-DOS | Denial of Service | model/package sizes | medium | mitigate | Pydantic caps, output-token cap, file/count/line/token/package limits, and preflight before filesystem creation. |
</threat_model>

## Artifacts this phase produces

- New source files: `src/skillscout/domain/skill_artifacts.py`, `src/skillscout/adapters/skill_packages.py`, `src/skillscout/adapters/openai_generate.py`.
- New domain symbols: `GENERATION_PROMPT_VERSION`, `ARTIFACT_SCHEMA_VERSION`, `PROVENANCE_SCHEMA_VERSION`, `LINEAGE_VERSION`, `QUOTE_POLICY_VERSION`, `GeneratedSkillDraft`, `RegisteredQuote`, `SkillProvenance`, `SkillFile`, `SkillPackage`, `lineage_id`, `artifact_id`, `package_digest`, `render_skill_package`.
- New adapter symbols: `SkillPackageWriter`, `OpenAIGenerationClient`, `GenerationResult`, generator model/token constants and versioned instructions.
- New test/fixture paths: `tests/test_skill_generation.py`, `tests/test_openai_generate.py`, and `tests/fixtures/openai/generator/cases.json` containing the six named response recordings.
- Runtime artifacts produced by these symbols: `<slug>/SKILL.md`, `<slug>/references/provenance.json`, optional one-level text `references/` and `assets/`, plus an external exact package manifest/digest.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Standard, documentation-only, source-clear local Agent Skill | 03-03 | COVERED | Deterministic renderer and safe writer. |
| REQ | GEN-01 | Agent Skills directory, SKILL.md, necessary references/assets | 03-03 | COVERED | Closed standard tree and progressive disclosure. |
| REQ | GEN-02 | Documentation only; no scripts/binaries/executable bits/copied executable code | 03-03 | COVERED | Schema, renderer, writer and adversarial matrix. |
| REQ | GEN-03 | Generalized rewrite and bounded attributed excerpts | 03-03 | COVERED | One semantic call plus 120/240/80 versioned quote policy. |
| REQ | GEN-04 | Complete machine-readable provenance | 03-03 | COVERED | Canonical provenance contract with every required identity/version. |
| REQ | GEN-05 | Stable slug/fingerprint lineage and update-not-duplicate behavior | 03-03, 03-06 | COVERED | Separate lineage/artifact/package identities; pipeline reuse in 03-06. |
| RESEARCH | Structured draft + trusted renderer | Model owns meaning only | 03-03 | COVERED | Model schema cannot represent filesystem/authority decisions. |
| RESEARCH | No self-hash cycle | Package digest stays outside provenance | 03-03 | COVERED | Explicit identity preimages and tests. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | No locked/deferred decisions to map. |

<verification>
- Focused generation, writer, identity, provenance, and Generator adapter suites pass under the exact approved lock.
- Ruff passes all source/test modules owned by this plan.
- Adversarial fixtures prove tree/path/mode/binary/link/copy/secret/injection constraints and one-call credential confinement.
</verification>

<success_criteria>
- Qualified WorkflowSpec input yields a deterministic standard package with complete provenance and stable lineage.
- Model output never gains filesystem, identity, permission, tool, or publication authority.
- No package contains scripts, binary data, executable modes, excessive copying, or a self-hash cycle.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md` with exported contracts, package layout, policy thresholds, identity preimages, and Generator request evidence.</output>
