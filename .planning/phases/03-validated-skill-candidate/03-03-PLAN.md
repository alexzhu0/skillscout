---
phase: 03-validated-skill-candidate
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - VAL-01
must_haves:
  truths:
    - "A human reviews and approves the exact complete transitive graph, artifact hashes, and current uv.lock bytes before any dependency-backed command runs."
    - "Gate B3 approval is bound to one uv.lock SHA-256 and becomes invalid if those bytes change."
  artifacts:
    - path: ".planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md"
      provides: "Gate B3 decision, approved uv.lock SHA-256, and exact graph review record."
  key_links:
    - from: "uv.lock"
      to: "Gate B3 decision"
      via: "Human reviews exact bytes and every resolved artifact."
    - from: "Gate B3 approved digest"
      to: "Plan 03-04 preflight"
      via: "The preflight commits and enforces the approved lock identity."
  prohibitions:
    - "Do not run uv sync, uv run, Python imports, tests, linters, skills-ref, or validators before approval."
    - "Do not approve a range of acceptable lock states; approval binds one exact digest."
---

<objective>
Obtain the mandatory pre-execution Gate B3 decision for the exact locked validator graph.

Purpose: This gate separates metadata resolution from executing newly introduced supply-chain code and makes every later Python/tool command conditional on immutable approved lock bytes.
Output: An explicit approve/reject decision bound to the exact SHA-256 of `uv.lock`.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Decide Gate B3 for exact lock bytes and transitive artifacts</name>
  <files>None</files>
  <read_first>
    - `.planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md` — inspect the resolver command, graph output, and declared no-execution evidence.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — read Gate B3 and every `[SUS]` entry.
    - `pyproject.toml` — inspect the exact direct declaration.
    - `uv.lock` — inspect every skills-ref transitive package, source, version, and artifact hash.
  </read_first>
  <action>
Compute the SHA-256 of the current `uv.lock` bytes without importing project dependencies. Review every resolved artifact in the `skills-ref==0.1.1` dependency tree against its official registry page and source metadata, including `click` and `strictyaml`; verify exact versions, source URLs, hashes, ownership/provenance signals, and absence of unexpected VCS/path/editable sources. Reconfirm the audited skills-ref wheel hash. Approval must state the exact lock SHA-256 and authorizes later commands only when a dependency-free preflight proves byte-for-byte equality. Rejection stops Phase 3 execution because there is no approved official-validator fallback.
  </action>
  <what-built>`pyproject.toml` and `uv.lock` now describe the exact graph, but no new package code has run.</what-built>
  <how-to-verify>
    1. Run `shasum -a 256 uv.lock` and record the 64-character digest.
    2. Inspect `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" tree --locked --package skills-ref` from Plan 03-02 and each corresponding registry/source page.
    3. Confirm the skills-ref wheel hash equals `d35db5bb8de71ae301daf5ca9cb71f8a555e8c6f83a6d40e46a5bc09f8f461b5`.
    4. Confirm no unexpected source type or unhashed artifact exists.
    5. Respond `approved B3 <uv.lock-sha256>` or `rejected B3: <reason>`.
  </how-to-verify>
  <verify>
    <automated>git diff --check -- pyproject.toml uv.lock</automated>
  </verify>
  <acceptance_criteria>
    - The decision identifies one exact 64-character `uv.lock` SHA-256.
    - Every transitive artifact is reviewed at its authoritative registry/source.
    - Approval requires a dependency-free equality preflight before every later dependency-backed command.
    - Rejection executes no installed dependency code.
  </acceptance_criteria>
  <done>Gate B3 has an explicit human approve/reject signal bound to exact lock bytes and graph evidence in `03-03-SUMMARY.md`.</done>
  <resume-signal>Type `approved B3 <uv.lock-sha256>` or `rejected B3: <reason>`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locked artifacts → executable environment | Resolved packages become executable only after a human approves exact identities and bytes. |
| Human approval → future commands | A later lock mutation must not inherit stale approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | Locked package graph | high | mitigate | Blocking Gate B3 reviews every exact artifact and binds approval to a SHA-256 of `uv.lock`. |
| T-03-05 | Tampering | Post-approval lock mutation | high | mitigate | Plan 03-04 creates a dependency-free equality preflight required before every later command. |
| T-03-06 | Spoofing | Transitive package provenance | high | mitigate | Human checks official registry/source metadata and rejects unexpected source types. |
</threat_model>

## Artifacts this phase produces

- `03-03-SUMMARY.md` with the exact approved lock digest or rejection reason.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Safe local Skill validation | COVERED — prevents unapproved validator code execution. |
| REQ | VAL-01 | COVERED — authorizes the exact official validator graph or closes the phase. |
| RESEARCH | Gate B3; `[SUS]` transitive review; exact-lock binding | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Only dependency-free hashing and metadata/diff inspection are allowed in this plan. No `uv run`, imports, tests, or package entry points.
</verification>

<success_criteria>
- Gate B3 is approved for one exact lock digest before Plan 03-04, or rejected and execution stops.
- Approval cannot survive any subsequent `uv.lock` byte change.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md` when done.
</output>
