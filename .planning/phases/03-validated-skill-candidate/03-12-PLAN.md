---
phase: 03-validated-skill-candidate
plan: 12
type: execute
wave: 11
depends_on:
  - 03-06
  - 03-07
  - 03-08
  - 03-09
  - 03-10
  - 03-11
files_modified:
  - src/skillscout/application/phase3.py
  - src/skillscout/application/ports.py
  - tests/test_phase3_pipeline.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - VAL-01
  - VAL-02
  - VAL-03
  - REV-01
  - REV-02
  - REV-03
must_haves:
  truths:
    - "The application resolves and reverifies the Phase 2 source before opening/querying Phase 3 state; source failure returns candidate_source_unavailable with zero Phase 3 effects."
    - "Complete CandidateExecutionAuthorityV1 is assembled before the first Phase 3 lookup."
    - "Composition imports RENDERER_VERSION and ELIGIBILITY_POLICY_VERSION directly from their owner modules, exposes no runtime or CLI override, and cross-checks the same values in downstream reports, artifacts, and terminal evidence."
    - "A new run proceeds in the exact semantic order QUALIFIER → GENERATOR → VALIDATOR → REVIEWER with deterministic short-circuiting."
    - "Every successful stage/result is checkpointed in phase3_checkpoints with exact output-hash continuity before the next stage or resume."
    - "Only the exact 12 terminal outcomes exist, each with branch-appropriate immutable evidence; business rejections/refusals are successful closed outcomes and only infrastructure failures retry."
    - "Exact completed reuse returns stored terminal/package/report/attestation bytes with zero calls, rows, events, or output writes."
  artifacts:
    - path: "src/skillscout/application/phase3.py"
      provides: "Independent Phase 3 composition root, runtime profile, ports, and orchestrator."
    - path: "src/skillscout/application/ports.py"
      provides: "Narrow Generator, validator, Reviewer, state factory, and artifact output protocols."
    - path: "tests/test_phase3_pipeline.py"
      provides: "Full branch cascade, owner-constant authority, checkpoint/output-hash resume, raw transport count, failure, budget, and read-only exact-reuse integration tests."
  key_links:
    - from: "candidate source resolver"
      to: "Phase3 state factory"
      via: "Lazy state factory is not invoked until source and complete execution authority succeed."
    - from: "QualificationReportV1"
      to: "Generator → validator → Reviewer"
      via: "Strict stage gates and immutable outputs."
    - from: "skill_artifacts.RENDERER_VERSION + review.ELIGIBILITY_POLICY_VERSION"
      to: "CandidateExecutionAuthorityV1 and downstream evidence"
      via: "Direct owner-module imports with no configuration/CLI override and exact cross-checks."
    - from: "CandidateTerminalSummaryV1"
      to: "local output/reuse"
      via: "Branch-specific exact external evidence."
  prohibitions:
    - "Do not modify application/pipeline.py or compose PhaseTwoProcessor."
    - "Do not call Reviewer after validation errors, Generator after qualification/lineage rejection, or any semantic service on exact reuse."
    - "Do not retry semantic rejection, refusal, incomplete, or schema-failure outcomes."
---

<objective>
Compose the independent Phase 3 application state machine from verified source to exact local terminal candidate.

Purpose: All prior strict contracts and adapters need one fail-closed root that enforces ordering, budgets, resume, exact branch vocabulary, and zero-side-effect reuse.
Output: Phase 3 ports/runtime/composition root/orchestrator and complete integration matrix.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/application/candidate_source.py
@src/skillscout/application/ports.py
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/qualification.py
@src/skillscout/domain/skill_artifacts.py
@src/skillscout/domain/validation.py
@src/skillscout/domain/review.py
@src/skillscout/adapters/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble a lazy independent Phase 3 runtime boundary</name>
  <files>src/skillscout/application/phase3.py, src/skillscout/application/ports.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/application/candidate_source.py` — source resolution must run before any Phase 3 resource.
    - `src/skillscout/application/ports.py` — extend with narrow protocols and dependency factories.
    - `src/skillscout/application/pipeline.py` — use only as a protected non-modified analogy; do not extend PIPELINE_PROFILES.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — implement runtime budgets and prelookup authority inventory.
  </read_first>
  <behavior>
    - Runtime construction does not eagerly open Phase 3 state or call semantic/validator services.
    - Source resolution completes first; then lineage binding input and every configured version/model/lock/distribution/profile/retry value form complete execution authority; only then state lookup is allowed.
    - RENDERER_VERSION and ELIGIBILITY_POLICY_VERSION are imported directly from their owner modules, cannot be supplied by a runtime profile/CLI/caller, and are independently sensitivity-tested in execution authority.
    - Downstream generation, validation, and terminal evidence must carry the exact owner constants already bound into execution authority; either mismatch fails closed.
    - Ports expose no GitHub write/publish/merge or candidate-code execution capabilities.
    - Configured candidate/model/call/token/retry limits are hard bounded and versioned.
  </behavior>
  <action>
Create an independent `src/skillscout/application/phase3.py` composition root and runtime profile; export `PHASE_THREE_STAGE_SEQUENCE` exactly as qualifier, generator, validator, reviewer. Import `RENDERER_VERSION` directly from `src/skillscout/domain/skill_artifacts.py` and `ELIGIBILITY_POLICY_VERSION` directly from `src/skillscout/domain/review.py`; do not duplicate their string values, accept them as constructor arguments, add them to the runtime profile, read them from environment/configuration, or expose any CLI override. Add narrow Protocols/factories in `src/skillscout/application/ports.py` for Generator, official/local validator facade, Reviewer, lazy Phase 3 state, and local artifact projection. The entrypoint first invokes the Plan 03-06 source resolver; on `CandidateSourceUnavailable`, return the fixed pre-run outcome without instantiating state or downstream dependencies. Next copy the descriptor's optional prior-lineage-binding digest as opaque authority input and construct complete `CandidateExecutionAuthorityV1` from every configured authority value, the two directly imported owner constants, approved lock digest, and official distribution identity; only then call the lazy state factory and completed/resumable lookup. Cross-check every later generation authority/provenance/ValidationReport `RENDERER_VERSION` and CandidateTerminalSummary `ELIGIBILITY_POLICY_VERSION` against both the owner constant and the value bound in execution authority; reject stale or caller-forged downstream evidence.

For a new/resumable run, absence of the prior-binding digest means new-lineage evaluation with no search; presence authorizes lookup of exactly that canonical binding/approval/prior-evidence record from `phase3_artifacts`, followed by Plan 03-11 verification and Plan 03-05 resolution. Missing or invalid exact-digest evidence closes `lineage_rejected`; never search by title, slug, path, content, repository, or similarity. Make candidate count, one Generator/Reviewer call per ledger attempt, token ceilings, request-size ceilings, and bounded runner-owned infrastructure retries explicit versioned configuration. Expose no GitHub write, branch, PR, merge, shell, repository script, dependency install, or candidate execution port. Add independent sensitivity cases that change only renderer authority and only eligibility authority and prove each changes the complete lookup digest; also prove runtime/CLI/profile inputs cannot override either and mismatched downstream evidence fails before persistence.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k composition_boundary</automated>
  </verify>
  <acceptance_criteria>
    - Source failure leaves lazy state/service/output factories untouched.
    - Full execution authority is captured and asserted before the first lookup.
    - Owner-module renderer/eligibility constants are direct imports with no override surface; independent mutations each invalidate authority, and downstream evidence mismatches fail closed.
    - No global Phase 1/2 profile or processor is changed/composed.
    - Runtime hard limits and their versions are explicit and tested.
  </acceptance_criteria>
  <done>Phase 3 has an independent lazy root whose first state access is guarded by verified source and complete authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate the exact stage and terminal cascade</name>
  <files>src/skillscout/application/phase3.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/domain/qualification.py` — apply exact deterministic pass/hard-failure semantics.
    - `src/skillscout/domain/skill_artifacts.py` — freeze artifact before validation/review.
    - `src/skillscout/domain/validation.py` — stop before Reviewer on any error.
    - `src/skillscout/domain/review.py` — use exact terminal enum, attestation, and eligibility.
    - `src/skillscout/adapters/state.py` — append/resume/terminalize through the isolated ledger only.
  </read_first>
  <behavior>
    - qualification failure closes qualification_rejected; invalid supplied binding closes lineage_rejected; neither calls Generator.
    - Generator refusal/incomplete/schema failure close their exact branches with no package/validator/Reviewer.
    - Package freezes before validator; any validation error closes validation_rejected with package+report and no Reviewer.
    - Reviewer refusal/incomplete/schema failure, NO, YES below 0.80, and eligible YES at least 0.80 map to the exact five review terminal branches.
    - qualification_rejected records `LineageResolutionV1(status="not_evaluated_qualification_rejected")`; validation_rejected records `ReviewDispositionV1(status="review_skipped_validation_errors")`.
    - Every branch persists terminal-owned eligibility plus ELIGIBILITY_POLICY_VERSION and the exact branch-specific Generator outcome/actual-model, artifact identity, package, report, and attestation evidence matrix.
    - Every successful result is followed by one verified CandidateStageCheckpointV1 whose output hash is the next stage/resume input anchor.
    - Each Generator or Reviewer adapter invocation performs one raw Responses request; only PhaseThreeRunner creates another ledger attempt after a mapped transient failure.
  </behavior>
  <action>
Implement one `PhaseThreeRunner` state machine over the dedicated ledger. Start/verify QUALIFIER, persist exact QualificationReport, and write/verify the qualifier `CandidateStageCheckpointV1` with the exact report output hash before any next-stage action. On qualification failure construct exactly `LineageResolutionV1(status="not_evaluated_qualification_rejected")` before closing `qualification_rejected`; do not evaluate bindings or call Generator. On qualification pass, resolve lineage without heuristics; a supplied invalid/ambiguous binding closes `lineage_rejected`.

For each Generator ledger attempt, invoke `OpenAIGenerationClient.generate` once; because the adapter uses SDK `max_retries=0`, the raw transport count for that attempt is one. Always canonicalize branch-appropriate `GeneratorOutcomeEvidenceV1`, including configured/actual model and bounded request/usage evidence when returned. Refusal, incomplete, and schema failure close their exact terminal branches without another request. Only a mapped transient infrastructure error authorizes `PhaseThreeRunner` to persist the failed attempt and initiate a new bounded ledger attempt; the adapter never retries internally. On Generator success, persist/verify the Generator result and checkpoint output hash, then construct `GeneratedArtifactIdentityV1` from the canonical draft plus Plan 03-08's explicit generation-only authority projection, never the full CandidateExecutionAuthority or request/usage evidence. Render/freeze provenance, which does include bounded request/usage, and the distinct package digest before invoking validation.

Persist ValidationReport with its complete direct authority header and its exact validator checkpoint output hash. On any error construct exactly `ReviewDispositionV1(status="review_skipped_validation_errors")`, no ReviewAttestation, and close `validation_rejected`. Otherwise, for each Reviewer ledger attempt invoke `OpenAIReviewClient.review` once; raw transport count is one for the attempt. Refusal, incomplete, schema failure, NO, YES below 0.80, and eligible YES map to `reviewer_refusal`, `reviewer_incomplete`, `reviewer_schema_failure`, `review_rejected`, `review_low_confidence`, and `eligible_local_candidate` without semantic retries. Only mapped transient infrastructure failure authorizes another bounded runner-owned attempt. Persist/verify the Reviewer checkpoint output hash before terminalization. ReviewAttestation binds raw review evidence only; CandidateTerminalSummary derives and owns `eligible` plus the directly imported `ELIGIBILITY_POLICY_VERSION`. Persist the exact Plan 03-10 branch matrix of Generator evidence, semantic artifact identity, package identity/digest, report, review disposition/attestation, and summary bytes atomically. Cross-check every renderer/eligibility version against the owner constant and execution authority. Treat semantic outcomes as normal closed results; do not auto-rewrite or re-render.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k terminal_cascade</automated>
  </verify>
  <acceptance_criteria>
    - All and only the 12 exact terminal branches are reachable by explicit tests.
    - Stage call counts and the full terminal-owned eligibility, lineage/review companion status, Generator actual-model/outcome, artifact/package/report/attestation evidence matrix match each branch.
    - Stage order is exact and package bytes freeze before validation/review.
    - Every successful stage has one checkpoint with recomputed result output hash and unbroken previous-checkpoint/next-stage continuity.
    - Generator/Reviewer raw transport counts equal runner attempt counts for 429/500 retries and equal one for every decided success/failure attempt.
    - Only infrastructure failures retry; semantic outcomes close without retry.
  </acceptance_criteria>
  <done>Every workflow follows the exact stage cascade and closes with the correct immutable evidence and terminal branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove resume, budgets, and exact completed reuse end to end</name>
  <files>src/skillscout/application/phase3.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/application/phase3.py` — exercise Task 1/2 public runner only.
    - `src/skillscout/adapters/state.py` — inspect exact resume/completed projection guarantees.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — cover every zero-side-effect and failure-injection invariant.
  </read_first>
  <behavior>
    - Failure after each stage resumes only from a verified checkpoint at the next legal stage with prior canonical result/output/checkpoint bytes unchanged and no repeated semantic calls.
    - Exact completed reuse returns stored immutable terminal/package/report/attestation projections read-only and invokes no service, checkpoint/event/state writer, materializer, or output projection.
    - Changing any configured authority field yields a new identity and no exact reuse.
    - Candidate, model-call, token, request-size, and retry ceilings fail closed before exceeding limits.
    - Up to three full-fingerprint-sorted sibling descriptors keep independent execution authority, call budgets, ledger rows, artifact bytes, and outcomes when one succeeds, one rejects, and one fails pre-run.
  </behavior>
  <action>
Build a table-driven integration matrix with deterministic fake source/state/Generator/validator/Reviewer/output ports. Inject infrastructure failure after each successful stage, restart, and prove only the next legal stage executes after recomputing the stored result output hash and `CandidateStageCheckpointV1`; all prior canonical result/checkpoint bytes and hashes remain exact. Delete, insert, reorder, splice, or mutate a checkpoint/output hash and prove resume fails closed before semantic calls.

Run each terminal branch twice: the second exact-authority invocation must return the already stored immutable projections through the read-only completed-lookup result, with every relevant table count including `phase3_checkpoints` unchanged and zero Generator/validator/Reviewer/checkpoint/event/state-writer/materializer/output-writer calls. It must not materialize, rewrite, touch, or fsync local output files. Parameterize every execution-authority field mutation to prove lookup misses, including independent renderer-only and eligibility-only authority mutations. Feed the deterministic descriptor derivation four workflows and prove only the first three full-fingerprint-sorted descriptors execute; for the selected siblings, combine one eligible success, one semantic rejection, and one pre-run authority failure, then assert independent execution-authority keys, per-candidate call budgets/counts, ledger/checkpoint rows, artifact/report/attestation/terminal bytes, and outcomes with no cross-candidate suppression, reuse, or mutation. Exercise candidate count, request/input/output token, call-count, and retry ceilings at boundary and over-boundary. Use recorded/fake transport counters to prove each 429/500 creates at most one raw request in that adapter invocation and a new request only after the runner records a new bounded attempt. Corrupt resumable/completed chain or any external artifact and prove fail-closed without semantic calls.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Resume never repeats a successful semantic stage and preserves exact prior result/output/checkpoint bytes.
    - Exact completed reuse has zero calls/writes/checkpoints/events/materialization and unchanged table counts/bytes for all branches.
    - Every authority mutation prevents reuse.
    - Full-fingerprint ordering/cap and three-sibling success/rejection/pre-run-failure isolation are proven end to end.
    - Hard budgets and corrupt-state failures close before exceeding/calling downstream.
  </acceptance_criteria>
  <done>The full Phase 3 application is restart-safe, cost-bounded, tamper-evident, and exactly idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified Phase 2 source → Phase 3 runtime | No Phase 3 resource may be touched before source and complete authority succeed. |
| Stage output → next stage | Each structured result must pass deterministic/chain validation and short-circuit rules. |
| Runtime limits → external services | Calls and tokens must never exceed hard configured ceilings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-35 | Tampering | Prelookup identity | high | mitigate | Build complete authority before lazy state lookup and test every field mutation. |
| T-03-36 | Elevation of Privilege | Stage bypass | high | mitigate | Closed state machine enforces qualifier→generator→validator→reviewer and branch short circuits. |
| T-03-37 | Denial of Service | Model/token/retry cost | high | mitigate | Versioned hard ceilings enforced before calls/retries. |
| T-03-38 | Tampering | Resume/reuse | high | mitigate | Dedicated verified ledger and stage checkpoints bind each successful output to the next stage; completed reuse returns immutable stored projections with zero materializer, writer, event, row, or checkpoint mutation. |
</threat_model>

## Artifacts this phase produces

- Independent lazy Phase 3 runtime/composition root.
- Exact stage/terminal orchestrator.
- Full branch, checkpoint/output-hash resume, raw transport count, owner-authority, budget, and read-only reuse integration matrix.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Qualify, generate, validate, independently review, and explain a local Skill | COVERED end to end. |
| REQ | QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, REV-03 | COVERED by exact stage and terminal orchestration. |
| RESEARCH | Pre-run source barrier; prelookup authority; relative ledger; exact branches; budgets; resume/reuse | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Then run `sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_candidate_source.py tests/test_qualification.py tests/test_skill_generation.py tests/test_skill_validation.py tests/test_openai_review.py tests/test_phase3_pipeline.py`.
</verification>

<success_criteria>
- Verified source and complete authority precede all Phase 3 state.
- Exact stage/terminal semantics cover every requirement.
- Resume, budgets, corruption handling, and exact completed reuse are proven end to end.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-12-SUMMARY.md` when done.
</output>
