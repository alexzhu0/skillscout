---
phase: 03-validated-skill-candidate
plan: "07"
type: execute
wave: 6
depends_on: [03-06]
files_modified:
  - src/skillscout/cli.py
  - tests/test_cli_validate_skill.py
  - tests/test_cli_security.py
  - tools/verify_phase3_acceptance.py
  - tests/test_phase3_acceptance_tool.py
  - .planning/phases/03-validated-skill-candidate/03-VALIDATION.md
autonomous: true
requirements: [QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "A user can invoke build-candidate with a bounded repository subject and full workflow fingerprint and receive canonical JSON explaining qualification, validation, review, identities, reuse, and eligibility."
    - "Happy path materializes the exact local documentation-only Skill and reports zero remote writes; every qualification/generation/validation/review rejection exits as an auditable non-publication decision."
    - "Interrupted execution resumes verified stages, and every same-identity completed terminal branch performs zero GitHub/OpenAI/official-validator calls and zero ledger writes while reproducing its exact summary and its package only when one exists."
    - "Hostile arguments, subjects, provider data, package findings, secrets, source text, absolute paths, and raw exceptions never reach stdout, stderr, SQLite, manifests, summaries, or generated Skill content outside allowed bounded provenance/excerpts."
    - "The full locked test/Ruff/lock/capability/package scan gate passes and the validation strategy maps every final task ID."
  artifacts:
    - { path: src/skillscout/cli.py, provides: "safe build-candidate CLI entrypoint" }
    - { path: tests/test_cli_validate_skill.py, provides: "recorded end-to-end happy/rejection/resume/reuse/security evidence" }
    - { path: tests/test_cli_security.py, provides: "additive subparser and byte-exact non-echo assertions" }
    - { path: tools/verify_phase3_acceptance.py, provides: "deterministic capability/package/tree/mode/secret/provenance/self-hash phase scan" }
    - { path: tests/test_phase3_acceptance_tool.py, provides: "acceptance scanner positive and adversarial fixture proof" }
    - { path: .planning/phases/03-validated-skill-candidate/03-VALIDATION.md, provides: "finalized task-to-requirement/threat/test map" }
  key_links:
    - { from: src/skillscout/cli.py, to: src/skillscout/application/pipeline.py, via: "build_phase_three_runtime only", pattern: "build_phase_three_runtime" }
    - { from: src/skillscout/cli.py, to: src/skillscout/domain/subjects.py, via: "bounded subject/binding load plus complete pre-lookup execution authority", pattern: "CandidateSubjectV1" }
    - { from: tests/test_cli_validate_skill.py, to: tests/recorded_transport.py, via: "reject-unrecorded GitHub/OpenAI transports and exact call counts", pattern: "RecordedTransport" }
  prohibitions:
    - "CLI adds no live-network test, remote-write adapter, publication command, branch/PR/merge action, runtime package install, subprocess, or candidate execution."
    - "Rejected argv/subject/provider/validator bytes, credentials, full repository text, raw secret matches, absolute operator paths, and exception representations must never be echoed or persisted."
    - "Validation error, Reviewer NO, confidence below 0.80, refusal, incomplete, schema invalidity, or qualification failure must never output a publication plan or eligible=true."
    - "No test may weaken Phase 1/2 assertions, depend on network, watch mode, a PATH uv, floating lock, or downloadable Python."
---

<objective>
Expose and prove the complete local validated-candidate workflow through a safe CLI and close Phase 3 validation evidence.

Purpose: Deliver the user-visible outcome, rejection explanations, idempotent reuse, and full regression/security gate required by the roadmap.
Output: `build-candidate` command, recorded E2E suite, non-echo/canary evidence, and finalized validation map.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-VERIFICATION.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-06-SUMMARY.md
@tools/verify_phase3_gate_b3.sh
@src/skillscout/cli.py
@tests/test_cli_extract_repo.py
@tests/test_cli_security.py
</context>

<interfaces>
Mirror the established `extract-repo` safe parser/lifecycle: validate user input before opening SQLite, construct through the closed production root, emit canonical compact JSON, catch only `SafeFailure` publicly, collapse unexpected failures to a closed code, and always close state. `CandidateSubjectV1` requires the loaded RepositorySubject plus exact full workflow fingerprint and optional descriptor-supplied `PriorLineageBindingV1`; runtime derives the complete configured execution authority before any resume/completed lookup.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-07-01: Add the safe build-candidate command and non-echo boundary</name>
  <files>src/skillscout/cli.py, tests/test_cli_validate_skill.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/cli.py, tests/test_cli_validate_skill.py (new; confirm absent), tests/test_cli_security.py, src/skillscout/domain/subjects.py, src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, tests/test_cli_extract_repo.py, tests/test_phase3_pipeline.py, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md</read_first>
  <behavior>
    - `build-candidate` accepts `--subject`, `--workflow-fingerprint`, `--state`, `--output`, and only Phase 3 stages for `--fail-after`; malformed input returns one closed non-echoing diagnostic before state creation.
    - The command loads/validates descriptor bytes first, opens state only to resolve an optional exact prior-lineage binding, then constructs CandidateSubjectV1 and the complete configured execution authority before any reuse lookup.
    - Successful output is one canonical compact ValidatedCandidateSummary JSON object with relative artifact locator and no operator path, credential, raw provider/source, or exception text.
  </behavior>
  <action>Add sibling `build-candidate` parser branch. Require an exact `sha256:<64 lowercase hex>` full fingerprint, existing safe subject loader, `--state`, `--output`, and `--fail-after` choices only from `PHASE_THREE_STAGE_SEQUENCE`; reject missing/extra/malformed args through `SafeArgumentParser` without echo. The bounded subject descriptor may include one strict `PriorLineageBindingV1`; validate it before state open, then resolve it against durable state after the state handle opens and before any run lookup. Construct `CandidateSubjectV1`, `LineageResolutionV1`, and the complete `PhaseThreeExecutionAuthorityV1` before calling the resumable/completed lookup; a human-review-required lineage resolution emits its closed local terminal result without creating a new lineage/package. Resolve configured Generator/Reviewer models from `SKILLSCOUT_GENERATE_MODEL` and `SKILLSCOUT_REVIEW_MODEL`; credentials remain environment-only. Construct only via `build_phase_three_runtime`; no caller authority/version override is accepted. Emit `ValidatedCandidateSummary` with canonical JSON, catch only closed failures publicly, and always close state/runtime. Extend the exact subparser-set assertion additively and add parser/subject/binding/prelookup/path/key non-echo tests without changing earlier command semantics.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_validate_skill.py -k 'parser or subject or lineage or execution_authority or non_echo or summary' tests/test_cli_security.py</automated>
  </verify>
  <acceptance_criteria>
    - Help/subparser tests show the additive command and exact arguments/stage choices while every existing command test remains unchanged and green.
    - Hostile argv/subject/path/fingerprint/key canaries exit with the closed diagnostic, create no state/package, and appear in neither stdout nor stderr.
    - State opens only after descriptor validation; lineage resolution and CandidateSubjectV1 execution authority precede reuse lookup, and runtime construction has no authority-widening parameter.
    - Success output parses as strict ValidatedCandidateSummary and contains only relative package location plus bounded decisions/identities.
  </acceptance_criteria>
  <done>The complete Phase 3 flow is reachable through one safe local command without widening CLI disclosure or runtime authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-07-02: Prove happy path, every rejection, resume, and exact zero-call reuse end to end</name>
  <files>tests/test_cli_validate_skill.py</files>
  <read_first>tests/test_cli_validate_skill.py, tests/test_cli_extract_repo.py, tests/test_phase3_pipeline.py, tests/recorded_transport.py, tests/fixtures/github/repo_mit.json, tests/fixtures/github/commits_pin.json, tests/fixtures/github/tree_full.json, tests/fixtures/github/license_mit.json, tests/fixtures/github/blob_readme.json, tests/fixtures/openai/parsed_2_workflows.json, tests/fixtures/openai/generator/cases.json, tests/fixtures/openai/reviewer/cases.json, tests/fixtures/injection/direct_override.md, src/skillscout/cli.py, src/skillscout/domain/review.py, .planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md</read_first>
  <behavior>
    - Recorded happy path produces an official/custom error-free report, YES confidence 0.800+, eligible local decision, exact documentation-only package, eight verified attempts, and zero remote writes.
    - Qualification low/hard fail, Generator refusal/incomplete/invalid, every validation error family, Reviewer NO/0.799/refusal/incomplete/invalid all produce exact auditable non-eligible results and no PublicationPlan.
    - Resume preserves successful prefix call counts; exact reruns of every terminal branch have zero GitHub/Extractor/Generator/official-validator/Reviewer calls, zero new ledger rows/events, byte-identical summaries, and byte-identical packages only where the branch has one.
  </behavior>
  <action>Build recorded-transport subprocess/in-process E2E cases patterned after `test_cli_extract_repo.py`. Use only repository-owned fixtures and block every unrecorded socket. Happy path verifies the full configured and finalized execution identities, exact package tree/modes/digest/provenance and zero remote writes; its deterministic acceptance variant materializes the final package plus external manifest under ignored `.tmp/phase3-acceptance/` for Task 03-07-03's independent scanner. Add table-driven first-run plus same-identity rerun cases for qualification hard fail, qualification low score, lineage human-review outcome, Generator refusal, incomplete, and schema invalid, validation failure, Reviewer refusal, incomplete, and schema invalid, Reviewer NO, Reviewer low confidence, and success. Assert exact first-run call counts, one business attempt, then zero GitHub/OpenAI/official-validator calls and zero new run/event/attempt/result rows on reuse; summary bytes are exact for every branch, package bytes are exact only after generation, and pre-generation branches create no package. Interrupt after each new stage and resume from the verified prefix. Table-mutate every `PhaseThreeExecutionAuthorityV1` dimension independently — subject/fingerprint, prior-lineage binding/resolution digest, run/retry/profile/producer/summary schema, qualification policy/report schema, generator prompt/output schema/configured model, renderer/artifact/provenance/quote versions, validation policy/report schema, official distribution/version/wheel/approved-lock digest, Reviewer prompt/output/eligibility policy/configured model — and assert lookup misses before reuse; inject actual generator/Reviewer model and terminal-branch mismatches into completed records and assert fail closed rather than reuse. Add approved lineage mappings whose new workflow changes title and evidence paths and prove retained lineage/slug, plus missing/stale/tampered/repository-mismatch/collision/multiple/ambiguous mappings that stop for human review without a new candidate. Sweep all durable/output surfaces for canaries and forbidden raw text.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_validate_skill.py</automated>
  </verify>
  <acceptance_criteria>
    - Happy path and every named terminal rejection parse into strict summaries with exact eligibility and first-run/reuse call-count expectations.
    - Every terminal branch proves full-chain reuse, zero same-identity calls/ledger additions, exact summary bytes, and package materialization only when one exists.
    - Each configured authority field has an independent invalidation case; actual-model/terminal mismatches fail closed.
    - Approved title/evidence-path remaps retain lineage/slug; invalid/multiple/ambiguous mappings stop for human review without duplicate authority.
    - Durable/output canary sweeps and the outbound socket sentinel are empty except for specifically allowed bounded provenance/excerpt/header locations.
  </acceptance_criteria>
  <done>The user-visible command proves safe success, explainable rejection, recovery, and exact idempotency over the full Phase 3 boundary.</done>
</task>

<task type="auto">
  <name>Task 03-07-03: Run the complete phase gate and close the Nyquist execution map</name>
  <files>tools/verify_phase3_acceptance.py, tests/test_phase3_acceptance_tool.py, .planning/phases/03-validated-skill-candidate/03-VALIDATION.md</files>
  <read_first>.planning/phases/03-validated-skill-candidate/03-VALIDATION.md, .planning/phases/03-validated-skill-candidate/03-01-PLAN.md, .planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-02-PLAN.md, .planning/phases/03-validated-skill-candidate/03-02-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-03-PLAN.md, .planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-04-PLAN.md, .planning/phases/03-validated-skill-candidate/03-04-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-05-PLAN.md, .planning/phases/03-validated-skill-candidate/03-05-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-06-PLAN.md, .planning/phases/03-validated-skill-candidate/03-06-SUMMARY.md, .planning/phases/03-validated-skill-candidate/03-07-PLAN.md, pyproject.toml, uv.lock, config/supply-chain/phase3-gate-b3.lock.sha256, tools/verify_phase3_gate_b3.sh, tools/verify_phase3_acceptance.py (new; confirm absent), src/skillscout/domain/qualification.py, src/skillscout/domain/skill_artifacts.py, src/skillscout/domain/validation.py, src/skillscout/domain/review.py, src/skillscout/application/phase3.py, src/skillscout/application/pipeline.py, src/skillscout/cli.py, tests/test_qualification.py, tests/test_skill_generation.py, tests/test_lineage.py, tests/test_skill_validation.py, tests/test_openai_generate.py, tests/test_openai_review.py, tests/test_phase3_pipeline.py, tests/test_cli_validate_skill.py, tests/test_phase3_acceptance_tool.py (new; confirm absent)</read_first>
  <action>Create a standard-library-only `tools/verify_phase3_acceptance.py` with fixed repository-root discovery and no network/subprocess/package imports. Its closed subchecks inspect Python AST/imports for forbidden capability call sites while allowing `openai` only in the two named adapters and `skills_ref` only in `adapters/skills_ref.py`; walk explicitly supplied generated fixture/runtime package roots without following links; require the exact allowed one-level tree, regular single-link UTF-8 files at mode 0644, no binary signatures, scripts directory, hidden/nested/extra path, absolute-path/operator-root bytes, or secret canaries; parse canonical provenance and independently prove package digest is absent from hashed package bytes while external manifest hashes/modes/sizes and artifact/provenance bindings recompute exactly. Add tool tests with one valid package and one focused hostile case per capability/tree/mode/link/binary/secret/provenance/self-hash control. The automated gate must run, in order, Gate-B3 preflight plus full pytest, Gate-B3 preflight plus Ruff, Gate-B3 preflight plus lock check, and Gate-B3 preflight plus the acceptance tool over all committed/generated package roots; a preflight mismatch must stop before each dependency command. Update `03-VALIDATION.md` with these complete literal commands and exact task IDs, mark green only from fresh evidence, and keep the validation document outside its own authority.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked python tools/verify_phase3_acceptance.py --package-root tests/fixtures/skills/valid-skill --discover-runtime-packages .tmp/phase3-acceptance</automated>
  </verify>
  <acceptance_criteria>
    - Exact lock hash, full pytest, Ruff and lock check pass from fresh output under the repository-local no-download toolchain.
    - Capability/package scans prove the closed import/effect/file-mode/tree/secret/self-hash boundaries without counting comments as executable evidence.
    - Validation map has no TBD task IDs, covers every Phase 3 requirement/threat, and marks green/complete only from actual gate records and commands.
    - Phase 1 and Phase 2 regressions pass; no existing assertion is weakened and no schema-push task is introduced.
  </acceptance_criteria>
  <done>All Phase 3 requirements have fresh automated evidence, the exact supply chain remains approved, and the validation strategy truthfully records execution status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI argv/subject/environment → runtime | User-controlled selectors/paths and environment credentials must be admitted without echo or persistence. |
| Recorded providers → E2E pipeline | Fixtures model untrusted GitHub/OpenAI responses; unrecorded network must fail. |
| Runtime outputs → operator/durable evidence | Summaries/packages/state must disclose only approved bounded facts and no publication authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-E2E | Tampering / Elevation of Privilege | CLI-to-runtime flow | critical | mitigate | Strict pre-state input validation, closed root only, full call-count/capability/remote-write tests, exact local terminal schema. |
| T-03-07-ID | Information Disclosure | CLI/durable surfaces | high | mitigate | Closed diagnostics, canonical bounded summary, header-only keys, canary sweeps across every output/state/package byte. |
| T-03-07-R | Repudiation | resume/reuse/rejection evidence | high | mitigate | Full-chain ledger verification, exact call counts, immutable identity/version keys and byte-identical reuse assertions. |
| T-03-07-NET | Elevation of Privilege | automated tests | high | mitigate | Recorded transports and outbound socket sentinel; no live network or remote-write adapter. |
</threat_model>

## Artifacts this phase produces

- Modified source file: `src/skillscout/cli.py` with additive `build-candidate` parser/dispatch.
- New test file: `tests/test_cli_validate_skill.py`.
- Modified tests: `tests/test_cli_security.py` additive subparser/non-echo assertions only.
- New deterministic gate files: `tools/verify_phase3_acceptance.py`, `tests/test_phase3_acceptance_tool.py`.
- Modified planning evidence: `.planning/phases/03-validated-skill-candidate/03-VALIDATION.md` finalized task/status map.
- User-visible runtime files: `<slug>/SKILL.md`, `<slug>/references/provenance.json`, optional bounded one-level text resources, external package manifest/digest, `validated-candidate-summary.json`, state/checkpoint manifests.
- User-visible command/symbol: `skillscout build-candidate`, using existing `build_parser()`/`main()` and new `build_phase_three_runtime`/`CandidateSubjectV1`/`PriorLineageBindingV1` interfaces; no publication command.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Convert extracted workflow into standard documented source-clear local Skill and explain every decision | 03-02..03-07 | COVERED | CLI demonstrates full local outcome and all explanations. |
| REQ | QUAL-01 | Versioned deterministic specificity/reuse/verifiability/evidence/safety qualification | 03-02, 03-06, 03-07 | COVERED | Pure policy, pipeline order, E2E rejection. |
| REQ | QUAL-02 | 100 points, threshold 75, no hard failure, itemized report | 03-02, 03-07 | COVERED | Exact boundaries and output evidence. |
| REQ | GEN-01 | Standard directory/SKILL.md/necessary references/assets | 03-03, 03-06, 03-07 | COVERED | Trusted rendering, materialization and E2E tree. |
| REQ | GEN-02 | Documentation only; no scripts/binaries/executable bits/copied executable code | 03-03, 03-04, 03-07 | COVERED | Generation controls, validation defense, package scan. |
| REQ | GEN-03 | Generalized rewrite; bounded attributed excerpts | 03-03, 03-04, 03-07 | COVERED | Structured generation and exact quote/copy policy. |
| REQ | GEN-04 | Complete machine-readable provenance | 03-03, 03-04, 03-07 | COVERED | Rendered provenance plus independent revalidation. |
| REQ | GEN-05 | Stable slug/fingerprint lineage and update-not-duplicate | 03-03, 03-06, 03-07 | COVERED | Separate identities, selector, changed/same identity E2E. |
| REQ | VAL-01 | Official validator and structural/progressive disclosure checks | 03-01, 03-04, 03-07 | COVERED | Approved dependency, confined adapter, E2E/full gate. |
| REQ | VAL-02 | Deterministic safety/source/copy checks | 03-04, 03-07 | COVERED | Adversarial fixtures and durable/package scans. |
| REQ | VAL-03 | Structured severities/versions; errors block | 03-04, 03-06, 03-07 | COVERED | Report invariants, call cascade, E2E. |
| REQ | REV-01 | Fresh Reviewer with only four inputs | 03-05, 03-06, 03-07 | COVERED | Adapter request proof and E2E canaries. |
| REQ | REV-02 | Strict judge-only output, no edits | 03-05, 03-07 | COVERED | Recursive schema proof and no replacement surface. |
| REQ | REV-03 | Zero errors + YES + confidence ≥0.80 | 03-05, 03-06, 03-07 | COVERED | Pure gate, processor and E2E boundary. |
| RESEARCH | Two-gate `skills-ref` legitimacy/lock ceremony | 03-01 | COVERED | Gate A3/B3 block all official-validator execution. |
| RESEARCH | Qualification 100-point architecture | 03-02 | COVERED | Versioned exact rubric/hard failures/order. |
| RESEARCH | Structured draft/trusted renderer; quote policy | 03-03 | COVERED | Model semantics only; 120/240/80 boundaries. |
| RESEARCH | Separate lineage/artifact/package identities; no self-hash | 03-03, 03-07 | COVERED | Identity properties plus E2E scan. |
| RESEARCH | Layered official/custom validation and descriptor admission | 03-04 | COVERED | Third-party call after exact admission. |
| RESEARCH | Independent judge-only Reviewer | 03-05 | COVERED | Fresh four-input request and exact gate. |
| RESEARCH | Additive phase3-v1, full-chain reuse, REMOTE_READ ceiling | 03-06, 03-07 | COVERED | Root/reuse/E2E capability proof. |
| PHASE2 | WorkflowSpec sole downstream semantic boundary | 03-02..03-07 | COVERED | No raw Reader bundle enters Phase 3 stages. |
| PHASE2 | Runner-owned retry and zero-call exact reuse | 03-05..03-07 | COVERED | One-call adapters and full-chain E2E. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | No locked decisions or deferred ideas exist. |

<verification>
- `build-candidate` happy path and complete rejection/resume/reuse matrix pass over recorded transports with zero unauthorized remote writes.
- Full pytest, Ruff and lock checks pass under the exact Gate-B3-approved lock using the repository-local uv/Python configuration.
- Capability and package scans prove no execution/publication authority, secret/raw-text leakage, invalid tree/mode, or self-hash cycle.
- `03-VALIDATION.md` contains exact final task IDs and truthful fresh statuses.
</verification>

<success_criteria>
- The user can build and inspect an explainable local validated Skill candidate from one exact extracted workflow.
- Every failure path is auditable, non-retry-seeking, and ineligible; no failure reaches publication planning.
- Same identity is exactly reusable with zero provider/validator calls, while changed identity/version re-evaluates.
- All 13 Phase 3 requirements and relevant Phase 2/security boundaries have direct automated evidence.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-07-SUMMARY.md` with CLI contract, E2E call-count matrix, exact lock/full-gate output, canary scan results, and final validation-map status.</output>
