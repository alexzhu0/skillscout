---
phase: 03-validated-skill-candidate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - config/supply-chain/phase3-gate-b3.lock.sha256
  - tools/verify_phase3_gate_b3.sh
  - tests/test_phase3_lock_preflight.py
autonomous: false
requirements: [VAL-01]
must_haves:
  truths:
    - "Gate A3 approves exactly skills-ref==0.1.1 and its official-source provenance before any lock discovery."
    - "Gate B3 approves every new registry-only node, artifact hash, and the exact uv.lock SHA-256 before sync, import, tests, or validator execution."
    - "The official validator dependency is admitted as one signal only; no new package is introduced outside the two-gate record."
    - "A committed one-line Gate-B3 approval record and dependency-free preflight compare exact uv.lock bytes before every later uv/import/test/validator command."
  artifacts:
    - { path: pyproject.toml, provides: "exact direct skills-ref declaration after Gate A3" }
    - { path: uv.lock, provides: "registry-only graph whose exact bytes receive Gate B3 approval" }
    - { path: config/supply-chain/phase3-gate-b3.lock.sha256, provides: "committed exact approved uv.lock SHA-256 authority" }
    - { path: tools/verify_phase3_gate_b3.sh, provides: "dependency-free fail-before-execution lock equality preflight" }
    - { path: tests/test_phase3_lock_preflight.py, provides: "missing/malformed/mismatched/exact approval-record proof" }
  key_links:
    - { from: pyproject.toml, to: uv.lock, via: "repository-local non-building uv lock discovery", pattern: "skills-ref==0.1.1" }
    - { from: uv.lock, to: "Gate B3 approval record", via: "review of every new node/artifact and exact lock SHA-256", pattern: "name = \"skills-ref\"" }
    - { from: tools/verify_phase3_gate_b3.sh, to: config/supply-chain/phase3-gate-b3.lock.sha256, via: "read approved digest before hashing uv.lock", pattern: "phase3-gate-b3.lock.sha256" }
  prohibitions:
    - "No sync, import, build, pytest, Ruff, or official-validator execution may occur before Gate B3 approval."
    - "No Git/path/editable/direct-URL/alternate-registry dependency and no undeclared direct dependency may enter the graph."
    - "A rejected Gate A3 or Gate B3 stops Phase 3; the official-validator requirement must not be replaced with a hand-rolled substitute."
---

<objective>
Admit the official Agent Skills validator through the existing two-gate human supply-chain ceremony.

Purpose: Satisfy VAL-01 without weakening the exact-lock authority established in Phases 1 and 2.
Output: Human-approved direct declaration, registry-only candidate lock graph, exact Gate-B3-approved lock bytes, and a committed dependency-free preflight authority.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 03-01-01: Gate A3 — approve the exact official-validator declaration and provenance</name>
  <files>None — this checkpoint precedes edits, resolution, download, import, and execution.</files>
  <read_first>pyproject.toml, uv.lock, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/03-validated-skill-candidate/03-PATTERNS.md, .planning/phases/02-safe-single-repository-extraction/02-01-SUMMARY.md, .planning/STATE.md</read_first>
  <action>Present the Gate A3 record and stop for explicit human approval. Record the one proposed direct declaration `skills-ref==0.1.1`, PyPI project and wheel provenance, official `agentskills/agentskills` repository relationship, the repository/PyPI version and CLI-name discrepancies, demonstration-only warning, absent Trusted Publishing signal, wheel SHA-256 `d35db5bb8de71ae301daf5ca9cb71f8a555e8c6f83a6d40e46a5bc09f8f461b5`, and intended in-process `skills_ref.validate(Path)` use. Name `click` and `strictyaml` only as expected transitives whose exact versions/artifacts remain undecided until Gate B3. Record explicit non-additions: no Markdown parser, secret-scanning package, subprocess wrapper, retry package, direct `click`, or direct `strictyaml`. State that the official validator is one versioned validation family and never the sole safety/source validator. Do not allow auto-advance to approve the package.</action>
  <verify><automated>N/A — package legitimacy and publisher/source trust are intentionally human-only.</automated></verify>
  <acceptance_criteria>
    - The approval names the exact `skills-ref==0.1.1` declaration, wheel digest, official-source relationship, all discrepancies, and all explicit non-additions.
    - The reviewer acknowledges that transitives and their artifacts require a separate Gate B3 decision.
    - No dependency file changed and no resolver/download/import/execution action occurred before the approval signal.
  </acceptance_criteria>
  <done>Gate A3 is explicitly approved in the execution record; otherwise execution stops with the dependency graph unchanged.</done>
</task>

<task type="auto">
  <name>Task 03-01-02: Discover the registry-only lock graph without building or executing it</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>pyproject.toml, uv.lock, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/02-safe-single-repository-extraction/02-01-SUMMARY.md</read_first>
  <action>Proceed only after Gate A3. Add exactly `skills-ref==0.1.1` to the sorted `[project].dependencies` list; leave every existing pin, build requirement, dev dependency, and source declaration unchanged. Run only `UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --no-build --no-sources --no-cache --managed-python --no-python-downloads --python 3.13.14`. Stop if resolution requests a build or any source outside the default registry. Produce a Gate B3 inventory containing every changed `[[package]]`, dependency edge, marker, sdist/wheel URL, SHA-256 and size, plus the SHA-256 of the exact new `uv.lock`; compare it against the Gate-B2-approved predecessor. Do not sync, import, build, run tests, run Ruff, or invoke the validator.</action>
  <verify>
    <automated>UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; grep -q '"skills-ref==0.1.1"' pyproject.toml &amp;&amp; grep -q 'name = "skills-ref"' uv.lock</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` adds exactly one sorted direct dependency and changes no other declared dependency or source.
    - `uv.lock` resolves under managed Python 3.13.14 without build/source execution and contains only registry nodes.
    - The Gate B3 inventory and exact candidate-lock SHA-256 exist before approval; no package/project import or test command has run.
  </acceptance_criteria>
  <done>The candidate dependency graph is resolved but remains unexecuted and unauthorized until Gate B3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 03-01-03: Gate B3 — approve every resolved artifact and exact lock bytes</name>
  <files>uv.lock</files>
  <read_first>uv.lock, pyproject.toml, the Task 03-01-02 Gate B3 inventory, .planning/phases/03-validated-skill-candidate/03-RESEARCH.md, .planning/phases/02-safe-single-repository-extraction/02-01-SUMMARY.md</read_first>
  <action>Before any sync, import, build, pytest, Ruff, or validator call, present every new or changed node and artifact relative to the Gate-B2 lock. Require exact versions, expected edges, default-registry PyPI sources, artifact URLs, hashes, and sizes for `skills-ref` and every resolver-selected transitive. Reject lookalikes, yanked/adverse releases, missing hashes, alternate registries, Git/path/editable/direct-URL sources, source-build requirements, unexpected dependency families, or a root declaration other than the approved exact pin. Record and obtain explicit human approval for the SHA-256 of the exact `uv.lock`; any later byte change invalidates Gate B3. Do not allow auto-advance to approve executable supply-chain bytes.</action>
  <verify><automated>N/A — complete transitive provenance and exact-lock authority are mandatory human decisions.</automated></verify>
  <acceptance_criteria>
    - Every new node, edge, marker, URL, artifact hash, and size has an explicit human disposition.
    - All non-root packages are exact registry-only nodes and the existing pins/dev group are unchanged.
    - The human explicitly approves the recorded exact-lock SHA-256 and understands later byte changes invalidate approval.
    - No sync/import/build/test/Ruff/validator command ran before the approval signal.
  </acceptance_criteria>
  <done>Gate B3 authorizes execution of the exact reviewed graph; rejection stops Phase 3 before official validation is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-04: Commit and prove the Gate-B3 exact-lock preflight</name>
  <files>config/supply-chain/phase3-gate-b3.lock.sha256, tools/verify_phase3_gate_b3.sh, tests/test_phase3_lock_preflight.py</files>
  <read_first>uv.lock, pyproject.toml, the approved Task 03-01-03 Gate B3 record, .planning/phases/03-validated-skill-candidate/03-01-PLAN.md, .planning/phases/03-validated-skill-candidate/03-VALIDATION.md</read_first>
  <behavior>
    - The committed approval record is exactly one lowercase 64-hex SHA-256 plus newline and equals the Gate-B3-approved uv.lock bytes.
    - The dependency-free shell preflight exits zero only for exact equality and exits nonzero for a missing, malformed, stale, or mismatched record or lock.
    - A mismatch stops before the sentinel dependency command is reached.
  </behavior>
  <action>Proceed only after the explicit Gate B3 approval signal. Write the approved full digest, and no metadata or mutable status field, to `config/supply-chain/phase3-gate-b3.lock.sha256`. Create mode-0644 `tools/verify_phase3_gate_b3.sh`, invoked explicitly with `sh`, using only POSIX shell plus `/usr/bin/shasum -a 256`: resolve the repository root from the script location, require both the record and `uv.lock` to be regular non-symlink files, require the record to contain exactly one lowercase 64-hex digest and newline, compute the current lock digest, compare exact strings, emit only a fixed success/failure code, and return nonzero before invoking any caller-supplied command. The tool must not call uv, Python, import project/package code, install, sync, build, source another file, accept an alternate path/hash, or mutate either input. Add subprocess-isolated tests that copy the tool/record/lock to a private directory and cover exact, one-byte lock mutation, malformed/missing record, symlink inputs, and a sentinel proving mismatch prevents the following command. After the preflight itself succeeds, prefix it immediately before the focused pytest/Ruff commands; every post-Gate-B3 command in this and later plans must use the same prefix.</action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_lock_preflight.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_CACHE_DIR="$PWD/.tools/uv-cache" UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check tests/test_phase3_lock_preflight.py</automated>
  </verify>
  <acceptance_criteria>
    - The committed digest is byte-for-byte the human-approved Gate-B3 value and exact current uv.lock SHA-256.
    - The preflight is dependency-free, has no bypass parameters, is invoked with `sh`, and remains non-executable mode 0644.
    - Mutation/malformed/missing/link tests fail before the sentinel dependency command; exact bytes pass.
    - Every Plan 03-02 through 03-07 automated uv/import/test/official-validator command is prefixed by this tool.
  </acceptance_criteria>
  <done>One committed approval record now mechanically prevents any later dependency execution against unapproved lock bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PyPI metadata/artifacts → candidate lock | Registry bytes and dependency metadata are untrusted until reviewed. |
| Candidate lock → executable environment | Only the exact Gate-B3-approved bytes authorize sync/import/test execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-SC | Spoofing / Tampering | `skills-ref` and transitives | high | mitigate | Gate A3 verifies the exact declaration/provenance; Gate B3 reviews every registry artifact and approves the exact lock hash before execution. |
| T-03-01-DG | Tampering | `uv.lock` after approval | high | mitigate | Record exact SHA-256 at Gate B3 and require equality before every downstream locked command. |
| T-03-01-DOS | Denial of Service | dependency resolution | low | accept | Resolution is one bounded local metadata operation; unexpected builds/sources fail closed and leave the previous approved lock in version control. |
</threat_model>

## Artifacts this phase produces

- Modified tracked files: `pyproject.toml`, `uv.lock`.
- New committed authority/tool/test files: `config/supply-chain/phase3-gate-b3.lock.sha256`, `tools/verify_phase3_gate_b3.sh`, `tests/test_phase3_lock_preflight.py`.
- Package identity: direct `skills-ref==0.1.1`; resolver-selected transitives remain lock-owned and are not imported directly.
- Human evidence: Gate A3 provenance approval, Gate B3 full graph/artifact disposition, exact approved `uv.lock` SHA-256.
- New source symbols: dependency-free `verify_phase3_gate_b3.sh` preflight contract; no application import surface.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Standard local Agent Skill validation | 03-01 | COVERED | Safely admits the official validation signal required by the goal. |
| REQ | VAL-01 | Run the official Agent Skills validator | 03-01, 03-04 | COVERED | This plan admits the dependency; Plan 03-04 implements and tests the adapter. |
| RESEARCH | Package Legitimacy Audit | `skills-ref`, `click`, `strictyaml` are SUS | 03-01 | COVERED | Two blocking human gates; no silent fallback. |
| RESEARCH | Exact-lock authority | Registry-only non-building discovery before execution | 03-01 | COVERED | Task 03-01-02 is the only pre-Gate-B3 automated operation. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | Planning directly from approved roadmap/requirements/research. |

<verification>
- Gate A3 precedes lock discovery; Gate B3 precedes every sync/import/test/validator action.
- `pyproject.toml` contains the exact approved pin and `uv.lock` passes the repository-local `uv lock --check` command.
- The current lock SHA-256 exactly matches the Gate B3 approval record.
</verification>

<success_criteria>
- The official-validator graph is human-approved at declaration and exact-artifact levels.
- No unapproved dependency bytes execute.
- Downstream plans have a stable exact-lock authority and no permission to hand-roll or omit VAL-01.
</success_criteria>

<output>Create `.planning/phases/03-validated-skill-candidate/03-01-SUMMARY.md` with both approval signals, the full package inventory, and the exact approved lock SHA-256.</output>
