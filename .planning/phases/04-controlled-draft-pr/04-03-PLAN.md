---
phase: 04-controlled-draft-pr
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/skillscout/domain/publication.py
  - tests/test_publication_domain.py
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-05]
must_haves:
  truths:
    - "Only one canonical eligible Phase 3 bundle can become candidate publication evidence, before token/network/state access."
    - "Publication intent and its authority-bound admission digest exist only after protected catalog and individual-reviewer authority are supplied."
    - "Catalog, target root, branch, marker, reviewers, title, and body are closed deterministic domain facts."
    - "Only exact frozen manifest files and bytes are publishable."
    - "Stable publication identity is independent of revision-specific package evidence, while verified machine lineage links successive desired revisions."
  artifacts:
    - path: src/skillscout/domain/publication.py
      provides: "Strict publication authority, admission, identity, marker, rendering, transition, and result contracts."
  key_links:
    - from: CompletedCandidateProjectionV1
      to: CandidatePublicationEvidenceV1
      via: "Strict canonical parse and complete Phase 3 artifact matrix revalidation."
    - from: CandidatePublicationEvidenceV1 + CatalogAuthorityV1 + ReviewerTargetsV1
      to: PublicationIntentV1
      via: "Protected code-owned derivation of catalog root, branch, marker, desired revision, and authority-bound admission digest."
  prohibitions:
    - "No directory walk, caller-provided catalog path/ref/body, token, transport, filesystem write, or LLM call."
    - "Default branch can never equal the derived machine branch."
---

<objective>
Implement the pure publication domain, candidate-evidence gate, and protected authority composition contract.

Purpose: Prove exact approved bytes without leaking protected authority across jobs, then derive deterministic human-review intent before any remote capability exists.
Output: `publication.py` and green domain tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-01-SUMMARY.md
@.planning/phases/04-controlled-draft-pr/04-RESEARCH.md
@src/skillscout/adapters/state.py
@src/skillscout/domain/canonical.py
@src/skillscout/domain/skill_artifacts.py
@src/skillscout/domain/qualification.py
@src/skillscout/domain/validation.py
@src/skillscout/domain/review.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-03-01: Define closed catalog and publication identities</name>
  <files>src/skillscout/domain/publication.py, tests/test_publication_domain.py</files>
  <read_first>
    - `src/skillscout/domain/skill_artifacts.py` strict frozen/canonical/digest pattern.
    - `04-RESEARCH.md` recommended identities and closed grammar.
  </read_first>
  <action>Define code-owned versions and strict frozen `CandidatePublicationEvidenceV1`, `CatalogAuthorityV1`, `ReviewerTargetsV1`, `PublicationIntentV1`, `PublicationAdmissionV1`, `PublicationMarkerV1`, `MachineLineageV1`, remote observation/transition models, `PublicationRecordV1`, and `PublicationResultV1`. `CandidatePublicationEvidenceV1` contains only Phase 2/3 candidate-bound identities, digests, and exact frozen files; it contains no catalog, reviewer, branch, intent, or authority-dependent admission field. `ReviewerTargetsV1` contains exactly one non-empty sorted unique tuple `reviewers` of bounded individual GitHub logins; it has no team field and rejects team-shaped input. Derive `PublicationIntentV1` only from candidate evidence plus `CatalogAuthorityV1` and `ReviewerTargetsV1`; derive target root `skills/{stable_slug}/`, head `skillscout/{stable_slug}`, and stable publication key from numeric catalog ID/full name/base/head/slug only, then derive desired revision separately from publication key/package digest/policy. Define `PublicationAdmissionV1` as the protected composition of exact candidate evidence and canonical intent, with an admission digest that binds both. The marker carries stable identity, current revision evidence, verified machine lineage, and only the individual reviewer tuple, so a prior desired revision/package digest may differ without invalidating ownership. Use bounded login/ref/full-name grammars and canonical SHA-256 preimages; reject malformed, spoofed, cross-catalog, or internally inconsistent lineage.</action>
  <acceptance_criteria>
    - Unknown/missing fields and malformed catalog/reviewer/ref/path values fail strict validation.
    - Candidate evidence serialization has no catalog/reviewer/intent/admission field; intent and admission digest cannot be constructed without both protected authority objects.
    - Every stable-identity field mutation changes the publication key; package changes alter desired revision without altering stable identity.
    - No constructor accepts arbitrary REST path, PR body, target root, or machine ref.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_domain.py -k 'identity or grammar or marker'</automated></verify>
  <done>Publication identity and remote observations are immutable, versioned, and content addressed.</done>
</task>

<task type="auto" tdd="true">
  <name>04-03-02: Admit the exact eligible Phase 3 bundle</name>
  <files>src/skillscout/domain/publication.py, tests/test_publication_domain.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` `_artifact_declared_digest` and `_validate_terminal_artifact_matrix`.
    - `src/skillscout/domain/review.py` `CandidateTerminalSummaryV1`.
  </read_first>
  <action>Implement `admit_phase3_candidate(terminal_summary, terminal_summary_bytes, artifacts)` to return `CandidatePublicationEvidenceV1`, plus `derive_publication_intent(candidate_evidence, catalog_authority, reviewer_targets)` and `bind_publication_admission(candidate_evidence, publication_intent)`. Strictly parse and byte-compare terminal, qualification, frozen package, manifest, validation, and review attestation; reuse or extract one shared public verifier rather than weaken the Phase 3 projector. Require outcome `eligible_local_candidate`, eligible true, qualification pass, zero validation errors, clean report, eligible review disposition, exact attestation and package/manifest cross-digests, sorted unique files, mode 0644, no symlink, exact size/hash, and only paths below the derived slug root. Candidate admission returns exact `(catalog_path, content)` entries from `FrozenSkillPackageV1.files` and candidate-bound digests only; never inspect a materialized directory or accept catalog/reviewer authority. Intent and authority-bound admission derivation are pure but require explicit protected authority inputs. Reject before invoking any passed dependency probes.</action>
  <acceptance_criteria>
    - Complete one-at-a-time mutation matrix passes.
    - Rejected input makes zero token/network/state/filesystem probe calls.
    - Candidate evidence output contains exactly one entry per frozen manifest file and identical bytes, with no authority-dependent digest.
    - Changing catalog or reviewer authority changes intent/admission digests but cannot change candidate evidence digests.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_domain.py -k admission</automated></verify>
  <done>Only exact Phase 3-approved bytes can cross into publication.</done>
</task>

<task type="auto" tdd="true">
  <name>04-03-03: Render deterministic Draft metadata and machine marker</name>
  <files>src/skillscout/domain/publication.py, tests/test_publication_domain.py</files>
  <read_first>
    - `04-RESEARCH.md` PUB-02 required body and marker fields.
    - `src/skillscout/domain/qualification.py` itemized report fields.
  </read_first>
  <action>Implement `render_pull_request_title`, `render_pull_request_body`, and `parse_publication_marker`. Render fixed ordered sections from strict provenance, WorkflowSpec evidence summaries, qualification items/score, validation counts/findings summary, review verdict/confidence/reasons, exact source/catalog identities, and an explicit human-only approval/merge warning. Append exactly one bounded canonical marker whose stable identity fields are validated separately from revision evidence and machine lineage; parsing a prior valid marker does not require its desired revision/package digest to equal the newly admitted intent. Do not include raw repository documents, arbitrary provider errors, credentials, or candidate-origin shell syntax.</action>
  <acceptance_criteria>
    - Same admitted bundle renders byte-identical title/body/marker.
    - Every PUB-02 field is asserted by golden tests.
    - Duplicate, altered, oversized, or cross-catalog marker fails closed.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_domain.py</automated></verify>
  <done>Draft metadata is complete, deterministic, bounded, and recovery-authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Completed Phase 3 projection → CandidatePublicationEvidenceV1 | Canonical candidate bytes and every Phase 2/3 digest are revalidated without protected authority. |
| Candidate evidence + protected authority → PublicationAdmissionV1 | Intent and authority-bound admission are derived only inside the protected boundary. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-01 | Elevation | Token-before-admission | medium | critical | high | mitigate | Pure admission plus zero-call dependency probes | Composition can violate ordering | Rechecked in Plans 06/09 |
| T-04-03 | Tampering | Manifest substitution or unlisted bytes | medium | critical | high | mitigate | Canonical package/manifest reconstruction and byte mapping | Corrupt upstream store | Fail closed |
| T-04-02 | Confused deputy | Arbitrary repo/ref/path authority | medium | critical | high | mitigate | Bound catalog and code-derived root/head | Misconfigured protected vars | Live gate |
| T-04-14 | Tampering | Authority-dependent intent/admission digest is falsely trusted as unprivileged handoff evidence | medium | critical | high | mitigate | Candidate-only evidence type; protected intent/admission construction requires explicit authority inputs | Composition regression | Plans 06/09 block |
| T-04-08 | Spoofing | Machine marker spoof | medium | high | high | mitigate | Canonical versioned marker cross-bound to intent | Human edits body | Manual intervention |
</threat_model>

## Artifacts this phase produces

- `src/skillscout/domain/publication.py`
- Constants: `PUBLICATION_POLICY_VERSION`, `PUBLICATION_MARKER_SCHEMA_VERSION`, `PUBLICATION_RECORD_SCHEMA_VERSION`.
- Models: `CandidatePublicationEvidenceV1`, `CatalogAuthorityV1`, `ReviewerTargetsV1`, `PublicationFileV1`, `PublicationAdmissionV1`, `PublicationIntentV1`, `PublicationMarkerV1`, `MachineLineageV1`, `CatalogRepositoryObservationV1`, `MachineRefObservationV1`, `OwnedSubtreeEntryV1`, `OwnedSubtreeObservationV1`, `PullRequestObservationV1`, `ReviewerObservationV1`, `CompletedReviewObservationV1`, `PublicationCheckpointV1`, `PublicationRecordV1`, `PublicationResultV1`.
- Functions: `admit_phase3_candidate`, `derive_publication_intent`, `bind_publication_admission`, `render_pull_request_title`, `render_pull_request_body`, `parse_publication_marker`.
- `CandidatePublicationEvidenceV1` fields are candidate-bound provenance/digests and frozen files only; `PublicationIntentV1` binds catalog/reviewer authority; `PublicationAdmissionV1` binds both and owns `admission_digest`.
- `MachineLineageV1` fields: `schema_version`, `publication_key`, `machine_commit_sha`, `parent_commit_sha`, `tree_sha`, `previous_marker_digest`, `previous_desired_revision`, `lineage_digest`.
- `OwnedSubtreeEntryV1` fields: `relative_path`, `blob_sha`, `mode`, `object_type`, `size`; `OwnedSubtreeObservationV1` fields: `schema_version`, `catalog_repository_id`, `root_path`, `tree_sha`, `entry_count`, `entries`, `truncated`, `observation_digest`.
- Other new fields include `publication_key`, `desired_revision`, `marker_digest`, `catalog_repository_id`, `catalog_full_name`, `base_branch`, `head_branch`, `target_root`, `package_digest`, `manifest_digest`, `terminal_summary_digest`, `validation_report_digest`, `review_attestation_digest`, and `reviewers`; no domain/intent/marker/record model contains a team field.
- No CLI flags or additional files.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Only gated Skill reaches controlled Draft intent | COVERED. |
| REQ | PUB-01, PUB-02, PUB-03, PUB-05 | COVERED by domain authority and marker identity. |
| RESEARCH | Patterns 1–2; exact manifest; deterministic body/marker | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run the full domain suite in the locked environment.</verification>
<success_criteria>Candidate evidence is canonical and authority-free; protected authority deterministically produces the only valid catalog-bound Draft intent/admission before effects.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-03-SUMMARY.md` when done.</output>
