---
phase: 04-controlled-draft-pr
plan: 10
type: execute
wave: 7
depends_on: [04-09]
files_modified: []
autonomous: false
requirements: [PUB-01, PUB-03, PUB-04, PUB-05, SEC-02]
user_setup:
  - service: github
    why: "Run the protected catalog publication and causal platform-permission canary."
    env_vars:
      - name: SKILLSCOUT_APP_CLIENT_ID
        source: "Protected environment variable for the approved GitHub App."
      - name: SKILLSCOUT_APP_PRIVATE_KEY
        source: "Protected environment secret; never copy into chat, files, or logs."
      - name: SKILLSCOUT_CATALOG_REPOSITORY_ID
        source: "Numeric ID of the controlled catalog."
      - name: SKILLSCOUT_CATALOG_FULL_NAME
        source: "Exact owner/repository of the controlled catalog."
      - name: SKILLSCOUT_CANARY_INSTALLATION_ID
        source: "Positive installation ID from independently reviewed GitHub App configuration; not discovered with the installation token."
      - name: SKILLSCOUT_CATALOG_BASE_BRANCH
        source: "Exact protected default/base branch reviewed before token issuance and verified remotely after token issuance."
      - name: SKILLSCOUT_CATALOG_REVIEWERS
        source: "Non-empty bounded list of authorized individual GitHub reviewer logins."
      - name: SKILLSCOUT_PUBLICATION_POLICY_VERSION
        source: "Exact code-approved publication policy version."
    dashboard_config:
      - task: "Protect environment skillscout-catalog-publish with required human reviewers."
        location: "GitHub repository Settings → Environments."
      - task: "Install the App only on the catalog with Contents write and Pull requests write, no Administration."
        location: "GitHub App installation settings."
      - task: "Enable active default-branch ruleset with required PR/human review and no App bypass."
        location: "Catalog Settings → Rules → Rulesets."
must_haves:
  truths:
    - "A human reviews independent configuration evidence and a real canary from the same production installation identity."
    - "The identity succeeds only on machine branch/Draft/reviewer flow and causally fails default write, merge, ruleset administration, unauthorized resource, and secret access with unchanged remote state."
    - "Approve and ready-for-review remain human-only because the production adapter/CLI/workflow cannot express them; no false platform-denial claim is made for the coarse Pull requests write token."
    - "Cleanup is performed only by separately authorized human/admin authority."
  artifacts:
    - path: .planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md
      provides: "Human-reviewed live canary evidence and cleanup attestation."
  key_links:
    - from: publish-candidate.yml
      to: controlled catalog
      via: "Protected environment and scoped installation token."
  prohibitions:
    - "This gate is never auto-approvable."
    - "Publisher production code cannot clean up, merge, approve, ready, or change rules; residual out-of-process token capability is documented."
---

<objective>
Run and human-review the real protected-environment permission canary.

Purpose: Prove the platform boundary that application code and offline tests cannot establish.
Output: Approved live evidence bundle and separate-authority cleanup attestation.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-09-SUMMARY.md
@.github/workflows/publish-candidate.yml
@tests/test_publication_live_canary.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>04-10-01: Verify catalog governance and live production-identity canary</name>
  <files>None — the reviewed evidence and decision are recorded in the executor-created `04-10-SUMMARY.md` output.</files>
  <read_first>
    - `04-09-SUMMARY.md` workflow and offline security results.
    - `04-VALIDATION.md` causal canary requirements.
  </read_first>
  <action>Before token release, have an independently authorized human/admin export and review catalog numeric/full-name/base-branch identity, installed App repository/permission scope, active ruleset target/status/rules/bypass actors, protected environment reviewers, configured individual reviewer logins, proof that team configuration is absent/empty, exact approved workflow action SHA, and explicit unauthorized repository/secret-resource targets. Require workflow evidence that the unprivileged job has no protected environment/config and exports only the three candidate locators plus seven candidate digests; require the protected pre-token step to re-read/compare candidate evidence, then load reviewed authority and derive local `publication_intent_digest`/`admission_digest`, with neither digest accepted from the unprivileged job. Require the post-token catalog observation to match the protected base branch before writes. Run the opt-in canary through the protected environment. Review the bounded evidence for machine-branch commit, one Draft PR, individual reviewer request, a bounded public ruleset observation (whose read may succeed), and causal denial of default-ref update, otherwise-valid merge, ruleset mutation, unauthorized repository/resource access, and repository/environment secret access; require equal pre/post default SHA and unchanged ruleset/unauthorized-resource state. Separately review the isolated transport/static evidence that production exposes no review-submission/approval endpoint, no GraphQL/ready transition, no team request body field, and no CLI/workflow path for those actions. Record that Pull requests write may permit ready-for-review if the token is stolen or used outside SkillScout; this residual platform capability is not accepted as production authority and any appearance of the route in production blocks the phase. Then use separate human/admin authority to remove only the canary branch/PR and attest cleanup; never give cleanup credentials to SkillScout.</action>
  <acceptance_criteria>
    - Positive flow uses the exact production installation identity and configured catalog/reviewer.
    - Every live platform negative probe is causally attributed and all protected remote state is unchanged.
    - Approve/ready evidence proves production capability absence, and the residual coarse-token risk is explicitly reviewed rather than reported as platform denial.
    - App has no Administration and no ruleset bypass.
    - Secret scan is clean; cleanup identity is distinct and human/admin controlled.
  </acceptance_criteria>
  <verify>
    <automated>SKILLSCOUT_LIVE_CANARY=1 .tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_live_canary.py -x</automated>
    <human-check>Review independent ruleset/App/environment evidence, the canary bundle, secret scan, and separate-authority cleanup; type approved or describe the failed control.</human-check>
  </verify>
  <done>Human-approved evidence proves intended Draft capability, scoped platform denials, and separate production-surface approve/ready prohibitions with residual risk recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub control-plane configuration → security claim | Ruleset/App/environment state needs independent human evidence. |
| Production identity → cleanup | Cleanup must use distinct human/admin authority. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-05 | Elevation | App has broader scope or bypass | medium | critical | high | mitigate | Independent permission/ruleset export + live probes | Platform admin later changes config | Re-run on change |
| T-04-04 | Elevation | Default write/merge denial is non-causal or approve/ready is falsely claimed as platform-denied | medium | critical | high | mitigate | Otherwise-valid live canary plus separate production route/transport absence evidence | Stolen token may have ready capability | Human reviews residual; production surface blocks |
| T-04-06 | Elevation/Repudiation | Publisher cleans up or cleanup is unaudited | low | critical | high | mitigate | Separate authority and cleanup attestation | Human admin error | Blocking gate |
| T-04-10 | Disclosure | Private key/token leaks during canary | low | critical | high | mitigate | Protected secret, closed logs, secret scan | Runner compromise | Platform residual |
</threat_model>

## Artifacts this phase produces

- Executor-created `.planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md`.
- Evidence fields: `catalog_repository_id`, `installation_id`, `app_permissions`, `ruleset_id`, `ruleset_digest`, `bypass_actors`, `environment_name`, `environment_reviewers`, `workflow_sha`, `action_sha`, `unprivileged_candidate_output_allowlist`, `unprivileged_protected_config_absent`, `protected_candidate_revalidation`, `protected_local_intent_admission_derivation`, `cross_job_authority_digest_absent`, `positive_pull_number`, `default_sha_before`, `default_sha_after`, `ruleset_state_before_after`, `unauthorized_resource_state_before_after`, `denial_classifications`, `approval_route_absent`, `ready_transition_absent`, `graphql_transport_absent`, `residual_platform_ready_risk`, `secret_scan`, `cleanup_actor`, `cleanup_attestation`.
- No production file, symbol, CLI flag, or secret is written.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Platform-enforced human-only merge | COVERED by live gate. |
| REQ | PUB-01, PUB-03, PUB-04, PUB-05, SEC-02 | COVERED by real environment evidence. |
| RESEARCH | Live canary and separate cleanup authority | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Opt-in canary passes and the human approves independent governance, secret, and cleanup evidence.</verification>
<success_criteria>Production identity can create/update only the intended Draft path and is demonstrably blocked from protected control surfaces.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md` when done.</output>
