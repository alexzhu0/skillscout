---
phase: 04-controlled-draft-pr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/github_publish/repository.json
  - tests/fixtures/github_publish/ref.json
  - tests/fixtures/github_publish/commit.json
  - tests/fixtures/github_publish/blob.json
  - tests/fixtures/github_publish/tree.json
  - tests/fixtures/github_publish/pull_draft.json
  - tests/fixtures/github_publish/pulls_page.json
  - tests/fixtures/github_publish/reviewers.json
  - tests/fixtures/github_publish/error_matrix.json
  - tests/test_github_publish_adapter.py
  - tests/test_publication_recovery.py
  - tests/test_publication_live_canary.py
autonomous: true
requirements: [PUB-01, PUB-04, PUB-05]
must_haves:
  truths:
    - "Wave 0 fixes exact GitHub request/response, crash, ambiguity, and opt-in live-canary contracts without live credentials."
    - "All offline provider behavior is reproducible through recorded MockTransport routes."
    - "Recovery tests cover complete owned-subtree enumeration, revision lineage, durable individual-reviewer evidence, and local-state loss."
  artifacts:
    - path: tests/fixtures/github_publish/
      provides: "Bounded provider response corpus."
    - path: tests/test_github_publish_adapter.py
      provides: "Closed transport contract."
    - path: tests/test_publication_recovery.py
      provides: "Crash/reconciliation and local-state-loss contract."
    - path: tests/test_publication_live_canary.py
      provides: "Explicit opt-in live environment contract."
  key_links:
    - from: tests/fixtures/github_publish/
      to: tests/test_github_publish_adapter.py
      via: "Exact method/path route map."
    - from: tests/test_publication_recovery.py
      to: src/skillscout/application/publication.py
      via: "Crash seams and remote reconstruction outcomes."
  prohibitions:
    - "Offline tests must not fall through to network."
    - "Live tests must skip unless every explicit canary variable and opt-in marker is present."
---

<objective>
Establish transport, recovery, and live-canary Wave 0 fixtures/tests.

Purpose: Define remote behavior and ambiguity handling before write-capable code exists.
Output: Bounded fixtures and collectable provider/recovery/canary suites.

Scope note: The fixture corpus and three test modules are intentionally coupled as one Wave 0 contract. Each fixture is a small synthetic provider response, production imports remain deferred, and every task has one focused collect-only/offline command; no implementation subsystem is included in this plan.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-controlled-draft-pr/04-RESEARCH.md
@.planning/phases/04-controlled-draft-pr/04-PATTERNS.md
@tests/recorded_transport.py
@tests/test_github_adapter.py
@tests/test_phase3_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-02-01: Freeze bounded GitHub publish fixtures and adapter contract</name>
  <files>tests/fixtures/github_publish/repository.json, tests/fixtures/github_publish/ref.json, tests/fixtures/github_publish/commit.json, tests/fixtures/github_publish/blob.json, tests/fixtures/github_publish/tree.json, tests/fixtures/github_publish/pull_draft.json, tests/fixtures/github_publish/pulls_page.json, tests/fixtures/github_publish/reviewers.json, tests/fixtures/github_publish/error_matrix.json, tests/test_github_publish_adapter.py</files>
  <read_first>
    - `tests/recorded_transport.py` exact route-key behavior.
    - `tests/test_github_adapter.py` request headers, bounds, and provider failure assertions.
    - `04-RESEARCH.md` provider response and forbidden-route matrices.
  </read_first>
  <action>Create small credential-free JSON fixtures for repository/ref/commit/blob/tree/Draft/list/current-requested-user/completed-user-review success and a table describing redirect, 401, 403, 404, 409, 422, 429, 5xx, oversized, malformed, wrong-content-type, missing-request-ID, pagination, unknown-field, and truncated-tree cases. Build strict tests for every named publish operation, including catalog-bound bounded `get_tree` owned-subtree enumeration, truncation rejection, `get_requested_reviewers` current-user observation, and bounded read-only `list_reviews` completed-user history. Assert full pagination, exact route/body, serial calls, `draft=true`, `maintainer_can_modify=false`, and `force=false`. Provider `teams` output is never projected as authority; any non-empty team result is a closed ambiguity. Review observations expose only bounded user login/review ID/commit SHA/state, and the expected interface has no timeline, review-submission/approval, GraphQL, or ready-for-review method. Defer production imports to test fixtures/functions so collection succeeds before the adapter exists while normal execution remains red. Unrecorded requests must raise.</action>
  <acceptance_criteria>
    - Fixtures contain bounded synthetic IDs only.
    - Tests enumerate the complete provider matrix and exact request bodies.
    - No generic request method or arbitrary route appears in the expected interface.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_github_publish_adapter.py</automated></verify>
  <done>The closed GitHub transport protocol is frozen as offline executable specification.</done>
</task>

<task type="auto" tdd="true">
  <name>04-02-02: Specify crash recovery and remote ambiguity</name>
  <files>tests/test_publication_recovery.py</files>
  <read_first>
    - `04-RESEARCH.md` reconcile decision table and crash matrix.
    - `tests/test_phase3_pipeline.py` completed-first, interruption, and zero-write proof styles.
  </read_first>
  <action>Create table-driven tests for crashes after blob/tree/commit creation, ref visibility, PR create/update, reviewer request, final remote verification, and remote success before local commit. Add local-state-loss recovery and duplicate PR, non-Draft, wrong base/head, malformed/cross-catalog marker, markerless machine commit, inconsistent lineage, legitimate later package revision, human commit, force update, closed/reopened/deleted PR, default-branch change, pending reviewer, completed reviewer, removed-after-request reviewer, malformed/paginated review evidence, and ref conflict. Configure only sorted unique individual logins. Pending current request and completed review after recovery both suppress notification. If an existing recovered PR has a configured login in neither the fully paginated current-request set nor completed-review set, including removed-after-request, return `manual_intervention_required` and never re-request it. Only a newly created PR with no prior reviewer-request opportunity may request a previously unseen configured login. Reject any team configuration before token/network and treat non-empty provider team observations as manual ambiguity. Test stale owned catalog files as before. Load planned production symbols inside tests so collection succeeds before implementation. Require one Draft, no repeated notifications, no force update, and bounded `manual_intervention_required` on ambiguity.</action>
  <acceptance_criteria>
    - Every research crash and ambiguity row has a named case.
    - Repeated desired revision performs no content write.
    - Human-head and marker conflicts never select or overwrite a remote object.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_publication_recovery.py</automated></verify>
  <done>Recovery and conflict semantics are executable before persistence/orchestration exists.</done>
</task>

<task type="auto" tdd="true">
  <name>04-02-03: Specify explicit live-canary admission</name>
  <files>tests/test_publication_live_canary.py</files>
  <read_first>
    - `04-VALIDATION.md` live canary and cleanup authority.
    - `04-RESEARCH.md` causal canary requirements.
  </read_first>
  <action>Create an opt-in pytest module that requires `SKILLSCOUT_LIVE_CANARY=1`, catalog numeric ID/full name, App token, ruleset evidence digest, reviewer target, disposable canary slug, otherwise-mergeable PR identity, and explicitly unauthorized repository/resource identifiers. Without the complete set, skip before client construction. Encode positive machine-branch/Draft/reviewer assertions and causal live negative probes for default-ref update, merge, ruleset access/mutation, unauthorized repository access, and repository/environment secret access, with bounded provider classifications and pre/post remote-state equality. Approve and ready-for-review are not claimed as platform denials: their evidence is isolated negative transport/static proof that the production adapter, CLI, and workflow cannot express review submission, GraphQL, or Draft transition. The live test must never perform cleanup; it emits a bounded cleanup manifest for separately authorized human/admin action.</action>
  <acceptance_criteria>
    - Default collection and execution require no network and skip safely.
    - Partial environment configuration fails closed or skips before token use.
    - Live result schema contains only stable IDs, safe classifications, pre/post SHAs, and cleanup targets.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_live_canary.py</automated></verify>
  <done>The live test boundary is explicit, causal, and has no automation cleanup authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub HTTP → provider models | Provider status, JSON, pagination, and IDs are untrusted. |
| Remote branch/PR state → recovery decision | Remote state cannot be selected by title or first match. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-02 | Spoofing | Arbitrary repository/ref response is accepted | medium | critical | high | mitigate | Bound catalog fixtures and exact response cross-checks | Live provider drift | Block release |
| T-04-07 | Tampering | Human head is overwritten after crash | medium | critical | high | mitigate | Full crash/divergence matrix and force-false assertion | Out-of-band admin mutation | Manual intervention |
| T-04-08 | Spoofing/DoS | Duplicate PR or marker spoof selects wrong Draft | medium | high | high | mitigate | Paginated exact head/base plus one-marker match | Provider eventual consistency | Bounded retry then block |
| T-04-06 | Elevation | Canary cleanup uses publishing identity | low | critical | high | mitigate | No cleanup API in test; bounded manifest only | Human/admin error | Blocking human gate |
</threat_model>

## Artifacts this phase produces

- `tests/fixtures/github_publish/repository.json`
- `tests/fixtures/github_publish/ref.json`
- `tests/fixtures/github_publish/commit.json`
- `tests/fixtures/github_publish/blob.json`
- `tests/fixtures/github_publish/tree.json`
- `tests/fixtures/github_publish/pull_draft.json`
- `tests/fixtures/github_publish/pulls_page.json`
- `tests/fixtures/github_publish/reviewers.json`
- `tests/fixtures/github_publish/error_matrix.json`
- `tests/test_github_publish_adapter.py`; fixture helper `github_publish_routes`.
- `tests/test_publication_recovery.py`; crash seam enum `PUBLICATION_CRASH_POINTS`.
- `tests/test_publication_live_canary.py`; env parser `load_live_canary_config`, result field `cleanup_manifest`.
- No production symbols or CLI flags are created.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Create/update one traceable Draft safely | COVERED by transport/recovery contract. |
| REQ | PUB-01, PUB-04, PUB-05 | COVERED by Wave 0 tests. |
| RESEARCH | Provider, crash, ambiguity, live matrices | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>All three test modules collect; live module skips with no configuration and creates no client.</verification>
<success_criteria>Every remote provider and recovery failure mode has deterministic fixture/test coverage before implementation.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-02-SUMMARY.md` when done.</output>
