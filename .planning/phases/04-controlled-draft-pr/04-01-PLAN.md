---
phase: 04-controlled-draft-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_publication_domain.py
  - tests/test_publication_security.py
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, SEC-02]
must_haves:
  truths:
    - "Wave 0 collects executable tests for canonical admission, deterministic publication identity/body, and every forbidden publication surface before production implementation receives credit."
    - "The tests require Phase 3 authority revalidation before token, transport, publication-state, or filesystem mutation."
    - "Recovery distinguishes stable publication identity from revision-specific package evidence, accepts verified prior machine lineage, and rejects malformed, cross-catalog, or inconsistent lineage."
  artifacts:
    - path: tests/test_publication_domain.py
      provides: "Admission, identity, marker, body, and record contract matrix."
    - path: tests/test_publication_security.py
      provides: "Negative-capability, secret/log, and candidate-to-shell static matrix."
  key_links:
    - from: tests/test_publication_domain.py
      to: src/skillscout/domain/publication.py
      via: "Imports the exact Phase 4 contracts downstream plans must implement."
    - from: tests/test_publication_security.py
      to: src/skillscout/adapters/github_publish.py
      via: "AST and transport assertions constrain the production surface."
  prohibitions:
    - "No test may mint a token, call live GitHub, execute candidate content, or pass because the target module is absent."
    - "No fixture may contain a real credential or private key."
---

<objective>
Establish the domain and security side of the Phase 4 Wave 0 executable specification.

Purpose: Make the token-before-admission and forbidden-capability boundaries fail visibly before implementation.
Output: Collectable pytest modules with named tests and strict downstream imports.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-controlled-draft-pr/04-RESEARCH.md
@.planning/phases/04-controlled-draft-pr/04-PATTERNS.md
@src/skillscout/domain/skill_artifacts.py
@src/skillscout/domain/qualification.py
@src/skillscout/domain/validation.py
@src/skillscout/domain/review.py
@tests/test_cli_security.py
@tests/test_side_effect_policy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-01-01: Specify canonical publication admission and rendering</name>
  <files>tests/test_publication_domain.py</files>
  <read_first>
    - `04-RESEARCH.md` Patterns 1–2 and PUB-02 body requirements.
    - `src/skillscout/domain/skill_artifacts.py` exact frozen package/manifest contracts.
    - `src/skillscout/domain/review.py` terminal eligibility and attestation contracts.
  </read_first>
  <behavior>
    - One valid completed Phase 3 projection produces deterministic catalog root, branch, publication key, desired revision, marker, title, and complete PR body.
    - One-at-a-time mutation of canonical artifact bytes, digest binding, eligibility, validation error count, review evidence, path, mode, size, repository identity, or reviewer grammar rejects before any dependency factory is touched.
    - Marker/body parsing rejects duplicates, malformed or spoofed stable identity, cross-catalog identity, inconsistent machine lineage, unknown schema, oversized content, and missing human-review warning.
    - A later admitted package keeps the same stable publication identity while its desired revision/package digest changes; a verified prior marker and machine parent lineage remain recoverable rather than being treated as spoofing.
  </behavior>
  <action>Create strict tests that load the planned public symbols listed below inside test fixtures/functions so Wave 0 collection succeeds while ordinary execution fails if production symbols are absent or incorrect. Use a spy factory bundle whose protected-config, token, transport, publication-state, and filesystem methods raise immediately; every rejected candidate evidence bundle must leave all counters at zero. Assert `CandidatePublicationEvidenceV1` contains only Phase 2/3 candidate-bound provenance/digests/files and cannot contain catalog, reviewer, intent, or authority-dependent admission fields. Assert `PublicationIntentV1` and `PublicationAdmissionV1` require explicit catalog plus reviewer authority, their digests change when authority changes, and candidate evidence digests do not. Define the marker contract as stable publication identity (`catalog_repository_id`, `catalog_full_name`, `base_branch`, `head_branch`, `stable_slug`, `publication_key`) plus revision evidence (`desired_revision`, `package_digest`) and verified machine lineage (`machine_commit_sha`, `machine_parent_sha`, `prior_marker_digest`). Test a legitimate later package whose revision evidence differs while stable identity and verified lineage agree, alongside malformed, spoofed, cross-catalog, and inconsistent-lineage cases. Name parameterized cases for every Phase 3 artifact relationship and every PUB-02 body field. Do not use xfail, skip, or a fallback implementation.</action>
  <acceptance_criteria>
    - `pytest --collect-only` reports named admission, mutation, marker-spoof, deterministic-body, and reviewer-grammar cases.
    - Tests contain explicit zero-call assertions for token, network, state, and filesystem dependencies.
    - Golden body assertions cover source repository, exact SHA, license, workflow fingerprint, evidence summary, qualification, format/safety validation, independent review, and human-review notice.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_publication_domain.py</automated></verify>
  <done>The complete domain contract is executable and collectable before production code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>04-01-02: Specify forbidden publication and workflow surfaces</name>
  <files>tests/test_publication_security.py</files>
  <read_first>
    - `04-RESEARCH.md` Pattern 7, known threat patterns, and required forbidden-route matrix.
    - `tests/test_cli_security.py` AST and surface snapshot conventions.
    - `tests/test_side_effect_policy.py` effect-scope assertions.
  </read_first>
  <behavior>
    - Production publication code exposes no arbitrary repository/ref/path/method, PUT, DELETE, GraphQL, merge, approval review, ready, auto-merge, update-branch, ruleset, administration, or default-ref mutation.
    - Logs/public results cannot contain token, private key, authorization header, raw response body, exception text, or candidate prose.
    - Workflow tests require exact full-SHA action pins, protected environment, minimal permissions, fixed shell command, and no candidate expression interpolation.
  </behavior>
  <action>Create AST/YAML-text and public-output tests with explicit allowlists. Resolve production/workflow paths and imports inside test functions so absent files fail behavior execution while collection succeeds in Wave 0. Assert `EffectScope.REMOTE_WRITE` is isolated to the planned publish adapter and never added to `GitHubReadClient` or dry-run composition. Avoid brittle negative string searches where a positive AST call/route allowlist is more exact.</action>
  <acceptance_criteria>
    - Named tests cover every forbidden surface in PUB-03 and SEC-02.
    - Tests prove the default branch is rejected independently of route text.
    - Canary secrets placed in env, exception, provider body, and candidate fields are absent from stdout, stderr, records, markers, and PR body.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_publication_security.py</automated></verify>
  <done>The negative-capability and secret-handling contract is collectable and cannot silently weaken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 3 durable bytes → publication admission | Durable bytes remain untrusted until strict canonical cross-validation. |
| Publication source/config → Python/GitHub/Actions sinks | Candidate and provider data must never become authority or shell source. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-01 | Elevation/Tampering | Token or network factory is invoked before admission | medium | critical | high | mitigate | Zero-call spy matrix for every invalid bundle | Defect outside instrumented factories | Block implementation credit |
| T-04-04 | Elevation | Arbitrary method/path or merge/approve/ready/admin surface appears | medium | critical | high | mitigate | Positive AST/route allowlist and effect-scope isolation | Provider permissions remain coarse | Block release |
| T-04-09 | Injection/Disclosure | Candidate data becomes shell/log/secret output | medium | high | high | mitigate | Workflow/public-output canaries and closed keys | GitHub platform masking is not relied upon | Block release |
</threat_model>

## Artifacts this phase produces

- `tests/test_publication_domain.py`
- Test helpers: `valid_publication_bundle`, `RejectingDependencyFactories`, `mutate_canonical_artifact`.
- Required production symbols encoded by tests: `CandidatePublicationEvidenceV1`, `CatalogAuthorityV1`, `ReviewerTargetsV1`, `PublicationIntentV1`, `PublicationMarkerV1`, `PublicationAdmissionV1`, `PublicationRecordV1`, `PublicationResultV1`, `admit_phase3_candidate`, `derive_publication_intent`, `bind_publication_admission`, `render_pull_request_title`, `render_pull_request_body`, `parse_publication_marker`.
- `tests/test_publication_security.py`
- No CLI flags or production fields are created by this plan.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Controlled Draft PR only | COVERED by executable admission/negative-capability contract. |
| REQ | PUB-01, PUB-02, PUB-03, SEC-02 | COVERED by Wave 0 tests; implementation follows in later plans. |
| RESEARCH | Admission matrix, deterministic marker/body, forbidden routes, secret/shell controls | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>
Both Wave 0 modules collect under the locked environment; ordinary execution is expected to remain red until Plans 03, 04, 06, and 09 implement the named contracts.
</verification>

<success_criteria>
- Every critical Phase 4 admission and negative-capability rule has a named failing test before implementation.
- No live credential or network access is required.
</success_criteria>

<output>Create `.planning/phases/04-controlled-draft-pr/04-01-SUMMARY.md` when done.</output>
