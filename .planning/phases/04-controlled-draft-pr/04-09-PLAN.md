---
phase: 04-controlled-draft-pr
plan: 09
type: execute
wave: 6
depends_on: [04-06, 04-08]
files_modified:
  - .github/workflows/publish-candidate.yml
  - tests/test_publication_security.py
  - tests/test_publication_live_canary.py
autonomous: true
requirements: [PUB-01, PUB-03, PUB-04, SEC-02]
must_haves:
  truths:
    - "The production workflow uses the exact human-approved action SHA set, a protected environment, minimal permissions, and a short-lived catalog-scoped installation token."
    - "Unprivileged admission completes before protected secret release and token minting."
    - "The workflow provides no candidate-to-shell interpolation, approve/ready/other forbidden publication surface, or automated canary cleanup."
    - "The unprivileged job emits only candidate-bound locators/digests; the protected job revalidates them and locally derives authority-bound intent/admission before the App-token action can run."
  artifacts:
    - path: .github/workflows/publish-candidate.yml
      provides: "Protected manual controlled publication workflow."
    - path: tests/test_publication_live_canary.py
      provides: "Production-identity positive/negative canary runner."
  key_links:
    - from: admission job
      to: protected publish job
      via: "Exactly three canonical candidate locators plus seven candidate digests; no catalog/reviewer/intent/admission value crosses."
    - from: protected catalog/reviewer authority
      to: token action
      via: "Fixed pre-token candidate equality, team rejection, and local intent/admission derivation gate."
    - from: approved action SHA set
      to: checkout and token steps
      via: "Exact immutable `uses:` references."
  prohibitions:
    - "No schedule, pull_request trigger, broad GITHUB_TOKEN write, mutable action tag, candidate expression in run, third-party action after token mint, cache as state, merge/approve/ready/admin, or automated cleanup."
---

<objective>
Implement the protected Actions publication workflow and executable live-canary probes.

Purpose: Enforce least privilege and late secret release around the already-closed CLI.
Output: Pinned workflow plus green static/offline canary tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-06-SUMMARY.md
@.planning/phases/04-controlled-draft-pr/04-08-SUMMARY.md
@.planning/phases/04-controlled-draft-pr/04-RESEARCH.md
@src/skillscout/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-09-01: Create exact protected publication workflow</name>
  <files>.github/workflows/publish-candidate.yml, tests/test_publication_security.py</files>
  <read_first>
    - `04-08-SUMMARY.md` exact approved action identities; stop if rejected/missing/mismatched.
    - `04-RESEARCH.md` workflow permission/environment shape.
    - `tests/test_publication_security.py` Wave 0 workflow contract.
  </read_first>
  <action>Create a `workflow_dispatch`-only workflow with top-level `permissions: contents: read`. Pin checkout and App-token creation to their exact Gate A4-approved full SHAs. Use an unprivileged admission job with no protected environment, catalog/reviewer variables, or secrets to invoke the Plan 06 `verify-publication-admission` command without comparison. Expose exactly `candidate_descriptor_locator`, `phase2_state_locator`, `phase3_state_locator`, `candidate_descriptor_digest`, `phase2_chain_digest`, `terminal_summary_digest`, `package_digest`, `manifest_digest`, `validation_report_digest`, and `review_attestation_digest` as job outputs; prohibit `PublicationIntentV1`, catalog/reviewer fields, `publication_intent_digest`, and `admission_digest` from that job and output boundary. Make the publish job depend on admission, use environment `skillscout-catalog-publish`, and retain only contents-read GITHUB_TOKEN permission. Before the token action, map those exact candidate outputs to `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE2_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE3_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_DIGEST`, `SKILLSCOUT_EXPECTED_PHASE2_CHAIN_DIGEST`, `SKILLSCOUT_EXPECTED_TERMINAL_SUMMARY_DIGEST`, `SKILLSCOUT_EXPECTED_PACKAGE_DIGEST`, `SKILLSCOUT_EXPECTED_MANIFEST_DIGEST`, `SKILLSCOUT_EXPECTED_VALIDATION_REPORT_DIGEST`, and `SKILLSCOUT_EXPECTED_REVIEW_ATTESTATION_DIGEST`. Invoke exactly `.tools/uv-0.11.29/bin/uv run --locked python -m skillscout.cli verify-publication-admission --candidate "$CANDIDATE_LOCATOR" --phase2-state "$PHASE2_STATE_LOCATOR" --phase3-state "$PHASE3_STATE_LOCATOR" --compare-env`; inside this protected pre-token step, re-open/revalidate all candidate artifacts under the fixed locator grammar, compare all ten candidate fields, load protected catalog repository ID/full name/base branch plus individual-reviewer authority, reject non-empty `SKILLSCOUT_CATALOG_TEAM_REVIEWERS`, construct/canonicalize `PublicationIntentV1`, and derive the authority-bound `admission_digest` locally. Keep the resulting intent/admission digests job-local for the subsequent publish invocation; never compare them to an unprivileged value or expose them as job outputs. The step has no token, network, state write, candidate-derived shell, or mutable handoff; any missing/substituted byte, locator, candidate field/digest, invalid authority, or non-empty team configuration exits before the token action. Only after all checks mint the installation token narrowed to configured catalog owner/repository and Contents write + Pull requests write. Invoke the fixed publish CLI using fixed shell text and environment values, require it to reproduce the protected-local intent/admission digests and verify the first remote catalog observation's default branch equals the protected base branch before any write, and never interpolate candidate fields in `run:`. Ensure no third-party action follows token minting and no cache/artifact is canonical state.</action>
  <acceptance_criteria>
    - Workflow parser tests assert exact trigger, permissions, environment, dependencies, and both full approved SHAs.
    - Protected catalog/reviewer variables are absent from the unprivileged job; App credentials appear only in the token step after protected admission.
    - Workflow-order tests substitute each candidate artifact, locator, canonical output field, and digest and prove token action plus all network/state-capable publication steps remain unreachable.
    - Static/mutation tests reject any unprivileged catalog/reviewer/intent/admission field, any cross-job `publication_intent_digest`/`admission_digest` comparison, or any intent construction before the protected job.
    - Workflow-order tests inject non-empty `SKILLSCOUT_CATALOG_TEAM_REVIEWERS` and prove closed manual-review failure occurs before token minting or network; the production workflow otherwise leaves it absent/empty.
    - No candidate-derived expression occurs in any shell block.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_security.py -k workflow</automated></verify>
  <done>Production credential exposure is late, protected, scoped, and immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>04-09-02: Implement causal positive and negative live probes</name>
  <files>tests/test_publication_live_canary.py, tests/test_publication_security.py</files>
  <read_first>
    - `04-VALIDATION.md` live canary behavior.
    - `src/skillscout/adapters/github_publish.py` production allowed operations.
  </read_first>
  <action>Complete the opt-in live runner so the production installation identity proves machine branch/Draft/reviewer success and independently attempts default-ref update, merge, ruleset read/mutation, unauthorized repository/resource access, and repository/environment secret access through an isolated canary-only test client that is never imported by production. Require independently supplied active-ruleset/bypass evidence digest, an otherwise mergeable canary for causal merge classification, and explicit unauthorized target identities. Record bounded provider classifications and unchanged pre/post default, ruleset, unauthorized target, and secret-resource state. Separately, add isolated negative transport/static tests proving the production adapter has no review-submission/approval route and no GraphQL/ready-for-review transition; the CLI/workflow cannot express either action. Document the residual platform risk: the Pull requests write token may be technically capable of ready-for-review outside SkillScout, so human-only Draft transition is enforced by closed production capability plus code/workflow review, while default write/merge/admin/unauthorized access retain live platform-denial evidence. Emit cleanup manifest only.</action>
  <acceptance_criteria>
    - Offline default remains skipped before network.
    - Live result distinguishes ruleset denial from Draft/check/conflict/validation denial.
    - Same installation identity is attested for every live positive/negative platform probe; approve and ready are separately blocked by production-surface transport/static evidence.
    - No production code gains forbidden canary methods or cleanup authority.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_live_canary.py tests/test_publication_security.py -x</automated></verify>
  <done>The canary can causally test platform enforcement without widening production capability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Unprivileged admission job → protected publish job | Only three canonical Phase 2/3 locators and seven candidate-bound digests cross; protected authority and authority-dependent digests do not. |
| Protected authority + candidate handoff → token action | Candidate evidence is re-read/compared and intent/admission are derived locally before credential release. |
| GitHub App token → catalog/ruleset | Token is coarse; adapter and platform rules are independent controls. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-05 | Elevation | Token minted before admission or with broad repository/permissions | medium | critical | high | mitigate | Job split, protected env, repository/permission narrowing | Environment misconfiguration | Live gate |
| T-04-04 | Elevation | Merge/default/approve/ready/admin capability | medium | critical | high | mitigate | Live default/merge/admin probes plus closed production approve/ready route allowlist | App token may permit ready outside SkillScout | Block on any production surface; residual recorded |
| T-04-09 | Injection | Candidate-to-shell expression | medium | high | high | mitigate | Fixed run block and static expression test | Workflow editor regression | Block release |
| T-04-13 | Confused deputy | Unsupported team reviewer configuration crosses the protected boundary | medium | high | high | mitigate | Protected pre-token authority parse rejects any non-empty team input before token/network | Protected environment editor error | Block release |
| T-04-14 | Tampering | Cross-job equality falsely treats unprivileged intent/admission digest as authority proof | medium | critical | high | mitigate | Candidate-only output allowlist; protected-local intent/admission construction and mutation test | Workflow editor regression | Block release |
| T-04-10 | Disclosure | Secret leakage | medium | high | high | mitigate | Late secret scope, closed logs, secret canaries | Runner compromise | Accepted platform residual |
| T-04-SC | Tampering | Action identity substitution | medium | critical | high | mitigate | Exact human-approved SHA assertion | Approved commit compromise | Human gate residual |
| T-04-06 | Elevation | Live-canary cleanup authority | low | critical | high | mitigate | Cleanup manifest only, separate human/admin | Human cleanup error | Plan 10 |
</threat_model>

## Artifacts this phase produces

- `.github/workflows/publish-candidate.yml`.
- Workflow inputs: `candidate_descriptor`, `phase2_state_locator`, `phase3_state_locator`, `publication_state_locator`, `live_canary`.
- Jobs: `admit`, `publish`; environment `skillscout-catalog-publish`.
- Finalized `load_live_canary_config` and `LiveCanaryEvidenceV1` in `tests/test_publication_live_canary.py`.
- Admission job output fields: `candidate_descriptor_locator`, `phase2_state_locator`, `phase3_state_locator`, `candidate_descriptor_digest`, `phase2_chain_digest`, `terminal_summary_digest`, `package_digest`, `manifest_digest`, `validation_report_digest`, `review_attestation_digest`.
- Protected pre-token local fields: `publication_intent_digest`, `admission_digest`; these are never admission-job outputs or cross-job equality inputs.
- Live evidence fields: `installation_id`, `catalog_repository_id`, `ruleset_evidence_digest`, `positive_pull_number`, `positive_draft`, `requested_reviewers`, `default_sha_before`, `default_sha_after`, `default_write_denial`, `merge_denial`, `ruleset_read_denial`, `ruleset_mutation_denial`, `unauthorized_repository_denial`, `secret_resource_denial`, `remote_state_unchanged`, `cleanup_manifest`.
- Production-surface evidence fields: `approval_route_absent`, `ready_transition_absent`, `graphql_transport_absent`, `workflow_invocation_absent`, `residual_platform_ready_risk`.
- No production CLI flag or forbidden adapter method.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Platform-constrained Draft and human review | COVERED by workflow/canary. |
| REQ | PUB-01, PUB-03, PUB-04, SEC-02 | COVERED. |
| RESEARCH | Protected job split, pin, minimal permissions, two-layer negative proof | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run offline live/security suites and confirm exact Gate A4 SHA appears once in workflow.</verification>
<success_criteria>Workflow static controls are green and live probes are ready for the non-auto-approvable Plan 10 gate.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-09-SUMMARY.md` when done.</output>
