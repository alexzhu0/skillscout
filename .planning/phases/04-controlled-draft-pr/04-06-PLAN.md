---
phase: 04-controlled-draft-pr
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - src/skillscout/bootstrap.py
  - src/skillscout/cli.py
  - tests/test_publication_security.py
  - tests/test_cli_security.py
  - tests/test_cli_validate_skill.py
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-05, SEC-02]
must_haves:
  truths:
    - "The publish CLI revalidates a completed Phase 3 candidate and derives intent before constructing the token-backed client."
    - "The credential-free verifier emits only canonical Phase 2/3 candidate locators and digests; it cannot load protected authority or derive intent/admission digests."
    - "Comparison mode runs inside the protected job, revalidates candidate evidence, then loads protected authority and derives intent/admission before token minting."
    - "Catalog/reviewer authority comes only from strict protected configuration; candidate input cannot choose remote authority."
    - "Public output is bounded JSON and cannot expose credentials, candidate prose, provider bodies, or arbitrary exceptions."
  artifacts:
    - path: src/skillscout/bootstrap.py
      provides: "Strict publication runtime config and lazy composition."
    - path: src/skillscout/cli.py
      provides: "Fixed `verify-publication-admission` and `publish-candidate` commands with safe projections."
  key_links:
    - from: DescriptorAnchoredCompletedCandidateProjector
      to: admit_phase3_candidate
      via: "Exact Phase 3 projection before publication dependencies."
    - from: CandidatePublicationEvidenceV1 + PublicationAuthorityConfig
      to: PublicationAdmissionV1
      via: "Protected pre-token intent construction after candidate handoff equality."
    - from: PublicationRuntimeConfig
      to: GitHubPublishClient
      via: "Catalog-bound lazy factory after admission."
  prohibitions:
    - "No CLI repository, branch, target path, package directory/glob, title/body, REST path, reviewer, merge, or force option."
    - "No candidate value interpolated into shell or environment configuration."
---

<objective>
Compose the admission-first publisher and expose its safe CLI.

Purpose: Provide the production entry point without widening earlier dry-run/read/Phase 3 graphs.
Output: Strict config/bootstrap, concrete admission verifier, `publish-candidate`, and public security tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-05-SUMMARY.md
@src/skillscout/application/phase3.py
@src/skillscout/adapters/state.py
@src/skillscout/bootstrap.py
@src/skillscout/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-06-01: Build strict protected publication configuration</name>
  <files>src/skillscout/bootstrap.py, tests/test_publication_security.py</files>
  <read_first>
    - `src/skillscout/bootstrap.py` Phase 3 fail-closed gate patterns.
    - `src/skillscout/domain/publication.py` catalog/reviewer models.
  </read_first>
  <action>Define strict frozen `PublicationAuthorityConfig`, `PublicationRuntimeConfig`, `load_publication_authority_config`, and `load_publication_runtime_config`. The authority loader reads protected `SKILLSCOUT_CATALOG_REPOSITORY_ID`, `SKILLSCOUT_CATALOG_FULL_NAME`, `SKILLSCOUT_CATALOG_BASE_BRANCH`, non-empty `SKILLSCOUT_CATALOG_REVIEWERS`, and `SKILLSCOUT_PUBLICATION_POLICY_VERSION` without reading `SKILLSCOUT_GITHUB_TOKEN`; parse the base branch with the closed ref grammar and reviewers into one sorted unique bounded individual-login tuple. Treat `SKILLSCOUT_CATALOG_TEAM_REVIEWERS` only as a forbidden compatibility input: it must be absent or empty; any non-whitespace value fails before token/network/client construction with a closed manual-catalog-review outcome. Require exact numeric/full-name agreement, fixed policy version, and no administration/permission override. The protected base branch makes pre-token `PublicationIntentV1` construction complete; after token minting, the first catalog observation must prove GitHub's repository default branch exactly equals it before any write. `load_publication_runtime_config` composes already-validated authority with the late `token_factory` only after protected admission succeeds. Tests must prove the unprivileged handoff path never calls either protected loader, and rejected team/every other authority error leaves token/network/state counters zero and values never enter logs/results/state.</action>
  <acceptance_criteria>
    - Missing/malformed/cross-repository config fails before client construction.
    - At least one individual human reviewer is mandatory; team configuration is rejected before token/network.
    - Unprivileged verifier execution succeeds without, and cannot observe, any protected catalog/reviewer/token environment value.
    - Token factory call count is zero for all rejected/completed-no-client paths.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_security.py -k 'config or token_order or secret'</automated></verify>
  <done>Protected catalog authority is strict and token access is structurally late.</done>
</task>

<task type="auto" tdd="true">
  <name>04-06-02: Wire completed Phase 3 projection to publication application</name>
  <files>src/skillscout/bootstrap.py, src/skillscout/cli.py, tests/test_cli_validate_skill.py</files>
  <read_first>
    - `src/skillscout/cli.py` `_run_build_candidate` and safe cleanup.
    - `src/skillscout/application/phase3.py` authority construction and completed projector.
    - `src/skillscout/application/publication.py` dependency contract.
  </read_first>
  <action>Implement `build_publication_application`, `_run_publish_candidate`, strict `PublicationEvidenceLocatorV1`, and project-owned `verify_publication_admission_handoff` in `src/skillscout/bootstrap.py`. Resolve the candidate descriptor through the existing Phase 2 source and complete Phase 3 authority, use `DescriptorAnchoredCompletedCandidateProjector` only, require an exact completed eligible projection, and derive `CandidatePublicationEvidenceV1`. Workflow locators use normalized repository-relative POSIX grammar under only `evidence/` or `state/`, maximum 255 bytes, ASCII alphanumeric/`._/-`, no empty/dot/dot-dot segment, absolute path, backslash, symlink, non-regular file, or caller-selected root. Without `--compare-env`, the verifier emits canonical compact sorted JSON containing exactly `candidate_descriptor_locator`, `phase2_state_locator`, `phase3_state_locator`, `candidate_descriptor_digest`, `phase2_chain_digest`, `terminal_summary_digest`, `package_digest`, `manifest_digest`, `validation_report_digest`, and `review_attestation_digest`; it must not call protected config loaders or construct `PublicationIntentV1`, `PublicationAdmissionV1`, `publication_intent_digest`, or `admission_digest`. In comparison mode, require those same ten fields from fixed `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE2_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE3_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_DIGEST`, `SKILLSCOUT_EXPECTED_PHASE2_CHAIN_DIGEST`, `SKILLSCOUT_EXPECTED_TERMINAL_SUMMARY_DIGEST`, `SKILLSCOUT_EXPECTED_PACKAGE_DIGEST`, `SKILLSCOUT_EXPECTED_MANIFEST_DIGEST`, `SKILLSCOUT_EXPECTED_VALIDATION_REPORT_DIGEST`, and `SKILLSCOUT_EXPECTED_REVIEW_ATTESTATION_DIGEST`; re-open all three locators, revalidate exact candidate bytes, canonicalize locators, and compare all fields. Only after equality, call `load_publication_authority_config`, reject team configuration, derive/canonicalize `PublicationIntentV1`, and bind `PublicationAdmissionV1`; comparison-mode output may expose `publication_intent_digest` and `admission_digest` only as protected-job-local values for the later publish invocation, never as unprivileged/cross-job outputs. Neither mode reads the token or constructs publication state, HTTP, or any network-capable dependency. Only the publish path, after successful protected admission, may instantiate `PublicationStateStore` and token-backed `GitHubPublishClient`; it rebinds the same candidate evidence/authority and checks the protected-job-local intent/admission digests before remote access. Close every projector/store/client. Integrity/evidence/authority/comparison failure must not fall back to mutable Phase 3 execution, local output directories, or remote calls.</action>
  <acceptance_criteria>
    - Source/Phase 3/admission failure makes zero publication state/token/network calls.
    - Every locator, artifact-byte, expected-field, and digest substitution fails comparison with zero token/network/state writes.
    - Non-comparison output is exactly the ten candidate locator/digest fields and remains byte-identical under any protected authority change.
    - Comparison mode rejects empty individual reviewers or non-empty team compatibility input before reading the token or constructing any network-capable dependency; authority changes alter only protected-local intent/admission digests.
    - Exact eligible projection reaches publication application.
    - Existing commands and dry-run/read-only effect scopes are unchanged.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_cli_validate_skill.py -k 'publish or admission_handoff'</automated></verify>
  <done>The production graph admits exact Phase 3 evidence before exposing REMOTE_WRITE.</done>
</task>

<task type="auto" tdd="true">
  <name>04-06-03: Expose closed admission-verifier and publish CLIs</name>
  <files>src/skillscout/cli.py, tests/test_cli_security.py, tests/test_publication_security.py</files>
  <read_first>
    - `src/skillscout/cli.py` `SafeArgumentParser` and public JSON behavior.
    - `tests/test_cli_security.py` argv and secret canary tests.
  </read_first>
  <action>Add token-free command `verify-publication-admission` with exactly `--candidate`, `--phase2-state`, `--phase3-state`, and optional fixed boolean `--compare-env`; route it only to `verify_publication_admission_handoff`. Without the flag it is the unprivileged candidate-handoff producer and must remain authority-blind. Its exact protected-job invocation is `.tools/uv-0.11.29/bin/uv run --locked python -m skillscout.cli verify-publication-admission --candidate "$CANDIDATE_LOCATOR" --phase2-state "$PHASE2_STATE_LOCATOR" --phase3-state "$PHASE3_STATE_LOCATOR" --compare-env`; with the flag it performs candidate equality followed by protected authority/intent/admission construction, still without token/network/state access. Add `publish-candidate` with exactly `--candidate`, `--phase2-state`, `--phase3-state`, and `--publication-state`. Emit compact sorted JSON for `draft_created`, `draft_updated`, `draft_reused`, or `manual_intervention_required`, limited to stable catalog/branch/commit/PR URL-number IDs, digests, reviewer names, and closed reason code. Reject all extra authority/mutation options, including approve/ready/merge, with fixed status-2 behavior. Map internal/provider errors to existing closed safe codes with no arbitrary text.</action>
  <acceptance_criteria>
    - Verifier help exposes exactly three locator flags plus `--compare-env`; publisher help exposes exactly four path flags and neither exposes remote authority/mutation flags.
    - Canonical unprivileged verifier output is byte-identical for identical candidate evidence regardless of protected authority; comparison failure emits one closed status-2 diagnostic with zero token/network/state calls.
    - Secret/candidate/provider canaries are absent from stdout, stderr, state, and result.
    - Existing CLI command contracts remain green.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_cli_security.py tests/test_publication_security.py tests/test_cli_validate_skill.py -x</automated></verify>
  <done>A human/operator can invoke controlled Draft publication through one safe fixed CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI paths + protected config → composition root | Paths identify local evidence; config alone owns remote authority. |
| Admission → token factory | Token must be unavailable before exact local authority succeeds. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-01 | Elevation | Token-before-admission through eager composition | medium | critical | high | mitigate | Separate token factory with call-order tests | Future refactor | Static regression blocks |
| T-04-02 | Confused deputy | CLI chooses repo/ref/path/reviewer | medium | critical | high | mitigate | No such flags; strict protected config | Environment compromise | Live gate |
| T-04-14 | Tampering | Unprivileged job fabricates or compares authority-dependent intent/admission digest | medium | critical | high | mitigate | Ten-field candidate-only handoff; protected job derives intent/admission locally after exact re-read/equality | Workflow composition regression | Plan 09 static gate |
| T-04-13 | Confused deputy | Team reviewer configuration bypasses the individual-only evidence contract | medium | high | high | mitigate | Reject non-empty compatibility input before token, client, or network construction | Protected environment editor error | Static regression blocks |
| T-04-09 | Injection | Candidate-to-shell/config interpolation | medium | high | high | mitigate | Fixed CLI argv and no candidate-derived env | Workflow regression | Plan 09 tests |
| T-04-10 | Disclosure | Secret leakage in public output | medium | high | high | mitigate | Closed projection and canary tests | Host runner logs | Secret scan |
</threat_model>

## Artifacts this phase produces

- `PublicationAuthorityConfig`, `PublicationRuntimeConfig`, `PublicationEvidenceLocatorV1`, `load_publication_authority_config`, `load_publication_runtime_config`, `verify_publication_admission_handoff`, `build_publication_application` in `src/skillscout/bootstrap.py`.
- `_run_verify_publication_admission`, `_run_publish_candidate`, `_public_publication_payload`, `verify-publication-admission`, and `publish-candidate` parser branches in `src/skillscout/cli.py`.
- Verifier CLI flags: `--candidate`, `--phase2-state`, `--phase3-state`, `--compare-env`; publisher CLI flags: `--candidate`, `--phase2-state`, `--phase3-state`, `--publication-state`.
- Unprivileged verifier canonical fields: `candidate_descriptor_locator`, `phase2_state_locator`, `phase3_state_locator`, `candidate_descriptor_digest`, `phase2_chain_digest`, `terminal_summary_digest`, `package_digest`, `manifest_digest`, `validation_report_digest`, `review_attestation_digest`.
- Environment fields: `SKILLSCOUT_CATALOG_REPOSITORY_ID`, `SKILLSCOUT_CATALOG_FULL_NAME`, `SKILLSCOUT_CATALOG_BASE_BRANCH`, `SKILLSCOUT_CATALOG_REVIEWERS`, `SKILLSCOUT_PUBLICATION_POLICY_VERSION`, `SKILLSCOUT_GITHUB_TOKEN`; forbidden compatibility input `SKILLSCOUT_CATALOG_TEAM_REVIEWERS` must be absent/empty.
- Verifier comparison environment fields: `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE2_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_PHASE3_STATE_LOCATOR`, `SKILLSCOUT_EXPECTED_CANDIDATE_DESCRIPTOR_DIGEST`, `SKILLSCOUT_EXPECTED_PHASE2_CHAIN_DIGEST`, `SKILLSCOUT_EXPECTED_TERMINAL_SUMMARY_DIGEST`, `SKILLSCOUT_EXPECTED_PACKAGE_DIGEST`, `SKILLSCOUT_EXPECTED_MANIFEST_DIGEST`, `SKILLSCOUT_EXPECTED_VALIDATION_REPORT_DIGEST`, `SKILLSCOUT_EXPECTED_REVIEW_ATTESTATION_DIGEST`.
- `publication_intent_digest` and `admission_digest` are protected-job-local comparison outputs only and never part of the unprivileged handoff.
- Public result fields: `outcome`, `catalog_repository_id`, `base_branch`, `head_branch`, `commit_sha`, `pull_number`, `pull_url`, `package_digest`, `marker_digest`, `reviewers`, `reason_code`; no team field.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Operable controlled Draft publication | COVERED. |
| REQ | PUB-01, PUB-02, PUB-03, PUB-05, SEC-02 | COVERED at composition/CLI boundary. |
| RESEARCH | Late token, strict config, fixed CLI, safe output | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run the three focused suites and then `.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_cli_dry_run.py tests/test_cli_extract_repo.py tests/test_cli_validate_skill.py`.</verification>
<success_criteria>All pre-admission failures are zero-write/zero-token and the only live entry point has a closed authority-free argv surface.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-06-SUMMARY.md` when done.</output>
