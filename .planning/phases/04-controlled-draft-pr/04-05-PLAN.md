---
phase: 04-controlled-draft-pr
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - src/skillscout/adapters/publication_state.py
  - src/skillscout/application/publication.py
  - tests/test_publication_recovery.py
autonomous: true
requirements: [PUB-01, PUB-05]
must_haves:
  truths:
    - "Publication is a reconcile-first checkpointed state machine that can recover from local-state loss."
    - "Exactly one matching open Draft with exact marker/head/base/machine ancestry is reusable; every ambiguity is manual intervention."
    - "Retries never duplicate a PR, re-notify fulfilled user/team reviewer requests, force a ref, or overwrite a human head."
    - "A later package revision reuses the stable publication identity and verified machine lineage, deletes stale owned files, and exactly verifies the resulting frozen-manifest subtree."
  artifacts:
    - path: src/skillscout/adapters/publication_state.py
      provides: "Dedicated canonical publication attempt/checkpoint/record store."
    - path: src/skillscout/application/publication.py
      provides: "Admission-first reconcile/mutate/verify orchestrator."
  key_links:
    - from: PublicationIntentV1
      to: PublicationStateStore
      via: "Complete intent digest is the local exact-reuse key."
    - from: remote marker/head/base
      to: PublicationRecordV1
      via: "Reconstruction only after exact remote verification."
  prohibitions:
    - "Never trust local PR number without remote revalidation."
    - "Never choose first/closest PR, force push, retarget, reopen, close, merge, or absorb human commits."
---

<objective>
Implement durable publication checkpoints and reconcile-first orchestration.

Purpose: Make remote writes crash-safe, idempotent, and recoverable even when local state disappears.
Output: Focused publication store, application state machine, and green recovery matrix.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-03-SUMMARY.md
@.planning/phases/04-controlled-draft-pr/04-04-SUMMARY.md
@src/skillscout/application/phase3.py
@src/skillscout/adapters/state.py
@src/skillscout/adapters/localfs.py
@tests/test_publication_recovery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-05-01: Persist canonical publication attempts and checkpoints</name>
  <files>src/skillscout/adapters/publication_state.py, tests/test_publication_recovery.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` snapshot transactions, canonical artifact validation, and poison-on-write-failure behavior.
    - `src/skillscout/adapters/localfs.py` anchored private-file rules.
  </read_first>
  <action>Create `PublicationStateStore` as a focused dedicated SQLite snapshot store, without changing Phase 1–3 tables. Persist canonical `PublicationIntentV1`, ordered checkpoint events, safe request IDs/route IDs/status classes, remote stable IDs/SHAs, and terminal `PublicationRecordV1`; never store tokens, headers, response bodies, private keys, candidate prose, or arbitrary errors. Implement `find_completed`, `find_pending`, `begin_attempt`, `append_checkpoint`, `complete`, and `close`. Verify intent/checkpoint hash continuity, exact one pending/completed row, canonical bytes, and atomic durable replacement; poison after persistence uncertainty.</action>
  <acceptance_criteria>
    - Mutation/corruption tests fail before projection or remote calls.
    - Store schema is isolated and canonical.
    - Secret canaries never enter database or side files.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_recovery.py -k state</automated></verify>
  <done>Every externally visible publication step can be durably and safely checkpointed.</done>
</task>

<task type="auto" tdd="true">
  <name>04-05-02: Reconcile exact remote ownership before mutation</name>
  <files>src/skillscout/application/publication.py, tests/test_publication_recovery.py</files>
  <read_first>
    - `04-RESEARCH.md` reconcile decision table.
    - `src/skillscout/application/phase3.py` lazy completed-first factory ordering.
    - `src/skillscout/domain/publication.py` marker and remote observation contracts.
  </read_first>
  <action>Define `PublicationDependencies`, `PublicationApplicationResult`, and `PublicationApplication`. Accept an already canonical `PublicationAdmissionV1`/intent, check exact local completed state, then construct the remote client and read catalog/default SHA, machine ref/commit, complete non-truncated owned subtree, all exact head/base PR pages, markers, current requested users/teams, completed user reviews, and the complete bounded review-request timeline before writes. Return a no-write reconstructed completion when desired revision and the exact frozen-manifest subtree are current. Match ownership on stable publication identity and verified machine lineage; allow the prior marker's desired revision/package digest to differ from the newly admitted intent. Derive reviewer receipts only from unique ordered events bound to this PR/head: active current requests and completed user reviews satisfy users; the original exact `review_requested` event for a configured team slug is the durable one-time team notification receipt even after fulfillment, deliberate removal, or assignment expansion. A removal/assignment without its prior team receipt, duplicate/conflicting event, unknown event, pagination/cap loss, mismatched PR/head/team, or reordering returns `manual_intervention_required`. Require one open Draft and consistent marker/machine trailer/parent/tree lineage; return `manual_intervention_required` for duplicate/ambiguous/non-Draft/wrong target/malformed or cross-catalog marker/markerless machine commit/inconsistent lineage/human commit/truncated tree/default change/ref conflict. A local completed row is advisory until the same remote checks pass.</action>
  <acceptance_criteria>
    - Every ambiguity row returns one bounded manual result with no mutation.
    - Local-state-loss reconstruction produces exact `PublicationRecordV1`.
    - Exact desired remote state returns with zero content/reviewer writes; local-state loss after completed user review, fulfilled/removed team request, or assignment expansion produces no repeat notification, while ambiguous event history stops.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_recovery.py -k reconcile</automated></verify>
  <done>Remote ownership is established deterministically before any mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>04-05-03: Execute and recover the atomic Draft publication cascade</name>
  <files>src/skillscout/application/publication.py, src/skillscout/adapters/publication_state.py, tests/test_publication_recovery.py</files>
  <read_first>
    - `src/skillscout/adapters/github_publish.py` exact named operations.
    - `tests/test_publication_recovery.py` Wave 0 crash matrix.
  </read_first>
  <action>Implement the ordered cascade: checkpoint reconciliation; create admitted blobs; derive null-SHA deletions from the complete owned-subtree observation for every stale path; create one base tree whose owned subtree is exactly the frozen manifest; create one deterministic commit with observed parent and verified machine-lineage trailer; create/update machine ref force-false; create Draft or update only an existing open Draft title/body; combine current requested users/teams, completed user reviews, and exact timeline-derived team receipts, request only configured identities lacking durable evidence, and verify accepted/previously-fulfilled coverage; re-read ref/commit/tree/PR/marker/current requests/completed reviews/review-request timeline/default SHA and require the exact frozen-manifest subtree before persisting the terminal record. At every crash seam, including local state loss, restart by reconciling remote truth rather than replaying blindly. Bound transient retry attempts and never retry decided ambiguity or semantic/provider permission denial.</action>
  <acceptance_criteria>
    - Complete crash/local-state-loss matrix passes with one PR, exact owned subtree, stale-file deletion, and no duplicate user/team notifications after current, completed, fulfilled, or assignment-expanded evidence.
    - Human conflict and non-Draft states remain untouched.
    - Remote success before local commit reconstructs and completes on restart.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_publication_recovery.py</automated></verify>
  <done>Publication is externally idempotent, durable, and safe across every interruption point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local checkpoints ↔ remote GitHub | Neither side alone authorizes recovery after interruption. |
| Remote branch/PR marker → machine ownership | Exact marker, ancestry, head, base, and Draft state must agree. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-07 | Tampering/Repudiation | Human-head overwrite | medium | critical | high | mitigate | Exact trailer/parent/tree and force=false | Human uses same machine grammar | Marker+ancestry then manual |
| T-04-08 | Spoofing/DoS | PR ambiguity or marker spoof | medium | high | high | mitigate | Full pagination and exactly one cross-bound match | Eventual consistency | Bounded reread then block |
| T-04-11 | Repudiation | Local record claims success after uncertain write | medium | high | high | mitigate | Remote terminal revalidation and durable checkpoint ordering | Provider outage | Remains pending |
| T-04-12 | DoS | Reviewer notification storm | medium | medium | medium | mitigate | Read/diff/request/verify missing set only | Reviewer state changes concurrently | Bounded retry |
</threat_model>

## Artifacts this phase produces

- `src/skillscout/adapters/publication_state.py`
- Class/methods: `PublicationStateStore.find_completed`, `find_pending`, `begin_attempt`, `append_checkpoint`, `complete`, `close`.
- State fields: `intent_digest`, `checkpoint_no`, `checkpoint_kind`, `previous_checkpoint_digest`, `observed_base_sha`, `observed_head_sha`, `commit_sha`, `pull_number`, `pull_node_id`, `marker_digest`, `actual_reviewers`, `request_id`, `route_id`, `status_class`.
- `src/skillscout/application/publication.py`
- Symbols: `PublicationDependencies`, `PublicationApplicationResult`, `PublicationApplication`, `PublicationCrashPoint`.
- No CLI flags or workflow files.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Create/update one traceable Draft | COVERED end to end at application layer. |
| REQ | PUB-01, PUB-05 | COVERED by exact recovery/idempotency. |
| RESEARCH | Patterns 3, 4, 5, 6; crash/ambiguity matrices | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run the complete recovery suite and the domain/adapter regressions.</verification>
<success_criteria>Every restart converges to one verified Draft or one bounded manual-intervention result without overwriting human work.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-05-SUMMARY.md` when done.</output>
