---
phase: 04-controlled-draft-pr
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/skillscout/adapters/github_publish.py
  - tests/fixtures/github_publish/repository.json
  - tests/fixtures/github_publish/ref.json
  - tests/fixtures/github_publish/commit.json
  - tests/fixtures/github_publish/blob.json
  - tests/fixtures/github_publish/tree.json
  - tests/fixtures/github_publish/pull_draft.json
  - tests/fixtures/github_publish/pulls_page.json
  - tests/fixtures/github_publish/reviewers.json
  - tests/fixtures/github_publish/error_matrix.json
  - tests/test_github_publish_adapter.py
  - tests/test_publication_security.py
autonomous: true
requirements: [PUB-01, PUB-03, PUB-05, SEC-02]
must_haves:
  truths:
    - "One catalog-bound REMOTE_WRITE adapter exposes only named Git Data, ref, Draft PR, and reviewer operations."
    - "Every request derives its repository, ref, path, method, and body internally from strict domain inputs."
    - "The adapter cannot merge, approve, mark ready, enable auto-merge, mutate rulesets, or write the default ref."
    - "Read-only reconciliation can completely enumerate the owned slug subtree, completed user reviews, and exact user/team review-request timeline without exposing review submission."
  artifacts:
    - path: src/skillscout/adapters/github_publish.py
      provides: "Closed bounded GitHub REST publisher."
  key_links:
    - from: PublicationIntentV1
      to: GitHubPublishClient
      via: "Constructor-bound catalog and per-operation strict identifiers."
    - from: Frozen publication files
      to: Git blobs/tree/commit
      via: "Exact bytes and one fast-forward ref move."
  prohibitions:
    - "No public generic request method, arbitrary URL/path/repository/ref, GraphQL, PUT, DELETE, force=true, or response/body logging."
    - "Do not modify GitHubReadClient or its REMOTE_READ declaration."
---

<objective>
Implement the separate closed GitHub publication adapter.

Purpose: Make the smallest truthful remote-write capability required for Draft publication.
Output: `GitHubPublishClient` with full offline transport/security coverage.

Scope note: The adapter and finalized synthetic fixture corpus remain in one bounded transport plan because every fixture is consumed by the exact-route tests for this single class. The two tasks partition read/reconcile from write operations, and the verification commands exercise only these coupled files.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-controlled-draft-pr/04-02-SUMMARY.md
@.planning/phases/04-controlled-draft-pr/04-03-SUMMARY.md
@src/skillscout/domain/publication.py
@src/skillscout/adapters/github.py
@tests/recorded_transport.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>04-04-01: Implement bounded read/reconcile operations</name>
  <files>src/skillscout/adapters/github_publish.py, tests/fixtures/github_publish/repository.json, tests/fixtures/github_publish/ref.json, tests/fixtures/github_publish/commit.json, tests/fixtures/github_publish/pulls_page.json, tests/fixtures/github_publish/reviewers.json, tests/fixtures/github_publish/error_matrix.json, tests/test_github_publish_adapter.py</files>
  <read_first>
    - `src/skillscout/adapters/github.py` client/header/cap/safe-failure pattern.
    - `src/skillscout/domain/publication.py` strict observation contracts.
    - `tests/test_github_publish_adapter.py` Wave 0 route matrix.
  </read_first>
  <action>Create `GitHubPublishClient` bound at construction to one `CatalogAuthorityV1` and one token. Declare `EffectScope.REMOTE_WRITE`. Implement only `get_catalog`, `get_ref`, `get_commit`, catalog-bound bounded `get_tree`, `list_open_pulls`, `get_requested_reviewers`, read-only `get_completed_reviews`, and read-only `get_review_request_timeline`; internally derive exact catalog routes, consume all bounded pagination, capture request ID/rate facts, reject redirects, wrong repository/default/head/base IDs, malformed/oversized/wrong-type bodies, and return project-owned strict observations. `get_tree` must reject GitHub's `truncated=true`, cap entries/pages/bytes, normalize and confine paths to the derived `skills/{stable_slug}/` subtree, and never accept a caller-supplied root. Completed reviews return only bounded reviewer identity, review ID, commit SHA, and terminal state. Timeline observations accept only review-requested/review-request-removed/assignment events with unique ordered event IDs, timestamp, requested user login or requested team slug, and bounded actor identity; they preserve team slugs after GitHub removes the current request. There is no method for submitting/approving a review or changing Draft state. Classify transient/rate/provider failures with closed `SafeFailure` codes and never persist/echo headers, body, token, or exception text.</action>
  <acceptance_criteria>
    - Every read method is catalog-bound and strict.
    - PR/review pagination and owned-tree enumeration complete before selection or mutation; truncated trees fail closed.
    - Error matrix passes without leaking provider content.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_github_publish_adapter.py -k 'catalog or reconcile or provider'</automated></verify>
  <done>Remote reconciliation observations are bounded, strict, and repository-bound.</done>
</task>

<task type="auto" tdd="true">
  <name>04-04-02: Implement exact Git Data and Draft-only mutations</name>
  <files>src/skillscout/adapters/github_publish.py, tests/fixtures/github_publish/ref.json, tests/fixtures/github_publish/commit.json, tests/fixtures/github_publish/blob.json, tests/fixtures/github_publish/tree.json, tests/fixtures/github_publish/pull_draft.json, tests/fixtures/github_publish/reviewers.json, tests/test_github_publish_adapter.py, tests/test_publication_security.py</files>
  <read_first>
    - `04-RESEARCH.md` Patterns 4–5 and stale-file deletion rule.
    - `tests/test_publication_security.py` forbidden-surface contract.
  </read_first>
  <action>Implement only `create_blob`, `create_tree`, `create_commit`, `create_machine_ref`, `update_machine_ref`, `create_draft_pull`, `update_draft_pull`, and `request_reviewers`. Blob input is admitted bytes; compare the complete non-truncated owned-subtree observation with the admitted frozen manifest, preserve no unlisted owned file, and derive explicit null-SHA deletions for every prior owned path absent from the new manifest. Reject extra paths outside the exact owned root and verify the planned tree is exactly the frozen-manifest subtree after applying additions/deletions. Commit has one observed parent and deterministic machine lineage trailer; ref creation/update accepts only the derived machine ref and always sends false for force; PR create fixes Draft true and maintainer modification false; PR update accepts title/body only after strict open-Draft observation; reviewer request accepts only configured identities missing after reconciliation of current requests, completed user reviews, and exact team-slug timeline receipts. Use POST/PATCH only. Add AST and transport tests proving review submission/approval, ready-for-review/GraphQL, and the entire forbidden surface are absent.</action>
  <acceptance_criteria>
    - Exact method/path/body assertions pass for every mutation.
    - Default ref and non-derived refs fail before HTTP.
    - Static tests prove no merge/review approval/ready/auto-merge/ruleset/admin/generic request/PUT/DELETE/GraphQL surface.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_github_publish_adapter.py tests/test_publication_security.py -x</automated></verify>
  <done>The production adapter can only publish one coherent Draft snapshot and request humans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Strict publication intent → HTTP | Named methods are the only serialization boundary. |
| GitHub response → remote observations | Provider content is bounded and strictly parsed. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-02 | Confused deputy | Arbitrary repo/ref/path/method | medium | critical | high | mitigate | Constructor-bound catalog and named route derivation | Defect in path helper | Block release |
| T-04-04 | Elevation | Merge/approve/ready/admin/default write | medium | critical | high | mitigate | Closed operations plus AST/transport allowlist | Stolen token outside adapter | Ruleset/live canary |
| T-04-07 | Tampering | Human head overwritten | medium | critical | high | mitigate | Observed parent and force=false only | Admin force update | Manual intervention |
| T-04-10 | Disclosure | Token/provider error leaks | medium | high | high | mitigate | Closed projections; no raw headers/body/exception | HTTP library internals | Secret scan |
</threat_model>

## Artifacts this phase produces

- `src/skillscout/adapters/github_publish.py`
- Class: `GitHubPublishClient`.
- Methods: `get_catalog`, `get_ref`, `get_commit`, `get_tree`, `list_open_pulls`, `get_requested_reviewers`, `get_completed_reviews`, `get_review_request_timeline`, `create_blob`, `create_tree`, `create_commit`, `create_machine_ref`, `update_machine_ref`, `create_draft_pull`, `update_draft_pull`, `request_reviewers`, `close`.
- Provider models remain private; public returns use `CatalogRepositoryObservationV1`, `MachineRefObservationV1`, `OwnedSubtreeObservationV1`, `PullRequestObservationV1`, `ReviewerObservationV1`, `CompletedReviewObservationV1`, and `ReviewRequestReceiptObservationV1`.
- Existing fixture files are finalized; no new CLI flags or config fields.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Controlled catalog machine branch/Draft/review request | COVERED. |
| REQ | PUB-01, PUB-03, PUB-05, SEC-02 | COVERED by adapter surface. |
| RESEARCH | Separate adapter; Git Data; force false; provider matrix | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run adapter and security suites under the locked environment.</verification>
<success_criteria>All intended requests succeed offline and no forbidden request can be expressed through production code.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-04-SUMMARY.md` when done.</output>
