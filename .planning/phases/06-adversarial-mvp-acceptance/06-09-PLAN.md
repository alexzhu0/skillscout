---
phase: 06-adversarial-mvp-acceptance
plan: "09"
type: execute
wave: 7
depends_on: [06-08]
files_modified:
  - tests/test_phase6_acceptance.py
autonomous: false
requirements: [TEST-03]
must_haves:
  truths:
    - "D-21 changed source requires a new exact semantic authority and explicit human-approved prior-lineage binding."
    - "Repository/path/title/content similarity never grants update identity."
    - "Old/new repository ID, SHA, WorkflowSpec authority, package identity, and lineage remain reconstructible."
    - "Live changed-source credential use is authorized only for the exact selected old/new pair."
  artifacts:
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Approved changed-source reevaluation job"}
  key_links:
    - {from: PriorLineageApprovalRecordV1, to: SQLiteStateStore.persist_prior_lineage_binding, via: "exact binding digest and prior-chain verification"}
---

<objective>
Approve and execute one exact changed-source reevaluation while preserving explicit old-to-new lineage.

Purpose: Establish the semantic half of TEST-03 update behavior before any catalog write.
Output: Human-approved prior-lineage record, new candidate authority, and changed-source evidence ready for protected same-Draft publication.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/state.py
@src/skillscout/application/acceptance.py
@src/skillscout/application/phase3.py
@.planning/phases/06-adversarial-mvp-acceptance/06-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 06-09-01: Approve exact prior lineage and changed SHA</name>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-08-SUMMARY.md, src/skillscout/adapters/state.py, src/skillscout/domain/candidate_authority.py, src/skillscout/domain/acceptance.py, AGENTS.md</read_first>
  <action>Present one eligible benchmark workflow’s exact repository ID, old SHA, proposed new fixed SHA, initial/current WorkflowSpec authorities, lineage ID, stable slug, prior terminal/package digests, bounded changed files, and proposed `PriorLineageBindingV1` digest. Ask the human to confirm that the new SHA is the intended source revision and authorize its bounded GitHub reads, DeepSeek reevaluation, and persistence. The approval does not authorize publication, Gate B4, arbitrary lineage inference, another repository/SHA, merge, approval, ready transition, or cleanup.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase3_pipeline.py -k 'prior_lineage or changed_source or approval' -x</automated></verify>
  <acceptance_criteria>
    - Approval binds exact old/new identities and one binding digest.
    - New SHA is fixed and license/read-only rules revalidate.
    - No changed-source request or state mutation precedes approval.
  </acceptance_criteria>
  <done>One exact source change and prior lineage binding are human approved.</done>
</task>

<task type="auto">
  <name>Task 06-09-02: Reevaluate changed source and persist explicit lineage</name>
  <files>tests/test_phase6_acceptance.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, src/skillscout/application/acceptance.py, src/skillscout/adapters/state.py, src/skillscout/application/phase3.py, tests/test_phase6_acceptance.py</read_first>
  <action>After approval, dispatch `run-changed-source` for only the approved pair through the already frozen source-only Phase 6 workflow. Re-pin/read through existing GitHub REST boundaries, rerun all affected deterministic and Flash/Flash/Pro stages, and construct a new workflow/package authority. Call the existing `persist_prior_lineage_binding(binding, approval)` path and verify the complete prior chain; do not derive identity from path/title/slug similarity. Persist `ChangedSourceEvidenceV1` with exact kind `acceptance_changed_source` as immutable pre-publication changed-source intent/evidence, including old/new SHA, old/new authority and package digests, binding/approval digests, affected evidence IDs, and planned stable publication lineage. Plan 06-10 later persists the distinct post-effect `ChangedSourceDraftUpdateCompletionV1`/`acceptance_changed_source_draft_update_completion` fact; it never overwrites this intent fact. No catalog token or remote Draft write is available in this job, and this plan does not modify any of the four Gate B4-bound workflows.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase3_pipeline.py -k 'changed_source or prior_lineage or new_authority or no_publication' -x</automated></verify>
  <acceptance_criteria>
    - New SHA creates new semantic/package authority and retains the exact prior link.
    - Missing/mismatched approval, wrong repository, wrong prior terminal/package, or heuristic binding fails before semantic reuse/publication.
    - Catalog/branch/PR/reviewer effects remain zero until Plan 06-10.
  </acceptance_criteria>
  <done>The changed source is independently reevaluated and explicitly linked without remote publication.</done>
</task>

</tasks>

<threat_model>
## Assets
- Old/new source authority, lineage ID, prior terminal/package evidence, human approval, and future stable Draft identity.

## Trust Boundaries
| Boundary | Description |
|---|---|
| New source SHA → existing lineage | Heuristic identity could overwrite the wrong Skill. |
| Human approval → live reevaluation | Approval must be exact and non-reusable across changes. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-26 | Spoofing | prior lineage | high | mitigate | Complete prior-chain verification and exact approval digest. |
| T-06-27 | Tampering | changed-source evidence | high | mitigate | New authority plus old/new canonical digests. |
| T-06-28 | Elevation of Privilege | publication | high | mitigate | Changed-source job has no catalog/publisher capability. |
| T-06-SC | Tampering | source supply chain | high | mitigate | Fixed-SHA REST read only; no clone/install/import/run. |
</threat_model>

## Artifacts this phase produces
- `PriorLineageApprovalRecordV1` for one exact old/new pair
- `ChangedSourceEvidenceV1`
- Workflow action `run-changed-source`
- New WorkflowSpec/package authorities linked to the prior lineage

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Reproducible source update | COVERED |
| REQ | TEST-03 | Changed source triggers reevaluation and later same-Draft update | COVERED |
| RESEARCH | — | Explicit prior-lineage binding, no heuristic identity | COVERED |
| CONTEXT | D-06,D-21,D-23 | Revision rerun, changed-SHA lineage, invalidation | COVERED |

<verification>
Focused lineage and Phase 6 tests prove exact approval, new authority, preserved old-to-new chain, and zero publication effects.
</verification>

<success_criteria>
The changed source cannot reuse stale evidence or target an existing Skill without explicit verified lineage and human approval.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-09-SUMMARY.md`.</output>
