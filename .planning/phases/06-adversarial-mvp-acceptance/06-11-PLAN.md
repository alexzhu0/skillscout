---
phase: 06-adversarial-mvp-acceptance
plan: "11"
type: execute
wave: 9
depends_on: [06-10]
files_modified:
  - tests/fixtures/acceptance/reviewer_calibration.json
  - tests/test_phase6_acceptance.py
autonomous: false
requirements: [TEST-02, TEST-04]
must_haves:
  truths:
    - "D-16 the real value Draft receives `publishable` or `publishable_with_changes`; creation/request alone is insufficient."
    - "D-17 human review covers usefulness, fidelity, repository/SHA/license/attribution, instruction safety, and exact diff scope."
    - "Attestation binds the exact current open Draft head, package, marker, source SHA, reviewer identity, and timestamp."
    - "Pro Reviewer remains semantic advice only and is calibrated against at least ten human labels with no publication authority."
    - "Human-attestation execution uses the workflow bytes frozen before Plan 06-10 and makes no post-canary workflow change."
    - "Plan 06-04 owns strict `ReviewerCalibrationV1` and `acceptance_reviewer_calibration` persistence before this plan records advice-only calibration."
  artifacts:
    - {path: tests/fixtures/acceptance/reviewer_calibration.json, provides: "Versioned synthetic/redacted reviewer-human calibration labels"}
  key_links:
    - {from: HumanSkillReviewAttestationV1, to: live Draft, via: "fresh remote open/Draft/head/marker equality"}
    - {from: Pro review facts, to: calibration metrics, via: "human labels, agreement, Cohen's kappa"}
    - {from: ReviewerCalibrationV1, to: acceptance_reviewer_calibration, via: "Plan 06-04 exact typed persistence/export/rebuild mapping"}
---

<objective>
Obtain and ingest the exact-head human value verdict, then calibrate the Pro Reviewer as advice only.

Purpose: Prove the generated Skill is genuinely useful and faithful, not merely valid or successfully published.
Output: Canonical human attestation, live head reconciliation, and ≥10-case Pro/human calibration facts.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md
@src/skillscout/domain/acceptance.py
@src/skillscout/application/acceptance.py
@src/skillscout/adapters/github_publish.py
@.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-11-01: Review the exact open value Draft head</name>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md, .planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, src/skillscout/domain/acceptance.py, docs/ARCHITECTURE.md</read_first>
  <action>Pause and present the exact catalog/PR number, open/Draft status, head SHA, base SHA, source repository/ID/SHA, license/attribution, package and marker digests, evidence references, and changed-source lineage. Ask the human to inspect the exact GitHub diff without inspecting credentials or running source code, and record each D-17 checklist item plus one verdict: `publishable`, `publishable_with_changes`, or `rejected`. Notes are bounded and sanitized. The human may later request changes/merge/close, but Phase 6 performs none of those actions. If the head changes during review, reject the attestation and repeat review at the new exact head.</action>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'human_review or exact_head or checklist or draft_state' -x</automated>
    <human-check>Confirm usefulness, fidelity, repository/SHA/license/attribution, instruction safety, and diff scope on the displayed exact Draft head, then choose the explicit verdict.</human-check>
  </verify>
  <acceptance_criteria>
    - All six identity/checklist groups are explicitly completed for one exact head.
    - `rejected` remains useful negative evidence but does not satisfy the positive value gate.
    - No merge, approval, ready transition, cleanup, source execution, or secret inspection occurs.
  </acceptance_criteria>
  <done>An accountable human content verdict exists for the exact current value Draft.</done>
</task>

<task type="auto">
  <name>Task 06-11-02: Reconcile and persist the human attestation</name>
  <files>tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/application/acceptance.py, src/skillscout/domain/acceptance.py, src/skillscout/adapters/github_publish.py, src/skillscout/adapters/operations_state.py, .github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py</read_first>
  <action>Serialize `HumanSkillReviewAttestationV1`, then execute the already finalized `record-acceptance-attestation --kind human-review` read/reconcile job without changing any workflow byte. Freshly observe the exact catalog, open/Draft state, current head/base, marker, package/source/lineage identities, requested/submitted reviewer evidence, and default branch before accepting the attestation. Persist it through operations state with exact kind `acceptance_human_review` and its self-digest. The job has read-only GitHub/state authority and no publication client, review-submission, approval, merge, ready, or cleanup method. Any stale head/marker/identity or incomplete checklist blocks the positive gate.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'attestation or reconcile or submitted_review or stale_head or no_publication' -x</automated></verify>
  <acceptance_criteria>
    - Stored attestation exactly matches a fresh remote observation and the current candidate/package evidence.
    - Reviewer request alone cannot satisfy human review; completed human verdict is required.
    - Positive value gate passes only for `publishable` or `publishable_with_changes`.
  </acceptance_criteria>
  <done>The exact-head human verdict is canonical and independently revalidated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-11-03: Calibrate Pro Reviewer against human labels</name>
  <files>tests/fixtures/acceptance/reviewer_calibration.json, tests/test_phase6_acceptance.py</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md, tests/fixtures/openai/reviewer/cases.json, tests/test_openai_review.py, src/skillscout/domain/review.py, tests/test_phase6_acceptance.py</read_first>
  <action>Create a versioned content-addressed calibration set of at least ten eligible/reject cases using bounded redacted facts and human labels. Compute exact agreement and Cohen’s kappa deterministically, record every disagreement and all safety/provenance cases for human review, validate the strict Plan 06-04-owned `ReviewerCalibrationV1`, and persist it through `OperationsStateStore.record_acceptance_fact` with exact kind `acceptance_reviewer_calibration`; require typed export/rebuild equality before report credit. Require ≥80% agreement and κ≥0.70 before reporting calibrated Reviewer quality. Missing threshold never grants or removes publication permission: keep Pro advisory and leave the release gate failed until required human judgments are complete. Never feed calibration labels into Reviewer requests, and reject kind/model/schema mismatch or raw label prose in canonical state.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'calibration or agreement or kappa or reviewer_advisory or label_leakage' -x</automated></verify>
  <acceptance_criteria>
    - At least ten labels, exact agreement, kappa, disagreements, and policy version are reproducible.
    - Labels are absent from serialized Reviewer inputs.
    - Calibration cannot override validation, hard gates, or human exact-head verdict.
  </acceptance_criteria>
  <done>Reviewer quality is measured against humans without gaining decision or publication authority.</done>
</task>

</tasks>

<threat_model>
## Assets
- Exact Draft head/diff, human identity/verdict, provenance/license checklist, and Reviewer calibration labels.

## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub Draft observation → human attestation | Stale head or reviewer routing must not count as content judgment. |
| Human labels → Pro calibration | Labels must not contaminate model requests or grant authority. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-33 | Spoofing | human review identity/head | high | mitigate | Fresh remote exact-head reconciliation. |
| T-06-34 | Tampering | checklist/verdict | high | mitigate | Strict self-digested attestation and complete fields. |
| T-06-35 | Elevation of Privilege | Reviewer recommendation | high | mitigate | Advice-only type and no publication dependency. |
| T-06-SC | Tampering | calibration fixtures | high | mitigate | Synthetic/redacted facts and canonical digests. |
</threat_model>

## Artifacts this phase produces
- `HumanSkillReviewAttestationV1`
- Execution of the pre-finalized workflow action `record-human-review`
- `tests/fixtures/acceptance/reviewer_calibration.json`
- Canonical `acceptance_reviewer_calibration` fact carrying strict advice-only `ReviewerCalibrationV1` agreement, Cohen's kappa, disagreement, and advisory status

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Human confirms value/safety on a real Draft | COVERED |
| REQ | TEST-02, TEST-04 | Reviewer rejection/calibration and real human-reviewed Draft | COVERED |
| RESEARCH/AI-SPEC | — | Exact-head attestation and ≥10 label calibration | COVERED |
| CONTEXT | D-08,D-11,D-15..D-17,D-19 | Independent advice, non-waivable gates, human checklist, open Draft | COVERED |

<verification>
Attestation/head mutation tests and deterministic calibration tests pass; live state contains an exact reviewed verdict.
</verification>

<success_criteria>
At least one exact value Draft has a real human content verdict, and Pro quality is calibrated without authority escalation.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-11-SUMMARY.md`.</output>
