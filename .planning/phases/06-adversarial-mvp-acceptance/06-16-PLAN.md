---
phase: 06-adversarial-mvp-acceptance
plan: "16"
type: execute
wave: 6
depends_on: [06-07, 06-15]
files_modified:
  - src/skillscout/domain/acceptance.py
  - src/skillscout/adapters/operations_state.py
  - src/skillscout/application/acceptance.py
  - src/skillscout/bootstrap.py
  - src/skillscout/cli.py
  - .github/workflows/phase6-acceptance.yml
  - tests/test_acceptance_domain.py
  - tests/test_operations_state.py
  - tests/test_phase6_acceptance.py
  - tests/test_phase6_workflow.py
  - tests/test_phase6_source_execution.py
  - tests/test_cli_security.py
  - tools/verify_phase6_source_execution.py
autonomous: false
requirements: [TEST-01, TEST-02]
user_setup:
  - service: github
    why: "The protected benchmark-lock job requires a platform-enforced human review; no credential value is inspected by implementation or tests."
    dashboard_config:
      - task: "Create environment `phase6-human-benchmark-lock`; require reviewer `alexzhu0`; leave Prevent self-review disabled because the sole required reviewer may approve a run they initiated; enable no custom deployment protection rule."
        location: "Repository Settings → Environments → phase6-human-benchmark-lock"
must_haves:
  truths:
    - "D-01/D-02/D-03: a fresh Search-derived nomination is selected through the fixed five-entry V1 selection manifest, then only an environment-approved V2 benchmark-lock fact can bind the canonical state repository, read-verified parent commit/root, immutable source/workflow, nomination, and selected entries for a fresh campaign."
    - "D-06: stale, duplicate, malformed, V1-only, or mismatched lock evidence fails closed with no retry, compensation, live authority, semantic call, candidate execution, catalog, pull-request, review, or publication effect."
    - "Historical `locked-benchmark-manifest-v1` facts remain byte-preserved and rebuildable history; fresh admission accepts only `locked-benchmark-manifest-v2` selected by schema version under the same fact kind."
    - "The dedicated benchmark-lock job waits on `phase6-human-benchmark-lock`, reads GitHub Actions approval history from the fixed GitHub API host using only `GITHUB_TOKEN` Actions-read authority, and never trusts an actor assertion, caller reviewer field, or approval comment."
  artifacts:
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json, provides: "Fixed human-selected V1 selection manifest whose digest is separately bound by the dynamic V2 lock fact"}
    - {path: src/skillscout/domain/acceptance.py, provides: "Strict `LockedBenchmarkManifestV2` and approval-receipt models"}
    - {path: .github/workflows/phase6-acceptance.yml, provides: "State-only `skillscout-phase6-benchmark-lock` protected job"}
  key_links:
    - {from: 06-BENCHMARK-MANIFEST.json, to: LockedBenchmarkManifestV2.selection_manifest_digest and nomination_set_digest, via: "canonical checked-out V1 bytes must exactly match the current fresh nomination"}
    - {from: canonical state repository and read-verified parent commit/root, to: LockedBenchmarkManifestV2, via: "immutable source SHA and workflow digest containing that exact parent"}
    - {from: GitHub Actions approvals REST response, to: LockedBenchmarkManifestV2.manifest_digest, via: "strict redacted approval receipt and canonical digest"}
    - {from: acceptance_benchmark_lock schema discriminator, to: later live authority, via: "V2-only fresh admission; V1 records are historical"}
---

<objective>
Replace the self-asserted fresh benchmark lock with a separately versioned, GitHub protected-environment proof while preserving historical V1 facts exactly.

Purpose: Give the five-repository selection a technically verifiable human boundary before any later live authority can exist.
Output: V2 lock schema/state support, a state-only protected benchmark-lock route, source-execution tests, and a named human environment checkpoint.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md
@.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json
@src/skillscout/domain/acceptance.py
@src/skillscout/adapters/operations_state.py
@src/skillscout/application/acceptance.py
@src/skillscout/adapters/github.py
@.github/workflows/phase6-acceptance.yml
@tools/verify_phase6_source_execution.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-16-01: Add schema-discriminated V2 benchmark-lock evidence without rewriting V1 history</name>
  <files>src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, tests/test_acceptance_domain.py, tests/test_operations_state.py</files>
  <behavior>
    - `LockedBenchmarkManifestV1` remains the exact parser and rebuild target for historical `locked-benchmark-manifest-v1` rows.
    - A new `LockedBenchmarkManifestV2` has purpose `benchmark_lock` and canonically binds canonical state repository identity, read-verified parent commit/root, immutable source SHA and workflow digest containing that exact parent, V1 selection-manifest digest, fresh nomination digest, environment, approved reviewer identity, workflow run ID and attempt, trigger identity, redacted approval-record digest, and its own lock digest.
    - The same `acceptance_benchmark_lock` fact kind selects V1 or V2 from a schema-version-discriminated `ACCEPTANCE_FACT_MODELS` registry; a naked union, V1 reinterpretation, and SQLite migration are forbidden.
    - V2 rejects forged identity, a bot/self-dispatch without a matching approval, wrong environment, wrong state repository or parent commit/root, stale selection/source/workflow linkage, a selection-manifest nomination digest mismatch, changed/missing/duplicate/stale selected entries, absent approval evidence, malformed approval records, comments in canonical evidence, and a V1 fact offered as fresh authority.
  </behavior>
  <action>Write RED tests first, then extend the strict frozen/canonical patterns in `src/skillscout/domain/acceptance.py` and the existing `ACCEPTANCE_FACT_MODELS`/raw-canonical-row restore pattern in `src/skillscout/adapters/operations_state.py`. Per D-01, D-02, D-03, and D-06, retain `locked-benchmark-manifest-v1` byte-for-byte as historical data while adding `LockedBenchmarkManifestV2` selected by explicit schema version under the existing `acceptance_benchmark_lock` kind. The V2 fact must bind `purpose=benchmark_lock`, canonical state repository identity, a read-verified parent commit/root, immutable source SHA and workflow digest that contain that exact parent, fresh nomination digest, the static checked-in `.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json` digest, a separately canonical approval receipt, and its final lock digest; do not change SQLite because its rows already retain schema version and canonical JSON. Define the selection chain so `selection_manifest.nomination_set_digest` equals the fresh nomination digest and every selected entry matches one fresh nomination entry in numeric repository ID, full name/commit SHA, permissive license, provenance/selection evidence, and role; reject changed, missing, duplicate, or stale entries. The receipt permits only the expected environment `phase6-human-benchmark-lock`, reviewer login `alexzhu0` plus GitHub-returned numeric identity, workflow run/attempt, source SHA, workflow digest, trigger identity, and a digest of a redacted approval record. Validate any API comment only as hostile transport data and discard it before logging, facts, prompts, or digests. Extend export/rebuild, duplicate identity, recovery-parent, and selection-chain tests so V1 history restores, V2 fresh locks restore, later V2 authority/live admission can revalidate every binding, and cross-version substitution fails closed.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_domain.py tests/test_operations_state.py -k 'benchmark_lock or manifest_v2 or schema_version or approval_receipt or canonical or rebuild' -x</automated></verify>
  <done>Historical V1 lock rows remain historical, while one strict V2 lock contract can persist and rebuild approval-bound fresh evidence under the same fact kind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-16-02: Implement the state-only protected benchmark-lock route and source proof</name>
  <files>src/skillscout/application/acceptance.py, src/skillscout/bootstrap.py, src/skillscout/cli.py, .github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py, tests/test_phase6_workflow.py, tests/test_phase6_source_execution.py, tests/test_cli_security.py, tools/verify_phase6_source_execution.py</files>
  <behavior>
    - `prepare-fresh-campaign` accepts only closed fresh-state inputs, runs bounded Search, and records one nomination; it never records a lock or live authority.
    - `lock-fresh-campaign` loads only the fixed V1 selection-manifest path from the immutable checkout, read-verifies the current post-nomination parent state and state repository identity, proves the static manifest's nomination digest and every selected entry exactly match that fresh nomination, obtains an independent V2 approval receipt, and writes one V2 benchmark lock by one forward compare-and-swap.
    - The `skillscout-phase6-benchmark-lock` job is gated by `environment: phase6-human-benchmark-lock`, has only `contents: read` plus `actions: read` for its `GITHUB_TOKEN`, and uses separate bounded state credentials only after the approval evidence validates.
    - The fixed-host Actions reader accepts only the `GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals` response shape and optional fixed run-attempt metadata; it performs no approval write, follows no caller host/path, and emits no approval comment.
  </behavior>
  <action>Write RED tests first and follow the capability-limited composition in `src/skillscout/application/acceptance.py`, `read_exact_discovery_state` in `bootstrap.py`, the closed parser style in `cli.py`, and fixed-host/header discipline in `src/skillscout/adapters/github.py`. Add only the state-only `prepare-fresh-campaign` and `lock-fresh-campaign` routes. The lock route must derive canonical state repository identity and parent commit/root from a read-verified current state, resolve the manifest only at the fixed static path, calculate the checked-out source/workflow binding internally with the exact parent present, and obtain approval only from the fixed GitHub API host with `GITHUB_TOKEN` Actions-read scope. Before one forward compare-and-swap, require `selection_manifest.nomination_set_digest` to equal the fresh nomination digest and every selected entry to match exactly one fresh nomination entry by numeric repository ID, full name/commit SHA, permissive license, provenance/selection evidence, and role; reject changed, missing, duplicate, or stale entries. Configure the new `skillscout-phase6-benchmark-lock` job with the literal protected environment, full-SHA checkout, pinned local toolchain, and direct checkout-local locked invocation; its permissions and dependency construction must exclude DeepSeek, semantic-provider, candidate-code, catalog, pull-request, reviewer, and publication authority. Strictly parse hostile approval JSON, require an approved record for the exact environment and `alexzhu0`, bind the current run ID/attempt and trigger identity, and retain only redacted canonical fields plus digests. Extend the source-execution verifier and mutation suite for both new commands so substituted command/source, forged identity, bot or self-dispatch without approval, wrong environment, wrong state repository/parent, stale workflow/source/selection/nominations, missing or malformed approval record, wrappers, external work directories, reordered setup, and unknown routes fail before state persistence. This is an allowed pre-Gate-B4 workflow mutation, but not the final one; Plan 06-18 Task 02 owns that final mutation.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase6_workflow.py tests/test_phase6_source_execution.py tests/test_cli_security.py -k 'fresh_campaign or benchmark_lock or manifest_v2 or protected_environment or approval_receipt or source_execution or state_only' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py</automated></verify>
  <done>Fresh nomination and approval-bound V2 lock transitions are the only effects of the new protected route, and both are source-execution and mutation proven without a live semantic or publication effect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-16-03: Configure and prove the protected V2 benchmark lock</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json, .github/workflows/phase6-acceptance.yml, src/skillscout/adapters/operations_state.py</files>
  <action>Per D-01, D-02, D-03, and D-06, configure the named GitHub environment, commit the exact static V1 selection manifest, and dispatch only the completed state-only preparation and lock routes. Approve the waiting benchmark-lock job through the configured environment, then confirm the completed V2 persistence/rebuild path records redacted evidence only. Do not edit a workflow, retry a failed run, resolve live credentials, invoke a semantic provider, execute candidate code, or create a catalog, pull-request, review, or publication effect.</action>
  <what-built>Offline-tested V2 lock contract and the state-only `skillscout-phase6-benchmark-lock` job. No DeepSeek call, candidate-code execution, catalog/PR/review/publication effect, or live authority has occurred.</what-built>
  <how-to-verify>
    1. Configure the GitHub environment named `phase6-human-benchmark-lock` with required reviewer `alexzhu0`, leave Prevent self-review disabled for the sole reviewer, and enable no custom deployment protection rule or signature/key scheme.
    2. Trigger only `prepare-fresh-campaign`; confirm the output is a new Search-derived nomination from a server-verified current canonical parent and does not reuse a historical lock.
    3. As the accountable human, update the exact static `.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json` selection manifest with the five nominated fixed identities, licenses, provenance, and required role distribution; commit it before dispatching the lock job.
    4. Trigger only `lock-fresh-campaign` at the immutable source containing that manifest. Confirm the `skillscout-phase6-benchmark-lock` job waits for the named environment, then approve it through GitHub as `alexzhu0`.
    5. Confirm the resulting canonical V2 lock records purpose, canonical state repository, read-verified parent commit/root, environment, reviewer identity, run ID/attempt, source SHA, workflow digest containing that parent, fresh nomination digest, static selection-manifest digest, V2 lock digest, approval-record digest, and trigger identity; also confirm the static manifest's nomination digest and all five selected entries exactly match the fresh nomination. It must retain no comment and create no live authority.
  </how-to-verify>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase6_workflow.py tests/test_phase6_source_execution.py -k 'benchmark_lock or manifest_v2 or approval_receipt or protected_environment or state_only' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py</automated></verify>
  <resume-signal>Report the non-secret V2 lock, source/workflow, approval-record, and state digest identifiers, or describe the failed check.</resume-signal>
  <done>A human-selected fixed five-repository V1 selection manifest is bound into one independently environment-approved V2 benchmark lock and nothing beyond state-only preparation is authorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static selection manifest → V2 lock | Human-selected V1 bytes must be immutable, canonical, and separately digested. |
| GitHub environment approval API → V2 fact | Hostile response fields must be strictly parsed and comments must not cross the boundary. |
| state-only job → later live authority | A V2 lock proves benchmark selection only; it cannot invoke semantic or publication capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-48 | Spoofing | benchmark-lock approval | critical | mitigate | Literal environment/reviewer checks, fixed-host Actions review-history read, run-attempt binding, and V2 canonical receipt. |
| T-06-49 | Tampering | lock/state lineage and recovery | critical | mitigate | Server-verified state repository/parent commit/root, source/workflow bytes containing that parent, full nomination-to-selection equality, schema-discriminated canonical restore, and one CAS. |
| T-06-50 | Repudiation | human approval | high | mitigate | Persist redacted reviewer/run/environment/trigger/record-digest evidence, never a self-attestation or comment. |
| T-06-51 | Elevation of Privilege | benchmark-lock job | critical | mitigate | State-only dependency graph, Actions-read `GITHUB_TOKEN`, separate late state credential, and source-execution mutation tests. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package-manager changes or new dependencies. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Repeatable real-repository benchmark with human control | 06-16 | COVERED |
| REQ | TEST-01 | Five fixed-SHA Search-derived repositories | 06-16 | COVERED |
| REQ | TEST-02 | Required mixed benchmark evidence | 06-16 | COVERED |
| RESEARCH | — | Two-step nominate/lock, fixed-SHA source, canonical state | 06-16 | COVERED |
| CONTEXT | D-01..D-06 | Human-selected, reproducible, revision-bound benchmark | 06-16 | COVERED |

<verification>
Run the focused contract, state, application/CLI, workflow, and source-execution mutation suites. The protected human checkpoint then creates one V2 lock; it does not create live authority.
</verification>

<success_criteria>
A fresh benchmark can be locked only by strict GitHub protected-environment approval evidence bound to a read-verified canonical parent and complete immutable V1-selection-to-fresh-nomination chain, while historical V1 facts remain history and no live capability is opened.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-16-SUMMARY.md` with non-secret V2 lock and state identifiers only.</output>
