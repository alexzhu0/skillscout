---
phase: 06-adversarial-mvp-acceptance
plan: "12"
type: execute
wave: 10
depends_on: [06-11]
files_modified:
  - tests/test_phase6_acceptance.py
autonomous: false
requirements: [TEST-04]
must_haves:
  truths:
    - "D-20 probe cleanup occurs only after denial/value evidence is durable and only through a separate human/admin identity."
    - "Automation/App token exposes no cleanup, delete, merge, approve, or ready capability."
    - "Cleanup closes only named probe PRs and deletes only named probe branches; the value Draft remains open and Draft."
    - "Default branch and value Draft head/marker remain unchanged after cleanup."
    - "Cleanup attestation executes the workflow bytes frozen before Plan 06-10 and makes no post-canary workflow change."
  artifacts:
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Read-only cleanup-attestation ingestion job"}
  key_links:
    - {from: Gate B4 cleanup manifest, to: ProbeCleanupAttestationV1, via: "human/admin exact-target action and post-cleanup observation"}
---

<objective>
Complete the separately authorized human/admin cleanup of canary probe artifacts and ingest its attestation.

Purpose: Remove test-only remote artifacts without weakening or giving cleanup authority to the automation identity.
Output: Verified cleanup attestation while the value Draft remains open for human control.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-11-SUMMARY.md
@tools/gate_b4_canary.py
@src/skillscout/domain/acceptance.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 06-12-01: Perform exact probe-only cleanup as human/admin</name>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md, .planning/phases/06-adversarial-mvp-acceptance/06-11-SUMMARY.md, canonical Gate B4 state commit/root/fact locators from 06-10-SUMMARY.md, tools/gate_b4_canary.py</read_first>
  <action>After all canary and value evidence is durable, present only the exact probe PR numbers/branches from the reviewed cleanup manifest and the exact value Draft that must remain untouched. A separately authenticated human/admin closes probe PRs without merge, deletes probe branches, and records actor, action time, exact targets, post-cleanup branches, default SHA, value Draft number/state/head, and attestation identity. Do not ask for or use the GitHub App token; do not close/update/merge/ready the value Draft or alter the default branch.</action>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_gate_b4_canary.py tests/test_phase6_acceptance.py -k 'cleanup or probe_only or value_draft_untouched' -x</automated>
    <human-check>Confirm only listed probe PRs/branches were removed with a human/admin identity and the value Draft/default branch remain unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - Cleanup actor is distinct from the GitHub App automation identity.
    - Every manifest probe target is cleaned and no unlisted target is affected.
    - Value Draft remains open/Draft at the attested head and default SHA is unchanged.
  </acceptance_criteria>
  <done>Probe artifacts are removed by the intended separate authority.</done>
</task>

<task type="auto">
  <name>Task 06-12-02: Verify and persist cleanup attestation</name>
  <files>tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/application/acceptance.py, src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, .github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py</read_first>
  <action>Execute the already finalized `record-acceptance-attestation --kind probe-cleanup` read-only protected job without changing any workflow byte. Re-read the catalog and require exact absence/closed state of only the strict cleanup targets nested in the canonical `GateB4BindingV1`, unchanged default SHA, and the value Draft still open/Draft at its human-reviewed head. Validate and persist `ProbeCleanupAttestationV1` with exact kind `acceptance_cleanup`, actor and target identities, but no credential or administrative session data. The workflow and production Python surface must still contain no cleanup mutation route.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_gate_b4_canary_workflow.py -k 'cleanup_attestation or exact_targets or no_cleanup_route or value_draft' -x</automated></verify>
  <acceptance_criteria>
    - Attestation matches fresh remote observations and exact original cleanup manifest.
    - Automation only verifies/persists; it does not delete or close anything.
    - Any extra deletion, surviving probe, changed default, or changed/closed value Draft blocks release.
  </acceptance_criteria>
  <done>Cleanup is canonical evidence without granting cleanup authority to SkillScout.</done>
</task>

</tasks>

<threat_model>
## Assets
- Probe PRs/branches, value Draft, default branch, cleanup manifest, and separate human authority.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Automation cleanup manifest → human/admin action | Exact targets must not expand. |
| Human action → canonical attestation | Post-state must be independently reobserved. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-36 | Elevation of Privilege | automation cleanup | high | mitigate | No cleanup mutation route; human-only checkpoint. |
| T-06-37 | Tampering | value/default branch | high | mitigate | Exact pre/post equality observations. |
| T-06-38 | Repudiation | cleanup actor/targets | high | mitigate | Strict target-bound attestation and fresh reread. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package changes. |
</threat_model>

## Artifacts this phase produces
- `ProbeCleanupAttestationV1`
- Execution of the pre-finalized workflow action `record-probe-cleanup`
- Canonical post-cleanup observations for probes, value Draft, and default SHA

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Human-controlled publication lifecycle | COVERED |
| REQ | TEST-04 | Least-privilege remote behavior | COVERED |
| RESEARCH | — | Separate human/admin cleanup and canonical attestation | COVERED |
| CONTEXT | D-17,D-19,D-20,D-22,D-23 | Human control, open value Draft, separate probe cleanup, fresh bindings | COVERED |

<verification>
Cleanup and workflow tests prove exact human-only target removal and unchanged value/default state.
</verification>

<success_criteria>
All probe artifacts are cleaned by a separate human/admin while SkillScout remains structurally incapable of cleanup and the value Draft stays open.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-12-SUMMARY.md`.</output>
