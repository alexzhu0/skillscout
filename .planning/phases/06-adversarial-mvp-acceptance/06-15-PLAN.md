---
phase: 06-adversarial-mvp-acceptance
plan: "15"
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - .github/workflows/phase6-acceptance.yml
  - .github/workflows/discover.yml
  - .github/workflows/publish-candidate.yml
  - .github/workflows/gate-b4-canary.yml
  - tools/verify_phase6_source_execution.py
  - tests/test_publication_security.py
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-11..D-17 workflow jobs preserve business-terminal evidence while blocking system failures and every unsatisfied hard gate."
    - "D-18/D-22/D-23 catalog authority appears only after fresh Gate B4 and binds the exact frozen workflow bytes and fixed catalog identity."
    - "Before fresh Gate B4, every authoritative SkillScout execution in discover, publish-candidate, Gate B4 canary, and Phase 6 acceptance uses checked-out locked repository source."
    - "No wheel, dist, registry, downloaded artifact, wrapper, indirect script, external working directory, or preinstalled command can become an execution source."
    - "Plan 06-06 may complete only the already source-only Phase 6 offline job; Plans 06-07..06-09 only execute/read workflows, and Plans 06-10 onward cannot repair workflow bytes."
  artifacts:
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Separated offline, nomination, live, publication, attestation, and rebuild authority zones"}
    - {path: .github/workflows/publish-candidate.yml, provides: "Pre-Gate-B4 protected publication bytes with repository-local locked source execution"}
    - {path: tools/verify_phase6_source_execution.py, provides: "Closed fail-closed verifier over all four authoritative workflow paths"}
    - {path: tests/test_publication_security.py, provides: "Owned publication-workflow contract and launcher mutation rejection"}
  key_links:
    - {from: all four authoritative workflows, to: checked-out SkillScout modules/tools, via: "preceding same-job full-SHA checkout plus exact local uv 0.11.29 run-locked invocation"}
    - {from: tools/verify_phase6_source_execution.py, to: discover/publish/canary/Phase6 workflow jobs, via: "ordered YAML step parsing and a closed authoritative-invocation grammar"}
    - {from: protected publication job, to: fresh Gate B4 output, via: "exact needs order and late catalog-token minting after source-only verification"}
---

<objective>
Finalize the pre-Gate-B4 workflow authority zones and checked-out-source execution contract.

Purpose: Separate security-critical workflow corrections from application/CLI composition while preserving the exact freeze before fresh Gate B4.
Output: Four source-only authoritative workflows, one closed structural verifier, and owned publication-security coverage.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-02-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-03-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-04-SUMMARY.md
@.github/workflows/discover.yml
@.github/workflows/publish-candidate.yml
@.github/workflows/gate-b4-canary.yml
@.github/workflows/phase6-acceptance.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-15-01: Finalize protected workflow zones and locked source execution</name>
  <files>.github/workflows/phase6-acceptance.yml, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, tools/verify_phase6_source_execution.py, tests/test_publication_security.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, tests/test_phase6_source_execution.py, tests/test_phase6_workflow.py, tests/test_publication_security.py, tests/test_discovery_security.py, tools/verify_phase5_acceptance.py, tools/verify_phase4_action_audit.py, tests/test_phase5_acceptance.py, tests/test_phase4_action_audit.py, pyproject.toml, uv.lock</read_first>
  <behavior>
    - Closed workflow choices and needs edges grant each job only its required credential zone.
    - Every authoritative repository invocation is preceded by same-job full-SHA checkout and exact repository-local locked uv execution.
    - Unknown, indirect, downloaded, mutable, or empty-scan execution paths fail closed.
  </behavior>
  <action>Extend the existing probe workflow while preserving the Plan 06-02-owned `phase6_action=isolation-probe` choice/job, and add closed choices for `nominate`, `offline-adversarial`, `run-benchmark`, `run-replay`, `run-changed-source`, `gate-b4-and-publish`, `record-human-review`, `record-probe-cleanup`, and `rebuild-report`. Per D-11..D-17, define separate jobs/environments and exact `needs` order so missing evidence or a system failure blocks later authority. Offline jobs have no credentials/network; nomination has Search/state only; semantic jobs have source/state/DeepSeek only; per D-18/D-22/D-23, publication has protected fixed-catalog authority only after fresh exact-byte Gate B4; attestation/rebuild have read/verified-state only. Use one non-cancelling `skillscout-phase6-acceptance` concurrency group, exact Action SHAs, explicit timeouts, and bounded raw artifact retention of one day. Create `tools/verify_phase6_source_execution.py` from the Phase 5 acceptance and Phase 4 action-audit patterns as a standard-library-only, read-only, network-free, project-import-free verifier with the exact closed path set `.github/workflows/discover.yml`, `.github/workflows/publish-candidate.yml`, `.github/workflows/gate-b4-canary.yml`, and `.github/workflows/phase6-acceptance.yml`. Safely parse ordered `jobs.*.steps`, reject malformed/duplicate/ambiguous YAML shapes, and find every authoritative `skillscout` module/CLI import plus checked-out repository tool invocation; zero findings or an unknown form fails. For every finding require an earlier same-job `actions/checkout@{40 lowercase hex}` of the current revision with `persist-credentials:false`, no external checkout path, followed by exact pinned `astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9`, `version: 0.11.29`, caching disabled, verified materialization at `.tools/uv-0.11.29/bin/uv`, and direct `.tools/uv-0.11.29/bin/uv run --locked` execution from the checkout root. Correct all four workflows to that contract before fresh Gate B4, including every inline Python block importing SkillScout and checked-out tools such as `tools/gate_b4_canary.py`. The workflow input grammar has no wheel/source selector. Reject pip, registry, wheel, `dist/`, `uvx`, `uv tool`, `uv run --with`, bare/preinstalled `skillscout`, download-artifact/release/download execution, external working directories, command variables, aliases/functions, sourced/delegated wrappers, indirect scripts, and every unrecognized acquisition/invocation form. Do not install, import, or execute candidate-repository content. Turn the complete Wave 0 mutation suite in `tests/test_phase6_source_execution.py` GREEN without modifying that Plan 06-02-owned test file, and extend the owned `tests/test_publication_security.py` assertions for the corrected publication launcher. These are pre-Gate-B4 bytes; Plan 06-06 may only complete the already source-only Phase 6 offline job, Plans 06-07..06-09 execute/read workflows, and Plans 06-10 onward must not modify any of the four workflow files.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_source_execution.py tests/test_publication_security.py tests/test_phase6_workflow.py -k 'source_execution or publish_workflow or locked_source_execution or workflow or zone or action or retention' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py</automated></verify>
  <acceptance_criteria>
    - No job receives a credential it does not need; catalog authority appears only in the protected publication job.
    - Live jobs cannot run before required prior outputs/attestations; workflow choices are closed.
    - Candidate/evaluator values are passed through bounded files, never direct shell interpolation.
    - Every authoritative step in all four exact workflows has preceding same-job full-SHA checkout, exact pinned setup-uv 0.11.29, verified repository-local uv materialization, and direct locked checked-out-source execution.
    - The closed verifier and mutation suite reject every wheel/dist/registry/pip/artifact, bare/unlocked/PATH-only, wrapper/alias/function/variable, external-working-directory, indirect, empty-scan, or unknown path.
    - No source selector exists, no setup or execution step downloads or runs candidate-repository content, and no workflow byte is left for Plans 06-10 through 06-14 to repair.
  </acceptance_criteria>
  <done>The workflow skeleton and protected publication workflow express the exact release chain, credential timing, and locked checked-out-source execution baseline that Plan 06-06 freezes and Plan 06-10 binds.</done>
</task>

</tasks>

<threat_model>
## Assets
- Workflow source bytes, credential zones, protected catalog authority, and source-execution verifier.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Workflow input → job authority | Closed choices must not widen credentials or effects. |
| Checked-out source → authoritative execution | Repository source must not be replaced by packages, artifacts, wrappers, or external paths. |
| Pre-Gate workflow bytes → Gate B4 | Every later publication effect must bind the exact frozen bytes. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-47 | Elevation of Privilege | workflow job authority | critical | mitigate | Closed choices, exact needs edges, and isolated credential zones. |
| T-06-48 | Tampering | authoritative execution source | critical | mitigate | Same-job full-SHA checkout and closed repository-local uv grammar with mutation coverage. |
| T-06-49 | Spoofing | pre-Gate workflow identity | high | mitigate | Four-path structural verifier and exact-byte Gate B4 binding after the final Plan 06-06 change. |
| T-06-SC | Tampering | dependencies/Actions | high | mitigate | Existing full-SHA Actions, exact uv 0.11.29 materialization, and locked dependencies; no new package install. |
</threat_model>

## Artifacts this phase produces
- Workflow input `phase6_action` with retained `isolation-probe` and all closed Phase 6 transitions
- Workflow jobs `nominate`, `offline_adversarial`, `live_benchmark`, `changed_source`, `fresh_gate_b4`, `value_publication`, `human_attestation`, `cleanup_attestation`, `rebuild_report`
- Pre-Gate-B4 source-only bytes for discover, publish-candidate, Gate B4 canary, and Phase 6 acceptance
- Exact closed structural verifier plus publication-specific launcher assertions

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Permission-bounded real production path | COVERED |
| REQ | TEST-01, TEST-03, TEST-04 | End-to-end workflow, replay/update, Draft/denial boundaries | COVERED |
| RESEARCH/AI-SPEC | — | Pre-Gate-B4 source-only finalization and late authority | COVERED |
| CONTEXT | D-11..D-18,D-22,D-23,D-26,D-27 | Hard gates, authority zones, exact-byte binding, and bounded evidence | COVERED |

<verification>
All four workflows pass the closed source-execution mutation suite and publication-specific authority tests without live credentials or effects.
</verification>

<success_criteria>
Every authoritative workflow path is source-only, capability-bounded, and ready for the final Plan 06-06 freeze before fresh Gate B4.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-15-SUMMARY.md`.</output>
