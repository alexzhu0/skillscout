---
phase: 06-adversarial-mvp-acceptance
plan: "03"
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/skillscout/adapters/semantic_provider.py
  - tests/test_semantic_provider.py
  - tests/test_openai_extract.py
  - tests/test_openai_generate.py
  - tests/test_openai_review.py
autonomous: true
requirements: [TEST-01, TEST-04]
must_haves:
  truths:
    - "D-07 live acceptance needs only DeepSeek; recorded OpenAI tests remain green and no OpenAI key is required."
    - "D-08 extraction/generation use `deepseek-v4-flash`; independent review uses `deepseek-v4-pro` in a fresh context."
    - "D-09/D-10 exact stage/model and official endpoint admission occurs before credential lookup and HTTP."
    - "One durable semantic attempt still maps to exactly one SDK request with no tools and `max_retries=0`."
  artifacts:
    - {path: src/skillscout/adapters/semantic_provider.py, provides: "Closed stage-specific DeepSeek policy", exports: [SemanticStage, DEEPSEEK_MODEL_BY_STAGE]}
    - {path: tests/test_openai_review.py, provides: "Pro Reviewer identity and isolation proof"}
  key_links:
    - {from: SemanticProviderSettings, to: Extractor/Generator/Reviewer clients, via: "exact stage model identity"}
---

<objective>
Replace the historical all-Flash live profile with a closed Flash/Flash/Pro provider policy without adding a runtime escape hatch.

Purpose: Make independent Pro review a new exact semantic authority while preserving historical facts and deterministic OpenAI coverage.
Output: Typed stage policy, guarded request admission, and complete adapter request/telemetry tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/semantic_provider.py
@src/skillscout/adapters/openai_extract.py
@src/skillscout/adapters/openai_generate.py
@src/skillscout/adapters/openai_review.py
@.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-03-01: Implement exact stage-aware DeepSeek admission</name>
  <files>src/skillscout/adapters/semantic_provider.py, tests/test_semantic_provider.py</files>
  <read_first>src/skillscout/adapters/semantic_provider.py, tests/test_semantic_provider.py, src/skillscout/application/ports.py, docs/DEVELOPMENT.md, .planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md</read_first>
  <behavior>
    - `SemanticStage.EXTRACTION` and `.GENERATION` admit only `deepseek-v4-flash`; `.REVIEW` admits only `deepseek-v4-pro`.
    - The sole admitted endpoint is `https://api.deepseek.com`; a trailing slash may normalize, but arbitrary compatible endpoints fail.
    - Stage/model/endpoint validation precedes environment key resolution and HTTP; client retries remain zero.
  </behavior>
  <action>RED: extend the Wave 0 matrix for exact stage/model pairs, wrong pairs, arbitrary strings/URLs, missing key, one-request accounting, JSON mode, no tools, non-thinking mode, empty/incomplete/extra-field failures, and sanitized telemetry. GREEN: add `SemanticStage` and immutable `DEEPSEEK_MODEL_BY_STAGE` with exact D-08 identifiers; change `resolve_semantic_provider` to produce Flash/Flash/Pro settings and change `request_deepseek_json` to require a typed stage whose configured model equals the map before any I/O. Preserve `DEEPSEEK_OFFICIAL_BASE_URL`, strict local Pydantic validation, `stream=False`, `max_retries=0`, and existing transport disposition rules. Do not add a model or endpoint environment variable and do not rewrite historical all-Flash evidence.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_provider.py -x</automated></verify>
  <acceptance_criteria>
    - Settings resolve exactly Flash/Flash/Pro for DeepSeek and unchanged `gpt-5.6-terra` identities for recorded OpenAI.
    - Every invalid endpoint or stage/model combination produces zero credential lookup spy and zero transport calls.
    - DeepSeek response JSON is locally strict-validated; empty, truncated, malformed, or extra-field content is never repaired.
  </acceptance_criteria>
  <done>The provider boundary admits one exact model per semantic stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-03-02: Wire Flash extraction/generation and Pro isolated review</name>
  <files>tests/test_openai_extract.py, tests/test_openai_generate.py, tests/test_openai_review.py</files>
  <read_first>src/skillscout/adapters/openai_extract.py, src/skillscout/adapters/openai_generate.py, src/skillscout/adapters/openai_review.py, tests/test_openai_extract.py, tests/test_openai_generate.py, tests/test_openai_review.py, src/skillscout/application/phase3.py</read_first>
  <behavior>
    - Extractor sends one Flash request with an 8,000-token cap; Generator one Flash request with a 6,000-token cap; Reviewer one Pro request with a 2,000-token cap.
    - Reviewer receives only WorkflowSpec, rendered Skill, provenance, and zero-error ValidationReport; no raw repository, expected label, human notes, generator history, edit or publication authority.
    - Actual returned model and usage telemetry remain stage-bound and sanitized.
  </behavior>
  <action>Update the three adapter tests and any necessary constructor/call arguments so each passes its exact `SemanticStage` to `request_deepseek_json` and asserts the AI-SPEC token cap, no-tools body, actual model, request ID, usage, and one-call discipline. Mutation-test raw-corpus/evaluator-field leakage into Reviewer payload and wrong actual model evidence. Preserve all existing structured business results and retry/outcome-unknown classification; this task changes provider identity only, not deterministic qualification, validation, eligibility, or publication authority.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_openai_extract.py tests/test_openai_generate.py tests/test_openai_review.py tests/test_semantic_provider.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/semantic_provider.py</automated></verify>
  <acceptance_criteria>
    - Recorded request bodies show Flash/Flash/Pro and exact 8000/6000/2000 output caps.
    - Pro Reviewer has a separate client request/context and cannot edit or publish.
    - Deterministic OpenAI mock suites pass without any live OpenAI credential.
  </acceptance_criteria>
  <done>All three semantic adapters consume the closed stage policy and preserve Reviewer isolation.</done>
</task>

</tasks>

<threat_model>
## Assets
- Semantic credentials, provider endpoint, exact model/prompt/schema authority, request accounting, and Reviewer independence.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Runtime configuration → provider client | Arbitrary compatible endpoints/models could exfiltrate bounded inputs. |
| Generator evidence → independent Reviewer | Context leakage could collapse independent judgment. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-07 | Spoofing | provider/model identity | high | mitigate | Typed exact stage map and pre-key/pre-HTTP admission. |
| T-06-08 | Information Disclosure | semantic payload | high | mitigate | Official URL only; no tools; bounded stage envelopes. |
| T-06-09 | Repudiation | request accounting | high | mitigate | SDK retries disabled and exact actual-model/usage facts. |
| T-06-SC | Tampering | Python dependencies | high | mitigate | No dependency changes or installs. |
</threat_model>

## Artifacts this phase produces
- `SemanticStage`
- `DEEPSEEK_MODEL_BY_STAGE`
- Stage-specific DeepSeek settings fields `extract_model`, `generator_model`, `reviewer_model`
- Exact model/token-cap adapter tests

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Independent semantic acceptance over real repositories | COVERED |
| REQ | TEST-01, TEST-04 | Live end-to-end and human-review path | COVERED |
| RESEARCH/AI-SPEC | — | Direct SDK, JSON mode, local validation, Flash/Flash/Pro | COVERED |
| CONTEXT | D-07..D-10 | DeepSeek-only live campaign and closed policy | COVERED |

<verification>
All provider and semantic adapter tests pass with exact request bodies, zero live credentials, and zero calls on invalid configuration.
</verification>

<success_criteria>
The only Phase 6 live semantic profile is official-DeepSeek Flash/Flash/Pro; no caller can widen it.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-03-SUMMARY.md`.</output>
