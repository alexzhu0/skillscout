---
phase: 06-adversarial-mvp-acceptance
plan: "14"
type: execute
wave: 12
depends_on: [06-13]
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/DEVELOPMENT.md
  - docs/TESTING.md
  - RELEASE.md
  - tools/verify_phase6_source_execution.py
  - tests/test_phase6_source_execution.py
  - dist/skillscout-0.1.0-py3-none-any.whl
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "Documentation accurately describes the closed DeepSeek Flash/Flash/Pro policy, no live OpenAI requirement, and no arbitrary model/endpoint."
    - "Operators can follow the full locked nomination → adversarial → benchmark/replay → changed source → fresh Gate B4/value Draft → human review → cleanup → rebuild chain."
    - "Docs preserve no untrusted execution, no secret inspection/persistence, fixed catalog, Draft-only, and human merge/approval/ready/cleanup boundaries."
    - "Release posture derives from current measured evidence: Gate B4 binds protected-workflow bytes independently, while the rebuilt wheel SHA-256 is release-document evidence only."
    - "A dedicated fail-closed verifier proves every authoritative SkillScout execution in the three protected workflows follows a preceding full-SHA checkout in the same job and uses the checked-out locked project environment rather than a registry, wheel, artifact, wrapper, or preinstalled command; the corrected publish-workflow heredoc was finalized in Plan 06-05 and bound unchanged in Plan 06-10."
  artifacts:
    - {path: dist/skillscout-0.1.0-py3-none-any.whl, provides: "Rebuilt exact Phase 6 release bytes"}
    - {path: RELEASE.md, provides: "Truthful controlled-pilot release posture"}
    - {path: tools/verify_phase6_source_execution.py, provides: "Closed structural verifier for protected-workflow source execution"}
    - {path: tests/test_phase6_source_execution.py, provides: "Mutation coverage for every forbidden acquisition, invocation, artifact, wrapper, and checkout-order path"}
  key_links:
    - {from: Phase 6 report/map, to: README/docs/RELEASE, via: "actual current evidence identities and limitations"}
    - {from: pyproject.toml and uv.lock, to: dist/skillscout-0.1.0-py3-none-any.whl and RELEASE.md, via: "offline repository-local rebuild plus exact SHA-256 equality"}
    - {from: protected workflow bytes, to: fresh Gate B4 evidence, via: "Plan 06-10 exact workflow hashes; no release-wheel consumption"}
    - {from: tools/verify_phase6_source_execution.py, to: discover/publish/canary workflow jobs, via: "ordered YAML job/step parsing plus a closed authoritative-invocation grammar"}
    - {from: .github/workflows/publish-candidate.yml inline bootstrap import, to: Plan 06-10 binding, via: "Plan 06-05 exact repository-local uv 0.11.29 `run --locked` launcher and setup/materialization contract"}
---

<objective>
Update operator/release documentation, rebuild exact package bytes, and run the complete locked release chain.

Purpose: Make the implemented acceptance path operable and ensure public release claims match current evidence rather than historical Phase 4/5 facts.
Output: Updated docs, rebuilt wheel digest, and one complete measured regression/release execution.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@docs/ARCHITECTURE.md
@docs/CONFIGURATION.md
@docs/DEVELOPMENT.md
@docs/TESTING.md
@RELEASE.md
@.planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md
@.planning/phases/06-adversarial-mvp-acceptance/06-RELEASE-REQUIREMENTS.json
</context>

<tasks>

<task type="auto">
  <name>Task 06-14-01: Document provider, architecture, and operating boundaries</name>
  <files>README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md</files>
  <read_first>README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md, .planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md, .planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md, src/skillscout/adapters/semantic_provider.py</read_first>
  <action>Update user-facing provider text from historical all-Flash to the exact DeepSeek extraction Flash / generation Flash / review Pro policy and official endpoint, while retaining recorded OpenAI as the default supported path and disclosing that Phase 6 live release evidence is DeepSeek-only. Document the trust flow from Search nomination and human lock through WorkflowSpec, validation, Pro advice, fresh Gate B4, fixed test catalog Draft, exact-head human verdict, human-only cleanup, and deterministic rebuild. List the exact finalized CLI commands/workflow actions and credentials by variable name only. State that no arbitrary model/endpoint/catalog, raw-corpus downstream flow, repository execution, automatic merge/approve/ready, or automation cleanup is supported.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_semantic_provider.py tests/test_phase6_workflow.py tests/test_phase6_acceptance.py -k 'docs or provider or command or boundary' -x &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Docs name exact Flash/Flash/Pro models and official URL with no configurable escape hatch.
    - Full authority chain and persistence boundary match code/workflow behavior.
    - No secret value, live private identity beyond approved evidence, or claim of universal provider validation appears.
  </acceptance_criteria>
  <done>Users and operators see the exact implemented trust and provider model.</done>
</task>

<task type="auto">
  <name>Task 06-14-02: Document development, testing, and truthful release posture</name>
  <files>docs/DEVELOPMENT.md, docs/TESTING.md, RELEASE.md</files>
  <read_first>docs/DEVELOPMENT.md, docs/TESTING.md, RELEASE.md, .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, .planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md, tools/verify_phase6_acceptance.py, tools/verify_phase6_validation_map.py</read_first>
  <action>Document the bounded stage-policy change process and evidence invalidation rules; list fast offline, kernel-isolated hosted, protected live, human/manual, cleanup, independent rebuild, and exact release-chain commands. Explain business rejection versus system failure and that live skips never become pass evidence. Update `RELEASE.md` only from the actual current report: identify controlled-pilot readiness or truthful FAIL/INCOMPLETE, current exact workflow/evidence/wheel identities, warnings, limitations including no live OpenAI campaign, open value Draft/human verdict, and remaining actions. Do not copy raw logs or hand-edit the release verdict.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Testing docs use only `.tools/uv-0.11.29/bin/uv run --locked pytest -q` for repository tests.
    - Release claims and limitations exactly match current verified report; historical Gate B4 is labeled historical.
    - Credential and secret-handling guidance forbids reading/persisting real values.
  </acceptance_criteria>
  <done>Developer and release documentation is executable and evidence-consistent.</done>
</task>

<task type="auto">
  <name>Task 06-14-03: Rebuild exact wheel and run the full locked release chain</name>
  <files>tools/verify_phase6_source_execution.py, tests/test_phase6_source_execution.py, dist/skillscout-0.1.0-py3-none-any.whl, RELEASE.md</files>
  <read_first>pyproject.toml, uv.lock, docs/TESTING.md, RELEASE.md, .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, .planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md, .planning/phases/06-adversarial-mvp-acceptance/06-05-SUMMARY.md, .planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, .github/workflows/phase6-acceptance.yml, tests/test_publication_security.py, tools/verify_phase6_source_execution.py, tests/test_phase6_source_execution.py, tools/verify_phase6_acceptance.py, tools/verify_phase6_validation_map.py, tests/test_phase6_workflow.py</read_first>
  <action>First prove the Plan 06-10 authority remains current: run the Phase 6 acceptance inspector against canonical evidence, recompute all four workflow SHA-256 values and require exact equality with the fresh Gate B4 binding, and require both staged and unstaged diffs for those workflows to be empty. Create `tools/verify_phase6_source_execution.py` and `tests/test_phase6_source_execution.py` without editing any protected workflow. The verifier owns the exact closed path set `.github/workflows/discover.yml`, `.github/workflows/publish-candidate.yml`, and `.github/workflows/gate-b4-canary.yml`; it safely parses YAML into ordered `jobs.*.steps`, rejects malformed, duplicate, or ambiguous structures, and identifies every authoritative SkillScout execution step, including `python -m skillscout.cli`, checked-out repository tools such as `tools/gate_b4_canary.py`, and inline Python that imports `skillscout`. For each authoritative step, require an earlier same-job `actions/checkout@{40 lowercase hex}` step with no external checkout path and require the invocation itself to match one closed anchored grammar: Python launched by `run --locked` through either `.tools/uv-0.11.29/bin/uv` or a bare `uv` whose same job has an earlier full-SHA `astral-sh/setup-uv` step pinned to version `0.11.29`; execution remains at the checked-out project root and resolves project code from the locked environment. For the publication workflow specifically, require the Plan 06-05 contract-tested setup/materialization steps to precede its exact repository-local uv launcher, and classify the heredoc importing `skillscout.bootstrap` as authoritative; a bare or unlocked launcher is a verification failure, not a workflow-edit opportunity. Reject every `pip install` acquisition path, `uvx`, `uv tool run`, `uv run --with` package injection, bare or preinstalled `skillscout`, wheel or `dist/` execution, `actions/download-artifact`, release, registry, or download sources used for execution, non-project working directories, command variables, shell aliases or functions, sourced or delegated wrapper scripts, and any unrecognized SkillScout invocation form. Fail closed when a step contains a SkillScout import or entry point but the verifier cannot classify one direct anchored invocation. Mutation tests must independently prove rejection for missing, late, or non-full-SHA checkout; missing, late, or unpinned setup-uv where bare uv is used; missing/late/wrong-version publication local-uv materialization; every forbidden acquisition and invocation form just listed; external artifact execution; aliases, functions, command variables, sourced wrappers, and indirect scripts; and unknown syntax. Positive fixtures cover every authoritative invocation in all three exact workflows and prove the verifier finds the expected complete step set rather than passing an empty scan. Run this semantic verifier against the exact unchanged post-Plan-06-10 bytes before release credit. The three workflows contain no release-wheel consumption, so Gate B4 binds their exact bytes independently and the wheel digest is release-document evidence only; do not create a wheel/workflow authority link. If a protected workflow ever must consume the wheel, stop: finalize that linkage before Plan 06-10 and run a new Gate B4 canary before publication credit. For the wheel, run `.tools/uv-0.11.29/bin/uv --no-cache --offline lock --check`, create a private `mktemp -d` staging directory, and run `.tools/uv-0.11.29/bin/uv --no-cache build --offline --no-sources --wheel --out-dir "$build_stage" .`. Require the staging directory to contain exactly one regular file named `skillscout-0.1.0-py3-none-any.whl`; fail on missing, extra, or differently named output. Before replacement, require `dist/` to contain no other `skillscout-*.whl`; replace only the controlled target and leave unrelated artifacts untouched. Compute SHA-256 from the installed wheel bytes and write exactly one `Release wheel SHA-256: \`{64 lowercase hex}\`` line in `RELEASE.md`, followed by the exact release-document-only sentence already specified by this plan. Do not claim that Plan 06-13's earlier deterministic report contains this post-report build hash. Finally, rebuild again in a fresh staging directory during automated verification and require byte-for-byte digest equality among the fresh build, the sole controlled `dist/` wheel, and the sole documented release digest before running the validation-map verifier, full locked pytest suite, Ruff, `git diff --check`, the source-execution verifier, and the acceptance inspector. Do not reuse partial results; on any failure leave release status non-passing. Record measured counts, durations, and hashes only. Do not read any `.env`, PEM, JWT, token, private-key, or credential material.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_source_execution.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py &amp;&amp; git diff --quiet -- .github/workflows/discover.yml .github/workflows/publish-candidate.yml .github/workflows/gate-b4-canary.yml .github/workflows/phase6-acceptance.yml &amp;&amp; git diff --cached --quiet -- .github/workflows/discover.yml .github/workflows/publish-candidate.yml .github/workflows/gate-b4-canary.yml .github/workflows/phase6-acceptance.yml &amp;&amp; .tools/uv-0.11.29/bin/uv --no-cache --offline lock --check &amp;&amp; build_stage="$(mktemp -d)" &amp;&amp; trap 'rm -rf "$build_stage"' EXIT &amp;&amp; .tools/uv-0.11.29/bin/uv --no-cache build --offline --no-sources --wheel --out-dir "$build_stage" . &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python -c 'import hashlib,re,sys; from pathlib import Path; stage=Path(sys.argv[1]); expected="skillscout-0.1.0-py3-none-any.whl"; built=list(stage.iterdir()); published=list(Path("dist").glob("skillscout-*.whl")); assert len(built)==1 and built[0].is_file() and built[0].name==expected, built; assert len(published)==1 and published[0].is_file() and published[0].name==expected, published; release=Path("RELEASE.md").read_text(encoding="utf-8"); documented=re.findall(r"Release wheel SHA-256: `([0-9a-f]{64})`", release); assert len(documented)==1, documented; digest=lambda path: hashlib.sha256(path.read_bytes()).hexdigest(); assert digest(built[0])==digest(published[0])==documented[0]; assert "release-document evidence only" in release and "Gate B4 independently binds" in release' "$build_stage" &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check . &amp;&amp; git diff --check &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py</automated></verify>
  <acceptance_criteria>
    - `tools/verify_phase6_source_execution.py` structurally finds every authoritative SkillScout execution in the exact discover, publish-candidate, and Gate B4 canary workflows; each has a preceding full-SHA same-job checkout and a closed locked checked-out-source invocation, and zero authoritative steps is a failure.
    - Mutation tests reject every registry/pip, uvx, uv tool, injected-package, preinstalled/bare command, wheel/dist, downloaded/release artifact, checkout-order, mutable-action, external-working-directory, alias/function/variable/wrapper, indirect-script, and unknown-invocation path.
    - The controlled `dist/` namespace contains exactly one `skillscout-*.whl`, named `skillscout-0.1.0-py3-none-any.whl`; a fresh offline locked rebuild produces the same SHA-256.
    - `RELEASE.md` contains exactly one lowercase 64-hex wheel digest and it equals both the installed wheel and the fresh verification build; missing, stale, multiple, or mismatched wheel evidence fails.
    - All four Gate B4-bound workflows remain unchanged in this task; the three protected execution workflows pass the dedicated source-execution verifier, contain no wheel consumption, and retain their independently verified Plan 06-10 byte binding; `RELEASE.md` does not claim a workflow-wheel linkage or that Plan 06-13's earlier report contains the wheel hash.
    - The current publish-workflow SHA-256 equals the corrected Plan 06-10 binding; its inline bootstrap import still uses the Plan 06-05 exact repository-local uv 0.11.29 `run --locked` launcher after setup/materialization, and any mismatch fails without editing the workflow.
    - One uninterrupted complete chain passes, or the release report truthfully remains failed.
    - No dependency graph, secret material, candidate code, remote authority, Draft state, or default branch is changed by the offline chain.
  </acceptance_criteria>
  <done>Current source, package bytes, docs, tests, maps, and release verdict agree under one measured locked chain.</done>
</task>

</tasks>

<threat_model>
## Assets
- Operator guidance, release claims, exact wheel bytes, workflow/evidence hashes, and full regression authority.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Verified report → human-readable docs/release | Documentation must not overclaim or stale-bind evidence. |
| Source/lock → release wheel | Stale or changed dependency bytes must not receive release credit. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-43 | Spoofing | release readiness claim | high | mitigate | Docs derive from current verified report and measured chain. |
| T-06-44 | Tampering | wheel/workflow identity | high | mitigate | Fresh offline wheel rebuild must equal the sole documented/installed SHA-256; Gate B4 separately binds unchanged protected-workflow bytes. |
| T-06-45 | Information Disclosure | docs/test output | high | mitigate | Secret names only; no values/raw logs. |
| T-06-46 | Spoofing / Tampering | protected workflow execution source | high | mitigate | Plan 06-05 finalizes the publish heredoc/setup contract before Gate B4; Plan 06-10 binds those bytes; this plan's closed structural verifier only reads them and rejects alternate acquisition, artifact, wrapper, invocation, or binding paths. |
| T-06-SC | Tampering | dependency supply chain | high | mitigate | Existing uv.lock and repository-local uv only; no install changes. |
</threat_model>

## Artifacts this phase produces
- Updated README and architecture/configuration/development/testing/release documentation
- `tools/verify_phase6_source_execution.py` and mutation-complete `tests/test_phase6_source_execution.py`
- Rebuilt `dist/skillscout-0.1.0-py3-none-any.whl` and its SHA-256 evidence
- Exact full locked release-chain command and measured result

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Operable, reviewable, truthful MVP acceptance | COVERED |
| REQ | TEST-01..04 | Documented evidence and final regression | COVERED |
| RESEARCH/AI-SPEC | — | Provider docs, pre-Gate-B4 workflow finalization, invalidation, release bytes, full chain | COVERED |
| CONTEXT | D-07..D-27 | Provider, verdict, Draft/human control, pre-Gate-B4 correction, freshness, security, report content | COVERED |

<verification>
Documentation checks, mutation-complete protected-workflow source-execution verification, pre/post acceptance inspection, unchanged protected-workflow checks, a fresh offline wheel rebuild with three-way SHA-256 equality, validation-map verification, full pytest, Ruff, and diff check run as one complete locked chain.
</verification>

<success_criteria>
The repository’s public documentation, package bytes, canonical evidence, and final release posture are exact, current, and reproducible.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-14-SUMMARY.md` with the measured final chain and wheel digest.</output>
