---
phase: 06-adversarial-mvp-acceptance
plan: "10"
type: execute
wave: 8
depends_on: [06-09]
files_modified:
  - .github/workflows/phase6-acceptance.yml
  - .planning/phases/06-adversarial-mvp-acceptance/06-GATE-B4-EVIDENCE.json
  - tests/test_gate_b4_canary.py
  - tests/test_gate_b4_canary_workflow.py
  - tests/test_phase6_acceptance.py
autonomous: false
requirements: [TEST-03, TEST-04]
user_setup:
  - service: github
    why: "Fresh least-privilege canary and Draft-only value publication"
    env_vars:
      - {name: SKILLSCOUT_GITHUB_APP_ID, source: "Protected environment secret; value not inspected"}
      - {name: SKILLSCOUT_GITHUB_APP_PRIVATE_KEY, source: "Protected environment secret; value not inspected"}
must_haves:
  truths:
    - "D-18 only `alexzhu0/skillscout-catalog-test` is admitted; no runtime catalog choice exists."
    - "D-22 fresh Gate B4 immediately precedes publication credit and binds exact discover/publish/canary/Phase6 workflow bytes plus platform identities."
    - "D-23 any bound byte or App/catalog/ruleset/environment/reviewer/installation change invalidates the evidence."
    - "D-19/D-21 create one open Draft, replay with zero duplicate effects, then update the same PR for the approved changed source."
    - "D-20 value Draft and probe PRs/branches remain separate; automation has no cleanup, merge, approve, ready, or default-write route."
  artifacts:
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-GATE-B4-EVIDENCE.json, provides: "Fresh exact-bound permission evidence"}
  key_links:
    - {from: fresh GateB4BindingV1, to: value publication, via: "exact equality immediately before catalog token minting"}
    - {from: old/new candidate lineage, to: one open Draft, via: "publication key, marker, desired revision, remote reconciliation"}
---

<objective>
Run a fresh exact-bound Gate B4 canary, then create/replay/update one separate value-bearing open Draft.

Purpose: Complete platform least-privilege and remote idempotency evidence without widening GitHub App authority.
Output: Fresh causal denials, one real Draft, zero-effect publication replay, same-PR changed-source update, and a separate cleanup manifest.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tools/gate_b4_canary.py
@.github/workflows/discover.yml
@.github/workflows/publish-candidate.yml
@.github/workflows/gate-b4-canary.yml
@.github/workflows/phase6-acceptance.yml
@src/skillscout/application/publication.py
@src/skillscout/adapters/github_publish.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 06-10-01: Authorize fresh Gate B4 and exact value publication</name>
  <files>.github/workflows/phase6-acceptance.yml</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-09-SUMMARY.md, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, .github/workflows/phase6-acceptance.yml, tools/gate_b4_canary.py, AGENTS.md</read_first>
  <action>Pause before GitHub App credential resolution. Present SHA-256 for all four bound workflows, source HEAD, App installation and scope identifiers, fixed catalog `alexzhu0/skillscout-catalog-test`, ruleset digest, protected environment, individual reviewer configuration, installation identity, original/changed candidate authorities, planned branch/marker, positive/negative probes, and separate cleanup boundary. Request one authorization covering fresh Gate B4 plus initial value Draft, exact replay, and approved same-Draft update. Do not display credential values. Authorization grants no merge, approval, ready, default-branch write, arbitrary repository, secret read, or cleanup.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_gate_b4_canary.py tests/test_gate_b4_canary_workflow.py tests/test_phase6_acceptance.py -k 'preflight or binding or publication_authority' -x</automated></verify>
  <acceptance_criteria>
    - Approval is bound to exact current bytes/identities and occurs before token minting.
    - Any mismatch/staleness blocks instead of reusing Phase 4/5 historical evidence.
    - Publication target and effects are exact and bounded.
  </acceptance_criteria>
  <done>Fresh canary and value publication are explicitly authorized.</done>
</task>

<task type="auto">
  <name>Task 06-10-02: Run fresh Gate B4 and bind causal denials</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-GATE-B4-EVIDENCE.json, tests/test_gate_b4_canary.py, tests/test_gate_b4_canary_workflow.py</files>
  <read_first>tools/gate_b4_canary.py, tests/test_gate_b4_canary.py, tests/test_gate_b4_canary_workflow.py, .planning/phases/04-controlled-draft-pr/04-10-SUMMARY.md, .planning/phases/05-automated-discovery-operations/05-10-SUMMARY.md</read_first>
  <action>After approval, run the existing preflight before token minting, then the exact fresh canary. Record positive Draft/reviewer behavior and causal denials for default-ref write, merge, ruleset mutation/admin, unauthorized repository, and secret access, plus unchanged default branch and production-surface absence of approve/ready. Bind the exact four workflow hashes and all platform identities in `GateB4BindingV1`; historical Phase 4/5 facts remain labeled history only. Write `06-GATE-B4-EVIDENCE.json` with bounded non-secret facts and a human/admin cleanup manifest, but perform no cleanup. Any unexpected allowed effect, missing probe, stale binding, or secret scan hit blocks publication.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_gate_b4_canary.py tests/test_gate_b4_canary_workflow.py tests/test_phase6_acceptance.py -k 'gate_b4 or causal or stale or cleanup_manifest' -x</automated></verify>
  <acceptance_criteria>
    - Fresh evidence matches exact current workflow/platform identities and follows the approval.
    - Default SHA is unchanged and all forbidden effects are causally denied or production-surface-proven absent as specified.
    - Probe artifacts are distinct from the value Draft and only listed for later human cleanup.
  </acceptance_criteria>
  <done>Fresh exact-bound least-privilege evidence is current and publication-admissible.</done>
</task>

<task type="auto">
  <name>Task 06-10-03: Create, replay, and changed-source update one open value Draft</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/application/publication.py, src/skillscout/adapters/github_publish.py, src/skillscout/adapters/publication_state.py, src/skillscout/bootstrap.py, .planning/phases/06-adversarial-mvp-acceptance/06-GATE-B4-EVIDENCE.json, .planning/phases/06-adversarial-mvp-acceptance/06-09-SUMMARY.md</read_first>
  <action>Revalidate fresh Gate B4 immediately before protected catalog token minting. Publish the original eligible value candidate as a Draft to the fixed catalog and request the configured individual reviewer. Re-run the identical publication authority and prove zero new branch/commit/PR/reviewer request. Then publish the approved changed-source candidate through the existing publication key/marker/desired-revision and remote-reconciliation path; require the same PR number/head branch, a new exact head commit, preserved old-to-new source lineage, and no duplicate Draft. Leave the resulting value PR open and Draft. Keep probe PRs/branches separate and uncleaned. Never call merge, approve, review submission, ready, force, delete, ruleset/admin, or default-branch mutation.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_publication_recovery.py tests/test_publication_security.py -k 'value_draft or publication_replay or changed_source or same_draft or forbidden' -x</automated></verify>
  <acceptance_criteria>
    - One distinct open Draft exists in the fixed catalog and requests the configured human reviewer.
    - Exact replay has zero new branch/commit/PR/reviewer effects.
    - Changed source updates the same open Draft/branch with exact lineage and current head; no duplicate appears.
    - Value Draft remains open/Draft and probe cleanup remains pending human/admin action.
  </acceptance_criteria>
  <done>Remote publication idempotency/update and one real human-reviewable Draft are proven.</done>
</task>

</tasks>

<threat_model>
## Assets
- GitHub App token boundary, workflow/platform bindings, catalog default branch, value Draft lineage, probe artifacts, and reviewer routing.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected identities → installation token | Stale/mismatched authority must fail before minting. |
| Candidate/lineage → catalog mutations | Only one owned branch/subtree/Draft may change. |
| Automation → cleanup/merge/review controls | Forbidden effects must remain structurally absent and platform denied. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-29 | Elevation of Privilege | GitHub App effects | critical | mitigate | Fresh causal Gate B4 and closed adapter routes. |
| T-06-30 | Tampering | Draft/default branch | high | mitigate | Fixed catalog/branch/marker, non-force update, unchanged default proof. |
| T-06-31 | Repudiation | replay/update | high | mitigate | Remote reread, publication records, exact effect counters. |
| T-06-32 | Information Disclosure | App/private key/secrets | critical | mitigate | Protected late minting; bounded evidence; no value reads. |
</threat_model>

## Artifacts this phase produces
- `06-GATE-B4-EVIDENCE.json`
- Fresh `GateB4BindingV1`
- One open value Draft, machine branch/marker, original and changed desired revisions
- Publication `ReplayEvidenceV1` and completed `ChangedSourceEvidenceV1`
- Human-only probe cleanup manifest

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Permission-bounded real Draft | COVERED |
| REQ | TEST-03, TEST-04 | No duplicates, same-Draft update, real Draft and denials | COVERED |
| RESEARCH | — | Fresh canary, fixed catalog, exact-head value lifecycle | COVERED |
| CONTEXT | D-18..D-23,D-25..D-27 | Catalog, Draft/update, cleanup separation, freshness, evidence/report | COVERED |

<verification>
Fresh canary tests and publication recovery/security suites pass; live facts show causal denials, one Draft, zero-effect replay, and same-Draft update.
</verification>

<success_criteria>
Current exact authority can create/update only the intended open Draft while forbidden platform effects remain denied and probe cleanup remains human-only.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md` with non-secret fresh canary and value-Draft identities.</output>
