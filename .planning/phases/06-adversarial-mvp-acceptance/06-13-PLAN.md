---
phase: 06-adversarial-mvp-acceptance
plan: "13"
type: execute
wave: 11
depends_on: [06-12]
files_modified:
  - tools/verify_phase6_acceptance.py
  - tools/verify_phase6_validation_map.py
  - tests/test_phase6_acceptance.py
  - .planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md
  - .planning/phases/06-adversarial-mvp-acceptance/06-RELEASE-REQUIREMENTS.json
  - .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-11 PASS is the conjunction of every named hard gate and a positive human verdict; warnings cannot waive it."
    - "D-26 main branch retains only concise report/map/benchmark while canonical redacted evidence stays on state branch and raw diagnostics are non-canonical."
    - "D-27 report contains every required identity, count, budget, telemetry, outcome, replay/update, canary, human, warning, limitation, and recommendation field."
    - "Every one of all 44 v1 requirements maps both directions to exact evidence and gate consequence."
    - "Independent stdlib tools rebuild report/map byte-for-byte and fail under every mutation."
    - "TEST-03/report credit requires independently rebuilding the distinct post-publication replay and changed-source Draft-update completion facts, not inferring completion from their earlier intent facts."
  artifacts:
    - {path: tools/verify_phase6_acceptance.py, provides: "Final independent hard-gate/rebuild inspector"}
    - {path: tools/verify_phase6_validation_map.py, provides: "Exact Phase 6 task and all-44 inverse-map verifier"}
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md, provides: "Concise deterministic whole-MVP verdict"}
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-RELEASE-REQUIREMENTS.json, provides: "Exact all-44 evidence map"}
  key_links:
    - {from: verified acceptance evidence root, to: report/map bytes, via: "deterministic renderer and independent byte comparison"}
---

<objective>
Render and independently verify the deterministic Phase 6 report and exact all-44 requirement map.

Purpose: Convert complete canonical facts into a reproducible release recommendation without self-certification, mutable logs, or score-based waiver.
Output: Final stdlib inspectors, mutation suite, concise report, inverse map, and executed Nyquist validation record.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tools/verify_phase5_acceptance.py
@tools/verify_phase5_validation_map.py
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md
@.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-13-01: Finalize independent hard-gate and validation-map verifiers</name>
  <files>tools/verify_phase6_acceptance.py, tools/verify_phase6_validation_map.py, tests/test_phase6_acceptance.py</files>
  <read_first>tools/verify_phase6_acceptance.py, tools/verify_phase5_acceptance.py, tools/verify_phase5_validation_map.py, tests/test_phase6_acceptance.py, tests/test_phase5_acceptance.py, .planning/REQUIREMENTS.md, .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md</read_first>
  <action>Finalize both standard-library-only, read-only, network-free, project-import-free verifiers. The acceptance registry must independently require all named hard gates, exact evidence roots, current workflow/platform/Draft/attestation/cleanup bindings, source-surface prohibitions, and deterministic report/map rebuild. For TEST-03, load the canonical operations export and independently validate one `PublicationReplayCompletionV1` under `acceptance_publication_replay_completion` and one `ChangedSourceDraftUpdateCompletionV1` under `acceptance_changed_source_draft_update_completion`; require their exact schemas, self-digests, intent/receipt links, natural identities, source/policy/fingerprint/authority/marker/target/PR/head fields, zero replay effects, exact update effects, previous/new lineage/revision/head rules, separate per-kind projections, and export → rebuild → fresh export equality. Reject a report that uses only `ReplayEvidenceV1`/`ChangedSourceEvidenceV1`, planned lineage, mutable publication state, or summaries as completion authority. The validation-map verifier must parse all 14 plans and exact task commands, TEST-01..TEST-04 inverse coverage, and all 44 v1 requirement IDs/evidence consequences. Extend isolated-tree mutation tests for every registry row, source path, requirement, inverse link, workflow digest, benchmark identity, intent/completion swap or omission, completion kind/model/schema/digest/natural-identity/count/lineage/PR/head mutation, canary binding, Draft head/verdict, cleanup target, report byte, and self-reference attempt. Never recursively scan secret paths or trust a declared success flag.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'verifier or validation_map or mutation or read_only or all_44' -x</automated></verify>
  <acceptance_criteria>
    - Both tools reject missing/swapped/duplicate/stale/self-referential evidence and produce stable diagnostics only.
    - Exact plan/task/requirement mappings have no orphan in either direction.
    - Source scans prove no untrusted execution, arbitrary endpoint/model/catalog, secret read, merge/approve/ready/default-write, or cleanup route.
  </acceptance_criteria>
  <done>Independent tools causally require every release gate and every requirement mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-13-02: Render exact report and all-44 inverse map from canonical evidence</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-ACCEPTANCE-REPORT.md, .planning/phases/06-adversarial-mvp-acceptance/06-RELEASE-REQUIREMENTS.json, tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/application/acceptance.py, src/skillscout/domain/acceptance.py, .planning/REQUIREMENTS.md, .planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json, tools/verify_phase6_acceptance.py, tests/test_phase6_acceptance.py</read_first>
  <action>Use `skillscout rebuild-acceptance` against the exact verified state commit/root to first reconstruct and validate the two post-effect completion facts and their distinct operations projections, then evaluate every named hard gate into strict `AcceptanceGateResultV1` facts with exact kind `acceptance_gate`, construct and persist one `AcceptanceEvidenceRootV1` with exact kind `acceptance_report_root`, and rebuild both typed projections before deterministically rendering the checked-in report and requirement map. TEST-03 and report credit require the rebuilt `PublicationReplayCompletionV1` and `ChangedSourceDraftUpdateCompletionV1` identities/effects/lineage/PR-head facts; pre-publication intent facts alone are insufficient. `AcceptanceReleaseVerdictV1` is a deterministic projection of that rebuilt root and is not persisted as a second self-certifying authority. Per D-27 include manifest/version/digest, five fixed IDs/SHAs/licenses, funnel counts, reader budgets, actual stage/model/request/token/latency/cost telemetry, terminal and expected-vs-observed results, controlled scenario coverage, semantic replay intent plus publication replay completion, changed-source intent plus same-Draft update completion, exact fresh canary bindings/denials, current exact-head human verdict, cleanup attestation, warnings with impact/follow-up, known limitations including no live OpenAI campaign, and final recommendation. The JSON must contain exactly all 44 requirement IDs with evidence IDs/gates and an exact inverse evidence map. Derive the verdict from hard gates; never hand-edit a conclusion or fabricate a missing value.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'report or requirement_map or rebuild or warning or recommendation' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py</automated></verify>
  <acceptance_criteria>
    - Rebuilding twice from the same root produces byte-identical Markdown and JSON.
    - Every D-27 field and exactly 44 requirement IDs are present; no secret, raw corpus, raw provider body, or raw diagnostic output appears.
    - Report says PASS only if all hard gates and positive human verdict pass; otherwise it truthfully says FAIL/INCOMPLETE.
  </acceptance_criteria>
  <done>The main branch has a concise deterministic report and exact all-44 evidence map.</done>
</task>

<task type="auto">
  <name>Task 06-13-03: Close the Phase 6 Nyquist validation contract</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, .planning/phases/06-adversarial-mvp-acceptance/06-01-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-10-SUMMARY.md, .planning/phases/06-adversarial-mvp-acceptance/06-12-SUMMARY.md, tools/verify_phase6_validation_map.py, tools/verify_phase6_acceptance.py</read_first>
  <action>Update every exact task row with executed command/evidence status and retain explicit human checkpoint identities. Set `wave_0_complete:true` only if Wave 0 RED contracts and hosted isolation evidence exist; set `nyquist_compliant:true` only if every task has passing automated feedback or its required exact human dependency plus passing post-ingest tests. Record the exact offline release command and measured results without hard-coded recalled counts. Run both independent verifiers after the update; any missing live/manual evidence remains failed rather than checked off.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py</automated></verify>
  <acceptance_criteria>
    - No provisional ID/status remains and every human gate has exact evidence identity.
    - Validation map and report/map verifiers independently pass from current files/evidence.
    - Measured commands/results are actual execution evidence, not recalled baselines.
  </acceptance_criteria>
  <done>Phase 6 validation is exact, current, and independently reproducible.</done>
</task>

</tasks>

<threat_model>
## Assets
- Canonical evidence root, hard-gate registry, all-44 map, report bytes, release verdict, and validation history.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Canonical state → main-branch report | Renderer must not omit or alter blocking evidence. |
| Production renderer → independent verifiers | Shared bugs/self-certification must be prevented. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-39 | Tampering | report/map | high | mitigate | Independent exact-byte rebuild and mutation suite. |
| T-06-40 | Repudiation | requirement evidence | high | mitigate | Exact forward/inverse 44-ID mapping. |
| T-06-41 | Information Disclosure | verifier reads/output | high | mitigate | Fixed allowlist, byte bounds, stable diagnostics. |
| T-06-42 | Spoofing | PASS recommendation | critical | mitigate | Named hard-gate conjunction; no declared-success trust. |
</threat_model>

## Artifacts this phase produces
- Final `CHECK_REGISTRY` / required gate identifiers
- `tools/verify_phase6_validation_map.py`
- `06-ACCEPTANCE-REPORT.md`
- `06-RELEASE-REQUIREMENTS.json`
- Final exact `06-VALIDATION.md`
- CLI-generated report/map bytes from `rebuild-acceptance`

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Human-reviewable repeatable acceptance verdict | COVERED |
| REQ | TEST-01..04 plus all 44 | Exact evidence and inverse map | COVERED |
| RESEARCH/AI-SPEC | — | Evidence-first report, independent rebuild, hard-gate decision rule | COVERED |
| CONTEXT | D-11..D-17,D-21..D-27 | Verdict, human review, update, fresh canary, persistence/report content | COVERED |

<verification>
Mutation tests, acceptance inspector, validation-map verifier, and byte-exact rebuild all pass from current canonical evidence.
</verification>

<success_criteria>
The release recommendation is a deterministic projection of complete evidence and all 44 requirements, not an editable claim.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-13-SUMMARY.md`.</output>
