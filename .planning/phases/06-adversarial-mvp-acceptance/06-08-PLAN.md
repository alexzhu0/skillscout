---
phase: 06-adversarial-mvp-acceptance
plan: "08"
type: execute
wave: 6
depends_on: [06-07]
files_modified:
  - .github/workflows/phase6-acceptance.yml
  - tests/test_phase6_acceptance.py
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]
user_setup:
  - service: deepseek
    why: "Protected live extraction, generation, and independent review"
    env_vars:
      - {name: DEEPSEEK_API_KEY, source: "Protected workflow secret; value must not be inspected"}
  - service: github
    why: "Fixed-SHA source reads and canonical state persistence"
    env_vars:
      - {name: SKILLSCOUT_SOURCE_GITHUB_TOKEN, source: "Protected workflow runtime"}
      - {name: SKILLSCOUT_STATE_GITHUB_TOKEN, source: "Protected workflow runtime"}
must_haves:
  truths:
    - "Exactly the human-locked five fixed-SHA repositories traverse real Search-derived admission to complete publication decisions."
    - "D-07..D-10 use DeepSeek Flash/Flash/Pro only; absence of live OpenAI is a disclosed non-blocking warning."
    - "D-11..D-15 business rejection counts only at its correct structured boundary; any system failure blocks."
    - "Identical replay produces zero new semantic requests, WorkflowSpecs, Skills, candidate facts, or later remote publication effects."
  artifacts:
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Authorized live benchmark and exact replay jobs"}
  key_links:
    - {from: locked manifest, to: live campaign state root, via: "exact nomination/manifest/provider/prompt/schema/policy admission"}
    - {from: original campaign root, to: replay evidence, via: "verified completed lookup and zero-effect comparison"}
---

<objective>
Run the authorized DeepSeek live benchmark and an identical replay over the exact human-locked manifest.

Purpose: Produce real funnel, budget, telemetry, terminal, and semantic idempotency evidence without OpenAI or publication authority.
Output: Five complete fixed-SHA scenario facts and content-addressed zero-effect replay evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json
@src/skillscout/application/acceptance.py
@src/skillscout/application/discovery.py
@src/skillscout/adapters/semantic_provider.py
@.github/workflows/phase6-acceptance.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 06-08-01: Authorize exact live benchmark credential use</name>
  <files>.github/workflows/phase6-acceptance.yml, .planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json, .github/workflows/phase6-acceptance.yml, src/skillscout/adapters/semantic_provider.py, AGENTS.md</read_first>
  <action>Pause before any live credential resolution. Present the exact source/workflow/manifest/provider-policy/prompt/schema/query/state identities, five fixed repository IDs/SHAs/licenses, read and 100/20 semantic budgets, and expected maximum effects. Request approval for one live benchmark plus one exact replay using `DEEPSEEK_API_KEY` and bounded GitHub source/state credentials. This approval requires no OpenAI key and grants no catalog token, Gate B4, Draft publication, changed-lineage mutation, human attestation, or cleanup.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase6_workflow.py -k 'live_authority or exact_manifest or deepseek_only or no_catalog' -x</automated></verify>
  <acceptance_criteria>
    - Approval binds exact immutable identities and bounded call/read effects.
    - Protected job verifies all non-secret authority before reading any credential.
    - No credential value is opened, printed, copied, scanned, or persisted.
  </acceptance_criteria>
  <done>The exact DeepSeek benchmark/replay operation is authorized.</done>
</task>

<task type="auto">
  <name>Task 06-08-02: Run the five-repository live campaign</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, src/skillscout/application/acceptance.py, src/skillscout/application/discovery.py, src/skillscout/adapters/operations_state.py, tests/test_phase6_acceptance.py</read_first>
  <action>After approval, dispatch `run-benchmark` at the exact source SHA. Revalidate Search nomination, locked manifest, provider stage map, prompt/schema/policy identities, state head/root, and repository fixed SHA/license before each real path. Run the production discovery → filter → read → extract → qualification → generation → validation → Pro review path, stopping each entry at its actual structured boundary. Persist each terminal and its candidate funnel, reader order/budgets, stage outcomes, request IDs, actual model, token/latency usage, prompt/schema/policy versions, expected-vs-observed evaluator comparison, and final publication decision inside strict `AcceptanceScenarioResultV1` facts with exact kind `acceptance_scenario`; reuse existing typed semantic-attempt facts rather than introduce an unowned telemetry fact type. Never send expected labels to a semantic request, run repository code, or treat provider/schema exhaustion as a negative outcome.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'benchmark or five_repositories or evaluator_blind or terminal or telemetry' -x</automated></verify>
  <acceptance_criteria>
    - Exactly five locked entries have complete terminal evidence and fixed identities.
    - At least one positive path is eligible for later value publication; otherwise the hard value gate remains failed rather than relabeled.
    - All system failures are release blockers and every valid rejection has a complete closed reason at the correct stage.
  </acceptance_criteria>
  <done>The real benchmark has complete, redacted, content-addressed live evidence.</done>
</task>

<task type="auto">
  <name>Task 06-08-03: Prove exact semantic replay idempotency</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/application/acceptance.py, src/skillscout/application/discovery.py, src/skillscout/adapters/state.py, src/skillscout/adapters/operations_state.py, tests/test_phase6_acceptance.py</read_first>
  <action>Dispatch `run-replay` against the exact prior state commit/root and identical repo/SHA/workflow/provider/prompt/schema/policy authorities. Require verified completed lookup before opening mutable state. Compare stage attempts, semantic request counts, WorkflowSpec authorities, package/Skill identities, reservation facts, state objects, and eligible locators; persist `ReplayEvidenceV1` with exact kind `acceptance_replay`. Expect zero new provider calls, zero duplicate WorkflowSpecs/Skills/facts, and no branch/PR/reviewer effects because publication authority is absent. Any mismatch, mutable miss, or unconfirmed state is a blocking system failure.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'replay or idempotent or zero_effect' -x</automated></verify>
  <acceptance_criteria>
    - Replay evidence binds exact before/after roots and every zero-effect counter.
    - Repeated invocation reconstructs stored projections without new semantic attempt rows.
    - Publication counters remain zero and cannot be claimed complete until Plan 06-10.
  </acceptance_criteria>
  <done>Identical semantic authority is reused with zero new semantic or remote effects.</done>
</task>

</tasks>

<threat_model>
## Assets
- DeepSeek/GitHub credentials, fixed repository evidence, semantic attempt budget, live state root, and replay authority.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Locked manifest → live adapters | Stale or substituted authority must fail before credentials. |
| Provider response → typed terminal evidence | Malformed or unknown outcomes must not count as business evidence. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-22 | Spoofing | live campaign authority | high | mitigate | Exact manifest/state/provider/policy re-admission. |
| T-06-23 | Repudiation | provider calls/telemetry | high | mitigate | One durable attempt/request and actual model/usage facts. |
| T-06-24 | Tampering | replay result | high | mitigate | Verified completed lookup and full before/after comparison. |
| T-06-25 | Information Disclosure | credentials/prompts | critical | mitigate | Late protected injection, bounded inputs, redacted facts. |
</threat_model>

## Artifacts this phase produces
- Workflow actions `run-benchmark` and `run-replay`
- Five live `AcceptanceScenarioResultV1` facts
- One `ReplayEvidenceV1`
- Funnel/read/token/latency/model/prompt/schema/policy fields inside `AcceptanceScenarioResultV1` plus existing typed semantic-attempt facts

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Real-repository value and idempotency evidence | COVERED |
| REQ | TEST-01, TEST-02, TEST-03 | Five repositories, terminal mix, identical replay | COVERED |
| RESEARCH/AI-SPEC | — | Live DeepSeek campaign, evaluator blindness, telemetry | COVERED |
| CONTEXT | D-07..D-15,D-24..D-27 | Provider, hard gates, adversarial linkage, report facts | COVERED |

<verification>
Authorized live campaign and replay produce strict canonical facts; focused tests independently validate completeness and zero effects.
</verification>

<success_criteria>
Five exact repositories reach structured decisions and identical authority reuses completed evidence with zero new semantic effects.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-08-SUMMARY.md` with non-secret live evidence identities only.</output>
