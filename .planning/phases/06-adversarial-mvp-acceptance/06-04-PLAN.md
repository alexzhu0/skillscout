---
phase: 06-adversarial-mvp-acceptance
plan: "04"
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/skillscout/domain/acceptance.py
  - src/skillscout/adapters/operations_state.py
  - tests/test_acceptance_domain.py
  - tests/test_operations_state.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-01..D-06 benchmark and nomination facts are strict, versioned, self-digested, and human-lock/revision bound."
    - "D-11 requires every named hard gate plus positive human review for PASS."
    - "D-12 credits only complete structured filter/no-workflow/qualification/validation/reviewer business rejections at their correct boundaries."
    - "D-13 makes provider/schema exhaustion, missing evidence, duplicate effects, unauthorized effects, secret exposure, execution, harness failure, and rebuild failure release-blocking system failures."
    - "D-14 permits only explicit non-security warnings with impact and follow-up; D-15 forbids waiving any hard gate by score or override."
    - "D-16/D-17 human review binds the exact open Draft head and complete usefulness/fidelity/provenance/safety/diff checklist."
    - "D-26 canonical acceptance facts live in the existing operations owner and rebuild from content-addressed JSON; no fourth store is created."
  artifacts:
    - {path: src/skillscout/domain/acceptance.py, provides: "Strict Phase 6 acceptance domain"}
    - {path: src/skillscout/adapters/operations_state.py, provides: "Acceptance fact persistence and rebuild in the operations owner"}
  key_links:
    - {from: acceptance domain models, to: OperationsStateStore, via: "canonical JSON and typed fact kinds"}
    - {from: AcceptanceEvidenceRootV1, to: release verdict, via: "named hard-gate registry, never aggregate score"}
---

<objective>
Implement the strict acceptance domain and persist its redacted facts through the existing canonical operations-state boundary.

Purpose: Give nomination, campaign, replay/update, canary, human, cleanup, and report stages one immutable schema vocabulary without logs or a fourth state store.
Output: Versioned acceptance models, canonical digests, operations fact APIs, and mutation/rebuild tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/domain/discovery.py
@src/skillscout/domain/publication.py
@src/skillscout/adapters/operations_state.py
@tests/test_discovery_domain.py
@tests/test_operations_state.py
@.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-04-01: Implement strict benchmark, scenario, attestation, and verdict models</name>
  <files>src/skillscout/domain/acceptance.py, tests/test_acceptance_domain.py</files>
  <read_first>src/skillscout/domain/discovery.py, src/skillscout/domain/publication.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, tests/test_acceptance_domain.py, tests/test_discovery_domain.py</read_first>
  <behavior>
    - Manifest entries bind public repository full name, numeric repository ID, 40-hex SHA, admitted SPDX license, selection source, intended evaluator role, Search nomination evidence, and self-digest.
    - Terminal classes distinguish eligible/valid business rejection/system failure with closed reasons and gate consequences.
    - Replay/update/canary/human/cleanup evidence binds exact old/new authorities, workflow/platform bytes, Draft head, and self-digests.
  </behavior>
  <action>Turn Task 06-01-01 GREEN with `StrictFrozenModel` subclasses and canonical self-digests. Implement `BenchmarkSelectionSource`, `BenchmarkCoverageRole`, `BenchmarkEntryV1`, `NominationSetV1`, `BenchmarkLockAttestationV1`, `LockedBenchmarkManifestV1`, `AcceptanceTerminalClass`, `AcceptanceScenarioResultV1`, `ReplayEvidenceV1`, `ChangedSourceEvidenceV1`, `GateB4BindingV1`, `HumanSkillReviewAttestationV1`, `ProbeCleanupAttestationV1`, `AcceptanceGateResultV1`, `AcceptanceEvidenceRootV1`, and `AcceptanceReleaseVerdictV1`. Enforce D-03’s exact five-role distribution and D-11..D-17 hard-gate semantics. Human identity fields are bounded non-secret identifiers; no signature/key scheme is introduced. Expected labels live in evaluator-only models that semantic-input models cannot contain.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_domain.py -x</automated></verify>
  <acceptance_criteria>
    - Strict validation rejects unknown fields, stale digests, invalid distribution, wrong SHA/license, stale head, incomplete D-17 checklist, and positive verdict without all blocking gates.
    - `business_terminal` includes deterministic filter/no-workflow/qualification/validation/reviewer rejection; provider/schema/evidence/harness failures are `system_failure`.
    - Warnings contain bounded impact and follow-up and cannot alter hard-gate status.
  </acceptance_criteria>
  <done>The complete acceptance vocabulary is strict, immutable, and independently digestible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-04-02: Persist acceptance facts in the operations owner</name>
  <files>src/skillscout/adapters/operations_state.py, tests/test_operations_state.py</files>
  <read_first>src/skillscout/adapters/operations_state.py, src/skillscout/domain/acceptance.py, src/skillscout/domain/discovery.py, tests/test_operations_state.py, src/skillscout/adapters/state_branch.py</read_first>
  <behavior>
    - Acceptance facts append as canonical content-addressed operations-owned facts and rebuild into the same typed projection.
    - Fact kinds cover nomination, benchmark lock/revision, scenario terminal, replay, changed lineage, canary binding, human review, cleanup, gate result, and report root.
    - Raw repository text, provider bodies, logs, credentials, and Actions diagnostics are not accepted.
  </behavior>
  <action>Extend the operations schema and `_FactKind`/`_FACT_TABLES` ownership with one bounded acceptance-fact table and exact typed APIs `record_acceptance_fact` and `acceptance_snapshot`; do not create another SQLite owner or duplicate pipeline/publication schemas. Export/rebuild must include acceptance rows in contiguous canonical sequence with exact payload digest, typed schema revalidation, projection equality, and existing state-branch three-store assembly. Mutation-test missing, swapped, duplicate, noncanonical, cross-run, stale-root, and valid-DB/JSON-disagreement cases. Persist only redacted model JSON and exact public identifiers; reject keys such as raw corpus, response body, authorization, token, private key, or unrestricted path.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_operations_state.py -k 'acceptance or export or rebuild or canonical' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/domain/acceptance.py src/skillscout/adapters/operations_state.py</automated></verify>
  <acceptance_criteria>
    - Export → rebuild → fresh export is byte/field equal for every acceptance fact kind.
    - Any missing, reordered, duplicated, mutated, cross-run, or untyped fact fails before report or live authority.
    - Existing discovery projection and three-store tests remain green.
  </acceptance_criteria>
  <done>Canonical state can retain and rebuild every Phase 6 acceptance fact without retaining untrusted or secret text.</done>
</task>

</tasks>

<threat_model>
## Assets
- Benchmark lock, evidence roots, replay/update lineage, canary bindings, human attestations, and release verdict.

## Trust Boundaries
| Boundary | Description |
|---|---|
| External/public identities → strict domain | Moving or forged identities must not become authority. |
| Typed acceptance facts → operations state branch | Mutated or incomplete evidence must not rebuild. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-10 | Tampering | evidence facts | high | mitigate | Canonical self-digests, typed revalidation, exact rebuild. |
| T-06-11 | Repudiation | human/canary evidence | high | mitigate | Exact identity/head/workflow/platform binding. |
| T-06-12 | Information Disclosure | durable state | high | mitigate | Closed schemas and forbidden-field tests. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package changes. |
</threat_model>

## Artifacts this phase produces
- All named acceptance domain models and enums
- Operations fact kinds `acceptance_nomination`, `acceptance_benchmark_lock`, `acceptance_scenario`, `acceptance_replay`, `acceptance_changed_source`, `acceptance_gate_b4`, `acceptance_human_review`, `acceptance_cleanup`, `acceptance_gate`, `acceptance_report_root`
- `OperationsStateStore.record_acceptance_fact`
- `OperationsStateStore.acceptance_snapshot`

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Audit-ready acceptance evidence | COVERED |
| REQ | TEST-01..04 | All Phase 6 evidence forms | COVERED |
| RESEARCH/AI-SPEC | — | Strict contracts, evidence first, existing state owner | COVERED |
| CONTEXT | D-01..D-06,D-11..D-17,D-21..D-27 | Benchmark, hard gates, lineage, attestations, persistence/report | COVERED |

<verification>
Domain and state mutation/rebuild tests pass; existing three-store ownership remains intact.
</verification>

<success_criteria>
Every future Phase 6 action consumes and emits typed, redacted, content-addressed facts that independently rebuild.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-04-SUMMARY.md`.</output>
