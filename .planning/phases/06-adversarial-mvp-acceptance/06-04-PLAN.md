---
phase: 06-adversarial-mvp-acceptance
plan: "04"
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/skillscout/domain/acceptance.py
  - src/skillscout/adapters/operations_state.py
  - tests/test_acceptance_domain.py
  - tests/test_operations_state.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-01..D-06 benchmark and nomination facts are strict, versioned, self-digested, and human-lock/revision bound."
    - "D-11 requires every named hard gate plus positive human review for PASS."
    - "D-12 credits only complete structured filter/no-workflow/qualification/validation/reviewer business rejections at their correct boundaries."
    - "D-13 makes provider/schema exhaustion, missing evidence, duplicate effects, unauthorized effects, secret exposure, execution, harness failure, and rebuild failure release-blocking system failures."
    - "D-14 permits only explicit non-security warnings with impact and follow-up; D-15 forbids waiving any hard gate by score or override."
    - "D-16/D-17 human review binds the exact open Draft head and complete usefulness/fidelity/provenance/safety/diff checklist."
    - "D-26 canonical acceptance facts live in the existing operations owner and rebuild from content-addressed JSON; no fourth store is created."
    - "D-24..D-26 `HostedIsolationCapabilityV1` and `OfflineAdversarialRunV1` are strict canonical contracts whose exact operations fact kinds persist, export, rebuild, and reject raw diagnostics before Plan 06-06 consumes them."
    - "TEST-03 post-publication identical replay and changed-source same-Draft update have distinct strict completion models/fact kinds; their natural identities coexist with the pre-publication intent facts and rebuild independently."
    - "The advice-only reviewer calibration fact has an owned strict model and fact-kind mapping before Plan 06-11 consumes it."
  artifacts:
    - {path: src/skillscout/domain/acceptance.py, provides: "Strict Phase 6 acceptance domain including hosted-isolation, offline-run, and reviewer-calibration models"}
    - {path: src/skillscout/adapters/operations_state.py, provides: "Typed acceptance fact-kind/model persistence, projection, export, and rebuild in the operations owner"}
  key_links:
    - {from: acceptance domain models, to: OperationsStateStore, via: "canonical JSON and typed fact kinds"}
    - {from: HostedIsolationCapabilityV1, to: acceptance_hosted_isolation_capability, via: "exact model/kind registry and canonical operations projection"}
    - {from: OfflineAdversarialRunV1, to: acceptance_offline_adversarial_run, via: "capability-digest referential validation before insert/export/rebuild"}
    - {from: PublicationReplayCompletionV1, to: acceptance_publication_replay_completion, via: "post-effect zero-counter completion mapping distinct from ReplayEvidenceV1 intent"}
    - {from: ChangedSourceDraftUpdateCompletionV1, to: acceptance_changed_source_draft_update_completion, via: "post-effect same-Draft update mapping distinct from ChangedSourceEvidenceV1 intent"}
    - {from: AcceptanceEvidenceRootV1, to: release verdict, via: "named hard-gate registry, never aggregate score"}
---

<objective>
Implement the strict acceptance domain and persist its redacted facts through the existing canonical operations-state boundary.

Purpose: Give nomination, campaign, replay/update, canary, human, cleanup, and report stages one immutable schema vocabulary without logs or a fourth state store.
Output: Versioned acceptance models, canonical digests, operations fact APIs, and mutation/rebuild tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/domain/discovery.py
@src/skillscout/domain/publication.py
@src/skillscout/adapters/operations_state.py
@tests/test_discovery_domain.py
@tests/test_operations_state.py
@.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-04-01: Implement strict benchmark, scenario, attestation, and verdict models</name>
  <files>src/skillscout/domain/acceptance.py, tests/test_acceptance_domain.py</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, .planning/phases/06-adversarial-mvp-acceptance/06-RESEARCH.md, .planning/phases/06-adversarial-mvp-acceptance/06-PATTERNS.md, src/skillscout/domain/discovery.py, src/skillscout/domain/publication.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, tests/test_acceptance_domain.py, tests/test_discovery_domain.py</read_first>
  <behavior>
    - Manifest entries bind public repository full name, numeric repository ID, 40-hex SHA, admitted SPDX license, selection source, intended evaluator role, Search nomination evidence, and self-digest.
    - Terminal classes distinguish eligible/valid business rejection/system failure with closed reasons and gate consequences.
    - Pre-publication replay/update intent and post-publication replay/update completion bind exact old/new authorities, workflow/platform bytes, Draft head/PR linkage, effect counters, and separate self-digests.
    - Hosted isolation capability binds reviewed workflow/source/run/mechanism and causal control/direct/child probe outcomes; offline run binds that exact capability and complete adversarial/scan effects.
    - Reviewer calibration is strict, redacted, self-digested, and cannot contribute release authority.
  </behavior>
  <action>Turn Task 06-01-01 GREEN with `StrictFrozenModel` subclasses and canonical self-digests. Implement `BenchmarkSelectionSource`, `BenchmarkCoverageRole`, `BenchmarkEntryV1`, `NominationSetV1`, `BenchmarkLockAttestationV1`, `LockedBenchmarkManifestV1`, `AcceptanceTerminalClass`, `AcceptanceScenarioResultV1`, `HostedIsolationCapabilityV1`, `OfflineAdversarialRunV1`, `ReplayEvidenceV1`, `ChangedSourceEvidenceV1`, `PublicationReplayCompletionV1`, `ChangedSourceDraftUpdateCompletionV1`, `GateB4BindingV1`, `HumanSkillReviewAttestationV1`, `ProbeCleanupAttestationV1`, `ReviewerCalibrationV1`, `AcceptanceGateResultV1`, `AcceptanceEvidenceRootV1`, and `AcceptanceReleaseVerdictV1`. Implement the exact hosted/offline and publication-completion schema versions, fields, equality/inequality constraints, effect counts, and invalid combinations frozen by Task 06-01-01. Recompute `capability_digest`, `run_digest`, both `completion_digest` fields, and `calibration_digest` from canonical JSON excluding only their own digest field. Treat `ReplayEvidenceV1` and `ChangedSourceEvidenceV1` as immutable pre-publication semantic intent/evidence only. `PublicationReplayCompletionV1` must link the exact replay intent and original publication receipt to the unchanged source/policy/fingerprint/authority/marker/PR/head, require identical before/after remote observations and five zero effect counts, and remain open/Draft. `ChangedSourceDraftUpdateCompletionV1` must link the exact changed-source intent and approved prior-lineage record, require changed old/new source/fingerprint/authority/revision/head, preserve the same lineage/publication key/marker/PR/head branch, require zero new branch/PR/reviewer requests plus exactly one head update and one remote commit, and remain open/Draft. Enforce `HostedIsolationCapabilityV1` as the reviewed `docker_network_none` mechanism with a passing control, denied direct and child probes, zero credentials, no state-write capability, zero synthetic-canary hits, and bounded public workflow/source/run/artifact/reviewer identities. Enforce `OfflineAdversarialRunV1` as a same-workflow/source/run-attempt/mechanism binding to one `HostedIsolationCapabilityV1` digest, with sorted unique complete scenario IDs, aligned unique scenario-result digests, at least fifteen controlled scenarios, all three network-denial booleans true, zero untrusted execution/unapproved network/unauthorized effects/canary hits, and monotonic aware timestamps. Enforce `ReviewerCalibrationV1` as bounded redacted label/result digests, case count at least ten, deterministic agreement/kappa/disagreement fields, and `advice_only=true`; it cannot appear in hard-gate authority. Keep `GateB4BindingV1`'s cleanup manifest as a strict bounded tuple of probe PR/branch target identities so Plans 06-10/12 do not introduce an unowned free-form evidence type. Reject extra/raw log, fixture prose, response body, authorization, token, key, credential, home/repository scan, or unrestricted-path fields and every invalid combination frozen in Wave 0. Enforce D-03’s exact five-role distribution and D-11..D-17 hard-gate semantics. Human identity fields are bounded non-secret identifiers; no signature/key scheme is introduced. Expected labels live in evaluator-only models that semantic-input models cannot contain.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_domain.py -x</automated></verify>
  <acceptance_criteria>
    - Strict validation rejects unknown fields, stale digests, invalid distribution, wrong SHA/license, stale head, incomplete D-17 checklist, and positive verdict without all blocking gates.
    - `business_terminal` includes deterministic filter/no-workflow/qualification/validation/reviewer rejection; provider/schema/evidence/harness failures are `system_failure`.
    - Warnings contain bounded impact and follow-up and cannot alter hard-gate status.
    - Hosted capability/offline run/calibration models use the exact frozen schema versions, fields, canonical digests, redaction rules, cross-model bindings, and invalid-combination failures.
  </acceptance_criteria>
  <done>The complete acceptance vocabulary is strict, immutable, and independently digestible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-04-02: Persist acceptance facts in the operations owner</name>
  <files>src/skillscout/adapters/operations_state.py, tests/test_operations_state.py</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, .planning/phases/06-adversarial-mvp-acceptance/06-RESEARCH.md, .planning/phases/06-adversarial-mvp-acceptance/06-PATTERNS.md, src/skillscout/adapters/operations_state.py, src/skillscout/domain/acceptance.py, src/skillscout/domain/discovery.py, tests/test_operations_state.py, src/skillscout/adapters/state_branch.py</read_first>
  <behavior>
    - Acceptance facts append as canonical content-addressed operations-owned facts and rebuild into the same typed projection.
    - Fact kinds cover nomination, benchmark lock/revision, scenario terminal, hosted-isolation capability, offline adversarial run, pre-publication replay/changed-source intent, post-publication replay/update completion, canary binding, human review, cleanup, reviewer calibration, gate result, and report root.
    - Raw repository text, provider bodies, logs, credentials, and Actions diagnostics are not accepted.
  </behavior>
  <action>Extend the operations schema and `_FactKind`/`_FACT_TABLES` ownership with one bounded `operations_acceptance_facts` table and exact kinds `acceptance_nomination`, `acceptance_benchmark_lock`, `acceptance_scenario`, `acceptance_hosted_isolation_capability`, `acceptance_offline_adversarial_run`, `acceptance_replay`, `acceptance_changed_source`, `acceptance_publication_replay_completion`, `acceptance_changed_source_draft_update_completion`, `acceptance_gate_b4`, `acceptance_human_review`, `acceptance_cleanup`, `acceptance_reviewer_calibration`, `acceptance_gate`, and `acceptance_report_root`; do not create another SQLite owner or duplicate pipeline/publication schemas. Give the table a closed checked fact-kind column plus bounded `acceptance_run_id`, model `schema_version`, canonical `fact_json`, `fact_digest`, and deterministic insertion/recorded identity needed by the existing snapshot transaction. Represent the shared table once in `_FACT_TABLES` with the row's `fact_kind` discriminator and canonical `(acceptance_run_id, fact_kind, fact_digest)` ordering; do not rescan the same table once per kind. Add an explicit immutable fact-kind → exact model registry: nomination → `NominationSetV1`; benchmark lock → `LockedBenchmarkManifestV1`; scenario → `AcceptanceScenarioResultV1`; hosted capability → `HostedIsolationCapabilityV1`; offline run → `OfflineAdversarialRunV1`; semantic replay intent → `ReplayEvidenceV1`; changed-source intent → `ChangedSourceEvidenceV1`; publication replay completion → `PublicationReplayCompletionV1`; changed-source Draft update completion → `ChangedSourceDraftUpdateCompletionV1`; Gate B4 → `GateB4BindingV1`; human review → `HumanSkillReviewAttestationV1`; cleanup → `ProbeCleanupAttestationV1`; reviewer calibration → `ReviewerCalibrationV1`; gate → `AcceptanceGateResultV1`; report root → `AcceptanceEvidenceRootV1`. Implement typed APIs `record_acceptance_fact(acceptance_run_id, kind, fact)` and `acceptance_snapshot(acceptance_run_id)` so exact model type, schema version, run binding, canonical bytes, self-digest, and kind mapping validate before mutation. Define exact natural identities: publication replay completion uses `(acceptance_run_id, replay_intent_digest, publication_key, pull_request_number, head_commit_sha)`; changed-source Draft update completion uses `(acceptance_run_id, changed_source_intent_digest, publication_key, pull_request_number, new_head_commit_sha)`. Because intent and completion use different exact kinds/models/scopes, both immutable facts coexist; exact duplicate completion ingestion is a no-op, while different bytes for one completion natural identity conflict. Reject a completion without its exact prior intent and required publication/lineage receipt, a completion digest reused under another kind/run, or an offline run whose capability digest is absent/mismatched. Extend `_FACT_TABLES`, fact replay, canonical ordering, `OperationsOwnedFactV1`, operations projection with separate digest tuples for both intent and both completion kinds, owned export envelope, state-object export, `_parse_bundle_exports`, `rebuild_owned_state`, and three-store restore so every acceptance kind revalidates through the registry and export → rebuild → fresh export is byte/field equal. Mutation-test missing/swapped/duplicate/noncanonical/cross-run/stale-root facts; kind/model/schema mismatch; intent/completion referential drift; changed marker/PR/head/lineage/effect counts; capability/offline referential or workflow/source/mechanism drift; duplicate/idempotency conflict; redaction-key injection; fact-order drift; and valid-DB/JSON disagreement. Persist only redacted model JSON and exact public identifiers; reject keys or nested keys representing raw corpus, response body, raw logs, fixture prose, authorization, token, credentials, private keys, home/repository scans, or unrestricted paths.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_operations_state.py -k 'acceptance or export or rebuild or canonical' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/domain/acceptance.py src/skillscout/adapters/operations_state.py</automated></verify>
  <acceptance_criteria>
    - Export → rebuild → fresh export is byte/field equal for every acceptance fact kind.
    - Any missing, reordered, duplicated, mutated, cross-run, or untyped fact fails before report or live authority.
    - Existing discovery projection and three-store tests remain green.
    - Exact duplicate writes are no-ops, conflicting duplicates fail, intent and completion coexist under distinct kinds, each completion requires its exact intent/receipt linkage, and an offline run cannot persist or rebuild without its exact prior hosted-capability fact.
  </acceptance_criteria>
  <done>Canonical state can retain and rebuild every Phase 6 acceptance fact without retaining untrusted or secret text.</done>
</task>

</tasks>

<threat_model>
## Assets
- Benchmark lock, evidence roots, replay/update lineage, canary bindings, human attestations, and release verdict.

## Trust Boundaries
| Boundary | Description |
|---|---|
| External/public identities → strict domain | Moving or forged identities must not become authority. |
| Typed acceptance facts → operations state branch | Mutated or incomplete evidence must not rebuild. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-10 | Tampering | hosted/offline and other evidence facts | high | mitigate | Exact fact-kind/model registry, capability/run binding, canonical self-digests, typed revalidation, idempotency-conflict checks, and exact rebuild. |
| T-06-11 | Repudiation | human/canary evidence | high | mitigate | Exact identity/head/workflow/platform binding. |
| T-06-12 | Information Disclosure | durable state | high | mitigate | Closed schemas and forbidden-field tests. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package changes. |
</threat_model>

## Artifacts this phase produces
- All named acceptance domain models and enums, including `HostedIsolationCapabilityV1`, `OfflineAdversarialRunV1`, `PublicationReplayCompletionV1`, `ChangedSourceDraftUpdateCompletionV1`, and `ReviewerCalibrationV1`
- Operations fact kinds `acceptance_nomination`, `acceptance_benchmark_lock`, `acceptance_scenario`, `acceptance_hosted_isolation_capability`, `acceptance_offline_adversarial_run`, `acceptance_replay`, `acceptance_changed_source`, `acceptance_publication_replay_completion`, `acceptance_changed_source_draft_update_completion`, `acceptance_gate_b4`, `acceptance_human_review`, `acceptance_cleanup`, `acceptance_reviewer_calibration`, `acceptance_gate`, `acceptance_report_root`
- Exact fact-kind → strict-model registry plus typed acceptance projection/export/rebuild mapping
- `OperationsStateStore.record_acceptance_fact`
- `OperationsStateStore.acceptance_snapshot`

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Audit-ready acceptance evidence | COVERED |
| REQ | TEST-01..04 | All Phase 6 evidence forms | COVERED |
| RESEARCH/AI-SPEC | — | Strict contracts, evidence first, existing state owner | COVERED |
| CONTEXT | D-01..D-06,D-11..D-17,D-21..D-27 | Benchmark, hard gates, lineage, attestations, persistence/report | COVERED |

<verification>
Domain and state mutation/rebuild tests pass; existing three-store ownership remains intact.
</verification>

<success_criteria>
Every future Phase 6 action consumes and emits typed, redacted, content-addressed facts that independently rebuild.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-04-SUMMARY.md`.</output>
