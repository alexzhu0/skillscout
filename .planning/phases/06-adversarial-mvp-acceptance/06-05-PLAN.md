---
phase: 06-adversarial-mvp-acceptance
plan: "05"
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - src/skillscout/application/acceptance.py
  - src/skillscout/bootstrap.py
  - src/skillscout/cli.py
  - tests/test_acceptance_application.py
  - tests/test_phase6_acceptance.py
  - .github/workflows/phase6-acceptance.yml
  - .github/workflows/discover.yml
  - .github/workflows/publish-candidate.yml
  - .github/workflows/gate-b4-canary.yml
  - tools/verify_phase6_source_execution.py
  - tests/test_publication_security.py
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-01 nomination uses the real Search/filter path but has no semantic or publication authority."
    - "Locked execution re-admits exact Search nomination, manifest revision, fixed IDs/SHAs/licenses, provider policy, and canonical state before credentials/effects."
    - "Expected labels and evaluator notes are unavailable to Extractor, Generator, and Pro Reviewer request builders."
    - "Structured business rejection continues as evidence; system failure stops the campaign and release credit."
    - "Before fresh Gate B4, every authoritative SkillScout execution in the exact closed set of discover, publish-candidate, Gate B4 canary, and Phase 6 acceptance workflows uses checked-out locked repository source; no wheel/dist/registry/artifact/wrapper route exists."
  artifacts:
    - {path: src/skillscout/application/acceptance.py, provides: "Capability-separated Phase 6 orchestrator"}
    - {path: src/skillscout/cli.py, provides: "Closed acceptance CLI commands"}
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Separated offline, nomination, live, publication, attestation, and rebuild jobs"}
    - {path: .github/workflows/publish-candidate.yml, provides: "Pre-Gate-B4 protected publication bytes with repository-local locked source execution"}
    - {path: tools/verify_phase6_source_execution.py, provides: "Closed fail-closed verifier over all four authoritative workflow paths"}
    - {path: tests/test_publication_security.py, provides: "Exact publication-workflow contract and launcher mutation rejection"}
  key_links:
    - {from: nominate-benchmark, to: DiscoveryApplication/Search, via: "real query policy and deterministic filtering only"}
    - {from: run-acceptance, to: existing discovery/phase2/phase3/publication coordinators, via: "strict locked manifest and late capability factories"}
    - {from: all four authoritative workflows, to: checked-out skillscout modules/tools, via: "preceding same-job full-SHA checkout plus exact local uv 0.11.29 `run --locked` invocation enforced by one closed verifier"}
---

<objective>
Implement the capability-separated acceptance orchestrator, safe CLI surface, and protected workflow skeleton.

Purpose: Exercise the real product path instead of a parallel demonstration while keeping nomination, semantics, publication, attestations, and reporting in separate authority zones.
Output: Acceptance application, exact commands, lazy bootstrap factories, tests, and workflow job boundaries.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/application/discovery.py
@src/skillscout/application/publication.py
@src/skillscout/bootstrap.py
@src/skillscout/cli.py
@.github/workflows/discover.yml
@.github/workflows/gate-b4-canary.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-05-01: Compose nomination, locked campaign, replay/update, attestation, and rebuild services</name>
  <files>src/skillscout/application/acceptance.py, tests/test_acceptance_application.py</files>
  <read_first>src/skillscout/application/discovery.py, src/skillscout/application/publication.py, src/skillscout/application/phase3.py, src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, tests/test_acceptance_application.py, tests/test_discovery_application.py</read_first>
  <behavior>
    - Nomination runs Search and deterministic admission only and persists nomination facts.
    - Locked campaign composes existing discovery and candidate coordinators and maps exact terminal facts into acceptance classes.
    - Report/attestation paths read verified state and have no semantic or publication capability.
  </behavior>
  <action>Turn the application RED suite GREEN with separate frozen dependency dataclasses for nomination, locked campaign, replay/update, human-attestation reconciliation, cleanup-attestation reconciliation, and report rebuild. Implement exact manifest-to-nomination re-admission, five-entry completeness, evaluator-only expected comparison after terminal facts, and a closed `business_terminal|eligible|system_failure` mapper. Reuse existing discovery, phase-two, phase-three, prior-lineage, publication, and state owners; do not duplicate GitHub requests or schemas. Exact replay must query verified completed authority before mutable state. Credential factories remain lazy and absent from nomination/offline/rebuild signatures.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_application.py -x</automated></verify>
  <acceptance_criteria>
    - Signature/AST tests prove nomination cannot construct semantic clients or publishers and rebuild cannot construct any live client.
    - Serialized semantic inputs omit expected outcome, role, selection notes, and human labels.
    - Provider/schema exhaustion, evidence mismatch, duplicate effect, and harness exception stop with a closed system-failure code.
  </acceptance_criteria>
  <done>One orchestrator composes production boundaries without widening any capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-05-02: Add closed acceptance CLI and lazy bootstrap factories</name>
  <files>src/skillscout/bootstrap.py, src/skillscout/cli.py, tests/test_phase6_acceptance.py</files>
  <read_first>src/skillscout/bootstrap.py, src/skillscout/cli.py, src/skillscout/application/acceptance.py, tests/test_phase6_acceptance.py, tests/test_cli_security.py, tests/test_discovery_security.py</read_first>
  <behavior>
    - CLI exposes only `nominate-benchmark`, `run-acceptance`, `record-acceptance-attestation`, and `rebuild-acceptance`.
    - No command accepts model, endpoint, catalog, expected result, secret, arbitrary Git ref, or cleanup operation.
    - Parser failures and runtime failures remain fixed sanitized JSON.
  </behavior>
  <action>Add exact argparse commands and canonical JSON outputs. `nominate-benchmark` accepts only the fixed state repository identity inputs used by discovery; `run-acceptance` accepts the strict manifest path and exact state authority; `record-acceptance-attestation` accepts a strict file plus closed kind `human-review|probe-cleanup`; `rebuild-acceptance` accepts exact evidence-root identity and fixed Phase 6 output paths. Bootstrap factories must revalidate provider settings, state root, fixed `alexzhu0/skillscout-catalog-test` publication authority, and manifest before late credential resolution. Keep all env values as existing secret-name boundaries and never print them.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'cli or parser or bootstrap or credential or target' -x</automated></verify>
  <acceptance_criteria>
    - Unknown flags/commands return the existing fixed status-2 JSON diagnostic.
    - Exact invalid state/manifest/provider/catalog facts fail before live factory spies.
    - No command can merge, approve, mark ready, delete, clean up, choose another catalog, or select another model/endpoint.
  </acceptance_criteria>
  <done>The installed CLI exposes only bounded acceptance transitions with late authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-05-03: Define protected workflow authority zones</name>
  <files>.github/workflows/phase6-acceptance.yml, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, tools/verify_phase6_source_execution.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, .github/workflows/discover.yml, .github/workflows/publish-candidate.yml, .github/workflows/gate-b4-canary.yml, tests/test_phase6_source_execution.py, tests/test_phase6_workflow.py, tests/test_publication_security.py, tests/test_discovery_security.py, tools/verify_phase5_acceptance.py, tools/verify_phase4_action_audit.py, tests/test_phase5_acceptance.py, tests/test_phase4_action_audit.py, pyproject.toml, uv.lock</read_first>
  <action>Extend the existing probe workflow while preserving the Plan 06-02-owned `phase6_action=isolation-probe` choice/job, and add closed choices for `nominate`, `offline-adversarial`, `run-benchmark`, `run-replay`, `run-changed-source`, `gate-b4-and-publish`, `record-human-review`, `record-probe-cleanup`, and `rebuild-report`. Define separate jobs/environments and `needs` order, but do not execute live work in this plan. Offline jobs have no credentials/network; nomination has Search/state only; semantic jobs have source/state/DeepSeek only; publication has protected catalog authority only after fresh Gate B4; attestation/rebuild have read/verified-state only. Use one non-cancelling `skillscout-phase6-acceptance` concurrency group, exact Action SHAs, explicit timeouts, and bounded raw artifact retention chosen and documented as 1 day. Create `tools/verify_phase6_source_execution.py` from the Phase 5 acceptance and Phase 4 action-audit patterns as a standard-library-only, read-only, network-free, project-import-free verifier with the exact closed path set `.github/workflows/discover.yml`, `.github/workflows/publish-candidate.yml`, `.github/workflows/gate-b4-canary.yml`, and `.github/workflows/phase6-acceptance.yml`. Safely parse ordered `jobs.*.steps`, reject malformed/duplicate/ambiguous YAML shapes, and find every authoritative `skillscout` module/CLI import plus checked-out repository tool invocation; zero findings or an unknown form fails. For every finding require an earlier same-job `actions/checkout@{40 lowercase hex}` of the current revision with `persist-credentials:false`, no external checkout path, followed by exact pinned `astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9`, `version: 0.11.29`, caching disabled, verified materialization at `.tools/uv-0.11.29/bin/uv`, and direct `.tools/uv-0.11.29/bin/uv run --locked` execution from the checkout root. Correct all four workflows to that contract before fresh Gate B4, including every inline Python block importing SkillScout and checked-out tools such as `tools/gate_b4_canary.py`. The workflow input grammar has no wheel/source selector. Reject pip, registry, wheel, `dist/`, `uvx`, `uv tool`, `uv run --with`, bare/preinstalled `skillscout`, download-artifact/release/download execution, external working directories, command variables, aliases/functions, sourced/delegated wrappers, indirect scripts, and every unrecognized acquisition/invocation form. Do not install, import, or execute candidate-repository content. Turn the complete Wave 0 mutation suite in `tests/test_phase6_source_execution.py` GREEN and retain publication-specific security assertions. These are pre-Gate-B4 bytes; Plan 06-06 may only complete the already source-only Phase 6 offline job, Plans 06-07..06-09 execute/read the workflow, and Plans 06-10 onward must not modify any of the four workflow files.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_source_execution.py tests/test_publication_security.py tests/test_phase6_workflow.py -k 'source_execution or publish_workflow or locked_source_execution or workflow or zone or action or retention' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py</automated></verify>
  <acceptance_criteria>
    - No job receives a credential it does not need; catalog authority appears only in the protected publication job.
    - Live jobs cannot run before required prior outputs/attestations; workflow choices are closed.
    - Candidate/evaluator values are passed through bounded files, never direct shell interpolation.
    - Every authoritative step in all four exact workflows has preceding same-job full-SHA checkout, exact pinned setup-uv 0.11.29, verified repository-local uv materialization, and direct locked checked-out-source execution.
    - The closed verifier and mutation suite reject every wheel/dist/registry/pip/artifact, bare/unlocked/PATH-only, wrapper/alias/function/variable, external-working-directory, indirect, empty-scan, or unknown path.
    - No source selector exists, no setup or execution step downloads or runs candidate-repository content, and no workflow byte is left for Plans 06-10 through 06-14 to repair.
  </acceptance_criteria>
  <done>The workflow skeleton and protected publication workflow express the exact release chain, credential timing, and locked checked-out-source execution bytes that Plan 06-10 will bind.</done>
</task>

</tasks>

<threat_model>
## Assets
- Search nominations, locked manifest, stage facts, credential zones, CLI authority, and catalog boundary.

## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI/workflow input → application dependencies | Runtime input must not widen models, endpoints, refs, or catalog. |
| Nomination/evaluator → semantic campaign | Selection hypotheses must stay out of prompts. |
| Discovery → protected publication | Exact state re-admission precedes catalog token minting. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-13 | Elevation of Privilege | capability composition | high | mitigate | Distinct dependency types and lazy factories. |
| T-06-14 | Injection | CLI/workflow shell | high | mitigate | Closed argparse, bounded file handoffs, and exact direct local-uv launcher mutation tests. |
| T-06-15 | Spoofing | manifest/state authority | high | mitigate | Exact nomination/root/provider/catalog re-admission. |
| T-06-SC | Tampering | dependencies/Actions | high | mitigate | Existing full-SHA setup-uv installs exact 0.11.29, which is version-checked and materialized at the repository-local path; `run --locked` uses `uv.lock` and candidate content is never installed or executed. |
</threat_model>

## Artifacts this phase produces
- `src/skillscout/application/acceptance.py`
- CLI commands `nominate-benchmark`, `run-acceptance`, `record-acceptance-attestation`, `rebuild-acceptance`
- Workflow input `phase6_action` with retained `isolation-probe` plus the closed choices added in Task 06-05-03
- Workflow jobs `nominate`, `offline_adversarial`, `live_benchmark`, `changed_source`, `fresh_gate_b4`, `value_publication`, `human_attestation`, `cleanup_attestation`, `rebuild_report`
- Pre-Gate-B4 source-only bytes for discover, publish-candidate, Gate B4 canary, and Phase 6 acceptance plus the exact closed structural verifier

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Real production path with human review | COVERED |
| REQ | TEST-01, TEST-03, TEST-04 | End-to-end, replay/update, Draft/denials | COVERED |
| RESEARCH | — | Two-step nomination, evidence-first orchestration, late authority | COVERED |
| CONTEXT | D-01..D-06,D-11..D-23,D-26,D-27 | Benchmark, gates, pre-Gate-B4 protected workflow bytes, publication, evidence/report chain | COVERED |

<verification>
Application, CLI, authority-zone, and exact protected publication source-execution mutation tests pass without live credentials or external effects.
</verification>

<success_criteria>
Every Phase 6 transition is a closed command/job with only the authority required for that transition.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-05-SUMMARY.md`.</output>
