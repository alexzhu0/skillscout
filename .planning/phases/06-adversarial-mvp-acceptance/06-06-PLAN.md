---
phase: 06-adversarial-mvp-acceptance
plan: "06"
type: execute
wave: 4
depends_on: [06-02, 06-04, 06-05]
files_modified:
  - src/skillscout/application/acceptance.py
  - tests/fixtures/acceptance/scenario_matrix.json
  - tests/test_phase6_adversarial.py
  - tests/test_phase6_acceptance.py
  - tests/test_phase6_workflow.py
  - .github/workflows/phase6-acceptance.yml
autonomous: false
requirements: [TEST-02, TEST-03, TEST-04]
user_setup:
  - service: github
    why: "Persist only the redacted hosted capability and completed offline-campaign facts to the canonical state branch after the credential-free job finishes"
    env_vars:
      - {name: SKILLSCOUT_STATE_GITHUB_TOKEN, source: "Protected post-job state persistence boundary; value must not be inspected"}
must_haves:
  truths:
    - "D-04/D-24 all seven injection fixtures and shell/subprocess/import/source/network/executable denials run as inert controlled scenarios."
    - "Hosted offline execution revalidates the exact one-day probe artifact located by 06-02-SUMMARY.md and fails closed if stale, unavailable, or digest-mismatched."
    - "D-25 synthetic canaries are absent from allowlisted sanitized logs, state, reports, bounded artifacts, prompts, and PR-diff fixtures without reading real secrets."
    - "D-26 canonical hosted-isolation and complete offline-campaign facts are persisted through OperationsStateStore/state-branch evidence; the summary locator and one-day artifacts are never evidence authority."
    - "Task 06-04-01/02 already owns the exact `HostedIsolationCapabilityV1`/`OfflineAdversarialRunV1` schemas and `acceptance_hosted_isolation_capability`/`acceptance_offline_adversarial_run` persistence mappings consumed here."
    - "Plan 06-07 cannot start until the exact `offline_adversarial` run passes and its redacted canonical facts rebuild."
  artifacts:
    - {path: tests/test_phase6_adversarial.py, provides: "Complete offline adversarial campaign"}
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Credential-free kernel-isolated offline job"}
    - {path: src/skillscout/domain/acceptance.py, provides: "Plan 06-04-owned hosted capability and offline run models consumed without modification"}
    - {path: src/skillscout/adapters/operations_state.py, provides: "Plan 06-04-owned exact fact-kind/model persistence and rebuild mapping consumed without modification"}
  key_links:
    - {from: 06-02-SUMMARY.md locator and one-day probe artifact, to: offline_adversarial job, via: "exact digest/workflow/mechanism equality before execution"}
    - {from: HostedIsolationCapabilityV1, to: acceptance_hosted_isolation_capability, via: "Plan 06-04 exact typed record/export/rebuild mapping"}
    - {from: OfflineAdversarialRunV1, to: acceptance_offline_adversarial_run, via: "capability-bound Plan 06-04 mapping and separate post-job redacted persistence before Plan 06-07"}
---

<objective>
Implement and pass the complete offline adversarial campaign under the proven hosted kernel isolation mechanism.

Purpose: Close injection, supply-chain, syscall-network, synthetic-secret, and hard-gate evidence before any live benchmark credentials are authorized.
Output: At least fifteen controlled scenario facts, a completed kernel-isolated hosted workflow, synthetic scans, and rebuilt canonical state-branch evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tests/fixtures/injection/
@tests/conftest.py
@tools/verify_phase1_gap_evidence.py
@tools/verify_phase5_acceptance.py
@.planning/phases/06-adversarial-mvp-acceptance/06-02-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-06-01: Execute the complete controlled terminal and adversarial matrix</name>
  <files>src/skillscout/application/acceptance.py, tests/fixtures/acceptance/scenario_matrix.json, tests/test_phase6_adversarial.py</files>
  <read_first>src/skillscout/application/acceptance.py, tests/fixtures/acceptance/scenario_matrix.json, tests/test_phase6_adversarial.py, tests/fixtures/injection/, tests/test_extractor_boundary.py, src/skillscout/domain/acceptance.py</read_first>
  <behavior>
    - Controlled cases cover success, deterministic filter, no workflow, low qualification, format/security rejection, Reviewer rejection, and every injection/supply-chain class.
    - Each case names expected terminal boundary, required effects, forbidden effects, gate IDs, and sanitized evidence IDs.
    - Semantic/business outcomes never count provider/schema/harness failure as coverage.
  </behavior>
  <action>Implement a deterministic evaluator over at least 20 total labeled records with at least 15 controlled cases. Reuse all seven existing injection files as inert data; add compact scenario instructions for shell, subprocess, dynamic import, source-code execution, unapproved network, executable/`scripts/`, and synthetic-secret propagation. Drive the actual filter/reader/extractor/qualification/generator/validator/reviewer seams with recorded transports and strict contracts. Assert exact stop stage and zero downstream effects. Expected labels remain evaluator-only. Minimize and persist only closed reason/effect/digest facts, never fixture prose or model bodies.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_adversarial.py -x</automated></verify>
  <acceptance_criteria>
    - Every required business terminal and attack class has a complete typed terminal fact.
    - Shell/subprocess/import/source execution and executable/`scripts/` cases produce zero execution and zero unapproved network effects.
    - Missing, malformed, provider-exhausted, or harness-failed cases block instead of satisfying scenario coverage.
  </acceptance_criteria>
  <done>The controlled corpus proves every required terminal and attack boundary deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-06-02: Enforce hosted kernel isolation and synthetic-secret scans</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_workflow.py, tests/test_phase6_adversarial.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, tests/test_phase6_workflow.py, tests/test_phase6_adversarial.py, .planning/phases/06-adversarial-mvp-acceptance/06-02-SUMMARY.md, src/skillscout/adapters/operations_state.py, tests/conftest.py</read_first>
  <action>Wire `offline_adversarial` to resolve the exact one-day capability artifact from the non-authoritative Plan 06-02 locator, verify its digest/workflow/source/run/mechanism identities, and run the controlled suite with that recorded kernel mechanism. Fail closed if the artifact expired or any identity differs; a new probe and review is required instead of fallback. The isolated job has no credentials, state-write access, or live adapters; allowed inputs are the locked project wheel/source plus controlled fixtures, never candidate repository code. Add direct and child-process outbound probes that must be denied and keep the Python socket sentinel as a second detector. Seed unique synthetic canaries into controlled headers/payloads and scan only an explicit manifest of sanitized logs, redacted state projections, generated reports, one-day artifacts, and PR-diff fixtures; never recursively scan the repository/home or open `.env`, PEM, JWT, token, private-key, or credential files.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py tests/test_phase6_adversarial.py -k 'network or kernel or isolation or secret or canary or artifact' -x</automated></verify>
  <acceptance_criteria>
    - Stale/missing capability evidence prevents the job from running and cannot fall back to socket monkeypatch proof.
    - Both direct and child-process external connections are causally denied; approved live adapters are absent from the job.
    - Synthetic canary hits fail without echoing the value; the scan manifest contains no secret-bearing path class.
  </acceptance_criteria>
  <done>The offline campaign has kernel/OS denial and synthetic-secret absence evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-06-03: Dispatch and canonically attest the full hosted offline campaign</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_acceptance.py, tests/test_phase6_workflow.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, .planning/phases/06-adversarial-mvp-acceptance/06-02-SUMMARY.md, .planning/phases/06-adversarial-mvp-acceptance/06-04-SUMMARY.md, src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, tests/test_acceptance_domain.py, tests/test_operations_state.py, tests/test_phase6_acceptance.py, tests/test_phase6_workflow.py, tools/verify_phase6_acceptance.py</read_first>
  <action>After Tasks 06-06-01 and 06-06-02 pass static admission, first require the Plan 06-04 domain and operations tests to prove the exact owned schemas and mappings. Dispatch the exact frozen `offline-adversarial` workflow. Require the credential-free isolated job to complete all controlled terminal, seven injection, supply-chain, direct/child network-denial, and synthetic-canary scenarios. In a separate protected post-job step with state-write authority only, validate the bounded result into the exact `HostedIsolationCapabilityV1` schema version `hosted-isolation-capability-v1` and persist it with kind `acceptance_hosted_isolation_capability`; then validate `OfflineAdversarialRunV1` schema version `offline-adversarial-run-v1`, require its capability digest and workflow/source/run-attempt/mechanism fields to equal the just-persisted capability, and persist it with kind `acceptance_offline_adversarial_run`. Use only `OperationsStateStore.record_acceptance_fact`; exact duplicate ingestion is a no-op and conflicting duplicate/cross-run/kind/model/schema input blocks. Push the redacted content-addressed bundle to the canonical state branch, restore it into fresh stores, and require `acceptance_snapshot` plus export → rebuild → fresh export equality to reproduce both exact typed facts and their per-kind digest projections before credit. Persist exact workflow/source/run/mechanism/control/direct/child denial/scenario/effect/scan/digest identities, not raw logs, fixture prose, artifact bodies, response bodies, paths, or credentials. A failed/missing scenario, denial, scan, state push, typed projection, or rebuild blocks Plan 06-07.</action>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py tests/test_phase6_adversarial.py tests/test_phase6_acceptance.py -k 'offline_adversarial or hosted_campaign or canonical_state or rebuild' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py --offline-only</automated>
    <human-check>Confirm the exact hosted run completed under the reviewed kernel mechanism, all required scenarios and both denial probes passed, the isolated job had no credentials, and the displayed state commit/root/fact digests rebuild from the canonical state branch.</human-check>
  </verify>
  <acceptance_criteria>
    - The exact full offline workflow was dispatched after static admission and completed every required scenario under the kernel boundary.
    - Canonical hosted capability and campaign facts rebuild through OperationsStateStore/state branch; the Plan 06-02 locator and one-day artifact remain non-authoritative.
    - The rebuilt snapshot contains exactly one matching `acceptance_hosted_isolation_capability`/`HostedIsolationCapabilityV1` fact and one capability-bound `acceptance_offline_adversarial_run`/`OfflineAdversarialRunV1` fact; kind/model/schema drift or conflicting duplicates fail.
    - Future live gates remain explicit failures, and Plan 06-07 is dependency-blocked unless the offline-only independent verifier passes.
  </acceptance_criteria>
  <done>The full hosted adversarial campaign has passing, rebuilt canonical evidence.</done>
</task>

</tasks>

<threat_model>
## Assets
- Injection corpus, offline runner isolation, synthetic canaries, canonical adversarial facts, and independent gate registry.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted fixture bytes → semantic pipeline | Fixture instructions must not gain authority. |
| Process/child process → network | Python-only controls are insufficient. |
| Sanitized surfaces → secret scanner | Scanner itself must not read real secrets. |
| Bounded hosted result → canonical operations facts | Artifact bytes must pass the Plan 06-04 strict models, exact kind mapping, and rebuild before becoming authority. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-16 | Elevation of Privilege | untrusted execution | critical | mitigate | Recorded REST/data-only paths plus kernel-isolated denial matrix. |
| T-06-17 | Information Disclosure | outbound network/secrets | critical | mitigate | No-network job and synthetic-only exact-path scans. |
| T-06-18 | Tampering | hosted/offline fact substitution | high | mitigate | Exact Plan 06-04 model/kind registry, capability binding, idempotent insert rules, typed rebuild, and independent fixed registry. |
| T-06-SC | Tampering | supply chain | high | mitigate | No candidate install/import/run and no new package. |
</threat_model>

## Artifacts this phase produces
- Controlled scenario matrix with all required terminal/attack classes
- Workflow job `offline_adversarial`
- Canonical state-branch kind `acceptance_hosted_isolation_capability` carrying `HostedIsolationCapabilityV1`
- Canonical state-branch kind `acceptance_offline_adversarial_run` carrying capability-bound `OfflineAdversarialRunV1`, including `os_syscall_network_denied` and `synthetic_secret_canaries_absent` evidence

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Adversarial safety evidence | COVERED |
| REQ | TEST-02, TEST-03, TEST-04 | Scenario, idempotency mutation, permission/secret denials | COVERED |
| RESEARCH/AI-SPEC | — | ≥20 records, kernel isolation, synthetic scans, independent registry | COVERED |
| CONTEXT | D-04,D-11..D-15,D-24..D-27 | Attacks, hard-gates, denial, persistence/report facts | COVERED |

<verification>
Controlled suites pass locally, the exact hosted offline job completes, and the independent inspector credits only rebuilt OperationsStateStore/state-branch facts.
</verification>

<success_criteria>
No injection, source-code, process, network, executable, or synthetic-secret scenario crosses its intended boundary, and no missing evidence is mistaken for a business rejection.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-06-SUMMARY.md` with exact offline hosted evidence references.</output>
