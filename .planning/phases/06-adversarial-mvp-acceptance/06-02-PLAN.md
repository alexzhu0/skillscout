---
phase: 06-adversarial-mvp-acceptance
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase6_adversarial.py
  - tests/test_phase6_acceptance.py
  - tests/test_phase6_workflow.py
  - .github/workflows/phase6-acceptance.yml
  - .planning/phases/06-adversarial-mvp-acceptance/06-HOSTED-ISOLATION-EVIDENCE.json
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "Wave 0 freezes all required scenario, mutation, workflow, and report-rebuild tests before dependent implementation."
    - "D-24 closes `os_syscall_network_denial` only with hosted kernel/OS evidence, never with the Python socket sentinel alone."
    - "D-25 isolation evidence and fixtures use synthetic canaries and never inspect real secret material."
    - "The hosted capability probe has no GitHub App, catalog, DeepSeek, state-write, merge, approve, ready, cleanup, or arbitrary-network authority."
  artifacts:
    - {path: tests/test_phase6_adversarial.py, provides: "Adversarial and supply-chain RED matrix"}
    - {path: tests/test_phase6_acceptance.py, provides: "Whole-phase mutation and rebuild RED suite"}
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Secretless hosted isolation capability probe"}
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-HOSTED-ISOLATION-EVIDENCE.json, provides: "Human-reviewed exact hosted capability fact"}
  key_links:
    - {from: phase6-acceptance.yml isolation_probe, to: 06-HOSTED-ISOLATION-EVIDENCE.json, via: "exact workflow SHA, run identity, mechanism, denial observations"}
---

<objective>
Establish Wave 0 adversarial/report contracts and prove an exact hosted kernel-enforced offline mechanism before dependent work.

Purpose: Prevent Phase 6 from claiming process/network isolation through Python-only mocks or selecting an unavailable runner mechanism.
Output: RED adversarial/acceptance/workflow suites, a no-credential probe job, and reviewed content-addressed capability evidence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@tests/conftest.py
@tests/fixtures/injection/
@tools/verify_phase1_gap_evidence.py
@tools/verify_phase5_acceptance.py
@.github/workflows/discover.yml
@.github/workflows/gate-b4-canary.yml
</context>

<tasks>

<task type="auto">
  <name>Task 06-02-01: Freeze adversarial, workflow, and independent-rebuild contracts</name>
  <files>tests/test_phase6_adversarial.py, tests/test_phase6_acceptance.py, tests/test_phase6_workflow.py</files>
  <read_first>tests/conftest.py, tests/test_extractor_boundary.py, tests/fixtures/injection/, tests/test_phase5_acceptance.py, tests/test_discovery_workflow.py, tests/test_gate_b4_canary_workflow.py, tools/verify_phase1_gap_evidence.py</read_first>
  <action>Create collectable RED tests for all seven injection fixture classes plus shell, subprocess, dynamic import, source execution, executable/`scripts/`, unapproved outbound network, and synthetic-secret propagation. Add whole-phase mutation cases for missing/swapped/duplicate/stale/self-referential evidence, identical replay, changed lineage, stale Gate B4, stale Draft head, hard-gate deletion, and all-44 inverse map drift. Add workflow tests for full-SHA Actions, non-cancelling concurrency, protected environment boundaries, bounded artifact retention, no untrusted shell interpolation, separate offline/live jobs, and a hosted kernel isolation evidence record. The Python socket sentinel remains fast feedback but cannot satisfy D-24.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_phase6_adversarial.py tests/test_phase6_acceptance.py tests/test_phase6_workflow.py</automated></verify>
  <acceptance_criteria>
    - All required test nodes collect without network, credentials, project installation changes, or secret reads.
    - Only explicitly named missing Phase 6 production/evidence surfaces may fail; fixture parsing and test infrastructure remain ordinary green.
    - Tests distinguish a valid structured rejection from provider/schema/evidence/harness failure.
  </acceptance_criteria>
  <done>The complete adversarial and evidence failure surface is executable before implementation.</done>
</task>

<task type="auto">
  <name>Task 06-02-02: Implement the no-credential hosted isolation capability probe</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_workflow.py</files>
  <read_first>.github/workflows/discover.yml, .github/workflows/gate-b4-canary.yml, tests/test_discovery_workflow.py, tests/test_gate_b4_canary_workflow.py, .planning/phases/06-adversarial-mvp-acceptance/06-RESEARCH.md</read_first>
  <action>Create `phase6-acceptance.yml` with `workflow_dispatch` choice `isolation-probe` and an `isolation_probe` job only at this step. Use Ubuntu 24.04, explicit timeout, `contents:read`, immutable Action SHAs already admitted by the repository, locked uv, no repository/user-controlled shell interpolation, and no secrets or write permission. Probe the documented candidate mechanism (prefer Docker with `--network none`) by proving ordinary offline tests run while direct Python sockets and a child-process network attempt fail below the application layer; record kernel/runner/Docker identity, workflow SHA-256, run ID/attempt, command digests, exit classifications, and a synthetic canary scan without raw diagnostic bodies. Fail closed if the mechanism, permissions, or denial cannot be proven.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py -k 'isolation or workflow or permission or secret' -x</automated></verify>
  <acceptance_criteria>
    - The probe job has no DeepSeek, GitHub App, catalog, state-branch write, publication, or cleanup credential.
    - Network denial covers the Python process and child process; loopback-only behavior is documented and no external success is accepted.
    - Workflow bytes and expected evidence schema are exact and mutation-tested before dispatch.
  </acceptance_criteria>
  <done>A safe exact hosted capability probe exists without claiming it has run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-02-03: Authorize and review hosted isolation capability evidence</name>
  <files>.github/workflows/phase6-acceptance.yml, .planning/phases/06-adversarial-mvp-acceptance/06-HOSTED-ISOLATION-EVIDENCE.json</files>
  <read_first>.github/workflows/phase6-acceptance.yml, tests/test_phase6_workflow.py, .planning/phases/06-adversarial-mvp-acceptance/06-RESEARCH.md, AGENTS.md</read_first>
  <action>Pause before dispatch. After explicit approval, dispatch only the exact checked-in `isolation-probe` action, inspect the bounded structured result without opening any secret-bearing file or environment value, and write `06-HOSTED-ISOLATION-EVIDENCE.json` with the exact source/workflow SHA, run identity, runner/kernel/mechanism identities, denial classifications, synthetic scan outcome, artifact retention, reviewer identity, approval timestamp, and self-digest. Do not record credentials or raw logs. If the mechanism is unavailable or a network probe succeeds, record a failed capability outcome and block Plan 06-06 instead of substituting Python monkeypatch evidence.</action>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py -k 'hosted_isolation_evidence or isolation' -x</automated>
    <human-check>Confirm the exact hosted run used no credentials, both process-level probes were causally denied, the ordinary offline control completed, and the evidence JSON matches the reviewed run.</human-check>
  </verify>
  <acceptance_criteria>
    - Explicit human approval precedes the workflow dispatch.
    - Evidence is bound to exact workflow/source bytes and contains no guessed value.
    - A failed or unavailable mechanism remains a blocking fact; it is not converted to a pass.
  </acceptance_criteria>
  <done>Wave 0 has reviewed hosted kernel/OS isolation capability evidence.</done>
</task>

</tasks>

<threat_model>
## Assets
- Hosted runner authority, exact workflow bytes, network-denial evidence, injection corpus, and synthetic scan results.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository workflow → hosted runner/kernel | Local tests cannot prove hosted namespace behavior. |
| Raw hosted diagnostics → canonical evidence | Only bounded redacted facts may persist. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-04 | Spoofing | hosted isolation claim | high | mitigate | Exact workflow/run binding and causal process probes. |
| T-06-05 | Elevation of Privilege | outbound network | critical | mitigate | Kernel-enforced no-network mechanism; fail closed on capability miss. |
| T-06-06 | Information Disclosure | hosted artifacts | high | mitigate | No credentials; bounded facts; synthetic-only scan. |
| T-06-SC | Tampering | Actions/runtime | high | mitigate | Existing full-SHA Actions and locked uv; no new dependency. |
</threat_model>

## Artifacts this phase produces
- `.github/workflows/phase6-acceptance.yml` input `phase6_action=isolation-probe` and job `isolation_probe`
- `06-HOSTED-ISOLATION-EVIDENCE.json` fields for exact bytes, hosted mechanism, denials, retention, review, and digest
- Phase 6 adversarial, workflow, mutation, and report-rebuild test modules

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Reproducible adversarial acceptance | COVERED |
| REQ | TEST-02, TEST-04 | Injection/supply-chain and least-privilege evidence | COVERED |
| RESEARCH | — | Wave 0 hosted capability probe and independent mutation tests | COVERED |
| CONTEXT | D-04,D-24,D-25,D-26 | Controlled fixtures, syscall denial, synthetic scans, bounded artifacts | COVERED |

<verification>
The Wave 0 suites collect, the probe workflow passes static security tests, and a reviewed exact hosted denial record exists before dependent offline campaign work.
</verification>

<success_criteria>
Phase 6 has a real kernel/OS isolation capability or an explicit blocking failure; it never substitutes a Python-only claim.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-02-SUMMARY.md` with the reviewed hosted evidence locator and digest.</output>
