---
phase: 06-adversarial-mvp-acceptance
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/acceptance/scenario_matrix.json
  - tests/test_acceptance_domain.py
  - tests/test_acceptance_application.py
  - tests/test_semantic_provider.py
  - tools/verify_phase6_red_contracts.py
  - tools/verify_phase6_acceptance.py
  - tools/verify_phase6_validation_map.py
  - .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "D-01..D-06 benchmark nomination, human lock, revision, and mixed-distribution rules are executable failing contracts before implementation."
    - "D-07..D-10 require the exact DeepSeek Flash/Flash/Pro policy and reject arbitrary models or endpoints before credential lookup or HTTP."
    - "D-11..D-17 distinguish structured business rejection from release-blocking system failure and require a positive exact-head human verdict."
    - "D-24..D-27 adversarial, secret-scan, persistence, and deterministic-report behaviors have named tests and exact task ownership."
    - "D-24..D-26 require strict `HostedIsolationCapabilityV1` and `OfflineAdversarialRunV1` contracts, exact operations fact kinds, and canonical export/rebuild coverage before hosted evidence can gate a later plan."
  artifacts:
    - {path: tests/fixtures/acceptance/scenario_matrix.json, provides: "Bounded synthetic scenario and mutation instructions"}
    - {path: tests/test_acceptance_domain.py, provides: "Strict acceptance contract RED suite, including hosted-isolation/offline-run canonical binding and redaction cases"}
    - {path: tests/test_acceptance_application.py, provides: "Capability and evaluator-blind orchestration RED suite"}
    - {path: tools/verify_phase6_red_contracts.py, provides: "Deterministic expected-RED verifier that rejects collection and infrastructure failures"}
    - {path: tools/verify_phase6_validation_map.py, provides: "Wave 0 exact task/checkpoint/requirement/command map verifier"}
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, provides: "Exact Phase 6 task-to-test map"}
  key_links:
    - {from: tests/test_acceptance_domain.py, to: src/skillscout/domain/acceptance.py, via: "deferred imports of exact strict model names"}
    - {from: tests/test_acceptance_application.py, to: src/skillscout/application/acceptance.py, via: "capability/signature and serialized-request assertions"}
    - {from: tests/test_acceptance_domain.py, to: src/skillscout/domain/acceptance.py, via: "deferred imports of HostedIsolationCapabilityV1, OfflineAdversarialRunV1, and ReviewerCalibrationV1 before Plan 06-04 turns them GREEN"}
    - {from: tools/verify_phase6_validation_map.py, to: all 14 Phase 6 plans and 06-VALIDATION.md, via: "exact bidirectional task, checkpoint, requirement, command, and Wave 0 ownership checks"}
---

<objective>
Freeze Wave 0 acceptance contracts and the exact Nyquist task map before production implementation.

Purpose: Make every locked decision and TEST-01..TEST-04 behavior fail visibly rather than emerge as unreviewed implementation.
Output: Bounded fixtures, collectable RED contract/application/provider tests, and exact `06-VALIDATION.md` task identifiers.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md
@.planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md
@.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md
@src/skillscout/domain/discovery.py
@src/skillscout/application/discovery.py
@src/skillscout/adapters/semantic_provider.py
</context>

<tasks>

<task type="auto">
  <name>Task 06-01-01: Freeze strict benchmark, evidence, gate, and attestation contracts</name>
  <files>tests/fixtures/acceptance/scenario_matrix.json, tests/test_acceptance_domain.py, tools/verify_phase6_red_contracts.py</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, .planning/phases/06-adversarial-mvp-acceptance/06-AI-SPEC.md, src/skillscout/domain/discovery.py, src/skillscout/domain/publication.py, tests/test_discovery_domain.py, tests/test_publication_domain.py</read_first>
  <action>Create bounded canonical fixture instructions and deferred-import RED tests for `NominationSetV1`, `BenchmarkEntryV1`, `BenchmarkLockAttestationV1`, `LockedBenchmarkManifestV1`, `AcceptanceScenarioResultV1`, `HostedIsolationCapabilityV1`, `OfflineAdversarialRunV1`, `ReplayEvidenceV1`, `ChangedSourceEvidenceV1`, `GateB4BindingV1`, `HumanSkillReviewAttestationV1`, `ProbeCleanupAttestationV1`, `ReviewerCalibrationV1`, `AcceptanceEvidenceRootV1`, and `AcceptanceReleaseVerdictV1`. Per D-01..D-06, test exact repository ID/SHA/license/provenance, two-positive including multi-workflow/two-negative/one-borderline distribution, supplemental `user_nominated` isolation, manifest revision, and human lock. Per D-11..D-17, use closed enums for business terminals versus system failures, hard-gate conjunction, exact-head positive human verdict, and non-waivable security/permission/idempotency/provenance/license gates. Per D-24..D-26, freeze `HostedIsolationCapabilityV1` as an extra-forbidden frozen model with schema version `hosted-isolation-capability-v1` and exact fields `workflow_sha256`, `source_commit_sha`, `hosted_run_id`, `run_attempt`, `runner_image`, `isolation_mechanism`, `probe_artifact_locator`, `probe_artifact_digest`, `control_command_digest`, `direct_probe_command_digest`, `child_probe_command_digest`, `control_outcome`, `direct_network_outcome`, `child_network_outcome`, `credential_count`, `state_write_capability`, `synthetic_scan_manifest_digest`, `synthetic_canary_hit_count`, `reviewer_id`, `reviewed_at`, and recomputed `capability_digest`. Freeze `OfflineAdversarialRunV1` with schema version `offline-adversarial-run-v1` and exact fields `acceptance_run_id`, `hosted_capability_digest`, `workflow_sha256`, `source_commit_sha`, `hosted_run_id`, `run_attempt`, `isolation_mechanism`, `scenario_matrix_digest`, sorted-unique `required_scenario_ids`, sorted-unique `completed_scenario_ids`, aligned `scenario_result_digests`, `controlled_scenario_count`, `os_syscall_network_denied`, `direct_network_denied`, `child_network_denied`, `untrusted_execution_count`, `unapproved_network_effect_count`, `unauthorized_effect_count`, `synthetic_scan_manifest_digest`, `synthetic_canary_hit_count`, `started_at`, `completed_at`, and recomputed `run_digest`. Require the capability model to admit only the reviewed `docker_network_none` mechanism, a passing offline control, both probes denied, zero credentials/state-write capability/canary hits; require the run model to bind the persisted capability digest plus identical workflow/source/run-attempt/mechanism identities, at least fifteen completed controlled scenarios, all required scenario IDs exactly once, all three network-denial booleans true, and zero execution/network/unauthorized/canary effects. Mutation-test stale self-digests, capability/run binding mismatch, missing/duplicate/reordered scenarios or result digests, false denial, nonzero forbidden effects, completion before start, artifact locator used as authority, and any raw log, fixture prose, response body, authorization, token, private-key, unrestricted-path, or credential field. `ReviewerCalibrationV1` is likewise strict, versioned, self-digested, bounded to redacted label/result digests and deterministic agreement/kappa/disagreement facts, and advice-only. Add a standard-library `verify_phase6_red_contracts.py` runner that invokes locked pytest, requires successful collection, checks the exact named missing-contract failures for each suite, rejects unexpected node IDs/failure text/tracebacks, and exits zero only for that intended RED state. Fixtures contain only synthetic identities and canaries; never repository bodies or secret-bearing material.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_red_contracts.py --suite domain</automated></verify>
  <acceptance_criteria>
    - Tests collect without importing a nonexistent module at collection time and fail only on named missing Phase 6 contracts.
    - Extra fields, stale self-digests, moving refs, wrong license, wrong distribution, missing lock, stale Draft head, and score-based hard-gate waiver are explicit failures.
    - Provider/schema exhaustion cannot satisfy any negative-business-outcome case.
    - Hosted capability/offline run contracts reject identity drift, incomplete scenario coverage, false denial, any forbidden effect/canary hit, noncanonical ordering, or raw/secret-bearing payload fields.
  </acceptance_criteria>
  <done>Every acceptance authority and terminal class has a strict executable contract.</done>
</task>

<task type="auto">
  <name>Task 06-01-02: Freeze evaluator-blind orchestration and closed provider policy</name>
  <files>tests/test_acceptance_application.py, tests/test_semantic_provider.py, tools/verify_phase6_red_contracts.py</files>
  <read_first>src/skillscout/application/discovery.py, src/skillscout/bootstrap.py, src/skillscout/adapters/semantic_provider.py, tests/test_discovery_application.py, tests/test_semantic_provider.py, tests/test_openai_extract.py, tests/test_openai_generate.py, tests/test_openai_review.py</read_first>
  <action>Add deferred-import RED tests for separate nomination, locked-run, replay/update, attestation, and rebuild dependency surfaces. Nomination must expose Search/read/state only; offline evaluation must expose no live adapter; expected role, expected outcome, evaluator notes, and human labels must be absent from serialized Extractor, Generator, and Reviewer messages. Extend provider tests for a typed `SemanticStage` and exact D-07..D-10 mapping: `deepseek-v4-flash` extraction, `deepseek-v4-flash` generation, `deepseek-v4-pro` review, official `https://api.deepseek.com`, no tools, `max_retries=0`, and zero credential/HTTP access for arbitrary model or endpoint input. Preserve deterministic mocked OpenAI coverage without requiring `OPENAI_API_KEY`.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_red_contracts.py --suite application-provider</automated></verify>
  <acceptance_criteria>
    - Capability signature tests prove benchmark hypotheses cannot enter semantic request builders.
    - Wrong stage/model and endpoint variants fail before key lookup and before the transport records a call.
    - Reviewer tests require a fresh request over WorkflowSpec/package/provenance/validation only and expose no edit/publication dependency.
  </acceptance_criteria>
  <done>The intended provider and orchestration boundaries are frozen before implementation.</done>
</task>

<task type="auto">
  <name>Task 06-01-03: Finalize and independently verify the exact Phase 6 validation map</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, tools/verify_phase6_validation_map.py, tools/verify_phase6_acceptance.py</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md, .planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/06-adversarial-mvp-acceptance/06-01-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-02-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-03-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-04-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-05-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-06-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-07-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-08-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-09-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-10-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-11-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-12-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-13-PLAN.md, .planning/phases/06-adversarial-mvp-acceptance/06-14-PLAN.md, tools/verify_phase5_validation_map.py, tools/verify_phase5_acceptance.py</read_first>
  <action>Read all fourteen Phase 6 plans and replace every provisional row with exactly one primary row for each of the 38 exact `06-NN-NN` task IDs. Record each plan/wave, TEST-01..TEST-04 coverage, automated command, checkpoint type and exact predecessor/post-ingest dependency, and every Wave 0 file owner. Build an exact cross-plan symbol ownership registry covering every named model, enum, operations fact kind and model mapping, attestation, report type, CLI command, workflow input/action/job, verifier, and persistence/export/rebuild surface: each later consumption must resolve to an existing source symbol or an earlier/same-plan creation task reachable through `depends_on`; reject missing owners and file-owning producer/consumer inversions. The registry must specifically bind `HostedIsolationCapabilityV1` → `acceptance_hosted_isolation_capability` and `OfflineAdversarialRunV1` → `acceptance_offline_adversarial_run` to Task 06-04-01/02 before Task 06-06-03, and `ReviewerCalibrationV1` → `acceptance_reviewer_calibration` before Task 06-11-03. Create `verify_phase6_validation_map.py` now as a standard-library-only, read-only, network-free, project-import-free verifier that parses all fourteen plans plus `06-VALIDATION.md`, rejects missing/duplicate/orphan tasks or symbols, checks checkpoint dependencies, proves TEST-01..TEST-04 forward and inverse coverage, requires a concrete automated command for every task, and verifies the complete Wave 0 file set before Wave 1 admission. Also create the initial fixed hard-gate registry in `verify_phase6_acceptance.py`; future live facts remain explicit failures. Keep `nyquist_compliant:false` / `wave_0_complete:false` until execution evidence exists, and do not fabricate any approval, hosted result, repository, SHA, token, or evidence value.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py --plan-contract &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py --registry-only</automated></verify>
  <acceptance_criteria>
    - Every task in every Phase 6 PLAN appears exactly once as a primary validation row or explicit checkpoint dependency.
    - TEST-01, TEST-02, TEST-03, and TEST-04 each map in both directions.
    - Every checkpoint identifies its automated predecessor and post-ingest verifier, and every Wave 0 file is owned and required before Wave 1.
    - Every later named contract, command, workflow surface, verifier, and persistence mapping has one existing or reachable earlier/same-plan owner; hosted/offline/calibration mappings are explicit.
    - No status, approval, hosted result, repository, SHA, token, or evidence value is fabricated.
  </acceptance_criteria>
  <done>Executors have an exact, non-provisional Phase 6 feedback map.</done>
</task>

</tasks>

<threat_model>
## Assets
- Locked benchmark identities, evaluator labels, provider policy, task map, and synthetic fixture corpus.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted fixture data → contract tests | Fixture text must remain inert and bounded. |
| Evaluator metadata → semantic inputs | Expected labels must not enter model requests. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-01 | Tampering | benchmark/hosted/offline evidence contracts | high | mitigate | Strict models, exact capability/run bindings, canonical self-digests, mutation tests. |
| T-06-02 | Elevation of Privilege | evaluator-to-prompt boundary | high | mitigate | Separate types and serialized-request absence assertions. |
| T-06-03 | Information Disclosure | fixtures/tests | high | mitigate | Synthetic canaries and exact allowlisted fixtures; no secret reads. |
| T-06-SC | Tampering | package supply chain | high | mitigate | No package additions or installs; locked toolchain only. |
</threat_model>

## Artifacts this phase produces
- `tests/fixtures/acceptance/scenario_matrix.json`
- Test contracts for all named Phase 6 models, including `HostedIsolationCapabilityV1`, `OfflineAdversarialRunV1`, `ReviewerCalibrationV1`, and `SemanticStage`
- Cross-plan ownership registry for models, enums, fact kinds/mappings, attestations, report types, CLI/workflow surfaces, verifiers, and persistence seams
- Exact `06-VALIDATION.md` task IDs and commands

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Repeatable evidence-backed reviewer acceptance | COVERED |
| REQ | TEST-01..04 | Real benchmark, scenarios, idempotency, real Draft/denials | COVERED |
| RESEARCH | — | Strict contracts, evaluator blindness, closed provider policy, Wave 0 map | COVERED |
| CONTEXT | D-01..D-17,D-24..D-27 | Locked benchmark/provider/verdict/evidence rules | COVERED |

<verification>
Wave 0 contract suites collect and fail only for the intended missing production surfaces; the exact validation map contains no provisional identifiers.
</verification>

<success_criteria>
No Phase 6 implementation can pass by omitting a locked decision, collapsing business/system outcomes, leaking expected labels, or widening provider policy.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-01-SUMMARY.md`.</output>
