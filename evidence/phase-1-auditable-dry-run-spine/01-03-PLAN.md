---
phase: 01-auditable-dry-run-spine
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/skillscout/cli.py
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/domain/enums.py
  - src/skillscout/domain/models.py
  - src/skillscout/domain/canonical.py
  - src/skillscout/adapters/fixtures.py
  - src/skillscout/adapters/state.py
  - tests/test_cli_dry_run.py
  - tests/test_stage_contracts.py
  - tests/test_pipeline_resume.py
autonomous: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Strict frozen contracts persist precomputed input_hash, producer_version, retry_policy_version and canonical reusable_key_digest on every running attempt before processor invocation."
    - "A frozen database produced by the real Wave-2 CLI with Generator as the last checkpoint and no Validators attempt migrates transactionally to version 2, then invokes Validators first without replaying Scout through Generator."
    - "Migration uses BEGIN IMMEDIATE, validates copied rows/digests/foreign keys/non-null identity, sets user_version=2 last, and rolls back fully to readable v1 on error."
    - "Manifests are content-addressed and durable before one result/succeeded-attempt/checkpoint transaction; reuse verifies manifest and output hashes."
    - "Retry exhaustion counts exactly three attempts for one reusable digest, refuses a fourth invocation, and changed input/producer/retry-policy identity receives a separate budget without old-result reuse."
    - "The operator can interrupt, resume and inspect durable attempts/results/checkpoint JSON."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "strict immutable run/attempt/result/checkpoint contracts", exports: [StrictFrozenModel, TokenUsage, StageInput, StageEnvelope, StageAttempt, RunRecord, Checkpoint, PublicationPlan, RunSummary] }
    - { path: src/skillscout/domain/canonical.py, provides: "canonical bytes, non-circular hashes and retry-key identity", exports: [canonical_json_bytes, sha256_digest, stage_input_hash, stage_output_hash, stage_manifest_hash, reusable_key_digest, make_result_id] }
    - { path: src/skillscout/adapters/state.py, provides: "v1-to-v2 migration, transactional attempts/results/checkpoints and atomic manifests", exports: [SQLiteStateStore] }
    - { path: tests/fixtures/state/v1-cli.db, provides: "frozen real Wave-2 CLI-produced schema-v1 database" }
    - { path: tests/test_pipeline_resume.py, provides: "migration, rollback, identity-scoped retry and resume evidence" }
  key_links:
    - { from: tests/fixtures/state/v1-cli.db, to: src/skillscout/adapters/state.py, via: "copy then StateStore.open BEGIN IMMEDIATE migration", pattern: "user_version" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "precomputed StageAttempt identity committed before processor call", pattern: "reusable_key_digest" }
    - { from: src/skillscout/adapters/state.py, to: "<db-stem>.manifests/<stage>/<manifest-hash>.json", via: "fsync/replace before result/checkpoint commit", pattern: "os.replace" }
    - { from: src/skillscout/cli.py, to: src/skillscout/adapters/state.py, via: "inspect-run and resume consume verified durable state", pattern: "inspect_run" }
---

<objective>
Upgrade the safe Walking Skeleton to strict immutable contracts, migrate its real schema-v1 state to v2 without loss, and deliver digest-scoped retry/resume/inspect behavior.

Purpose: Close OPS-01 and recovery semantics while proving that the preceding vertical slice's on-disk state is preserved rather than silently replaced.
Output: Domain contracts/canonical identity, frozen v1 fixture, transactional migration/rollback, atomic manifests, bounded retry, inspect and resume.
</objective>

## Phase Goal

**As a** SkillScout operator, **I want to** run a frozen fixture through the complete dry-run pipeline, **so that** I can inspect structured, resumable evidence without any remote side effects.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md
@.planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md
@.planning/phases/01-auditable-dry-run-spine/01-02-SUMMARY.md
@src/skillscout/application/pipeline.py
@src/skillscout/adapters/fixtures.py
@src/skillscout/adapters/state.py
@src/skillscout/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-02-01: Define immutable envelopes and precomputed retry identity</name>
  <files>src/skillscout/domain/enums.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, tests/test_stage_contracts.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_cli_dry_run.py, .planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md</read_first>
  <behavior>
    - Strict frozen models reject extras/coercion/mutation and retain explicit nullable prompt/policy/model/request/latency/token/error fields.
    - Input, output and manifest hashes use exact non-self-referential preimages; semantic output identity excludes run/attempt/time/request fields.
    - reusable_key_digest equals SHA-256 of canonical subject_id, stage, input_hash, producer_version and retry_policy_version.
    - StageAttempt requires all four precomputed identity/budget fields before status running can validate.
    - Only fixed stages and legal run/attempt transitions validate.
  </behavior>
  <action>Write RED cases, then create closed `PipelineStage`, `ExecutionMode`, `RunStatus`, `AttemptStatus` and explicit `validate_stage_successor`, `validate_run_transition`, `validate_attempt_transition` maps. Use one Pydantic `StrictFrozenModel` with `extra="forbid"`, `frozen=True`, `strict=True`; define `TokenUsage`, `StageInput`, `StageEnvelope`, `StageAttempt`, `RunRecord`, `Checkpoint`, `PublicationPlan`, `RunSummary`. `StageAttempt` structurally requires `input_hash`, `producer_version`, `retry_policy_version`, `reusable_key_digest`, attempt number/status and all nullable telemetry before running. Make `canonical_json_bytes` the only encoder: JSON-compatible dump with explicit nulls, sorted keys, compact separators, Unicode, `allow_nan=False`, UTF-8. Implement `sha256_digest`, three exact input/output/manifest preimages, `reusable_key_digest` over exactly the five identity fields, and deterministic `make_result_id`; stored digests use `sha256:<64 lowercase hex>` and never hash themselves.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_stage_contracts.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain tests/test_stage_contracts.py</automated></verify>
  <acceptance_criteria>
    - Focused tests prove strict extras/coercion/frozen behavior, explicit nulls, exact stages/transitions and digest format.
    - Output hash is stable across run/attempt/time/request changes and changes with semantic payload or producer/prompt/policy/model version.
    - Manifest hash covers the immutable envelope except itself; input and reusable digests have their exact independent preimages.
    - A running StageAttempt missing any input/producer/retry-policy/reusable-digest field fails validation.
    - Changing each reusable-key preimage field independently changes the digest; attempt number/status/telemetry changes do not.
  </acceptance_criteria>
  <done>All ledger and retry decisions have strict, stable, non-circular identity contracts before persistence refactoring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02-02: Migrate real v1 state to atomic schema v2 or roll back</name>
  <files>src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py</files>
  <read_first>src/skillscout/domain/enums.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/fixtures/state/v1-cli.db, tests/fixtures/state/v1-cli-provenance.json, tests/test_cli_dry_run.py, .planning/phases/01-auditable-dry-run-spine/01-02-SUMMARY.md</read_first>
  <behavior>
    - A frozen DB generated by the actual Wave-2 `skillscout dry-run` command has user_version 1 and provenance/hash evidence, not hand-authored SQL.
    - Opening a temporary copy of that interrupted DB under the new adapter migrates to v2 under BEGIN IMMEDIATE, preserving run_id and Generator checkpoint; invocation canaries make Scout through Generator fail if replayed and prove Validators is first invoked.
    - Forced failure at each migration seam rolls back to user_version 1 with no partial v2 objects/rows and reports state_schema_migration_error.
    - Missing DB creates current v2 transactionally; existing v2 is idempotent; version 0, malformed or future version fails state_schema_incompatible without recreation.
    - Successful stage manifest bytes become durable before one result/succeeded-attempt/checkpoint transaction; processor runs outside transactions.
  </behavior>
  <action>Verify the tracked `tests/fixtures/state/v1-cli.db` SHA-256 and facts against `v1-cli-provenance.json`: it must be an immutable real packaged schema-v1 CLI output created in Wave 2 with run status `interrupted`, Generator as the durable last checkpoint and no Validators attempt. Stop if it is absent, modified, completed, hand-authored or internally inconsistent. RED tests copy that exact frozen DB to a temporary path; migration and resume never modify the tracked fixture.

Define provider-independent `StageProcessor`, `StateStore`, `Clock`, `IdProvider` ports and refactor the runner/fixture processors to strict domain objects. Implement `SQLiteStateStore.open()` with exact state versions: nonexistent path creates schema v2 transactionally; version 2 opens idempotently; version 1 starts `BEGIN IMMEDIATE`, creates v2 structures, copies rows using the already-persisted run/stage/input/producer/retry/digest/output identity, validates non-null fields, recomputed digest, row counts and foreign keys, sets `PRAGMA user_version=2` as the final mutation, then commits. Version 0, malformed database or version greater than 2 returns `state_schema_incompatible` without delete/recreate. Any migration exception rolls back completely and returns `state_schema_migration_error`, leaving the copied v1 DB readable with no partial v2 state. For successful migration/resume, install invocation canaries for Scout, Filter, Reader, Extractor, Qualifier and Generator that fail the test if called; require the first processor invocation to be Validators, followed by Reviewer and PublicationPlanner, while preserving the original run ID, six result hashes and Generator checkpoint identity.

Use explicit transactions/foreign keys and preserve the running-attempt-before-processor invariant. Write canonical `StageEnvelope` bytes to an internally derived same-filesystem temporary path, flush/fsync, `os.replace`, sync the parent where supported, then in one transaction insert immutable result, mark attempt succeeded and advance checkpoint. Failures record sanitized attempt state separately and never advance. Reuse/inspect validates schema, full manifest hash and embedded output hash. Keep the complete CLI acceptance test green after migration/refactor.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_pipeline_resume.py tests/test_cli_dry_run.py</automated></verify>
  <acceptance_criteria>
    - Frozen v1 fixture provenance hash matches its bytes and proves it was emitted by the actual previous CLI/adapter with required identity fields.
    - Its temporary migrated copy reaches user_version 2 with identical run ID, Generator checkpoint and six reused result hashes; canaries prove Validators is the first invocation and Scout through Generator are never replayed. A new DB starts directly at v2.
    - Every forced mid-migration failure leaves user_version 1, original rows readable, zero partial v2 state, and `state_schema_migration_error`.
    - Version 0, malformed and future-version files return `state_schema_incompatible` and retain original bytes/state.
    - Manifest-before-DB and processor-outside-transaction failure tests pass; focused CLI acceptance remains green.
  </acceptance_criteria>
  <done>The real prior vertical slice survives an explicit fail-closed v1-to-v2 migration, and v2 persistence is atomically auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02-03: Complete digest-scoped retry, resume and inspect</name>
  <files>src/skillscout/cli.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py</files>
  <read_first>src/skillscout/cli.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py</read_first>
  <behavior>
    - The existing fail-after Generator behavior remains schema-v1 compatible; fresh v2 failure injection durably checkpoints the named stage, and rerun reuses verified results and finishes.
    - Stale running attempts become abandoned before a new attempt number starts.
    - Exactly three attempts sharing one reusable_key_digest consume that digest's budget; a fourth call is refused without processor invocation.
    - Changing canonical input, producer version or retry-policy version independently creates a new digest/budget and old results/attempts do not count or reuse.
    - inspect-run returns persisted run, ordered attempts/results, checkpoint, reused count and sanitized error fields.
  </behavior>
  <action>Retain and migrate the existing `dry-run --fail-after STAGE` contract; add `inspect-run RUN_ID --state PATH --format json`. In fresh schema-v2 runs, failure injection still occurs only after the named durable checkpoint, marks interrupted and stops before the successor. On resume, verify manifests/hashes, mark stale running attempts abandoned and use monotonically increasing attempt numbers. Define `RetryPolicy(version, max_attempts=3, transient_error_codes)`; query/count attempts only through an index on canonical `reusable_key_digest` and persist the full preimage fields for audit. A fourth unchanged-digest attempt returns `retry_exhausted` before processor invocation. Add independent tests changing input hash, producer version and retry-policy version: each must produce a distinct digest/fresh three-attempt budget and must not reuse the old digest's result. Permanent errors never retry. `inspect_run` emits every required nullable telemetry/identity field from SQLite, not process memory, with exit codes 0/1/2 preserved. Keep the frozen Wave-2 database immutable; its migration and Validators-first proof remain owned by Task 01-02-02 rather than being replaced by a completed-state shortcut.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - The Wave-2 frozen fixture still proves fail-after exits 1 after durable Generator with no Validators attempt; its migrated copy resumes at Validators, while a fresh v2 interruption/rerun exits 1 then 0 with positive reuse and identical reused result/output/manifest hashes.
    - Stale running attempt becomes abandoned and replacement attempt number increments.
    - Three transient failures exist for one digest; the fourth request returns `retry_exhausted` with processor call count still three.
    - Input, producer and retry-policy changes each yield a different digest, do not reuse old output, and receive an independent budget; attempt number/telemetry changes do not create a new digest.
    - `inspect-run` reports precomputed identity, all attempts/results/checkpoint and explicit nullable request/latency/token/error fields.
  </acceptance_criteria>
  <done>Recovery and retry are finite, digest-scoped and inspectable, including preserved state migrated from the Walking Skeleton.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Schema-v1 file → schema-v2 state adapter | Existing durable bytes must migrate without silent deletion, invention or partial commit. |
| Stage processor → typed envelope | Output crosses into immutable audit identity. |
| Persisted attempt/result → retry/resume | Stored identity and bytes determine reuse and retry budget. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | locked execution graph | high | mitigate | Use unchanged Gate-B-approved lock and repository-local managed toolchain; no new dependency. |
| T-01-03 | Tampering / Repudiation | envelopes and reusable identity | high | mitigate | Strict frozen schema, exact hash preimages, persisted preprocessor identity and canonical digest. |
| T-01-04 | Tampering / Repudiation | interrupted v1 migration and manifest/DB durability | high | mitigate | Immutable real CLI-produced Generator-interrupted fixture, temporary-copy migration, Scout–Generator replay canaries, Validators-first invocation, BEGIN IMMEDIATE validate-copy-version-last transaction/rollback and manifest fsync/replace before DB commit. |
| T-01-05 | Denial of Service / Spoofing | retry/resume | high | mitigate | Digest-indexed three-attempt ceiling, no fourth invocation, changed-identity separation, schema/hash/transition verification and abandoned-attempt handling. |

</threat_model>

## Artifacts this phase produces

- Files: domain `enums.py`, `models.py`, `canonical.py`; `tests/test_stage_contracts.py`, `tests/test_pipeline_resume.py`; refined CLI/application/adapters and CLI tests. This plan consumes but never modifies the real frozen `tests/fixtures/state/v1-cli.db` and provenance JSON produced by Plan 02.
- Contracts: `PipelineStage`, `ExecutionMode`, `RunStatus`, `AttemptStatus`, transition validators, `StrictFrozenModel`, `TokenUsage`, `StageInput`, `StageEnvelope`, `StageAttempt`, `RunRecord`, `Checkpoint`, `PublicationPlan`, `RunSummary`.
- Identity functions: `canonical_json_bytes`, `sha256_digest`, `stage_input_hash`, `stage_output_hash`, `stage_manifest_hash`, `reusable_key_digest`, `make_result_id`.
- Ports/policies: `StageProcessor`, `StateStore`, `Clock`, `IdProvider`, `RetryPolicy(version, max_attempts, transient_error_codes)`.
- State schema v2/migration: `StateStore.open`, BEGIN-IMMEDIATE v1 migration, `user_version=2`, rollback errors `state_schema_incompatible`/`state_schema_migration_error`, digest retry index, atomic manifest tree.
- CLI: `inspect-run RUN_ID --state PATH --format json`, `dry-run --fail-after STAGE`.
- Runtime states/errors: interrupted, abandoned, retry_exhausted, state_integrity_error; structured inspect JSON and content-addressed manifests.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Inspectable resumable stage ledger | 01-03 | COVERED | Migration, manifests, resume and inspect deliver it. |
| REQ | OPS-01 | Complete stage/attempt identity and telemetry | 01-03 | COVERED | Strict models plus v2 persistence/inspect. |
| REQ | OPS-04 | Limited retry and checkpoint recovery | 01-03 | COVERED | Retry budget is exact digest identity, not mutable configuration. |
| RESEARCH | — | Explicit v1→v2 transactional migration | 01-03 | COVERED | Immutable interrupted CLI fixture, temporary-copy version-last commit, Validators-first no-replay canaries and rollback evidence. |
| RESEARCH | — | Precomputed retry identity before processor | 01-03 | COVERED | Required on model, DB and running attempt. |
| RESEARCH | — | Changed identity receives separate retry budget | 01-03 | COVERED | Each input/producer/retry-policy dimension has a test. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No D-IDs exist. |

<verification>
- Post-Gate-B verification command: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_stage_contracts.py tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_stage_contracts.py tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q`.
- Exact self-contained prefixed contract, migration, resume and CLI focused suites pass; the Wave-3 full suite runs only after those tasks are GREEN.
- A copy of the real interrupted v1 fixture migrates/resumes with Validators first and Scout–Generator never invoked; forced failure rolls back to intact v1; unknown versions fail closed.
- Fourth unchanged-digest retry is refused; each changed identity receives an independent budget.
- Full CLI happy path remains green after refactor.
</verification>

<success_criteria>
- OPS-01 is represented in strict models, DB rows, manifests and inspect output.
- OPS-04 retry/resume is finite and keyed by immutable persisted identity.
- No prior state is silently recreated and no checkpoint points at unverified bytes.
</success_criteria>

<output>After completion, create `.planning/phases/01-auditable-dry-run-spine/01-03-SUMMARY.md` with migration/rollback fixture hashes and digest-budget evidence.</output>
