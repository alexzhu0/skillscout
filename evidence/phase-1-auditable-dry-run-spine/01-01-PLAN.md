---
phase: 01-auditable-dry-run-spine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .python-version
  - pyproject.toml
  - uv.lock
  - src/skillscout/__init__.py
  - tests/conftest.py
  - tests/fixtures/pipeline/approved.json
  - tests/test_cli_dry_run.py
autonomous: false
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Gate A blocks before any untrusted toolchain artifact is downloaded or executed and approves the exact Darwin/aarch64 uv and managed-CPython artifacts plus direct declarations."
    - "Only checksum-verified uv 0.11.29 and Astral CPython 3.13.14 build 20260623 exist in ignored repository-local directories; system uv/Python and automatic downloads are never fallbacks."
    - "Static project/test metadata is resolved with the exact non-building, no-source, no-cache lock command without syncing, building, importing or testing a locked package."
    - "Gate B allows exactly one first-party root skillscout==0.1.0 node at canonical editable dot with reviewed static metadata and no artifacts, then blocks before execution until every external registry-only node/artifact and the exact uv.lock bytes are approved."
  artifacts:
    - { path: .gitignore, provides: "exclusion of repository-local toolchain, managed runtime, environments, build and demo outputs" }
    - { path: .python-version, provides: "exact managed CPython selector 3.13.14" }
    - { path: pyproject.toml, provides: "static PEP 621 declarations, exact build backend/dependencies and packaged CLI metadata" }
    - { path: uv.lock, provides: "non-building discovered graph awaiting then receiving Gate B approval" }
    - { path: tests/test_cli_dry_run.py, provides: "unexecuted RED and minimum fixture-safety contract for Wave 2" }
  key_links:
    - { from: "Gate A approval", to: .tools/uv-0.11.29/bin/uv, via: "immutable asset name, checksum and optional trusted-base attestation", pattern: "61c04acc52a33ef0f331e494bdfbedcdb6c26c6970c022ed3699e5860f8930e3" }
    - { from: .tools/python-mirror, to: .tools/python, via: "file-only UV_PYTHON_INSTALL_MIRROR and pinned build 20260623", pattern: "795a5aeeb050f00aa8a2214d779bad9f1b9113edb6923317a80c042a11a087d7" }
    - { from: pyproject.toml, to: uv.lock, via: "uv lock --no-build --no-sources --no-cache with only managed CPython 3.13.14", pattern: "uv_build==0.11.29" }
    - { from: uv.lock, to: "Gate B approval", via: "every package/edge/source/artifact and exact lock hash reviewed before execution", pattern: "[[package]]" }
---

<objective>
Establish a verified repository-local uv/managed-CPython toolchain, safely discover the complete dependency graph, and obtain two distinct blocking approvals before any project build or execution.

Purpose: Make the phase executable on a host that has no uv and only Python 3.9.6 without silently trusting a floating installer, system fallback or unseen transitive package.
Output: Ignored verified toolchain/runtime, static package/test scaffold, non-building `uv.lock`, Gate A evidence and Gate B exact-lock approval.
</objective>

## Phase Goal

**As a** SkillScout operator, **I want to** run a frozen fixture through the complete dry-run pipeline, **so that** I can inspect structured, resumable evidence without any remote side effects.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md
@.planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md
@.planning/phases/01-auditable-dry-run-spine/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 01-01-01: Gate A — approve exact toolchain and direct declarations</name>
  <files>None — Gate A precedes every external toolchain download, extraction or execution.</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md</read_first>
  <action>Present the full Gate A record and stop until a human explicitly approves it. Verify host tuple `Darwin/aarch64`; uv release `0.11.29`, commit `901092ee11a89ba287f274e3c6e3a2e18ec2fba2`, asset `uv-aarch64-apple-darwin.tar.gz`, SHA-256 `61c04acc52a33ef0f331e494bdfbedcdb6c26c6970c022ed3699e5860f8930e3`, and official GitHub attestation instructions; upstream CPython `3.13.14`; distinct Astral python-build-standalone release `20260623`, asset `cpython-3.13.14+20260623-aarch64-apple-darwin-install_only_stripped.tar.gz`, URL `https://github.com/astral-sh/python-build-standalone/releases/download/20260623/cpython-3.13.14%2B20260623-aarch64-apple-darwin-install_only_stripped.tar.gz` and SHA-256 `795a5aeeb050f00aa8a2214d779bad9f1b9113edb6923317a80c042a11a087d7`; and direct declarations `uv-build==0.11.29`/backend `uv_build`, `pydantic==2.13.4`, `pytest==9.1.1`, `ruff==0.15.21` with official owners/repositories. A different host, asset, hash, version or owner stops execution for a new review. Do not let auto-advance approve SUS inputs.</action>
  <verify><automated>N/A — this provenance decision is intentionally human-only and blocks before external executable bootstrap.</automated></verify>
  <acceptance_criteria>
    - The approval records the complete host, uv commit/asset/hash/attestation evidence, both upstream and redistributed Python identities, and all four direct Python declarations.
    - The reviewer explicitly distinguishes Python.org's upstream release from Astral's managed binary provenance.
    - No uv/runtime archive is downloaded, extracted or executed before the approval signal.
  </acceptance_criteria>
  <done>Gate A is explicitly approved in the execution record; otherwise the plan stops with no external toolchain state.</done>
</task>

<task type="auto">
  <name>Task 01-01-01A: Bootstrap verified local tools and discover the lock without execution</name>
  <files>.gitignore, .python-version, pyproject.toml, uv.lock, src/skillscout/__init__.py, tests/conftest.py, tests/fixtures/pipeline/approved.json, tests/test_cli_dry_run.py</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md</read_first>
  <action>Proceed only after Gate A. Add `.tools/`, `.venv/`, `dist/` and `.tmp/` to `.gitignore`. Download only the fixed uv archive and `.sha256` from `https://releases.astral.sh/github/uv/releases/download/0.11.29/`; require `shasum -a 256 -c uv-aarch64-apple-darwin.tar.gz.sha256` and the full approved digest to match before extraction. If `gh` is already a trusted base executable, also run `gh attestation verify uv-aarch64-apple-darwin.tar.gz --repo astral-sh/uv`; do not install `gh` for this optional check. Extract verified bytes into `.tools/uv-0.11.29/`, expose the binaries only through `.tools/uv-0.11.29/bin`, prepend that directory to this task shell, and require `command -v uv` to resolve there plus `uv --version` to report `0.11.29` and commit prefix `901092e` when present. Never pipe an installer to a shell, run self-update, edit shell profiles, or use package-manager/system uv.

Download only `https://github.com/astral-sh/python-build-standalone/releases/download/20260623/cpython-3.13.14%2B20260623-aarch64-apple-darwin-install_only_stripped.tar.gz` and stage it at `.tools/python-mirror/20260623/cpython-3.13.14+20260623-aarch64-apple-darwin-install_only_stripped.tar.gz`; verify the full approved digest. Set `UV_PYTHON_INSTALL_MIRROR=file:///Users/alexzhu/Lenovo/skillscout/.tools/python-mirror`, `UV_PYTHON_CPYTHON_BUILD=20260623` and `UV_PYTHON_INSTALL_DIR=/Users/alexzhu/Lenovo/skillscout/.tools/python`, then invoke only the verified uv with `uv python install --mirror "$UV_PYTHON_INSTALL_MIRROR" --install-dir "$UV_PYTHON_INSTALL_DIR" --no-bin cpython@3.13.14`. Resolve with `uv python find --managed-python --no-python-downloads cpython@3.13.14`; require the returned path below that install directory and execute it only to assert `sys.implementation.name == "cpython"` and `sys.version_info[:3] == (3, 13, 14)`. Record that path under ignored `.tools/approved-python-path`. From then on set `UV_MANAGED_PYTHON=1`, `UV_PYTHON_DOWNLOADS=never`, the same install directory and verified uv path for every command; no system fallback is permitted.

Write `.python-version` exactly `3.13.14`. Create only static PEP 621 `pyproject.toml`: exact `pydantic==2.13.4`, dev `pytest==9.1.1` and `ruff==0.15.21`, `[build-system] requires=["uv_build==0.11.29"]`, `build-backend="uv_build"`, and `[project.scripts] skillscout="skillscout.cli:main"`, with no Git/path/editable/direct-URL source or dynamic metadata. Create the package marker plus unexecuted deterministic fixture/test scaffold; `test_cli_dry_run.py` must name the happy-path assertion and minimum symlink/non-regular/declared-size/stream-overflow/same-descriptor-change cases. Run exactly `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --no-build --no-sources --no-cache --managed-python --no-python-downloads --python 3.13.14`. It may fetch metadata bytes but must stop if resolution needs a build; do not sync, build, import project/locked packages, run uv run, pytest or Ruff.</action>
  <verify>
    <automated>test "$(cat .python-version)" = "3.13.14" &amp;&amp; test -s uv.lock &amp;&amp; /Users/alexzhu/Lenovo/skillscout/.tools/uv-0.11.29/bin/uv --version | grep -F "uv 0.11.29" &amp;&amp; test -x "$(cat /Users/alexzhu/Lenovo/skillscout/.tools/approved-python-path)" &amp;&amp; "$(cat /Users/alexzhu/Lenovo/skillscout/.tools/approved-python-path)" -c 'import sys; assert sys.implementation.name == "cpython" and sys.version_info[:3] == (3, 13, 14)'</automated>
  </verify>
  <acceptance_criteria>
    - uv archive and managed-Python archive match the exact Gate A SHA-256 values before extraction/install; any mismatch stops.
    - `command -v uv` resolves under `.tools/uv-0.11.29/`, uv reports 0.11.29, and the resolved interpreter path is under `.tools/python` and reports CPython 3.13.14.
    - `.gitignore` excludes all local toolchain/runtime/environment/build/demo outputs; `.python-version` contains exactly `3.13.14`.
    - `pyproject.toml` contains only the audited static registry declarations and console entry point; `uv.lock` was produced by the exact non-building/no-source/no-cache command.
    - No package sync/build/import/test occurred, no system Python/uv was used, and no automatic Python download remained enabled after bootstrap.
  </acceptance_criteria>
  <done>The exact local toolchain is verified, static contracts exist, and a complete but uninstalled lock graph is ready for independent review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 01-01-01B: Gate B — approve every locked distribution and exact lock bytes</name>
  <files>uv.lock</files>
  <read_first>uv.lock, pyproject.toml, .planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md</read_first>
  <action>Before any `uv sync`, `uv build`, `uv run`, project/package import, pytest or Ruff command, inspect every `[[package]]` entry and every `sdist`/`wheels` artifact in `uv.lock`. Allow exactly one first-party root node: normalized name `skillscout`, version `0.1.0`, source exactly `source = { editable = "." }` with `.` resolving to the canonical repository root, no `sdist`/`wheels` artifacts, and dependency edges/markers/`requires-dist`/dependency-group metadata exactly matching the already reviewed static `pyproject.toml`. Reject a missing, duplicate, mismatched, symlink-escaping or other workspace/root node. Treat every other node as external: require an exact version, expected edge/marker, the reviewed PyPI registry source, and artifact URL/SHA-256/size; reject Git, path, editable, workspace, direct-URL and alternate-registry sources for every non-root node. Rerun the GSD package-legitimacy seam and official owner/source review for every external transitive distribution. Reject missing hashes, yanked/adverse or unexpected packages, lookalikes, or any Darwin/aarch64 source-build requirement. Compute and record SHA-256 of the exact lock bytes and require the human to approve that hash; Gate A cannot approve packages that were not visible until resolution.</action>
  <verify><automated>N/A — complete transitive provenance and exact-lock approval is a mandatory human security decision.</automated></verify>
  <acceptance_criteria>
    - Exactly one first-party `skillscout==0.1.0` root uses canonical editable `.`, has no artifacts, and its dependency metadata exactly matches reviewed static project declarations.
    - Every package, edge, marker, source and artifact URL/hash/size in `uv.lock` has an explicit disposition; every non-root node is an exact reviewed external PyPI registry node.
    - Every external transitive distribution passes legitimacy/official-owner review; no non-root Git/path/editable/workspace/direct-URL/alternate-registry source or required source build is present.
    - The human explicitly approves the recorded SHA-256 of the current `uv.lock`; later byte changes invalidate Gate B.
    - No sync/build/import/test command has run before approval.
  </acceptance_criteria>
  <done>Gate B explicitly approves the complete graph and exact lock bytes; otherwise execution stops before any locked code is installed or run.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Official release evidence → local executable bytes | Host-specific uv and redistributed CPython artifacts become executable only after immutable identity/integrity review. |
| Static declarations → resolved lock graph | Unseen transitive packages and artifacts emerge during metadata resolution. |
| Approved lock → later build/test environment | Gate B's exact bytes define the only authorized installed graph. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering / Spoofing | complete Phase 1 software supply chain | high | mitigate | Gate A and Gate B remain distinct blocking approvals around verified bootstrap and non-building lock discovery; later execution is bound to the exact approved lock/toolchain. |
| T-01-SC-A | Tampering / Spoofing | uv and managed CPython bootstrap | high | mitigate | Gate A host/commit/asset/hash/provenance approval, mandatory archive checksums, optional trusted-base attestation, repository-local extraction and exact path/version assertions. |
| T-01-SC-B | Tampering / Spoofing | root metadata and external Python distributions | high | mitigate | Non-building/no-source/no-cache discovery; allow only the canonical artifact-free `skillscout==0.1.0` editable root with reviewed metadata, then require registry-only source and every-record/artifact review for all external nodes plus exact-lock approval before execution. |
| T-01-SC-C | Elevation of Privilege | tool/interpreter selection | high | mitigate | Verified uv path first, only managed CPython, automatic downloads disabled, repository-local install directory and no system fallback/self-update/profile mutation. |

</threat_model>

## Artifacts this phase produces

- Tracked files: `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`, `src/skillscout/__init__.py`, `tests/conftest.py`, `tests/fixtures/pipeline/approved.json`, `tests/test_cli_dry_run.py`.
- Package identifiers: `uv_build==0.11.29`, backend `uv_build`, `skillscout = "skillscout.cli:main"`, `pydantic==2.13.4`, `pytest==9.1.1`, `ruff==0.15.21`.
- Ignored bootstrap artifacts: `.tools/uv-0.11.29/`, `.tools/python-mirror/20260623/...tar.gz`, `.tools/python/`, `.tools/approved-python-path`.
- Environment contract: repository-local uv first in `PATH`; `UV_PYTHON_INSTALL_MIRROR`, `UV_PYTHON_CPYTHON_BUILD=20260623`, `UV_PYTHON_INSTALL_DIR`, `UV_MANAGED_PYTHON=1`, `UV_PYTHON_DOWNLOADS=never`.
- Human evidence: Gate A complete toolchain/direct-declaration approval and Gate B approval of the one canonical artifact-free first-party root, every registry-only external lock record/artifact, and the exact lock hash.
- Unexecuted test identities: `test_approved_fixture_reaches_planned_not_published` plus minimum fixture-open safety cases.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible foundation for the operator dry-run | 01-01 | COVERED | Supply-chain gates are prerequisite safety work; Wave 2 immediately delivers the vertical path. |
| REQ | OPS-01 | Versioned/reproducible execution inputs | 01-01 | COVERED | Runtime, declarations and exact lock authority are recorded. |
| REQ | OPS-04 | Safe, repeatable execution environment | 01-01 | COVERED | No unseen package or system fallback can enter recovery tests. |
| RESEARCH | — | No local uv/acceptable Python; exact host-specific bootstrap | 01-01 | COVERED | Gate A and deterministic repository-local install are explicit. |
| RESEARCH | — | Safe lock discovery then complete graph approval | 01-01 | COVERED | Gate B remains a real second stop before code execution. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No CONTEXT.md decisions exist. |

<verification>
- Gate A and Gate B have separate explicit approval records in order.
- Verified uv/interpreter paths and versions match the exact approved artifacts.
- `uv.lock` exists from the exact discovery command and its approved SHA-256 matches current bytes.
- Gate B permits exactly one canonical first-party editable root and rejects every forbidden source form for all non-root nodes.
- No build, sync, import, uv run, pytest, Ruff or full-suite command occurred before Gate B; Wave 1 ends at static approval evidence.
</verification>

<success_criteria>
- The previously missing local toolchain is bootstrapped without floating installers or system fallback.
- The complete dependency graph is approved before any executable package enters the environment.
- Wave 2 can safely run the exact RED command using only the approved lock/toolchain.
</success_criteria>

<output>After completion, create `.planning/phases/01-auditable-dry-run-spine/01-01-SUMMARY.md` with both gate signals, exact artifact hashes/paths and exact approved uv.lock hash.</output>
