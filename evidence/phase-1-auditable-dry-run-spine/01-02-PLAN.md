---
phase: 01-auditable-dry-run-spine
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/skillscout/__init__.py
  - src/skillscout/cli.py
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/adapters/fixtures.py
  - src/skillscout/adapters/state.py
  - tests/conftest.py
  - tests/fixtures/state/v1-cli.db
  - tests/fixtures/state/v1-cli-provenance.json
  - tests/test_cli_dry_run.py
autonomous: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "The exact RED command runs only after Gate B, builds successfully, collects the intended test, and fails only at the named missing-functional-behavior assertion."
    - "The packaged CLI carries the approved fixture through all nine stages to planned_not_published using real SQLite write/read and a local publication-plan.json."
    - "The first runnable fixture reader already rejects symlink/non-regular/oversized/changed input using one descriptor and a 65,536-byte before/during cap."
    - "The first runnable CLI exposes only closed schema-v1 error codes and fixed generic ASCII summaries of at most 160 characters; raw/Pydantic/exception/credential/path input never reaches any emitted or persisted surface."
    - "SQLite schema v1 sets user_version=1 and persists canonical input/producer/retry-policy identity plus reusable digest on the running attempt before processor invocation."
    - "The schema-v1 CLI supports --fail-after generator, durably interrupts after Generator with no Validators attempt, and freezes that real database plus provenance for Plan 03 migration."
    - "No HTTP, GitHub, OpenAI, shell, candidate-code or remote-publication capability exists."
  artifacts:
    - { path: src/skillscout/cli.py, provides: "packaged dry-run command and structured JSON exit contract", exports: [main, build_parser] }
    - { path: src/skillscout/adapters/fixtures.py, provides: "minimum safe one-descriptor fixture reader and deterministic processors", exports: [MAX_FIXTURE_BYTES, FixtureSubject, load_fixture, FixtureProcessor] }
    - { path: src/skillscout/application/pipeline.py, provides: "complete ordered fixture pipeline and v1 reusable identity", exports: [STAGE_SEQUENCE, RETRY_POLICY_VERSION, canonical_v1_digest, PipelineRunner, RunSummary, PublicationPlan] }
    - { path: src/skillscout/adapters/state.py, provides: "real SQLite schema-v1 ledger", exports: [SQLiteStateStore] }
    - { path: tests/test_cli_dry_run.py, provides: "RED/GREEN CLI, minimum fixture-open controls, hostile disclosure canaries and interruption evidence" }
    - { path: tests/fixtures/state/v1-cli.db, provides: "frozen real schema-v1 CLI database interrupted after Generator" }
    - { path: tests/fixtures/state/v1-cli-provenance.json, provides: "command/hash/schema/checkpoint/row-count provenance for the frozen v1 database" }
  key_links:
    - { from: pyproject.toml, to: src/skillscout/cli.py, via: "Gate-B-approved skillscout.cli:main entry point", pattern: "skillscout.cli:main" }
    - { from: src/skillscout/cli.py, to: src/skillscout/adapters/fixtures.py, via: "one bounded descriptor is validated before run creation", pattern: "load_fixture" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "precomputed identity is committed on running attempt before processor call", pattern: "reusable_key_digest" }
    - { from: src/skillscout/cli.py, to: "stdout/stderr, SQLite, manifests and publication-plan.json", via: "closed ErrorCode plus fixed ERROR_SUMMARIES only", pattern: "ERROR_SUMMARIES" }
    - { from: src/skillscout/adapters/state.py, to: publication-plan.json, via: "real ledger readback precedes local-only completion", pattern: "planned_not_published" }
    - { from: "skillscout dry-run --fail-after generator", to: tests/fixtures/state/v1-cli.db, via: "real packaged schema-v1 execution after GREEN", pattern: "user_version=1" }
---

<objective>
Execute the exact RED gate, then make the smallest complete packaged CLI path GREEN with minimum safe fixture opening and a durable schema-v1 retry identity.

Purpose: Deliver the Walking Skeleton only after the approved toolchain/lock exists, while closing the high-severity fixture race and future migration seam in the first runnable slice.
Output: Complete local CLI → nine stages → SQLite v1 write/read → local publication plan behavior and focused security acceptance.
</objective>

## Phase Goal

**As a** SkillScout operator, **I want to** run a frozen fixture through the complete dry-run pipeline, **so that** I can inspect structured, resumable evidence without any remote side effects.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md
@.planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md
@.planning/phases/01-auditable-dry-run-spine/01-SKELETON.md
@.planning/phases/01-auditable-dry-run-spine/01-01-SUMMARY.md
@pyproject.toml
@uv.lock
@tests/test_cli_dry_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 01-01-02: Prove the packaged CLI contract is RED for the intended reason</name>
  <files>tests/test_cli_dry_run.py</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-01-SUMMARY.md, uv.lock, pyproject.toml, src/skillscout/__init__.py, tests/test_cli_dry_run.py, tests/conftest.py, tests/fixtures/pipeline/approved.json</read_first>
  <action>First verify the Plan 01 summary contains both explicit gate approvals and that the current `uv.lock` SHA-256 equals Gate B's approved hash; stop on any mismatch. Every command must independently start with `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv"`; no prior export, activated environment or ambient `PATH` counts. Run the exact prefixed `build --no-sources` and require success. Run the exact prefixed `run --locked pytest --collect-only -q tests/test_cli_dry_run.py` and require `tests/test_cli_dry_run.py::test_approved_fixture_reaches_planned_not_published` plus the minimum fixture-safety and disclosure-canary cases to collect. Then execute exactly `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; ! UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_dry_run.py`, retain stdout/stderr and the un-negated pytest status, and require pytest status 1 with the named happy-path functional assertion failure. Status 2, 4 or 5, import/collection/usage/no-test errors, build failure, missing interpreter or bootstrap/dependency failure is setup failure—not valid RED—and must be fixed before continuing. Do not add production behavior in this task.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; ! UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_dry_run.py</automated></verify>
  <acceptance_criteria>
    - Current lock bytes match Gate B and the verified repository-local uv/CPython paths are active.
    - The exact self-contained prefixed `build --no-sources` succeeds and the exact prefixed pytest collection lists the named happy-path, fixture-safety and hostile-disclosure tests.
    - Raw pytest exits exactly 1 and output identifies the intended missing dry-run behavior assertion; statuses 2/4/5 and setup/import/collection failures are absent.
    - No production module beyond the Plan 01 package marker is implemented during RED.
  </acceptance_criteria>
  <done>The executable contract is demonstrably RED for missing product behavior after a successful approved build and collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-03: Make the safe schema-v1 Walking Skeleton GREEN</name>
  <files>src/skillscout/__init__.py, src/skillscout/cli.py, src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, tests/conftest.py, tests/fixtures/state/v1-cli.db, tests/fixtures/state/v1-cli-provenance.json, tests/test_cli_dry_run.py</files>
  <read_first>tests/test_cli_dry_run.py, tests/conftest.py, tests/fixtures/pipeline/approved.json, src/skillscout/__init__.py, pyproject.toml, .planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md</read_first>
  <behavior>
    - Approved input produces Scout, Filter, Reader, Extractor, Qualifier, Generator, Validators, Reviewer and PublicationPlanner in exact order, then status planned_not_published and zero remote writes.
    - Fixture symlink, directory/non-regular object, declared size over 65,536, cap-plus-one streaming overflow or pre/post same-descriptor metadata change is rejected before stage processing.
    - The reader opens once, validates lstat/fstat identity, reads bounded chunks and compares device/inode/size/mtime_ns/ctime_ns after read before decode/strict parse.
    - Schema v1 persists run/stage/attempt/checkpoint evidence and precomputed input_hash, producer_version, retry_policy_version and reusable_key_digest before each processor invocation.
    - Happy CLI exits 0; structured failure exits 1; argparse errors exit 2.
    - Error output is restricted to `invalid_fixture`, `fixture_changed`, `state_operation_failed` or `pipeline_interrupted` plus fixed generic ASCII summaries no longer than 160 characters.
    - Hostile raw JSON, Pydantic-invalid input, credential canary, attacker absolute path and exception-argument canaries are absent from stdout, stderr, SQLite text, manifests, publication plan and test-visible logs.
    - `--fail-after generator` exits 1 only after durable Generator success, marks the run interrupted, creates no Validators attempt and remains restricted to dry-run mode.
  </behavior>
  <action>Implement the complete local slice. In `fixtures.py`, set `MAX_FIXTURE_BYTES=65536`; `lstat` and reject symlink/non-regular input; open once with `O_NOFOLLOW`, `O_NONBLOCK` and `O_CLOEXEC` where available; `fstat` that descriptor, require regular mode, compare device/inode to the pre-open observation, and reject declared size over the cap. Read bounded chunks with a cap-plus-one probe, never reopen, then `fstat` again and reject changes to device, inode, size, `mtime_ns` or `ctime_ns` before UTF-8 decode, JSON parse and strict Pydantic validation. Platforms without `O_NOFOLLOW` still require lstat/fstat identity checks. Always close the descriptor and never start a run on rejection.

In `application/ports.py` define only local provider-independent stage/state interfaces. In `pipeline.py`, define the exact `STAGE_SEQUENCE`, fixed `RETRY_POLICY_VERSION`, deterministic fixture processors, `PipelineRunner`, thin `RunSummary`/stage result/`PublicationPlan`, and `canonical_v1_digest`: canonicalize `{subject_id, stage, input_hash, producer_version, retry_policy_version}` with sorted compact UTF-8 JSON and SHA-256. Before invoking each processor, freeze those fields, compute `input_hash` and `reusable_key_digest`, then ask state to persist a running attempt; processor work happens only after that commit. Keep explicit nullable prompt/policy/model/request/latency/token fields and stable output hashes.

In `state.py`, create real tables `runs`, `stage_attempts`, `stage_results`, `checkpoints`, set `PRAGMA foreign_keys=ON` and `PRAGMA user_version=1`, and retain `subject_id`, `stage`, precomputed input/producer/retry-policy/digest identity, attempt number/status and successful output hash so Wave 3 can migrate without invention. Complete every stage, advance checkpoint, read the final run back, and write local `publication-plan.json` with `planned_not_published`; no remote method/field/client exists.

Define the schema-v1 diagnostic boundary before any failure can be output or persisted: closed `ErrorCode` values `invalid_fixture`, `fixture_changed`, `state_operation_failed`, `pipeline_interrupted`; a static `ERROR_SUMMARIES` mapping to fixed generic ASCII summaries of at most 160 characters; and a sanitized structured error containing only code and mapped summary. Never serialize or interpolate Pydantic `input`/context/URL, `ValidationError.errors()` payloads, exception `str`/`repr`/`args`, raw JSON/text/bytes, environment values, credential-shaped strings, identifiers or operator/attacker-selected absolute paths. Construct CLI stdout/stderr, SQLite text, manifests, publication plan and test-visible logs only from that allowlist. Add hostile canaries for credential text, an attacker absolute path, raw/Pydantic-invalid JSON and exception args, then byte-search every possible surface and require the canaries absent.

`cli.py` exposes `dry-run --fixture --state --output [--fail-after STAGE]`; validate the stage against the closed stage sequence and reject the option outside dry-run mode. After the named stage result/checkpoint is durable, `--fail-after` marks the run `interrupted`, emits only `pipeline_interrupted`, exits 1 and returns without invoking the successor. Turn all happy-path, minimum fixture-open, sanitized-error and interruption tests GREEN using real SQLite/filesystem I/O. After GREEN and before any schema-v2 edit, ensure the state/output targets are fresh and execute exactly `! UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout dry-run --fixture tests/fixtures/pipeline/approved.json --state tests/fixtures/state/v1-cli.db --output .tmp/v1-cli-out --fail-after generator`; inspect the raw command status and require it to be exactly 1 for intentional interruption rather than setup failure, then freeze the database and `v1-cli-provenance.json`. Provenance records the exact command, approved fixture and database SHA-256, `PRAGMA user_version=1`, run ID, interrupted status, durable Generator checkpoint, table row counts and proof that no Validators attempt exists. Do not hand-author, mutate after freeze or derive this fixture from a completed run.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - Focused pytest and Ruff commands exit 0 under the approved lock/toolchain.
    - CLI output contains `run_id`, `status=planned_not_published`, `last_stage=publication_planner`, `reused_stage_count=0`, `publication_plan_path` and `remote_writes_attempted=0`.
    - SQLite has `user_version=1`, one run, nine ordered stage results/checkpoints, and running/succeeded attempts whose identity fields were non-null before processor canaries were invoked.
    - `reusable_key_digest` equals SHA-256 of the exact canonical subject/stage/input/producer/retry-policy preimage and the successful output hash is retained.
    - Symlink, non-regular, declared oversize, streaming cap-plus-one and same-descriptor change cases all exit 1 before run creation; one open descriptor supplies the consumed bytes.
    - Every schema-v1 failure surface contains only one of the four closed codes and its fixed generic summary (ASCII, at most 160 characters); no hostile raw/Pydantic/exception/credential/path canary appears anywhere.
    - The real packaged `--fail-after generator` run exits 1 with Generator durable, status interrupted and no Validators attempt; frozen DB/provenance hashes and facts agree.
    - Output contains only `publication-plan.json`; no branch, commit, PR, merge, credential, network or candidate-code artifact/method exists.
  </acceptance_criteria>
  <done>The operator can safely run the complete local pipeline, and the resulting schema-v1 ledger is a real migration/retry contract rather than disposable scaffolding.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Gate-B-approved environment → packaged CLI | Only the reviewed lock/toolchain may build/import/run product code. |
| Operator fixture path → parsed fixture | File type, identity, size and bytes may be swapped or malicious. |
| Pipeline → schema-v1 SQLite/output | Precomputed retry identity and stage evidence become durable before/after processor execution. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | package/tool execution | high | mitigate | Verify both Plan 01 gates and exact lock hash before every locked build/run; managed Python/no-download environment remains active. |
| T-01-01 | Tampering / DoS | fixture reader | high | mitigate | lstat, one nonblocking no-follow descriptor, regular-mode/device/inode checks, 65,536-byte before/during cap, cap-plus-one probe and post-read identity/time/size comparison before parse. |
| T-01-02 | Elevation of Privilege | dry-run composition | critical | mitigate | Only fixture/local-state/publication-plan interfaces exist; no remote adapter/package or candidate execution method is created. |
| T-01-08 | Information Disclosure | schema-v1 errors and every output/persistence surface | high | mitigate | Four closed error codes, static generic ASCII summaries ≤160 characters, construction-only allowlist and hostile raw/Pydantic/exception/credential/path canaries across stdout/stderr/SQLite/manifests/publication plan/logs. |
| T-01-12 | Repudiation / Tampering | schema-v1 retry and interruption evidence | high | mitigate | user_version=1 and preprocessor persistence of canonical input/producer/retry-policy identity and digest before processor invocation; real `--fail-after generator` freeze proves the durable handoff. |

</threat_model>

## Artifacts this phase produces

- Refined/created files: `src/skillscout/__init__.py`, `src/skillscout/cli.py`, `src/skillscout/application/ports.py`, `src/skillscout/application/pipeline.py`, `src/skillscout/adapters/fixtures.py`, `src/skillscout/adapters/state.py`, `tests/conftest.py`, `tests/test_cli_dry_run.py`.
- CLI command/options: `skillscout dry-run`, `--fixture`, `--state`, `--output`, `--fail-after STAGE`; exit codes 0/1/2.
- Symbols: `main`, `build_parser`, `MAX_FIXTURE_BYTES`, `FixtureSubject`, `load_fixture`, `FixtureProcessor`, `STAGE_SEQUENCE`, `RETRY_POLICY_VERSION`, `canonical_v1_digest`, `PipelineRunner`, `RunSummary`, thin stage result, `PublicationPlan`, `SQLiteStateStore`, closed `ErrorCode`, `ERROR_SUMMARIES`, sanitized structured error.
- SQLite schema v1: `PRAGMA user_version=1`; `runs`, `stage_attempts`, `stage_results`, `checkpoints`; precomputed `input_hash`, `producer_version`, `retry_policy_version`, `reusable_key_digest` and successful output hash.
- Runtime/tracked artifacts: schema-v1 SQLite database, local `publication-plan.json`, structured CLI JSON, Gate-B-approved build distributions, frozen interrupted `tests/fixtures/state/v1-cli.db` and `v1-cli-provenance.json`.
- Minimum fixture-open security contract: symlink/non-regular rejection, single descriptor, 65,536-byte pre/during limit, cap-plus-one probe and pre/post identity/size/time checks.
- Minimum schema-v1 diagnostic contract: four closed codes, fixed generic ASCII summaries ≤160 characters, and no raw/Pydantic/exception/credential/path disclosure on any surface.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Frozen fixture reaches inspectable local publication result | 01-02 | COVERED | This is the first runnable vertical slice. |
| REQ | OPS-01 | Structured stage/attempt identity | 01-02, 01-03 | COVERED | v1 retains migration/retry identity; Wave 3 adds immutable envelopes. |
| REQ | OPS-04 | Dry-run with no remote write | 01-02 | COVERED | Complete local terminal path is GREEN. |
| RESEARCH | — | Exact RED attribution | 01-02 | COVERED | Build, collection, raw status and named assertion are distinguished. |
| RESEARCH | — | Minimum safe one-descriptor reader in first slice | 01-02 | COVERED | All high-severity primitives and tests land here. |
| RESEARCH | — | Minimum sanitized schema-v1 errors in first slice | 01-02 | COVERED | Closed codes, fixed summaries and hostile canaries land before any runnable failure. |
| RESEARCH | — | Real interrupted v1 migration fixture | 01-02 | COVERED | Packaged CLI freezes Generator checkpoint with no Validators attempt after GREEN. |
| RESEARCH | — | Durable schema-v1 migration seam | 01-02 | COVERED | Identity is persisted before processor and user_version is explicit. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No D-IDs exist. |

<verification>
- Exact RED evidence is retained and attributable only to the named functional assertion.
- Post-Gate-B verification command: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q`.
- Freeze command: `! UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout dry-run --fixture tests/fixtures/pipeline/approved.json --state tests/fixtures/state/v1-cli.db --output .tmp/v1-cli-out --fail-after generator`, with raw status inspected as exactly 1.
- Exact self-contained prefixed focused pytest/Ruff commands pass after GREEN, then the first full pytest sample passes at the end of Wave 2.
- Happy CLI uses real SQLite v1 write/read and emits local-only completion.
- Every minimum fixture-open rejection occurs before run creation.
- Every minimum diagnostic canary is absent from stdout/stderr and all durable surfaces.
- Exact prefixed packaged CLI freeze exits intentionally after Generator, produces schema-v1 interrupted state and has no Validators attempt.
- No remote capability or unapproved package/tool path appears.
</verification>

<success_criteria>
- A real user can execute the complete approved-fixture path after verified gates.
- High-severity fixture-open threats are mitigated in the first runnable slice.
- High-severity error disclosure is mitigated in the first runnable slice.
- The frozen interrupted v1 DB contains enough identity for transactional migration, exact retry accounting and Validators-first resume.
</success_criteria>

<output>After completion, create `.planning/phases/01-auditable-dry-run-spine/01-02-SUMMARY.md`, including raw RED attribution and one real schema-v1 DB/CLI evidence record.</output>
