---
phase: 01-auditable-dry-run-spine
plan: "16"
type: execute
wave: 15
depends_on: ["01-14", "01-15"]
files_modified:
  - tools/verify_phase1_gap_evidence.py
  - tests/test_phase1_evidence_verifier.py
  - tests/test_phase1_gap_closure.py
  - .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Evidence validity is bound to the exact current production/test/tool bytes and to normalized captured outputs from a fixed local command registry."
    - "The standalone verifier independently reruns the locked command registry, resolves every named test node from the bound source bytes, and rejects stale or self-asserted success."
    - "All three current blockers and all four current warnings map to named passing regressions; the Phase-6 OS/syscall network item remains a separate deferral."
    - "The complete acceptance runs from a temporary working directory using only the approved repository-local uv, managed Python, locked graph, offline mode, and temporary local data."
    - "The approved lock and frozen schema-version-1 database hashes remain unchanged before and after smoke, full test, lint, lock, build, and evidence rerun gates."
  artifacts:
    - { path: tools/verify_phase1_gap_evidence.py, provides: "record and independently rerunnable authority-bound evidence verification" }
    - { path: tests/test_phase1_evidence_verifier.py, provides: "stale source/output/node/self-assertion rejection tests" }
    - { path: tests/test_phase1_gap_closure.py, provides: "current seven-finding AST mapping and composed smoke coverage" }
    - { path: .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, provides: "schema-version-2 current-round evidence with source/output digests" }
  key_links:
    - { from: evidence.source_digests, to: src/skillscout and tests, via: "exact sorted allowlisted path/digest set recomputed on every verification", pattern: "source_digests" }
    - { from: evidence.command_results, to: fixed command registry, via: "normalized stdout/stderr digests, exit, and parsed count compared to an independent rerun", pattern: "output_sha256" }
    - { from: evidence.current_findings, to: bound test sources, via: "AST resolution of each module/function node before command execution is credited", pattern: "current_findings" }
    - { from: temporary working directory, to: repository-local toolchain, via: "absolute --project and managed-Python/no-download/offline invocation", pattern: "--project" }
---

<objective>
Close WR-02 and prove the complete second gap-closure round under current, independently rerun authority.

Purpose: Replace a prose-shape/self-assertion checker with a reproducible gate that binds exact code/test bytes, named test definitions, captured command outputs, immutable inputs, and fresh local execution.
Output: Authority-bound evidence tooling/tests, current finding map, refreshed evidence index, and the final locked temporary-directory acceptance.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-12-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-14-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-15-SUMMARY.md
@tools/verify_phase1_gap_evidence.py
@tests/test_phase1_gap_closure.py
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-16-01: Bind evidence to exact source bytes, named nodes, and captured command outputs</name>
  <files>tools/verify_phase1_gap_evidence.py, tests/test_phase1_evidence_verifier.py</files>
  <read_first>tools/verify_phase1_gap_evidence.py, tests/test_phase1_gap_closure.py, .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md, pyproject.toml</read_first>
  <behavior>
    - Changing any bound production, test, verifier, review, verification, or project-metadata byte invalidates evidence before a claimed result is accepted.
    - Changing a command exit/count or normalized stdout/stderr digest invalidates evidence.
    - A named node must exist as a top-level test function in the exact AST bytes whose digest is bound by the document.
    - Verification reruns a closed command registry and compares fresh normalized output digests; document literals alone can never establish success.
    - The evidence document and verifier outcome remain outside the source/output claims, preventing self-hash and self-success cycles.
  </behavior>
  <action>Create `tests/test_phase1_evidence_verifier.py` with RED temporary-repository cases for stale production bytes, stale test bytes, missing/renamed node functions, duplicate paths/keys, forged zero exits, changed counts, changed output digests, unexpected command IDs, forbidden verifier/self-hash claims, and a rerun mismatch. Refactor the stdlib-only tool into two explicit modes: `record DOCUMENT` executes the fixed registry and writes schema version 2 evidence; `verify --rerun DOCUMENT` is read-only, recomputes authority, independently reruns the registry, and compares. Resolve the repository root from the verifier file, never caller cwd. Bind one exact sorted path/digest set covering `pyproject.toml`, all `src/skillscout/**/*.py`, all `tests/**/*.py`, this verifier, `01-REVIEW.md`, and `01-VERIFICATION.md`; reject missing, extra, duplicate, symlink, non-regular, oversized, or digest-mismatched entries. Keep `uv.lock` and the frozen database as separately fixed pre/post authorities. Define a closed command registry for packaged smoke, current-finding focused tests, full pytest, Ruff, lock check, and build. Capture stdout/stderr, normalize only documented elapsed-time and temporary-root substrings, hash the normalized bytes, parse the actual exit/count, and store argv tokens plus digests. On verify, reconstruct the same commands, rerun them with `UV_PYTHON_DOWNLOADS=never`, managed Python, offline mode, `--locked` where supported, and compare every result. Resolve every finding node with `ast` against the already digest-verified source before crediting it. Keep all failures on one fixed ASCII verifier diagnostic.</action>
  <acceptance_criteria>
    - Every stale-source, missing-node, forged-result, output-digest, and rerun-mismatch test fails closed.
    - The bound file set is exact and deterministic; adding an in-scope source/test file invalidates old evidence until record mode is rerun.
    - Command output normalization is narrowly specified and unit-tested; it cannot remove failure text, exit status, counts, node names, or arbitrary lines.
    - `verify --rerun` executes rather than trusts every credited command and performs no document rewrite.
    - The document contains neither its own hash nor record/verify command success as evidence.
    - The verifier imports no project package and uses only the Python standard library plus the already approved local executables it launches.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase1_evidence_verifier.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check tools/verify_phase1_gap_evidence.py tests/test_phase1_evidence_verifier.py</automated></verify>
  <done>Evidence validity now depends on exact authority bytes, real node definitions, and independently rerun captured command results.</done>
</task>

<task type="auto">
  <name>Task 01-16-02: Run temporary-root smoke/full gates and record the current seven-finding evidence</name>
  <files>tests/test_phase1_gap_closure.py, .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md</files>
  <read_first>.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md, .planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-12-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-14-SUMMARY.md, .planning/phases/01-auditable-dry-run-spine/01-15-SUMMARY.md, tests/test_phase1_gap_closure.py, tools/verify_phase1_gap_evidence.py</read_first>
  <action>Update `tests/test_phase1_gap_closure.py` with a distinct `CURRENT_REVIEW_FINDING_NODES` mapping for exactly CR-01, CR-02, CR-03, WR-01, WR-02, WR-03, and WR-04 from the 2026-07-19 review; point each to the new focused regression definitions and keep the older closure map separately labeled. Add composed local smoke assertions covering post-commit snapshot truth, event-derived reuse tamper rejection, fixed invalid-argv diagnostics, fail-once resume, namespace collision, and private-file rejection. Represent the older OS/syscall network-denial item only as `deferred.os_syscall_network_denial.addressed_in = Phase 6`; never label the current ownership/mode WR-04 deferred.

Before recording, check `uv.lock` SHA-256 `caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32` and frozen database SHA-256 `49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251`. From a newly created temporary working directory, invoke the repository-local managed Python through the absolute uv path and `run --project REPO --locked`, with downloads disabled and offline mode, to run `tools/verify_phase1_gap_evidence.py record` against the authoritative document. The fixed registry must use temporary state/output/build destinations and must perform no network, external service, credential, remote write, or candidate-code action. Record actual source digests, node resolutions, normalized output digests, exits/counts, CLI facts, current finding matrix, and immutable pre/post hashes in `01-GAP-VALIDATION.md`. Then, from a different new temporary working directory, run `verify --rerun` once; it must independently repeat and compare every command. Recheck both authoritative hashes after verification and do not rewrite the document afterward. Commit the current finding-map test changes separately from the final generated evidence so RED/GREEN behavior and evidence recording remain atomic.</action>
  <acceptance_criteria>
    - Current finding coverage is exact: three blockers and four warnings, each mapped to an existing bound test definition and a passing fixed command.
    - Packaged smoke proves all seven repaired behaviors in temporary local roots with zero remote writes and no canary disclosure.
    - Full pytest, Ruff, lock check, and build pass under the closed offline registry and captured results are digest-bound.
    - `verify --rerun` succeeds from a caller cwd outside the repository only after independently rerunning all credited commands.
    - Both authoritative SHA-256 values match before record, after record, and after rerun verification.
    - The current ownership/mode WR-04 is PASS; only the separately named OS/syscall network-denial item is deferred to Phase 6.
    - `.planning/config.json` remains user-owned, unmodified, unstaged, and absent from the evidence authority set.
  </acceptance_criteria>
  <verify><automated>repo_root="$(pwd -P)" &amp;&amp; test "$(shasum -a 256 "$repo_root/uv.lock" | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 "$repo_root/tests/fixtures/state/v1-cli.db" | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; acceptance_root="$(mktemp -d)" &amp;&amp; cd "$acceptance_root" &amp;&amp; UV_PYTHON_INSTALL_DIR="$repo_root/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$repo_root/.tools/uv-0.11.29/bin/uv" run --project "$repo_root" --locked python "$repo_root/tools/verify_phase1_gap_evidence.py" verify --rerun "$repo_root/.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md" &amp;&amp; test "$(shasum -a 256 "$repo_root/uv.lock" | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; test "$(shasum -a 256 "$repo_root/tests/fixtures/state/v1-cli.db" | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251"</automated></verify>
  <done>The current code/test tree has fresh independently rerun evidence for every blocker and warning under unchanged approved inputs.</done>
</task>

</tasks>

<prohibitions>
- Do not accept a document's claimed exit, count, node, digest, or source path without recomputation or rerun.
- Do not include the document self-hash, record command, verifier command, or verifier outcome inside the evidence it validates.
- Do not normalize away arbitrary output, failure text, node names, exit status, or counts.
- Do not access the network, install or relock packages, call external services, write remotely, or execute candidate repository content.
- Do not implement or mark complete the separately deferred Phase-6 OS/syscall network-denial item.
- Do not read, stage, commit, hash into evidence, or modify `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source/test/tool bytes → evidence authority | A stale document must not survive any in-scope byte or file-set change. |
| Command process output → success claim | Exit, parsed result, and normalized captured bytes require fresh independent execution. |
| Finding node string → regression evidence | A plausible-looking node is not authority until resolved in the bound AST and executed. |
| Temporary acceptance cwd → repository-local runtime | Caller location must not cause ambient tool, Python, network, or path authority. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16-01 | Repudiation | evidence command claims | high | mitigate | Capture normalized output digests, exits/counts, then independently rerun the fixed registry. |
| T-01-16-02 | Tampering | source/test authority | high | mitigate | Bind and recompute an exact sorted file/digest set plus immutable lock/frozen hashes. |
| T-01-16-03 | Spoofing | named pytest nodes | high | mitigate | Resolve every node through AST in its digest-verified source before credit and execution. |
| T-01-16-04 | Information Disclosure | captured output/evidence | high | mitigate | Store only bounded normalized digests and allowlisted structured facts; canary tests cover streams/durable bytes. |
| T-01-16-05 | Elevation of Privilege | acceptance command | critical | mitigate | Absolute approved uv, managed Python, offline locked project, temporary local data, and sealed zero-remote runtime. |
| T-01-16-SC | Tampering | dependency graph | high | mitigate | Exact pre/post lock hash, `--locked`, offline mode, and no install/relock operation. |
</threat_model>

## Artifacts this phase produces

- New path: `tests/test_phase1_evidence_verifier.py`.
- Tool modes: `record DOCUMENT` and `verify --rerun DOCUMENT` in `tools/verify_phase1_gap_evidence.py`.
- Evidence schema fields: `schema_version: "2"`, `source_digests`, `command_results`, `current_findings`, `cli_facts`, `immutable_inputs`, and `deferred.os_syscall_network_denial`.
- Command-result fields: canonical argv tokens, exit code, parsed result kind/count, normalized stdout SHA-256, and normalized stderr SHA-256.
- Test mapping: `CURRENT_REVIEW_FINDING_NODES` for CR-01..CR-03 and WR-01..WR-04.
- Refreshed path: `.planning/phases/01-auditable-dry-run-spine/01-GAP-VALIDATION.md` with exact current authority and no self-claim.
- Acceptance artifact: temporary-root packaged smoke, focused findings, full pytest, Ruff, lock check, build, and independent rerun results.

## Current Finding Coverage

| Finding | Required behavior | Owning plan(s) | Final evidence |
|---|---|---|---|
| CR-01 | Post-commit snapshot outcome equals reopened state | 01-12 | Fault-injection node + temporary smoke |
| CR-02 | Reuse count is checkpoint/event bound and tamper rejected | 01-13, 01-14 | Event-chain tamper nodes + inspect smoke |
| CR-03 | Invalid argv uses fixed non-echoing diagnostics | 01-15 | Subprocess canary nodes + packaged smoke |
| WR-01 | Unexpected processor interruption retries finitely without replay | 01-15 | Fail-once/exhaustion nodes + resume smoke |
| WR-02 | Evidence is source/output/node authority-bound and rerunnable | 01-16 | Verifier adversarial tests + `verify --rerun` |
| WR-03 | State/manifest namespace collision rejected early | 01-12 | State/CLI collision nodes + temporary smoke |
| WR-04 | Existing state/manifests enforce owner/mode/link policy | 01-12 | Permission matrix + temporary smoke |

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Complete auditable/resumable local-only dry-run | 01-12..01-16 | COVERED | All current blockers/warnings are fixed and freshly rerun together. |
| REQ | OPS-01 | Versioned trustworthy audit and attempts | 01-12, 01-13, 01-14, 01-15, 01-16 | COVERED | Files, events, diagnostics, projections, and evidence authority are bound. |
| REQ | OPS-04 | Finite retry, checkpoint recovery, no remote writes | 01-12, 01-13, 01-14, 01-15, 01-16 | COVERED | Truthful persistence and no-replay retry pass composed smoke/full gates. |
| RESEARCH | — | Locked local toolchain, strict contracts, content-addressed state, no remote adapters | 01-12..01-16 | COVERED | No new dependency or remote capability; final rerun uses exact approved runtime. |
| CONTEXT | — | No CONTEXT.md; recommended defaults approved | — | N/A | The separate Phase-6 OS/syscall denial remains excluded. |

<verification>
- Runnable smoke: the fixed `packaged_smoke` registry command exercises snapshot truth, verified reuse, sanitized argv, retry, collision, permissions, and zero remote writes in temporary roots.
- Full locked verification: current-finding focused nodes, complete pytest, Ruff, `uv lock --check`, and `uv build --no-sources` run through the absolute approved uv/managed-Python/offline boundary.
- Independent evidence: `verify --rerun` starts outside the repository, recomputes the exact file set and immutable hashes, resolves AST nodes, reruns all commands, and compares captured-output digests.
- Both immutable SHA-256 values remain unchanged and `.planning/config.json` stays outside all operations.
</verification>

<success_criteria>
- All seven current review findings have named, bound, freshly passing evidence.
- A stale or self-asserted document cannot pass independent verification.
- The final acceptance is local-only, temporary-root, locked, offline, and hash-preserving.
- OPS-01 and OPS-04 are ready for a new independent phase verification.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-16-SUMMARY.md` with source authority set, command output digests, seven-finding matrix, temporary-root command, and immutable pre/post hashes.</output>
