---
phase: 01-auditable-dry-run-spine
plan: "10"
type: execute
wave: 10
depends_on: ["01-09"]
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/domain/canonical.py
  - src/skillscout/application/ports.py
  - src/skillscout/adapters/state.py
  - tests/test_state_integrity.py
  - tests/test_pipeline_resume.py
  - tests/test_cli_dry_run.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "One central verifier binds run, attempt, result row, checkpoint and manifest for every persisted stage position."
    - "The verifier recomputes StageInput, input_hash, reusable_key_digest, output_hash, schema-specific semantic result_id, run-scoped result_row_id and manifest_hash, comparing every duplicate field."
    - "Stage order/cardinality is the exact closed PipelineStage prefix for interrupted runs and all nine stages for planned_not_published runs; result/checkpoint associations are one-to-one."
    - "A full chain is produced only for identity_state=bound; migration validates all available legacy facts but cannot authorize resume/full inspect until expected RunIdentity binds and passes the central verifier."
    - "Post-binding resume, completed-result verification and inspect all call the same verifier and reject the same corruption without mutation or disclosure."
  artifacts:
    - { path: src/skillscout/adapters/state.py, provides: "central full-chain ledger verifier", exports: [verify_run_chain] }
    - { path: src/skillscout/domain/models.py, provides: "structured verified-chain result", exports: [VerifiedRunChain] }
    - { path: src/skillscout/application/ports.py, provides: "provider-independent VerifiedRunChain and typed recovery contracts" }
    - { path: tests/test_state_integrity.py, provides: "CR-05 duplicate-field and order/cardinality tamper matrix" }
    - { path: tests/test_pipeline_resume.py, provides: "shared migration/resume verifier evidence" }
  key_links:
    - { from: verify_run_chain, to: src/skillscout/domain/canonical.py, via: "all canonical identities are recomputed from persisted and expected inputs", pattern: "make_result_id" }
    - { from: resume_identity_matches, to: verify_run_chain, via: "reuse cannot bypass full-chain proof", pattern: "verify_run_chain" }
    - { from: inspect_run, to: verify_run_chain, via: "public projection consumes only verified models", pattern: "VerifiedRunChain" }
    - { from: bind_legacy_run, to: verify_run_chain, via: "expected RunIdentity is transactionally applied and fully verified before legacy binding commits", pattern: "legacy_unbound" }
    - { from: _validate_migration_copy, to: legacy_unbound, via: "pre-promotion validator proves only facts available without fixture_hash and never emits VerifiedRunChain", pattern: "identity_state" }
---

<objective>
Close the canonical identity half of verification root gap 3 and CR-05 by replacing partial row checks with one full-chain verifier.

Purpose: Make the ledger cryptographically and structurally self-consistent across every duplicated field and every path that trusts it.
Output: A single structured verification primitive wired into migration, resume, inspect and completed-result checks, plus field-by-field corruption regressions.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-09-SUMMARY.md
@src/skillscout/domain/models.py
@src/skillscout/domain/canonical.py
@src/skillscout/application/ports.py
@src/skillscout/adapters/state.py
@tests/test_state_integrity.py
@tests/test_pipeline_resume.py
@tests/test_cli_dry_run.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-10-01: Build one full run-attempt-result-checkpoint-manifest verifier</name>
  <files>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/state.py, src/skillscout/domain/enums.py, tests/test_state_integrity.py, .planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md</read_first>
  <behavior>
    - For each stage index, stored stage equals tuple(PipelineStage)[index] in run, attempt, result, checkpoint and envelope.
    - StageInput is rebuilt from RunIdentity, prior verified output hash and fixture hash; every input/digest/result/manifest identity is recomputed with schema-1 versus schema-2 rules.
    - Attempt number, run/subject/stage/index/producer/retry and result identifiers match across all duplicate locations.
    - Result and checkpoint sets have exact one-to-one cardinality; planned_not_published has nine, unfinished runs have a valid contiguous prefix, and orphan/extra/reordered rows fail.
  </behavior>
  <action>Write a parameterized RED tamper matrix for every omitted field named by CR-05: envelope/result/attempt input hash, reusable digest, attempt number, producer/retry version, semantic result ID, run-scoped row ID, stage label/index/order, checkpoint IDs/hashes/path/cardinality and prior-output chain. Add strict `VerifiedRunChain` output and implement `SQLiteStateStore.verify_run_chain(run_id, expected_identity=None)` for normal bound tables. Require `identity_state='bound'`, a complete persisted RunIdentity and equality with any supplied expected identity before reading the stage chain; a legacy_unbound row returns only `STATE_IDENTITY_UNBOUND`, never a partial chain. Use explicit joined queries over strict run, attempt, result and checkpoint records; reject duplicate or missing rows before hashing. Rebuild each `StageInput`, recompute all canonical identities with the preserved schema-1 preimage and schema-2 retry-policy preimage, then verify exact manifest bytes/envelope fields and progression. Return verified structured objects only after the whole chain succeeds. Map parse, range, Pydantic and identity failures to fixed STATE_INTEGRITY_ERROR.</action>
  <acceptance_criteria>
    - Every individual field tamper is detected even when an attacker coherently rehashes the manifest and output fields they control.
    - Stage relabel/reorder and result/checkpoint missing/extra/duplicate cases fail before any checkpoint reuse or inspect projection.
    - Schema-1 migrated semantic result IDs use the historical preimage; schema-2 uses the current retry-policy-aware preimage.
    - The verifier returns a complete typed chain for valid happy, interrupted and migrated runs without exposing raw SQL rows.
    - Migrated means exactly bound-after-current-identity proof; a merely promoted legacy_unbound database cannot produce VerifiedRunChain.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py -k "canonical or chain or tamper or relabel or order or cardinality or result_id or reusable or attempt"</automated></verify>
  <done>One verifier establishes the canonical and relational identity of the entire persisted stage chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-10-02: Route every trust decision through the full-chain verifier</name>
  <files>src/skillscout/application/ports.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/application/pipeline.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py, tests/test_state_integrity.py, tests/fixtures/state/v1-cli-provenance.json</read_first>
  <behavior>
    - _validate_migration_copy verifies only the complete set of facts available without fixture_hash and requires the promoted row to remain legacy_unbound.
    - bind_legacy_run applies an expected current RunIdentity in a candidate transaction, calls verify_run_chain, and commits binding only after complete proof.
    - resume identity and latest checkpoint are derived from the verified chain rather than independent partial queries.
    - verify_completed_results delegates to the central verifier and cannot accept a forged semantic result ID.
    - inspect validates schema/diagnostics and the full chain before returning any run, attempt, result or checkpoint field.
  </behavior>
  <action>Replace `_verify_manifest_row`-only trust decisions with `verify_run_chain` for bound state. Keep a narrowly named `_validate_legacy_migration_copy` for suffixed pre-promotion tables: it verifies exact schema, row counts, foreign keys, closed diagnostics, stage prefix/order, attempt/result/checkpoint associations, producer/retry/reusable digests, output hashes, schema-1 semantic result IDs and manifest hashes that do not require fixture_hash; it must assert `legacy_unbound` and must not return `VerifiedRunChain` or authorize reuse/inspect. Refactor `bind_legacy_run(expected_identity)` so it updates a private serialized candidate transaction with the expected fixture identity, calls the central `verify_run_chain` against that candidate, and persists/promotes the binding only after proof; wrong identity rolls back byte-for-byte.

Make resume use the verified bound prefix and exact expected RunIdentity; make `latest_checkpoint` return the verifier's typed checkpoint when used for recovery; make `verify_completed_results` a compatibility wrapper that additionally checks the expected count; and make inspect project only the already validated persisted models/envelopes from `VerifiedRunChain`. Update `StateStore` so `verify_run_chain`, typed exact resumable lookup, legacy binding, typed checkpoint and lifecycle reconciliation signatures match the concrete adapter and expose no `sqlite3.Row`, raw mapping or table suffix. Remove duplicated weaker bound-state logic once all call sites share the verifier. Add inspect-before-binding, wrong binding, exact binding, Validators-first resume, post-binding inspect and consistent corruption rejection tests.</action>
  <acceptance_criteria>
    - A coherently rehashed forged result_id fails verify_completed_results, resume and inspect with the same fixed integrity code.
    - Every trust entry point calls verify_run_chain directly or through one documented wrapper; no alternate weaker result loop remains.
    - Pre-promotion migration never claims a full chain; inspect/resume fail while legacy_unbound, wrong binding is non-mutating, and exact binding enables full post-binding inspect plus Validators-first resume.
    - StateStore protocol and concrete adapter have matching domain-model signatures with no SQLite-specific return value.
    - Corruption rejection performs no run restart, checkpoint advance, migration promotion or public partial output.
    - Frozen v1 migration still resumes first at Validators and the standard interruption/inspect flow remains green.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_pipeline_resume.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_state_integrity.py tests/test_pipeline_resume.py</automated></verify>
  <done>Migration, recovery, completed verification and inspect all trust exactly one complete canonical-chain proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Duplicated ledger fields → canonical fact | Coherently altered rows/manifests can evade partial equality checks. |
| Verified checkpoint → resume skip | Skipping processors is authorized only by a complete chain proof. |
| Persisted chain → public inspect | Partial output before full verification could disclose or bless corruption. |
| Legacy-unbound migration → full-chain claim | Frozen v1 lacks fixture_hash, so only caller-supplied current identity can complete StageInput proof. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G3-04 | Tampering | canonical ledger chain | critical | mitigate | Recompute and cross-bind every run/attempt/result/checkpoint/manifest identity in one verifier. |
| T-01-G3-05 | Elevation of Privilege | checkpoint reuse | high | mitigate | Resume derives skip authority only from verified exact identity and contiguous closed stage prefix. |
| T-01-G3-06 | Information Disclosure | inspect partial projection | high | mitigate | Build public output only after schema, diagnostics and full-chain verification complete. |
| T-01-G3-07 | Spoofing / Repudiation | legacy binding | high | mitigate | Pre-bind inspect/resume forbidden; expected identity is applied in a candidate transaction and full verifier must pass before durable binding. |
| T-01-SC | Tampering | frozen migration evidence | high | mitigate | Exact frozen DB and lock hashes are checked; schema-1 identities remain unchanged. |
</threat_model>

## Artifacts this phase produces

- Symbols: `SQLiteStateStore.verify_run_chain`, `VerifiedRunChain`, `_validate_legacy_migration_copy`, transactionally verified `bind_legacy_run`.
- Port APIs: domain-typed full-chain, resumable lookup, checkpoint, binding and lifecycle reconciliation contracts; no SQLite row/table-suffix leakage.
- Recomputed identities: StageInput/input hash, reusable-key digest, output hash, semantic result ID, result-row ID and manifest hash.
- Tests: one corruption case per duplicated field plus stage relabel/order/cardinality; inspect-before-binding, wrong/exact binding, Validators-first resume, post-binding inspect; consistent bound resume/completed/inspect rejection.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every checkpoint and inspect result is auditable and recoverable | 01-10 | COVERED | Full canonical chain closes root gap 3. |
| REQ | OPS-01 | Stable IDs/input-output hashes and versioned structured evidence | 01-10 | COVERED | Every persisted duplicate is recomputed and bound. |
| REQ | OPS-04 | Resume without replay from latest verified checkpoint | 01-10 | COVERED | Only a full exact-identity proof grants reuse. |
| RESEARCH | — | Content-addressed manifests and canonical non-circular hashes | 01-10 | COVERED | Verifier reuses the established schema-specific preimages. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No deferred functionality appears. |

<verification>
- CR-05 forged-result and every omitted-field reproduction are explicit tests.
- Bound resume, completed-result and inspect paths reject the same tamper matrix; migration is explicitly limited to facts available before fixture binding.
- Focused integrity/recovery/CLI suites and Ruff pass with the frozen hash unchanged.
</verification>

<success_criteria>
- Verification root gap 3 is closed across schema, diagnostics and complete canonical identity.
- No weaker alternate trust path remains.
- Valid happy/interrupted/migrated chains remain fully inspectable and resumable.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-10-SUMMARY.md` with the field tamper matrix, entry-point consistency and frozen-hash evidence.</output>
