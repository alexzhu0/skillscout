---
phase: 01-auditable-dry-run-spine
plan: "07"
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - src/skillscout/adapters/localfs.py
  - src/skillscout/adapters/state.py
  - src/skillscout/application/pipeline.py
  - tests/test_state_integrity.py
  - tests/test_pipeline_resume.py
  - tests/test_cli_security.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Manifest and publication-plan operations remain anchored to verified directory descriptors, so swapping a checked parent pathname cannot redirect writes."
    - "SQLite never opens the operator pathname: state bytes are read through an anchored descriptor, deserialized into a private in-memory connection, and serialized back through anchored durable atomic replacement under an exclusive process lock."
    - "Every required file and directory fsync failure aborts before checkpoint or terminal run status commits."
    - "New directory entries are synced from the created leaf back to an already durable trusted root."
  artifacts:
    - { path: src/skillscout/adapters/localfs.py, provides: "descriptor-anchored and durability-checked local file primitives", exports: [AnchoredDirectory, DurableWriteError] }
    - { path: src/skillscout/adapters/state.py, provides: "exclusive descriptor-anchored serialized SQLite state with no pathname VFS/sidecars", exports: [SQLiteStateStore, MAX_STATE_DB_BYTES] }
    - { path: src/skillscout/application/pipeline.py, provides: "durable anchored publication-plan writer" }
    - { path: tests/test_state_integrity.py, provides: "CR-07 parent-swap regression evidence" }
    - { path: tests/test_pipeline_resume.py, provides: "CR-08 file/directory fsync failure evidence" }
  key_links:
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/adapters/localfs.py, via: "manifest directories and files use dir_fd-relative create/open/rename/unlink/fsync", pattern: "AnchoredDirectory" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/localfs.py, via: "publication plan is durable before terminal status", pattern: "publication-plan.json" }
    - { from: SQLiteStateStore, to: sqlite3.Connection, via: "anchored bounded bytes deserialize into :memory: and committed candidate snapshots serialize before durable replace", pattern: "deserialize" }
---

<objective>
Close the filesystem durability and path-race half of verification root gap 2: CR-07 and CR-08.

Purpose: Prevent local evidence from being redirected after a pathname check and prevent SQLite from claiming a checkpoint or terminal plan whose referenced bytes are not durable.
Output: Shared descriptor-anchored file primitives, secure state-directory checks, fatal fsync handling and adversarial race/failure tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-06-SUMMARY.md
@src/skillscout/adapters/state.py
@src/skillscout/application/pipeline.py
@tests/test_state_integrity.py
@tests/test_pipeline_resume.py
@tests/test_cli_security.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-07-01: Anchor state and manifest operations to verified directories</name>
  <files>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/application/pipeline.py, tests/test_state_integrity.py, tests/test_pipeline_resume.py, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md</read_first>
  <behavior>
    - Trusted directories are opened with O_DIRECTORY/O_NOFOLLOW/O_CLOEXEC, fstat-verified and retained for the operation lifetime.
    - Child checks, opens, mkdir, replace and unlink resolve relative to dir_fd rather than re-resolving a previously checked ancestor pathname.
    - Swapping the visible parent at each instrumented seam either leaves writes in the originally anchored directory or fails closed; attacker target bytes never change.
    - SQLite state bytes are bounded, read only through the retained dir_fd, deserialized into `sqlite3.connect(":memory:")`, and never opened through a pathname or `/dev/fd` URI.
    - One retained ordinary lock file plus `fcntl.flock(LOCK_EX|LOCK_NB)` supplies live ownership; process death releases the flock while the inert file remains reusable.
  </behavior>
  <action>Write parent-swap and second-writer regressions at deterministic seams before implementation. Create `adapters/localfs.py` with a small `AnchoredDirectory` abstraction: take an lstat identity snapshot, open the directory with `O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC`, require fstat identity equality plus current-user ownership and no group/other write bits, retain the descriptor, validate closed single-component child names, and expose bounded read, mkdir, replace and unlink operations using `dir_fd`; fail closed when a required macOS primitive is unavailable. Replace manifest pathname check-then-open flows with anchored calls.

Refactor `SQLiteStateStore` to a descriptor-anchored serialized design compatible with stdlib Python on macOS. It must never call `sqlite3.connect(path)`, never use `/dev/fd`, and never enable a disk journal/WAL. Open `.<state-basename>.lock` relative to the retained anchor with exactly `O_RDWR|O_CREAT|O_NOFOLLOW|O_CLOEXEC` and mode `0600`; never use `O_EXCL` as lock ownership and never unlink the lock file on close or error recovery. Compare anchored `stat` and `fstat`, require one ordinary file owned by the current uid with no group/other permission bits, then acquire `fcntl.flock(fd, LOCK_EX|LOCK_NB)` and hold that descriptor until `close`. Live ownership is the kernel flock only: a killed/crashed process releases it automatically, and the retained inert lock inode is reopened and reused by the next process.

Read the state child through the anchor with `MAX_STATE_DB_BYTES = 67_108_864`; deserialize existing bytes into a private `sqlite3.connect(":memory:")`, or create the current schema in memory when absent. Validate schema/integrity after deserialization. For each mutation, build/commit a candidate private in-memory snapshot from the last durable serialized bytes, serialize and bound it, durably replace the state child through the anchor, then promote that candidate as the active connection; on persistence failure discard the candidate, poison the store and require reopen. This is intentionally a single-process, bounded-state Phase-1 adapter: concurrent opens fail, no live cross-process readers are promised, and a process crash exposes either the prior durable snapshot or the fully replaced snapshot, never SQLite WAL/journal sidecars. Preserve the CLI `--state` filename and frozen v1 bytes.</action>
  <acceptance_criteria>
    - Existing static symlink cases remain green and instrumented parent swaps cannot redirect a state or manifest write.
    - Every child name is closed and relative; no `..`, separator, absolute path or stored root override reaches dir_fd operations.
    - SQLite never opens the state pathname; existing bytes enter only through bounded anchored read and `Connection.deserialize`, while new state begins only in memory.
    - A second store for the same anchored state fails while the first exclusive lock is held and succeeds after close.
    - A subprocess holding the flock blocks a second process; after SIGKILL/forced termination, a fresh process acquires the same retained lock file, validates the prior snapshot and resumes without deleting or recreating the lock inode.
    - State size above 64 MiB, deserialize failure or unavailable serialize/deserialize/flock support fails before mutation with a closed code.
    - Unsupported secure-open primitives fail with a closed state code rather than a weaker fallback.
    - Descriptor cleanup is deterministic on success and every failure path.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py -k "symlink or parent or swap or manifest or lock or killed or termination" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/adapters tests/test_state_integrity.py</automated></verify>
  <done>SQLite and manifest operations are bound to verified local directories and parent-path swaps cannot redirect them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-07-02: Make manifest and publication durability a prerequisite for DB success</name>
  <files>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, src/skillscout/application/pipeline.py, tests/test_pipeline_resume.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/adapters/localfs.py, src/skillscout/adapters/state.py, src/skillscout/application/pipeline.py, tests/test_pipeline_resume.py, tests/test_cli_security.py, tests/test_cli_dry_run.py</read_first>
  <behavior>
    - Atomic write requires full payload write, file fsync, anchored rename, containing-directory fsync and sync of every newly created ancestor entry.
    - File-fsync, leaf-directory-fsync and ancestor-directory-fsync failures are fatal and no result/checkpoint or planned_not_published transition follows.
    - Publication plan is durably renamed and its directory synced before the terminal run status commits.
    - Every SQLite mutation persists one bounded serialized candidate snapshot through the same anchored writer before that candidate becomes the active state connection.
    - Temporary entries are removed relative to the anchored directory without following a swapped path.
  </behavior>
  <action>Add injectable seams for file fsync, rename, leaf/ancestor directory fsync, snapshot serialization and parent swaps before load, during new-state creation, before snapshot persistence and before later transactions. Implement one durable atomic-write primitive in `localfs.py` and reuse it for manifests, serialized state snapshots and `publication-plan.json`. Sync each newly created child directory before descending, sync the temporary file, rename within the same anchored directory, then sync the containing directory; propagate every required failure. For replacement failure after rename, retain a bounded anchored backup of the prior snapshot, best-effort restore visible old bytes, poison/close the in-memory store, and require reopen plus schema/integrity validation; never continue from an indeterminate connection. New-state creation similarly removes an unconfirmed target on failure. Document that real fsync failure is an indeterminate durability event handled by fail-stop/reopen, not a retry on the same connection.

Make every SQLite write helper use the copy-on-write snapshot transaction described in Task 01-07-01: apply/commit only on a candidate in-memory connection; serialize and durably publish it; promote it only after successful file and directory sync. Manifest durability still precedes the candidate result/checkpoint snapshot. Change terminal ordering so publication bytes are durable before the terminal run snapshot is persisted. Swapping the visible parent after anchoring must never change the attacker target because lock, state, temp, backup, manifest and publication operations all use retained dir_fds.</action>
  <acceptance_criteria>
    - Each injected pre-rename sync/serialize failure leaves the prior canonical snapshot byte-identical and produces no checkpoint or terminal transition.
    - Each injected post-rename directory-sync failure fail-stops, restores the visible prior snapshot when possible, and on reopen accepts only an exact valid old-or-new snapshot; it never continues an in-memory transaction or exposes partial bytes.
    - A manifest is file- and directory-durable before `_commit_success` starts its SQLite transaction.
    - A publication plan is file- and directory-durable before the run becomes planned_not_published.
    - Parent-swap plus failure-cleanup tests leave the attacker directory byte-identical.
    - Normal interruption/resume and happy publication planning remain green.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py tests/test_state_integrity.py tests/test_cli_security.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_pipeline_resume.py tests/test_state_integrity.py tests/test_cli_security.py</automated></verify>
  <done>No checkpoint or terminal publication state can outrun the required durable local bytes, and every durability failure is observable and resumable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checked path → later filesystem operation | An ancestor may be renamed or replaced between separate pathname resolutions. |
| Renamed file → durable evidence claim | A successful rename is not sufficient if file or directory metadata was not synced. |
| Anchored state bytes → in-memory SQLite | Untrusted bounded bytes are deserialized only after exclusive locking; SQLite VFS never receives an operator pathname. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G2-04 | Tampering | state/manifest/publication parents | high | mitigate | Retained O_NOFOLLOW directory descriptors, relative child operations, persistent validated lock inode plus kernel-only flock ownership, and swaps before load/create/persist/later transactions. |
| T-01-G2-05 | Repudiation / Tampering | checkpoint durability ordering | high | mitigate | File plus directory fsync is mandatory and fatal before SQLite checkpoint or terminal status. |
| T-01-G2-06 | Denial of Service | failure cleanup | medium | mitigate | Bounded temporary names, dir_fd-relative cleanup and explicit resumable lifecycle. |
| T-01-G2-07 | Tampering / DoS | serialized SQLite state | high | mitigate | 64 MiB cap, private in-memory deserialize/serialize, copy-on-write snapshots, no WAL/sidecars, fail-stop on indeterminate persistence and exclusive single-process lock. |
| T-01-SC | Tampering | dependency graph | high | mitigate | No dependency changes and exact Gate-B hash check. |
</threat_model>

## Artifacts this phase produces

- File/symbols: `src/skillscout/adapters/localfs.py`, `AnchoredDirectory`, durable atomic-write helper, `MAX_STATE_DB_BYTES`, serialized snapshot transaction and lifetime flock.
- Tests: swaps before load/during creation/before persistence/later transactions; live-owner contention; SIGKILL then retained-lock recovery; oversize/deserialize failure; file fsync, rename, leaf/ancestor fsync failures; durable publication ordering.
- Durable invariants: manifest bytes precede result/checkpoint transaction; publication bytes precede terminal run state.
- Explicit limitations: Phase-1 state is capped at 64 MiB, single-process/exclusive-writer, snapshot-serialized at transaction boundaries, and has no WAL/sidecar or live cross-process read support.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Checkpoints and publication plan are auditable local evidence | 01-07 | COVERED | Descriptor anchoring and mandatory sync close root gap 2 filesystem half. |
| REQ | OPS-01 | Durable structured stage evidence | 01-07 | COVERED | DB never references undurable manifest bytes. |
| REQ | OPS-04 | Safe resumable local publication plan | 01-07 | COVERED | Terminal status follows durable local plan only. |
| RESEARCH | — | Atomically written content-addressed manifests | 01-07 | COVERED | Atomicity is strengthened with directory durability and race resistance. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No later-phase feature is pulled in. |

<verification>
- CR-07 and CR-08 reproductions are encoded as deterministic seam tests and pass.
- Existing path, migration, interruption/resume and CLI disclosure suites remain green.
- Gate-B lock hash remains unchanged; no package is added.
</verification>

<success_criteria>
- Verification root gap 2 is closed across bounds, lifecycle, path anchoring and durability.
- Parent swaps cannot redirect local evidence.
- Required fsync failures prevent both checkpoint and terminal success claims.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-07-SUMMARY.md` with race-seam, fsync-failure, checkpoint-count and unchanged-hash evidence.</output>
