---
phase: 01-auditable-dry-run-spine
plan: "15"
type: execute
wave: 14
depends_on: ["01-13"]
files_modified:
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/cli.py
  - tests/test_pipeline_resume.py
  - tests/test_cli_security.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Every invalid CLI shape exits 2 with one fixed JSON diagnostic and never echoes the rejected token, path, credential canary, or argparse message."
    - "Argument parsing failures create no state, manifest, publication plan, or durable diagnostic surface."
    - "An unexpected processor exception is a sanitized retryable interruption within the finite identity-scoped budget."
    - "Retry after an unexpected exception resumes at the failed stage without replaying any successful prefix side effect."
  artifacts:
    - { path: src/skillscout/cli.py, provides: "non-echoing SafeArgumentParser boundary", exports: [SafeArgumentParser] }
    - { path: src/skillscout/application/ports.py, provides: "fixed invalid_cli_arguments diagnostic" }
    - { path: src/skillscout/application/pipeline.py, provides: "policy-consistent retryable unexpected interruption" }
    - { path: tests/test_cli_security.py, provides: "subprocess invalid-choice/unknown/missing-value disclosure regressions" }
    - { path: tests/test_pipeline_resume.py, provides: "fail-once success and finite unexpected-exception retry coverage" }
  key_links:
    - { from: argparse parsing, to: ErrorCode.INVALID_CLI_ARGUMENTS, via: "SafeArgumentParser.error/exit discards parser-generated detail", pattern: "invalid_cli_arguments" }
    - { from: src/skillscout/application/pipeline.py, to: RetryPolicy.transient_error_codes, via: "unexpected exceptions map to PIPELINE_INTERRUPTED and close the attempt as retryable", pattern: "PIPELINE_INTERRUPTED" }
    - { from: successful checkpoints, to: resumed failed stage, via: "exact identity lookup and persisted resume event skip the verified prefix", pattern: "latest_checkpoint" }
---

<objective>
Close CR-03 and WR-01 at the two external behavior boundaries: CLI input rejection and processor interruption recovery.

Purpose: Keep rejected argv outside every diagnostic surface and preserve finite, policy-correct recovery for sanitized unexpected failures.
Output: SafeArgumentParser, a fixed invalid-arguments code, retry classification alignment, and subprocess/resume regressions.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-06-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md
@src/skillscout/application/ports.py
@src/skillscout/application/pipeline.py
@src/skillscout/cli.py
@tests/test_pipeline_resume.py
@tests/test_cli_security.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-15-01: Put argparse failures inside a fixed non-echoing diagnostic boundary</name>
  <files>src/skillscout/application/ports.py, src/skillscout/cli.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/application/ports.py, src/skillscout/cli.py, tests/test_cli_security.py, tests/test_cli_dry_run.py, pyproject.toml</read_first>
  <behavior>
    - Invalid stage choice, unknown option/subcommand, missing value, and missing required argument all exit 2.
    - Stderr is exactly one canonical JSON object containing the fixed invalid_cli_arguments code and summary; stdout is empty.
    - Credential, path, newline/control, Unicode, and oversized canaries from rejected argv never appear in either stream.
    - Parser failure happens before fixture/state/output construction and leaves no durable files.
    - Valid help remains status 0 and valid dry-run/inspect behavior remains unchanged.
  </behavior>
  <action>Write RED subprocess cases that execute the packaged module boundary for an invalid `--fail-after` value, an unknown option with a canary value, an unknown subcommand, a missing option value, and missing required options. Add `ErrorCode.INVALID_CLI_ARGUMENTS` with one fixed ASCII summary bounded by the existing closed vocabulary. Implement `SafeArgumentParser` as the parser class for the root and all subparsers; override failure handling so parser-generated messages and rejected tokens are discarded, status 2 writes exactly the canonical fixed JSON diagnostic, and no default usage/error text is emitted. Keep status-0 help behavior separate. `main` may continue parsing before its runtime try block only because the parser itself is now the complete sanitized boundary. Do not pre-log, join, stringify, or persist argv. Assert the temporary working directory has no state, lock, manifest, or publication-plan artifact after each rejected invocation.</action>
  <acceptance_criteria>
    - All hostile invalid-argv subprocesses return 2 with byte-exact fixed JSON and empty stdout.
    - No rejected canary appears in stdout, stderr, exception text, or temporary-directory bytes.
    - Root parser and subparser errors use the same class and diagnostic.
    - Valid `--help`, dry-run, interruption, resume, and inspect paths keep their established exit/output contracts.
    - The error vocabulary test proves exact enum/summary coverage, ASCII, and maximum length.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_security.py -k 'argument or argparse or invalid_choice or unknown_option or missing_value or vocabulary' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/application/ports.py src/skillscout/cli.py tests/test_cli_security.py</automated></verify>
  <done>Every invalid CLI form is rejected through one fixed diagnostic without disclosing or persisting any argv content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-15-02: Make unexpected processor interruptions retryable under the finite policy</name>
  <files>src/skillscout/application/pipeline.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py, .planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md</read_first>
  <behavior>
    - A one-time arbitrary processor exception closes the attempt as failed/retryable with only pipeline_interrupted and its fixed summary.
    - The next exact-identity invocation calls the processor again at the failed stage and completes.
    - Successful earlier stage attempts, results, checkpoints, and manifests are not replayed or rewritten.
    - Three unexpected exceptions consume the same reusable-digest budget; the fourth invocation fails with retry_exhausted before processor call.
    - Explicit permanent stage failures remain non-retryable and are never invoked twice.
  </behavior>
  <action>Add fail-once-then-succeed and always-raise processors whose exception arguments contain credential/path canaries. Include `ErrorCode.PIPELINE_INTERRUPTED` in the default `RetryPolicy.transient_error_codes` beside the explicit transient stage code, so `_close_started_attempt` persists unexpected exceptions as retryable while retaining the closed code/summary and exact reusable digest. Keep intentional `--fail-after` as a run interruption after a successful checkpoint, not a failed attempt. Prove the second invocation uses the persisted resume event and starts at the failed stage, while hashes/bytes for the already successful prefix remain unchanged. Extend finite-budget tests to show three retryable unexpected failures exhaust the digest and a fourth call does not reach the processor. Retain `has_permanent_failure` behavior for explicit permanent errors.</action>
  <acceptance_criteria>
    - The reproduced one-time exception calls the processor twice total and ends planned_not_published on the second invocation.
    - Prefix processor call counts remain one and prior result/checkpoint/manifest bytes are unchanged.
    - Failed attempt rows contain only the allowlisted interruption code/summary with retryable true; canaries are absent from all emitted and durable surfaces.
    - The exact retry ceiling stops the fourth invocation, while a changed canonical identity receives its separate budget.
    - Permanent failures retain one-invocation semantics.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py -k 'unexpected or retry or permanent or exhaustion or fail_once' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_security.py tests/test_pipeline_resume.py</automated></verify>
  <done>Unexpected sanitized interruptions obey the same finite retry policy as their durable lifecycle classification and never replay successful prefix effects.</done>
</task>

</tasks>

<prohibitions>
- Do not emit argparse's message, usage-on-error text, rejected value, or raw argv.
- Do not catch parser failure and reformat the original exception detail.
- Do not make permanent failures retryable or widen retry budget beyond the existing maximum.
- Do not replay successful stages to recover from a later unexpected exception.
- Do not add network, remote writes, candidate execution, dependencies, or changes to locked artifacts.
- Do not stage or commit `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw process argv → CLI parser | Rejected values are untrusted and may contain credentials or control content. |
| Processor exception → durable/public failure | Exception arguments are untrusted and must become only a closed code/summary. |
| Retry decision → processor invocation | Classification must agree with the finite persisted retry policy. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15-01 | Information Disclosure | argparse stderr | high | mitigate | SafeArgumentParser discards generated detail and emits one fixed JSON diagnostic. |
| T-01-15-02 | Denial of Service | processor retry loop | high | mitigate | Unexpected interruptions consume the existing digest-scoped maximum and stop before a fourth call. |
| T-01-15-03 | Repudiation | attempt retry classification | high | mitigate | Persist pipeline_interrupted as retryable consistently with RetryPolicy and exact attempt identity. |
| T-01-15-04 | Elevation of Privilege | resumed side effects | critical | mitigate | Reuse only verified local checkpoints; preserve the no-remote runtime and no replay of successful prefix stages. |
| T-01-15-SC | Tampering | approved toolchain | high | mitigate | Run only exact offline locked commands and preserve approved hashes. |
</threat_model>

## Artifacts this phase produces

- Enum value: `ErrorCode.INVALID_CLI_ARGUMENTS` with fixed bounded summary.
- CLI symbol: `SafeArgumentParser` used by the root parser and every subparser.
- Policy change: default `RetryPolicy.transient_error_codes` includes `PIPELINE_INTERRUPTED` and `STAGE_TRANSIENT_FAILURE`.
- Tests: invalid choice, unknown option/subcommand, missing value/required option subprocess canaries; one-time unexpected exception recovery; three-failure exhaustion; permanent-error non-retry.
- Durable behavior: unexpected processor failure rows are retryable, sanitized, and scoped to the canonical reusable digest.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe local CLI and resumable pipeline | 01-15 | COVERED | Input rejection and unexpected interruption behavior become closed and recoverable. |
| REQ | OPS-01 | Structured attempt errors without secret leakage | 01-15 | COVERED | Parser and processor failures use fixed codes/summaries. |
| REQ | OPS-04 | Finite retry and checkpoint resume | 01-15 | COVERED | Unexpected interruptions retry under the same persisted policy without replay. |
| RESEARCH | — | Closed diagnostics and digest-scoped finite retry | 01-15 | COVERED | Existing architecture is extended consistently. |
| CONTEXT | — | No CONTEXT.md; defaults approved | — | N/A | No deferred feature is introduced. |

<verification>
- Current argparse disclosure and one-time-exception reproductions fail RED, then pass through subprocess and durable-state checks.
- Focused CLI/recovery suites and Ruff pass with no external access.
- Exact lock/frozen hashes remain unchanged.
</verification>

<success_criteria>
- Invalid CLI arguments can never echo rejected content.
- Unexpected processor failures can recover within the finite policy.
- Successful prefix effects remain single-execution across retry.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-15-SUMMARY.md` with byte-exact CLI diagnostics, retry attempt matrix, no-replay evidence, and preserved hashes.</output>
