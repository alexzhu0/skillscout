---
phase: 01-auditable-dry-run-spine
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/skillscout/cli.py
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/domain/enums.py
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/fixtures.py
  - src/skillscout/adapters/state.py
  - tests/conftest.py
  - tests/test_cli_dry_run.py
  - tests/test_side_effect_policy.py
  - tests/test_state_integrity.py
  - tests/test_cli_security.py
autonomous: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Dry-run construction rejects remote_read and remote_write registrations before any adapter method runs; production contains only none/local_state capabilities."
    - "The full approved-fixture CLI succeeds with outbound socket connects disabled and reports remote_writes_attempted=0."
    - "Minimum one-descriptor fixture safeguards from Wave 2 remain green while a broader invalid-encoding/schema/path/change/error matrix fails closed."
    - "Missing/tampered/path-inconsistent manifests, incompatible state/schema and illegal transitions never advance, restart or leak raw content."
    - "CLI/SQLite/manifests/publication plan expose only allowlisted bounded errors, never raw exception/payload/secret/path canaries."
  artifacts:
    - { path: src/skillscout/application/ports.py, provides: "closed effect-scoped registration contract", exports: [ScopedAdapter, AdapterRegistration] }
    - { path: src/skillscout/application/pipeline.py, provides: "fail-closed dry-run composition", exports: [SideEffectPolicy, DryRunRuntime, build_dry_run_runtime] }
    - { path: tests/test_side_effect_policy.py, provides: "remote capability rejection evidence" }
    - { path: tests/test_state_integrity.py, provides: "manifest/schema/transition corruption evidence" }
    - { path: tests/test_cli_security.py, provides: "expanded untrusted-input and disclosure matrix" }
  key_links:
    - { from: src/skillscout/cli.py, to: src/skillscout/application/pipeline.py, via: "only build_dry_run_runtime can construct the runner", pattern: "build_dry_run_runtime" }
    - { from: src/skillscout/adapters/fixtures.py, to: src/skillscout/domain/models.py, via: "bounded same-descriptor bytes enter strict schema", pattern: "model_validate_json" }
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/domain/canonical.py, via: "resume recomputes internally addressed manifest/output identity", pattern: "state_integrity_error" }
    - { from: tests/test_cli_dry_run.py, to: src/skillscout/cli.py, via: "in-process packaged main under socket sentinel", pattern: "remote_writes_attempted" }
---

<objective>
Harden the complete migrated local slice against forbidden capabilities, corrupted state, hostile local inputs and diagnostic disclosure, then prove final locked zero-network acceptance.

Purpose: Make Phase 1's no-remote/no-trust promises executable before later phases add GitHub and OpenAI adapters.
Output: Effect policy/composition root, expanded fail-closed integrity tests, sanitized errors and final build/recovery/no-network evidence.
</objective>

## Phase Goal

**As a** SkillScout operator, **I want to** run a frozen fixture through the complete dry-run pipeline, **so that** I can inspect structured, resumable evidence without any remote side effects.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md
@.planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md
@.planning/phases/01-auditable-dry-run-spine/01-PATTERNS.md
@.planning/phases/01-auditable-dry-run-spine/01-03-SUMMARY.md
@src/skillscout/cli.py
@src/skillscout/application/ports.py
@src/skillscout/application/pipeline.py
@src/skillscout/adapters/fixtures.py
@src/skillscout/adapters/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-03-01: Reject remote capability at composition time</name>
  <files>src/skillscout/domain/enums.py, src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/cli.py, tests/test_side_effect_policy.py</files>
  <read_first>src/skillscout/domain/enums.py, src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/cli.py, tests/test_cli_dry_run.py, .planning/phases/01-auditable-dry-run-spine/01-RESEARCH.md</read_first>
  <behavior>
    - EffectScope is closed to none, local_state, remote_read, remote_write; Phase 1 allows exactly the first two.
    - Runtime construction rejects either remote scope before any invocation canary runs.
    - Approved fixture processors, SQLite/manifests, clock/IDs and local publication planner pass and complete.
  </behavior>
  <action>Write RED registration cases. Add `EffectScope`; immutable `AdapterRegistration` and `ScopedAdapter`; `SideEffectPolicy`, `DryRunRuntime`, `build_dry_run_runtime`. Validate the complete registry before returning a runner; Phase 1 allowed scopes equal `{none, local_state}` and forbidden scope returns structured `forbidden_effect_scope`. Make `cli.main` obtain its runner exclusively through this composition root. Use synthetic registration canaries in tests; do not create fake/real HTTP, GitHub, OpenAI or remote publisher classes because production capability omission is the stronger boundary.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_side_effect_policy.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_side_effect_policy.py</automated></verify>
  <acceptance_criteria>
    - Remote-read and remote-write registrations fail before invocation canaries; none/local-state registrations pass.
    - Production registry contains only fixture processing, local state/manifests, clock/IDs and local publication planning.
    - CLI has one runner-construction path through `build_dry_run_runtime`.
    - No remote service implementation/interface/package is added.
  </acceptance_criteria>
  <done>Remote authority is structurally unavailable to dry-run rather than hidden behind a boolean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03-02: Expand fail-closed state, input and disclosure coverage</name>
  <files>src/skillscout/domain/models.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, src/skillscout/cli.py, tests/test_state_integrity.py, tests/test_cli_security.py</files>
  <read_first>src/skillscout/domain/enums.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, src/skillscout/cli.py, tests/test_stage_contracts.py, tests/test_pipeline_resume.py, tests/test_cli_dry_run.py</read_first>
  <behavior>
    - All Wave-2 one-descriptor primitive tests remain green; invalid UTF-8/JSON, extra/coerced fields and expanded race/path cases stop before processing.
    - Manifest paths derive only from DB stem, closed stage enum and validated lowercase hash hex, never stored/fixture IDs.
    - Missing/tampered manifest, embedded output mismatch, unsupported schema/producer, illegal transition and incompatible DB fail closed without mutation/restart.
    - Persisted/emitted errors contain only closed code and bounded generic summary; hostile raw payload/secret/path/exception canaries never appear.
  </behavior>
  <action>Add tests beyond—not replacements for—the already-green minimum fixture and diagnostic primitives: invalid UTF-8/JSON, unknown/coerced schema fields, repeated metadata changes, hostile IDs/path strings, symlinked output/state/manifest targets and additional bounded error canaries. Keep the same single-descriptor loader invariant. In state, derive manifest root from operator DB path and filename only from closed stage plus validated 64-character lowercase hash; require regular file, bounded bytes, `StageEnvelope`, supported schema/producer versions and recomputed manifest/output hashes before inspect/reuse. Reassert version 0/malformed/future DB and illegal transition fail closed. Extend—but never replace or weaken—the Plan-02 closed code/fixed-summary table for fixture/integrity/policy/retry failures; every added summary is still fixed generic ASCII of at most 160 characters, and no path serializes exception repr/args, Pydantic input, raw JSON, credentials or attacker-selected absolute paths. Rejection leaves run/checkpoint unchanged and never restarts Scout.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_cli_security.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_state_integrity.py tests/test_cli_security.py</automated></verify>
  <acceptance_criteria>
    - Expanded focused suites and all Wave-2 minimum fixture tests pass.
    - Manifest path accepts only known stage plus `[0-9a-f]{64}` and never incorporates run/subject/fixture/stored override.
    - Every corrupt/incompatible state case returns the documented code, preserves prior checkpoint/bytes and never restarts.
    - Secret/path/payload/exception canaries are absent from DB, manifests, publication plan and CLI JSON.
  </acceptance_criteria>
  <done>All local untrusted-input, persisted-state and diagnostic boundaries fail closed without weakening the first-slice primitives.</done>
</task>

<task type="auto">
  <name>Task 01-03-03: Prove final locked no-network acceptance</name>
  <files>tests/conftest.py, tests/test_cli_dry_run.py</files>
  <read_first>tests/conftest.py, tests/test_cli_dry_run.py, tests/test_side_effect_policy.py, tests/test_state_integrity.py, tests/test_cli_security.py, .planning/phases/01-auditable-dry-run-spine/01-VALIDATION.md, .planning/phases/01-auditable-dry-run-spine/01-01-SUMMARY.md</read_first>
  <action>Before final commands, require current lock SHA-256 still equals Gate B's approved hash and verified repo-local uv/managed CPython environment remains active. Add a socket sentinel that makes `socket.socket.connect` and `socket.create_connection` fail and records destinations. Call the installed package's `skillscout.cli.main` with a real argument vector in-process under the sentinel; keep real SQLite/filesystem I/O, traverse all stages, inspect state, and require zero attempted connections, `remote_writes_attempted=0`, `planned_not_published`. No command may depend on ambient `PATH`, activated environments or previous exports.

Use fresh `.tmp` demo paths and run the exact final sequence: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check`; `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources`; `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check .`; `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q`; `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout dry-run --fixture tests/fixtures/pipeline/approved.json --state .tmp/demo.db --output .tmp/demo-out`; `! UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout dry-run --fixture tests/fixtures/pipeline/approved.json --state .tmp/resume.db --output .tmp/resume-out --fail-after generator`; `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout dry-run --fixture tests/fixtures/pipeline/approved.json --state .tmp/resume.db --output .tmp/resume-out`; and `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked skillscout inspect-run RUN_ID --state .tmp/resume.db --format json` using the returned persisted run ID. Inspect the raw interrupted status so shell negation cannot mask setup failure. No network mock/credential/remote interface is introduced.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - Current lock bytes match Gate B; full command exits 0 under repository-local managed toolchain and target runtime stays under 15 seconds.
    - Socket sentinel records zero attempts while all nine stages, real SQLite write/read and publication planning complete.
    - Happy dry-run exits 0; fail-after Generator exits 1; rerun exits 0 with positive reused count and unchanged reused hashes.
    - Inspect includes all OPS-01 identity/telemetry and no disclosure canary; summary/plan each report zero remote writes.
    - No source/test adds HTTP/GitHub/OpenAI/shell/candidate execution/remote publication capability.
  </acceptance_criteria>
  <done>Phase 1 has reproducible build, migration, recovery, integrity and zero-network evidence under the exact approved graph.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter registry → running pipeline | Declared effects determine whether remote authority enters dry-run. |
| Filesystem/state bytes → trusted resume/inspect | Paths, types, schemas and hashes remain untrusted. |
| Exceptions/internal values → durable/operator output | Diagnostic data may contain raw secrets, paths or payloads. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | final build/test graph | high | mitigate | Exact Gate-B lock hash and verified managed toolchain are checked before final locked commands; no dependency change. |
| T-01-06 | Elevation of Privilege | dry-run registry | critical | mitigate | Closed scopes, startup rejection, production capability omission and outbound socket sentinel. |
| T-01-07 | Tampering / DoS | fixture/manifest/state paths and bytes | high | mitigate | Preserve minimum same-descriptor controls; expand strict schema/path/size/change/hash/version/transition validation. |
| T-01-08 | Information Disclosure | errors and durable/output surfaces | high | mitigate | Closed fields, sanitized bounded error map and hostile canary coverage across every surface. |

</threat_model>

## Artifacts this phase produces

- Files: `tests/test_side_effect_policy.py`, `tests/test_state_integrity.py`, `tests/test_cli_security.py`; refined CLI/domain/application/adapters/conftest/CLI acceptance.
- Capability symbols: `EffectScope`, `ScopedAdapter`, `AdapterRegistration`, `SideEffectPolicy`, `DryRunRuntime`, `build_dry_run_runtime`.
- Security behaviors: allowed scopes `{none, local_state}`, production remote capability omission, expanded fixture/path/state/schema/hash/transition checks, closed sanitized error map.
- Structured errors: `forbidden_effect_scope`, `invalid_fixture`, `state_integrity_error`, `state_schema_incompatible`, `state_schema_migration_error`, `retry_exhausted`.
- Test helpers/evidence: outbound socket sentinel, hostile disclosure canaries, corrupt/incompatible manifest/state cases, final zero-connection/zero-remote-write/recovery evidence.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Architecture-level side-effect firewall and inspectable evidence | 01-04 | COVERED | Capability policy and final E2E close the goal. |
| REQ | OPS-01 | Structured evidence remains safe under hostile state/errors | 01-04 | COVERED | Integrity and disclosure tests protect the ledger. |
| REQ | OPS-04 | Publication planning with no remote writes | 01-04 | COVERED | Registry rejection and socket sentinel prove it. |
| RESEARCH | — | Capability omission and SideEffectPolicy | 01-04 | COVERED | Architectural and defense-in-depth controls both execute. |
| RESEARCH | — | Broader matrix beyond minimum reader safeguards | 01-04 | COVERED | Minimum remains Wave 2-owned; this wave expands it. |
| RESEARCH | — | Final locked validation commands | 01-04 | COVERED | Exact approved graph is rechecked first. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No deferred decision is introduced. |

<verification>
- Post-Gate-B final command: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q`.
- Exact self-contained prefixed side-effect, integrity, CLI security and full suites pass under the exact approved lock; the Wave-4 full suite runs after focused tasks are GREEN.
- Minimum Wave-2 fixture controls remain green.
- Full CLI completes with socket connects disabled and zero remote writes.
- Fail/resume/inspect demonstrations preserve verified reused hashes.
</verification>

<success_criteria>
- Every Phase 1 high/critical threat is mitigated at its primary trust boundary.
- OPS-01 and OPS-04 have automated evidence except the two intentional supply-chain approvals.
- No remote capability, candidate execution or unapproved package/tool path exists.
</success_criteria>

<output>After completion, create `.planning/phases/01-auditable-dry-run-spine/01-04-SUMMARY.md` with final gate-hash, no-network and recovery evidence.</output>
