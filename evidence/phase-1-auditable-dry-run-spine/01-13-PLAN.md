---
phase: 01-auditable-dry-run-spine
plan: "13"
type: execute
wave: 13
depends_on: ["01-12"]
files_modified:
  - src/skillscout/domain/canonical.py
  - src/skillscout/domain/models.py
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/adapters/state.py
  - tests/test_state_integrity.py
  - tests/test_pipeline_resume.py
autonomous: true
gap_closure: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Every run has a content-addressed genesis invocation event, and every resume appends an immutable event bound to its exact verified prefix; a resume before any successful checkpoint is represented by count zero and an all-null checkpoint tuple."
    - "The run row's reuse count and latest event head are updated atomically; no caller can independently set an unattested count."
    - "The runner records a resume decision before invoking the first new stage and returns the count from persisted event authority."
    - "Schema migration never blesses a nonzero historical reuse claim that lacks a resume event."
  artifacts:
    - { path: src/skillscout/domain/models.py, provides: "strict ResumeEvent contract" , exports: [ResumeEvent] }
    - { path: src/skillscout/domain/canonical.py, provides: "non-self-referential resume-event hash", exports: [resume_event_hash] }
    - { path: src/skillscout/adapters/state.py, provides: "schema version 3 resume-event ledger and atomic resume decision" }
    - { path: src/skillscout/application/pipeline.py, provides: "resume-event-first orchestration" }
    - { path: tests/test_pipeline_resume.py, provides: "fresh/interrupted/multi-resume event lifecycle coverage" }
  key_links:
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "record_resume_decision executes before the first non-reused processor invocation", pattern: "record_resume_decision" }
    - { from: src/skillscout/adapters/state.py, to: src/skillscout/domain/canonical.py, via: "event_hash covers the exact run, ordinal, prior head, checkpoint association, reuse count, and timestamp", pattern: "resume_event_hash" }
    - { from: runs.latest_resume_event_hash, to: resume_events.event_hash, via: "atomic head and denormalized count update", pattern: "latest_resume_event_hash" }
---

<objective>
Create the immutable authority required to close CR-02 without trusting the mutable `runs.reused_stage_count` column.

Purpose: Record exactly which checkpoint prefix each invocation reused before new work begins, so later full-chain verification has an independent canonical fact to derive and check.
Output: ResumeEvent contracts/hashing, schema-version-3 persistence, safe migrations, and runner wiring.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-10-SUMMARY.md
@.planning/phases/01-auditable-dry-run-spine/01-12-SUMMARY.md
@src/skillscout/domain/canonical.py
@src/skillscout/domain/models.py
@src/skillscout/application/ports.py
@src/skillscout/application/pipeline.py
@src/skillscout/adapters/state.py
@tests/test_state_integrity.py
@tests/test_pipeline_resume.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-13-01: Add a canonical resume-event ledger and safe schema migration</name>
  <files>src/skillscout/domain/canonical.py, src/skillscout/domain/models.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py</files>
  <read_first>src/skillscout/domain/canonical.py, src/skillscout/domain/models.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, tests/test_state_integrity.py, tests/test_pipeline_resume.py</read_first>
  <behavior>
    - A genesis event has ordinal zero, no prior head, no checkpoint association, reuse count zero, and a recorded timestamp equal to run creation.
    - A non-genesis zero-prefix resume event has the next contiguous ordinal, the prior event hash, reuse count zero, and an all-null checkpoint stage/result-row/manifest tuple.
    - A positive-prefix resume event has the next contiguous ordinal, the prior event hash, reuse count equal to checkpoint stage index plus one, and exact checkpoint stage/result-row/manifest identities.
    - Event hash calculation excludes only its own hash and is stable under canonical JSON.
    - Fresh and migrated databases have one exact schema-version-3 fingerprint including `resume_events` and `runs.latest_resume_event_hash`.
    - A schema-version-1 or schema-version-2 run whose pre-event reuse is absent or zero migrates to genesis only; a schema-version-2 run with nonzero reuse and no event is rejected unchanged rather than converted into self-attested evidence.
  </behavior>
  <action>Write RED model, hash, schema-fingerprint, and migration cases. Add strict frozen `ResumeEvent` with `event_hash`, `run_id`, contiguous `event_index`, `prior_event_hash`, `reused_stage_count`, nullable `checkpoint_stage`, `checkpoint_result_row_id`, `checkpoint_manifest_hash`, and `recorded_at`. Enforce exactly three shapes: ordinal zero has no prior head, count zero, and the all-null checkpoint tuple; a later zero-prefix event has a prior head, count zero, and the same all-null tuple; a later positive-prefix event has a prior head, count equal to the named checkpoint stage index plus one, and a complete checkpoint tuple. Add `resume_event_hash` in `domain/canonical.py` over the canonical JSON object `{run_id, event_index, prior_event_hash, reused_stage_count, checkpoint_stage, checkpoint_result_row_id, checkpoint_manifest_hash, recorded_at}`, excluding only `event_hash`. Advance the SQLite file schema to version 3 by adding immutable `resume_events` rows keyed by `event_hash` with unique `(run_id, event_index)`, plus `runs.latest_resume_event_hash`; extend the exact SQL/column/foreign-key/index fingerprint rather than accepting additive objects. Fresh `create_run` must insert the run and its genesis event/head/count with `recorded_at = created_at` in one snapshot transaction. Rebuild schema-version-1 and schema-version-2 inputs through the same current descriptor set. During migration, normalize a missing legacy count to zero and create exactly one genesis event at the run's `created_at` only when the pre-event count is zero; never synthesize a later zero-prefix event because an older invocation boundary is unknowable. If any pre-event run asserts a nonzero count, fail closed and leave source bytes and manifests unchanged. Preserve all stage schema versions and the frozen database bytes.</action>
  <acceptance_criteria>
    - ResumeEvent accepts a non-genesis zero-prefix event only with a prior hash and the all-null checkpoint tuple, and rejects partial tuples, invalid ordinal/prior/count combinations, unknown fields, and self-inconsistent hashes.
    - Fresh creation and both supported migrations end on the identical exact schema-version-3 fingerprint.
    - Migrated zero-reuse runs contain genesis only with a matching head/count; migration never invents a zero-prefix resume decision.
    - The frozen schema-version-1 fixture migrates from a copy and its source SHA-256 remains unchanged.
    - Unsupported, malformed, or nonzero-unattested historical state is rejected without durable mutation or new manifests.
    - Every created run has exactly one genesis event and matching run head/count before any stage attempt exists.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 tests/fixtures/state/v1-cli.db | awk '{print $1}')" = "49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_pipeline_resume.py -k 'schema or migration or resume_event or genesis' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/domain src/skillscout/application/ports.py src/skillscout/adapters/state.py tests/test_state_integrity.py</automated></verify>
  <done>Every current run begins with a canonical invocation event, and migration cannot manufacture authority for a historical nonzero reuse claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-13-02: Record each exact resume decision before new stage work</name>
  <files>src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/domain/models.py, tests/test_pipeline_resume.py</read_first>
  <behavior>
    - New runs use their atomic genesis event and report reuse zero.
    - Resuming interrupted or orphan-reconciled state appends an event against the latest verified checkpoint, or an all-null zero-prefix event when no checkpoint exists, before invoking the next processor.
    - The event insert, latest-head update, reuse-count update, and interrupted-to-running transition are one snapshot mutation.
    - Repeated interruption/resume creates a contiguous prior-hash chain and does not replay any already successful stage.
    - A run reopened after creation but before its first attempt appends a zero-prefix event; if it crashes after that decision but before the attempt, the next invocation appends another zero-prefix event and still starts at stage zero.
    - The runtime summary reads the persisted latest event count instead of a caller-local integer.
  </behavior>
  <action>Replace `StateStore.set_reused_stage_count` with `record_resume_decision(run_id, checkpoint: Checkpoint | None, recorded_at) -> ResumeEvent`. In the SQLite implementation, verify the current run and optional checkpoint association inside the private candidate transaction, compute the next ordinal/prior head, use count `checkpoint.stage_index + 1` with the exact checkpoint tuple when a checkpoint exists, otherwise use count zero with all three checkpoint fields null, insert the immutable event, and atomically update the run's head/count/status/timestamp. Never derive the zero count from `runs.reused_stage_count`; derive it from the verified absence of checkpoints. In `PipelineRunner.run`, keep new-run genesis behavior, but for every selected resumable run call `record_resume_decision` after full-chain checkpoint selection and before `abandon_stale_running` or any processor invocation; use the returned event count for `start_index`, previous output selection, and `RunSummary`. Remove the independent count setter from the port and production code. Add a crash-before-first-attempt regression by reopening a genesis-only running run, and add a crash-after-zero-prefix-decision regression by faulting immediately after the zero event commits but before `start_attempt`; on the following invocation assert a second zero-prefix event is appended, stage zero is attempted once successfully, and the complete event chain verifies. Retain fail-after, fail-on-resume, A/B/A, and multiple-positive-resume tests that assert events are durable before new work, successful prefix attempts/manifests are byte-identical, and event ordinals/prior hashes remain exact.</action>
  <acceptance_criteria>
    - No production method can update `reused_stage_count` without inserting and heading a canonical event in the same transaction.
    - A crash after run creation but before the first attempt is recoverable from genesis authority and records a zero-prefix resume event without trusting the run row count.
    - A crash after zero-prefix event durability but before processor invocation leaves that event auditable; the next invocation appends another zero-prefix event and begins at stage zero.
    - A failure after a positive-prefix event is durable resumes again without replaying the verified prefix.
    - A/B/A exact-identity selection records the resumed A checkpoint and never associates B evidence with A's event.
    - Fresh, one-resume, and multiple-resume summaries match their persisted event counts.
    - The full existing recovery suite remains green under schema version 3.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_pipeline_resume.py -k 'resume or interrupt or a_b_a or reused or event or crash_before_first or zero_prefix' &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never UV_OFFLINE=1 "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_state_integrity.py tests/test_pipeline_resume.py</automated></verify>
  <done>Every reuse count now originates in an immutable, verified-prefix-bound event persisted before the resumed invocation performs work.</done>
</task>

</tasks>

<prohibitions>
- Do not derive reuse authority from timestamps, subject recency, or the mutable run count alone.
- Do not synthesize a trusted nonzero migration event from an older unattested count.
- Do not alter stage input/output/result identity semantics or the frozen schema-version-1 fixture.
- Do not add dependencies, network access, remote writes, or candidate-code execution.
- Do not stage or commit `.planning/config.json`.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verified prefix, including empty → next invocation | Reuse authority must be recorded before the first new processor call. |
| Older SQLite schema → current event ledger | Historical mutable counts cannot become immutable facts by migration assertion. |
| Application resume choice → persisted run summary | Head, count, event, and status must commit atomically. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Tampering | `reused_stage_count` | high | mitigate | Derive it from a canonical verified-prefix event and update the denormalized value only with the event head. |
| T-01-13-02 | Repudiation | resume lifecycle | high | mitigate | Persist contiguous hash-linked events before new work and retain every invocation decision. |
| T-01-13-03 | Tampering | schema migration | high | mitigate | Reject unattested historical nonzero reuse and validate the exact current fingerprint before promotion. |
| T-01-13-04 | Elevation of Privilege | resumed pipeline | critical | mitigate | Reuse only full-chain verified local checkpoints; preserve the sealed no-remote composition. |
| T-01-13-SC | Tampering | lock/frozen inputs | high | mitigate | Recheck exact approved hashes and introduce no package-manager operation beyond locked local tests. |
</threat_model>

## Artifacts this phase produces

- Domain symbol: `ResumeEvent`.
- Canonical symbol: `resume_event_hash` and its explicit non-self-referential preimage.
- SQLite schema: version 3, table `resume_events`, column `runs.latest_resume_event_hash`, and exact fingerprint descriptors/indexes.
- Port method: `StateStore.record_resume_decision(run_id, checkpoint: Checkpoint | None, recorded_at) -> ResumeEvent`; independent `set_reused_stage_count` is removed.
- Runtime behavior: `PipelineRunner.run` persists the resume decision before stage invocation and returns its durable count.
- Tests: genesis, crash-before-first-attempt, crash-after-zero-prefix-decision, consecutive zero-prefix events, positive-prefix resume, repeated-resume, A/B/A association, event-before-work, safe migration, and unattested-nonzero rejection.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Auditable checkpoint recovery | 01-13 | COVERED | Invocation reuse becomes a durable canonical fact. |
| REQ | OPS-01 | Stable, versioned run audit data | 01-13 | COVERED | ResumeEvent and schema fingerprint make reuse provenance structural. |
| REQ | OPS-04 | Resume from latest successful checkpoint without replay | 01-13 | COVERED | The exact selected prefix, including an empty prefix, is recorded before new work. |
| RESEARCH | — | Content-addressed ledger and exact identity recovery | 01-13 | COVERED | Event hashes and checkpoint associations reuse the canonical boundary. |
| CONTEXT | — | No CONTEXT.md; defaults approved | — | N/A | No deferred feature is introduced. |

<verification>
- RED event/schema/runner tests fail against the mutable count implementation and pass after the event ledger is wired.
- Fresh, migrated, crash-before-first-attempt, consecutive-zero-prefix, positive-prefix, repeated-resume, and A/B/A flows remain deterministic and local.
- The lock and frozen database remain byte-identical.
</verification>

<success_criteria>
- Every reuse count has verified-prefix provenance recorded before new work, including explicit empty-prefix provenance at count zero.
- Schema migration cannot turn an unverifiable historical count into trusted evidence.
- Successful prior stages are never replayed solely to reconstruct resume provenance.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-13-SUMMARY.md` with schema fingerprint, event preimage, migration matrix, and invocation ordering evidence.</output>
