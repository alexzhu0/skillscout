---
phase: 01-auditable-dry-run-spine
plan: "02"
subsystem: pipeline
tags: [cli, sqlite, pydantic, fixture-safety, audit, dry-run]

requires:
  - phase: 01-auditable-dry-run-spine/01-01
    provides: "Gate-A-verified local toolchain and Gate-B-approved exact lock bytes"
provides:
  - "Packaged local-only CLI crossing nine deterministic stages to planned_not_published"
  - "Single-descriptor 65,536-byte fixture boundary with change detection"
  - "SQLite schema v1 with pre-processor reusable identity and durable checkpoints"
  - "Frozen real Generator-interrupted v1 database and provenance for v2 migration"
affects: [01-03-audit-ledger, 01-04-capability-firewall]

tech-stack:
  added: []
  patterns: [closed diagnostic allowlist, pre-processor identity commit, local-only composition, frozen migration fixture]

key-files:
  created:
    - src/skillscout/cli.py
    - src/skillscout/application/ports.py
    - src/skillscout/application/pipeline.py
    - src/skillscout/adapters/fixtures.py
    - src/skillscout/adapters/state.py
    - tests/fixtures/state/v1-cli.db
    - tests/fixtures/state/v1-cli-provenance.json
  modified:
    - tests/conftest.py
    - tests/test_cli_dry_run.py

key-decisions:
  - "Load and strictly validate the bounded fixture before opening SQLite, so rejected input cannot create a run or state file."
  - "Persist the exact five-field reusable identity on each running attempt before invoking its processor."
  - "Expose only four closed error codes and fixed ASCII summaries on every schema-v1 failure surface."

patterns-established:
  - "Schema-v1 processors receive no network, shell, GitHub, OpenAI, candidate-code or remote-publication capability."
  - "Intentional interruption happens only after the named stage result and checkpoint are committed."
  - "Tracked migration fixtures are generated by the packaged CLI and bound to a provenance hash/fact record."

requirements-completed: [OPS-01, OPS-04]

coverage:
  - id: D1
    description: "The packaged CLI traverses all nine stages through real SQLite and produces only a local planned_not_published publication plan."
    requirement: OPS-04
    verification:
      - kind: e2e
        ref: "tests/test_cli_dry_run.py#test_approved_fixture_reaches_planned_not_published"
        status: pass
      - kind: other
        ref: "uv run --locked pytest -q (11 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Fixture and diagnostic boundaries reject unsafe input without creating state or disclosing hostile canaries."
    requirement: OPS-01
    verification:
      - kind: integration
        ref: "tests/test_cli_dry_run.py fixture safety and disclosure cases"
        status: pass
      - kind: other
        ref: "uv run --locked ruff check src/skillscout tests/test_cli_dry_run.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "A real packaged Generator-interrupted schema-v1 database is frozen with reusable identity and no Validators attempt."
    requirement: OPS-01
    verification:
      - kind: integration
        ref: "tests/test_cli_dry_run.py#test_frozen_v1_cli_fixture_matches_provenance"
        status: pass
      - kind: manual_procedural
        ref: "raw packaged CLI exit 1; database SHA-256 49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251"
        status: pass
    human_judgment: false

duration: 15 min
completed: 2026-07-17
status: complete
---

# Phase 1 Plan 02: Safe Schema-v1 Walking Skeleton Summary

**A packaged local-only CLI now moves a bounded fixture through nine deterministic stages, commits auditable SQLite v1 evidence, and stops at a review-only publication plan.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-07-17T05:41:00Z
- **Completed:** 2026-07-17T05:56:06Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Proved the pre-implementation contract RED only at the named missing CLI behavior after a successful approved build and nine-test collection.
- Implemented a complete fixture → nine-stage pipeline → SQLite v1 write/read → local `publication-plan.json` path with zero remote writes.
- Closed the first input and diagnostic trust boundaries with one-descriptor reading, size/change controls, strict Pydantic parsing and four fixed sanitized errors.
- Generated and froze a real packaged CLI database interrupted after Generator, with six durable checkpoints and no Validators attempt.

## RED Gate Evidence

- Gate A and Gate B approvals were present in `01-01-SUMMARY.md`.
- Current `uv.lock` SHA-256 was and remains `caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32`.
- Exact self-contained `build --no-sources` succeeded.
- Collection listed the named happy path, all minimum fixture controls and both hostile-disclosure cases: 9 tests collected.
- The un-negated pytest status was exactly `1`; the sole failure was `test_approved_fixture_reaches_planned_not_published` at `dry-run CLI behavior is not implemented`. No setup, import, collection, usage or no-test failure occurred.

## Task Commits

1. **Task 01-01-02: Prove the packaged CLI contract is RED** — no new file commit; the precommitted scaffold from `10ee77f` was executed without production changes
2. **Task 01-01-03: Make the safe schema-v1 Walking Skeleton GREEN** — `6d5b5dc` (`feat`)

The plan metadata and this summary are committed separately by `docs(01-02)`.

## Files Created/Modified

- `src/skillscout/cli.py` — packaged `dry-run` parser, structured success and sanitized failure boundary.
- `src/skillscout/application/ports.py` — local-only processor/state protocols and the closed schema-v1 error vocabulary.
- `src/skillscout/application/pipeline.py` — ordered nine-stage runner, canonical v1 identity and local publication plan.
- `src/skillscout/adapters/fixtures.py` — bounded one-descriptor reader and deterministic fixture processor.
- `src/skillscout/adapters/state.py` — real SQLite v1 runs, attempts, results and checkpoints.
- `tests/test_cli_dry_run.py` — end-to-end, persistence, fixture-race, disclosure and interruption evidence.
- `tests/fixtures/state/v1-cli.db` — immutable real CLI-produced Generator-interrupted state.
- `tests/fixtures/state/v1-cli-provenance.json` — generating command, hashes, run/checkpoint and row-count facts.

## Frozen Schema-v1 Evidence

- Raw packaged CLI exit: `1`, with only `pipeline_interrupted` and its fixed summary.
- Database SHA-256: `49fa8067a2cc7e55b3afb2e2c93aca91f2b3d6cfbaee1bc32242f7b175bc0251`.
- `PRAGMA user_version`: `1`.
- Run: `884039fcafca4757a194a9a69ca0e306`, status `interrupted`.
- Last checkpoint: stage `generator`, index `5`, output hash `sha256:141156ca85ae35297f9208f48ffff2c4ad5783e8882dd952d9d88c0b6cfdfbc1`.
- Row counts: one run; six attempts, results and checkpoints; zero Validators attempts.

## Decisions Made

- Rejected fixture input is validated completely before SQLite construction, keeping the failure boundary side-effect free.
- Reusable identity uses `sha256:` plus lowercase hex over the exact canonical five-field v1 preimage required by the next migration wave.
- The publication artifact intentionally contains no branch, commit, PR, merge, credential or remote-operation field.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The managed sandbox initially denied the default uv cache directory. The exact approved uv commands were rerun with scoped permission; neither the command prefix, lock bytes nor dependency graph changed.

## Threat Status

- **T-01-SC:** mitigated; every command used the repository-local verified uv/managed-Python/no-download prefix and the lock hash stayed unchanged.
- **T-01-01:** mitigated; symlink, non-regular, declared oversize, streaming overflow and same-descriptor change cases pass.
- **T-01-02:** mitigated; source inspection finds no HTTP, GitHub, OpenAI, shell, socket or remote-publication client/capability.
- **T-01-08:** mitigated; raw, Pydantic, exception, credential and path canaries are absent from emitted and durable failure surfaces.
- **T-01-12:** mitigated; identity is committed on the running attempt before processor invocation, and the real Generator interruption is durable.

## Verification

- Focused pytest: **11 passed**.
- Ruff on `src/skillscout` and `tests/test_cli_dry_run.py`: **passed**.
- Full pytest: **11 passed**.
- Final `build --no-sources`: **passed**, producing wheel and sdist.
- Final `uv.lock` SHA-256: **unchanged and Gate-B approved**.

## User Setup Required

None. This plan has no service, credential or external runtime configuration.

## Next Phase Readiness

The frozen v1 CLI database and exact provenance are ready for Plan 03 to copy, transactionally migrate to schema v2 and resume first at Validators without replaying Scout through Generator.

---
*Phase: 01-auditable-dry-run-spine*
*Completed: 2026-07-17*

## Self-Check: PASSED

- All seven created implementation/evidence files exist.
- Task commit `6d5b5dc` exists and contains no tracked deletion.
- Frozen database hash and provenance facts agree.
- `01-02-SUMMARY.md` exists with `status: complete`.
