---
phase: 01-auditable-dry-run-spine
plan: "05"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/adapters/fixtures.py
  - src/skillscout/adapters/state.py
  - tests/test_side_effect_policy.py
autonomous: true
gap_closure: true
requirements: [OPS-04]
must_haves:
  truths:
    - "Phase-1 runtime construction always enforces the immutable {none, local_state} authority ceiling; no caller argument can widen it."
    - "Every production registration derives its scope from a supported concrete adapter, so an arbitrary object cannot be blessed with a false caller-provided label."
    - "A remote-declaring or unsupported processor/state adapter is rejected before PipelineRunner construction or adapter invocation."
  artifacts:
    - { path: src/skillscout/application/ports.py, provides: "truthful adapter-declared effect contract", exports: [ScopedAdapter, AdapterRegistration] }
    - { path: src/skillscout/application/pipeline.py, provides: "immutable Phase-1 composition ceiling", exports: [PHASE_ONE_MAX_SCOPES, SideEffectPolicy, build_dry_run_runtime] }
    - { path: tests/test_side_effect_policy.py, provides: "CR-01 policy-widening and mislabel regression evidence" }
  key_links:
    - { from: src/skillscout/adapters/fixtures.py, to: src/skillscout/application/ports.py, via: "FixtureProcessor declares its own closed effect_scope", pattern: "effect_scope" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/application/ports.py, via: "registrations derive authority from adapters and validate against PHASE_ONE_MAX_SCOPES", pattern: "PHASE_ONE_MAX_SCOPES" }
    - { from: src/skillscout/cli.py, to: src/skillscout/application/pipeline.py, via: "CLI retains the sole production build_dry_run_runtime path", pattern: "build_dry_run_runtime" }
---

<objective>
Close verification root gap 1 and CR-01 by making Phase-1 authority immutable and every production capability declaration truthful at the composition boundary.

Purpose: Prevent callers from widening dry-run into remote-read/write authority or attaching an effect-free label to an arbitrary processor.
Output: Adapter-owned effect declarations, a fixed Phase-1 ceiling, a closed production registry, and adversarial construction tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditable-dry-run-spine/01-VERIFICATION.md
@.planning/phases/01-auditable-dry-run-spine/01-REVIEW.md
@.planning/phases/01-auditable-dry-run-spine/01-04-SUMMARY.md
@src/skillscout/application/ports.py
@src/skillscout/application/pipeline.py
@src/skillscout/adapters/fixtures.py
@src/skillscout/adapters/state.py
@tests/test_side_effect_policy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-05-01: Make effect declarations adapter-owned and non-forgeable</name>
  <files>src/skillscout/application/ports.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, tests/test_side_effect_policy.py</files>
  <read_first>src/skillscout/application/ports.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, tests/test_side_effect_policy.py, .planning/phases/01-auditable-dry-run-spine/01-REVIEW.md</read_first>
  <behavior>
    - Registration reads one immutable EffectScope from the adapter itself; callers cannot submit an unrelated scope beside an arbitrary object.
    - FixtureProcessor declares none and SQLiteStateStore declares local_state.
    - Missing, malformed, remote_read and remote_write declarations fail before an invocation canary runs.
  </behavior>
  <action>Write the CR-01 RED cases first. Refactor `ScopedAdapter` and `AdapterRegistration` so registration construction takes a name plus an adapter and obtains `effect_scope` from that adapter; remove the independently supplied scope field that enabled mislabeling. Give the supported Phase-1 `FixtureProcessor` and `SQLiteStateStore` concrete immutable declarations. Validate name, adapter type and the closed enum without invoking adapter behavior. Keep `PipelineRunner` provider-independent for focused tests, but do not let the production composition root accept an arbitrary object merely because it exposes a convenient label. Do not add remote adapter types, network clients, dynamic imports, shell execution or dependencies.</action>
  <acceptance_criteria>
    - Constructing a registration cannot pair an adapter with a caller-selected different scope.
    - FixtureProcessor and SQLiteStateStore expose exactly none and local_state respectively.
    - Missing/non-enum/remote declarations are rejected before any invocation counter changes.
    - Existing immutable-registration and local happy-path behaviors remain green.
  </acceptance_criteria>
  <verify><automated>test "$(shasum -a 256 uv.lock | awk '{print $1}')" = "caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32" &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_side_effect_policy.py</automated></verify>
  <done>Authority metadata is owned by supported adapters and cannot be forged independently by a registration caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-05-02: Seal the production composition root at the Phase-1 maximum</name>
  <files>src/skillscout/application/pipeline.py, tests/test_side_effect_policy.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, src/skillscout/adapters/fixtures.py, src/skillscout/adapters/state.py, src/skillscout/cli.py, tests/test_side_effect_policy.py, tests/test_cli_dry_run.py</read_first>
  <behavior>
    - build_dry_run_runtime has no public path that accepts a broader authority policy or arbitrary extra production registrations.
    - PHASE_ONE_MAX_SCOPES is fixed to none plus local_state and is applied before PipelineRunner exists.
    - A processor subclass declaring remote authority and an unsupported processor declaring none are both rejected before process is called.
    - The CLI-supported concrete fixture/state combination still traverses all nine stages.
  </behavior>
  <action>Write regressions that reproduce the previously accepted permissive policy and mislabeled processor. Define `PHASE_ONE_MAX_SCOPES` as the immutable upper bound and make `SideEffectPolicy.phase_one()` the only policy used by `build_dry_run_runtime`. Remove the public policy-widening and arbitrary-registration inputs instead of intersecting after construction. Construct the complete fixed registry only from the supported concrete Phase-1 fixture processor, SQLite state adapter, clock, ID provider and local publication writer; require supported concrete types or explicit trusted wrappers and validate all declared scopes before creating `PipelineRunner`. Preserve lower-level dependency injection on `PipelineRunner` for deterministic tests. Retain the CLI call shape and structured `FORBIDDEN_EFFECT_SCOPE` outcome for declared remote authority.</action>
  <acceptance_criteria>
    - Calling the prior permissive-policy path cannot construct a runtime; the regression proves no runner or adapter invocation occurs.
    - A remote-declaring processor and an unsupported effect-free object are rejected before processor invocation.
    - Runtime registrations contain only the fixed Phase-1 supported objects and scopes are a subset of PHASE_ONE_MAX_SCOPES.
    - The local registry and packaged dry-run happy path remain successful with zero remote writes.
  </acceptance_criteria>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_side_effect_policy.py tests/test_cli_dry_run.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_side_effect_policy.py</automated></verify>
  <done>The only production runtime builder enforces an immutable Phase-1 ceiling and cannot bless caller-injected remote or unsupported authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller → production composition root | Caller-provided objects and policy choices are untrusted authority requests. |
| Adapter declaration → registration | A declared scope must originate from the supported adapter rather than a parallel label. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-G1-01 | Elevation of Privilege | build_dry_run_runtime | critical | mitigate | Remove widening inputs, enforce PHASE_ONE_MAX_SCOPES and validate the fixed registry before runner construction. |
| T-01-G1-02 | Spoofing | AdapterRegistration | high | mitigate | Derive scope from supported concrete adapters and reject missing, malformed or remote declarations before invocation. |
| T-01-SC | Tampering | locked toolchain graph | high | mitigate | No dependency or lock change; every test uses the exact approved repository-local uv prefix and verifies the lock hash. |
</threat_model>

## Artifacts this phase produces

- Symbols: `PHASE_ONE_MAX_SCOPES`, adapter-owned `effect_scope`, non-forgeable `AdapterRegistration`, sealed `build_dry_run_runtime`.
- Tests: permissive-policy widening, mismatched/missing declaration, remote-declaring processor, unsupported effect-free processor, and supported local registry completion.
- Preserved evidence: Gate-B `uv.lock` SHA-256 remains `caeeddcf4a6d5758d0b4182b49bf87730c2351a6f9d06986ebf612c7e5b4ac32`; no remote service or credential surface is introduced.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dry-run blocks remote writes at architecture level | 01-05 | COVERED | Immutable composition ceiling closes verification root gap 1. |
| REQ | OPS-04 | Dry-run produces only a local publication plan | 01-05 | COVERED | Caller cannot widen runtime authority. |
| RESEARCH | — | Structural capability omission, not a boolean | 01-05 | COVERED | Production registry is closed before runner construction. |
| CONTEXT | — | No CONTEXT.md; discussion was explicitly skipped | — | N/A | No deferred decision is introduced. |

<verification>
- Focused authority regressions and the existing CLI happy path pass under the exact locked toolchain.
- `uv.lock` bytes remain the Gate-B-approved hash.
- No HTTP, GitHub, OpenAI, socket, subprocess, credential or remote-publication implementation is added.
</verification>

<success_criteria>
- CR-01 and verification root gap 1 are closed by construction, not merely by default configuration.
- Unsupported or remote authority is rejected before any adapter runs.
- OPS-04 retains a real nine-stage local-only happy path.
</success_criteria>

<output>Create `.planning/phases/01-auditable-dry-run-spine/01-05-SUMMARY.md` with the adversarial policy/declaration results and unchanged lock hash.</output>
