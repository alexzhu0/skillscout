---
phase: 04-controlled-draft-pr
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-controlled-draft-pr/04-ACTION-AUDIT.md
  - tools/verify_phase4_action_audit.py
  - tests/test_phase4_action_audit.py
autonomous: true
requirements: [PUB-04, SEC-02]
must_haves:
  truths:
    - "Exact full commit SHAs of actions/checkout and actions/create-github-app-token are identified without installing or executing either action."
    - "Each repository identity, commit, tree, source paths, permissions, transitive action references, and reviewed hashes are recorded for a separate human decision."
  artifacts:
    - path: .planning/phases/04-controlled-draft-pr/04-ACTION-AUDIT.md
      provides: "Non-authorizing exact action-set identities and evidence."
    - path: tools/verify_phase4_action_audit.py
      provides: "Dependency-free audit consistency verifier."
    - path: tests/test_phase4_action_audit.py
      provides: "Mutation tests for the verifier."
  key_links:
    - from: 04-ACTION-AUDIT.md
      to: 04-08 checkpoint
      via: "Exact candidate SHA set and evidence for human approval."
  prohibitions:
    - "Audit is not approval and must not modify workflow, lockfile, environment, or execute action code."
    - "No mutable action tag may become authority."
---

<objective>
Produce a dependency-free, non-authorizing supply-chain audit for every executable action used by the workflow.

Purpose: Give the human gate exact immutable evidence instead of a tag or package name.
Output: Audited SHA/evidence document and standalone consistency verifier.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-controlled-draft-pr/04-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-01-PLAN.md
@.planning/phases/03-validated-skill-candidate/03-02-PLAN.md
@.planning/phases/03-validated-skill-candidate/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>04-07-01: Record exact workflow action identities without execution</name>
  <files>.planning/phases/04-controlled-draft-pr/04-ACTION-AUDIT.md, tools/verify_phase4_action_audit.py</files>
  <read_first>
    - `04-RESEARCH.md` Open Question 4 and workflow dependency boundary.
    - Phase 3 Gate A/B plans for non-executing supply-chain evidence discipline.
  </read_first>
  <action>First create the repository-local standard-library-only `tools/verify_phase4_action_audit.py` with the fixed audit schema and exact expected repository identities; it must be read-only, dependency-free, offline, and reject missing evidence, mutable authority, malformed digests/SHAs, unresolved nested actions/install hooks, or any status other than `audited_not_approved`. Then, using read-only GitHub metadata/content access, resolve the selected official `actions/checkout` and `actions/create-github-app-token` releases to exact full 40-hex commit SHAs. For each record repository numeric ID/full name, selected release/tag as non-authority metadata, commit/tree SHA, source action manifest and lock/package metadata paths, exact content SHA-256 values, runtime, input/output/permission behavior, nested `uses:` identities, network/install/code-execution behavior, and provenance/release evidence. Classify every unresolved claim. Do not checkout, install, import, build, or execute either action. End with `status: audited_not_approved` and the exact candidate SHA set for Plan 08.</action>
  <acceptance_criteria>
    - Document contains both exact candidate commits and complete evidence tables.
    - Mutable tag is clearly non-authoritative.
    - No approval language or workflow mutation occurs.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase4_action_audit.py</automated></verify>
  <done>The action candidate is identified and reviewable without executing third-party code.</done>
</task>

<task type="auto" tdd="true">
  <name>04-07-02: Make the action audit independently verifiable</name>
  <files>tests/test_phase4_action_audit.py, tools/verify_phase4_action_audit.py, .planning/phases/04-controlled-draft-pr/04-ACTION-AUDIT.md</files>
  <read_first>
    - `04-ACTION-AUDIT.md` exact fields and hashes.
    - `tools/verify_phase3_validation_map.py` dependency-free verifier style.
  </read_first>
  <action>Add mutation tests for every audit field and invariant, including repository substitution, mutable authority, candidate/tree/content digest changes, unresolved nested actions, executable install hooks, and status promotion. Tighten the Task 1 verifier only where a mutation exposes a missing fixed-schema invariant. The verifier must not import project code, access network, write files, spawn subprocesses, or execute action content.</action>
  <acceptance_criteria>
    - Original audit passes; every authority/hash/status mutation fails.
    - Verifier has no third-party import, subprocess, shell, or network code.
    - Audit remains explicitly non-authorizing.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase4_action_audit.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase4_action_audit.py</automated></verify>
  <done>Plan 08 receives exact independently checked supply-chain evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable release metadata → exact action identity | Tags/releases cannot authorize workflow execution. |
| External source bytes → human approval evidence | Bytes are inspected and hashed, never run. |

## STRIDE / Abuse Register

| Threat ID | Category | Abuse case | Likelihood | Impact | Severity | Disposition | Mitigation | Residual risk | Blocking |
|---|---|---|---|---|---|---|---|---|---|
| T-04-SC | Tampering | Workflow/action supply-chain substitution | medium | critical | high | mitigate | Exact repository/commit/tree/content hashes and nested-action audit | Upstream compromise before observed commit | Human gate |
| T-04-SC2 | Elevation | Audit accidentally executes action code | low | critical | high | mitigate | Read-only metadata/content and static dependency-free verifier | Tool implementation bug | Tests/static review |
</threat_model>

## Artifacts this phase produces

- `.planning/phases/04-controlled-draft-pr/04-ACTION-AUDIT.md`
- Audit fields: `status`, `actions`, and per-action `repository_id`, `repository_full_name`, `candidate_commit_sha`, `tree_sha`, `release_tag_metadata`, `evidence_files`, `nested_actions`, `runtime`, `permissions`, `unresolved_claims`.
- `tools/verify_phase4_action_audit.py`: `main`, `verify_audit`.
- `tests/test_phase4_action_audit.py`.
- No production symbols, workflow, CLI flags, or package install.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Safe platform credential path | COVERED as prerequisite evidence. |
| REQ | PUB-04, SEC-02 | COVERED by immutable action identity audit. |
| RESEARCH | Exact action SHA open gate | COVERED. |
| CONTEXT | No CONTEXT.md | N/A. |

<verification>Run the audit mutation suite and standalone verifier.</verification>
<success_criteria>The complete exact workflow action set is auditable but cannot be used until Plan 08 human approval.</success_criteria>
<output>Create `.planning/phases/04-controlled-draft-pr/04-07-SUMMARY.md` when done.</output>
