---
phase: 02-safe-single-repository-extraction
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/skillscout/application/ports.py
  - src/skillscout/application/pipeline.py
  - src/skillscout/application/processors.py
  - src/skillscout/adapters/github.py
  - src/skillscout/adapters/state.py
  - src/skillscout/domain/models.py
  - tests/recorded_transport.py
  - tests/fixtures/github/repo_mit.json
  - tests/fixtures/github/repo_archived.json
  - tests/fixtures/github/repo_fork.json
  - tests/fixtures/github/repo_private.json
  - tests/fixtures/github/commits_pin.json
  - tests/fixtures/github/commits_pin_sha256.json
  - tests/fixtures/github/tree_full.json
  - tests/fixtures/github/tree_no_readme.json
  - tests/fixtures/github/tree_license_multiple.json
  - tests/fixtures/github/tree_truncated.json
  - tests/fixtures/github/license_mit.json
  - tests/fixtures/github/license_noassertion.json
  - tests/fixtures/github/license_mismatch.json
  - tests/fixtures/github/license_404.json
  - tests/fixtures/github/rate_limit_429.json
  - tests/fixtures/github/redirect_301.json
  - tests/fixtures/github/blob_readme.json
  - tests/test_github_adapter.py
  - tests/test_phase2_pipeline.py
  - tests/test_scout_filter.py
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, READ-01]
must_haves:
  truths:
    - "The runner resolves a closed PipelineProfile from producer_version (fixture-v1 -> all nine stages, no context, PLANNED_NOT_PUBLISHED; phase2-v1 -> (SCOUT, FILTER, READER, EXTRACTOR) as a spine prefix with global indices 0..3, runtime-only context, COMPLETED), the prefix property is validated at import time, and every Phase 1 test including all process(self, stage_input) override subclasses stays byte-for-byte unchanged and green."
    - "StageOutcome.telemetry is copied into the running attempt row via the additive StateStore.record_attempt_telemetry seam before complete_stage, and into the StageEnvelope and stage_output_hash preimage; fixture-v1 telemetry remains None everywhere so Phase 1 hashes are unchanged, and verify_run_chain passes on both producers."
    - "Scout pins the exact commit SHA before any content read: after resolve_commit returns, every recorded request URL (tree, license, blob) contains the 40-hex pinned SHA, no floating branch ref reappears, and a 64-hex SHA-256 repository is a recorded deterministic Scout rejection, never a fetch."
    - "Every filter decision record carries rule id, filter-policy-v1 version, observed value, pass|fail|not_applicable and a closed rationale; private, archived, fork, missing-default-branch and missing-README fixtures each fail their named rule, and every license boundary (null, NOASSERTION, non-listed, multiple root license files, endpoint 404, metadata/endpoint mismatch) is a deterministic rejection with the license rules never delegated to any LLM."
    - "A rejected Scout or Filter outcome makes every downstream stage return a deterministic skipped payload with zero adapter calls, and these business rejections are succeeded attempts with outcome payloads that consume no retry budget."
    - "build_phase_two_runtime validates one closed registry (phase2_processor, sqlite_and_manifests, github_read, clock, run_ids, extraction_summary_writer) under SideEffectPolicy.phase_two() admitting exactly {none, local_state, remote_read}; REMOTE_WRITE and caller-injected policies or registrations are rejected before any adapter invocation, and build_dry_run_runtime plus PHASE_ONE_MAX_SCOPES are untouched."
    - "GitHubReadClient is the only HTTP capability: fixed https://api.github.com base with templated closed endpoints, pinned X-GitHub-Api-Version 2022-11-28, serial requests, per-endpoint response byte caps, same-host-only recorded redirects, 429/403-remaining-0/5xx/timeout mapped to stage_transient_failure with a bounded Retry-After sleeper, and a canary token that appears only in the Authorization header of recorded requests and nowhere else."
  artifacts:
    - { path: src/skillscout/application/ports.py, provides: "runtime-only context/outcome carriers and the context-aware processor protocol", exports: [StageTelemetry, StageOutcome, StageContext, ContextStageProcessor] }
    - { path: src/skillscout/application/pipeline.py, provides: "profile-driven runner, phase-two policy ceiling and composition root, extraction summary durability", exports: [PipelineProfile, PIPELINE_PROFILES, PHASE_TWO_MAX_SCOPES, PhaseTwoRuntime, build_phase_two_runtime, PHASE_TWO_STAGE_SEQUENCE, MAX_EXTRACTION_SUMMARY_BYTES] }
    - { path: src/skillscout/adapters/state.py, provides: "additive attempt-telemetry update seam", exports: [record_attempt_telemetry] }
    - { path: src/skillscout/domain/models.py, provides: "bounded ExtractionSummary terminal artifact contract", exports: [ExtractionSummary, StageOutcomeEntry] }
    - { path: src/skillscout/adapters/github.py, provides: "closed read-only GitHub REST adapter", exports: [GitHubReadClient, RepoMetadata, TreeEntry, TreeSnapshot, LicenseResponse, RateLimitFacts, GITHUB_API_BASE, GITHUB_API_VERSION] }
    - { path: src/skillscout/application/processors.py, provides: "phase2-v1 stage processor with Scout/Filter dispatch and deterministic skip cascade", exports: [PhaseTwoProcessor, SCOUT_MAX_CANDIDATE_ENTRIES] }
    - { path: tests/recorded_transport.py, provides: "recorded-fixture MockTransport loader with request recording" }
    - { path: tests/test_github_adapter.py, provides: "MockTransport adapter evidence including SHA-in-URL invariant and error mapping" }
    - { path: tests/test_phase2_pipeline.py, provides: "profile slice, context, telemetry, COMPLETED terminal and composition-root evidence" }
    - { path: tests/test_scout_filter.py, provides: "Scout pinning/snapshot and filter rule-matrix evidence over recorded fixtures" }
  key_links:
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/domain/models.py, via: "additive (\"2\",\"phase2-v1\") registration resolves the closed profile", pattern: "PIPELINE_PROFILES" }
    - { from: src/skillscout/application/pipeline.py, to: src/skillscout/adapters/state.py, via: "telemetry lands on the running attempt before complete_stage so envelope and attempt agree under verify_run_chain", pattern: "record_attempt_telemetry" }
    - { from: src/skillscout/application/processors.py, to: src/skillscout/adapters/github.py, via: "every content URL embeds the pinned 40-hex SHA after resolve_commit", pattern: "resolve_commit" }
    - { from: tests/recorded_transport.py, to: tests/fixtures/github, via: "recorded (method, path) responses with call counts doubling as no-replay evidence", pattern: "httpx.MockTransport" }
  prohibitions:
    - "The phase-two runtime must never admit a REMOTE_WRITE registration, and neither build_dry_run_runtime nor PHASE_ONE_MAX_SCOPES may be widened to accommodate phase two."
    - "GitHub credentials must never appear in domain objects, stage payloads, manifests, SQLite rows, logs, stdout, request URLs or request bodies; the token exists only as an Authorization header read once from the environment at adapter construction."
    - "The adapter must never construct a request URL from response-supplied fields, follow a cross-host redirect, or derive repository identity from names; identity is the numeric GitHub repo id and every endpoint is a fixed template."
    - "A business rejection (filter failure, truncated tree, license 404, SHA-256 repository) must never be represented as an infrastructure failure that consumes the three-attempt retry budget."
---

<objective>
Generalize the Phase 1 runner for profile-driven stage slices, runtime-only context and real telemetry, then land the closed read-only GitHub adapter and the Scout/Filter stages of the `phase2-v1` processor behind a phase-two composition root that admits `REMOTE_READ` and nothing more.

Purpose: Turn pinning, deterministic filtering and the no-LLM-hard-gate guarantee into executable, recorded-fixture-backed code while the Phase 1 spine and tests stay byte-for-byte intact.
Output: `PipelineProfile` machinery with telemetry plumbing, `GitHubReadClient` with recorded fixtures, `PhaseTwoProcessor` Scout/Filter dispatch with skip cascade, `build_phase_two_runtime`, and three focused green test modules.
</objective>

## Phase Goal

**As a** SkillScout reviewer, **I want to** point the pipeline at one public GitHub repository and receive at most three evidenced `WorkflowSpec`s or a clear filtered/no-workflow conclusion, **so that** real extraction is proven safe — pinned, filtered, budgeted, never executed — before generation, review and publishing exist.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md
@.planning/phases/02-safe-single-repository-extraction/02-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md
@.planning/phases/02-safe-single-repository-extraction/02-01-SUMMARY.md
@src/skillscout/application/pipeline.py
@src/skillscout/application/ports.py
@src/skillscout/adapters/state.py
@src/skillscout/domain/models.py
@src/skillscout/domain/filtering.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-02-01: Profile-driven runner, telemetry seam and COMPLETED terminal</name>
  <files>src/skillscout/application/ports.py, src/skillscout/application/pipeline.py, src/skillscout/adapters/state.py, src/skillscout/domain/models.py, tests/test_phase2_pipeline.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, src/skillscout/adapters/state.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/domain/enums.py, tests/test_pipeline_resume.py, tests/test_side_effect_policy.py, .planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md</read_first>
  <behavior>
    - fixture-v1 runs byte-for-byte as before: nine stages, one-argument processor call, None telemetry, publication plan, PLANNED_NOT_PUBLISHED; every Phase 1 test stays green untouched.
    - phase2-v1 runs exactly (SCOUT, FILTER, READER, EXTRACTOR) with global stage indices 0..3, receives a runtime-only context (subject, prior payloads hydrated across resume, per-invocation scratch), and terminates COMPLETED with a durable extraction-summary.json.
    - Processor-returned telemetry is recorded on the running attempt before completion and mixed into the envelope and stage_output_hash; changing telemetry changes the output hash.
  </behavior>
  <action>Write the RED mechanics tests first in `tests/test_phase2_pipeline.py` using a test-local double `DoublePhaseTwoProcessor` (producer_version `"phase2-v1"`, `process(stage_input, context) -> StageOutcome` with canned payloads plus a `StageTelemetry(prompt_version="extract-prompt-v1", policy_version=None, model_id="test-model", request_id="req-double", latency_ms=7, token_usage=TokenUsage(3, 5, 8))`): four attempts with stage_index 0..3 in spine order; context carries the subject and each prior stage payload; after resume at READER the prior payloads hydrate from the verified chain and no succeeded stage re-runs (double call counts); attempt row and envelope carry the telemetry and `verify_run_chain` passes; terminal status is COMPLETED, `extraction-summary.json` exists with run identity, per-stage outcomes, extractor outcome, workflow count and fingerprints; telemetry variation changes `stage_output_hash`.

In `application/ports.py` add frozen carriers: `StageTelemetry` (prompt_version, policy_version, model_id, request_id, latency_ms, token_usage — all optional), `StageOutcome` (payload: Mapping, telemetry: StageTelemetry | None), `StageContext` (runtime-only: subject, prior_payloads dict, scratch dict; explicitly not a persisted model and never canonicalized) and `ContextStageProcessor(Protocol)` with `producer_version` and `process(stage_input, context) -> StageOutcome`. Do not change the existing `StageProcessor` protocol or `FixtureProcessor` — profile dispatch, not a signature bump, keeps the ~20 Phase 1 override subclasses working.

In `application/pipeline.py` add `PipelineProfile` (stages tuple, uses_context bool, terminal_status) and `PIPELINE_PROFILES = {"fixture-v1": (tuple(PipelineStage), False, PLANNED_NOT_PUBLISHED), "phase2-v1": ((SCOUT, FILTER, READER, EXTRACTOR), True, COMPLETED)}` with an import-time guard that each profile's stages equal the spine prefix of the same length; `PHASE_TWO_STAGE_SEQUENCE = tuple(stage.value for stage in (SCOUT, FILTER, READER, EXTRACTOR))`; `PHASE_TWO_MAX_SCOPES = frozenset({EffectScope.NONE, EffectScope.LOCAL_STATE, EffectScope.REMOTE_READ})`; `SideEffectPolicy.phase_two()`; `PhaseTwoRuntime` dataclass mirroring `DryRunRuntime`; `MAX_EXTRACTION_SUMMARY_BYTES = 65_536`. In `PipelineRunner.run`, widen the `subject` parameter annotation to `FixtureSubject | RepositorySubject` (importing `RepositorySubject` from `skillscout.domain.subjects`; the body consumes only `subject.model_dump` and `subject.subject_id`, so runtime behavior is unchanged), resolve the profile from `producer_version` after the existing producer gate, iterate `enumerate(profile.stages)` (global indices via the prefix guard), build one context per invocation when `uses_context` (hydrating `prior_payloads` from `self.state.verify_run_chain(run_id).results` when resuming with a nonempty verified prefix, and appending each completed stage payload), dispatch `self.processor.process(stage_input, context)` for context profiles and wrap legacy `self.processor.process(stage_input)` results in `StageOutcome(payload=..., telemetry=None)` otherwise, validate the outcome shape (`StageOutcome` with Mapping payload and optional `StageTelemetry`, else `STAGE_OUTPUT_INVALID`), and copy telemetry into `stage_output_hash` and the provisional envelope in place of the three hardcoded `None` triples. In `domain/models.py` add `StageOutcomeEntry` (stage, outcome) and `ExtractionSummary(StrictFrozenModel)` (run_id, subject_id, repository, pinned_commit_sha: str | None, stage_outcomes tuple, extractor_outcome, workflow_count, workflow_fingerprints tuple ≤ 3, remote_writes_attempted = 0, `as_dict()`).

In `adapters/state.py` add `record_attempt_telemetry(attempt_id, telemetry)` following the existing `_snapshot_transaction` pattern: one `UPDATE stage_attempts SET prompt_version=?, policy_version=?, model_id=?, request_id=?, latency_ms=?, prompt_tokens=?, completion_tokens=?, total_tokens=? WHERE attempt_id=? AND status='running'` requiring rowcount 1, mapping failures to `STATE_OPERATION_FAILED`; add the method to the `StateStore` protocol additively. The runner calls it after a successful `process` when telemetry is not None, before `complete_stage`.

Terminal: keep `_write_publication_plan`'s exact Phase 1 behavior (target name, `publication_` seam prefix, `after_publication_durable` marker, lock sidecar, byte cap) while extracting a shared private durable-artifact core both paths call; the phase-two terminal resolves the verified chain, builds `ExtractionSummary` from its envelopes, writes `extraction-summary.json` through `_ExtractionSummaryWriter` (LOCAL_STATE, cap `MAX_EXTRACTION_SUMMARY_BYTES`, seam prefix `extraction_`, marker `after_extraction_durable`) and sets run status COMPLETED; `RunSummary.publication_plan_path` carries the actual artifact name. No retry, ledger, migration or Phase 1 builder change.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_pipeline.py tests/test_pipeline_resume.py tests/test_side_effect_policy.py tests/test_state_integrity.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_phase2_pipeline.py</automated></verify>
  <acceptance_criteria>
    - The double-processor run completes exactly four stages at global indices 0..3 with COMPLETED status, and `extraction-summary.json` parses as `ExtractionSummary` with four per-stage outcomes and bounded fingerprints.
    - Telemetry appears identically on the attempt row and envelope, `verify_run_chain` accepts the run, and a telemetry change alters the stage output hash.
    - Resume after an injected reader-stage interruption re-invokes only the extractor in the double and hydrates prior payloads from the verified chain.
    - The `run()` signature in `src/skillscout/application/pipeline.py` annotates `subject: FixtureSubject | RepositorySubject`, so the type contract on the modified file covers the phase-two call path.
    - `git diff --stat` shows zero changes under `src/skillscout/adapters/fixtures.py` and zero changes in Phase 1 test files; the full focused set above passes.
  </acceptance_criteria>
  <done>The runner serves both producers from one closed profile mechanism with real telemetry, and the phase-two terminal artifact is durable and verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02-02: Closed read-only GitHub adapter over recorded MockTransport fixtures</name>
  <files>src/skillscout/adapters/github.py, tests/recorded_transport.py, tests/fixtures/github/repo_mit.json, tests/fixtures/github/repo_archived.json, tests/fixtures/github/repo_fork.json, tests/fixtures/github/repo_private.json, tests/fixtures/github/commits_pin.json, tests/fixtures/github/commits_pin_sha256.json, tests/fixtures/github/tree_full.json, tests/fixtures/github/tree_no_readme.json, tests/fixtures/github/tree_license_multiple.json, tests/fixtures/github/tree_truncated.json, tests/fixtures/github/license_mit.json, tests/fixtures/github/license_noassertion.json, tests/fixtures/github/license_mismatch.json, tests/fixtures/github/license_404.json, tests/fixtures/github/rate_limit_429.json, tests/fixtures/github/redirect_301.json, tests/fixtures/github/blob_readme.json, tests/test_github_adapter.py</files>
  <read_first>src/skillscout/application/ports.py, src/skillscout/adapters/fixtures.py, src/skillscout/domain/enums.py, tests/conftest.py, tests/test_side_effect_policy.py, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md, .planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md</read_first>
  <behavior>
    - Every request targets a fixed templated https://api.github.com endpoint with pinned API version headers; after pinning, tree/license/blob URLs embed the 40-hex SHA.
    - 429, 403 with x-ratelimit-remaining: 0, 5xx and transport timeouts map to stage_transient_failure with a bounded recorded Retry-After sleep; 404 on metadata/pin maps to stage_permanent_failure; license 404 is business data, not a failure.
    - A canary token appears only in the Authorization header of recorded requests; raw httpx exceptions never cross the adapter boundary.
  </behavior>
  <action>Author `tests/recorded_transport.py`: a loader mapping fixture JSON files (status, headers, body) to an `httpx.MockTransport` handler that records every request and asserts no body bytes leave the recorded set. Author the `tests/fixtures/github/` set per 02-RESEARCH.md §Required fixtures: `repo_mit.json` (200 metadata: id 840001, owner example, name approved-repo, default_branch main, private/fork/archived/disabled false, visibility public, license.spdx_id MIT, x-ratelimit headers), the archived/fork/private variants, `commits_pin.json` (sha `0123456789abcdef0123456789abcdef01234567`), `commits_pin_sha256.json` (64-hex sha), `tree_full.json` (recursive entries: README.md, docs/guide.md, docs/external as mode 160000 type commit submodule, docs/link.md as mode 120000 symlink, docs/big.md size 200000, examples/basic.md, examples/data.bin, pyproject.toml, src/core.py, lib/helper.py, script.py, LICENSE — each with mode/type/size/sha), `tree_no_readme.json`, `tree_license_multiple.json` (LICENSE plus COPYING), `tree_truncated.json` (truncated true), `license_mit.json`, `license_noassertion.json` (spdx_id NOASSERTION), `license_mismatch.json` (spdx_id Apache-2.0 against MIT metadata), `license_404.json`, `rate_limit_429.json` (429 with Retry-After 1 and x-ratelimit-remaining 0), `redirect_301.json` (301 Location `https://api.github.com/repos/example/renamed-repo`), `blob_readme.json` (base64 README text with encoding/size/sha).

Implement `src/skillscout/adapters/github.py`: module constants `GITHUB_API_BASE = "https://api.github.com"`, `GITHUB_API_VERSION = "2022-11-28"`, `MAX_METADATA_BYTES = 1_048_576`, `MAX_TREE_BYTES = 8_388_608`, `MAX_LICENSE_BYTES = 1_048_576`, `MAX_BLOB_BYTES = 262_144`, `MAX_RETRY_AFTER_SECONDS = 60`; frozen response models `RateLimitFacts` (limit/remaining/reset, all optional), `RepoMetadata` (id, owner, name, default_branch: str | None, private, fork, archived, disabled, visibility, license_spdx: str | None, rate_limit), `TreeEntry` (path, mode, type, size: int | None, sha), `TreeSnapshot` (entries tuple, truncated), `LicenseResponse` (status Literal `confirmed|not_found|noassertion`, spdx_id: str | None, license_blob_sha: str | None), `RedirectFacts` (from_url, to_url). `GitHubReadClient(*, token=None, transport=None, sleeper=time.sleep, timeout=10.0)` reads `SKILLSCOUT_GITHUB_TOKEN` once when token is None, builds one serial `httpx.Client` (fixed base URL, `follow_redirects=False`, headers Accept `application/vnd.github+json`, X-GitHub-Api-Version pinned, User-Agent `skillscout/0.1.0`, Authorization Bearer only when a token exists), declares `effect_scope = EffectScope.REMOTE_READ`, and exposes `get_repo_metadata`, `resolve_commit`, `get_tree`, `get_license`, `get_blob` (base64-decoded bytes with the tree-declared size re-checked against the fetched payload) plus `close()`. Error mapping is total and `raise ... from None`: timeout/network → `STAGE_TRANSIENT_FAILURE`; 429, 403 with remaining 0, 5xx → bounded `sleeper(min(retry_after, 60))` then `STAGE_TRANSIENT_FAILURE`; 404 on metadata/pin → `STAGE_PERMANENT_FAILURE`; license 404 → `LicenseResponse(status="not_found")`; NOASSERTION spdx → `status="noassertion"`; 301/307/308 → follow one same-host redirect and record `RedirectFacts`, cross-host → `STAGE_PERMANENT_FAILURE`; over-cap or malformed bodies → `STAGE_PERMANENT_FAILURE`. Tests in `tests/test_github_adapter.py` assert every mapping, the closed URL set, the SHA-in-URL invariant after pin, redirect recording, sleeper call with the bounded value, and canary-token confinement to the Authorization header.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_github_adapter.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/adapters/github.py tests/recorded_transport.py tests/test_github_adapter.py</automated></verify>
  <acceptance_criteria>
    - All error/redirect/rate-limit mappings pass against MockTransport with zero real network (socket sentinel unused by design; only MockTransport clients are constructed).
    - Recorded request URLs match the closed template set, and after `resolve_commit` every tree/license/blob URL contains `0123456789abcdef0123456789abcdef01234567`.
    - The canary token string appears in no recorded request URL or body, no fixture file, and only in the Authorization header.
    - Response-cap, malformed-body and cross-host-redirect cases fail closed with the documented codes; the socket-sentinel suites from Phase 1 remain green.
  </acceptance_criteria>
  <done>GitHub read authority exists only inside one closed, recorded-fixture-proven adapter with total error mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02-03: Scout/Filter processors with skip cascade and the phase-two composition root</name>
  <files>src/skillscout/application/processors.py, src/skillscout/application/pipeline.py, tests/test_scout_filter.py, tests/test_phase2_pipeline.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/ports.py, src/skillscout/application/processors.py, src/skillscout/adapters/github.py, src/skillscout/domain/filtering.py, src/skillscout/domain/subjects.py, src/skillscout/adapters/fixtures.py, tests/test_side_effect_policy.py, tests/recorded_transport.py, tests/fixtures/github/tree_full.json</read_first>
  <behavior>
    - Scout emits a bounded snapshot: repo facts, requested ref, pinned 40-hex SHA, redirect facts, rate-limit facts, tree entry count and a capped candidate projection; truncated trees, 64-hex SHAs and unresolvable refs are recorded Scout rejections.
    - Filter evaluates filter-policy-v1 over the snapshot with at most one Filter-owned license GET and returns accepted/rejected with the full decision list; a rejected upstream outcome yields deterministic skipped payloads downstream with zero adapter calls.
    - build_phase_two_runtime admits exactly {none, local_state, remote_read}, rejects remote_write before any invocation, and rejects caller-supplied policies, extra registrations or wrong concrete types.
  </behavior>
  <action>Create `src/skillscout/application/processors.py` with `PhaseTwoProcessor` (`producer_version = "phase2-v1"`, `effect_scope = EffectScope.REMOTE_READ`, constructed with the `GitHubReadClient`; OpenAI injection arrives in Plan 02-04) and `SCOUT_MAX_CANDIDATE_ENTRIES = 512`. `process(stage_input, context)` guards first: SCOUT dispatches to `_scout`; FILTER/READER/EXTRACTOR return a deterministic `{"outcome": "skipped", "skip_reason": "scout_rejected"}` payload when the Scout payload outcome is not accepted; READER/EXTRACTOR likewise skip with `"filter_rejected"`; READER and EXTRACTOR handlers arrive in Plans 02-03/02-04 and any unhandled stage falls through to a fail-closed `SafeFailure(STAGE_PERMANENT_FAILURE)` guard.

`_scout`: parse owner/repo from the context subject URL; `get_repo_metadata`; ref = subject.ref or metadata.default_branch (missing both → Scout outcome `rejected` reason `no_ref_resolvable` with repo facts still recorded); `resolve_commit` and require `^[0-9a-f]{40}$` (64-hex → rejection `sha256_repository_unsupported`); `get_tree` (truncated → rejection `repository_too_large`); project candidates (paths matching the Plan 02-01 tier predicate or root `LICENSE*`/`COPYING*`/`README*`, plus mode 160000/120000 entries under candidate roots, path-sorted; projection larger than `SCOUT_MAX_CANDIDATE_ENTRIES` → rejection `repository_too_large`); payload carries schema facts, outcome, repo facts, ref_requested, pinned_commit_sha, redirect, rate_limit, tree entry_count/truncated/candidates; telemetry request_id from the last `X-GitHub-Request-Id` response header and measured latency.

`_filter`: build `RepoFacts` from the Scout payload, `TreeFacts` from the candidates (case-insensitive root `README*`; root `LICENSE*`/`COPYING*` list); call `get_license` at the pinned SHA only when the allowlist and single-file rules pass, mapping to `LicenseConfirmation` (endpoint spdx vs metadata spdx mismatch → `mismatch`); `evaluate_filter` produces the verdict; payload carries `policy_version: filter-policy-v1`, the full decision list, outcome, license_spdx when accepted, and telemetry with policy_version and request_id.

Extend `build_phase_two_runtime(state, processor)` in `pipeline.py` beside the untouched Phase 1 builder: closed registry `phase2_processor`, `sqlite_and_manifests`, `github_read` (the exact client held by the processor), `clock`, `run_ids`, `extraction_summary_writer`; `SideEffectPolicy.phase_two()` validation; concrete `type(...) is` checks against `(PhaseTwoProcessor, SQLiteStateStore, GitHubReadClient, SystemClock, UUIDIdProvider, _ExtractionSummaryWriter)`; keyword-only signature with no policy/registration parameters (caller injection raises TypeError exactly as Phase 1 asserts for its builder). Extend `tests/test_phase2_pipeline.py` with the root-policy cases (REMOTE_WRITE registration rejected before invocation canary, wrong-type rejection, phase_one ceiling rejecting a phase-two processor) and write `tests/test_scout_filter.py` over the recorded fixtures: happy-path Scout payload shape and pin, each Scout rejection, each FILT-01 variant failing its named rule with observed values, every FILT-02 boundary, decision-record completeness (rule id/version/observed/result/rationale), the license GET occurring only when earlier license rules pass, and skip cascades with recorded call counts proving zero downstream adapter calls after any rejection.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_scout_filter.py tests/test_phase2_pipeline.py tests/test_github_adapter.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_scout_filter.py tests/test_phase2_pipeline.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - Every FILT-01 variant fixture fails its named rule with the observed value recorded; every FILT-02 boundary fixture is a deterministic rejection; the happy-path verdict is accepted with eight decision records.
    - Skip cascades emit skipped outcomes with recorded transport counts proving zero adapter calls after a Scout or Filter rejection, and rejected runs consume zero retry budget (attempt count 1 per stage).
    - The phase-two root admits the six-registration closed set, rejects REMOTE_WRITE/caller policies/wrong types before invocation, and the Phase 1 builder tests are untouched and green.
    - The full suite passes under the canonical prefix; no Phase 1 file changed.
  </acceptance_criteria>
  <done>Pinning and deterministic filtering execute over recorded fixtures, hard gates structurally precede any LLM path, and the phase-two runtime ceiling admits remote reads only.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub REST → Scout snapshot | All repository bytes and metadata are adversary-controlled until validated into bounded frozen contracts. |
| Adapter → credential handling | The GitHub token must exist only as an Authorization header, never in domain or durable surfaces. |
| Runner profile → authority ceiling | The phase-two slice gains REMOTE_READ; the mechanism must not leak that authority into Phase 1 or toward REMOTE_WRITE. |
| HTTP errors → retry budget | Naive rate-limit handling can burn the three-attempt budget or punish the integration; business facts must not become failures. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02-CR | Information Disclosure | GitHub credential handling | high | mitigate | Env-read once at construction, header-only confinement, canary-token assertions over recorded requests, fixtures and durable surfaces (Tasks 02-02-02/03). |
| T-02-02-SS | Tampering / SSRF | request URL construction and redirects | high | mitigate | Fixed base URL, closed templated endpoints, no response-derived URLs, same-host-only recorded redirects, numeric repo id identity (Task 02-02-02). |
| T-02-02-EOP | Elevation of Privilege | phase-two registry and policy ceiling | critical | mitigate | Closed six-registration registry, adapter-owned scopes, `SideEffectPolicy.phase_two()` admitting at most REMOTE_READ, concrete-type checks, caller-injection TypeError, Phase 1 ceiling untouched (Task 02-02-03). |
| T-02-02-DOS | Denial of Service | rate limits and oversized responses | medium | mitigate | Serial client, per-endpoint byte caps, bounded Retry-After sleeper, transient mapping scoped to 429/limited-403/5xx/timeout (Task 02-02-02). |
| T-02-02-ID | Tampering | stage identity and telemetry | medium | mitigate | Spine-prefix profile guard, global indices, telemetry written to the running attempt before completion and mixed into schema-"2" output identity, verify_run_chain over the completed run (Task 02-02-01). |

</threat_model>

## Artifacts this phase produces

- Source files: `src/skillscout/application/processors.py` (`PhaseTwoProcessor`, `SCOUT_MAX_CANDIDATE_ENTRIES`), `src/skillscout/adapters/github.py` (`GitHubReadClient`, `RepoMetadata`, `TreeEntry`, `TreeSnapshot`, `LicenseResponse`, `RateLimitFacts`, `RedirectFacts`, `GITHUB_API_BASE`, `GITHUB_API_VERSION`, byte-cap and `MAX_RETRY_AFTER_SECONDS` constants); additive edits in `src/skillscout/application/ports.py` (`StageTelemetry`, `StageOutcome`, `StageContext`, `ContextStageProcessor`, `StateStore.record_attempt_telemetry`), `src/skillscout/application/pipeline.py` (`PipelineProfile`, `PIPELINE_PROFILES`, `PHASE_TWO_STAGE_SEQUENCE`, `PHASE_TWO_MAX_SCOPES`, `SideEffectPolicy.phase_two`, `PhaseTwoRuntime`, `build_phase_two_runtime`, `MAX_EXTRACTION_SUMMARY_BYTES`, `_ExtractionSummaryWriter`), `src/skillscout/adapters/state.py` (`record_attempt_telemetry`), `src/skillscout/domain/models.py` (`StageOutcomeEntry`, `ExtractionSummary`).
- Test files: `tests/recorded_transport.py`, `tests/test_github_adapter.py`, `tests/test_phase2_pipeline.py`, `tests/test_scout_filter.py` and the `tests/fixtures/github/*.json` recorded set (metadata variants, pin variants, tree variants, license variants, 429, 301, one blob).
- Behaviors: pinned-SHA URL invariant, total HTTP error mapping, bounded Retry-After, skip cascade, telemetry plumbing, COMPLETED terminal with durable `extraction-summary.json`.
- Non-artifacts: no Reader/Extractor stage handlers, no OpenAI adapter, no CLI surface yet; unhandled stages fail closed.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Pinned, filtered single-repository path with hard gates before any LLM | 02-02 | COVERED | Scout pin + filter rules + skip cascade over recorded fixtures. |
| REQ | FILT-01 | private/archived/fork/no-default-branch/no-README rejections | 02-02 | COVERED | Per-rule variant fixtures with observed values. |
| REQ | FILT-02 | license allowlist boundary | 02-02 | COVERED | null/NOASSERTION/non-listed/multiple/404/mismatch all reject. |
| REQ | FILT-03 | versioned decision records; hard gates never reach the LLM | 02-02 | COVERED | Decision payload contract + skip cascade; the LLM-call-count-0 proof completes in 02-04 when the OpenAI adapter exists. |
| REQ | READ-01 | pin before read; no floating refs | 02-02 | COVERED | SHA-in-URL recorded-transport invariant; SHA-256 repo rejection. |
| RESEARCH | — | Profile slice with global indices; runtime-only context; telemetry population | 02-02 | COVERED | Delivered via profile dispatch instead of a protocol signature bump — see plan deviation note in the summary. |
| RESEARCH | — | Phase-two composition root admitting REMOTE_READ only | 02-02 | COVERED | Closed registry + phase_two ceiling; Phase 1 builder untouched. |
| PROBE | rows | FILT-01..03 and READ-01 edge predicates authored into must_haves truths | 02-02 | COVERED | Remaining probe rows stay assigned to 02-03/02-04. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No CONTEXT.md decisions exist. |

<verification>
- `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_github_adapter.py tests/test_scout_filter.py tests/test_phase2_pipeline.py` passes, followed by `... run --locked ruff check src/skillscout tests` and the full `... run --locked pytest -q`.
- Recorded-transport assertions prove the SHA-in-URL invariant, the closed endpoint set, canary-token confinement and zero downstream calls after rejections.
- The phase-two root rejects REMOTE_WRITE, caller policies and wrong concrete types before any adapter invocation; `build_dry_run_runtime` and every Phase 1 test are byte-for-byte unchanged and green.
- No credential, raw exception, or response-derived URL crosses any boundary; no network access occurs outside MockTransport.
</verification>

<success_criteria>
- The runner executes the four-stage phase2-v1 slice with global indices, runtime-only context, hydrated resume and real telemetry while every Phase 1 behavior stays identical.
- Scout pins the commit and snapshots the tree; Filter emits complete versioned decision records with every license ambiguity deterministically rejected.
- GitHub read authority exists only as one closed adapter behind a REMOTE_READ-capped composition root proven by recorded fixtures.
</success_criteria>

<output>After completion, create `.planning/phases/02-safe-single-repository-extraction/02-02-SUMMARY.md` with the profile/telemetry design, the recorded-fixture inventory, the root-policy evidence and the deviation note on profile dispatch versus a protocol signature bump.</output>
