---
phase: 02-safe-single-repository-extraction
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/skillscout/application/processors.py
  - tests/recorded_transport.py
  - tests/fixtures/github/blob_doc.json
  - tests/fixtures/github/blob_example.json
  - tests/fixtures/github/blob_pyproject.json
  - tests/fixtures/github/blob_source.json
  - tests/fixtures/github/blob_lfs.json
  - tests/fixtures/github/blob_binary.json
  - tests/test_reader.py
autonomous: true
requirements: [READ-02, READ-03, READ-04, READ-05, READ-06]
must_haves:
  truths:
    - "Reader reads in exactly the fixed tier order README -> docs/ -> examples/ -> package manifests -> source with path-sorted determinism inside each tier, and the persisted read record's read_order sequence proves it."
    - "The five reader-policy-v1 budgets hold at ±1 boundaries: the 25th file is read and the 26th stops with BUDGET_EXHAUSTED; the 5th source file is read and the 6th is not; a 131072-byte file is read and a 131073-byte file is skipped OVER_BUDGET_SIZE and never fetched; fetching stops before total bytes would exceed 524288 and before ceil(bytes/4) estimated tokens would exceed 40000; budgets derive only from the validated ReaderPolicy defaults and no per-run operator input can raise them above the organization ceilings."
    - "Early stop fires only after the examples tier when estimated tokens reach the 24000 soft target, and the read record carries ordered files[] (path, tier, blob_sha, size, sha256 content_hash, read_order), rejections[] (path, rule, observed), budget consumption, source_code_loaded and a stop_reason from the closed set {soft_target_reached, budget_exhausted, candidates_exhausted, no_allowlisted_files}."
    - "The full READ-05 rejection matrix is enforced with recorded-transport proof that rejected content is never fetched when metadata suffices: submodule mode 160000, symlink mode 120000, path violations ('..', empty segments, backslash, NUL/control, over 512 chars, depth over 8), non-allowlisted extensions including archives, and over-size files are skip-recorded without a blob GET, while binary (UTF-8 decode failure or NUL sniff) and LFS pointer content are rejected immediately after fetch."
    - "No candidate code is ever clone, installed, built, imported or executed: the amended Phase 1 capability sweep keeps subprocess/importlib/socket/urllib/requests/aiohttp/github/http/eval/exec/os.popen/os.system banned in every source module (only adapters/github.py may import httpx and adapters/openai_extract.py may import openai), and the only runtime I/O is recorded MockTransport HTTP."
    - "Resume hydration is byte-verified: re-fetching the recorded read plan through hydrate_read_bundle reproduces the in-memory bundle exactly, every re-fetched blob's sha256 content hash must equal the recorded hash or the run fails closed with stage_permanent_failure, and the raw bundle text appears in no stage payload, manifest, SQLite row or log surface."
  artifacts:
    - { path: src/skillscout/application/processors.py, provides: "_reader stage handler with tiered budgeted read loop, rejection matrix and bundle scratch handoff plus hash-verified hydration", exports: [hydrate_read_bundle] }
    - { path: tests/recorded_transport.py, provides: "programmatic make_tree_fixture/make_blob_fixture builders for budget matrices beside the reviewed JSON recordings" }
    - { path: tests/test_reader.py, provides: "tier-order, ±1 budget, early-stop, rejection-matrix, hydration and no-persistence evidence" }
    - { path: tests/fixtures/github/blob_doc.json, provides: "recorded docs-tier blob" }
    - { path: tests/fixtures/github/blob_example.json, provides: "recorded examples-tier blob" }
    - { path: tests/fixtures/github/blob_pyproject.json, provides: "recorded manifest-tier blob" }
    - { path: tests/fixtures/github/blob_source.json, provides: "recorded source-tier blob" }
    - { path: tests/fixtures/github/blob_lfs.json, provides: "recorded LFS-pointer rejection blob" }
    - { path: tests/fixtures/github/blob_binary.json, provides: "recorded binary rejection blob" }
  key_links:
    - { from: src/skillscout/application/processors.py, to: src/skillscout/domain/reading.py, via: "reader-policy-v1 defaults validated against organization ceilings before any fetch", pattern: "ReaderPolicy" }
    - { from: src/skillscout/application/processors.py, to: src/skillscout/adapters/github.py, via: "tree-declared size gates every blob GET before bytes move", pattern: "get_blob" }
    - { from: tests/test_reader.py, to: tests/recorded_transport.py, via: "recorded request lists prove rejected content is never fetched", pattern: "MockTransport" }
  prohibitions:
    - "The raw read bundle must never be persisted, logged or written to any durable, debug or diagnostic surface; only bounded metadata, blob SHAs, content hashes and policy-bounded excerpts may leave process memory."
    - "Reading must never silently truncate and claim completeness; every stop carries an explicit closed stop_reason and every skipped candidate a recorded rejection rule with its observed value."
    - "Fetched content must never be executed, imported, compiled, or interpreted as instructions — including files that are themselves scripts, package manifests or CI configuration; repository text is inert data everywhere."
---

<objective>
Implement the budgeted tiered Reader as the third `phase2-v1` stage: deterministic candidate selection from the Scout snapshot, size-before-fetch blob reads under the five `reader-policy-v1` budgets, early stop, the complete READ-05 rejection matrix, a fully structured read record, and a runtime-only raw bundle handoff with hash-verified resume hydration.

Purpose: Prove bounded, execution-free reading with complete audit evidence before any LLM call exists.
Output: `_reader` handler and `hydrate_read_bundle` in `processors.py`, recorded blob fixtures plus programmatic builders, and a green `tests/test_reader.py` covering order, budgets, rejections and non-persistence.
</objective>

## Phase Goal

**As a** SkillScout reviewer, **I want to** point the pipeline at one public GitHub repository and receive at most three evidenced `WorkflowSpec`s or a clear filtered/no-workflow conclusion, **so that** real extraction is proven safe — pinned, filtered, budgeted, never executed — before generation, review and publishing exist.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md
@.planning/phases/02-safe-single-repository-extraction/02-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md
@.planning/phases/02-safe-single-repository-extraction/02-02-SUMMARY.md
@src/skillscout/application/processors.py
@src/skillscout/domain/reading.py
@src/skillscout/adapters/github.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-03-01: Tiered budgeted read loop with early stop and the structured read record</name>
  <files>src/skillscout/application/processors.py, tests/recorded_transport.py, tests/fixtures/github/blob_doc.json, tests/fixtures/github/blob_example.json, tests/fixtures/github/blob_pyproject.json, tests/fixtures/github/blob_source.json, tests/test_reader.py</files>
  <read_first>src/skillscout/application/processors.py, src/skillscout/domain/reading.py, src/skillscout/domain/filtering.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/github.py, tests/recorded_transport.py, tests/fixtures/github/tree_full.json, tests/fixtures/github/blob_readme.json, tests/test_scout_filter.py, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md</read_first>
  <behavior>
    - The read plan is deterministic: fixed tier order, path-sorted within tier, built only from the Scout candidate projection.
    - Budgets are enforced before bytes move: per-file size from tree metadata gates the fetch; file/source counts, total bytes and estimated tokens gate loop continuation.
    - The read record is complete and bounded; the raw text lives only in the runtime context scratch.
  </behavior>
  <action>Write the RED order/budget/record tests in `tests/test_reader.py` first, using programmatic `make_tree_fixture(entries)` and `make_blob_fixture(content)` builders added to `tests/recorded_transport.py` (the reviewed JSON fixtures stay the canonical set; builders synthesize budget matrices deterministically). Author the four recorded tier blobs (`blob_doc.json`, `blob_example.json`, `blob_pyproject.json`, `blob_source.json`) consistent with `tree_full.json`.

Implement the `_reader` handler in `src/skillscout/application/processors.py`: read the Scout payload candidates from `context.prior_payloads`; construct the default `ReaderPolicy()` (version `reader-policy-v1`, validated against the organization ceilings — never per-run input); classify every candidate through the Plan 02-01 predicates in this exact precedence — `validate_repo_path` failure → `PATH_VIOLATION`; mode 160000 → `SUBMODULE`; mode 120000 → `SYMLINK`; `assign_tier` None or non-allowlisted extension (archives included) → `NON_ALLOWLISTED_EXTENSION`; tree-declared size over `max_file_bytes` → `OVER_BUDGET_SIZE` — each recorded as `{path, rule, observed}` without any fetch. Sort survivors by (tier order, path). Iterate: stop with `BUDGET_EXHAUSTED` before a fetch when files_read == `max_files`, when the candidate is source-tier and source_files_read == `max_source_files`, when total_bytes + candidate size > `max_total_bytes`, or when `estimate_tokens(total_bytes + candidate size)` > `max_estimated_input_tokens`; otherwise `get_blob`, re-check fetched byte length against the tree-declared size, decode UTF-8 (failure → `BINARY_CONTENT`), NUL-sniff the first 8192 bytes (`\x00` → `BINARY_CONTENT`), prefix-check `LFS_POINTER_PREFIX` (→ `LFS_POINTER`), then record `{path, tier, blob_sha, size, content_hash: sha256_digest(raw_bytes), read_order}` and store the decoded text in `context.scratch["read_bundle"][path]`. After each successful read at tier EXAMPLES or later, stop with `SOFT_TARGET_REACHED` when estimated tokens reach `early_stop_soft_tokens`; when the loop drains, use `CANDIDATES_EXHAUSTED`, or `NO_ALLOWLISTED_FILES` when zero files were read and every candidate was rejected or absent. The payload carries schema facts, `outcome: "accepted"`, policy_version, ordered files, rejections, `budgets: {files_read, source_files_read, total_bytes, estimated_input_tokens}`, `source_code_loaded: bool`, `stop_reason`; telemetry carries policy_version, the last `X-GitHub-Request-Id` as request_id and measured latency. Tests assert: exact read_order across all five tiers with in-tier path sorting; ±1 boundaries on all five budgets including the never-fetched 131073-byte file; early stop only after the examples tier; read-record field completeness and closed stop_reason set; the payload and its manifest contain no full-text canary sentence from the README fixture (only hashes/metadata), and `MAX_STAGE_STRING_BYTES`/`MAX_MANIFEST_BYTES` bounds hold.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_reader.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/application/processors.py tests/recorded_transport.py tests/test_reader.py</automated></verify>
  <acceptance_criteria>
    - read_order is exactly README, docs/, examples/, manifests, source with path-sorted entries inside each tier across mixed synthetic trees.
    - All five ±1 budget cases pass, and recorded transport proves the 131073-byte and count-overflow candidates were never fetched.
    - Early stop produces SOFT_TARGET_REACHED only after the examples tier; draining produces CANDIDATES_EXHAUSTED; an all-rejected candidate set produces NO_ALLOWLISTED_FILES with zero blob GETs.
    - The read record carries every required field; the full-text canary appears in no payload, manifest or SQLite row; the raw bundle exists only in scratch.
  </acceptance_criteria>
  <done>Budgeted tiered reading with complete structured evidence runs deterministically over recorded fixtures with the raw text confined to process memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03-02: Full rejection matrix, hash-verified resume hydration and no-execution proof</name>
  <files>src/skillscout/application/processors.py, tests/fixtures/github/blob_lfs.json, tests/fixtures/github/blob_binary.json, tests/test_reader.py</files>
  <read_first>src/skillscout/application/processors.py, src/skillscout/domain/reading.py, src/skillscout/domain/canonical.py, src/skillscout/adapters/github.py, tests/test_reader.py, tests/recorded_transport.py, tests/test_phase1_gap_closure.py, tests/fixtures/github/tree_full.json</read_first>
  <behavior>
    - Every READ-05 rejection class is recorded with its rule and observed value; metadata-sufficient rejections never issue a blob GET.
    - hydrate_read_bundle rebuilds the exact in-memory bundle from the persisted read plan and fails closed on any content-hash mismatch.
    - No source path executes, imports or interprets candidate content.
  </behavior>
  <action>Author `blob_lfs.json` (blob whose decoded text starts with `version https://git-lfs.github.com/spec/v1`) and `blob_binary.json` (base64 content containing NUL and invalid UTF-8 sequences). Complete the rejection matrix in `tests/test_reader.py`: the `tree_full.json` submodule (`docs/external`, mode 160000) and symlink (`docs/link.md`, mode 120000) are skip-recorded with their modes as observed values and their SHAs never requested; synthetic path-violation entries (`docs/../evil.md`, `docs//gap.md`, `docs\\evil.md`, a 513-character path, a depth-9 path, a NUL-containing path) are recorded `PATH_VIOLATION` and never fetched; `examples/data.bin` and a synthetic `assets/pack.zip` are recorded `NON_ALLOWLISTED_EXTENSION`; the 200000-byte `docs/big.md` is recorded `OVER_BUDGET_SIZE` and never fetched; fetched-then-rejected cases assert `BINARY_CONTENT` for the binary blob and `LFS_POINTER` for the pointer blob with the blob GET count exactly 1 each.

Add module-level `hydrate_read_bundle(github_client, owner, repo, files) -> dict[str, str]` to `processors.py`: for each recorded file entry in read_order, `get_blob` at the recorded blob_sha, decode and re-run the binary/LFS checks, recompute `sha256_digest(raw_bytes)` and require exact equality with the recorded content_hash — any mismatch, decode failure or new rejection maps to `SafeFailure(STAGE_PERMANENT_FAILURE) from None`. Tests prove: hydration from the persisted reader payload reproduces the scratch bundle byte-for-byte; a tampered re-fetch (builder returns different bytes under the same blob SHA) fails closed with `stage_permanent_failure`; hydration issues exactly one GET per recorded file and zero calls to metadata/commits/tree/license endpoints.

No-execution evidence: assert the amended Phase 1 capability sweep passes (run `tests/test_phase1_gap_closure.py`) and add a reader-level runtime proof that a complete reader run performs only MockTransport HTTP — no subprocess, no import of candidate modules, no tempfile execution — by running the handler under the outbound socket sentinel with the recorded transport and asserting the only transport interactions are the recorded GitHub paths.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_reader.py tests/test_phase1_gap_closure.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout/application/processors.py tests/test_reader.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q</automated></verify>
  <acceptance_criteria>
    - Every READ-05 rejection class has a passing case with rule and observed value recorded; metadata-sufficient classes show zero blob GETs and fetched classes exactly one.
    - Hydration reproduces the bundle exactly, fails closed as `stage_permanent_failure` on any content-hash mismatch, and touches only blob endpoints.
    - The capability sweep and full suite pass; the runtime proof shows only recorded MockTransport interactions during a complete reader run.
    - No stage payload, manifest, SQLite row, stdout or stderr surface contains raw bundle text.
  </acceptance_criteria>
  <done>The complete READ-05 matrix, byte-verified hydration and the no-execution guarantee are proven over recorded fixtures with the full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Tree metadata → fetch decisions | Sizes, modes and paths are adversary-controlled and gate every byte moved. |
| Blob bytes → in-memory bundle | Content is hostile text that must stay inert, bounded and unpersisted. |
| Read record → downstream/extractor | The persisted record is the only reader output allowed to cross stages. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03-PT | Tampering | repository paths (traversal, separators, control bytes) | high | mitigate | Closed `validate_repo_path` predicate, `PATH_VIOLATION` skip-record, never-fetched assertion (canon: path-traversal is also owned by `/gsd:secure-phase` — breadcrumb, concrete checks here). |
| T-02-03-DOS | Denial of Service | oversized/many files, decompression-style abuse | high | mitigate | Size-before-fetch from tree metadata, five hard budgets with ±1 tests, per-endpoint response caps, early stop (Tasks 02-03-01/02). |
| T-02-03-SM | Tampering | binary, LFS pointer, submodule, symlink content smuggling | high | mitigate | Full rejection matrix with mode/extension sniffing and post-fetch binary/LFS checks (Task 02-03-02). |
| T-02-03-EX | Elevation of Privilege | candidate code execution | critical | mitigate | Capability omission: capability sweep keeps subprocess/importlib/socket/eval/exec banned everywhere; runtime proof shows only MockTransport HTTP; text is never interpreted (Task 02-03-02). |
| T-02-03-ID | Information Disclosure | raw bundle persistence into durable surfaces | high | mitigate | Memory-only scratch handoff, structural payload/manifest bounds, full-text canary sweep across payload, manifest and SQLite (Tasks 02-03-01/02). |

</threat_model>

## Artifacts this phase produces

- Source changes: `_reader` handler and `hydrate_read_bundle` in `src/skillscout/application/processors.py` (read-plan builder, budget accountant, rejection recorder, early-stop evaluator, scratch bundle handoff).
- Test assets: `tests/test_reader.py`; `make_tree_fixture`/`make_blob_fixture` builders in `tests/recorded_transport.py`; recorded blobs `blob_doc.json`, `blob_example.json`, `blob_pyproject.json`, `blob_source.json`, `blob_lfs.json`, `blob_binary.json` under `tests/fixtures/github/`.
- Behaviors: fixed tier order with in-tier path sorting, five enforced budgets with closed stop reasons, complete read record, full rejection matrix, hash-verified hydration, memory-only raw bundle.
- Non-artifacts: no extractor handler, no OpenAI adapter, no persistence format for raw text (deliberately never produced).

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Budgeted reading in fixed order with early stop | 02-03 | COVERED | Read loop + record + stop reasons over recorded fixtures. |
| REQ | READ-02 | README -> docs -> examples -> manifests -> source order | 02-03 | COVERED | read_order sequence proof. |
| REQ | READ-03 | Five default budgets, policy version, org ceilings | 02-03 | COVERED | ±1 boundary tests; defaults-only policy construction. |
| REQ | READ-04 | Early stop + complete structured read record | 02-03 | COVERED | Soft-target rule and field-completeness assertions. |
| REQ | READ-05 | Full rejection matrix | 02-03 | COVERED | Every class with never-fetched/exactly-once proofs. |
| REQ | READ-06 | No clone/install/build/import/execute | 02-03 | COVERED | Capability sweep + runtime transport proof. |
| RESEARCH | — | Size-before-fetch; silent-truncation ban; memory-only bundle | 02-03 | COVERED | Tree-size gate, closed stop reasons, canary sweeps. |
| PROBE | rows | READ-02..06 edge predicates authored into must_haves truths | 02-03 | COVERED | Budget ±1, order, record, matrix, no-execution. |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No CONTEXT.md decisions exist. |

<verification>
- `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_reader.py tests/test_phase1_gap_closure.py` passes, followed by `... run --locked ruff check src/skillscout/application/processors.py tests/test_reader.py` and the full `... run --locked pytest -q`.
- Recorded transport proves: exact tier order, never-fetched rejected candidates, one-GET fetched rejections, and blob-only hydration traffic.
- Hydration equality and tamper failure are proven; the full-text canary is absent from every durable surface.
- All Phase 1 and Plan 02-02 suites remain green; no Phase 1 file changed.
</verification>

<success_criteria>
- Reading is deterministic, budgeted, early-stopping and fully recorded, with the five READ-03 budgets proven at ±1 boundaries.
- The complete READ-05 rejection matrix is enforced with fetch-discipline evidence.
- Candidate content is never executed or persisted raw, and resume hydration is byte-identical or fails closed.
</success_criteria>

<output>After completion, create `.planning/phases/02-safe-single-repository-extraction/02-03-SUMMARY.md` with the budget matrix evidence, rejection-matrix inventory, hydration guarantees and non-persistence proof.</output>
