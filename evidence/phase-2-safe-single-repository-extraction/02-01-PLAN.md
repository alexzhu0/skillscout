---
phase: 02-safe-single-repository-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/skillscout/domain/subjects.py
  - src/skillscout/domain/filtering.py
  - src/skillscout/domain/reading.py
  - src/skillscout/domain/extraction.py
  - src/skillscout/domain/enums.py
  - src/skillscout/domain/models.py
  - src/skillscout/application/ports.py
  - src/skillscout/adapters/subjects.py
  - tests/test_phase2_contracts.py
  - tests/fixtures/subject/approved.json
  - tests/test_stage_contracts.py
  - tests/test_phase1_gap_closure.py
autonomous: false
requirements: [FILT-01, FILT-02, FILT-03, READ-03, READ-05, EXTR-02, EXTR-03]
must_haves:
  truths:
    - "Gate A2 blocks before any lock resolution and approves exactly two new direct declarations — httpx==0.28.1 (encode org) and openai==2.46.0 (OpenAI official) — with publisher legitimacy reviewed; any other new runtime dependency stops the plan."
    - "The lock update uses only the exact non-building, no-source, no-cache discovery command with the approved managed CPython 3.13.14; no sync, build, import, pytest or Ruff execution occurs before Gate B2 approves every new registry-only node/artifact and the recorded SHA-256 of the new uv.lock bytes."
    - "RepositorySubject strictly rejects extra fields, non-github.com URLs, subject_id/URL owner-name mismatch, and refs containing '..', leading/trailing slashes, backslashes or '@{'; load_subject maps symlink, non-regular, oversized, mid-read-changed and malformed inputs to the single closed INVALID_SUBJECT code without echoing input bytes."
    - "filter-policy-v1 evaluates a closed ordered rule set (repo.public, repo.not_archived, repo.not_fork, repo.has_default_branch, repo.has_readme, license.allowlisted, license.single_file, license.confirmed_at_sha) where every decision carries rule id, rule version, observed value, pass|fail|not_applicable and a closed rationale; the license allowlist is exactly {MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause} and null, NOASSERTION, non-listed, multiple-file, endpoint-404 and metadata-mismatch licenses all fail deterministically."
    - "reader-policy-v1 fixes the five default budgets (25 files, 5 source files, 131072 bytes per file, 524288 total bytes, 40000 estimated input tokens as ceil(bytes/4)) plus a 24000-token early-stop soft target, and any policy value above the equal-valued organization ceiling constants is rejected at validation; the tier order is fixed README -> docs/ -> examples/ -> manifests -> source and path validation rejects absolute, '..', empty-segment, backslash, NUL/control and over-512-character paths."
    - "workflow-spec-v1 carries the full EXTR-03 field list and extractor-response-v1 is generated from the Pydantic model with additionalProperties false, all-required fields with null-unions and workflows maxItems 3; fingerprints are computed only by skillscout as sha256 over a versioned wf-fingerprint-v1 preimage of repo id, normalized goal and normalized ordered steps, stable under case/whitespace/punctuation variation and sensitive to semantic and step-order change."
    - "Boundary validation deterministically drops any candidate workflow whose evidence path is absent from the read record, whose blob_sha disagrees with the record, whose excerpt is not a verbatim substring of the fetched content or exceeds 280 characters, or whose text matches closed URL, shell-command or secret-shaped patterns."
    - "Phase 1 authority stays intact: the only edits to Phase 1 tests are the three sanctioned additive-member updates (producer registry gains (\"2\",\"phase2-v1\"), the dependency list gains the two pins, the capability sweep gains named-module carve-outs for adapters/github.py importing httpx and adapters/openai_extract.py importing openai), and every other Phase 1 test is byte-for-byte unchanged and green."
  artifacts:
    - { path: pyproject.toml, provides: "two new exact-pinned runtime declarations admitted only through the two-gate ceremony" }
    - { path: uv.lock, provides: "non-building discovered graph receiving Gate B2 exact-byte approval" }
    - { path: src/skillscout/domain/subjects.py, provides: "RepositorySubject run-authority contract", exports: [RepositorySubject, SubjectId, RepositoryUrl, SubjectRef] }
    - { path: src/skillscout/adapters/subjects.py, provides: "bounded single-descriptor subject loader", exports: [load_subject, MAX_SUBJECT_BYTES] }
    - { path: src/skillscout/domain/filtering.py, provides: "filter-policy-v1 closed rules and pure verdict function", exports: [FILTER_POLICY_VERSION, FilterRuleId, FilterResult, RuleDecision, RepoFacts, TreeCandidate, TreeFacts, LicenseConfirmation, FilterVerdict, evaluate_filter, ALLOWED_LICENSE_SPDX] }
    - { path: src/skillscout/domain/reading.py, provides: "reader-policy-v1 budgets, tiers, allowlists and path/classification predicates", exports: [READER_POLICY_VERSION, ReadTier, TIER_ORDER, ReaderPolicy, StopReason, RejectionRule, validate_repo_path, assign_tier, estimate_tokens, LFS_POINTER_PREFIX, MANIFEST_FILENAMES, MAX_PATH_CHARS, MAX_PATH_DEPTH] }
    - { path: src/skillscout/domain/extraction.py, provides: "extractor-response-v1, workflow-spec-v1, fingerprint and deterministic boundary validation", exports: [EXTRACT_PROMPT_VERSION, FINGERPRINT_VERSION, MAX_WORKFLOWS_PER_REPO, MAX_EVIDENCE_EXCERPT_CHARS, ExtractorResponse, ExtractorWorkflow, WorkflowSpec, WorkflowEvidence, EvidenceRef, WorkflowStep, normalize_for_fingerprint, workflow_fingerprint, find_forbidden_text, validate_workflow_boundaries] }
    - { path: tests/test_phase2_contracts.py, provides: "pure contract evidence for subjects, filtering, reading, extraction, fingerprint and the additive registry/status/code members" }
  key_links:
    - { from: pyproject.toml, to: uv.lock, via: "the exact Phase 1 non-building/no-source/no-cache discovery command under the approved managed CPython", pattern: "httpx==0.28.1" }
    - { from: uv.lock, to: "Gate B2 approval", via: "every new package/edge/source/artifact plus the recorded lock SHA-256 reviewed before any execution", pattern: "[[package]]" }
    - { from: src/skillscout/domain/extraction.py, to: src/skillscout/domain/canonical.py, via: "keyword-only versioned fingerprint preimage over sha256_digest", pattern: "wf-fingerprint-v1" }
    - { from: src/skillscout/domain/models.py, to: src/skillscout/application/pipeline.py, via: "additive (\"2\",\"phase2-v1\") registration read by the existing producer gate", pattern: "SUPPORTED_PRODUCER_SCHEMAS" }
  prohibitions:
    - "No dependency beyond httpx and openai may enter the runtime graph, and neither package may be synced, built, imported or executed before Gate B2 approves the exact new lock bytes."
    - "The filter must never pass a repository whose license is missing, unrecognized, NOASSERTION, multiple or conflicting in order to keep candidates flowing; license ambiguity is always a recorded deterministic rejection, never an LLM question."
    - "Phase 1 closed vocabularies must never be narrowed, renamed or reordered to make phase two fit; new members are additive-only and every pre-existing behavior assertion stays unchanged."
---

<objective>
Admit exactly two new runtime dependencies (`httpx`, `openai`) through the Phase 1 two-gate lock-approval ceremony, then land every frozen Phase 2 domain contract — subject, filtering, reading, extraction — with pure unit tests and zero network code.

Purpose: Make the human-reviewed supply-chain decision and the semantic trust-boundary contracts before any adapter or runner change exists, so later waves build on approved bytes and frozen shapes.
Output: Approved updated `uv.lock`; `domain/subjects.py`, `domain/filtering.py`, `domain/reading.py`, `domain/extraction.py`; additive `COMPLETED`/`INVALID_SUBJECT`/`("2","phase2-v1")` members; green contract suite.
</objective>

## Phase Goal

**As a** SkillScout reviewer, **I want to** point the pipeline at one public GitHub repository and receive at most three evidenced `WorkflowSpec`s or a clear filtered/no-workflow conclusion, **so that** real extraction is proven safe — pinned, filtered, budgeted, never executed — before generation, review and publishing exist.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
@/Users/alexzhu/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md
@.planning/phases/02-safe-single-repository-extraction/02-VALIDATION.md
@.planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md
@pyproject.toml
@src/skillscout/domain/enums.py
@src/skillscout/domain/models.py
@src/skillscout/application/ports.py
@src/skillscout/adapters/fixtures.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 02-01-01: Gate A2 — approve exactly two new direct dependency declarations</name>
  <files>None — Gate A2 precedes every lock resolution, download and execution of new dependency bytes.</files>
  <read_first>pyproject.toml, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md, .planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md, .planning/phases/01-auditable-dry-run-spine/01-01-PLAN.md, .planning/STATE.md</read_first>
  <action>Present the full Gate A2 record and stop until a human explicitly approves it. The record contains: exactly two new direct declarations — `httpx==0.28.1` (publisher: encode org / Tom Christie; documented at python-httpx.org; pure Python, no build step; first-party `httpx.MockTransport` testing API; current PyPI latest verified at plan time 2026-07-21) and `openai==2.46.0` (publisher: OpenAI official; stainless-generated typed SDK; current PyPI latest verified at plan time 2026-07-21, superseding the STACK.md 2.45.x baseline by one minor line; only the Responses namespace will be used) — plus the explicit non-additions: no `tiktoken` (budgeting uses deterministic ceil(bytes/4)), no GitHub SDK (direct REST per STACK.md), no tenacity/retry library (Phase 1 `RetryPolicy` owns retry), no VCR/cassette library (MockTransport plus repository-owned recorded JSON). The record restates the Phase 1 Gate B decision that any byte change to `uv.lock` invalidates the approved dependency graph, and notes expected transitive families to be confirmed at Gate B2 (`anyio`, `sniffio`, `certifi`, `h11`, `httpcore`, `idna` for httpx; `annotated-types`, `distro`, `jiter`, `pydantic-core`, `tqdm`, `typing-extensions`, `typing-inspection` for openai — the exact set is whatever the resolver emits and is approved at Gate B2, not here). A different package, publisher, or any third new declaration stops execution for a new review. Do not let auto-advance approve supply-chain inputs.</action>
  <verify><automated>N/A — this provenance decision is intentionally human-only and blocks before new dependency resolution.</automated></verify>
  <acceptance_criteria>
    - The approval records the exact pins `httpx==0.28.1` and `openai==2.46.0`, both publishers, the health signals, and the four explicit non-additions.
    - The reviewer explicitly acknowledges the openai pin supersedes the STACK.md `2.45.x` baseline and that transitive families are indicative only until Gate B2.
    - No edit to `pyproject.toml`/`uv.lock`, no `uv lock`/`uv sync`/`uv run` and no download of new dependency bytes occurred before the approval signal.
  </acceptance_criteria>
  <done>Gate A2 is explicitly approved in the execution record; otherwise the plan stops with the dependency graph unchanged.</done>
</task>

<task type="auto">
  <name>Task 02-01-02: Apply the two pins, rediscover the lock non-building, and land all frozen contracts unexecuted</name>
  <files>pyproject.toml, uv.lock, src/skillscout/domain/subjects.py, src/skillscout/domain/filtering.py, src/skillscout/domain/reading.py, src/skillscout/domain/extraction.py, src/skillscout/domain/enums.py, src/skillscout/domain/models.py, src/skillscout/application/ports.py, src/skillscout/adapters/subjects.py, tests/test_phase2_contracts.py, tests/fixtures/subject/approved.json, tests/test_stage_contracts.py, tests/test_phase1_gap_closure.py</files>
  <read_first>pyproject.toml, src/skillscout/domain/enums.py, src/skillscout/domain/models.py, src/skillscout/domain/canonical.py, src/skillscout/application/ports.py, src/skillscout/adapters/fixtures.py, tests/test_stage_contracts.py, tests/test_phase1_gap_closure.py, .planning/phases/02-safe-single-repository-extraction/02-PATTERNS.md</read_first>
  <action>Proceed only after Gate A2. Edit `pyproject.toml` so `[project].dependencies` is exactly the sorted list `"httpx==0.28.1"`, `"openai==2.46.0"`, `"pydantic==2.13.4"` — no extras, no URL/path/editable sources, dev group untouched. Run exactly `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --no-build --no-sources --no-cache --managed-python --no-python-downloads --python 3.13.14`; it may fetch metadata bytes but must stop if resolution needs a build; do not sync, build, import project or locked packages, and do not run `uv run`, pytest or Ruff in this task.

Land the frozen contracts as static source (unexecuted): `src/skillscout/domain/subjects.py` with `SubjectId` (pattern `^repo:[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$`, min 8 max 128), a local `RepositoryUrl` annotated type carrying the same regex as `adapters/fixtures.py:22-29` (deliberate duplication — domain never imports from adapters), `SubjectRef` (pattern `^[A-Za-z0-9][A-Za-z0-9._/-]{0,254}$` plus rejection of `..`, leading/trailing `/`, `@{` and backslash) and `RepositorySubject(StrictFrozenModel)` with `schema_version: Literal["1"]`, `subject_id`, `repository`, `ref: SubjectRef | None = None` and a model validator requiring the subject_id owner/name to equal the URL path segments. `src/skillscout/adapters/subjects.py` with `MAX_SUBJECT_BYTES = 65_536` and `load_subject(path)` copying the `load_fixture` single-descriptor shape (lstat, `O_NOFOLLOW|O_NONBLOCK|O_CLOEXEC`, fstat identity recheck, cap-plus-one overflow probe, strict decode/validate) with every failure mapped to `ErrorCode.INVALID_SUBJECT` via `raise ... from None`.

`src/skillscout/domain/filtering.py`: `FILTER_POLICY_VERSION = "filter-policy-v1"`; `FilterRuleId` StrEnum with exactly `REPO_PUBLIC`, `REPO_NOT_ARCHIVED`, `REPO_NOT_FORK`, `REPO_HAS_DEFAULT_BRANCH`, `REPO_HAS_README`, `LICENSE_ALLOWLISTED`, `LICENSE_SINGLE_FILE`, `LICENSE_CONFIRMED_AT_SHA`; `FilterResult` StrEnum `PASS|FAIL|NOT_APPLICABLE`; `ALLOWED_LICENSE_SPDX = frozenset({"MIT", "Apache-2.0", "BSD-2-Clause", "BSD-3-Clause"})`; `RuleDecision(StrictFrozenModel)` with rule_id, rule_version, observed (bounded str), result, rationale from a closed per-rule rationale map; input models `RepoFacts` (private, archived, fork, disabled, visibility, default_branch, license_spdx: str|None), `TreeCandidate` (path, mode, type, size, sha), `TreeFacts` (has_root_readme: bool, root_license_files: tuple[str, ...]), `LicenseConfirmation` (status Literal `confirmed|not_found|mismatch|noassertion`, observed_spdx: str|None); pure `evaluate_filter(repo_facts, tree_facts, license_confirmation) -> FilterVerdict` where rules evaluate in the fixed order above, a rule whose precondition failed yields `not_applicable`, license rules fail on null/`NOASSERTION`/non-listed SPDX, more than one root `LICENSE*`/`COPYING*` candidate, endpoint 404, or metadata/endpoint mismatch, and the verdict is `accepted` only when no rule failed.

`src/skillscout/domain/reading.py`: `READER_POLICY_VERSION = "reader-policy-v1"`; `ReadTier` StrEnum `README|DOCS|EXAMPLES|MANIFESTS|SOURCE` and `TIER_ORDER` in exactly that order; `ReaderPolicy(StrictFrozenModel)` with `max_files=25`, `max_source_files=5`, `max_file_bytes=131_072`, `max_total_bytes=524_288`, `max_estimated_input_tokens=40_000`, `early_stop_soft_tokens=24_000`, validated never to exceed the equal-valued organization ceiling constants `READER_ORG_MAX_FILES`…`READER_ORG_MAX_TOKENS`; `MANIFEST_FILENAMES = frozenset({"pyproject.toml", "requirements.txt", "requirements-dev.txt", "setup.py", "setup.cfg", "package.json", "Cargo.toml", "go.mod"})`; documentation extensions `.md/.rst/.txt`, source extensions `.py` only, source roots `src/`,`lib/` plus root-level `*.py`; `MAX_PATH_CHARS = 512`, `MAX_PATH_DEPTH = 8`; `LFS_POINTER_PREFIX = "version https://git-lfs.github.com/spec/v1"`; `StopReason` StrEnum `SOFT_TARGET_REACHED|BUDGET_EXHAUSTED|CANDIDATES_EXHAUSTED|NO_ALLOWLISTED_FILES`; `RejectionRule` StrEnum `SUBMODULE|SYMLINK|PATH_VIOLATION|NON_ALLOWLISTED_EXTENSION|OVER_BUDGET_SIZE|BINARY_CONTENT|LFS_POINTER`; pure predicates `validate_repo_path`, `assign_tier`, `is_allowlisted_for_tier`, `estimate_tokens(byte_count) = ceil(byte_count / 4)`.

`src/skillscout/domain/extraction.py`: `EXTRACT_PROMPT_VERSION = "extract-prompt-v1"`, `FINGERPRINT_VERSION = "wf-fingerprint-v1"`, `WORKFLOW_SPEC_SCHEMA_VERSION = "workflow-spec-v1"`, `MAX_WORKFLOWS_PER_REPO = 3`, `MAX_EVIDENCE_EXCERPT_CHARS = 280`; LLM-facing `EvidenceRef` (path, blob_sha, excerpt ≤ 280 chars, supports), `WorkflowStep` (instruction, evidence tuple), `ExtractorWorkflow` (title, goal, applicability, non_goals, preconditions, inputs, steps, outputs, failure_modes, prohibited_actions, required_approvals, assumptions, evidence, confidence 0..1), `ExtractorResponse(StrictFrozenModel)` (repository_summary, rejection_reason: str | None, workflows bounded to max 3) shaped so the generated JSON Schema satisfies Structured Outputs (object root, `additionalProperties: false`, all fields required, optionals as null-unions); persisted `WorkflowEvidence` (adds content_hash) and `WorkflowSpec(StrictFrozenModel)` (schema_version, workflow_id, fingerprint, fingerprint_version plus every EXTR-03 field); `normalize_for_fingerprint` (NFKC, casefold, punctuation strip, whitespace collapse); keyword-only `workflow_fingerprint(*, repo_id, goal, steps)` over `sha256_digest` with the version string inside the preimage; closed `FORBIDDEN_TEXT_PATTERNS` (URLs, shell-shaped commands such as `curl …| sh`/`bash -c`/`sudo`, secret shapes such as `github_pat_`, `ghp_`, `sk-`, `AKIA`, PEM headers) with `find_forbidden_text`; `validate_workflow_boundaries(workflow, *, bundle_texts, recorded)` returning drop reasons for unknown evidence path, blob_sha mismatch, non-verbatim or over-length excerpt, and forbidden pattern hits.

Apply the three sanctioned additive Phase 1 edits: `domain/enums.py` gains `RunStatus.COMPLETED = "completed"`, the `RUNNING` successor frozenset gains `COMPLETED`, and `_RUN_TRANSITIONS` gains `RunStatus.COMPLETED: frozenset()`; `domain/models.py:35-37` registry gains exactly `("2", "phase2-v1")`; `application/ports.py` gains `ErrorCode.INVALID_SUBJECT = "invalid_subject"` with fixed summary `"Subject input was rejected."` (ASCII ≤ 160 guard untouched). Amend exactly three Phase 1 test assertions, additively: `tests/test_stage_contracts.py:441` exact registry set gains `("2", "phase2-v1")`; `tests/test_phase1_gap_closure.py:819` dependency list becomes the new sorted three-pin list; the same file's capability sweep keeps every forbidden module banned everywhere except that `adapters/github.py` may import `httpx` and `adapters/openai_extract.py` may import `openai` (carve-out keyed by exact module path, active before those files exist; `subprocess`, `importlib`, `socket`, `urllib`, `requests`, `aiohttp`, `github`, `http`, `compile`, `eval`, `exec`, `os.popen`, `os.system` remain banned in every module including the two carve-outs). Write `tests/test_phase2_contracts.py` (subject strictness matrix, loader failure mapping, full filter rule matrix including every license boundary, reading predicates and policy-ceiling rejection, extraction schema shape, fingerprint stability/sensitivity/order properties, boundary-validation drop matrix, additive enum/registry/error-code membership) and `tests/fixtures/subject/approved.json` (one `repo:example/approved-repo` subject with repository URL and no ref). No test executes in this task.</action>
  <verify><automated>grep -q '"httpx==0.28.1"' pyproject.toml &amp;&amp; grep -q '"openai==2.46.0"' pyproject.toml &amp;&amp; test -s uv.lock &amp;&amp; grep -q 'name = "httpx"' uv.lock &amp;&amp; grep -q 'name = "openai"' uv.lock &amp;&amp; ! grep -rn "^import httpx\|^from httpx\|^import openai\|^from openai" src/skillscout/ &amp;&amp; test -f src/skillscout/domain/subjects.py &amp;&amp; test -f src/skillscout/domain/filtering.py &amp;&amp; test -f src/skillscout/domain/reading.py &amp;&amp; test -f src/skillscout/domain/extraction.py &amp;&amp; test -f src/skillscout/adapters/subjects.py &amp;&amp; test -f tests/test_phase2_contracts.py &amp;&amp; test -f tests/fixtures/subject/approved.json</automated></verify>
  <acceptance_criteria>
    - `pyproject.toml` declares exactly the three sorted exact pins with no extras or non-registry sources; `uv.lock` was produced by the exact non-building/no-source/no-cache command and contains `httpx` and `openai` nodes.
    - No `uv sync`/`uv build`/`uv run`/pytest/Ruff/import of project or locked packages ran; no source file imports `httpx` or `openai` yet.
    - All four new domain modules, the loader, both additive enum/registry/error members, both amended Phase 1 assertions plus the sweep carve-out, the contract test module and the subject fixture exist as static source.
    - `ReaderPolicy` defaults equal the five READ-03 budgets and reject any above-ceiling value; `ExtractorResponse` carries no `tools`-related field and caps workflows at 3.
  </acceptance_criteria>
  <done>The two approved pins are declared, the updated lock graph awaits Gate B2, and every Phase 2 contract exists as reviewed-but-unexecuted source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 02-01-03: Gate B2 — approve every new locked distribution and the exact new lock bytes</name>
  <files>uv.lock</files>
  <read_first>uv.lock, pyproject.toml, .planning/phases/02-safe-single-repository-extraction/02-RESEARCH.md, .planning/phases/01-auditable-dry-run-spine/01-01-PLAN.md, .planning/STATE.md</read_first>
  <action>Before any `uv sync`, `uv build`, `uv run`, project/package import, pytest or Ruff command, inspect every new and changed `[[package]]` entry and every `sdist`/`wheels` artifact in the updated `uv.lock` against the Phase 1 approved baseline. The single first-party root stays exactly `skillscout==0.1.0` at canonical editable `.` with no artifacts; its `requires-dist` now lists exactly the three reviewed pins. Every new external node must be an exact-version, expected-edge, registry-only PyPI node with artifact URL/SHA-256/size reviewed; reject Git, path, editable, workspace, direct-URL and alternate-registry sources, missing hashes, yanked or adverse releases, lookalike names (e.g. `httpx2`, `openai-python` under a foreign owner), and any Darwin/aarch64 source-build requirement. Confirm no package beyond the Gate A2 declaration set and its resolver-chosen transitives entered the graph and that `pydantic==2.13.4` and the dev group are unchanged. Compute and record SHA-256 of the exact new lock bytes and require the human to approve that hash; Gate A2 cannot approve artifacts that were not visible until resolution.</action>
  <verify><automated>N/A — complete transitive provenance and exact-lock approval is a mandatory human security decision.</automated></verify>
  <acceptance_criteria>
    - Every new/changed package, edge, marker, source and artifact URL/hash/size has an explicit disposition; every non-root node is an exact reviewed registry-only PyPI node.
    - The graph contains only the two Gate A2 declarations plus their resolver-chosen transitives and the previously approved packages; no source build is required on Darwin/aarch64.
    - The human explicitly approves the recorded SHA-256 of the new `uv.lock`; later byte changes invalidate Gate B2.
    - No sync/build/import/test command ran before approval.
  </acceptance_criteria>
  <done>Gate B2 explicitly approves the complete new graph and exact lock bytes; otherwise execution stops before any new code is installed or run.</done>
</task>

<task type="auto">
  <name>Task 02-01-04: Execute the contract suite and full regression under the approved graph</name>
  <files>None (execution and evidence only; source fixes, if any, stay inside the files this plan already owns)</files>
  <read_first>tests/test_phase2_contracts.py, tests/test_stage_contracts.py, tests/test_phase1_gap_closure.py, tests/test_cli_security.py, src/skillscout/domain/subjects.py, src/skillscout/domain/filtering.py, src/skillscout/domain/reading.py, src/skillscout/domain/extraction.py</read_first>
  <action>Require the current `uv.lock` SHA-256 to equal the Gate B2 approved hash before running anything. Run the focused contract suite plus the three amended Phase 1 modules, then Ruff over the touched paths, then the full suite and `uv lock --check` — all with the canonical prefix: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_contracts.py tests/test_stage_contracts.py tests/test_phase1_gap_closure.py tests/test_cli_security.py`, then `... run --locked ruff check src/skillscout tests/test_phase2_contracts.py`, then `... run --locked pytest -q` and `... lock --check`. The first `uv run --locked` performs the post-approval sync; that is the only installation event. If a contract assertion fails, fix only the Phase 2 contract modules or the three amended assertions — never weaken a Phase 1 behavior assertion.</action>
  <verify><automated>UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_contracts.py tests/test_stage_contracts.py tests/test_phase1_gap_closure.py tests/test_cli_security.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check src/skillscout tests/test_phase2_contracts.py &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check</automated></verify>
  <acceptance_criteria>
    - The lock hash matches Gate B2 before the first command; the focused suites, Ruff, the full suite and `uv lock --check` all exit 0 under the repository-local managed toolchain.
    - `test_supported_producer_schema_registry_is_closed_and_immutable` passes with the three-member registry; the gap-closure dependency assertion passes with the three-pin list; the capability sweep passes with the two named-module carve-outs.
    - The contract suite proves: subject/loader rejection matrix, all eight filter rules across pass/fail/not_applicable including every FILT-02 license boundary, reading budgets/tiers/path predicates, Structured-Outputs-shaped extraction schema, fingerprint stability/sensitivity/order properties, and the boundary-validation drop matrix.
    - No Phase 1 source file changed except `domain/enums.py`, `domain/models.py`, `application/ports.py` additive members; no Phase 1 test changed except the three sanctioned assertions.
  </acceptance_criteria>
  <done>The approved graph executes, every frozen Phase 2 contract is proven by pure unit tests, and the entire Phase 1 suite remains green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| PyPI registry → resolved lock graph | Two new direct declarations pull unseen transitive packages and artifacts into the approved supply chain. |
| Approved lock → executed environment | Gate B2's exact bytes define the only authorized installed graph; later byte changes invalidate it. |
| Untrusted subject file → run authority | The subject JSON selects the remote repository; malformed or hostile bytes must fail closed before any state or network activity. |
| Model-facing contract shape → downstream trust | The frozen `WorkflowSpec`/filter/reading contracts are the semantic trust boundary every later wave relies on. |

## STRIDE Threat Register — ASVS L1, block unresolved high/critical

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-SC | Tampering / Spoofing | httpx/openai supply chain including transitives | high | mitigate | Gate A2 and Gate B2 remain distinct blocking human approvals around non-building lock discovery; registry-only sources, per-artifact hash review, lookalike rejection and exact-lock approval bind all later execution to the reviewed graph (Tasks 02-01-01/03). |
| T-02-01-IN | Tampering | subject input file | high | mitigate | Bounded single-descriptor load, strict frozen schema, subject_id↔URL cross-validation, ref character denylist, and one closed non-echoing `INVALID_SUBJECT` code (Task 02-01-02, proven in 02-01-04). |
| T-02-01-CT | Tampering / Elevation of Privilege | Phase 1 closed vocabularies and capability sweep | high | mitigate | Additive-only members; exactly three sanctioned Phase 1 test amendments; sweep keeps execution-capable modules banned everywhere with two exact-path carve-outs; full Phase 1 suite must stay green (Tasks 02-01-02/04). |
| T-02-01-LIC | Information Disclosure (legal/attribution) | license determination | medium | mitigate | filter-policy-v1 fails closed on null/NOASSERTION/non-listed/multiple/mismatched licenses with versioned rule records; the question never reaches an LLM (Task 02-01-02 contract, exercised live in Plan 02-02). |

</threat_model>

## Artifacts this phase produces

- Tracked files: `pyproject.toml`, `uv.lock`, `src/skillscout/domain/subjects.py`, `src/skillscout/domain/filtering.py`, `src/skillscout/domain/reading.py`, `src/skillscout/domain/extraction.py`, `src/skillscout/adapters/subjects.py`, `tests/test_phase2_contracts.py`, `tests/fixtures/subject/approved.json`; additive edits in `src/skillscout/domain/enums.py`, `src/skillscout/domain/models.py`, `src/skillscout/application/ports.py`, `tests/test_stage_contracts.py`, `tests/test_phase1_gap_closure.py`.
- Package identifiers: `httpx==0.28.1`, `openai==2.46.0` (runtime); dev group unchanged (`pytest==9.1.1`, `ruff==0.15.21`).
- Domain symbols: `RepositorySubject`, `SubjectId`, `RepositoryUrl` (domain-local), `SubjectRef`, `load_subject`, `MAX_SUBJECT_BYTES`, `FILTER_POLICY_VERSION`, `FilterRuleId`, `FilterResult`, `RuleDecision`, `RepoFacts`, `TreeCandidate`, `TreeFacts`, `LicenseConfirmation`, `FilterVerdict`, `evaluate_filter`, `ALLOWED_LICENSE_SPDX`, `READER_POLICY_VERSION`, `ReadTier`, `TIER_ORDER`, `ReaderPolicy`, `StopReason`, `RejectionRule`, `validate_repo_path`, `assign_tier`, `is_allowlisted_for_tier`, `estimate_tokens`, `LFS_POINTER_PREFIX`, `MANIFEST_FILENAMES`, `MAX_PATH_CHARS`, `MAX_PATH_DEPTH`, `EXTRACT_PROMPT_VERSION`, `FINGERPRINT_VERSION`, `WORKFLOW_SPEC_SCHEMA_VERSION`, `MAX_WORKFLOWS_PER_REPO`, `MAX_EVIDENCE_EXCERPT_CHARS`, `EvidenceRef`, `WorkflowStep`, `ExtractorWorkflow`, `ExtractorResponse`, `WorkflowEvidence`, `WorkflowSpec`, `normalize_for_fingerprint`, `workflow_fingerprint`, `FORBIDDEN_TEXT_PATTERNS`, `find_forbidden_text`, `validate_workflow_boundaries`.
- Additive Phase 1 members: `RunStatus.COMPLETED`, `ErrorCode.INVALID_SUBJECT`, `("2", "phase2-v1")` in `SUPPORTED_PRODUCER_SCHEMAS`.
- Human evidence: Gate A2 approval of exactly two declarations; Gate B2 approval of every new registry-only node/artifact and the recorded SHA-256 of the new `uv.lock`.
- Unexecuted-then-executed test identities: the `tests/test_phase2_contracts.py` contract suite.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Approved supply chain and frozen contracts before any network code | 02-01 | COVERED | Two-gate ceremony precedes execution; contracts are pure and tested. |
| REQ | FILT-01..03 | Closed versioned filter rules and license boundary | 02-01 | COVERED | Contract + rule matrix here; live Scout/Filter evidence in 02-02. |
| REQ | READ-03 / READ-05 | Budget constants, ceilings, classification and path predicates | 02-01 | COVERED | Policy contract here; budget/rejection behavior in 02-03. |
| REQ | EXTR-02 / EXTR-03 | ≤3 workflows, full field list, stable skillscout-owned fingerprint | 02-01 | COVERED | Contract and fingerprint properties here; live extraction in 02-04. |
| RESEARCH | — | Exactly two dependencies through the Phase 1 lock ceremony; no tiktoken/SDK/tenacity/VCR | 02-01 | COVERED | Gate A2 record names the four non-additions. |
| RESEARCH | — | Additive COMPLETED / INVALID_SUBJECT / ("2","phase2-v1") | 02-01 | COVERED | Three exact edits, Phase 1 suite green. |
| PROBE | 15 rows | Edge fallback authored into truths (FILT-01..03 here in contract form, READ-03, EXTR-02/03; SEC-01 double row accounted for in 02-04) | 02-01 | COVERED | Remaining rows land in 02-02 (READ-01), 02-03 (READ-02/04/05/06), 02-04 (EXTR-01/04, SEC-01×2). |
| CONTEXT | — | Discussion explicitly skipped | — | N/A | No CONTEXT.md decisions exist. |

<verification>
- Gate A2 and Gate B2 have separate explicit approval records in order, and the current lock SHA-256 equals the Gate B2 hash before any execution.
- `pyproject.toml` carries exactly `httpx==0.28.1`, `openai==2.46.0`, `pydantic==2.13.4`; `uv.lock` contains only registry-only nodes approved at Gate B2.
- Post-Gate command: `UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_contracts.py tests/test_stage_contracts.py tests/test_phase1_gap_closure.py tests/test_cli_security.py && ... run --locked ruff check src/skillscout tests/test_phase2_contracts.py && ... run --locked pytest -q && ... lock --check` all exit 0.
- No source module imports `httpx` or `openai` in this plan; no network, LLM, clone, install, build or candidate-execution capability exists after this plan.
</verification>

<success_criteria>
- Exactly two human-approved dependencies enter the graph through the Phase 1 ceremony with exact-byte lock authority recorded.
- Every Phase 2 semantic contract (subject, filter, reader policy, extraction, fingerprint, boundary validation) exists as frozen source with pure unit-test proof.
- The three sanctioned Phase 1 amendments are the only Phase 1 diffs, and the complete suite is green under the approved graph.
</success_criteria>

<output>After completion, create `.planning/phases/02-safe-single-repository-extraction/02-01-SUMMARY.md` with both gate signals, the exact approved lock hash, the contract inventory and the focused/full-suite evidence.</output>
