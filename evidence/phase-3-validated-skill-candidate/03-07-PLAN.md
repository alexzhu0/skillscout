---
phase: 03-validated-skill-candidate
plan: 07
type: execute
wave: 6
depends_on:
  - 03-05
  - 03-04
files_modified:
  - src/skillscout/domain/qualification.py
  - tests/test_qualification.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Every candidate receives deterministic versioned checks for specificity, reusability, verifiability, evidence sufficiency, and unauthorized execution risk."
    - "The default qualification threshold is exactly 75/100 and a pass also requires no hard failure."
    - "The report itemizes each check, weight, awarded score, policy version, pass/fail result, and bounded reasons."
    - "QualificationReportV1 directly binds the selected full fingerprint, complete WorkflowSpecAuthorityV1, and CandidateExecutionAuthorityV1 in its header; transitive identity is not sufficient."
    - "The closed hard-failure matrix blocks fewer than three steps, empty inputs/outputs, missing/unknown/hash-mismatched evidence, credential access, destructive or permission-bypass behavior, injection residue, approval-requiring side effects without a named approval step, and unauthorized execution regardless of numeric score."
  artifacts:
    - path: "src/skillscout/domain/qualification.py"
      provides: "Versioned deterministic qualification policy and strict report."
    - path: "tests/test_qualification.py"
      provides: "Boundary, hard-failure, itemization, and policy-sensitivity tests."
  key_links:
    - from: "WorkflowSpec"
      to: "QualificationReportV1"
      via: "Pure deterministic checks over structured Phase 2 fields."
    - from: "QualificationReportV1"
      to: "qualification_rejected or generator stage"
      via: "score >= 75 and no hard failure."
  prohibitions:
    - "Do not call an LLM or inspect raw repository text during qualification."
    - "Do not describe scoring/quote limits as a legal safe harbor."
---

<objective>
Implement deterministic, auditable qualification before any generation call.

Purpose: Low-value or unsafe workflows should close locally with explainable scores and zero generator cost.
Output: Versioned qualification policy, strict itemized report, and edge-case tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/domain/extraction.py
@src/skillscout/domain/canonical.py
@tests/test_extractor_boundary.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode versioned deterministic qualification checks</name>
  <files>src/skillscout/domain/qualification.py, tests/test_qualification.py</files>
  <read_first>
    - `src/skillscout/domain/extraction.py` — inspect exact WorkflowSpec fields available to pure checks.
    - `.planning/REQUIREMENTS.md` — implement QUAL-01's five required dimensions.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — adopt the recommended conservative weights and confidence floor as agent discretion.
  </read_first>
  <behavior>
    - Specificity, reusability, verifiability, evidence sufficiency, and unauthorized execution risk are independently evaluated.
    - Weights are exactly 25, 20, 20, 25, and 10, totaling 100.
    - Evidence confidence below 0.70 fails the evidence dimension under versioned policy.
    - Every closed hard-failure code creates a hard failure even when the other awarded points exceed 75.
    - A two-step workflow, empty input/output contract, unknown or hash-mismatched evidence, credential access, destructive action, permission bypass, injection residue, unnamed approval-requiring side effect, source execution, dependency installation, repository-script invocation, or download-and-execute attempt always rejects.
  </behavior>
  <action>
Create a pure domain module with exported version constants, strict frozen check result models, and deterministic evaluator functions over `WorkflowSpec`. Adopt research's conservative project-policy defaults under the agent's discretion: weights specificity 25, reusability 20, verifiability 20, evidence sufficiency 25, unauthorized-execution safety 10; evidence confidence floor 0.70. Give each check a stable ID, weight, awarded integer points, pass flag, hard-failure flag, and bounded reason from a closed versioned vocabulary. Derive results only from versioned structured fields/evidence already present in WorkflowSpec. The hard-failure vocabulary must cover: fewer than three workflow steps; empty declared inputs; empty declared outputs; missing evidence; evidence pointing to an unknown source; evidence path/blob/content hash mismatch; credential/secret access; destructive action; permission or approval bypass; prompt-injection residue; a side effect that requires human approval without an explicit named approval step; source-code execution; dependency installation; repository-script/tool invocation; download-and-execute; and any other unauthorized capability represented by the schema. Evaluate hashes and evidence membership against the complete verified WorkflowSpec/source authority, not model claims. A single hard-failure reason blocks regardless of points; multiple reasons remain itemized and deterministically ordered. Document weights, floor, and hard-failure vocabulary as reviewable versioned project policy, not legal or security safe-harbor thresholds.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_qualification.py -k checks</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five required dimensions have stable IDs and weights totaling 100.
    - All checks are pure and deterministic over strict structured input.
    - 0.69 fails and 0.70 passes the confidence boundary when other evidence conditions hold.
    - Every closed hard-failure code has a positive case and a nearby safe negative case.
    - The required two-step, empty-I/O, evidence missing/unknown/hash-mismatch, credential, destructive, permission-bypass, injection-residue, unnamed-approval, source-execution, install, repository-script, and download-execute cases reject even when non-safety scores would reach 75.
  </acceptance_criteria>
  <done>The five required qualification dimensions are deterministic, versioned, and independently testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce the exact thresholded qualification report</name>
  <files>src/skillscout/domain/qualification.py, tests/test_qualification.py</files>
  <read_first>
    - `src/skillscout/domain/qualification.py` — compose Task 1 check results without duplicating policy logic.
    - `.planning/REQUIREMENTS.md` — implement QUAL-02 threshold and report fields exactly.
    - `src/skillscout/domain/canonical.py` — use canonical serialization/digest conventions.
  </read_first>
  <behavior>
    - Total score is the exact sum of awarded item scores and is bounded 0 through 100.
    - Default pass is score at least 75 and no hard failure.
    - Score 74 rejects, score 75 passes absent a hard failure, and score 100 with a hard failure rejects.
    - Canonical report bytes contain policy/threshold versions, all items, total, pass, and bounded reasons.
    - Canonical report bytes directly contain the selected full fingerprint plus complete WorkflowSpec and execution authorities, and reject cross-candidate/header swaps.
  </behavior>
  <action>
Define `QUALIFICATION_REPORT_SCHEMA_VERSION`, `QUALIFICATION_POLICY_VERSION`, default threshold `75`, `QualificationReportV1`, and one report constructor that consumes the five check results together with the selected full fingerprint, complete `WorkflowSpecAuthorityV1`, and complete prelookup `CandidateExecutionAuthorityV1`. Put all three direct authority bindings plus the report-schema and policy/threshold versions in the report header; validate that fingerprint and WorkflowSpec authority exactly match the corresponding execution-authority projection and reject any cross-candidate or stale-authority swap. Validate item uniqueness, exact expected check set, weights totaling 100, awarded points within each weight, total equality, and pass equality to `total_score >= 75 and not any(hard_failure)`. Include ordered itemized checks, total score, overall pass, and bounded aggregate reason codes. Provide canonical bytes/digest for ledger persistence. Reject hand-authored inconsistent reports. Add boundary, permutation/canonicalization, strict-schema, inconsistent-total, missing/duplicate check, hard-failure, full header-field sensitivity, and fingerprint/WorkflowSpec/execution-authority swap tests.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_qualification.py</automated>
  </verify>
  <acceptance_criteria>
    - Default threshold is exactly 75/100.
    - Report pass cannot disagree with the score/hard-failure rule.
    - Every check, score, version, result, and bounded reason is itemized.
    - The selected full fingerprint, WorkflowSpec authority, execution authority, and report-schema/policy/threshold versions are direct header fields and every mutation changes the report digest.
    - Cross-candidate fingerprint/WorkflowSpec/execution-authority combinations fail validation.
    - Canonical report digest is stable and malformed reports fail validation.
  </acceptance_criteria>
  <done>Qualification produces a complete auditable report and exact pass/reject decision before generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structured WorkflowSpec → qualification policy | Semantically extracted fields remain untrusted and must only drive bounded deterministic checks. |
| Qualification report → generator eligibility | A forged or inconsistent report could trigger an unauthorized model call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Tampering | Qualification totals/pass | high | mitigate | Strict constructor recomputes totals and pass; inconsistent reports are rejected. |
| T-03-17 | Elevation of Privilege | Unauthorized execution workflow | high | mitigate | Deterministic hard failure blocks generation regardless of score. |
| T-03-18 | Repudiation | Policy changes | medium | mitigate | Version constants, full itemization, and canonical report digest bind each decision. |
</threat_model>

## Artifacts this phase produces

- Versioned five-dimension qualification policy.
- Strict itemized report and canonical digest.
- Threshold and hard-failure regression suite.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Explain why a workflow qualifies | COVERED — report exposes every check and reason. |
| REQ | QUAL-01, QUAL-02 | COVERED exactly. |
| RESEARCH | 25/20/20/25/10 weights; 0.70 confidence floor; deterministic pre-generation gate | COVERED as versioned agent-discretion policy. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run both task commands independently with Gate B3. Review the exact 74/75 and hard-failure boundary cases.
</verification>

<success_criteria>
- Required checks are deterministic and exhaustively itemized.
- Threshold and hard-failure semantics cannot be forged.
- Qualification requires no LLM, validator, or raw-source access.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-07-SUMMARY.md` when done.
</output>
