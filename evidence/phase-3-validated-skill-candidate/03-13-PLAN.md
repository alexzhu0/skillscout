---
phase: 03-validated-skill-candidate
plan: 13
type: execute
wave: 12
depends_on:
  - 03-12
files_modified:
  - src/skillscout/cli.py
  - tests/test_cli_validate_skill.py
  - tests/test_cli_security.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - VAL-01
  - VAL-02
  - VAL-03
  - REV-01
  - REV-02
  - REV-03
user_setup:
  - service: openai
    why: "Real Generator and Reviewer requests use the Responses API; automated tests use injected transports."
    env_vars:
      - name: OPENAI_API_KEY
        source: "Inject as a runtime secret with the minimum project access; never place it in files, CLI arguments, logs, state, prompts, or output."
must_haves:
  truths:
    - "An additive argparse SafeArgumentParser subcommand accepts one bounded candidate descriptor, read-only Phase 2 state, separate Phase 3 state, and local output path."
    - "The CLI resolves source before creating Phase 3 state/output and emits sanitized machine-readable candidate_source_unavailable on any upstream failure."
    - "Eligible and rejected terminal summaries are projected locally; the command cannot publish, push, open/merge PRs, execute candidate code, install dependencies, or expose credentials."
    - "A completed exact-authority invocation is strictly read-only at the public boundary: it uses the separate retained-lock/O_RDONLY/private-memory projector, never constructs normal state or materialization, and preserves identical DB/WAL/SHM, lock, artifact/output path sets, exact bytes, and full lstat metadata."
    - "Failure injection after each Phase 3 stage proves checkpoint-bound resume and exact completed reuse through the public command."
  artifacts:
    - path: "src/skillscout/cli.py"
      provides: "Strict `skillscout build-candidate` command wiring read-only completed projection separately from mutable resume/materialization."
    - path: "tests/test_cli_validate_skill.py"
      provides: "Argparse happy path, all terminal branches, failure injection, mutable resume, and public completed-reuse DB/WAL/SHM/artifact/output full-metadata no-write tests."
    - path: "tests/test_cli_security.py"
      provides: "Path, link, secret, output, capability, no-Click import, and pre-run zero-effect tests."
  key_links:
    - from: "`skillscout build-candidate` descriptor"
      to: "PhaseThreeRunner"
      via: "Safe parsed paths, read-only source adapter, lazy Phase 3 state/output factories."
    - from: "CandidateTerminalSummaryV1"
      to: "local output directory/stdout"
      via: "New runs materialize once; completed reuse returns verified stored projections without invoking the writer."
    - from: "complete CandidateExecutionAuthorityV1"
      to: "CompletedCandidateProjector or MutableCandidateStateFactory"
      via: "CLI delegates to the completed-first Phase 3 root; only a verified clean miss may open mutable state/output."
  prohibitions:
    - "Do not accept a separate workflow fingerprint argument; it is already bound inside the strict descriptor."
    - "Do not import or use Click in SkillScout source; the existing argparse SafeArgumentParser remains the sole CLI framework."
    - "Do not expose RENDERER_VERSION or ELIGIBILITY_POLICY_VERSION as CLI options."
    - "Do not add GitHub write/publish/PR/merge or arbitrary shell/candidate execution options."
    - "Do not print secrets, raw repository content, prompts, responses, or unsanitized exceptions."
---

<objective>
Expose the complete Phase 3 pipeline as a safe local, resumable command.

Purpose: A human reviewer needs one auditable entrypoint that consumes a verified Phase 2 candidate and materializes only local immutable evidence.
Output: Strict CLI command and public-boundary functional/security tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/application/phase3.py
@src/skillscout/application/candidate_source.py
@src/skillscout/cli.py
@tests/test_cli_extract_repo.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the strict local build-candidate command</name>
  <files>src/skillscout/cli.py, tests/test_cli_validate_skill.py</files>
  <read_first>
    - `src/skillscout/cli.py` — follow the existing `argparse` `SafeArgumentParser`, additive subparser, bounded path, sanitized output, and dependency-injection conventions.
    - `src/skillscout/application/phase3.py` — invoke only the public Phase 3 root/runner.
    - `src/skillscout/application/candidate_source.py` — let the source loader own descriptor parsing and pre-run failure.
    - `tests/test_cli_extract_repo.py` — match public argparse CLI testing style without composing PhaseTwoProcessor.
    - `src/skillscout/domain/canonical.py` — emit bounded public evidence from the repository's existing canonical bytes; do not invent another serialization dialect.
    - `src/skillscout/domain/skill_artifacts.py` — use Plan 03-08's sole `materialize_skill_package` writer for new/resumable outputs and bypass it for completed projection.
    - `src/skillscout/adapters/state.py` — wire Plan 03-11's `DescriptorAnchoredCompletedCandidateProjector` before the normal mutable `SQLiteStateStore`.
    - `src/skillscout/adapters/localfs.py` — preserve descriptor-anchored path admission and the writer's fixed-mode/atomic-restore/fsync invariants.
    - `tests/test_state_integrity.py` — copy the named retained-lock/private-memory, parent-swap, fixed-private-file, pre-commit restore, post-commit cleanup, and directory-fsync invariants.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — follow the mapped CLI composition order and anchored writer contracts exactly.
  </read_first>
  <behavior>
    - `skillscout build-candidate` requires `--candidate`, `--phase2-state`, `--state`, and `--output`; optional `--fail-after` accepts only qualifier, generator, validator, or reviewer in test/injected mode.
    - It does not accept copied WorkflowSpec/raw repository text or a separate workflow fingerprint.
    - candidate_source_unavailable occurs before creating the Phase 3 DB or output directory.
    - Terminal output is bounded JSON containing outcome and exact external evidence paths/digests appropriate to the branch.
    - A completed lookup runs only through the existing-lock/O_RDONLY/O_NOFOLLOW/private-`:memory:` projector and does not instantiate normal state/output factories, invoke an artifact writer, create WAL/SHM, or mutate any path.
    - Across all terminal branches, completed CLI reuse preserves the complete watched path set, exact regular-file bytes, and full lstat metadata for DB/WAL/SHM presence, retained lock, artifact/terminal/report/attestation/package stores, output files, and every directory.
    - A verified completed miss closes the projector before a new/resumable command uses the existing mutable ledger and the sole anchored materializer.
  </behavior>
  <action>
Extend `build_parser()` in `src/skillscout/cli.py` by adding a sibling `build-candidate` parser through the existing `argparse` `SafeArgumentParser` subparser collection. Add required `--candidate`, `--phase2-state`, `--state`, and `--output` `Path` arguments and one closed `--fail-after` choice set containing qualifier, generator, validator, and reviewer for injected tests. Preserve `SafeArgumentParser.error`/`exit` so rejected input never echoes. Add the corresponding additive branch in `main()` and use the same sanitized `SafeFailure` JSON boundary and explicit state closing pattern as `extract-repo`; do not restructure or regress existing commands. Add an AST/import test over `src/skillscout` that asserts there is no `click` import and pin `cli.py` to the standard-library `argparse` framework.

Validate path arguments without following links; delegate descriptor byte/schema checks to `load_candidate_subject`. Open Phase 2 state read-only and resolve/reverify the source before constructing any Phase 3 projector, mutable state, service, or output factory. On `candidate_source_unavailable`, emit fixed bounded JSON and a documented nonzero pre-run status while leaving the Phase 3 state and output paths absent.

After complete authority construction, delegate to the Phase 3 root's separate completed-first flow. Instantiate only `DescriptorAnchoredCompletedCandidateProjector` initially: it may acquire the existing retained lock and descriptor-read the existing Phase 3 DB/artifacts, but the CLI must not construct normal `SQLiteStateStore` or any output/materializer object until the root returns a verified clean miss. A completed hit emits bounded metadata derived from the exact stored terminal/package/report/attestation byte projections and exits; an integrity/lock/admission failure closes with no writable fallback. A clean miss closes every projector DB/directory/lock descriptor before the mutable state factory opens a new/resumable run.

For a new/resumable run, call only Plan 03-08's `materialize_skill_package` and its sibling anchored evidence writers after terminal persistence. Reuse `canonical_json_bytes` for summary/report/attestation output and copy the mapped writer invariants exactly: descriptor-anchored parents/children; one retained private 0600 flock inode; private create-new temporaries; fixed 0644 package leaves, fixed 0600 evidence/lock/temp leaves, and 0700 directories; complete file writes; leaf fsync; dir-fd-relative atomic replace with canonical backup restore on any pre-commit failure; and fsync of each affected child/parent directory before success. Do not add a Path-based shortcut in `cli.py`.

For a completed exact-authority hit, return verified stored immutable projections directly and never call `materialize_skill_package`, an evidence/output writer, normal state factory, checkpoint/event writer, or cleanup/recovery writer. Even when the caller supplies a different absent output path, it remains absent. In public tests for all 12 terminal branches, snapshot before/after the recursive relative path set, exact regular-file bytes, and complete `os.lstat()` result for state DB, `-wal`, `-shm` whether present or absent, retained lock, artifact roots/files, terminal/summary/report/attestation/package files, output roots/files, and all directories. Require exact equality. Keep business rejections/refusals as successful closed command outcomes. Do not expose renderer/eligibility authority options or add any remote write/publish or arbitrary execution option.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_validate_skill.py</automated>
  </verify>
  <acceptance_criteria>
    - Exact command/options and closed `--fail-after` vocabulary are documented by `argparse` parser/help tests, and an import test proves SkillScout source has no `click` import.
    - Source failure creates no Phase 3 state/output path.
    - All 12 terminal branches emit bounded branch-appropriate local evidence.
    - Failure injection resumes from the next verified checkpoint through the mutable store and anchored writer; exact completed reuse returns stored projections through the separate projector with zero normal-state/writer calls and identical watched path sets, bytes, DB/WAL/SHM presence, and full lstat metadata.
  </acceptance_criteria>
  <done>A human can run one strict local command from verified Phase 2 candidate to exact Phase 3 terminal evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close CLI capability, path, and credential leaks</name>
  <files>src/skillscout/cli.py, tests/test_cli_security.py</files>
  <read_first>
    - `src/skillscout/cli.py` — test the public boundary implemented by Task 1.
    - `tests/test_cli_security.py` — extend the existing file rather than creating a parallel security suite.
    - `tests/test_cli_extract_repo.py` — copy `test_extract_repo_hostile_subjects_fail_closed_without_state` and `test_extract_repo_resume_and_idempotent_rerun` no-state/no-replay/no-echo patterns.
    - `AGENTS.md` — enforce no candidate execution, no automatic publishing/merge, and credential boundaries.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — use the CLI threat and injection corpus.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — apply its exact CLI composition-root and completed-projection order.
    - `src/skillscout/adapters/state.py` and `src/skillscout/adapters/localfs.py` — target the precise descriptor-open and mutation primitives in low-level spies.
    - `tests/test_state_integrity.py` — reuse the named private-memory/retained-lock/parent-swap/atomic-restore/fsync tests as the writable-control baseline.
  </read_first>
  <behavior>
    - Symlink, non-regular, traversal, oversized descriptor, aliased state/output, and unsafe overwrite attempts fail before effects.
    - CLI help and runtime expose no GitHub write/publish/merge, shell, install, candidate execution, or arbitrary tool capabilities.
    - Completed reuse leaves DB/WAL/SHM presence, retained lock, artifact/output recursive path sets, exact bytes, and complete lstat metadata of files/directories unchanged and never reaches normal state or an output writer.
    - Low-level sentinels observe zero writable/create/truncate/append opens, mkdir/write/pwrite/replace/rename/unlink/chmod/touch/utime/fsync calls, zero SQLite file targets/serialize/writes/WAL/SHM, and only `:memory:` projector connections.
    - A verified miss/resume control still reaches the mutable store and anchored materializer, proving the no-write restriction did not disable normal resume writes.
    - OPENAI_API_KEY and sentinel secrets never appear in stdout/stderr, logs, SQLite, prompts, package, provenance, reports, attestations, or terminal summaries.
    - Injection-shaped WorkflowSpec strings remain inert data and cannot change command behavior/capabilities.
  </behavior>
  <action>
Add adversarial CLI tests for symlinks, FIFOs/non-regular inputs, traversal/aliasing, descriptor and output bounds, unsafe pre-existing output, database/output path collision, and failure cleanup. Inspect `SafeArgumentParser` actions/help plus injected port calls to prove the command has no GitHub write/PR/merge, shell, dependency install, repository-script, candidate-file execution, or arbitrary tool route; an AST import-set assertion must explicitly reject any `click` import in `src/skillscout`. Seed environment and fixtures with unique credential markers and assert they are absent from captured stdout/stderr/log records, database bytes, Generator/Reviewer request payloads, package/provenance, validation, attestation, terminal, and output files. Exercise prompt-injection-shaped workflow fields and confirm exact stage/capability behavior remains unchanged.

For every completed terminal branch, run the CLI against the same authority and existing output, plus a separate absent output path. Snapshot the watched state/artifact/output universe recursively before/after: existence and sorted relative children; exact bytes for every regular file; and the full `os.lstat()` result for the Phase 3 DB, `-wal`, `-shm` whether present or absent, retained lock, artifact/terminal/summary/report/attestation/package roots and leaves, output roots/leaves, and all directories. Install sentinels on `os.open` to reject writable/create/exclusive/truncate/append flags; on mkdir/makedirs, write/pwrite, replace/rename, unlink/remove/rmdir, chmod/fchmod, `Path.touch`, utime/timestamp setters, fsync/fdatasync; on SQLite path/URI connections, attach/backup/serialize/mutating SQL; and on normal state, checkpoint/event, materializer, and output writers. Assert the complete snapshots are identical, the alternate output remains absent, every SQLite target is `:memory:`, DB sidecars remain absent/present exactly as before, and all mutation/writer counts remain zero.

Add a separate verified-miss interrupted-run control: prove projector resources close, normal mutable state opens, checkpoint-bound resume appends its required rows, and the mapped anchored materializer performs its fixed-mode atomic replace/restore and directory-fsync path. Any projector integrity/lock/admission error must remain fail-closed and never use this fallback. Keep errors fixed, bounded, and free of raw paths/content/exceptions.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_cli_security.py</automated>
  </verify>
  <acceptance_criteria>
    - Unsafe files/paths/overwrites fail before state, output, or semantic calls.
    - Completed reuse is strictly read-only: stored projections are returned through the descriptor projector while low-level spies remain zero and complete DB/WAL/SHM, lock, artifact/output path/byte/full-lstat snapshots are identical.
    - Verified miss/resume still performs the expected mutable ledger and anchored materializer writes; integrity failure never falls back.
    - No prohibited remote-write or execution capability is reachable from CLI.
    - Credential sentinels are absent across every listed channel and artifact.
    - Injection corpus cannot alter policy, stage order, tools, or outcome mapping.
  </acceptance_criteria>
  <done>The public command remains local, non-executing, non-publishing, secret-safe, and fail-closed under adversarial inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments/filesystem → Phase 3 | Paths and descriptor input may be malicious, linked, oversized, or aliased. |
| Runtime environment → logs/prompts/state/artifacts | Credentials must be consumed only by the SDK and never propagated. |
| Terminal evidence → local filesystem | Exact immutable bytes must be materialized without unsafe overwrite or remote publishing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-39 | Tampering | CLI paths/output | high | mitigate | Non-link/regular checks, alias prevention, bounded reads/writes, fail-before-effect tests. |
| T-03-40 | Information Disclosure | Credentials/diagnostics | high | mitigate | Sentinel scans across every output channel and fixed sanitized errors. |
| T-03-41 | Elevation of Privilege | CLI capabilities | high | mitigate | Closed options and ports expose no remote write, shell, install, or candidate execution. |
| T-03-42 | Tampering | Public resume/reuse | high | mitigate | Completed CLI reuse is routed through the existing-lock/O_RDONLY/private-memory projector and proven by complete DB/WAL/SHM/lock/artifact/output path-byte-lstat snapshots plus low-level no-write sentinels; mutable resume is a separate control. |
</threat_model>

## Artifacts this phase produces

- Safe local argparse `skillscout build-candidate` CLI with an explicit no-Click source import gate.
- Public happy-path/terminal/mutable-resume and descriptor-read completed-reuse tests with full filesystem immutability snapshots.
- Path, secret, injection, and capability adversarial tests.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Produce an explainable local reviewed Skill candidate | COVERED through one human-facing local command. |
| REQ | QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, REV-03 | COVERED at the public boundary. |
| RESEARCH | Strict descriptor CLI; pre-run barrier; local-only output; secret/injection controls; failure injection | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run both task commands independently with Gate B3. Confirm `skillscout build-candidate --help` contains only the exact approved options.
</verification>

<success_criteria>
- Public CLI runs/resumes/reuses the exact Phase 3 architecture.
- Source failures have zero Phase 3 effects.
- All outputs remain local, immutable, secret-safe, and free of publish/execution capabilities.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-13-SUMMARY.md` when done.
</output>
