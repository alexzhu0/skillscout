---
phase: 03-validated-skill-candidate
plan: 14
type: execute
wave: 13
depends_on:
  - 03-13
files_modified:
  - tools/verify_phase3_acceptance.py
  - tests/test_phase3_acceptance_tool.py
  - tests/test_phase3_bootstrap.py
  - tests/test_phase1_gap_closure.py
  - tools/verify_phase3_validation_map.py
  - tests/test_phase3_validation_map.py
  - .planning/phases/03-validated-skill-candidate/03-VALIDATION.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - VAL-01
  - VAL-02
  - VAL-03
  - REV-01
  - REV-02
  - REV-03
must_haves:
  truths:
    - "One dependency-free acceptance tool proves approved lock equality before importing project/dependency code."
    - "OpenAI imports exist in exactly three source adapters relative to src/skillscout: adapters/openai_extract.py, adapters/openai_generate.py, and adapters/openai_review.py."
    - "skills_ref imports exist only in adapters/skills_ref.py; no production route writes GitHub, executes candidate code, installs source dependencies, or bypasses local-only scope."
    - "Acceptance checks package tree/modes, secrets, complete provenance, provenance self-hash exclusion, external identities, exact reuse, protected Phase 1/2 seams, repository-local uv build --no-sources, Ruff, full pytest, and lock consistency."
    - ".planning/phases/03-validated-skill-candidate/03-VALIDATION.md maps every plan task to a literal runnable command, with Gate B3 prepended to every post-B3 dependency-backed command."
    - "A dependency-free validation-map checker admits every plan/map input through bounded no-follow descriptor reads, proves the exact 29-task plan/map bijection and all 13 explicit requirement IDs, and accepts only a closed hard-coded one-line release-command language without executing mapped commands."
  artifacts:
    - path: "tools/verify_phase3_acceptance.py"
      provides: "Standard-library acceptance inspector with independent bounded no-follow lock/hash authority verification, fixed check registry, and static import/capability/provenance/package/reuse architecture checks."
    - path: "tests/test_phase3_acceptance_tool.py"
      provides: "Positive and mutation tests for lock/hash admission, fixed registry, immutable inputs, anchored materialization, descriptor-read completed reuse, and each acceptance invariant."
    - path: "tests/test_phase1_gap_closure.py"
      provides: "Regression gates for protected Phase 1/2 loaders, processor, profile, and chain verifier seams."
    - path: "tools/verify_phase3_validation_map.py"
      provides: "Read-only standard-library checker for safely admitted Phase 3 planning inputs, the complete task/requirement map, a closed canonical command allowlist, and Gate-B3 release ordering."
    - path: "tests/test_phase3_validation_map.py"
      provides: "Temporary-copy positive and adversarial plan/map admission, table, command-grammar, parity-preserving shell-bypass, and Gate-B3 ordering tests."
    - path: ".planning/phases/03-validated-skill-candidate/03-VALIDATION.md"
      provides: "Literal Nyquist validation map and complete final command sequence."
  key_links:
    - from: "tools/verify_phase3_gate_b3.sh"
      to: "all final dependency-backed checks"
      via: "Approved exact lock equality is always the first command."
    - from: "source/package/state invariants"
      to: "tools/verify_phase3_acceptance.py"
      via: "Bounded descriptor-read authority, fixed registry, immutable pre/post inputs, AST/filesystem contracts, and focused runtime tests."
    - from: "all 29 task IDs enumerated literally in .planning/phases/03-validated-skill-candidate/03-VALIDATION.md"
      to: ".planning/phases/03-validated-skill-candidate/03-VALIDATION.md"
      via: "One literal runnable automated command per task."
    - from: "03-01-PLAN.md through 03-14-PLAN.md"
      to: "tools/verify_phase3_validation_map.py"
      via: "Bounded no-follow descriptor admission followed by strict extraction of task IDs, automated commands, plan requirements, canonical command sequences, and Gate B3 segments."
  prohibitions:
    - "Do not use unresolved symbolic path tokens, requirement ranges, ellipses, or shorthand commands in .planning/phases/03-validated-skill-candidate/03-VALIDATION.md."
    - "Do not run dependency-backed checks without first running the Gate B3 preflight."
    - "Do not accept a command merely because the PLAN and map agree; only the checker-owned canonical command allowlist and closed grammar may authorize it."
    - "Do not broaden the OpenAI importer allowlist beyond the exact three adapters."
---

<objective>
Create the final mechanical acceptance gate and literal validation map for Phase 3.

Purpose: Cross-cutting supply-chain, import isolation, immutable artifact, security, protected-seam, and exact-reuse guarantees need one reproducible release gate.
Output: Dependency-free acceptance tool, mutation tests, Phase 1/2 seam regressions, and complete `.planning/phases/03-validated-skill-candidate/03-VALIDATION.md`.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/03-validated-skill-candidate/03-VALIDATION.md
@config/supply-chain/phase3-gate-b3.lock.sha256
@tools/verify_phase3_gate_b3.sh
@src/skillscout
@tests
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dependency-free Phase 3 acceptance inspector</name>
  <files>tools/verify_phase3_acceptance.py, tests/test_phase3_acceptance_tool.py, tests/test_phase3_bootstrap.py, tests/test_phase1_gap_closure.py</files>
  <read_first>
    - `tools/verify_phase3_gate_b3.sh` — invoke it before the acceptance tool is run under uv; keep the inspector itself standard-library-only.
    - `tools/verify_phase1_gap_evidence.py` — directly reuse exact lowercase full-hash parsing, bounded regular non-link authority reads, immutable pre/post input comparison, fixed command registry, canonical evidence parsing, and verify-before-command ordering.
    - `tests/test_phase1_evidence_verifier.py` — copy the named stale bytes, unsafe/unbounded authority, duplicate/unexpected registry, forged exit/count/hash, immutable rerun, and canonical duplicate-key mutation cases.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — enumerate final acceptance and protected-seam invariants.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — implement its exact Gate-B3/acceptance, generation/materialization, ledger, and import-allowlist mappings.
    - `src/skillscout/domain/canonical.py` — inspect the sole canonical JSON/tagged-digest implementation that all Phase 3 identities must reuse.
    - `src/skillscout/adapters/localfs.py` — inspect no-follow stable descriptor reads and fixed-mode atomic write/restore/fsync primitives.
    - `src/skillscout/adapters/openai_extract.py` — retain the existing allowed OpenAI import location.
    - `src/skillscout/application/pipeline.py` and `src/skillscout/adapters/state.py` — snapshot protected profile/verifier seams for regression checks.
    - `tests/test_state_integrity.py` — require the named retained-lock/private-memory, private temporary, parent-swap, pre-commit restore, post-commit cleanup, stale-temp directory-fsync, and unsafe-output tests to remain mapped acceptance evidence.
    - `tests/test_phase2_pipeline.py` — preserve the mapped durable terminal writer and completed no-reexecution analogs.
    - `tests/test_phase1_gap_closure.py` — extend the existing exact source-importer confinement tests instead of adding a parallel allowlist implementation.
  </read_first>
  <behavior>
    - Standard-library AST scan requires the exact OpenAI source-importer set and exact sole skills_ref importer.
    - Before any project/dependency import or acceptance credit, the inspector parses exactly one lowercase full hash, bounded-reads the digest and uv.lock through no-follow stable descriptors, proves equality, and snapshots immutable authority bytes pre/post.
    - The acceptance registry is fixed, rejects duplicate/unexpected checks and forged exit/count/output-hash credit, and runs only after authority verification.
    - Static checks reject remote GitHub writes/PR/merge, candidate execution, source dependency installation, package scripts/binaries/executable modes, missing/future/self-referential provenance, and hidden secret markers.
    - Static/fixture checks prove GeneratedArtifactIdentityV1 and rendered package digest use distinct preimages, ValidationReport binds both, ReviewAttestation omits derived eligibility, and CandidateTerminalSummary owns eligibility/policy plus exact lineage/review companion statuses.
    - Protected RepositorySubject/load_subject, PhaseTwoProcessor, PIPELINE_PROFILES, and existing verify_run_chain regression contracts remain intact.
    - Static and focused tests require materialize_skill_package to reuse canonical.py plus retained lock, fixed modes, create-new temp, atomic replace/restore, and leaf/directory fsync; completed projector must instead use existing-lock O_RDONLY/private-memory/descriptor reads with zero mutation calls.
    - Every acceptance invariant has a mutation test that makes the inspector or focused regression fail.
  </behavior>
  <action>
Implement `tools/verify_phase3_acceptance.py` using only the Python standard library so it can inspect files/AST without importing project or third-party modules. Port the exact authority/evidence invariants from `tools/verify_phase1_gap_evidence.py` and its adversarial tests, with the stricter Phase 3 no-follow descriptor discipline: parse exactly one lowercase 64-hex approved digest plus newline; lstat digest and `uv.lock` without following links; require bounded regular single-link private inputs; open once with `O_RDONLY|O_NOFOLLOW|O_CLOEXEC`; compare path/fd identity; cap-plus-one read; require identical pre/post fd device/inode/mode/link/uid/gid/size/mtime-ns/ctime-ns; hash the retained descriptor bytes; and prove equality before any project/dependency import or acceptance credit. Record exact pre/post authority bytes and metadata and require equality after the full inspector. Use a fixed named check registry, reject duplicate/missing/unexpected check IDs, and bind each result to actual status/count/output digest; never accept self-asserted success.

Require the exact source OpenAI importer set relative to `src/skillscout`: `adapters/openai_extract.py`, `adapters/openai_generate.py`, and `adapters/openai_review.py`; reject missing or additional importers. Require `skills_ref` imports only in `adapters/skills_ref.py`. Inspect production AST/capability definitions for prohibited GitHub write/branch/PR/merge, arbitrary shell/subprocess, dependency-install, repository-script, and candidate-code execution routes while allowing the explicitly static official validator boundary. Validate fixture/output package tree/modes, mandatory `SKILL.md` and `references/provenance.json`, absence of scripts/binaries/executable modes/allowed-tools/secrets, complete Phase 2/source/lineage/qualification/producer/profile/retry/request/usage provenance authority, no Reviewer/future facts or package self-digest inside provenance, and external manifest/digest/attestation/terminal placement. Prove the semantic `GeneratedArtifactIdentityV1` preimage calls `domain/canonical.py` over canonical draft plus the explicit generation-only field projection and excludes the full CandidateExecutionAuthority, downstream validator/Reviewer/eligibility versions, request/usage evidence, and renderer-only path/mode/manifest bytes; prove the distinct package digest preimage is rendered path/hash/mode/size after provenance and therefore covers request/usage stored in provenance.

Mechanically enforce the mapped materialization boundary: `materialize_skill_package` is the sole local package writer; it uses descriptor-anchored parents/closed child names, one retained private lock inode, create-new private temporaries, fixed 0644 package/0600 evidence-lock-temp/0700 directory modes, full writes, leaf fsync, dir-fd-relative atomic replace with exact prior-byte restore on pre-commit failure, and every affected directory fsync. Require focused tests named after `test_new_state_lock_backup_temporary_and_manifest_files_are_private`, `test_state_uses_private_memory_sqlite_and_one_reusable_live_lock`, `test_parent_swap_after_state_anchor_cannot_redirect_state_or_manifests`, `test_pre_commit_snapshot_failure_restores_prior_authority`, `test_post_commit_backup_cleanup_failure_returns_success_and_reopen_observes_mutation`, `test_recover_stale_temporary_removes_private_temp_and_fsyncs_directory`, and `test_symlinked_output_directory_is_rejected_without_external_write`.

Separately enforce completed reuse architecture in Plans 03-11 through 03-13: `DescriptorAnchoredCompletedCandidateProjector` must open only an existing retained lock; bounded-read DB with O_RDONLY/O_NOFOLLOW; deserialize only into query-only `:memory:` SQLite; descriptor-read immutable artifact/terminal/report/attestation/package bytes; and never call a normal state store, SQLite file connection/serialize, WAL/SHM path, materializer, output writer, or filesystem mutation primitive. Require all-12-branch tests to compare complete DB/WAL/SHM/lock/artifact/output path sets, exact bytes, and full lstat metadata under low-level zero-write sentinels, plus a verified-miss control proving normal resume still takes the mutable atomic-write/fsync path.

Require QualificationReport and ValidationReport to carry their direct authority headers, ValidationReport to bind both artifact/package identities, ReviewAttestation to omit derived eligibility, and CandidateTerminalSummary to own `eligible`, `ELIGIBILITY_POLICY_VERSION`, Generator evidence, `not_evaluated_qualification_rejected`, and `review_skipped_validation_errors` in the exact branch matrix. Add mutations for every rule, including symlink/non-regular/oversized/hash authority, registry omission/duplication/forged credit, canonical serializer bypass, writer durability bypass, completed-projector write/file-SQLite regression, and mutable-resume suppression. Assert authority/read-only mutations fail before any registered downstream sentinel. In `tests/test_phase1_gap_closure.py`, lock protected Phase 1/2 public contracts/behavior for RepositorySubject/load_subject, PhaseTwoProcessor, `PIPELINE_PROFILES`, and existing `verify_run_chain` while permitting isolated additions.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_acceptance_tool.py tests/test_phase3_bootstrap.py tests/test_phase1_gap_closure.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked python tools/verify_phase3_acceptance.py</automated>
  </verify>
  <acceptance_criteria>
    - Acceptance inspector uses standard-library imports only and never imports project/dependency code.
    - Exact lowercase hash parsing, bounded no-follow stable descriptor reads, pre/post immutable authority snapshots, and verify-before-registry ordering are copied explicitly from the mapped Phase 1 tool/tests and strengthened for Phase 3.
    - Exact OpenAI and skills_ref importer sets are enforced.
    - Every prohibited capability, package/provenance/secret condition, and protected seam has a failing mutation test.
    - Distinct semantic/package identity preimages and report/attestation/terminal ownership rules have failing conflation/omission/mutation tests.
    - Static/focused gates enforce canonical.py reuse, retained-lock/fixed-mode/atomic-restore/directory-fsync materialization, and separate existing-lock/O_RDONLY/private-memory completed projection with all-branch zero-write snapshots.
    - Inspector emits bounded deterministic diagnostics and exits nonzero on any violation.
  </acceptance_criteria>
  <done>One mechanical inspector enforces the cross-cutting Phase 3 architecture and protected upstream seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize literal Nyquist validation and run the release gate</name>
  <files>tools/verify_phase3_validation_map.py, tests/test_phase3_validation_map.py, .planning/phases/03-validated-skill-candidate/03-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/03-validated-skill-candidate/03-VALIDATION.md` — update every task row to the implemented exact command if paths changed.
    - `tools/verify_phase1_gap_evidence.py` — mirror its standard-library root resolution, bounded deterministic diagnostics, and verify-without-command-execution discipline for the map checker.
    - `tests/test_phase1_evidence_verifier.py` — use its dynamic module import and isolated temporary-copy mutation style; never mutate the repository planning artifacts during a test.
    - `.planning/phases/03-validated-skill-candidate/03-01-PLAN.md` — map Task 03-01-01 exactly once.
    - `.planning/phases/03-validated-skill-candidate/03-02-PLAN.md` — map Task 03-02-01 exactly once.
    - `.planning/phases/03-validated-skill-candidate/03-03-PLAN.md` — map Task 03-03-01 exactly once.
    - `.planning/phases/03-validated-skill-candidate/03-04-PLAN.md` — map Task 03-04-01 exactly once.
    - `.planning/phases/03-validated-skill-candidate/03-05-PLAN.md` — map Tasks 03-05-01 and 03-05-02 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-06-PLAN.md` — map Tasks 03-06-01 and 03-06-02 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-07-PLAN.md` — map Tasks 03-07-01 and 03-07-02 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-08-PLAN.md` — map Tasks 03-08-01, 03-08-02, and 03-08-03 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-09-PLAN.md` — map Tasks 03-09-01, 03-09-02, and 03-09-03 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-10-PLAN.md` — map Tasks 03-10-01, 03-10-02, and 03-10-03 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-11-PLAN.md` — map Tasks 03-11-01, 03-11-02, and 03-11-03 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-12-PLAN.md` — map Tasks 03-12-01, 03-12-02, and 03-12-03 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-13-PLAN.md` — map Tasks 03-13-01 and 03-13-02 exactly once each.
    - `.planning/phases/03-validated-skill-candidate/03-14-PLAN.md` — map Tasks 03-14-01 and 03-14-02 exactly once each.
    - `pyproject.toml` — use only configured Ruff/pytest commands.
    - `tools/verify_phase3_gate_b3.sh` and `tools/verify_phase3_acceptance.py` — place supply-chain preflight before every dependency-backed check.
  </read_first>
  <behavior>
    - The standard-library checker safely admits the fixed 14 Phase 3 PLAN files plus `03-VALIDATION.md`, derives the literal 29 task IDs from filename and task ordinal, and rejects any missing, duplicate, unexpected, malformed, or mismatched map row/table.
    - HTML-decoded automated command values from each PLAN task's verify element, the corresponding map command, and a checker-owned hard-coded `EXPECTED_TASK_COMMANDS` entry are byte-for-byte equal; the sole non-auto-approvable `N/A` entry remains the Gate A3 decision.
    - The checker recomputes the exact inverse Requirement Coverage table from per-task rows, requires every explicit QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, and REV-03 identifier, and rejects shorthand, ranges, placeholders, ellipses, duplicate tables, or malformed table cells.
    - Every command is one strictly decoded physical line composed only of approved literal segments joined by the exact ` &amp;&amp; ` delimiter. The only non-B3 task exceptions are the hard-coded exact commands for 03-01-01, 03-02-01, and 03-03-01; every other task must match its finite canonical sequence, with the dependency-free map self-check permitted only as the first segment of the canonical 03-14-02 release sequence.
    - The canonical 03-14-02 row and Test Infrastructure Full suite command are byte-for-byte equal to `EXPECTED_RELEASE_COMMAND` after HTML decoding and have this ordered sequence: map self-check; Gate B3 then focused map test; Gate B3 then lock check; Gate B3 then repository-local `uv build --no-sources`; Gate B3 then acceptance inspector; Gate B3 then Ruff; Gate B3 then full pytest; terminal Gate B3 postflight immediately after pytest.
    - Temporary-copy mutations independently fail for duplicate/missing rows or tables, dropped requirements, command drift, unsafe input files, synchronized PLAN/map/full-suite shell-bypass edits that preserve parity, an absent final postflight, and a missing required preflight while the unmodified planning tree passes.
  </behavior>
  <action>
Create `tools/verify_phase3_validation_map.py` as a standard-library-only, read-only checker exposing `verify_validation_map(repository_root: Path) -> None` and `main(argv: Sequence[str]) -> int`. With no arguments it resolves the repository root from the tool location, emits only a fixed success line or bounded deterministic sorted failure diagnostics, and exits zero or one; tests may pass an explicit `--repository-root` temporary copy. It reads exactly `03-01-PLAN.md` through `03-14-PLAN.md` and `03-VALIDATION.md`, never imports `skillscout` or a third-party package, executes a mapped command, accesses the network, or writes a file. Admit each of those 15 inputs through one stdlib-only helper: use `lstat` without link following; require a regular, single-link file; fail closed if `O_NOFOLLOW` or `O_CLOEXEC` is unavailable; open once with exactly `O_RDONLY | O_NOFOLLOW | O_CLOEXEC`; compare pre-open path and descriptor device/inode/type/link identity; bounded-read at most a declared cap plus one byte from the retained descriptor; require pre/post descriptor device/inode/mode/link/size/mtime-ns/ctime-ns stability; re-`lstat` the path and require the same identity/metadata after reading; then strict-decode UTF-8. Use explicit small constants for a single PLAN and map byte cap, never `Path.read_text`, a glob, a fallback that follows links, or a best-effort decode.

Strictly parse exactly one Per-Task Verification Map, exactly one Requirement Coverage table, and exactly one Test Infrastructure Full suite command. Derive task IDs from filename plus task ordinal, require the hard-coded full 29-ID set and exact task-to-row bijection, HTML-decode both PLAN and Markdown command cells before byte-for-byte comparison, require plan/wave cells and each plan's map-requirement union to match source-plan frontmatter, and regenerate the exact 13-row Requirement Coverage inverse from map rows. Require the full explicit requirement ID set with no range or shorthand syntax, reject placeholders and ellipses, and allow the exact `N/A` command only for 03-01-01. Do not learn authorization from mutable input: hard-code `EXPECTED_TASK_COMMANDS` for all 29 task IDs and `EXPECTED_RELEASE_COMMAND`; every decoded source PLAN command and map command must equal its canonical entry. The only canonical non-B3 task entries are exactly: 03-01-01's documented `N/A` decision, 03-02-01's exact three-command lock discovery/check/tree chain, and 03-03-01's exact `git diff --check -- pyproject.toml uv.lock` command. The exact dependency-free `PYTHONDONTWRITEBYTECODE=1 python3 tools/verify_phase3_validation_map.py` self-check is allowed only as the first segment of `EXPECTED_RELEASE_COMMAND`, never as a stand-alone substitute for a B3 task.

Implement a closed one-line command grammar before comparing command roles: require no leading/trailing or normalized whitespace; permit only canonical literal segments joined by the exact ` &amp;&amp; ` delimiter; and reject `||`, `;`, `|`, `|&amp;`, every `&amp;` not belonging to that delimiter, LF, CR, every other C0/DEL control, backticks, `$(`, `${`, `$$`, every dollar form other than canonical `$PWD`, redirections (`&gt;`, `&lt;`, and descriptor variants), process substitutions (`&lt;(` and `&gt;(`), heredoc introducers (`&lt;&lt;` and `&lt;&lt;-`), comments (`#`), backslash escapes, and any unapproved quote/glob/expansion form. No parser may split on a permissive shell token, pass a command to a shell, use `eval`, or accept a fragment that is not an expected exact segment. For every canonical dependency-backed segment, require the immediately previous segment to be the exact `sh tools/verify_phase3_gate_b3.sh`; this includes the focused map test, lock check, build, acceptance inspector, Ruff, and full pytest. Require the 03-14-02 map row and Test Infrastructure Full suite command to equal `EXPECTED_RELEASE_COMMAND` after HTML decoding. Preserve the existing literal release command in this task's verification element and the Validation map byte-for-byte: map self-check; B3/map test; B3/lock check; B3/`uv build --no-sources`; B3/acceptance; B3/Ruff; B3/full pytest; terminal B3 postflight.

Create `tests/test_phase3_validation_map.py` before the helper implementation. It copies the Phase 3 plan directory and validation map into a temporary repository-shaped fixture, proves the unmodified copy succeeds through the public helper interface, and mutates only the copy. Add isolated failures for duplicate and missing rows, duplicate/malformed tables, one dropped explicit requirement from the recomputed inverse, and PLAN/map command drift. Add parity-preserving triple mutations that change Plan 03-14 Task 2's automated command, its 03-14-02 map row, and the Full suite command together: append `|| true`, append a semicolon chain, add `|` and `|&amp;` chains, and inject numeric-entity LF/CR/tab plus raw escape control variants; prove the three decoded commands remain equal and the checker rejects every case. Parameterize equivalent synchronized mutations for backticks, `$(`, redirection, process-substitution, heredoc, comment, and escape forms. Add mutation cases that remove the terminal post-pytest B3 check from both mirrored release commands and remove one required B3 preflight from both relevant PLAN/map commands. Exercise input admission with a symlink, directory/non-regular input, cap-plus-one oversize input, invalid UTF-8, and controlled atomic pre-open and post-read swaps; all must fail closed. The table parser must reject an added duplicate Per-Task or Requirement Coverage table. Finalize `.planning/phases/03-validated-skill-candidate/03-VALIDATION.md` from the same canonical literals: regenerate all 13 Requirement Coverage rows from the map and keep its Full suite command and 03-14-02 row exactly mirrored with this task's automated release command. The checker owns only the helper, its tests, and the validation map; it never rewrites upstream plan commands. Record the exact commands and successful evidence in the plan summary; do not weaken checks to make failures pass.
  </action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 tools/verify_phase3_validation_map.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_validation_map.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" build --no-sources &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked python tools/verify_phase3_acceptance.py &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked ruff check . &amp;&amp; sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q &amp;&amp; sh tools/verify_phase3_gate_b3.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 29 Phase 3 tasks has a literal validation-map row.
    - All 13 requirement IDs appear explicitly in the validation coverage.
    - The validation-map checker admits all 15 planning inputs through bounded no-follow stable descriptor reads and its temporary-copy mutations prove unsafe file, duplicate-table, map-row uniqueness, explicit requirement, and strict UTF-8 failures close before command parsing or execution.
    - The checker-owned canonical command allowlist and closed one-line grammar reject synchronized PLAN/map/full-suite bypass edits as well as ordinary command drift; no command can gain release credit merely by preserving parity.
    - Every post-B3 dependency-backed command segment has the immediately preceding exact preflight.
    - Lock check, repository-local `uv build --no-sources`, acceptance inspector, Ruff, and full pytest all pass without changing approved lock bytes, as proven by the terminal post-pytest Gate B3 check.
  </acceptance_criteria>
  <done>Phase 3 has complete mechanically verified literal Nyquist coverage and passes the final approved-lock-bound release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository implementation → acceptance inspector | Static architecture/capability guarantees must be checked without executing untrusted/project code. |
| Validation documentation → release commands | Ambiguous or unguarded commands could silently bypass supply-chain approval or coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-43 | Tampering | Import/capability architecture | high | mitigate | Exact AST importer allowlists and prohibited-capability mutation tests. |
| T-03-44 | Information Disclosure | Package/provenance/secrets | high | mitigate | Mechanical package/provenance/secret scans plus runtime sentinels. |
| T-03-45 | Tampering | Validation commands and planning inputs | high | mitigate | Bounded no-follow plan/map admission plus a closed checker-owned command allowlist rejects malformed/synchronized bypass commands while enforcing the literal task bijection, coverage inverse, and Gate B3 segment placement. |
| T-03-46 | Elevation of Privilege | Upstream seam regression | high | mitigate | Dedicated protected Phase 1/2 contract/behavior tests. |
</threat_model>

## Artifacts this phase produces

- Dependency-free final acceptance inspector.
- Mutation suite and protected Phase 1/2 regression gates.
- Dependency-free validation-map checker with safe planning-input admission, canonical command grammar, and isolated mutation suite.
- Literal complete `.planning/phases/03-validated-skill-candidate/03-VALIDATION.md` and final release command sequence.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Safe, source-clear, independently reviewed local Skill with explainable evidence | COVERED by final cross-cutting acceptance. |
| REQ | QUAL-01, QUAL-02, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, VAL-01, VAL-02, VAL-03, REV-01, REV-02, REV-03 | COVERED explicitly in validation map and full suite. |
| RESEARCH | Exact import allowlists; protected seams; approved-lock preflight; package/provenance/secrets; final commands | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run Task 1 and Task 2 commands exactly. The canonical map self-check is the only dependency-free preflight exception and only appears as the first segment of the exact release sequence; do not run any dependency-backed subcommand without a fresh leading Gate B3 preflight.
</verification>

<success_criteria>
- Cross-cutting architecture and security invariants are mechanically enforced.
- Protected Phase 1/2 behavior remains intact.
- Every task/requirement has literal validation coverage, mechanically compared against safely admitted source PLAN files and a checker-owned canonical command contract.
- Final lock, repository-local build, acceptance, Ruff, and full test gates pass under approved bytes.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-14-SUMMARY.md` when done.
</output>
