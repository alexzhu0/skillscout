---
phase: 03-validated-skill-candidate
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - config/supply-chain/phase3-gate-b3.lock.sha256
  - tools/verify_phase3_gate_b3.sh
  - tests/test_phase3_lock_preflight.py
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "The repository commits the one uv.lock SHA-256 approved at Gate B3."
    - "A dependency-free preflight fails closed before package-backed commands whenever uv.lock is missing, malformed, or byte-different."
    - "Every Phase 3 post-B3 uv/import/test/lint/validator command can begin with the same preflight."
  artifacts:
    - path: "config/supply-chain/phase3-gate-b3.lock.sha256"
      provides: "Exactly one approved lowercase SHA-256 plus newline."
    - path: "tools/verify_phase3_gate_b3.sh"
      provides: "Dependency-free lock equality gate."
    - path: "tests/test_phase3_lock_preflight.py"
      provides: "Mutation, deletion, malformed digest, and success coverage."
  key_links:
    - from: "03-03-SUMMARY.md approved digest"
      to: "config/supply-chain/phase3-gate-b3.lock.sha256"
      via: "Executor copies the exact approved digest, not a newly chosen value."
    - from: "tools/verify_phase3_gate_b3.sh"
      to: "every later dependency-backed command"
      via: "The command exits successfully only when current uv.lock bytes equal the approved digest."
  prohibitions:
    - "Do not compute a replacement approved digest after Gate B3."
    - "Do not import Python packages or invoke uv from the preflight script itself."
---

<objective>
Turn the Gate B3 decision into a committed, dependency-free lock equality control.

Purpose: The human-reviewed graph must remain exactly the graph executed by all later Phase 3 commands.
Output: Approved digest, portable preflight, and tests proving fail-closed behavior.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the approved Gate B3 lock identity</name>
  <files>config/supply-chain/phase3-gate-b3.lock.sha256, tools/verify_phase3_gate_b3.sh, tests/test_phase3_lock_preflight.py</files>
  <read_first>
    - `.planning/phases/03-validated-skill-candidate/03-03-SUMMARY.md` — extract the exact digest from the literal Gate B3 approval.
    - `uv.lock` — hash the current bytes and confirm they still equal the approved digest before writing anything.
    - `pyproject.toml` — follow existing test and script conventions.
    - `tools/verify_phase1_gap_evidence.py` — copy `_is_sha256`'s exact lowercase full-hash grammar, `_read_regular_bytes`' bounded regular/non-link authority admission, immutable pre/post byte comparison, fixed command registry, and verify-before-command ordering.
    - `tests/test_phase1_evidence_verifier.py` — copy the named stale-byte, missing/symlink/non-regular/oversized authority, duplicate/unexpected registry, forged result, and rerun-with-immutable-input cases.
    - `src/skillscout/adapters/localfs.py` — reuse `AnchoredDirectory.read_bytes`' no-follow descriptor open, path-to-fd identity check, cap-plus-one read, and post-read stable-fstat invariant without importing project code into the shell gate.
    - `tests/test_state_integrity.py` — mirror `test_existing_state_requires_private_permissions_before_deserialize`, `test_existing_state_requires_one_link`, and `test_oversized_serialized_state_fails_before_sqlite_deserialize` for fail-before-consumer ordering.
  </read_first>
  <behavior>
    - The preflight succeeds when `uv.lock` hashes to the one committed approved digest.
    - It fails before dependency execution when the lock is absent, the digest file is malformed, or one lock byte changes.
    - It admits the digest file and `uv.lock` only through bounded regular non-link no-follow descriptor reads with stable path/fd metadata, and parses exactly one lowercase 64-hex digest plus newline.
    - It reads only repository files and uses operating-system hashing tools; it does not import project code or call uv.
  </behavior>
  <action>
First compare the current `uv.lock` SHA-256 to the exact digest recorded by Gate B3; stop if they differ. Commit that approved lowercase digest, and only that digest followed by a newline, in `config/supply-chain/phase3-gate-b3.lock.sha256`.

Implement `tools/verify_phase3_gate_b3.sh` as a portable dependency-free shell gate that ports the authority invariants from `tools/verify_phase1_gap_evidence.py` and its adversarial test suite: exact lowercase 64-hex parsing; one bounded regular non-link input; immutable admitted bytes; a fixed consumer; and authority verification before consumer execution. Strengthen both the digest-file and `uv.lock` reads with the repository's `AnchoredDirectory.read_bytes` discipline while remaining shell/OS-tool-only: reject unavailable secure descriptor primitives; lstat without following links; open once read-only with no-follow and close-on-exec semantics; compare path and descriptor device/inode/type/link/owner/mode identity; read at most the declared cap plus one from that retained descriptor; require pre/post descriptor size/mtime/ctime stability; and hash the admitted descriptor stream rather than reopening the pathname. Resolve repository-relative paths, compute SHA-256 with an available operating-system utility, compare exact values, emit no secrets or lock contents, and exit nonzero on every mismatch or missing input.

Name and mirror the exact existing evidence-verifier cases in `tests/test_phase3_lock_preflight.py`: stale bound bytes; missing, symlink, non-regular, and oversized authority; malformed/uppercase/extra-line hash; duplicate or unexpected authority claim; and forged-success evidence. Add downstream sentinel cases proving the following executable is never invoked for any admission/hash failure and a success case proving it runs exactly once. Make the script executable. Every automated verify command in this and all later plans must begin with this script.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_lock_preflight.py</automated>
  </verify>
  <acceptance_criteria>
    - The committed digest exactly equals the Gate B3-approved `uv.lock` SHA-256.
    - The script has no Python or uv dependency, parses exactly one lowercase full hash, uses bounded no-follow descriptor reads with pre/post identity stability, and exits before any dependency-backed action on admission/hash mismatch.
    - Tests cover exact success, one-byte mutation, missing lock, missing digest, symlink/non-regular/oversized authority, malformed/uppercase/extra-line digest, path/fd or post-read mutation, downstream sentinel suppression, and non-executable-script invocation through `sh`.
    - The script itself is executable in the repository.
  </acceptance_criteria>
  <done>The approved lock identity is committed and every later Phase 3 command has a single fail-closed preflight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-approved digest → repository control | The approved external decision becomes a deterministic local invariant. |
| Current uv.lock → dependency-backed command | Lock bytes must pass equality before external package code can run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `uv.lock` after Gate B3 | high | mitigate | Dependency-free SHA-256 equality preflight blocks every later command. |
| T-03-07 | Tampering | Approved digest file | high | mitigate | Strict one-digest grammar, committed reviewable bytes, and tests for malformed input. |
| T-03-08 | Information Disclosure | Preflight diagnostics | low | mitigate | Emit paths and status only; never print lock contents or environment values. |
</threat_model>

## Artifacts this phase produces

- Committed approved lock digest.
- Reusable dependency-free Gate B3 preflight.
- Automated mutation coverage for the supply-chain boundary.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Safe, repeatable local validation | COVERED — later execution is bound to approved bytes. |
| REQ | VAL-01 | COVERED — establishes the official-validator execution prerequisite. |
| RESEARCH | Gate B3 exact-lock hash; preflight before every command | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run the task command exactly. Then run `sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" lock --check`; both must pass without changing `uv.lock`.
</verification>

<success_criteria>
- The approved digest and current lock match exactly.
- A one-byte lock mutation blocks before uv/Python execution.
- All later plans can uniformly prefix dependency-backed commands with the preflight.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-04-SUMMARY.md` when done.
</output>
