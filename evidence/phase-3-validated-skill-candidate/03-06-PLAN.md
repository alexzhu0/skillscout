---
phase: 03-validated-skill-candidate
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
  - 03-04
files_modified:
  - src/skillscout/application/candidate_source.py
  - src/skillscout/adapters/phase2_state.py
  - src/skillscout/application/ports.py
  - tests/test_candidate_source.py
autonomous: true
requirements:
  - GEN-04
  - GEN-05
must_haves:
  truths:
    - "Phase 3 accepts only a small strict candidate descriptor that points to one already-completed Phase 2 run and one full workflow fingerprint."
    - "Descriptor derivation sorts the complete full fingerprints deterministically, emits at most three descriptors, and preserves independent authority, calls, ledger, artifacts, and outcome for each sibling."
    - "The descriptor loader admits only a private single-link regular file owned by the effective UID, uses descriptor-based no-follow/nonblocking/close-on-exec reads, path-to-fd and post-read identity checks, a cap-plus-one byte read, and exact canonical UTF-8 JSON/Pydantic bytes before the read-only Phase 2 query."
    - "The loader queries Phase 2 state read-only, reverifies the existing canonical chain, selects exactly one matching workflow, recomputes complete WorkflowSpec authority, and compares the descriptor anchor."
    - "Every upstream rejection/refusal/incomplete/schema failure, missing/duplicate result, chain failure, or authority mismatch returns sanitized candidate_source_unavailable before any Phase 3 lookup, row, model call, validator call, or output write."
    - "Existing RepositorySubject/load_subject and PhaseTwoProcessor behavior remain unchanged."
  artifacts:
    - path: "src/skillscout/application/candidate_source.py"
      provides: "Strict bounded descriptor loader and pre-run source resolution."
    - path: "src/skillscout/adapters/phase2_state.py"
      provides: "Read-only query adapter over completed Phase 2 state."
    - path: "src/skillscout/application/ports.py"
      provides: "PhaseTwoCandidateSource protocol and sanitized source-unavailable contract."
    - path: "tests/test_candidate_source.py"
      provides: "Private-owner/canonical-byte admission, hostile-file, authority mutation, upstream outcome, chain, ambiguity, and zero-side-effect tests."
  key_links:
    - from: "CandidateSubjectDescriptorV1"
      to: "SQLitePhaseTwoCandidateSource.resolve"
      via: "Bounded descriptor selects a completed Phase 2 identity and full fingerprint."
    - from: "Verified Phase 2 output"
      to: "ResolvedCandidateSourceV1"
      via: "Existing chain verification and complete authority equality."
  prohibitions:
    - "Do not widen src/skillscout/domain/subjects.py or src/skillscout/adapters/subjects.py."
    - "Do not compose or modify PhaseTwoProcessor."
    - "Do not create any Phase 3 state on candidate_source_unavailable."
---

<objective>
Create the strict read-only bridge from a completed Phase 2 workflow to Phase 3.

Purpose: Phase 3 must consume verified structured output, never raw repository content or an unverified copied WorkflowSpec, and must fail before creating state when its source is unavailable.
Output: Bounded descriptor loader, read-only Phase 2 query port/adapter, sanitized failure contract, and zero-side-effect adversarial tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/subjects.py
@src/skillscout/adapters/subjects.py
@src/skillscout/adapters/state.py
@src/skillscout/application/ports.py
@tests/test_phase2_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose a read-only completed-Phase-2 query seam</name>
  <files>src/skillscout/application/ports.py, src/skillscout/adapters/phase2_state.py, tests/test_candidate_source.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` — reuse its public Phase 2 projections and `verify_run_chain` without changing that verifier.
    - `src/skillscout/application/ports.py` — follow existing Protocol and sanitized error conventions.
    - `tests/test_phase2_pipeline.py` — reuse canonical completed/rejected/refusal fixtures.
    - `src/skillscout/domain/models.py` — use existing terminal enums and exact stored result types.
  </read_first>
  <behavior>
    - Query methods open Phase 2 state read-only and cannot insert/update/delete.
    - Exactly one completed canonical result for the requested run/fingerprint is returned only after existing chain verification succeeds.
    - Rejected, no_workflow, refusal, incomplete, schema failure, missing, duplicate, and broken-chain states close with a bounded source-unavailable error.
  </behavior>
  <action>
Add a narrow `PhaseTwoCandidateSource` Protocol to `src/skillscout/application/ports.py` and implement the `SQLitePhaseTwoCandidateSource.resolve` method in the new `src/skillscout/adapters/phase2_state.py` adapter using SQLite read-only mode. Query by the strict descriptor's completed Phase 2 run identity, then invoke the existing Phase 1/2 canonical chain verifier exactly as implemented; do not copy, bypass, or loosen it. Select exactly one extracted workflow by full fingerprint and return its exact canonical stored bytes plus the source-chain authority needed for complete recomputation. Reject all non-success terminal outcomes, missing rows, duplicate fingerprint rows, inconsistent run identity, invalid canonical bytes, and failed chain verification through one sanitized `CandidateSourceUnavailable` boundary. If a common error enum is required, add `CANDIDATE_SOURCE_UNAVAILABLE` with a fixed safe summary to `src/skillscout/application/ports.py`; never include raw workflow/repository text, SQL, paths, or exception details.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_candidate_source.py -k phase2_query</automated>
  </verify>
  <acceptance_criteria>
    - Adapter uses read-only SQLite access and exposes no mutation method.
    - Existing `verify_run_chain` remains unchanged and gates every successful projection.
    - Every non-success/ambiguous/corrupt case maps to one sanitized source-unavailable result.
    - Tests prove Phase 2 tables are byte/count unchanged by resolution.
  </acceptance_criteria>
  <done>A completed Phase 2 workflow can be queried and reverifed without mutating upstream state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load bounded descriptors and compare complete source authority before Phase 3</name>
  <files>src/skillscout/application/candidate_source.py, src/skillscout/application/ports.py, tests/test_candidate_source.py</files>
  <read_first>
    - `src/skillscout/domain/candidate_authority.py` — use strict descriptor and complete authority constructors from Plan 03-05.
    - `src/skillscout/adapters/subjects.py` — mirror safe bounded file-loading behavior without changing the existing loader.
    - `src/skillscout/adapters/localfs.py` — mirror the effective-UID, single-link, private regular-file admission predicate.
    - `src/skillscout/domain/canonical.py` — compare validated descriptor content against the repository's exact canonical JSON bytes.
    - `tests/test_phase2_contracts.py` — mirror the complete hostile-file, swap, bounded-read, strict-parse, and no-echo matrix.
    - `tests/test_state_integrity.py` — mirror foreign-owner, owner-check-unavailable, hard-link, and unsafe-mode cases.
    - `src/skillscout/application/ports.py` — connect to Task 1's query Protocol.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — follow the descriptor-first pre-run barrier.
  </read_first>
  <behavior>
    - Descriptor loading rejects unavailable owner checks, foreign ownership, group/other permission bits, multiple hard links, path-to-fd swaps, post-open metadata/content mutation, FIFO/non-regular files, symlinks, oversize-by-stat and oversize-by-read inputs, invalid UTF-8, malformed or non-canonical JSON, wrong schema, extras, or incomplete anchors before query.
    - Reads use `O_RDONLY` plus available `O_NOFOLLOW`, `O_NONBLOCK`, and `O_CLOEXEC`; both path and descriptor metadata must be private, single-link, regular, and effective-UID-owned; the path-to-fd identity and complete pre/post-fstat device/inode/mode/link-count/owner/group/size/mtime-ns/ctime-ns tuple must remain stable.
    - After strict UTF-8, JSON, and Pydantic validation, the original bytes equal `canonical_json_bytes(validated_descriptor.model_dump(mode="json"))` exactly, rejecting whitespace, key-order, duplicate-key, or alternate-encoding representations.
    - Descriptor derivation sorts by the complete fingerprint bytes, emits no more than three descriptors, and is stable across upstream row/order permutations.
    - The resolved exact WorkflowSpec is parsed strictly and its complete authority is recomputed and compared to the descriptor anchor.
    - Mutating every non-fingerprint WorkflowSpec field, evidence/source authority field, terminal anchor, or chain anchor fails closed.
    - Failure occurs before any Phase 3 lookup/table/file/call/validator interaction.
    - Resolving or failing one sibling descriptor does not change another sibling's authority, calls, ledger key, artifact projection, or outcome.
  </behavior>
  <action>
Create `src/skillscout/application/candidate_source.py` with exported `MAX_CANDIDATE_DESCRIPTOR_BYTES`, `MAX_CANDIDATE_DESCRIPTORS = 3`, `ResolvedCandidateSourceV1`, `CandidateSourceUnavailable`, and functions named `load_candidate_subject` and `derive_candidate_subject_descriptors`. Implement `load_candidate_subject` as its own descriptor-based safe loader. Require a callable successful `os.geteuid`; lstat first and admit only a non-link regular file with `st_nlink == 1`, `st_uid == os.geteuid()`, and no group/other permission bits (`st_mode &amp; 0o077 == 0`). Open with `O_RDONLY` plus platform-available `O_NOFOLLOW`, `O_NONBLOCK`, and `O_CLOEXEC`; require the same private single-link regular-file checks on fstat, exact lstat-to-fstat device/inode/mode/link-count/owner/group identity, and a declared size within the cap. Read from the retained descriptor in bounded chunks until EOF or exactly `MAX_CANDIDATE_DESCRIPTOR_BYTES + 1` bytes have been observed; reject the extra byte. Before parsing, require the complete pre-read and post-read fstat device/inode/mode/link-count/owner/group/size/mtime-ns/ctime-ns tuple to remain identical and reapply the private-file predicate. Decode strict UTF-8, parse JSON, and call strict Pydantic validation for exactly one `CandidateSubjectDescriptorV1`, with no fallback coercion; then require the original input bytes to equal `canonical_json_bytes(validated_descriptor.model_dump(mode="json"))` exactly so whitespace, key-order, duplicate-key, and alternate representation changes are rejected. Close the descriptor on every branch. Collapse unavailable/failed owner checks, foreign ownership, unsafe permissions, multiple hard links, path swaps, post-open metadata/content mutation, FIFO/non-regular files, symlinks, oversize-by-stat, oversize-by-stream, invalid UTF-8, malformed/non-canonical JSON, schema errors, and operating-system errors to the single bounded `candidate_source_unavailable` surface without echoing descriptor bytes, canaries, raw paths, exceptions, or parser diagnostics. Add explicit owner-check-unavailable, foreign-owner, group/other-permission, hard-link, path-to-fd swap, post-read metadata change, FIFO, symlink, oversize, non-canonical JSON, duplicate-key, malformed, and no-echo cases modeled on the complete Phase 2 loader and private-file matrices, including fakes that lie about `st_size` so the cap-plus-one read is independently proven.

After safe loading, call the read-only source port, strictly reconstruct the exact Phase 2 WorkflowSpec from canonical bytes, recompute the complete `WorkflowSpecAuthorityV1`, and use constant-shape equality against the descriptor's complete anchor. Preserve an optional prior-lineage-binding digest as opaque authority input only; source resolution must not open/search Phase 3 state or infer a binding. For descriptor derivation, reverify the completed Phase 2 result once, derive one strict descriptor per canonical WorkflowSpec with no prior binding by default (an explicit caller-supplied exact fingerprint→approved-binding-digest mapping is the only way to attach one), sort lexicographically by the complete full-fingerprint bytes, and emit the first at most three; source row or input ordering must not affect the selected descriptors or bytes. Return only the resolved structured source and authorities needed by Phase 3. All rejected/no_workflow/refusal/incomplete/schema failure/missing/duplicate/fingerprint ambiguity/chain failure/parse failure/authority mismatch paths must collapse to sanitized `candidate_source_unavailable`. Instrument tests with three sibling descriptors and per-sibling spies/keys: success, semantic rejection, and malformed/mismatched source must retain independent execution authority, lookup/call counts, ledger namespace, artifact projection, and outcome; one sibling must never suppress, reuse, mutate, or inherit another sibling's effects. Also prove a fourth workflow is deterministically excluded by the full-fingerprint sort/cap, absence never searches for a binding, and only an exact full-fingerprint mapping attaches a binding digest. Preserve `RepositorySubject`, `load_subject`, and `PhaseTwoProcessor` byte-for-byte.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_candidate_source.py</automated>
  </verify>
  <acceptance_criteria>
    - Descriptor loading proves effective-UID/private-mode/single-link admission, descriptor flags, path-to-fd identity, cap-plus-one bounded reads, complete post-read fstat stability, strict UTF-8/JSON/Pydantic parsing, and exact canonical JSON bytes; owner-check/foreign-owner/permission/hard-link/swap/FIFO/symlink/oversize/non-canonical/malformed/no-echo tests fail before query.
    - Descriptor derivation is full-fingerprint sorted, stable under order permutation, capped at exactly three, and has a tested fourth-candidate exclusion boundary.
    - Complete authority is recomputed from exact upstream canonical bytes.
    - Parameterized tests mutate every non-fingerprint authority field and fail before Phase 3 side effects.
    - Three sibling cases prove independent authority, lookup/calls, ledger namespace, artifact projection, and terminal/pre-run outcome.
    - Protected Phase 1/2 subject and processor APIs are unchanged.
  </acceptance_criteria>
  <done>Phase 3 receives exactly one fully verified structured workflow or closes pre-run with zero Phase 3 effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Descriptor file → loader | Local input can be foreign-owned, permission-exposed, multiply linked, non-canonical, oversized, swapped, malformed, or crafted to misbind authority. |
| Phase 2 database → Phase 3 | Stored rows are not trusted until the existing canonical chain and complete authority are reverified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | Candidate descriptor | high | mitigate | Effective-UID private single-link admission, no-follow descriptor reads, complete metadata stability, exact canonical JSON bytes, and complete authority equality. |
| T-03-13 | Spoofing | Phase 2 workflow selection | high | mitigate | Select exactly one full fingerprint within a verified completed run. |
| T-03-14 | Information Disclosure | Source resolution errors | medium | mitigate | Fixed sanitized `candidate_source_unavailable` boundary. |
| T-03-15 | Elevation of Privilege | Upstream-state mutation | high | mitigate | Dedicated read-only adapter and invariant tests for unchanged rows/bytes. |
</threat_model>

## Artifacts this phase produces

- Strict Phase 3 descriptor loader.
- Read-only Phase 2 query adapter.
- Complete-source-authority and zero-side-effect adversarial tests.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Source-clear local Skill derived from verified workflow | COVERED — only canonical Phase 2 structured output crosses the boundary. |
| REQ | GEN-04, GEN-05 | COVERED — exact source authority and stable selected workflow identity. |
| RESEARCH | Descriptor model; owner/link/size/canonical-JSON discipline; source-unavailable pre-run barrier; protected Phase 2 seams | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run both task commands independently with the Gate B3 preflight. Confirm `git diff -- src/skillscout/domain/subjects.py src/skillscout/adapters/subjects.py` is empty.
</verification>

<success_criteria>
- No unverified or ambiguous Phase 2 output reaches Phase 3.
- Every source failure leaves Phase 3 state, calls, validators, and outputs untouched.
- Existing Phase 1/2 behavior is not widened or relaxed.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-06-SUMMARY.md` when done.
</output>
