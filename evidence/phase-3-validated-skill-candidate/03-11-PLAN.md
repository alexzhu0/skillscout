---
phase: 03-validated-skill-candidate
plan: 11
type: execute
wave: 10
depends_on:
  - 03-05
  - 03-10
  - 03-04
files_modified:
  - src/skillscout/domain/models.py
  - src/skillscout/adapters/state.py
  - tests/test_phase3_pipeline.py
autonomous: true
requirements:
  - GEN-05
  - VAL-03
  - REV-03
must_haves:
  truths:
    - "Phase 3 has a dedicated ledger rooted in complete CandidateExecutionAuthorityV1 and ordered exactly QUALIFIER → GENERATOR → VALIDATOR → REVIEWER."
    - "Every successful stage writes a strict CandidateStageCheckpointV1 into phase3_checkpoints, binding the result output hash, previous checkpoint hash, and next-stage continuity."
    - "Phase 1/2 global PIPELINE_PROFILES and existing verify_run_chain behavior remain unchanged."
    - "Terminal, semantic generated-artifact identity, rendered package identity/bytes, validation report, and review attestation canonical bytes are stored as distinct external records and projected byte-for-byte."
    - "A completed exact-authority lookup uses a separate DescriptorAnchoredCompletedCandidateProjector: it acquires only an existing retained lock, bounded-reads the Phase 3 DB through O_RDONLY/O_NOFOLLOW, deserializes solely into private :memory: SQLite, and descriptor-reads immutable artifacts without creating WAL/SHM or writing any path."
    - "Across every terminal branch, completed projection returns identical bytes while the complete path set, exact regular-file bytes, and full lstat metadata of DB/WAL/SHM presence, retained lock, artifact stores, terminal/report/attestation/package files, output files, and directories remain identical; low-level spies observe zero write/create/replace/rename/unlink/chmod/touch/utime/fsync or SQLite file-write calls."
    - "A verified completed miss closes the read-only projector before the existing mutable SQLiteStateStore handles a new or resumable run; normal non-completed resume retains its current durable write behavior."
  artifacts:
    - path: "src/skillscout/domain/models.py"
      provides: "Dedicated Phase 3 stage/run/attempt/result/checkpoint/resume/verified-chain models, including CandidateStageCheckpointV1."
    - path: "src/skillscout/adapters/state.py"
      provides: "Additive Phase 3 tables, mutable resume store, and separate DescriptorAnchoredCompletedCandidateProjector for immutable lookup/projection."
    - path: "tests/test_phase3_pipeline.py"
      provides: "Ledger/checkpoint tests plus every-branch descriptor-read reuse snapshots, low-level no-write sentinels, zero SQLite file writes, and mutable-resume regression."
  key_links:
    - from: "CandidateExecutionAuthorityV1"
      to: "phase3_runs"
      via: "Complete canonical authority is the run root and exact-reuse key."
    - from: "PhaseThreeStageV1"
      to: "phase3_attempts/results/checkpoints/resume_events"
      via: "Closed monotonic stage order plus result-output-hash/checkpoint continuity."
    - from: "phase3_artifacts and phase3_terminals"
      to: "DescriptorAnchoredCompletedCandidateProjector.project_completed_candidate"
      via: "Existing retained lock + bounded no-follow DB snapshot → private in-memory query-only SQLite → descriptor-based exact artifact reads."
  prohibitions:
    - "Do not append Phase 3 to application/pipeline.py PIPELINE_PROFILES."
    - "Do not modify or relax the existing Phase 1/2 verify_run_chain implementation."
    - "Do not synthesize fake Phase 2 rows or import Phase 2 stages into the Phase 3 ledger."
    - "Do not construct normal SQLiteStateStore, open a file-backed SQLite connection, create a lock/sidecar/temp, recover stale files, or call any writer on the completed projection path."
---

<objective>
Persist Phase 3 in a separate canonical ledger with exact reuse and byte-stable projections.

Purpose: Qualification, generation, validation, and review need resumable auditable state without weakening the already-shipped Phase 1/2 chain.
Output: Dedicated Phase 3 domain events/tables/verifier, exact external artifact persistence, and mutation/reuse tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/review.py
@src/skillscout/domain/models.py
@src/skillscout/adapters/state.py
@src/skillscout/application/pipeline.py
@tests/test_phase2_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the isolated Phase 3 canonical event chain</name>
  <files>src/skillscout/domain/models.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/domain/models.py` — extend with new names while preserving all Phase 1/2 contracts.
    - `src/skillscout/application/pipeline.py` — observe protected global profiles; do not modify it.
    - `tests/test_phase2_pipeline.py` — reuse canonical hash-chain invariants without combining ledgers.
    - `src/skillscout/domain/candidate_authority.py` — root the new run in complete execution authority.
  </read_first>
  <behavior>
    - PhaseThreeStageV1 has exactly QUALIFIER, GENERATOR, VALIDATOR, REVIEWER in that order.
    - CandidateRunIdentityV1 is rooted in complete canonical execution authority.
    - Attempts/results/checkpoints/resume events bind previous hashes, stage, attempt number, versions, sanitized outcome, canonical payload digest, and exact result output hash.
    - CandidateStageCheckpointV1 binds each successful result hash/output hash to the previous checkpoint hash and the next legal stage boundary.
    - Missing, reordered, duplicated, cross-run, output-swapped, checkpoint-spliced, or otherwise tampered records fail strict chain verification.
  </behavior>
  <action>
Add dedicated strict frozen `PhaseThreeStageV1`, `CandidateRunIdentityV1`, `CandidateStageAttemptV1`, `CandidateStageResultV1`, `CandidateStageCheckpointV1`, `CandidateResumeEventV1`, and `VerifiedCandidateRunChain` models to `src/skillscout/domain/models.py` with separate Phase 3 schema/profile/checkpoint constants. The closed stage sequence is exactly QUALIFIER, GENERATOR, VALIDATOR, REVIEWER. Root every run/record hash in the complete canonical `CandidateExecutionAuthorityV1`; bind stage order, monotonically increasing attempt number, previous event hash, version authorities, sanitized typed outcome, canonical payload digest, and the result's exact canonical output hash. `CandidateStageCheckpointV1` is required after every successful stage result and directly binds run authority, stage, attempt/result hash, canonical output hash, previous checkpoint hash or the Phase 3 genesis sentinel, next legal stage or terminal marker, checkpoint schema/profile version, and its own canonical checkpoint hash. The following attempt/resume record must cite the preceding checkpoint hash and output hash exactly. Do not reuse or extend the global Phase 1/2 pipeline profile registry, do not import fake Phase 2 rows, and do not change existing models' validation behavior. Add construction and field-by-field mutation tests for result-output-hash continuity, checkpoint deletion/insertion/reorder/splice, cross-run/cross-stage checkpoint swaps, invalid next-stage markers, and every other invalid transition.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k domain_chain</automated>
  </verify>
  <acceptance_criteria>
    - Closed stage enum/order is exactly the four Phase 3 stages.
    - Complete execution authority roots every event.
    - CandidateStageCheckpointV1 is strict/versioned and binds every successful result output hash to previous-checkpoint and next-stage continuity.
    - All event/checkpoint/output-hash mutations and invalid transitions fail deterministically.
    - Existing Phase 1/2 model and profile tests remain byte-for-byte behavior compatible.
  </acceptance_criteria>
  <done>Phase 3 has a standalone canonical event vocabulary and chain that cannot be confused with Phase 1/2 state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and verify the dedicated Phase 3 ledger</name>
  <files>src/skillscout/adapters/state.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` — preserve existing schema/methods/verifier and add isolated tables/functions.
    - `src/skillscout/domain/models.py` — persist Task 1 exact canonical models.
    - `src/skillscout/domain/review.py` — store external package/report/attestation/terminal canonical bytes.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — follow the Phase 3-relative ledger and manifest-first persistence pattern.
  </read_first>
  <behavior>
    - Additive tables are exactly phase3_runs, phase3_attempts, phase3_results, phase3_checkpoints, phase3_resume_events, phase3_terminals, and phase3_artifacts.
    - New verify_candidate_run_chain independently checks authority root, stage order, attempts, result output hashes, checkpoint continuity, resume links, and canonical payload bytes.
    - Existing verify_run_chain source and behavior are unchanged.
    - Transactions never expose a terminal pointing to missing/mismatched external artifact/report/attestation bytes.
  </behavior>
  <action>
Add isolated additive SQLite tables `phase3_runs`, `phase3_attempts`, `phase3_results`, `phase3_checkpoints`, `phase3_resume_events`, `phase3_terminals`, and `phase3_artifacts` plus mutable-store APIs named `verify_candidate_run_chain`, `find_resumable_candidate`, `persist_candidate_terminal`, and `project_verified_prior_lineage_evidence`. Implement connection-scoped pure completed-query/projection helpers that accept an already-deserialized SQLite connection plus anchored artifact readers; Task 3 exposes `find_completed_candidate` and `project_candidate_terminal` only through `DescriptorAnchoredCompletedCandidateProjector`. Do not put a public completed lookup on the mutable `SQLiteStateStore`, because that would allow composition to reopen completed reuse through its create/recovery/write-capable constructor. Persist one strict canonical `CandidateStageCheckpointV1` row after every successful result; use foreign/uniqueness constraints rooted in execution authority and require exact result hash/output hash, previous checkpoint hash, stage/attempt, and next-stage/terminal continuity. The independent verifier must recompute every result output hash and checkpoint hash from canonical bytes and reject missing/extra/reordered/duplicated/spliced checkpoints, result-output substitution, broken resume links, and a next attempt whose cited checkpoint/output does not match.

Use exact canonical bytes/digests for all external records. Store qualification, generated-artifact identity, frozen package manifest/content, ValidationReport, ReviewAttestation, CandidateTerminalSummary, canonical prior-lineage binding, and its durable approval record as separately typed immutable `phase3_artifacts` records; validate digest equality before committing a terminal transaction. The prior-lineage projection API looks up only the exact digest supplied by the descriptor/execution authority and must first verify the binding/approval-record bytes, prior Phase 3 chain, exact terminal-summary bytes/digest, generated-artifact identity, package manifest/digest, and full lineage/slug/repository authority before constructing Plan 03-05's narrow `VerifiedPriorLineageEvidenceV1`; no caller can mark an unverified projection as valid, and absence never falls back to a search. Implement resume only from a verified checkpoint at the next legal Phase 3 stage with exact prior results and output hashes. Preserve existing Phase 1/2 DDL, queries, verifier body, and public behavior; do not append to global profiles.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k state_ledger</automated>
  </verify>
  <acceptance_criteria>
    - Only isolated `phase3_*` tables are added; `phase3_checkpoints` has strict authority/result/output/previous-checkpoint/next-stage constraints.
    - New verifier catches row or checkpoint deletion, insertion, reorder, splice, payload/output mutation, authority swap, resume discontinuity, and artifact mismatch.
    - Prior-lineage projection requires verified prior chain, terminal, generated-artifact identity, package digest, lineage/slug/repository authority, and approval-record binding; every mutation fails closed.
    - Terminal commit is atomic with all outcome-required external records.
    - Existing Phase 1/2 state tests still pass without expectation changes.
  </acceptance_criteria>
  <done>Phase 3 stages and external evidence persist atomically in an isolated verifiable ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reproject completed candidates byte-for-byte with zero side effects</name>
  <files>src/skillscout/adapters/state.py, tests/test_phase3_pipeline.py</files>
  <read_first>
    - `src/skillscout/adapters/state.py` — build exact reuse on Task 2 completed lookup/projection.
    - `src/skillscout/adapters/localfs.py` — reuse `AnchoredDirectory.open(create=False)` and `read_bytes` for retained descriptor anchoring, O_RDONLY/O_NOFOLLOW, private single-link admission, cap-plus-one reads, and pre/post identity stability.
    - `src/skillscout/domain/review.py` — validate branch-specific optional evidence.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — implement exact-reuse byte and count invariants.
    - `tests/test_state_integrity.py` — copy concrete invariants from `test_state_uses_private_memory_sqlite_and_one_reusable_live_lock`, `test_existing_state_requires_private_permissions_before_deserialize`, `test_existing_state_requires_one_link`, `test_existing_manifest_requires_private_single_owner_file_before_decode`, `test_oversized_serialized_state_fails_before_sqlite_deserialize`, and `test_parent_swap_after_state_anchor_cannot_redirect_state_or_manifests`.
    - `tests/test_phase2_pipeline.py` — start from `test_completed_phase_two_run_is_fully_reused_without_reexecution`, then add the stricter byte/path/metadata/no-write proof required here.
  </read_first>
  <behavior>
    - Exact complete execution authority returns the same terminal summary and every applicable generated-artifact identity/package identity/package/report/attestation byte.
    - The completed projector opens only an existing retained lock and the Phase 3 DB with O_RDONLY/O_NOFOLLOW/CLOEXEC, bounded-reads one stable snapshot, deserializes only into a private query-only `:memory:` SQLite connection, and reads all artifact/summary/report/attestation/package bytes through retained no-follow descriptors.
    - Reprojection does not open file-backed SQLite, create WAL/SHM, create/replace/touch/chmod/fsync a lock or any other path, insert a row/event, or invoke materialization.
    - For every terminal branch, the recursive path set, exact regular-file bytes, and full lstat metadata remain identical for present/absent DB/WAL/SHM, retained lock, artifact roots/files, terminal/summary/report/attestation/package files, output roots/files, and every directory.
    - Low-level spies see zero writable/create/truncate/append opens, mkdir/write/pwrite/replace/rename/unlink/remove/rmdir/chmod/fchmod/touch/utime/futimes/fsync/fdatasync calls, and every sqlite3 connection target is exactly `:memory:`.
    - A clean completed miss releases descriptors then permits the existing writable store path; non-completed resume still appends its resume/checkpoint/state evidence and exercises atomic write/fsync behavior.
    - Tampered/incomplete completed state is never returned as reuse.
  </behavior>
  <action>
Implement a separate `DescriptorAnchoredCompletedCandidateProjector` and strict `CompletedCandidateProjectionV1` in `src/skillscout/adapters/state.py`; do not route completed lookup through `SQLiteStateStore.__init__`. Its open contract has three distinguishable results: state absent or exact-authority not completed is a clean miss; a fully verified completed chain returns the projection; malformed/tampered state, missing/unsafe existing lock, lock contention, or incomplete completed evidence is a sanitized integrity failure that must never fall back to the mutable store.

For an existing state file, open the already-existing private parent with `AnchoredDirectory.open(create=False)`. Derive the closed lock leaf, lstat it without following links, then open it with exactly read-only/no-follow/close-on-exec flags and no create/truncate mode; require the common private single-link regular predicate and exact path-to-fd identity, acquire a nonblocking shared flock against that retained inode, and keep the descriptor until every DB and artifact byte has been verified/projected. A missing lock when the DB exists fails closed; never create, replace, chmod, touch, fsync, recover a temp, or retire a backup on this path.

Under the retained lock, read the Phase 3 DB through the anchored directory's bounded O_RDONLY/O_NOFOLLOW descriptor path with cap-plus-one enforcement and identical pre/post device/inode/mode/link/uid/gid/size/mtime-ns/ctime-ns facts. Create SQLite only with `sqlite3.connect(":memory:", isolation_level=None)`, deserialize the admitted bytes there, enable `PRAGMA query_only=ON`, and run the exact schema, complete-authority lookup, Phase 3 chain, checkpoint, terminal, and external-artifact verification against that private connection. Never pass a pathname/URI to SQLite, attach a database, serialize/persist the connection, issue mutating SQL/PRAGMAs, or permit WAL/SHM creation. Close it on every branch.

Resolve every artifact from a closed digest-derived locator. Anchor existing artifact directories with `create=False`; descriptor-read generated-artifact identity, rendered identity/manifest/package leaves, qualification/validation reports, review attestation, terminal summary, and prior-binding/approval evidence using the same bounded private regular no-follow/path-fd/post-read-stability discipline. Return the exact admitted canonical bytes stored on disk, not reserialized equivalents, and never materialize output or append a convenience reuse event.

Create a recursive snapshot helper in `tests/test_phase3_pipeline.py` that records, both before and after completed projection, each watched path's existence, type, exact regular-file bytes, and the complete `os.lstat()` result, plus sorted recursive child-name sets for every directory. Parameterize all 12 terminal branches and explicitly watch the Phase 3 DB, `-wal`, `-shm` whether present or absent, retained lock, artifact-store root/subdirectories/files, terminal/summary/report/attestation/package leaves, output root/files, and all parents. Assert snapshot equality, exact projected bytes, and unchanged table rows/counts including `phase3_checkpoints`.

Install low-level spies/sentinels across `os.open` flags and builtins/Path/AnchoredDirectory/SQLite mutation surfaces: reject any writable/create/exclusive/truncate/append open; `mkdir`/`makedirs`; `write`/`pwrite`; `replace`/`rename`; `unlink`/`remove`/`rmdir`; `chmod`/`fchmod`; `Path.touch`; `utime`/equivalent timestamp setters; `fsync`/`fdatasync`; `sqlite3.connect` targets other than `:memory:`; connection `serialize`, backup/attach, or mutating SQL; state/checkpoint/resume/event append; generator, official validator, Reviewer; and materializer/output writer. Corrupt any checkpoint/result output hash or terminal/generated-artifact identity/package identity/package/report/attestation byte or digest and prove the projector fails closed with the same zero-write snapshot invariant.

Finally force a clean no-completed-result miss followed by an interrupted/resumable exact authority. Prove projector descriptors close before the existing mutable `SQLiteStateStore` opens, then prove normal resume still appends the required resume/checkpoint/state facts and performs its existing descriptor-anchored atomic snapshot/restore/fsync sequence. The read-only restriction is exclusive to completed projection and must not weaken normal resume durability.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase3_pipeline.py -k exact_reuse</automated>
  </verify>
  <acceptance_criteria>
    - Every terminal branch returns exact stored bytes while watched path sets, full lstat metadata, DB/WAL/SHM presence/absence, artifact/output directories, and every stored canonical byte string remain identical.
    - All applicable semantic identity, rendered identity/package, report, attestation, and terminal artifacts are returned byte-for-byte.
    - Projector uses an existing retained lock, bounded O_RDONLY/O_NOFOLLOW DB snapshot, private query-only `:memory:` SQLite, and descriptor-based immutable artifact reads; SQLite never receives a file path and creates no WAL/SHM.
    - Low-level sentinels prove zero write/create/replace/rename/unlink/chmod/touch/utime/fsync, row/event, downstream service, materializer, or output-writer calls.
    - A clean miss followed by a resumable run still uses the existing mutable store and preserves all prior write/durability behavior; integrity failures never fall back.
    - Any corrupt or incomplete completed state is rejected.
  </acceptance_criteria>
  <done>Completed exact-authority candidates are replayed as immutable projections with zero state or external side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain stage outcomes → SQLite | Canonical events and external artifacts must remain ordered, authority-bound, and atomic. |
| SQLite completed state → reuse projection | Stored state may be tampered or incomplete and cannot be trusted without verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-31 | Tampering | Phase 3 ledger chain | high | mitigate | Dedicated authority-rooted verifier covers event order, hashes, payloads, external artifacts, and `phase3_checkpoints` output-hash/previous-checkpoint/next-stage continuity. |
| T-03-32 | Spoofing | Cross-run artifact reuse | high | mitigate | Foreign/unique constraints and complete execution-authority digest bind every record. |
| T-03-33 | Repudiation | Resume/reuse history | medium | mitigate | Resume stays on the mutable ledger; exact completed reuse is a separate retained-lock/O_RDONLY/private-memory projector proven by full path/byte/lstat snapshots and low-level no-write sentinels. |
| T-03-34 | Tampering | Existing Phase 1/2 verifier | high | mitigate | Protected behavior/source regression tests and separate Phase 3 APIs/tables. |
</threat_model>

## Artifacts this phase produces

- Dedicated Phase 3 canonical event models and isolated SQLite ledger.
- Strict CandidateStageCheckpointV1 records and `phase3_checkpoints` output-hash continuity.
- Separate chain/external-evidence verifier.
- Exact byte-stable, zero-side-effect reuse projection.
- DescriptorAnchoredCompletedCandidateProjector and CompletedCandidateProjectionV1, isolated from mutable resume state.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Auditable, repeatable qualification/generation/validation/review | COVERED by isolated canonical state and exact reuse. |
| REQ | GEN-05, VAL-03, REV-03 | COVERED — stable identity, report persistence, exact eligibility outcome. |
| RESEARCH | Phase 3-relative ledger; protected Phase 1/2 seams; exact-byte reuse | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Then run `sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_phase2_pipeline.py tests/test_phase3_pipeline.py`.
</verification>

<success_criteria>
- Phase 3 chain is isolated, ordered, authority-rooted, and tamper-evident.
- Existing Phase 1/2 state verification remains unchanged.
- Exact reuse returns identical bytes with unchanged DB/WAL/SHM, lock, artifact/output path sets and full metadata, and no writable filesystem/SQLite call.
- Normal non-completed resume retains the existing durable write path.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-11-SUMMARY.md` when done.
</output>
