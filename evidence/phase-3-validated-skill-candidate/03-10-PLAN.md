---
phase: 03-validated-skill-candidate
plan: 10
type: execute
wave: 9
depends_on:
  - 03-05
  - 03-07
  - 03-08
  - 03-09
files_modified:
  - src/skillscout/domain/review.py
  - src/skillscout/adapters/openai_review.py
  - tests/test_openai_review.py
  - tests/fixtures/openai/reviewer/cases.json
autonomous: true
requirements:
  - GEN-04
  - GEN-05
  - VAL-03
  - REV-01
  - REV-02
  - REV-03
must_haves:
  truths:
    - "Reviewer receives a fresh canonical user-role-only inert envelope with exactly four ordered sections: WorkflowSpec, rendered artifact files, provenance, and Validation Report; the developer role contains policy but zero payload bytes."
    - "OpenAIReviewClient constructs OpenAI with max_retries=0 and every review invocation issues exactly one Responses request; only PhaseThreeRunner may retry a mapped transient failure."
    - "Reviewer returns strict YES/NO, confidence, reasons, missing assumptions, and minimal modifications; it judges only and cannot produce replacement Skill content or auto-rewrite the package."
    - "Eligibility is true exactly when validation has zero errors, reviewer says YES, and confidence is at least 0.80."
    - "External ReviewAttestationV1 binds artifact/package/report digests, configured plus actual Reviewer model, versions, raw review outcome/judgment, and bounded telemetry without owning derived eligibility or rewriting package bytes."
    - "CandidateTerminalSummaryV1 owns derived eligibility plus ELIGIBILITY_POLICY_VERSION and binds all authorities, lineage disposition, Generator outcome/actual-model evidence, review disposition, and branch-appropriate artifact/package/report/attestation evidence."
  artifacts:
    - path: "src/skillscout/domain/review.py"
      provides: "Strict judgment, eligibility, attestation, terminal-summary contracts and exact terminal vocabulary."
    - path: "src/skillscout/adapters/openai_review.py"
      provides: "Separate tool-free OpenAI Reviewer adapter."
    - path: "tests/test_openai_review.py"
      provides: "Canonical four-section role/order/delimiter isolation, max_retries=0 one-raw-request counts, judge-only output, threshold, attestation, terminal, and failure-mode tests."
  key_links:
    - from: "Frozen package + ValidationReportV1"
      to: "OpenAIReviewClient"
      via: "Fresh bounded Reviewer context only after validation."
    - from: "ReviewerJudgment + ValidationReportV1"
      to: "ReviewAttestationV1"
      via: "Exact raw review evidence; terminal summary separately derives eligibility."
    - from: "All authorities and artifacts"
      to: "CandidateTerminalSummaryV1"
      via: "External terminal projection; package remains unchanged."
  prohibitions:
    - "Do not share Generator prompt, response, chain-of-thought, raw repository content, or unrelated state with Reviewer."
    - "Do not accept replacement Skill text or mutate/re-render package bytes after review."
    - "Do not place Reviewer/eligibility/terminal facts inside package provenance."
---

<objective>
Add an independent judge-only review and external attestation/terminal contracts.

Purpose: Generation and review must be separate semantic contexts, and final eligibility must be mechanically derived from immutable validation and review evidence.
Output: Strict Reviewer domain models, isolated OpenAI adapter, external attestation, terminal summary, and boundary tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@src/skillscout/domain/candidate_authority.py
@src/skillscout/domain/skill_artifacts.py
@src/skillscout/domain/validation.py
@src/skillscout/adapters/openai_generate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define judge-only review and exact eligibility contracts</name>
  <files>src/skillscout/domain/review.py, tests/test_openai_review.py</files>
  <read_first>
    - `src/skillscout/domain/validation.py` — bind review to exact package/report identity and zero-error semantics.
    - `src/skillscout/domain/skill_artifacts.py` — reference frozen artifact/provenance without exposing mutation APIs.
    - `.planning/REQUIREMENTS.md` — implement REV-01, REV-02, and REV-03 exactly.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — follow external attestation and immutable artifact boundaries.
  </read_first>
  <behavior>
    - ReviewerJudgment accepts only YES or NO, confidence 0 through 1, bounded reasons/missing assumptions/minimal modifications, and no replacement artifact fields.
    - Eligibility is true exactly for zero validation errors plus YES plus confidence at least 0.80.
    - 0.79 rejects and 0.80 passes when validation is clean and decision is YES.
    - NO always rejects, and any validation error rejects regardless of Reviewer output.
  </behavior>
  <action>
Create strict frozen versioned `ReviewerJudgment` and `ReviewResult` contracts. Export `ELIGIBILITY_POLICY_VERSION = "candidate-eligibility-v1"` plus review prompt/schema/policy versions. Limit judgment to exact YES/NO, numeric confidence, bounded reason codes/text, missing assumptions, and minimal modification suggestions; reject any output field that supplies replacement SKILL.md, files, patches, or rewrite instructions. Implement one pure eligibility function that returns eligible exactly when `ValidationReportV1.error_count == 0`, decision is YES, and confidence is at least 0.80. Preserve warning/info findings as Reviewer context but never let them override the exact rule. Add strict-schema and all threshold/cross-product cases.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_review.py -k domain</automated>
  </verify>
  <acceptance_criteria>
    - Judge output has all REV-02 fields and no content-replacement channel.
    - `ELIGIBILITY_POLICY_VERSION` is exported from the owning module.
    - Full validation/decision/confidence cross-product matches REV-03 exactly.
    - Confidence boundary 0.79/0.80 is tested.
  </acceptance_criteria>
  <done>Review judgment and eligibility are strict, judge-only, and mechanically derived.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute a separate tool-free OpenAI Reviewer request</name>
  <files>src/skillscout/adapters/openai_review.py, tests/test_openai_review.py, tests/fixtures/openai/reviewer/cases.json</files>
  <read_first>
    - `src/skillscout/adapters/openai_generate.py` — reuse transport/telemetry/sanitization mechanics but create a separate request and context.
    - `src/skillscout/domain/review.py` — request Task 1's strict judgment schema.
    - `src/skillscout/domain/skill_artifacts.py` — serialize exact frozen artifact/provenance for review.
    - `src/skillscout/domain/validation.py` — include the exact validation report and no hidden derived text.
  </read_first>
  <behavior>
    - Reviewer dynamic payload is a canonical user-role-only inert envelope containing exactly WorkflowSpec, rendered artifact files, provenance, then Validation Report, in that order.
    - Fresh unambiguous delimiters surround each section, cannot occur in any section bytes, and never grant role or instruction authority to injected text.
    - Developer content is static review policy and contains zero bytes copied from any of the four payload sections; no system, assistant, or tool payload message exists.
    - Request uses independently configured Reviewer model, `store=false`, no tools, and strict Structured Outputs.
    - The SDK client is constructed with `max_retries=0`; one adapter invocation performs exactly one `responses.parse` request and contains no internal retry loop or backoff.
    - Generator request/response/transcript and raw repository content are absent.
    - Refusal, incomplete response, schema failure, transient transport failure, permanent transport failure, and success are distinct bounded outcomes with actual Reviewer telemetry.
  </behavior>
  <action>
Implement `OpenAIReviewClient` as a separate adapter/request with its own configured model, prompt version, schema version, retry-policy identifier, and transport call. Construct `openai.OpenAI` with the injected API key/client and `max_retries=0`. Each `review` invocation must contain exactly one `responses.parse` call, with no adapter loop, sleep, backoff, or second SDK request; it returns a mapped transient error to its caller after the first 429, 500, timeout, or connection failure, leaving every retry decision to `PhaseThreeRunner`.

Build one canonical inert envelope whose dynamic bytes appear only in one user-role message and whose sections occur exactly once in this order: `WorkflowSpec`; rendered artifact files excluding `references/provenance.json`, serialized as a canonical path/bytes sequence; the exact canonical provenance record; the exact canonical `ValidationReportV1`. Generate fresh delimiter tokens for each invocation, prove none occurs in any serialized section, and fail closed or regenerate before issuing the request if a collision is detected. Keep the developer-role content static and limited to review instructions/policy; prove it contains zero bytes from every serialized section. Do not add system, assistant, or tool payload messages, and do not include raw repository content, Generator prompt/output transcript, chain-of-thought, Phase 2 raw evidence, or unrelated pipeline state. Set `store=false`, declare no tools, and request strict `ReviewerJudgment`. Capture actual model, request ID, latency, and token usage as bounded external evidence. Map refusal, incomplete, schema failure, retryable infrastructure, and terminal infrastructure outcomes distinctly; sanitize all diagnostics. Recorded fixtures must assert exact role order, exact four-section order and uniqueness, fresh delimiter non-collision, developer payload-byte exclusion, no tools, `store=false`, strict schema, judge-only rejection of replacement content, and prompt-injection variants that mimic roles, section headings, and delimiters. For parsed success, refusal, incomplete, schema-invalid, 429, and 500 fixtures, assert the raw recorded transport count is exactly one for one adapter invocation.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_review.py -k adapter</automated>
  </verify>
  <acceptance_criteria>
    - Reviewer has a separate configured model/request/context from Generator.
    - Exact request inspection proves the canonical four-section user-role-only envelope, exact section order, unique fresh delimiters, and zero dynamic payload bytes in the developer role.
    - No tools, storage, raw source, Generator transcript, or replacement-content channel exists.
    - Parsed, semantic-failure, 429, and 500 fixtures each record exactly one raw Responses transport call per adapter invocation; the adapter never retries.
    - Actual Reviewer telemetry is captured without logs/content/secrets.
  </acceptance_criteria>
  <done>The Reviewer independently judges the frozen candidate through a bounded tool-free structured request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind review attestation and exact terminal summary externally</name>
  <files>src/skillscout/domain/review.py, tests/test_openai_review.py</files>
  <read_first>
    - `src/skillscout/domain/review.py` — build attestations from strict Task 1/2 outcomes.
    - `src/skillscout/domain/candidate_authority.py` — bind complete execution/source/lineage authorities.
    - `src/skillscout/domain/skill_artifacts.py` — bind external artifact identity, not mutable package objects.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — implement exact terminal branch vocabulary and optional artifact rules.
  </read_first>
  <behavior>
    - ReviewAttestationV1 binds exact generated-artifact identity, package digest, validation report digest, configured+actual Reviewer, versions, raw result/judgment, and bounded telemetry; eligibility is absent.
    - CandidateTerminalSummaryV1 binds complete execution/source/lineage authority, explicit eligibility plus ELIGIBILITY_POLICY_VERSION, GeneratorOutcomeEvidenceV1 when generation was attempted, ReviewDispositionV1, and only branch-appropriate optional artifact/report/attestation identities.
    - Terminal outcomes are limited to the exact 12 Phase 3 branches from research.
    - package, report, and attestation canonical bytes remain external and package bytes never change.
    - qualification_rejected requires lineage status `not_evaluated_qualification_rejected`; validation_rejected requires review status `review_skipped_validation_errors`.
  </behavior>
  <action>
Define strict frozen external `ReviewAttestationV1`, `GeneratorOutcomeEvidenceV1`, `ReviewDispositionV1`, and `CandidateTerminalSummaryV1`, canonical bytes/digests, and constructor functions. Attestation binds the semantic generated-artifact identity, distinct package digest, ValidationReport digest, configured and actual Reviewer identities, review prompt/schema/policy/retry versions, exact raw review outcome/judgment, request ID, and bounded usage/latency; it must not contain `eligible` or `ELIGIBILITY_POLICY_VERSION`. `GeneratorOutcomeEvidenceV1` binds the attempted Generator result class, configured and actual model when returned, generation prompt/schema/policy/profile/retry versions, request ID, bounded usage/latency, and semantic generated-artifact identity on success. `ReviewDispositionV1` has closed statuses for not reached because generation did not succeed, exactly `review_skipped_validation_errors`, Reviewer refusal/incomplete/schema failure, completed NO, completed low confidence, and completed eligible review. Define a closed terminal enum containing only `qualification_rejected`, `lineage_rejected`, `generator_refusal`, `generator_incomplete`, `generator_schema_failure`, `validation_rejected`, `reviewer_refusal`, `reviewer_incomplete`, `reviewer_schema_failure`, `review_rejected`, `review_low_confidence`, and `eligible_local_candidate`. `candidate_source_unavailable` is excluded because it occurs before a Phase 3 run. Terminal summary must own `eligible` and `ELIGIBILITY_POLICY_VERSION`, bind complete source/execution authority and exact `LineageResolutionV1`, and include only branch-appropriate Generator evidence, semantic generated-artifact identity, rendered package identity/digest, ValidationReport, ReviewDisposition, and ReviewAttestation digests. Enforce the exact evidence matrix: `qualification_rejected` has failed qualification plus `LineageResolutionV1(status="not_evaluated_qualification_rejected")` and no Generator/package/report/attestation; `lineage_rejected` has an evaluated rejected lineage and no Generator; Generator semantic failures have Generator outcome/actual-model evidence and no package; `validation_rejected` has successful Generator evidence, both identity layers, report, `review_skipped_validation_errors`, and no attestation; Reviewer branches have successful Generator/identity/report plus their exact ReviewDisposition and attestation. All rejected branches own `eligible=false`; only `eligible_local_candidate` owns true under the versioned exact eligibility policy. Never modify package/provenance bytes to add review or terminal facts. Add the full branch/evidence matrix, authority/evidence sensitivity, impossible-combination, eligibility-ownership, and package-byte-immutability tests.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_openai_review.py</automated>
  </verify>
  <acceptance_criteria>
    - Attestation contains every required configured/actual Reviewer and report/artifact/package binding and contains no derived eligibility field.
    - Terminal vocabulary is exactly the 12 listed branches and excludes pre-run source-unavailable.
    - Terminal summary owns exact eligibility/policy, lineage and review companion statuses, Generator outcome/actual-model evidence, and validates optional evidence per branch.
    - qualification_rejected and validation_rejected require exactly the not-evaluated-lineage and review-skipped-validation-errors companion statuses.
    - Review/terminal construction leaves every package byte/path/mode unchanged.
  </acceptance_criteria>
  <done>Independent review evidence and the complete Phase 3 outcome are externally bound without circular provenance or artifact mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen candidate/report → Reviewer | Only an explicit bounded context may cross into an independent semantic request. |
| Reviewer output → eligibility | Model judgment cannot directly mutate content or forge deterministic validation. |
| Review evidence → terminal projection | External evidence must bind exact authorities and immutable digests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-27 | Tampering | Reviewer independence | high | mitigate | Fresh context contains exactly the four ordered user-role sections with fresh non-colliding delimiters; the static developer policy contains no payload bytes, Generator transcript, or raw source. |
| T-03-28 | Elevation of Privilege | Reviewer rewrite | high | mitigate | Strict judge-only schema rejects replacement files/content and renderer is never called after review. |
| T-03-29 | Tampering | Eligibility | high | mitigate | Pure exact zero-errors + YES + confidence>=0.80 rule with cross-product tests. |
| T-03-30 | Repudiation | Review/terminal evidence | high | mitigate | External canonical attestation/summary bind complete authorities and configured/actual model evidence. |
</threat_model>

## Artifacts this phase produces

- Strict independent Reviewer judgment and eligibility policy.
- Tool-free OpenAI Reviewer adapter and fixtures.
- External ReviewAttestationV1 and CandidateTerminalSummaryV1.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Explain independent review and local eligibility | COVERED — exact evidence and outcome are externally bound. |
| REQ | GEN-04, GEN-05, VAL-03, REV-01, REV-02, REV-03 | COVERED exactly. |
| RESEARCH | Independent context; external attestation/terminal; exact branches; immutable package | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run all three task commands independently with Gate B3. Inspect request fixtures for exact REV-01 context and test the complete terminal branch matrix.
</verification>

<success_criteria>
- Reviewer is semantically and contextually independent from Generator.
- Eligibility exactly matches REV-03.
- Attestation and terminal summary bind immutable evidence without rewriting the Skill package.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-10-SUMMARY.md` when done.
</output>
