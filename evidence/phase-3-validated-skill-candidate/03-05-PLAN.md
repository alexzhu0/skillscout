---
phase: 03-validated-skill-candidate
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/skillscout/domain/candidate_authority.py
  - tests/test_candidate_authority.py
  - tests/test_lineage.py
autonomous: true
requirements:
  - GEN-04
  - GEN-05
must_haves:
  truths:
    - "Candidate reuse is keyed by a complete, canonical execution authority rather than a partial fingerprint."
    - "The authority includes the complete WorkflowSpec authority, selected source fingerprint, every configured model/prompt/schema/policy/renderer/validator/profile/retry version, approved lock identity, and official distribution identity before any Phase 3 lookup."
    - "Lineage is repository ID plus the initial complete WorkflowSpec authority; only one exact approved prior binding can retain an existing lineage and slug."
    - "A prior binding is canonical evidence, not an approval flag: it binds repository ID, full lineage, stable slug, prior package and terminal-summary digests, new WorkflowSpec authority, binding schema/policy, and a durable approval-record digest."
    - "Stale, colliding, multiple, or ambiguous prior bindings close as lineage_rejected."
  artifacts:
    - path: "src/skillscout/domain/candidate_authority.py"
      provides: "Versioned strict authority, lineage, canonicalization, and digest contracts."
    - path: "tests/test_candidate_authority.py"
      provides: "Full-field sensitivity and canonical round-trip tests."
    - path: "tests/test_lineage.py"
      provides: "New-lineage, approved-update, stale/collision/ambiguity closure tests."
  key_links:
    - from: "WorkflowSpecAuthorityV1"
      to: "CandidateExecutionAuthorityV1"
      via: "The complete canonical authority is embedded, never represented only by wf-fingerprint-v1."
    - from: "PriorLineageBindingV1"
      to: "LineageResolutionV1"
      via: "Only an exact approved binding may preserve lineage/slug."
  prohibitions:
    - "Do not treat wf-fingerprint-v1 as authority; it remains a discriminator only."
    - "Do not use actual response model IDs in prelookup authority; configured IDs are authority and actual IDs are terminal evidence."
    - "Do not infer lineage from title, slug, path, or content similarity."
---

<objective>
Define the complete immutable identity and lineage contracts that make Phase 3 reuse safe.

Purpose: Every later stage, package, attestation, and terminal summary must bind to the same complete source and execution authority, and updates must never silently collide with unrelated Skills.
Output: Strict Pydantic authority/lineage models, canonical digest functions, and exhaustive sensitivity tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-skill-candidate/03-RESEARCH.md
@.planning/phases/03-validated-skill-candidate/03-PATTERNS.md
@.planning/phases/02-safe-single-repository-extraction/02-04-SUMMARY.md
@src/skillscout/domain/extraction.py
@src/skillscout/domain/canonical.py
@src/skillscout/domain/models.py
@tests/test_stage_contracts.py
@tests/test_extractor_boundary.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model complete source and execution authorities</name>
  <files>src/skillscout/domain/candidate_authority.py, tests/test_candidate_authority.py</files>
  <read_first>
    - `src/skillscout/domain/extraction.py` — reuse Phase 2 strict model and canonicalization conventions without widening it.
    - `src/skillscout/domain/canonical.py` — reuse repository canonical JSON/digest helpers.
    - `.planning/phases/03-validated-skill-candidate/03-PATTERNS.md` — implement the Complete Authority and Prelookup Authority patterns.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — enumerate every reuse-sensitive version field.
  </read_first>
  <behavior>
    - Strict models reject unknown, missing, blank, or malformed authority fields.
    - Canonical bytes and SHA-256 are deterministic across semantically identical construction order.
    - Mutating any complete WorkflowSpec field or any configured model/prompt/schema/policy/renderer/validator/profile/retry/lock/distribution field changes execution authority.
    - Mutating only renderer authority or only eligibility-policy authority independently changes execution authority; neither field has an implicit default.
    - Configured model IDs are required prelookup; actual response model IDs are excluded.
    - CandidateSubjectDescriptorV1 carries at most one optional exact prior-lineage-binding digest; absence means new-lineage evaluation and never authorizes catalog/state similarity search.
  </behavior>
  <action>
Create `src/skillscout/domain/candidate_authority.py` with strict frozen versioned contracts and exported constants `CANDIDATE_DESCRIPTOR_SCHEMA_VERSION`, `WORKFLOW_SPEC_AUTHORITY_SCHEMA_VERSION`, `PRIOR_LINEAGE_BINDING_SCHEMA_VERSION`, `LINEAGE_RESOLUTION_SCHEMA_VERSION`, and `CANDIDATE_EXECUTION_AUTHORITY_SCHEMA_VERSION`. Define `CandidateSubjectDescriptorV1`, `WorkflowSpecAuthorityV1`, and `CandidateExecutionAuthorityV1` plus deterministic constructors/digests named `workflow_spec_authority` and `candidate_execution_authority`. The descriptor identifies the already-completed Phase 2 run and one full workflow fingerprint, does not copy the WorkflowSpec, and may carry exactly one optional canonical `prior_lineage_binding_digest`; absence explicitly means no prior binding and must never trigger heuristic/state search. Recompute complete WorkflowSpec authority from every canonical semantic field and its upstream evidence/source authority, not only `wf-fingerprint-v1`. Execution authority must be complete before Phase 3 lookup and include the selected source fingerprint, the exact optional prior-lineage-binding digest from the descriptor, approved `uv.lock` digest, official validator distribution identity, and all configured semantic/policy/version identifiers named in research. Keep configured model identities in authority and reserve actual response model identities for terminal evidence. Use exact canonical bytes; prohibit implicit defaults for reuse-sensitive fields.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_candidate_authority.py</automated>
  </verify>
  <acceptance_criteria>
    - Every research-listed authority field is required and serialized.
    - All strict contracts reject extras and incomplete values.
    - A parameterized test mutates every authority field one at a time and observes a changed canonical digest.
    - Separate named tests mutate only renderer authority and only eligibility-policy authority and prove each independently invalidates reuse identity.
    - Missing versus present prior-binding digest changes execution authority; no-binding descriptors cannot authorize lookup by title/slug/path/content.
    - Actual response model IDs cannot enter prelookup authority.
  </acceptance_criteria>
  <done>Complete source and execution authority can be constructed deterministically before Phase 3 state access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve new and retained lineage without heuristics</name>
  <files>src/skillscout/domain/candidate_authority.py, tests/test_lineage.py</files>
  <read_first>
    - `src/skillscout/domain/candidate_authority.py` — build lineage on Task 1 authority contracts.
    - `.planning/REQUIREMENTS.md` — implement GEN-05 stable identity/update behavior.
    - `.planning/phases/03-validated-skill-candidate/03-RESEARCH.md` — follow the exact binding and rejection matrix.
  </read_first>
  <behavior>
    - With no prior binding, repository ID plus initial complete WorkflowSpec authority deterministically creates a new lineage and slug.
    - Exactly one valid canonical binding whose durable approval record and prior terminal/package evidence reverify retains its recorded lineage and slug even if title, path, or content changes.
    - A binding never creates a new lineage.
    - Stale, mismatched, duplicate, colliding, ambiguous, approval-tampered, prior-terminal-tampered, or initial-authority-tampered bindings produce a closed `lineage_rejected` resolution with sanitized reasons.
    - The closed resolution vocabulary includes `not_evaluated_qualification_rejected` so a qualification rejection records why lineage was intentionally not evaluated.
  </behavior>
  <action>
Define strict frozen `PriorLineageBindingV1`, `VerifiedPriorLineageEvidenceV1`, and `LineageResolutionV1` contracts plus functions named `derive_new_lineage` and `resolve_lineage`. The closed `LineageResolutionV1.status` vocabulary must distinguish new, retained, rejected, and exactly `not_evaluated_qualification_rejected`; the not-evaluated status carries no lineage/slug. Plan 03-10's terminal constructor, which has the QualificationReport contract available, will be the only production path allowed to pair that status with a failed qualification. New lineage authority is the repository ID combined with the initial complete WorkflowSpec authority; derive its stable identity and sanitized Agent Skills slug deterministically. `PriorLineageBindingV1` must canonically include the repository ID, full lineage authority/ID, stable slug, prior package digest, prior terminal-summary digest, the newly approved complete WorkflowSpec authority, binding schema version, binding policy version, and durable human approval-record digest; there is no boolean approval shortcut. `VerifiedPriorLineageEvidenceV1` is the narrow canonical projection that a later state adapter may construct only after verifying the prior Phase 3 chain, terminal-summary bytes, and package manifest/digest; it carries the exact prior digests and fields needed to recompute the prior initial authority without importing the future terminal-summary class into this plan. Before retention, `resolve_lineage` requires that verified projection, recomputes the prior lineage's initial complete WorkflowSpec authority, recomputes/verifies the durable approval-record digest and binding target, and compares every field. Retention is allowed only when exactly one complete binding and its prior evidence match all repository/lineage/new-authority invariants. Do not match by title, slug, path, content similarity, or fingerprint alone. A supplied invalid binding closes as `lineage_rejected`; it must never fall through to new-lineage creation. Detect multiple candidates, duplicate binding IDs, stale authorities, approval-record mutation, prior-terminal/package mutation, initial-authority mutation, slug collisions, repository mismatches, and ambiguous ownership, and return bounded machine-readable reasons with no raw external content. Tests use verified-evidence fixtures to prove that changing title/path/content under a fully valid binding retains identity while the same changes without a binding create a new lineage.
  </action>
  <verify>
    <automated>sh tools/verify_phase3_gate_b3.sh &amp;&amp; UV_PYTHON_INSTALL_DIR="$PWD/.tools/python" UV_MANAGED_PYTHON=1 UV_PYTHON_DOWNLOADS=never "$PWD/.tools/uv-0.11.29/bin/uv" run --locked pytest -q tests/test_lineage.py</automated>
  </verify>
  <acceptance_criteria>
    - New identity uses repository ID plus initial complete authority and is deterministic.
    - Only one exact canonical binding with all required fields, verified prior terminal/package evidence, recomputed initial authority, and verified durable approval record retains lineage and slug.
    - Every field mutation, including approval-record, prior-terminal/package, and initial-authority tampering, returns `lineage_rejected` and never creates a new lineage.
    - `LineageResolutionV1(status="not_evaluated_qualification_rejected")` rejects lineage/slug fields; the Phase 3 terminal matrix later proves it appears only with failed qualification.
    - Tests prove no title/path/content heuristic participates.
  </acceptance_criteria>
  <done>Local Skill lineage is stable for approved updates and fails closed for every ambiguous or stale case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 structured result → Phase 3 authority | Upstream output is untrusted until its complete canonical authority is recomputed. |
| Prior catalog binding → local lineage | Historical identity evidence can preserve, but never invent, ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | Partial execution identity | high | mitigate | Require full-field strict execution authority and sensitivity tests before lookup. |
| T-03-10 | Spoofing | Prior lineage binding | high | mitigate | Accept exactly one approved complete binding; reject heuristic or ambiguous matches. |
| T-03-11 | Repudiation | Version/policy inputs | medium | mitigate | Canonical authority records every configured version and approved lock identity. |
</threat_model>

## Artifacts this phase produces

- Complete source/execution authority contracts and digests.
- Exact prior-lineage binding/resolution contracts.
- Full-field sensitivity and adversarial lineage tests.

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | Source-clear, repeatable local Skill candidate | COVERED — authority and lineage make provenance/reuse auditable. |
| REQ | GEN-04, GEN-05 | COVERED — complete provenance authority and stable local identity/update semantics. |
| RESEARCH | Complete Authority; Prelookup Authority; exact lineage rules; version sensitivity | COVERED. |
| CONTEXT | No Phase 3 CONTEXT.md decisions exist | N/A. |

<verification>
Run both task commands, each independently prefixed by Gate B3. Inspect the parameterized sensitivity case list against the authority field list in research.
</verification>

<success_criteria>
- No reuse-sensitive input can change without changing authority.
- Lineage retention requires one exact prior binding.
- All stale/collision/ambiguity cases close deterministically.
</success_criteria>

<output>
Create `.planning/phases/03-validated-skill-candidate/03-05-SUMMARY.md` when done.
</output>
