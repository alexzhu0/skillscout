---
phase: 05-automated-discovery-operations
plan: "03"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/state_branch/valid_state.json
  - tests/fixtures/state_branch/conflict_matrix.json
  - tests/test_operations_state.py
  - tests/test_state_branch.py
  - tests/test_discovery_application.py
  - tests/test_discovery_publication_handoff.py
  - tests/test_semantic_durability.py
  - tests/test_discovery_workflow.py
  - tests/test_discovery_security.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "Wave 0 defines reservation, rebuild, CAS, orchestration, workflow and leakage behavior before production implementation."
    - "Crash seams exist around every durability barrier and prove no budget refund or prefix replay."
    - "State branch fixtures cover bootstrap, valid restore, conflict, rollback, corruption and prospective-tree rejection."
  artifacts:
    - {path: tests/test_operations_state.py, provides: "Transactional ledger/rebuild RED contract"}
    - {path: tests/test_state_branch.py, provides: "Remote state CAS RED contract"}
    - {path: tests/test_discovery_application.py, provides: "Composition/crash/outcome RED contract"}
    - {path: tests/test_discovery_publication_handoff.py, provides: "Separate-entry-point and exact re-admission RED contract"}
    - {path: tests/test_semantic_durability.py, provides: "Three-store pre-request/post-result barrier RED contract"}
    - {path: tests/test_discovery_workflow.py, provides: "Schedule/concurrency/authority-zone RED contract"}
    - {path: tests/test_discovery_security.py, provides: "Cross-surface canary RED contract"}
  key_links:
    - {from: test interruption wrappers, to: durability barriers, via: "fail immediately before/after persisted checkpoints"}
  prohibitions:
    - "No fixture contains secret material, full third-party body, active database copy, WAL or journal."
    - "No Wave 0 test weakens Phase 2/3/4 expectations or uses live network."
---

<objective>
Freeze the multi-candidate operations and workflow acceptance matrix before implementation.

Purpose: Make the hardest constraints—durable non-refundable budgets and remote state integrity—fail visibly in Wave 0.
Output: RED state, orchestration, workflow and security suites plus bounded state-branch fixtures.

Locked decisions: D-01 thin composition; D-03 non-refundable reservations; D-04 unknown-outcome quarantine; D-05/D-06 durable CAS state; D-07 credential separation; D-08 allowlisted persistence.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@tests/test_state_integrity.py
@tests/test_pipeline_resume.py
@tests/test_phase3_pipeline.py
@tests/test_publication_recovery.py
@tests/test_publication_security.py
@.github/workflows/publish-candidate.yml
</context>

<feature>
  <name>Durable discovery operations acceptance matrix</name>
  <files>tests/fixtures/state_branch/* and nine Phase 5 test modules</files>
  <behavior>Exact 100/20 boundaries; crash before/after reservation and every semantic barrier; immutable consumption across all terminals; JSON rebuild; parent-bound non-force state push; business continuation; unknown quarantine; separate exact-commit publication handoff; workflow serialization and credential isolation.</behavior>
  <implementation>Create failing tests and fixtures first. Production plans 05-05 through 05-09 and 05-14 make them green.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-03-01: Freeze operations-ledger and state-branch failure matrices</name>
  <files>tests/fixtures/state_branch/valid_state.json, tests/fixtures/state_branch/conflict_matrix.json, tests/test_operations_state.py, tests/test_state_branch.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/adapters/publication_state.py, src/skillscout/adapters/github_publish.py, tests/test_state_integrity.py, tests/test_publication_recovery.py</read_first>
  <action>RED: specify exact operations schema fingerprint, transactional first-seen/semantic reservations, contiguous ordinals, duplicate-ID behavior, 100th/101st and 20th/21st boundaries, non-refund for every terminal/failure, canonical exports from main/operations/publication owners, validated import/rebuild through existing store seams, cross-store projection equality, missing/corrupt individual SQLite rebuild, killed writer, absent state branch, valid tree/root/objects, parent-bound fast-forward, 409/422, changed head, lying mutation response, reread mismatch, wrong path/mode/symlink, database/object swap and rollback root. Assert exact paths `state/root.json`, `state/objects/sha256/{first-two-hex}/{64-hex}.json`, and `state/databases/{pipeline,operations,publication}.sqlite3`; operations state must not duplicate main/Phase3/publication schemas. Assert D-05/D-06: complete JSON facts plus three consistent owned SQLite snapshots, observed parent, `force=false`, exact reread, conflict stop, no merge or pruning. Mark only missing production nodes `xfail(strict=True)` with exact reasons `phase5-wave0-operations-store-missing`, `phase5-wave0-store-export-missing` or `phase5-wave0-state-branch-missing`; fixture integrity, schema ownership assertions, collection and static capability checks remain ordinary tests.</action>
  <acceptance_criteria>
    - RED failures identify missing operations/state-branch implementations only.
    - A physically valid but projection-disagreeing SQLite database is rejected.
    - Conflict cases prove zero additional writes after conflict classification.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_operations_state.py tests/test_state_branch.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q -rxX tests/test_operations_state.py tests/test_state_branch.py -x</automated></verify>
  <done>The complete durable authority and CAS behavior is frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-02: Freeze composition, workflow and cross-surface security matrices</name>
  <files>tests/test_discovery_application.py, tests/test_discovery_publication_handoff.py, tests/test_semantic_durability.py, tests/test_discovery_workflow.py, tests/test_discovery_security.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/phase3.py, src/skillscout/application/publication.py, tests/test_pipeline_resume.py, tests/test_phase3_pipeline.py, tests/test_publication_security.py, .github/workflows/publish-candidate.yml</read_first>
  <action>RED: add tiny dependency fakes and interruption wrappers at page/dedup, discovery reservation, semantic attempt-start/result, candidate terminal and protected publication checkpoints. For both providers and Extractor/Generator/Reviewer, freeze crashes immediately before/after the three-store pre-request and post-result barriers plus sync failures that block request/retry/terminal and restart with zero ambiguous replay. Prove completed reuse, no prefix replay, business rejection continuation, confirmed bounded retry and D-04 unknown-outcome quarantine/manual. Freeze separate entry points: unprotected discovery ends after Phase 3 with bounded eligible locators/authorities and has no Phase 4 factory/publisher construction path; protected publication rereads the exact state commit, re-derives admission, then mints token and constructs/invokes PublicationApplication. Add one repository fixture returning three workflows whose Phase 3 paths end eligible handoff, qualification rejection and Reviewer rejection independently under one repository semantic reservation. Add fixed daily/manual workflow, shared non-cancelling group, authority-zone split, exact handoff, late catalog token and canary absence assertions. Add a queue-grammar test that grants production credit only after repository/hosted validation; otherwise require the documented fixed-group fallback. Mark only named missing production nodes strict-xfail with exact reasons `phase5-wave0-discovery-application-missing`, `phase5-wave0-semantic-durability-missing`, `phase5-wave0-protected-handoff-missing` or `phase5-wave0-discovery-workflow-missing`; security canary fixture/static tests remain ordinary.</action>
  <acceptance_criteria>
    - All business/operational outcome classes have distinct expected behavior.
    - Catalog credentials are unreachable from discovery/state job construction.
    - Candidate/secret canaries are absent from every proposed durable or observable surface.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest --collect-only -q tests/test_discovery_application.py tests/test_discovery_publication_handoff.py tests/test_semantic_durability.py tests/test_discovery_workflow.py tests/test_discovery_security.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q -rxX tests/test_discovery_application.py tests/test_discovery_publication_handoff.py tests/test_semantic_durability.py tests/test_discovery_workflow.py tests/test_discovery_security.py -x</automated></verify>
  <done>Orchestration and production-workflow behavior is frozen before code exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local ledger ↔ remote state branch | A crash or concurrent writer can split authority. |
| Discovery job → protected publication job | Only bounded candidate/state locators may cross. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-07 | Tampering | reservation ledger | high | mitigate | Transaction/crash/tamper RED matrix. |
| T-05-08 | Tampering | state ref | high | mitigate | Parent/force/reread/conflict RED matrix. |
| T-05-09 | Elevation | catalog credential boundary | high | mitigate | Job-order and secret-absence tests. |
| T-05-10 | Information Disclosure | state/log/tree | high | mitigate | Cross-surface canary tests. |
| T-05-SC | Tampering | dependencies | high | mitigate | Test-only files, locked uv. |
</threat_model>

## Artifacts and symbols produced

The complete Phase 5 RED acceptance matrix and deterministic state-branch fixture corpus.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Resumable audited end-to-end operations | 05-03 | COVERED |
| REQ | DISC-01..03 | Trigger, limits and facts | 05-03 | COVERED |
| REQ | OPS-02..03 | Rebuild/CAS/concurrency and non-disclosure | 05-03 | COVERED |
| RESEARCH | — | Mandatory scenario matrix and Wave 0 gaps | 05-03 | COVERED |
| CONTEXT | D-01,D-03,D-04,D-05,D-06,D-07,D-08 | Approved defaults | 05-03 | COVERED |

<verification>
Every focused module collects successfully and fails only at deliberate missing production interfaces/behavior.
</verification>

<success_criteria>
All remaining production plans have executable RED targets for their highest-risk boundaries.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-03-SUMMARY.md`.</output>
