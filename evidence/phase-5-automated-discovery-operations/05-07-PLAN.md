---
phase: 05-automated-discovery-operations
plan: "07"
type: execute
wave: 6
depends_on: [05-04, 05-12, 05-13, 05-14]
files_modified:
  - src/skillscout/application/discovery.py
  - tests/test_discovery_application.py
  - tests/test_discovery_security.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "DiscoveryApplication is an unprotected thin controller over existing Phase 2 and Phase 3 applications and ends after persisting eligible handoff facts."
    - "DiscoveryApplication has no Phase 4 factory, PublicationApplication, remote catalog publisher, or catalog credential access."
    - "Every page, reservation, semantic attempt and candidate terminal crosses the three-store state-branch durability barrier at the required ordering."
    - "Business outcomes continue safely; integrity/config/permission/conflict fails closed; unknown LLM outcomes quarantine without replay."
    - "Completed exact authorities prevent duplicate semantic work and eligible outputs are bounded locators/authorities, never trusted publication admissions."
    - "One repository reservation covers all 0–3 extracted workflows, which receive independent Phase 3 authorities, stable identities and mixed terminal continuation."
  artifacts:
    - {path: src/skillscout/application/discovery.py, provides: "Multi-candidate discovery state machine", exports: [DiscoveryApplication, DiscoveryDependencies, DiscoveryApplicationResult]}
    - {path: tests/test_discovery_application.py, provides: "Crash/resume/outcome/composition proof"}
  key_links:
    - {from: DiscoveryApplication, to: existing PhaseTwo/PhaseThree applications, via: "constructor-injected narrow factories without any Phase 4 dependency"}
    - {from: semantic attempt start/result, to: provider request/retry/terminal, via: "Plan 05-14 three-store remote durability barrier"}
    - {from: one extracted repository, to: 0–3 Phase 3 workflow paths, via: "one repository reservation plus complete per-workflow authority and stable identity"}
    - {from: eligible Phase 3 result, to: protected entry point, via: "bounded locator/authority persisted at an exact state commit; no in-process publication call"}
  prohibitions:
    - "No second pipeline, agent framework, stage reimplementation, mutable local budget, semantic auto-replay after unknown outcome, or catalog bypass."
---

<objective>
Compose Search, durable state and the already verified Phase 2/3 candidate applications into one resumable unprotected discovery run.

Purpose: Deliver the discovery half of the Phase 5 operational slice while closing the authority handoff before protected publication.
Output: DiscoveryApplication, bounded eligible handoff facts and complete interruption/outcome/security proof.

Locked decisions: D-01 through D-08, especially thin Phase 2/3 composition, durable semantic barriers, unknown quarantine and separate protected Phase 4 authority.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/application/pipeline.py
@src/skillscout/application/phase3.py
@src/skillscout/application/ports.py
@src/skillscout/adapters/operations_state.py
@src/skillscout/adapters/state_branch.py
@.planning/phases/05-automated-discovery-operations/05-04-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-12-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-13-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-14-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-07-01: Compose exact run restore, Search and candidate funnel</name>
  <files>src/skillscout/application/discovery.py, src/skillscout/application/ports.py, tests/test_discovery_application.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/application/phase3.py, src/skillscout/adapters/operations_state.py, src/skillscout/adapters/state_branch.py, tests/test_discovery_application.py</read_first>
  <behavior>
    - Restore/verify exact state, resume one matching unfinished authority or create one, then acquire reviewed pages round-robin.
    - Page plus first-seen/duplicate transaction is remotely durable before the next page; stop at 100 selected IDs.
    - Existing Scout/Filter/Reader runs for each selected repository; business rejection persists and continues.
    - Semantic reservation becomes remotely durable immediately before the first real Extractor request and never resets.
    - Extractor 0–3 workflows fan out through independent complete Phase 3 authorities without additional repository reservations.
  </behavior>
  <action>Turn composition/reservation RED tests GREEN without defining publication-facing dependencies. Run order: restore the exact three-store bundle; exact unfinished lookup with ambiguity failure; round-robin Search; persist page and D-02 dedup; sync; invoke existing Phase 2 pre-Extractor path; record business rejection; reserve D-03 semantic repository once; cross the Plan 05-14 barrier; then permit existing Extractor. For each of its 0–3 `WorkflowSpec` outputs, derive and preserve the existing complete Phase 3 workflow authority/fingerprint/lineage independently, invoke Phase 3, persist its own terminal, and continue siblings after business rejection or quarantine. Persist each eligible result only as a bounded run/candidate/workflow locator plus exact authority and state-root facts for later protected re-admission. A three-workflow fixture must prove eligible handoff + qualification rejection + Reviewer rejection under one repository reservation and three distinct stable identities. Complete reuse short-circuits before mutable/semantic construction. Keep raw Reader bundle memory-only and preserve all existing stage accounting.</action>
  <acceptance_criteria>
    - Page/candidate/semantic crashes resume without prefix replay or extra slot.
    - Filter/Reader/no-workflow/qualification/validation/review business terminals continue to later safe candidates.
    - Exact Phase 2/3 completion generates no new semantic reservation or call.
    - A 3-workflow repository consumes one semantic reservation and preserves three independent mixed outcomes/identities without sibling suppression.
    - Discovery emits no publication admission or catalog mutation; eligible output is only a bounded locator/authority persisted in the exact state commit.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_application.py -k "search or reservation or resume or business" -x</automated></verify>
  <done>Selected repositories flow through existing verified applications under durable budget authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-07-02: Close semantic outcomes and the unprotected publication handoff</name>
  <files>src/skillscout/application/discovery.py, tests/test_discovery_application.py, tests/test_discovery_security.py</files>
  <read_first>src/skillscout/application/phase3.py, src/skillscout/application/publication.py, tests/test_phase3_pipeline.py, tests/test_discovery_application.py, tests/test_discovery_security.py</read_first>
  <behavior>
    - Confirmed-retryable semantic failure follows bounded policy; Plan 05-11/13 outcome-unknown consumes attempt and quarantines with no automatic replay.
    - Eligible candidates are persisted as bounded locators/authorities and DiscoveryApplication returns only the exact state commit that contains them.
    - Construction fails if any Phase 4 factory, PublicationApplication, remote catalog publisher, or catalog credential resolver is offered to discovery.
    - Candidate terminals and aggregate run health are separate; integrity/config/permission/state conflict stops the run.
  </behavior>
  <action>Consume the closed semantic dispositions and barrier outcomes delivered by Plans 05-13/14: project a workflow quarantine/manual result without replay when Phase 2 or any Phase 3 sibling reports unknown, and continue other safe workflows/candidates. It must never reinterpret unknown as confirmed retryable. End the application after a final verified three-store sync that persists bounded eligible locators/authorities and returns its exact state-head commit. Add constructor and import-boundary tests proving discovery has no Phase 4 factory slot, cannot construct `PublicationApplication`, cannot resolve catalog credentials, and cannot invoke any remote publisher; use spies that fail on construction as well as invocation. Add closed-handoff tests proving an eligible result alone grants no publication authority and that only the later protected entry point can re-read it. Collapse unexpected exceptions to closed failure codes and never persist raw exception text per D-08.</action>
  <acceptance_criteria>
    - Unknown outcome has one provider request, one consumed reservation/attempt and no automatic next request.
    - Crash before/after final discovery sync resumes to the same eligible locator/authority and exact state commit without catalog calls.
    - A forbidden publication factory/publisher spy has zero constructions and zero invocations throughout discovery.
    - Operational fatal classes stop; business/quarantine outcomes remain auditable and allow safe continuation.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_application.py tests/test_discovery_security.py -k "unknown or handoff or forbidden or publisher or eligible or health" -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/application/discovery.py tests/test_discovery_application.py tests/test_discovery_security.py</automated></verify>
  <done>The unprotected run is cost-bounded, crash-safe, honest about ambiguous semantic effects, and structurally incapable of publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable reservation → semantic provider | Crash window can create uncounted/replayed cost. |
| Phase 3 eligibility → persisted handoff | Eligibility is untrusted until protected re-admission from an exact state commit. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-22 | Repudiation | semantic unknown outcome | high | mitigate | Closed Plan 05-11/13 disposition, pre-call durable attempt, quarantine, no auto replay. |
| T-05-23 | Tampering | resume authority | high | mitigate | Exact authority lookup and ambiguity failure. |
| T-05-24 | Elevation | publication handoff | high | mitigate | No Phase 4 dependency in discovery; bounded persisted locator only. |
| T-05-25 | Information Disclosure | composed payloads | high | mitigate | WorkflowSpec boundary and canary sweep. |
| T-05-46 | Tampering | multi-workflow fan-out | high | mitigate | Complete per-workflow authority, stable identity and mixed-terminal tests under one repository reservation. |
| T-05-SC | Tampering | dependencies | high | mitigate | No package or agent framework. |
</threat_model>

## Artifacts and symbols produced

`DiscoveryApplication`, result/outcome projection and closed eligible-locator handoff.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Search through exact resumable Draft path | 05-07 | COVERED |
| REQ | DISC-01..03 | Search, limits, audit | 05-07 | COVERED |
| REQ | OPS-02..03 | Durable barriers and safe surfaces | 05-07 | COVERED |
| RESEARCH | — | Sequencing/outcome taxonomy/composition | 05-07 | COVERED |
| CONTEXT | D-01..D-08 | Approved defaults | 05-07 | COVERED |

<verification>
Composition/crash/security suites plus existing publication recovery and Ruff.
</verification>

<success_criteria>
The unprotected application safely processes the bounded funnel through Phase 3 and cannot construct or invoke publication authority.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-07-SUMMARY.md`.</output>
