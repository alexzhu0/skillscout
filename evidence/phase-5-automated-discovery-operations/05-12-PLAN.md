---
phase: 05-automated-discovery-operations
plan: "12"
type: execute
wave: 3
depends_on: [05-05, 05-06]
files_modified:
  - src/skillscout/adapters/state.py
  - src/skillscout/adapters/operations_state.py
  - src/skillscout/adapters/publication_state.py
  - tests/test_state_integrity.py
  - tests/test_operations_state.py
  - tests/test_publication_recovery.py
  - tests/test_state_branch.py
autonomous: true
requirements: [OPS-02, OPS-03]
must_haves:
  truths:
    - "The state bundle has exactly three owned database snapshots at fixed paths and complete content-addressed JSON rebuild facts."
    - "Existing main and publication stores expose canonical export and validated import/rebuild through their existing schema/transition validators."
    - "Operations state owns only discovery facts and never duplicates Phase 1/3 or Phase 4 private schemas."
    - "Cross-store projection equality is required before restore or reuse authority."
  artifacts:
    - {path: src/skillscout/adapters/state.py, provides: "Main pipeline/Phase3 canonical export and validated rebuild seams"}
    - {path: src/skillscout/adapters/publication_state.py, provides: "Publication canonical export and validated rebuild seams"}
    - {path: src/skillscout/adapters/operations_state.py, provides: "Discovery-only export and cross-store bundle assembly"}
  key_links:
    - {from: state/databases/pipeline.sqlite3, to: SQLiteStateStore, via: "store-owned serialize/export/import/rebuild"}
    - {from: state/databases/operations.sqlite3, to: OperationsStateStore, via: "discovery-only facts"}
    - {from: state/databases/publication.sqlite3, to: PublicationStateStore, via: "store-owned serialize/export/import/rebuild"}
  prohibitions:
    - "No private schema duplication in operations_state, no direct SQL replay into another store, no live DB copy and no cross-store success without exact root equality."
---

<objective>
Integrate all existing stores into one exact, ownership-preserving state bundle and rebuild protocol.

Purpose: Make JSON a complete rebuild authority without teaching the discovery store private Phase 1/3/4 schemas.
Output: Three store-owned export/import/rebuild seams, fixed remote paths and cross-store equality tests.

Locked decisions: D-05 complete JSON plus consistent SQLite snapshots; D-08 exact allowlisted paths/facts; D-09 no pruning.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/state.py
@src/skillscout/adapters/operations_state.py
@src/skillscout/adapters/publication_state.py
@src/skillscout/adapters/state_branch.py
@tests/test_state_integrity.py
@tests/test_publication_recovery.py
@.planning/phases/05-automated-discovery-operations/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-12-01: Add store-owned canonical export and validated rebuild seams</name>
  <files>src/skillscout/adapters/state.py, src/skillscout/adapters/publication_state.py, tests/test_state_integrity.py, tests/test_publication_recovery.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/adapters/publication_state.py, tests/test_state_integrity.py, tests/test_publication_recovery.py</read_first>
  <behavior>
    - Main store exports/rebuilds Phase 1 and Phase 3 facts through existing row/model/chain validators.
    - Publication store exports/rebuilds attempts/checkpoints/records through existing transition/hash/binding validators.
    - Each store produces a consistent locked snapshot and rejects missing, reordered, tampered or unknown facts before replacement.
  </behavior>
  <action>RED: mutation-test canonical export, valid import, missing/corrupt snapshot rebuild, unknown/duplicate/reordered/tampered facts and killed writer for both existing stores. GREEN: add explicit bounded canonical export and validated import/rebuild APIs to `SQLiteStateStore` and `PublicationStateStore`. Replay JSON through each store's existing insertion, transition, hash, terminal and chain validation paths—never direct unvalidated SQL or a schema clone in `operations_state.py`. Build fresh private databases, run exact schema/integrity/foreign-key/full-chain checks, serialize under retained store locks and atomically replace only after verification.</action>
  <acceptance_criteria>
    - Existing state and publication regression suites remain green.
    - Valid JSON rebuilds missing/corrupt owned DB; one mutated fact fails before replacement.
    - Store APIs expose only their owned canonical facts, not raw source/provider data.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_state_integrity.py tests/test_publication_recovery.py -k "export or import or rebuild or snapshot or integrity" -x</automated></verify>
  <done>Existing stores can independently export and rebuild their own canonical authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-12-02: Assemble exact three-database bundle and require projection equality</name>
  <files>src/skillscout/adapters/operations_state.py, tests/test_operations_state.py, tests/test_state_branch.py</files>
  <read_first>src/skillscout/adapters/state.py, src/skillscout/adapters/operations_state.py, src/skillscout/adapters/publication_state.py, src/skillscout/adapters/state_branch.py, tests/test_operations_state.py, tests/test_state_branch.py</read_first>
  <behavior>
    - Exact paths are root, content-addressed objects, pipeline DB, operations DB and publication DB under the approved state tree.
    - Root binds each store projection/digest and shared run/candidate/publication keys.
    - Restore/rebuild re-exports all stores and requires exact cross-store/root equality before reuse.
  </behavior>
  <action>RED: add path ownership, swapped-database, cross-store key/digest mismatch, partial rebuild and projection-equality mutations. GREEN: make `operations_state.py` orchestrate the three store-owned seams without reading or duplicating their schemas. Emit exactly `state/root.json`, `state/objects/sha256/{first-two-hex}/{64-hex}.json`, `state/databases/pipeline.sqlite3`, `state/databases/operations.sqlite3`, and `state/databases/publication.sqlite3`. Bind every DB/object digest and shared authority in the root; on restore call the owning store, then compare fresh exports against the complete root before state-branch or reuse success. Retain all reachable objects.</action>
  <acceptance_criteria>
    - Exact path/owner map is enforced by domain, state-branch and mutation tests.
    - Swapping any two DBs or changing a shared key/digest fails closed.
    - `operations_state.py` contains no copied Phase 1/3/4 schema statements or private-table replay.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_operations_state.py tests/test_state_branch.py tests/test_state_integrity.py tests/test_publication_recovery.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/state.py src/skillscout/adapters/operations_state.py src/skillscout/adapters/publication_state.py</automated></verify>
  <done>The remote bundle is complete, exact and reconstructible only through store-owned validators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON authority → three private store schemas | One coordinator must not bypass store ownership or validators. |
| Three DB projections → root authority | Individually valid stores may disagree across shared keys. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-40 | Tampering | private-schema replay | high | mitigate | Store-owned import/rebuild APIs only. |
| T-05-41 | Tampering | cross-store mismatch | high | mitigate | Fresh export and exact root equality. |
| T-05-42 | Information Disclosure | state bundle | high | mitigate | Closed facts and fixed path allowlist. |
| T-05-SC | Tampering | dependencies | high | mitigate | Existing SQLite/localfs stack only. |
</threat_model>

## Artifacts and symbols produced

Canonical export/import/rebuild APIs for `SQLiteStateStore` and `PublicationStateStore`, three-database bundle assembly, exact path ownership and projection equality.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Complete durable rebuild across all existing state | 05-12 | COVERED |
| REQ | OPS-02 | SQLite plus JSON rebuild authority | 05-12 | COVERED |
| REQ | OPS-03 | Trimmed fixed state paths | 05-12 | COVERED |
| RESEARCH | — | Exact three-store ownership and rebuild | 05-12 | COVERED |
| CONTEXT | D-05,D-08,D-09 | Approved defaults | 05-12 | COVERED |

<verification>
Run all three store suites, state-branch integration and Ruff; no live network.
</verification>

<success_criteria>
Every state fact can rebuild its owning database, and no mismatched combination can grant reuse or push authority.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-12-SUMMARY.md`.</output>
