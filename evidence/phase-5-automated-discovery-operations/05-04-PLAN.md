---
phase: 05-automated-discovery-operations
plan: "04"
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/skillscout/adapters/github.py
  - tests/test_github_search.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-03]
must_haves:
  truths:
    - "The existing serial GitHubReadClient executes only exact reviewed repository Search requests."
    - "Each response becomes strict page/item/rate observations; provider prose and arbitrary Link/header data are discarded."
    - "Pagination, body size, redirects, rate exhaustion and failures stay bounded and sanitized."
  artifacts:
    - {path: src/skillscout/adapters/github.py, provides: "Bounded repository Search extension", exports: [GitHubReadClient]}
    - {path: tests/test_github_search.py, provides: "GREEN Search boundary proof"}
  key_links:
    - {from: GitHubReadClient.search_repositories, to: SearchPageObservationV1, via: "lenient raw parse then strict domain projection"}
  prohibitions:
    - "No second general GitHub client, SDK retry, arbitrary URL follow or Search qualifier trust."
    - "No description/topic/text-match/provider-body persistence."
---

<objective>
Implement bounded repository Search on the existing verified read client.

Purpose: Deliver DISC-01/DISC-03 metadata acquisition without widening remote authority or exposing untrusted prose.
Output: GREEN Search adapter and transport tests.

Locked decisions: D-02 numeric repository ID; D-08 allowlisted fields only.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/github.py
@src/skillscout/adapters/github_publish.py
@src/skillscout/domain/discovery.py
@tests/test_github_search.py
@.planning/phases/05-automated-discovery-operations/05-01-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-02-SUMMARY.md

<interfaces>
`GitHubReadClient` already owns fixed `GITHUB_API_VERSION = "2022-11-28"`, Authorization headers, `REMOTE_READ`, capped streaming reads, sanitized `_get/_send`, and `_rate_limit_facts`. Extend that class; preserve existing metadata/tree/license/blob methods.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-04-01: Project exact Search pages and candidates</name>
  <files>src/skillscout/adapters/github.py, tests/test_github_search.py</files>
  <read_first>src/skillscout/adapters/github.py, src/skillscout/domain/discovery.py, tests/test_github_search.py, tests/test_github_adapter.py</read_first>
  <behavior>
    - One call uses GET `/search/repositories` with exact q/sort/order/per_page/page from the reviewed policy.
    - Raw envelope consumes only total_count, incomplete_results and allowlisted item fields.
    - Output binds exact page/item ordinals, integer next page, request ID and complete numeric rate facts.
  </behavior>
  <action>Use the RED tests from 05-02. Add lenient raw Search envelope/owner/item models and `GitHubReadClient.search_repositories(query, page)` accepting strict query authority, not free strings. Construct strict Plan 05-01 observations; do not expose raw dictionaries. Validate each numeric repository ID and owner/name/public/fork/archive/disabled/default-branch observation but treat it only as discovery metadata—existing Phase 2 Scout remains admission authority. Preserve one serial client and exact fixed API host/version.</action>
  <acceptance_criteria>
    - Happy, multi-page, duplicate, rename and incomplete fixtures pass.
    - Hostile discarded fields never appear in returned/repr/serialized observations.
    - Existing GitHub adapter suite remains byte-compatible in behavior.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_github_search.py -k "page or duplicate or incomplete" tests/test_github_adapter.py -x</automated></verify>
  <done>Reviewed Search requests produce complete strict observations only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-04-02: Close pagination, rate and resource failures</name>
  <files>src/skillscout/adapters/github.py, tests/test_github_search.py</files>
  <read_first>src/skillscout/adapters/github.py, src/skillscout/adapters/github_publish.py, tests/test_github_search.py</read_first>
  <behavior>
    - Link is validated to the fixed Search endpoint and exact policy parameters, then reduced to a bounded integer cursor.
    - 429 and rate-exhausted 403 become confirmed deferred/transient facts; malformed/missing mandatory facts and hostile links fail permanently.
    - Redirect, payload and retry delay remain bounded; raw provider failures never cross diagnostics.
  </behavior>
  <action>Turn the remaining RED matrix GREEN. Adapt the publication pagination validator but narrow it to the Search endpoint, same HTTPS host, exact reviewed q/sort/order/per_page and next page within policy. Extend rate projection to limit/remaining/used/reset/resource and preserve only the existing bounded request ID. Honor Retry-After/reset through the existing deterministic policy without parallelism or SDK retries. Collapse malformed, oversized, redirect and provider failures to closed `SafeFailure` codes with `from None`.</action>
  <acceptance_criteria>
    - Every hostile Link/body/rate/error case passes without extra requests.
    - Token canary occurs only in Authorization.
    - Full focused Search plus existing adapter suite is green and Ruff clean.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_github_search.py tests/test_github_adapter.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/github.py tests/test_github_search.py</automated></verify>
  <done>Search acquisition is bounded, rate-aware and fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Search response → discovery domain | Provider controls all response bytes and headers. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-11 | Spoofing | Link pagination | high | mitigate | Fixed host/endpoint/parameter validation and integer cursor. |
| T-05-12 | Information Disclosure | provider fields/errors | high | mitigate | Lenient consumed parse followed by strict projection. |
| T-05-13 | Denial of Service | payload/rate loop | high | mitigate | Streaming cap, serial request, bounded defer and page ceiling. |
| T-05-SC | Tampering | dependency graph | high | mitigate | No new package or lock change. |
</threat_model>

## Artifacts and symbols produced

`GitHubReadClient.search_repositories` plus internal raw Search models and strict projection helpers.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Automated bounded Search | 05-04 | COVERED |
| REQ | DISC-01 | Reviewed Search execution | 05-04 | COVERED |
| REQ | DISC-02 | Stable numeric IDs supplied to ledger | 05-04 | COVERED |
| REQ | DISC-03 | Page/source/rate facts | 05-04 | COVERED |
| REQ | OPS-03 | Discard raw provider content | 05-04 | COVERED |
| RESEARCH | — | Serial strict Search adapter | 05-04 | COVERED |
| CONTEXT | D-02,D-08 | Approved defaults | 05-04 | COVERED |

<verification>
Focused transport tests, existing GitHub regression suite, Ruff; no live network.
</verification>

<success_criteria>
Every reviewed query page can be acquired and audited without trusting or retaining provider prose.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-04-SUMMARY.md`.</output>
