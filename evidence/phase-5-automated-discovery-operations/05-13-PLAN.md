---
phase: 05-automated-discovery-operations
plan: "13"
type: execute
wave: 5
depends_on: [05-11, 05-14]
files_modified:
  - src/skillscout/application/pipeline.py
  - src/skillscout/application/phase3.py
  - tests/test_pipeline_resume.py
  - tests/test_phase3_pipeline.py
autonomous: true
requirements: [DISC-02, OPS-02, OPS-03]
must_haves:
  truths:
    - "Phase 2 Extractor and Phase 3 Generator/Reviewer runners retry only confirmed-retryable failures."
    - "Each runner persists attempt-start, then crosses the three-store remote durability barrier before every provider request."
    - "Each decided, confirmed-retryable, or outcome-unknown result crosses the barrier before retry authorization or terminal projection; sync failure blocks request/retry."
    - "Outcome-unknown attempts are durably consumed and terminally quarantined/manual with exactly one request and no automatic next attempt."
    - "Existing decided business outcomes, completed reuse, budgets and checkpoint continuity remain unchanged."
  artifacts:
    - {path: src/skillscout/application/pipeline.py, provides: "Phase 2 classified semantic retry orchestration"}
    - {path: src/skillscout/application/phase3.py, provides: "Phase 3 classified semantic retry orchestration"}
    - {path: tests/test_pipeline_resume.py, provides: "Extractor unknown/retry resume proof"}
    - {path: tests/test_phase3_pipeline.py, provides: "Generator/Reviewer unknown/retry resume proof"}
  key_links:
    - {from: SemanticProviderFailure.disposition, to: Phase 2/3 attempt ledger, via: "confirmed-only retry branch; unknown quarantine branch"}
    - {from: Phase 2/3 attempt ledgers, to: provider request/retry/terminal, via: "Plan 05-14 remote-confirmed three-store barrier"}
  prohibitions:
    - "No legacy generic transient branch may automatically retry an outcome-unknown semantic failure."
---

<objective>
Propagate the closed semantic transport disposition and explicit three-store remote durability barrier through existing Phase 2 and Phase 3 retry state machines.

Purpose: Make the no-ambiguous-replay guarantee durable across crash/resume, not merely an adapter property.
Output: Updated runners and exact attempt/request/history tests.

Locked decision: D-04 quarantine/manual retry for every ambiguous OpenAI or DeepSeek semantic effect.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/application/pipeline.py
@src/skillscout/application/phase3.py
@src/skillscout/adapters/semantic_provider.py
@tests/test_pipeline_resume.py
@tests/test_phase3_pipeline.py
@.planning/phases/05-automated-discovery-operations/05-11-SUMMARY.md
@.planning/phases/05-automated-discovery-operations/05-14-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-13-01: Enforce classified Extractor retry and quarantine</name>
  <files>src/skillscout/application/pipeline.py, tests/test_pipeline_resume.py</files>
  <read_first>src/skillscout/application/pipeline.py, src/skillscout/adapters/semantic_provider.py, src/skillscout/adapters/openai_extract.py, tests/test_pipeline_resume.py, tests/test_openai_extract.py</read_first>
  <behavior>
    - Confirmed-retryable Extractor failure consumes one persisted attempt and may start the next bounded attempt.
    - Outcome-unknown Extractor failure persists its one attempt as consumed/quarantined and cannot resume into another automatic call.
    - Persisted attempt-start is remotely confirmed before request; persisted result is remotely confirmed before retry or terminal.
    - Barrier failure before a request yields zero requests; barrier failure after a result yields zero retry/terminal transition until restart completes the same sync.
    - Completed prefix, decided outcomes and exact completed reuse remain no-replay.
  </behavior>
  <action>RED: add OpenAI and DeepSeek Extractor cases for confirmed retry followed by success, timeout/connection/ambiguous 5xx unknown, and crashes immediately before/after both Plan 05-14 barriers. GREEN: inject the explicit `ThreeStoreDurabilityBarrier` into `PipelineRunner`. Persist attempt-start in the owning store, invoke the barrier, and require its verified remote state-head acknowledgement before each provider request. Persist every decided, confirmed-retryable, or outcome-unknown result, invoke the barrier again, and require acknowledgement before authorizing the next retry or projecting a terminal. A barrier sync failure must return a closed operational stop and issue neither the guarded request nor a retry; restart completes/recognizes the same pending barrier without manufacturing a new attempt. Then branch on Plan 05-11 disposition: only confirmed-retryable enters existing finite retry accounting, while unknown exits quarantined/manual. Preserve retry digest, attempt ceiling, prefix checkpoints and completed reuse.</action>
  <acceptance_criteria>
    - Unknown cases show exactly one lifetime provider request across restart.
    - Confirmed retry request count equals bounded ledger attempt count.
    - Pre-request sync failure yields zero provider requests; post-result sync failure yields zero retry/terminal advancement until restart remotely confirms the same three-store state.
    - Full Phase 2 resume/extraction regression suite remains green.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_pipeline_resume.py tests/test_openai_extract.py -k "semantic or extractor or retry or unknown or resume" -x</automated></verify>
  <done>Phase 2 cannot replay an ambiguous Extractor effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-13-02: Enforce classified Generator and Reviewer retry and quarantine</name>
  <files>src/skillscout/application/phase3.py, tests/test_phase3_pipeline.py</files>
  <read_first>src/skillscout/application/phase3.py, src/skillscout/adapters/semantic_provider.py, src/skillscout/adapters/openai_generate.py, src/skillscout/adapters/openai_review.py, tests/test_phase3_pipeline.py</read_first>
  <behavior>
    - Generator and Reviewer confirmed failures may consume the next existing bounded attempt.
    - Generator or Reviewer unknown failure closes the workflow as quarantined/manual and is never auto-replayed on restart.
    - Attempt-start and every result are remotely confirmed through all three stores before request and before retry/terminal respectively.
    - Independent sibling workflows continue after one workflow quarantines, without sharing attempt identity.
  </behavior>
  <action>RED: extend the Phase 3 attempt/resume matrix for both providers and both Generator/Reviewer stages, including confirmed retry, timeout/connection/ambiguous 5xx unknown, crashes immediately before/after each pre-request and post-result barrier, restart and sibling-workflow continuation. GREEN: inject the same `ThreeStoreDurabilityBarrier` into `PhaseThreeRunner`. For Generator and Reviewer independently, persist attempt-start and require verified remote sync before calling the provider; persist decided/confirmed-retryable/outcome-unknown result and require verified remote sync before retry authorization or terminal projection. Sync failure blocks the request/retry and restart resumes the exact pending barrier with zero ambiguous replay. Authorize another attempt only for confirmed-retryable; unknown becomes quarantined/manual without fabricating a business decision. Preserve all existing 12 business terminals, exact completed projections, model/usage evidence and per-workflow authority.</action>
  <acceptance_criteria>
    - Every unknown Generator/Reviewer case has one request and zero automatic replay across restart.
    - Confirmed retry remains bounded and durable.
    - Every barrier failure matrix has exact request/attempt counts and no transition past an unconfirmed remote state.
    - Existing Phase 3 terminal, resume, reuse and budget suites remain green.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase3_pipeline.py tests/test_openai_generate.py tests/test_openai_review.py -k "semantic or generator or reviewer or retry or unknown or resume" -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/application/pipeline.py src/skillscout/application/phase3.py</automated></verify>
  <done>Phase 3 cannot replay ambiguous Generator/Reviewer effects and keeps sibling workflow authority independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter disposition/local attempt → three-store remote state | A correct local result cannot authorize request/retry/terminal until remotely durable. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-43 | Repudiation | runner resume | high | mitigate | Persist disposition, request count and terminal quarantine. |
| T-05-44 | Denial of Service | retry loop | high | mitigate | Confirmed-only existing attempt ceilings. |
| T-05-45 | Tampering | sibling workflow attempts | high | mitigate | Complete per-workflow authority and isolated histories. |
| T-05-49 | Repudiation | remote durability barrier | high | mitigate | Verified three-store state-head acknowledgement before request and after every result. |
| T-05-SC | Tampering | dependencies | high | mitigate | No package change. |
</threat_model>

## Artifacts and symbols produced

Classified semantic retry/quarantine branches in `PipelineRunner` and `PhaseThreeRunner`, wired to the three-store remote barrier with crash/resume request-count evidence.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Resume without duplicate semantic effects | 05-13 | COVERED |
| REQ | DISC-02 | Semantic budget/retry integrity | 05-13 | COVERED |
| REQ | OPS-02 | Durable attempt/quarantine resume | 05-13 | COVERED |
| REQ | OPS-03 | Closed semantic diagnostics | 05-13 | COVERED |
| RESEARCH | — | Resolved confirmed-vs-unknown orchestration | 05-13 | COVERED |
| CONTEXT | D-04 | Unknown quarantine/manual | 05-13 | COVERED |

<verification>
Phase 2/3 semantic attempt, restart, budget and regression suites plus Ruff.
</verification>

<success_criteria>
Across both providers and all three semantic stages, only proven retryable failures can cause another automatic request.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-13-SUMMARY.md`.</output>
