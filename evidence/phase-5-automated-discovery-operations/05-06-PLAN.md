---
phase: 05-automated-discovery-operations
plan: "06"
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - src/skillscout/adapters/state_branch.py
  - tests/test_state_branch.py
autonomous: true
requirements: [OPS-02, OPS-03]
must_haves:
  truths:
    - "A narrow state client reads only the fixed state branch and exact root/object/three-database allowlisted tree."
    - "Every state update has the observed head as sole parent, uses force=false and is accepted only after exact reread."
    - "Conflict, rollback, malformed trees/objects and prospective leaks stop without overwrite or merge."
  artifacts:
    - {path: src/skillscout/adapters/state_branch.py, provides: "Fixed state-branch read/verify/write capability", exports: [StateBranchClient, StateBranchStore]}
    - {path: tests/test_state_branch.py, provides: "CAS, restore and leak evidence"}
  key_links:
    - {from: StateBranchStore.sync, to: refs/heads/skillscout-state, via: "immutable objects + parent-bound commit + non-force ref + reread"}
  prohibitions:
    - "No PR/reviewer/catalog methods, force update, merge, unexpected deletion, arbitrary path/mode or object pruning."
---

<objective>
Implement exact remote state restore and fast-forward-only synchronization.

Purpose: Preserve durable JSON/SQLite authority across ephemeral runners without lost updates or widened GitHub capability.
Output: Narrow state-branch adapter/store and GREEN reconciliation tests.

Locked decisions: D-05 complete state bundle; D-06 observed-parent force-false reread conflict stop; D-08 exact tree allowlist; D-09 no pruning.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/adapters/github_publish.py
@src/skillscout/domain/discovery.py
@tests/test_state_branch.py
@tests/test_publication_recovery.py
@.planning/phases/05-automated-discovery-operations/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-06-01: Implement fixed state-branch observation and restore</name>
  <files>src/skillscout/adapters/state_branch.py, tests/test_state_branch.py</files>
  <read_first>src/skillscout/adapters/github_publish.py, src/skillscout/domain/discovery.py, tests/test_state_branch.py, tests/test_publication_recovery.py</read_first>
  <behavior>
    - Client is fixed to one configured repository identity and `refs/heads/skillscout-state`.
    - Read admits only `state/root.json`, `state/objects/sha256/{first-two-hex}/{64-hex}.json`, and `state/databases/{pipeline,operations,publication}.sqlite3` with regular 100644 mode and bounded sizes/counts.
    - Canonical root/prior/object/database facts are verified before local restore authority.
  </behavior>
  <action>Turn restore RED tests GREEN with a narrower client exposing only ref/commit/tree/blob read plus blob/tree/commit/ref creation. Preserve GitHub API version, body caps, redirect rejection, request-ID validation and sanitized failures from the publication adapter. Validate the exact fixed root/object/three-database paths, modes, locators, canonical bytes and all digests before handing owned projections to Plans 05-05/05-12. Reject symlink/submodule, WAL/journal/temp, unexpected path, missing/swapped database/object, rollback or oversized bundle.</action>
  <acceptance_criteria>
    - Absent branch bootstrap and valid restore are distinct closed observations.
    - Every malformed tree/object/root case fails before local reuse.
    - State client has no catalog/PR/reviewer or arbitrary-request method.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_state_branch.py -k "restore or tree or object or rollback or absent" -x</automated></verify>
  <done>Only an exact verified state branch grants restore authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-06-02: Implement parent-bound non-force state synchronization</name>
  <files>src/skillscout/adapters/state_branch.py, tests/test_state_branch.py</files>
  <read_first>src/skillscout/adapters/github_publish.py, src/skillscout/application/publication.py, tests/test_state_branch.py</read_first>
  <behavior>
    - Prospective exact tree is scanned before any blob write.
    - New commit has exactly one parent equal to observed head; absent bootstrap creates the fixed ref once.
    - Update serializes force false, then re-reads exact ref/commit/tree; 409/422/head/response/reread mismatch is conflict.
  </behavior>
  <action>Implement `StateBranchStore.sync(bundle, observed_head)`. Prevalidate the complete allowlisted bundle and canary scan; create immutable blobs/tree/commit with sole observed parent; create or update only the fixed state ref with `force=False`; independently reread and verify exact SHA/tree. Per D-06 never force, merge SQLite, retry a conflict as last-writer-wins, or delete unexpected files. Retain old immutable objects per D-09.</action>
  <acceptance_criteria>
    - Normal fast-forward and bootstrap pass.
    - Changed head, 409/422, lying response or reread mismatch becomes one closed conflict with no further writes.
    - Prospective secret/source canary fails before first blob creation.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_state_branch.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/adapters/state_branch.py tests/test_state_branch.py</automated></verify>
  <done>Remote state advances only through independently verified fast-forward commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Remote Git tree → local state | Remote bytes may be corrupt, rolled back or maliciously shaped. |
| Local state bundle → remote ref | Concurrent writers may move the observed head. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-18 | Tampering | remote tree/root | high | mitigate | Path/mode/size/digest/prior-root validation. |
| T-05-19 | Tampering | state ref race | high | mitigate | Sole observed parent, force false, exact reread. |
| T-05-20 | Information Disclosure | prospective tree | high | mitigate | Exact allowlist and pre-blob canary scan. |
| T-05-21 | Denial of Service | conflict | medium | mitigate | Closed conflict stop and later verified resume. |
| T-05-SC | Tampering | dependencies | high | mitigate | Existing httpx stack only. |
</threat_model>

## Artifacts and symbols produced

`StateBranchClient`, `StateBranchStore`, verified bundle observation and sync/conflict results.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Ephemeral-runner durable recovery | 05-06 | COVERED |
| REQ | OPS-02 | Dedicated state branch and safe conflict | 05-06 | COVERED |
| REQ | OPS-03 | Exact non-secret state tree | 05-06 | COVERED |
| RESEARCH | — | Parent-bound CAS and reread | 05-06 | COVERED |
| CONTEXT | D-05,D-06,D-08,D-09 | Approved defaults | 05-06 | COVERED |

<verification>
State-branch fixture suite and Ruff; no live network.
</verification>

<success_criteria>
Remote state can restore or advance exactly once without force, merge, overwrite ambiguity or data leakage.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-06-SUMMARY.md`.</output>
