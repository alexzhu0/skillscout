---
phase: 05-automated-discovery-operations
plan: "08"
type: execute
wave: 7
depends_on: [05-07]
files_modified:
  - src/skillscout/bootstrap.py
  - src/skillscout/cli.py
  - tests/test_discovery_application.py
  - tests/test_discovery_publication_handoff.py
  - tests/test_discovery_security.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, OPS-02, OPS-03]
must_haves:
  truths:
    - "Discovery runtime validates all non-secret authority before resolving credentials or opening state."
    - "Source/state credentials are late-bound only inside discovery factories, which cannot construct Phase 4 or a remote publisher."
    - "The discover CLI ends after Phase 3 and emits only bounded locators/authorities plus the exact durable state commit."
    - "A separate protected publication entry point re-reads that exact commit, re-derives canonical admission, then mints the catalog token and only afterward constructs and invokes PublicationApplication."
  artifacts:
    - {path: src/skillscout/bootstrap.py, provides: "Strict discovery composition and separate protected publication composition", exports: [build_discovery_application, run_protected_discovery_publication]}
    - {path: src/skillscout/cli.py, provides: "Separate closed discover and protected publication commands"}
    - {path: tests/test_discovery_publication_handoff.py, provides: "Exact-commit re-admission and late-token ordering proof"}
  key_links:
    - {from: skillscout discover, to: build_discovery_application, via: "validated locators/config with finally-close"}
    - {from: discovery factories, to: source/state tokens, via: "latest required boundary only"}
    - {from: bounded discovery handoff, to: protected publication, via: "exact state commit reread and canonical admission derivation before catalog token factory"}
  prohibitions:
    - "No free query/budget/branch/admission/token/retry CLI option or environment mapping passed into domain objects."
    - "No catalog App secret/token is resolved by discovery construction."
---

<objective>
Expose discovery and protected publication through separate strict entry points.

Purpose: Make Phase 5 operable while preventing unprotected discovery from ever obtaining catalog publication authority.
Output: `build_discovery_application`, closed discovery CLI, protected publication entry point/CLI and handoff security tests.

Locked decisions: D-01 fixed composition; D-03 no budget override; D-07 credential separation; D-08 allowlisted output.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@src/skillscout/bootstrap.py
@src/skillscout/cli.py
@src/skillscout/application/discovery.py
@src/skillscout/application/pipeline.py
@src/skillscout/application/phase3.py
@src/skillscout/application/publication.py
@.planning/phases/05-automated-discovery-operations/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-08-01: Build strict lazy discovery composition</name>
  <files>src/skillscout/bootstrap.py, tests/test_discovery_application.py, tests/test_discovery_security.py</files>
  <read_first>src/skillscout/bootstrap.py, src/skillscout/application/discovery.py, src/skillscout/application/pipeline.py, src/skillscout/application/phase3.py, src/skillscout/application/publication.py</read_first>
  <behavior>
    - Repository ID/full name, fixed state branch, query-set locator/digest, DB locators, provider/model and policy/profile versions validate before tokens/state.
    - Source/Search/state clients are constructed lazily and close deterministically.
    - Catalog token factory is absent from discovery authority and remains exclusively protected publication construction.
  </behavior>
  <action>RED: add zero-effect malformed-config, credential-access-order and forbidden-publisher-construction tests. GREEN: introduce strict discovery runtime/config carriers and `build_discovery_application`. Resolve reviewed query JSON and all identifiers/digests first; then create lazy factories for existing GitHub read, operations store, three-store barrier and state branch. Keep source/state environment access inside the relevant remote factories and pass only validated non-secret facts to domain/application objects. The discovery application receives only existing Phase 2/3 factories; its dependency carrier has no Phase 4, catalog token, PublicationApplication or remote publisher member. Per D-07 prove both construction and execution cannot access catalog App credentials.</action>
  <acceptance_criteria>
    - Every invalid config case fails before state, token or network factory invocation.
    - Source/state credentials are read at latest boundary and never persisted/logged.
    - Existing publication factory and protected token ordering remain unchanged.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_application.py tests/test_discovery_security.py -k "bootstrap or config or credential or factory" -x</automated></verify>
  <done>Production composition is lazy, exact and split across authority zones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-08-02: Add closed discovery and protected publication entry points</name>
  <files>src/skillscout/bootstrap.py, src/skillscout/cli.py, tests/test_discovery_application.py, tests/test_discovery_publication_handoff.py, tests/test_discovery_security.py</files>
  <read_first>src/skillscout/cli.py, src/skillscout/bootstrap.py, src/skillscout/application/discovery.py, src/skillscout/application/publication.py, tests/test_cli_security.py, tests/test_publication_security.py</read_first>
  <behavior>
    - `discover` accepts only reviewed config/state/source locators and ends after Phase 3 with stable run/root/state-head IDs, bounded eligible locators/authorities, counts and closed outcomes.
    - A distinct protected command accepts only the bounded locator set and exact state commit, checks out/re-reads that commit, verifies all three stores, and re-derives every Phase 4 admission locally.
    - Catalog credentials remain unread and PublicationApplication remains unconstructed until all admissions are derived and checked; token minting immediately precedes construction/invocation.
    - Invalid argv and unexpected exceptions emit fixed sanitized JSON and close every handle.
  </behavior>
  <action>Add the production `discover` subcommand through `SafeArgumentParser` and a dedicated `_run_discover`; it invokes only `build_discovery_application` and emits the closed bounded handoff. Add a separate protected publication command and `run_protected_discovery_publication` composition path. It must fetch and verify the exact state commit named by discovery, rebuild/validate the three stores, resolve each bounded locator against that state, and independently re-derive canonical Phase 4 admission using existing validators. Only after the entire handoff is closed and admissions succeed may it call the catalog token factory; only after successful minting may it construct and sequentially invoke the existing `PublicationApplication`. Tests must cover swapped/stale state SHA, forged/extra locator or authority, admission failure, token failure, and crash/restart recovery, asserting zero token reads and zero publisher constructions before admission. Do not expose query text, budget, mutable state branch, catalog admission digest, token or unsafe retry switches. Close every handle in `finally` and map errors to existing fixed diagnostics per D-08.</action>
  <acceptance_criteria>
    - Discovery happy/resume returns stable bounded JSON and performs zero publication constructions/calls.
    - Protected handoff re-reads the exact state commit and rejects stale, forged, extra or mismatched locators before token access.
    - Exact admission precedes token minting, which precedes PublicationApplication construction/invocation.
    - Hostile argv/config/error canaries never echo and create no state on preflight failure.
    - Parser help contains no policy, credential or publication-authority override.
  </acceptance_criteria>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_discovery_application.py tests/test_discovery_publication_handoff.py tests/test_discovery_security.py tests/test_cli_security.py tests/test_publication_security.py -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked ruff check src/skillscout/bootstrap.py src/skillscout/cli.py</automated></verify>
  <done>Operators use separate discovery and protected publication entry points with an exact, closed, late-authority handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime config/argv → separate factories | Operator input must not bridge discovery into publication authority or leak into output. |
| Credential environment → remote clients | Secrets must be resolved only at the required zone. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-26 | Elevation | CLI/config overrides | high | mitigate | Closed parser/config and pre-token validation. |
| T-05-27 | Information Disclosure | CLI/errors | high | mitigate | Bounded allowlisted payload and fixed diagnostics. |
| T-05-28 | Elevation | catalog credentials | high | mitigate | No catalog token factory in discovery construction. |
| T-05-48 | Elevation | eligible locator handoff | high | mitigate | Exact-commit reread and canonical re-admission before token minting or publisher construction. |
| T-05-SC | Tampering | dependencies | high | mitigate | Existing composition and locked graph only. |
</threat_model>

## Artifacts and symbols produced

Discovery runtime/config carriers, `build_discovery_application`, `_run_discover`, protected publication entry point, separate parser branch and bounded handoff payload.

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Manual/automated safe entrypoint | 05-08 | COVERED |
| REQ | DISC-01..03 | Fixed run and public audit | 05-08 | COVERED |
| REQ | OPS-02 | State configuration | 05-08 | COVERED |
| REQ | OPS-03 | Credentials/output sanitization | 05-08 | COVERED |
| RESEARCH | — | Late-bound factories and closed CLI | 05-08 | COVERED |
| CONTEXT | D-01,D-03,D-07,D-08 | Approved defaults | 05-08 | COVERED |

<verification>
CLI/application/security suites and Ruff; all tests offline.
</verification>

<success_criteria>
The installed CLI exposes separate discovery and protected publication entry points with no authority widening surface.
</success_criteria>

<output>Create `.planning/phases/05-automated-discovery-operations/05-08-SUMMARY.md`.</output>
