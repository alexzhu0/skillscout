---
phase: 06-adversarial-mvp-acceptance
plan: "17"
type: execute
wave: 7
depends_on: [06-16]
files_modified:
  - src/skillscout/domain/acceptance.py
  - src/skillscout/adapters/github.py
  - src/skillscout/adapters/operations_state.py
  - src/skillscout/application/acceptance.py
  - src/skillscout/bootstrap.py
  - src/skillscout/cli.py
  - tests/test_acceptance_domain.py
  - tests/test_operations_state.py
  - tests/test_phase6_acceptance.py
  - tests/test_cli_security.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "D-01/D-02/D-03/D-06: historical V1 authority facts remain parseable and rebuildable, while only a separately versioned V2 authority bound to a V2 benchmark lock and purpose `live_execution` receipt can reach live admission."
    - "D-07..D-10: `run-benchmark` and `run-replay` revalidate the complete V2 lock chain before any DeepSeek, source, or state credential factory or HTTP/provider operation."
    - "D-11..D-15: the closed state-only `record-live-authority` route turns only fixed-host environment-B approval history plus a rebuilt V2 lock into one V2 authority; a V1 fact, actor assertion, approval comment, caller JSON, malformed/missing/stale binding, or any recovery/selection-chain mismatch produces zero provider/source credential effect and no authority persistence."
  artifacts:
    - {path: src/skillscout/domain/acceptance.py, provides: "Strict `LiveAcceptanceAuthorityV2` and purpose-specific approval-receipt schema"}
    - {path: src/skillscout/adapters/operations_state.py, provides: "Schema-version-discriminated V1/V2 authority persistence and rebuild"}
    - {path: src/skillscout/application/acceptance.py, provides: "Closed V2-only live-admission guard and state-only authority-recording transition"}
    - {path: src/skillscout/cli.py, provides: "Closed record/benchmark/replay command ordering that cannot resolve live credentials before admission"}
  key_links:
    - {from: LockedBenchmarkManifestV2, to: LiveAcceptanceAuthorityV2, via: "same canonical state repository/parent/source/workflow/selection/nominations chain plus a distinct `live_execution` receipt"}
    - {from: V2 admission guard, to: credential factories and HTTP providers, via: "all fresh authority and recovery-chain checks complete before construction or invocation"}
    - {from: fixed-host Actions approval history, to: LiveAcceptanceAuthorityV2, via: "Plan 06-17 closed record command validates environment-B run/attempt/reviewer/purpose and persists one rebuilt V2 authority"}
    - {from: LiveAcceptanceAuthorityV2, to: Plan 06-18 protected route, via: "Plan 06-18 supplies the platform gate and invokes the completed closed command"}
---

<objective>
Build the V2 live-authority contract and the credential-before-admission barrier without changing any workflow bytes.

Purpose: Make the live benchmark and replay paths fail closed on every stale, forged, historical, or self-asserted authority form before a credential factory, provider, source, or state effect is possible.
Output: V2 authority model/registry, complete recovery-chain revalidation, and closed application/bootstrap/CLI admission wiring.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md
@.planning/phases/06-adversarial-mvp-acceptance/06-16-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json
@src/skillscout/domain/acceptance.py
@src/skillscout/adapters/github.py
@src/skillscout/adapters/operations_state.py
@src/skillscout/application/acceptance.py
@src/skillscout/bootstrap.py
@src/skillscout/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-17-01: Add the distinct V2 live-authority contract and versioned state registry</name>
  <files>src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, tests/test_acceptance_domain.py, tests/test_operations_state.py</files>
  <behavior>
    - `LiveAcceptanceAuthorityV2` requires a rebuilt `LockedBenchmarkManifestV2` and a distinct redacted receipt whose purpose is exactly `live_execution`; it cannot reuse the `benchmark_lock` receipt.
    - The authority binds the V2 lock digest and its canonical state repository, parent commit/root, source SHA, workflow digest, static V1 selection-manifest digest, fresh nomination digest, selected-entry chain, environment `skillscout-phase6-live-authority`, reviewer identity, run ID/attempt, trigger identity, and approval-record digest.
    - Historical `LiveAcceptanceAuthorityV1` remains parseable/rebuildable under its historical schema version, but no fresh-admission function can select it.
    - V1 substitution, receipt-purpose reuse, actor assertion, comment, caller JSON, missing/malformed/stale field, or a recovery/selection/nominations mismatch fails before persistence or admission.
  </behavior>
  <action>Write RED tests first, then mirror Plan 06-16's explicit schema-version selection in `ACCEPTANCE_FACT_MODELS`. Per D-01, D-02, D-03, and D-06, preserve V1 authority parser/export/rebuild behavior exactly while adding `LiveAcceptanceAuthorityV2` and a purpose-specific redacted approval-receipt model under an explicit V2 schema/version; never use a naked union or SQLite migration. Require the V2 model to carry and validate the full rechecked V2 lock chain: canonical state repository, read-verified parent commit/root, immutable source/workflow bytes containing that parent, static V1 manifest digest, fresh nomination digest, and selected-entry identities. It must bind a separate `purpose=live_execution` receipt from environment B and reject a benchmark-lock receipt, actor field, comment, caller authority JSON, or mismatched recovery chain. Cover cross-version restore, duplicate identity, export/rebuild, stale-binding, malformed receipt, and secret-redaction mutations.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_domain.py tests/test_operations_state.py -k 'live_authority_v2 or approval_receipt or schema_version or historical or recovery or selection_chain or rebuild' -x</automated></verify>
  <done>Only a strict V2 authority can represent fresh live authorization, and historical V1 rows still restore as history without becoming eligible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-17-02: Rebuild and revalidate the complete V2 authority chain before live admission</name>
  <files>src/skillscout/application/acceptance.py, tests/test_phase6_acceptance.py</files>
  <behavior>
    - A single closed live-admission function rebuilds the canonical V2 nomination, lock, and authority facts and rechecks the lock's state repository, parent commit/root, source/workflow binding, V1 selection-manifest digest, fresh nomination digest, selected entries, and distinct approval receipt.
    - The function returns no provider/source/state capability until every V2 fact and all cross-fact bindings are current; it never accepts a V1 model, actor field, comment, or caller-supplied JSON as authority.
    - Any stale/missing/malformed field or changed/missing/duplicate selected entry fails with zero credential factory construction, zero HTTP/provider operation, and zero source/state write operation.
  </behavior>
  <action>Write RED tests first, then add the closed V2-only admission seam in `src/skillscout/application/acceptance.py`, following existing rebuild/state-root and capability-composition patterns. Per D-07..D-10, rebuild the current facts rather than trusting a caller payload and revalidate all Plan 06-16 recovery bindings: canonical state repository and read-verified parent commit/root, immutable source SHA/workflow digest containing that parent, static V1 selection-manifest digest, fresh nomination digest, exact selected-entry numeric ID/full-name/SHA/license/provenance/role equality, and purpose-distinct live-approval receipt. Return only an admission result/capability after this succeeds. Use spies/fakes to prove every invalid authority form stops before any credential-factory construction, HTTP/provider call, source read/write, or state write. Do not modify a workflow, source-execution verifier, validation-map verifier, or protected-environment configuration in this task.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py -k 'live_admission or live_authority_v2 or recovery_chain or selection_chain or zero_credential or zero_http or zero_state_write' -x</automated></verify>
  <done>The application owns one complete V2 re-admission barrier whose failed paths have zero credential, provider, source, and state effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-17-03: Implement the closed state-only authority recorder and wire every live command before credentials</name>
  <files>src/skillscout/adapters/github.py, src/skillscout/application/acceptance.py, src/skillscout/bootstrap.py, src/skillscout/cli.py, tests/test_phase6_acceptance.py, tests/test_cli_security.py</files>
  <behavior>
    - The closed `record-live-authority` command accepts no actor, approval comment, authority JSON, arbitrary endpoint, or free-form receipt. It restores the current V2 lock and canonical parent once, reads only the fixed GitHub Actions approvals endpoint with a late-bound Actions-read token, strictly selects the matching environment-B approved record for `alexzhu0` and the current run/attempt/source/workflow/trigger identities, constructs a `purpose=live_execution` V2 receipt, and performs exactly one forward state-only V2-authority CAS plus rebuild verification.
    - The exact `run-benchmark` and `run-replay` paths invoke the closed V2 admission guard before resolving DeepSeek, source, or state credentials and before constructing any provider/client.
    - Parsing accepts no actor, comment, authority JSON, arbitrary approval host, or free-form receipt input; only fixed canonical state/run identities reach the recorder and admission guard.
    - Tests prove V1 authority, actor assertion, approval comment, caller JSON, missing/malformed/stale receipt, wrong host/environment/reviewer/run/attempt, and recovery/selection mismatch construct no provider or source credential factory, invoke no provider/source HTTP call or state write, and persist no authority fact.
  </behavior>
  <action>Write RED tests first, then use the fixed-host GitHub adapter, closed-parser, and late-bound bootstrap patterns to implement the actual state-only `record-live-authority` command before wiring the exact benchmark/replay routes. The recorder receives only closed canonical state/run identity inputs; it internally restores and revalidates the V2 lock/recovery/selection chain, queries `GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals` only at the fixed GitHub API host using an Actions-read token injected by the future Plan 06-18 job, discards raw approval comments, requires the literal environment B approval from `alexzhu0` for the exact run/attempt/source/workflow/trigger, constructs the `live_execution` receipt, persists exactly one V2 authority through the existing state-only durability/CAS path, and rebuilds it before reporting success. It must not construct a DeepSeek client, source client, catalog client, or publication dependency; it must never retry, compensate, or write after post-CAS uncertainty. Then invoke the Plan 06-17 admission seam on `run-benchmark` and `run-replay` before any credential resolution or provider/client construction. Per D-07..D-10 and D-11..D-15, expose no alternative command flag, environment override, actor assertion, comment, caller JSON, endpoint, or receipt channel. Keep the valid recorder compatible with the protected workflow that Plan 06-18 will add, but do not edit that workflow or call a live remote API in this task. Add unit/integration fakes for approval transport, state CAS, credential factories, and HTTP/provider/source/state writers; prove every rejected input causes zero forbidden constructions/calls/writes and no authority fact.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_cli_security.py -k 'record_live_authority or run_benchmark or run_replay or live_admission or credential_order or approval_history or authority_json or actor or comment or zero_effect' -x</automated></verify>
  <done>The completed state-only recorder can persist only one independently rebuilt V2 live authority from fixed-host environment-B evidence, and every benchmark/replay entry point validates it before it can resolve a credential, construct a provider, call HTTP, or write source/state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| V2 lock/receipt → live admission | Historical, self-asserted, stale, or cross-purpose evidence must not cross into live capabilities. |
| CLI/bootstrap → credential factory | Command parsing and dependency wiring must not construct a credential-bearing client before re-admission. |
| rebuilt state → provider/source/state effects | Only canonical, recovery-safe facts may grant the next capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-52 | Spoofing | fresh live authority | critical | mitigate | Version-discriminated V2 fact, purpose-distinct receipt, and no actor/comment/caller authority. |
| T-06-53 | Tampering | recovery and selection chain | critical | mitigate | Rebuild and revalidate state repository, parent/root, source/workflow, nomination, selection, and receipt bindings. |
| T-06-54 | Elevation of Privilege | credential/provider construction | critical | mitigate | Admission runs before every credential factory and provider/client path; zero-effect mutation tests. |
| T-06-55 | Repudiation | V2 authority evidence | high | mitigate | Redacted canonical receipt and export/rebuild identity, with no secret or comment retention. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package-manager changes or new dependencies. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Human-controlled, repeatable real-repository benchmark | 06-17 | COVERED |
| REQ | TEST-01 | Fresh fixed-SHA benchmark authority | 06-17 | COVERED |
| REQ | TEST-02 | Fail-closed authorization and adversarial evidence | 06-17 | COVERED |
| REQ | TEST-03 | Replay and credential-bound live execution integrity | 06-17 | COVERED |
| RESEARCH | — | Typed state, late credentials, no tool/provider authority before validation | 06-17 | COVERED |
| CONTEXT | D-01..D-15 | Reproducible benchmark, provider boundary, hard gates | 06-17 | COVERED |

<verification>
Run the contract/state, application, and CLI mutation suites. Each rejected authority must prove zero credential, HTTP/provider, source, and state-write effects without changing workflow bytes.
</verification>

<success_criteria>
The only live-admissible path rebuilds and validates V2 authority evidence before any credential or provider boundary, while V1 and all self-asserted forms remain historical or rejected.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-17-SUMMARY.md` with non-secret V2 state identities and zero-effect mutation results only.</output>
