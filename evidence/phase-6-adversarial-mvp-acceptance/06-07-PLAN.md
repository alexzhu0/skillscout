---
phase: 06-adversarial-mvp-acceptance
plan: "07"
type: execute
wave: 5
depends_on: [06-04, 06-06]
files_modified:
  - .planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json
  - tests/test_phase6_acceptance.py
  - tests/test_phase6_workflow.py
autonomous: false
requirements: [TEST-01, TEST-02]
user_setup:
  - service: github
    why: "Read-only Search nomination and canonical state persistence"
    env_vars:
      - {name: SKILLSCOUT_SOURCE_GITHUB_TOKEN, source: "Protected workflow runtime; value must not be inspected"}
      - {name: SKILLSCOUT_STATE_GITHUB_TOKEN, source: "Protected workflow runtime; value must not be inspected"}
must_haves:
  truths:
    - "Plan 06-07 remains blocked until canonical `acceptance_hosted_isolation_capability` and `acceptance_offline_adversarial_run` facts validate as the exact Plan 06-04 models and rebuild from state."
    - "D-01 real Search and deterministic filtering produce the formal nomination set before human selection."
    - "D-02/D-03 the locked five entries bind exact IDs/SHAs/licenses/provenance and the required 2-positive/2-negative/1-borderline mix."
    - "D-05 supplemental nominations remain visibly `user_nominated` and cannot silently replace formal entries."
    - "D-06 any promotion/replacement increments manifest version and invalidates/reruns affected evidence."
  artifacts:
    - {path: .planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json, provides: "Human-locked reproducible five-repository benchmark"}
  key_links:
    - {from: Plan 06-06 rebuilt offline snapshot, to: nomination credential authorization, via: "independent offline-only verification of the exact hosted/offline fact kinds and model bindings"}
    - {from: real Search nomination facts, to: benchmark manifest, via: "human lock attestation over exact nomination-set digest"}
---

<objective>
Run real Search nomination, then obtain the accountable human lock for the exact five-repository benchmark.

Purpose: Establish TEST-01 realism without letting the planner, model, or workflow silently select or replace benchmark repositories.
Output: Canonical nomination evidence and a human-locked, content-addressed benchmark manifest.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@config/discovery-queries-v1.json
@src/skillscout/application/discovery.py
@src/skillscout/application/acceptance.py
@.github/workflows/phase6-acceptance.yml
@.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 06-07-01: Authorize exact Search nomination credential use</name>
  <read_first>.github/workflows/phase6-acceptance.yml, config/discovery-queries-v1.json, src/skillscout/application/acceptance.py, AGENTS.md</read_first>
  <action>Before pausing or presenting GitHub authority, run the independent offline-only verifier against the exact canonical state commit/root produced by Plan 06-06. Require one `acceptance_hosted_isolation_capability` fact that validates as `HostedIsolationCapabilityV1`, one `acceptance_offline_adversarial_run` fact that validates as `OfflineAdversarialRunV1`, exact capability/workflow/source/run-attempt/mechanism equality, passing per-kind projection/export/rebuild evidence, and no conflicting duplicate. If any condition fails, do not ask for nomination credentials. Otherwise pause before any GitHub credential resolution or workflow dispatch. Present the exact workflow/source SHA, fixed query-set digest, state repository identity, 100-candidate ceiling, `nominate` job permissions, and fact schema. Request authorization for one read-only Search nomination run plus canonical state-branch persistence. Do not request, display, test, or copy token values. This approval grants no DeepSeek call, benchmark lock, publication, Gate B4, cleanup, or catalog authority.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_acceptance.py --offline-only &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py tests/test_phase6_acceptance.py -k 'nominate or search_credential or no_semantic or no_publication' -x</automated></verify>
  <acceptance_criteria>
    - Human approval names exact source/workflow/query identities and bounded effects.
    - Nomination job has no DeepSeek or catalog/App credential.
    - No live run occurs before the approval record.
    - Missing/mismatched/non-rebuildable hosted or offline facts prevent the authorization checkpoint from opening.
  </acceptance_criteria>
  <done>One exact read-only nomination run is authorized without broader live authority.</done>
</task>

<task type="auto">
  <name>Task 06-07-02: Run Search-derived nomination and persist canonical facts</name>
  <files>tests/test_phase6_acceptance.py, tests/test_phase6_workflow.py</files>
  <read_first>.github/workflows/phase6-acceptance.yml, src/skillscout/cli.py, src/skillscout/application/acceptance.py, src/skillscout/application/discovery.py, tests/test_phase6_acceptance.py</read_first>
  <action>After Task 06-07-01 approval, dispatch only `phase6_action=nominate` at the exact approved SHA. Run `skillscout nominate-benchmark` through the production Search query/filter path, pin repository IDs and commit SHAs, retain confirmed permissive license and selection evidence, and persist a self-hashed `NominationSetV1` through operations state with exact kind `acceptance_nomination`. Record supplemental human nominations in a separate lane as `user_nominated` after the same fixed-SHA/license/read-budget checks; do not promote them. Capture only bounded public metadata, request/rate facts, and state commit/root identities; do not persist repository prose.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase6_workflow.py -k 'nomination or search_derived or user_nominated or state_fact' -x</automated></verify>
  <acceptance_criteria>
    - Every formal candidate has real Search page/candidate/filter evidence and immutable repo ID/SHA/license facts.
    - Candidate and semantic budgets remain at 100/20 and no semantic or publication call occurs.
    - State restore and independent digest checks reproduce the nomination set exactly.
  </acceptance_criteria>
  <done>A reproducible real-Search nomination set exists for human selection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-07-03: Lock the exact five-repository benchmark</name>
  <files>.planning/phases/06-adversarial-mvp-acceptance/06-BENCHMARK-MANIFEST.json</files>
  <read_first>.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md, src/skillscout/domain/acceptance.py, src/skillscout/adapters/operations_state.py, tests/test_acceptance_domain.py, tests/test_operations_state.py</read_first>
  <action>Present the sanitized nomination manifest and ask a human to select exactly five entries satisfying D-03: two plausible positives including one multi-workflow repository, two expected negatives, and one borderline. Require review of full name, numeric ID, fixed SHA, permissive license, Search selection evidence, intended evaluator role, and reproducibility locator. After approval, serialize `06-BENCHMARK-MANIFEST.json` with manifest version, nomination-set digest, entry digests, lock reviewer/timestamp, and manifest self-digest. Persist the same strict `LockedBenchmarkManifestV1` with its nested `BenchmarkLockAttestationV1` through `OperationsStateStore.record_acceptance_fact` using exact kind `acceptance_benchmark_lock`, push the redacted bundle, and rebuild/compare its typed projection before any Plan 06-08 authority. Expected labels remain hypotheses and must not be copied into semantic inputs. Do not fabricate repository choices or approval tokens.</action>
  <verify>
    <automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_acceptance_domain.py tests/test_phase6_acceptance.py -k 'locked_manifest or distribution or search_provenance' -x</automated>
    <human-check>Confirm each of the five exact public repository identities/SHAs/licenses and the required role distribution, then approve the displayed manifest digest.</human-check>
  </verify>
  <acceptance_criteria>
    - Five entries and distribution are exact; every entry traces to the approved nomination set.
    - Lock approval predates any live semantic call.
    - Any later replacement/promotion requires a version increment and explicit affected-evidence rerun.
    - The checked-in manifest and rebuilt canonical `acceptance_benchmark_lock` fact are byte/field equivalent and bind the same nomination-set digest.
  </acceptance_criteria>
  <done>The formal benchmark is immutable, reproducible, and human locked.</done>
</task>

</tasks>

<threat_model>
## Assets
- Search provenance, fixed repository identities/SHAs/licenses, nomination state, and benchmark lock.

## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub Search → nomination facts | Provider metadata is untrusted and mutable until strict projection/pinning. |
| Nomination set → human lock | Silent selection/replacement would make evidence circular. |

## STRIDE Threat Register — ASVS L1; high severity blocks execution
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-06-19 | Spoofing | benchmark identity | high | mitigate | Numeric ID, fixed SHA, license, nomination digest. |
| T-06-20 | Tampering | human lock/revision | high | mitigate | Self-digest and explicit versioned approval. |
| T-06-21 | Information Disclosure | Search credential | high | mitigate | Protected late injection; no value inspection/persistence. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package changes. |
</threat_model>

## Artifacts this phase produces
- `06-BENCHMARK-MANIFEST.json`
- `NominationSetV1` live evidence and human `BenchmarkLockAttestationV1`
- Workflow action `nominate`
- Manifest fields for full name, numeric ID, SHA, SPDX license, selection source, evaluator role, nomination evidence, version, lock identity, and digest

## Multi-Source Coverage Audit
| Source | ID | Item | Status |
|---|---|---|---|
| GOAL | — | Five reproducible real repositories | COVERED |
| REQ | TEST-01, TEST-02 | Real benchmark and mixed outcomes | COVERED |
| RESEARCH | — | Search nomination → human lock | COVERED |
| CONTEXT | D-01..D-06 | Formal/supplemental benchmark and revisions | COVERED |

<verification>
The locked manifest strictly validates against the exact Search nomination state and contains an accountable human lock.
</verification>

<success_criteria>
No live semantic campaign can start from an unreviewed, moving, silently substituted, or non-Search formal benchmark.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-07-SUMMARY.md` with exact nomination/manifest digests but no credentials.</output>
