---
phase: 06-adversarial-mvp-acceptance
plan: "18"
type: execute
wave: 8
depends_on: [06-17]
files_modified:
  - tools/verify_phase6_source_execution.py
  - tests/test_phase6_source_execution.py
  - tools/verify_phase6_validation_map.py
  - tests/test_phase6_validation_map.py
  - .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md
  - .github/workflows/phase6-acceptance.yml
  - tests/test_phase6_workflow.py
autonomous: false
requirements: [TEST-01, TEST-02, TEST-04]
user_setup:
  - service: github
    why: "A separate platform-enforced approval must record the fresh live-authority receipt after Plan 06-17 has installed its no-credential admission barrier."
    dashboard_config:
      - task: "Create environment `skillscout-phase6-live-authority`; require reviewer `alexzhu0`; leave Prevent self-review disabled because the sole required reviewer may approve a run they initiated; enable no custom deployment protection rule or signature/key scheme."
        location: "Repository Settings → Environments → skillscout-phase6-live-authority"
must_haves:
  truths:
    - "D-01/D-06: environment B, `skillscout-phase6-live-authority`, produces only a distinct `live_execution` approval receipt and V2 state authority; it never reuses environment A or a `benchmark_lock` receipt."
    - "D-11..D-15: the final protected route has no actor allowlist or free `live_authority_json`, reads fixed-host Actions approval history with Actions-read authority, invokes only Plan 06-17's closed command, and has no semantic, candidate, catalog, PR, review, or publication authority."
    - "D-22/D-23: Task 06-18-02 is the sole final pre-Gate-B4 workflow-byte mutation; all later source verification, preflight, hashes, and Gate B4 evidence bind only its post-task bytes."
    - "The validation-map contract proves all 18 plans and 47 primary tasks, dependency existence, acyclicity, lower predecessor waves, exact derived waves, and only 06-18-03 as the live-admission human/platform gateway."
  artifacts:
    - {path: .github/workflows/phase6-acceptance.yml, provides: "Final protected `record-live-authority` route"}
    - {path: tools/verify_phase6_source_execution.py, provides: "Closed all-route source-execution grammar including protected benchmark-lock and live-authority routes"}
    - {path: tools/verify_phase6_validation_map.py, provides: "18-plan/47-task dependency-topology enforcement"}
  key_links:
    - {from: GitHub protected environment B approval, to: Plan 06-17 closed record-live-authority command, via: "fixed-host Actions review history, literal environment/purpose, and redacted V2 receipt"}
    - {from: Task 06-18-02 workflow bytes, to: GateB4BindingV1, via: "final source verifier, preflight, and exact SHA-256"}
    - {from: 06-18-03 checkpoint, to: 06-08-01, via: "the only current platform/human gateway for automatic V2 re-admission"}
---

<objective>
Integrate the final protected live-authority route and independently verify the completed Phase 6 dependency/freeze contract.

Purpose: Let GitHub provide a separate, auditable human approval receipt only after Plan 06-17 has made live credentials unreachable without a complete rebuilt V2 chain.
Output: Final workflow bytes, source-execution proof, 18-plan/47-task topology verifier, and the environment-B approval checkpoint.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-adversarial-mvp-acceptance/06-CONTEXT.md
@.planning/phases/06-adversarial-mvp-acceptance/06-16-SUMMARY.md
@.planning/phases/06-adversarial-mvp-acceptance/06-17-SUMMARY.md
@.github/workflows/phase6-acceptance.yml
@tools/verify_phase6_source_execution.py
@tools/verify_phase6_validation_map.py
@tests/test_phase6_source_execution.py
@tests/test_phase6_workflow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-18-01: Extend independent source and topology verifiers before the final workflow mutation</name>
  <files>tools/verify_phase6_source_execution.py, tests/test_phase6_source_execution.py, tools/verify_phase6_validation_map.py, tests/test_phase6_validation_map.py, .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md</files>
  <behavior>
    - The source verifier's isolated fixtures recognize the Plan 06-16 benchmark lock and future Plan 06-18 live-authority route, require full-SHA checkout/pinned local toolchain/direct checkout-local locked invocation, and reject alternate commands, permission widening, actor gates, free authority JSON, wrong environment, or approval-reader drift.
    - The validation-map verifier requires 18 plans and 47 primary tasks; every declared dependency exists, the graph is acyclic, each predecessor wave is lower, and every wave equals zero for a root or one plus its highest predecessor wave.
    - Mutation tests cover a missing dependency, cyclic edge, same-wave predecessor, future-wave predecessor, and incorrectly derived wave; they also enforce that 06-07-03 and 06-16-03 are historical/benchmark-only and only 06-18-03 opens 06-08-01.
  </behavior>
  <action>Write RED isolated-tree and fixture tests first, then extend the standard-library-only, read-only source verifier and validation-map verifier without modifying `.github/workflows/phase6-acceptance.yml` in this task. Per D-01, D-06, D-11..D-15, and D-22/D-23, make the source grammar recognize the literal environment A benchmark-lock path and the planned literal environment B live-authority path, but keep the verifier free of provider, candidate, catalog, pull-request, review, and publication authority. Update `06-VALIDATION.md` to 18 plans/47 primary tasks, V1 historical versus V2 fresh registry ownership, recovery/selection-chain consumers, new source surfaces, dependency-topology mutation coverage, and `CHECKPOINTS` so only 06-18-03 precedes 06-08-01. The topology code must fail on missing/cyclic/same/future/wrong-wave mutations. Do not touch workflow bytes, credentials, state, external services, or any product application source in this task.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_source_execution.py tests/test_phase6_validation_map.py -k 'fixture or protected_authority or dependency_topology or missing_dependency or cycle or wave' -x</automated></verify>
  <done>Independent verifiers and their isolated tests can reject every planned authority-route and topology mutation before the final workflow bytes are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-18-02: Apply the sole final protected live-authority workflow mutation</name>
  <files>.github/workflows/phase6-acceptance.yml, tests/test_phase6_workflow.py</files>
  <behavior>
    - The final `record-live-authority` job uses literal environment `skillscout-phase6-live-authority`, fixed-host Actions review history with `GITHUB_TOKEN` Actions-read authority, and invokes only the Plan 06-17 closed command.
    - The job has no actor allowlist, free `live_authority_json`, semantic/provider/candidate/catalog/PR/review/publication capability, or approval-writing authority; it waits for environment B before any state credential is resolved.
    - The job records a V2 `live_execution` receipt bound to the V2 lock and never accepts a V1 fact, environment-A receipt, actor assertion, comment, or caller JSON as evidence.
  </behavior>
  <action>Write RED workflow tests first, then make the single final workflow-byte change in `.github/workflows/phase6-acceptance.yml`. Per D-01, D-06, D-11..D-15, D-22, and D-23, replace the existing actor allowlist and arbitrary `live_authority_json` transport with a dedicated state-only `record-live-authority` job gated by literal environment B. Give `GITHUB_TOKEN` only the fixed approval-history read capability required by the Plan 06-17 closed command; require full-SHA checkout, pinned repository-local toolchain, direct checkout-local locked invocation, fixed run/attempt metadata, and a current rebuilt V2 lock. The route may persist the V2 live authority but must not resolve DeepSeek/source credentials, construct a provider, execute candidate code, access a catalog, or create a pull request, review, publication, merge, cleanup, or Gate B4 effect. This is the only final pre-Gate-B4 workflow mutation: after it, do not change any of the four bound workflow files in later plans; all later preflight, source verification, hashes, and Gate B4 evidence must bind its exact bytes.</action>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_workflow.py tests/test_phase6_source_execution.py -k 'record_live_authority or protected_environment or approval_history or no_actor or no_authority_json or locked_source_execution' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_source_execution.py &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py --plan-contract</automated></verify>
  <done>The exact final workflow bytes contain one bounded state-only V2 authority route and no legacy or broader authority surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 06-18-03: Configure and record the separately approved V2 live authority</name>
  <files>.github/workflows/phase6-acceptance.yml, src/skillscout/adapters/operations_state.py, .planning/phases/06-adversarial-mvp-acceptance/06-VALIDATION.md</files>
  <action>Per D-01, D-06, D-11..D-15, D-22, and D-23, configure environment B, dispatch only the completed protected route at the immutable source containing the V2 lock and Plan 06-17 admission barrier, and approve the waiting job through that environment. Confirm the V2 authority rebuilds from a distinct `live_execution` receipt and the complete recovered lock chain. Do not edit any workflow after Task 06-18-02, retry a failed run, resolve live credentials, invoke a provider, execute candidate code, or create a catalog, pull-request, review, publication, merge, cleanup, or Gate B4 effect.</action>
  <what-built>Plan 06-17's no-credential V2 admission barrier plus Plan 06-18's final protected state-only workflow route. The route can persist only a V2 live authority; it has no live benchmark or publication capability.</what-built>
  <how-to-verify>
    1. Configure `skillscout-phase6-live-authority` with required reviewer `alexzhu0`, leave Prevent self-review disabled for the sole reviewer, and enable no custom deployment protection rule or signature/key scheme.
    2. Trigger only the final `record-live-authority` job at the immutable source containing the current V2 benchmark lock. Confirm it waits for environment B before any state credential is resolved.
    3. Approve the waiting job through GitHub as `alexzhu0`; do not use a comment, actor assertion, or environment-A receipt as evidence.
    4. Confirm the canonical V2 authority binds `purpose=live_execution`, environment B, reviewer identity, run ID/attempt, source/workflow digests, V2 lock digest, approval-record digest, trigger identity, and the lock's revalidated state repository/parent/selection/nominations chain; then independently rebuild the state projection.
    5. Confirm no DeepSeek/source credential resolution, provider request, candidate execution, catalog, pull request, review, publication, merge, cleanup, or Gate B4 effect occurred. This completed checkpoint alone opens automatic Task 06-08-01 admission.
  </how-to-verify>
  <verify><automated>.tools/uv-0.11.29/bin/uv run --locked pytest -q tests/test_phase6_acceptance.py tests/test_phase6_workflow.py tests/test_phase6_source_execution.py tests/test_phase6_validation_map.py -k 'live_authority_v2 or protected_environment or approval_receipt or no_semantic or no_publication or checkpoint_topology' -x &amp;&amp; .tools/uv-0.11.29/bin/uv run --locked python tools/verify_phase6_validation_map.py --plan-contract</automated></verify>
  <resume-signal>Report only non-secret V2 authority, V2 lock, run/attempt, source/workflow, approval-record, and state digest identifiers, or describe the failed check.</resume-signal>
  <done>One independently environment-B-approved V2 live authority is persisted and rebuilt; every historical, benchmark-only, and non-state capability remains excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment B approval → Plan 06-17 command | GitHub platform evidence must be purpose-distinct, fixed-host, and incapable of broadening command authority. |
| final workflow bytes → Gate B4 | Any later workflow mutation invalidates the exact bytes that future authority must bind. |
| plan metadata → execution ordering | Invalid topology must fail before a checkpoint can be treated as a live-admission gateway. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-56 | Spoofing | environment-B receipt | critical | mitigate | Literal environment/purpose, fixed-host review history, V2 lock binding, and actor/comment/caller rejection. |
| T-06-57 | Elevation of Privilege | protected workflow job | critical | mitigate | State-only closed command, Actions-read approval evidence, no provider/candidate/catalog/publication dependencies, and verifier mutations. |
| T-06-58 | Tampering | final workflow freeze | critical | mitigate | Task 06-18-02 is the sole final mutation; later plans hash/read/verify only. |
| T-06-59 | Tampering | dependency topology | high | mitigate | Exact dependency/existence/acyclic/lower-wave/derived-wave verification with isolated mutations. |
| T-06-SC | Tampering | dependencies | high | mitigate | No package-manager changes or new dependencies. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Auditable human control before live benchmark and publication | 06-18 | COVERED |
| REQ | TEST-01 | Fresh protected authority for fixed-SHA benchmark | 06-18 | COVERED |
| REQ | TEST-02 | Fail-closed adversarial and workflow controls | 06-18 | COVERED |
| REQ | TEST-04 | Least-privilege final workflow and Gate B4 freeze | 06-18 | COVERED |
| RESEARCH | — | Fixed GitHub REST boundary, exact byte binding, static validation | 06-18 | COVERED |
| CONTEXT | D-01..D-06,D-11..D-15,D-22,D-23 | Human controls, no trust expansion, exact invalidation | 06-18 | COVERED |

<verification>
Run isolated source/topology mutations before the final workflow edit, then run the full source verifier and plan contract against the final bytes. The environment-B checkpoint records only the V2 authority.
</verification>

<success_criteria>
The sole final workflow mutation can create only a separately approved V2 live authority, and later Gate B4 work is forced to use its exact bytes while topology tests permit no alternate human gateway.
</success_criteria>

<output>Create `.planning/phases/06-adversarial-mvp-acceptance/06-18-SUMMARY.md` with non-secret final workflow and V2 authority identifiers only.</output>
